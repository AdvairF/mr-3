---
phase: 7.10.bug2.sub1
plan: 01
title: "Helper INPC custas avulsas + branches Valor Original + Saldo Atualizado UI"
status: SHIPPED
shipped_date: 2026-04-30
tag: v1.4-phase7.10.bug2.sub1
tag_target: 61dbabe (pai chore bump expansion)
---

# Phase 7.10.bug2.sub1 — SUMMARY

## Status
SHIPPED 2026-04-30 — Tag `v1.4-phase7.10.bug2.sub1` em `61dbabe` (pai chore bump expansion).

## Bug original
Motor `devedorCalc.js:172-176` filtra `_so_custas:true` antes do cálculo de saldos:
```js
const dividasCalc = parseDividas(devedor?.dividas).filter(
  (d) => !d._nominal && !d._so_custas
);
```
Resultado: `saldosMap[id] === undefined` para dívidas-fantasma de custas avulsas (criadas via D-23 da Phase 7.9, `valor_total=0`). UI consumers (TabelaDividas + DetalheDivida) renderizavam "Calculando..." indefinido na coluna **Saldo Atualizado** e R$ 0,00 na coluna **Valor Original**.

UAT prod 2026-04-30 (TASK 0 operador): ~50% linhas módulo Dívidas afetadas.

## Solução — Caminho A (helper externo)

### Plan original (commit `9c3da4d`)
Helper externo `src/utils/custasAvulsas.js` espelhando loop motor `devedorCalc.js:479-491`. UI consumers ganham branch `_so_custas` antes do fallback null. Motor (D-01) INTOCADO trivialmente.

Helper signature inicial: `(custas, hoje) → number` (apenas atualizado).

### Expansion mid-UAT (commit `21fd3b4`)
Operador requisitou durante UAT também exibir **Valor Original** das custas avulsas (soma raw `c.valor` sem fator INPC) — não só **Saldo Atualizado**.

Pattern `ux_consistency_principle_drives_scope_expansion` (precedente Phase 7.14b): princípio "operador precisa do valor pra decidir cobrança" inclui ver original + atualizado lado a lado.

Refactor Opção A (helper retorna object):
```js
// Antes
export function calcularValorAtualizadoCustasAvulsas(custas, hoje) {
  // ... return total (number)
}

// Depois — espelha shape motor result.custas (devedorCalc.js:513)
export function calcularValorAtualizadoCustasAvulsas(custas, hoje) {
  // ... return { original, atualizado }
}
```

Motor já calculava ambos — refactor fortalece parity arquitetural.

## Escopo entregue

### Arquivos NOVOS
- `src/utils/custasAvulsas.js` (~21 linhas) — helper signature `(custas, hoje) → { original, atualizado }`
- `src/utils/__tests__/custasAvulsas.test.js` (~88 linhas, 8 testes)

### Arquivos MOD
- `src/components/TabelaDividas.jsx` — branch `_so_custas` em valorOriginalExibido + saldoExibido
- `src/components/DetalheDivida.jsx` — branch `_so_custas` em valorOriginalExibido + saldoAtual (curto-circuita PagamentosDivida pra fantasma)

### Tests cobertos (8/8 PASS)
1. Custa única SEM data: `original === atualizado === valor puro`
2. Custa única COM data antiga: `atualizado === vc * fator INPC > original`
3. Múltiplas mix com/sem data: soma cumulativa em ambos
4. Valor inválido (`""`, `null`, `undefined`, `"abc"`): ignora via `parseFloat||0`
5. `c.pago=true`: contribui normal em ambos (D-pre-5 — NÃO filtra)
6. Array vazio/null/undefined/não-Array: retorna `{ original: 0, atualizado: 0 }`
7. **Parity D-pre-7 dual**: `helper.original === motor.custas.original` E `helper.atualizado === motor.custas.atualizado` (±1e-9)
8. `.original` retorna soma raw sem fator INPC mesmo com data antiga

## Commits

### Submódulo (2 commits cumulative origin/main)
- `9c3da4d` feat(07.10.bug2.sub1): helper INPC custas avulsas + branches TabelaDividas + DetalheDivida
- `21fd3b4` feat(07.10.bug2.sub1.expand): helper retorna { original, atualizado } + branches Valor Original UI

### Pai (3 commits cumulative origin/master + tag)
- `f09299c` docs(07.10.bug2.sub1): plan 01 — Helper INPC custas avulsas + branches UI
- `cbd6047` chore(07.10.bug2.sub1): bump submódulo mr-3 → 9c3da4d
- `61dbabe` chore(07.10.bug2.sub1.expand): bump submódulo mr-3 → 21fd3b4 — **TAG TARGET**

CONTEXT.md commit `ee1953f` (sessão anterior) preservado.

## UAT prod (mrcobrancas.com.br)

### Plan original UAT (parcial — descobriu expansion necessária)
| SC | Cenário | Resultado |
|---|---|---|
| **SC1** | Módulo Dívidas: linhas `_so_custas` em "Saldo Atualizado" exibem valor (não "Calculando...") | ✅ PASS |
| (mid-UAT) | Operador apontou: Valor Original ainda mostra R$ 0,00 — precisa exibir soma raw c.valor | → expansion mid-UAT |

### Re-UAT pós-expansion (5+1 SCs PASS REAL)
| SC | Cenário | Resultado |
|---|---|---|
| **SC1** | Módulo Dívidas: linhas `_so_custas` em "Saldo Atualizado" | ✅ PASS |
| **SC1.5** | Módulo Dívidas: linhas `_so_custas` em "Valor Original" mostram soma raw c.valor (não R$ 0,00) | ✅ PASS |
| **SC2** | Clicar Ver Detalhe → DetalheDivida exibe mesmos valores (Original + Atualizado consistentes) | ✅ PASS |
| **SC3** | Console F12 limpo (zero TypeError/ReferenceError novos) | ✅ PASS |
| **SC4** | Dívidas regulares (não-`_so_custas`) preservam comportamento bit-a-bit | ✅ PASS |
| **SC5** | Cross-check parity X1 (TabelaDividas) ≡ X2 (DetalheDivida) ≡ X3 (motor `result.custas.{original,atualizado}`) | ✅ PASS |

**5+1/5+1 SCs PASS REAL em prod.**

## D-01 motor INTOCADO cumulative

| File | Gate 4a (working tree) | Gate 4b (post-commit) |
|---|---|---|
| `src/utils/devedorCalc.js` | 0 hunks | 0 matches |
| `src/services/pagamentos.js` | 0 hunks | 0 matches |
| `src/services/dividas.js` | 0 hunks | 0 matches |
| `src/utils/masks.js` | 0 hunks | 0 matches |

Confirmed em ambos commits submódulo (`9c3da4d` + `21fd3b4`). Helper externo + 2 branches UI cirúrgicas — motor permanece source of truth.

## D-pre lockeds (7) validation

| ID | Locked | Validado por |
|---|---|---|
| D-pre-1 | Helper signature `(custas, hoje) → number` | **AJUSTADO mid-UAT** para `→ { original, atualizado }` (parity arquitetural mais forte) |
| D-pre-2 | Branch TabelaDividas L94-96 | grep + UAT SC1+SC1.5+SC4 |
| D-pre-3 | Branch DetalheDivida L79-86 (curto-circuita PagamentosDivida) | grep + UAT SC2+SC4 |
| D-pre-4 | D-01 motor INTOCADO | dual gate 4a+4b PASS em ambos commits |
| D-pre-5 | NÃO filtra c.pago | Test 5 explícito (helper) + UAT cumulative |
| D-pre-6 | c.data legacy shape | helper código + tests |
| D-pre-7 | Parity test helper === motor (±1e-9) | Test 7 dual (`.original` E `.atualizado`) PASS |

## Reconciliações

| Item | Status | Justificativa |
|---|---|---|
| `npm run lint` script | SKIP | Não configurado no projeto (cumulative since Phase 7.13b) |
| `npm test` (full) `filaDevedor.test.js` empty stub | SKIP | Pré-existente Phase 7.13b |
| `npm run test:regressao` 34/34 motor regression | ✅ GATE EFETIVO | Auto-roda via `prebuild` hook |
| `npm run build` exit 0 | ✅ GATE COMPOSTO | Build = test:regressao + vite build |
| Helper test suite 8/8 | ✅ GATE NOVO | `npm run test -- src/utils/__tests__/custasAvulsas.test.js` |

## Pré-flight orchestrator (lições cumulative aplicadas)

### Plan checker YELLOW (1 BLOCKER + 3 INFO)
- **BLOCKER #1** (CRITICAL): typo função fantasma `calcularDetalhesAtualizados` (não existe no motor) → corrigido para `calcularDetalheEncargos` em 4 linhas do PLAN.md (L217 + L248 + L249 + L598). Acesso ajustado de `totalCustasAtualizado` direto para `result.custas.atualizado`.
  - **Factchecked via grep direto antes dos EDITs** (lição `feedback_memory_must_factcheck_codebase_before_claiming_severity`).
- Issues #2/#3/#4 INFO: accept conscientemente (cosmético, custo > benefício).

### EDITs cosméticos pré-checker (2)
- L519-528: bash bloco morto com bug `&& exit 1` removido + título limpo.
- L545: `verify><automated>` `grep -vE` (sempre exit 0) → `! grep -qE` (semântica correta).

## Memory feedbacks aplicados (existentes)

7 memories documentadas em PLAN.md:
1. `feedback_so_custas_ui_calculando_indefinido` (origem do bug)
2. `feedback_custas_semantica_quitacao_dual` (D-pre-5 c.pago)
3. `feedback_post_hoc_d_pre_must_be_validated_in_child_phase` (TASK 0 metodologia)
4. `feedback_discuss_walkthrough_fluxo_real` (caso de uso real)
5. `feedback_memory_must_factcheck_codebase_before_claiming_severity` (catch pré-commit + checker BLOCKER #1 fix)
6. `feedback_gate_motor_grep_use_name_only` (Task 4 dual gate)
7. `feedback_grep_state_pattern_paralelo_em_componentes_relacionados` (state_pattern_audit profilático listando 7 consumers `_so_custas`)

## Memory feedbacks NOVOS candidatos

### 1. `ux_consistency_principle_drives_scope_expansion` (REFORÇO — segunda aplicação)
> Princípio arquitetural declarado pelo operador é gatilho válido pra expandir escopo da phase durante UAT, não regressão. Coerência arquitetural > rigor cerimonial.

Originalmente candidato em Phase 7.14b SUMMARY. **Phase 7.10.bug2.sub1 confirma o pattern com segunda aplicação** — operador apontou em UAT que princípio "operador precisa do valor pra decidir cobrança" inclui Valor Original + Saldo Atualizado lado a lado. Custo trivial (~30 linhas refactor + 1 commit submódulo + 1 chore bump pai). Forte caso para registrar memory.

### 2. `helper_signature_mirror_motor_return_shape` (NOVO)
> Helpers de espelhamento UI devem retornar OBJECT shape igual ao motor (não scalar simplificado), mesmo quando consumer inicial precisa só de 1 campo. Mid-UAT pode revelar necessidade de campos secundários — refactor scalar→object é caro (4 callsites + 8 tests). Espelhamento de shape arquitetural torna parity test mais robusto e evita expansion forçada.

Aplicação prática Phase 7.10.bug2.sub1: helper inicial retornou `number` (`atualizado` apenas) → expansion mid-UAT precisou `{ original, atualizado }` → refactor 4 arquivos. Lesson transferível: shape matters more than scalar early.

## Lições principais

1. **Refactor mid-UAT é viável quando phase ainda não foi UAT-validated definitively.** Phase 7.10.bug2.sub1 expansion ocorreu antes de UAT PASS final — refactor scalar→object foi atomic com 1 commit submódulo + 1 chore bump pai.

2. **Helper return shape espelhando motor return shape fortalece parity test.** Test 7 D-pre-7 com 2 asserts (`.original` + `.atualizado`) pega drift em 2 dimensões em vez de 1.

3. **state_pattern_audit profilático listou 7 consumers `_so_custas` antes de execução** — confirmou que App.jsx:3767 (drawer Pessoas) e DetalheContrato consumers não caem no bug "Calculando..." (lição 7.13d.bug2 aplicada).

4. **Factcheck pré-EDIT do checker output via grep direto** confirmou typo `calcularDetalhesAtualizados` (não existe no motor) — evitou aplicar 5 EDITs cega-confiança no checker. Custo 1 tool call, evita propagar erro.

5. **DetalheDivida `saldoCustas` curto-circuita `saldoDividaLocal`/`saldoDivida`** — fantasma não tem pagamentos parciais, PagamentosDivida não opera. Branch `_so_custas` em `saldoAtual` ternary garante que UI consumer não vai cair no caminho regular nem em hot reload.

## Estado pós-Phase 7.10.bug2.sub1

- **Tags v1.4 ativas (18):** v1.4-phase7.1, 7.2, 7.3, 7.4, 7.5.1, 7.7, 7.8.1, 7.8.2a, 7.9, 7.10a, 7.10.bug, 7.10bcd, 7.13, 7.13b, 7.13d, 7.14, 7.14b, **7.10.bug2.sub1 ← novo**
- **Backlog v1.4 restante:**
  - Phase 7.10.bug2 sub-bug 2 (BACKLOG — schema dual quitação custas, MÉDIA-BAIXA)
  - Phase 7.13d.bug2 (BACKLOG — DetalheContrato edit anomalia)
  - Phase 8 (MÉDIA — PDF Demonstrativo wrap milestone v1.4)
  - Cleanup working tree pai (33 D + 12 M + 23 ??)
- **D-01 motor INTOCADO cumulative** desde Phase 7.8.2a (60+ commits)
