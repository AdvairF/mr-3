---
phase: 7.10.bug2.sub1
plan: 01
type: execute
title: "Helper INPC custas avulsas + branches UI (TabelaDividas + DetalheDivida)"
wave: 1
depends_on: []
files_modified:
  - src/utils/custasAvulsas.js                             # NOVO
  - src/utils/__tests__/custasAvulsas.test.js              # NOVO
  - src/components/TabelaDividas.jsx                       # MOD (branch _so_custas)
  - src/components/DetalheDivida.jsx                       # MOD (branch _so_custas)
autonomous: false                                          # Task 5 = UAT humano (CHECKPOINT)
estimated_lines: 25-30                                     # cumulative diff submódulo
estimated_files: "3 NOVOS + 2 MOD"
requirements:
  - D-pre-1   # Helper signature calcularValorAtualizadoCustasAvulsas(custas, hoje) → number
  - D-pre-2   # Branch _so_custas em TabelaDividas.jsx L94-96
  - D-pre-3   # Branch _so_custas em DetalheDivida.jsx L79-86
  - D-pre-4   # D-01 herdada — motor devedorCalc.js INTOCADO (gate verbatim grep)
  - D-pre-5   # NÃO filtra c.pago (precedente Phase 7.9 P3 + dual-semântica advogado/devedor)
  - D-pre-6   # c.data legacy shape (não c.data_pagamento)
  - D-pre-7   # Test parity helper === motor (gate drift)
must_haves:
  truths:
    - "Linhas com `_so_custas:true` em /dividas (TabelaDividas) exibem valor monetário (não 'Calculando...')"
    - "DetalheDivida de uma dívida `_so_custas:true` exibe o mesmo valor monetário"
    - "Dívidas regulares (não-`_so_custas`) preservam comportamento atual bit-a-bit"
    - "Helper === motor para mesmo input (parity test PASS, drift ≤ 1e-9)"
    - "Motor devedorCalc.js não modificado (D-01 dual gate diff -U0 + log --name-only zero match)"
  artifacts:
    - path: "src/utils/custasAvulsas.js"
      provides: "Helper calcularValorAtualizadoCustasAvulsas(custas, hoje) → number"
      exports: ["calcularValorAtualizadoCustasAvulsas"]
    - path: "src/utils/__tests__/custasAvulsas.test.js"
      provides: "Cobertura helper isolado + parity test motor"
      contains: "describe.*custasAvulsas"
    - path: "src/components/TabelaDividas.jsx"
      provides: "Branch _so_custas antes de saldoExibido + import helper"
      contains: "calcularValorAtualizadoCustasAvulsas"
    - path: "src/components/DetalheDivida.jsx"
      provides: "Branch _so_custas antes de saldoAtual + import helper"
      contains: "calcularValorAtualizadoCustasAvulsas"
  key_links:
    - from: "src/utils/custasAvulsas.js"
      to: "src/utils/correcao.js"
      via: "import { calcularFatorCorrecao }"
      pattern: "import .* calcularFatorCorrecao .* from [\"']\\./correcao(\\.js)?[\"']"
    - from: "src/components/TabelaDividas.jsx"
      to: "src/utils/custasAvulsas.js"
      via: "import + chamada em saldoCustas"
      pattern: "calcularValorAtualizadoCustasAvulsas"
    - from: "src/components/DetalheDivida.jsx"
      to: "src/utils/custasAvulsas.js"
      via: "import + chamada em saldoCustas"
      pattern: "calcularValorAtualizadoCustasAvulsas"
---

<objective>
Eliminar o leak de `undefined` síncrono mascarado de loading state ("Calculando...") nas linhas `_so_custas:true` do módulo /dividas (TabelaDividas) e na tela DetalheDivida correspondente.

Helper externo `src/utils/custasAvulsas.js` espelha **exatamente** o loop INPC do motor `devedorCalc.js:479-491`. UI consumers ganham branch `_so_custas` antes do fallback null. Motor (D-01) permanece INTOCADO trivialmente.

Purpose: ~50% das linhas do módulo Dívidas em prod exibem "Calculando..." indefinido (UAT 2026-05-01) — operador precisa do valor pra decidir cobrança.
Output: 1 arquivo helper NOVO + 1 arquivo test NOVO + 2 callsites UI cirurgicamente patchados + 1 commit atomic submódulo + UAT humano 5 SCs.
Severidade: BAIXA (UX cleanup). Risco: BAIXO (motor INTOCADO, escopo cirúrgico, schema/service layer INTOCADOS).
</objective>

<execution_context>
**Manual orchestration mode** — `gsd-sdk` CLI indisponível. Executor escreve artefatos via Write/Edit tools diretamente. Cada PAUSA interna é checkpoint humano explícito (orchestrator revisa diff antes de prosseguir pra próxima Task).

Submódulo alvo: `src/mr-3/mr-cobrancas` (todos os paths abaixo são RELATIVOS a este submódulo, salvo indicação contrária).

Pai (`mr-cobrancas-vercel_1/mr-cobrancas`): apenas chore bump no final, separado do escopo deste plan.
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07.10-bug2-sub1-custas-helper-inpc/07.10-bug2-sub1-CONTEXT.md
@.planning/phases/07.13d-encargos-defaults-vazios/07.13d-LEARNINGS.md
</context>

<interfaces>
<!-- Contratos extraídos do submódulo HEAD. Executor usa estes diretamente — NÃO precisa explorar codebase. -->

From `src/mr-3/mr-cobrancas/src/utils/correcao.js` L419:
```js
export function calcularFatorCorrecao(indexador, dataInicio, dataFim) {
  return calcularFatorCorrecaoDetalhado(indexador, dataInicio, dataFim).fator;
}
```

From `src/mr-3/mr-cobrancas/src/utils/devedorCalc.js` L479-491 (REFERÊNCIA EXATA do motor — helper deve espelhar):
```js
// Entradas _so_custas (lançamentos avulsos de custas judiciais)
for (const div of dividasSoCustas) {
  for (const c of div.custas || []) {
    const vc = parseFloat(c.valor) || 0;
    totalCustasOriginal += vc;
    if (c.data && c.data < hoje) {
      const fatorC = calcularFatorCorrecao("inpc", c.data, hoje);
      totalCustasAtualizado += vc * fatorC;
    } else {
      totalCustasAtualizado += vc;
    }
  }
}
```

From `src/mr-3/mr-cobrancas/src/utils/devedorCalc.js` L172-176 (filter responsável pelo bug, INTOCADO):
```js
export function calcularSaldosPorDivida(devedor, pagamentos, hoje) {
  const dividasCalc = parseDividas(devedor?.dividas).filter(
    (d) => !d._nominal && !d._so_custas
  );
  ...
}
```

From `src/mr-3/mr-cobrancas/src/components/TabelaDividas.jsx` L93-96 (BEFORE — alvo D-pre-2):
```jsx
const obj = buildDevedorObjParaSaldo(d, devedores, allPagamentos);
const saldosMap = obj ? calcularSaldosPorDivida(obj.devedor, obj.pagamentos, hoje) : null;
const saldo = saldosMap != null ? (saldosMap[String(d.id)] ?? null) : null;
const saldoExibido = d.saldo_quitado === true ? 0 : saldo;
```

From `src/mr-3/mr-cobrancas/src/components/TabelaDividas.jsx` L132-136 (label "Calculando..." consumer):
```jsx
{saldoExibido == null
  ? <span style={{ color: "#94a3b8", fontSize: 13 }}>Calculando...</span>
  : fmtBRL(saldoExibido)
}
```

From `src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx` L79-86 (BEFORE — alvo D-pre-3):
```jsx
const saldosMap = devedor ? calcularSaldosPorDivida(devedor, pagamentosDoDevedor, hoje) : null;
const saldoDivida = saldosMap != null ? (saldosMap[String(divida.id)] ?? null) : null;
// saldoDividaLocal: sobrescreve saldoDivida quando PagamentosDivida reporta novo saldo via onSaldoChange
const [saldoDividaLocal, setSaldoDividaLocal] = useState(null);
const [totalPagoDivida, setTotalPagoDivida] = useState(null);
// Valor efetivo: se PagamentosDivida já propagou um saldo (saldoDividaLocal !== null), usar esse;
// senão usar o calculado pelos pagamentos_parciais do devedor (saldoDivida).
const saldoAtual = saldoDividaLocal !== null ? saldoDividaLocal : saldoDivida;
```

`devedorCalc.js` import existente em DetalheDivida (L7): `import { calcularSaldosPorDivida, calcularTotalPagoPorDivida } from "../utils/devedorCalc.js";` — adicionar import paralelo do helper.

`devedorCalc.js` import existente em TabelaDividas (verificar imediatamente antes de Task 2 via Read tool — provavelmente similar a `import { calcularSaldosPorDivida } from "../utils/...";`).
</interfaces>

<state_pattern_audit>
<!-- Lição feedback_grep_state_pattern_paralelo_em_componentes_relacionados (Phase 7.13d Lição 1). -->
<!-- Antes de declarar varredura completa, audit de TODOS consumers de _so_custas no HEAD do submódulo: -->

| Arquivo | Linhas | Tipo de uso | Em escopo? |
|---|---|---|---|
| `src/utils/devedorCalc.js` | L61, L174, L279, L366-367, L479, L571 | Motor (filter + agregação) | **INTOCADO** (D-01) |
| `src/components/TabelaDividas.jsx` | L94-96 | Consumer saldosMap → "Calculando..." | **EM ESCOPO** (D-pre-2) |
| `src/components/DetalheDivida.jsx` | L79-86 | Consumer saldosMap → saldoDivida null | **EM ESCOPO** (D-pre-3) |
| `src/components/DetalheContrato.jsx` | L626-630, L1075-1076 | Lista custas avulsas + saldosMap por contrato | **FORA DE ESCOPO** — exibe `totalCustasAtualizado` agregado pelo motor (não cai em `null`) |
| `src/App.jsx` | L3767-3799 (drawer Pessoas → aba Dívidas) | Render `_so_custas` com `totalCustas` (soma raw sem INPC) | **FORA DE ESCOPO** — não exibe "Calculando..."; mostra soma original. CONTEXT seção 4 confirma "renomear label 'Calculando...' em outros lugares fora do escopo". Phase 7.14b já removeu drawer DividaForm — App.jsx:3767 é renderer read-only de `divida.custas`. NÃO consumir helper aqui — ficaria fora do escopo CONTEXT/D-pre. Anotar em SUMMARY como observação. |
| `src/services/contratos.js` | L578, L583, L605, L610, L647 | D-23 criação dívida-fantasma | **INTOCADO** (CONTEXT seção 4) |
| `src/hooks/useSaldoAtualizadoCache.js` | L26-46 | D-06 fingerprint | **INTOCADO** (CONTEXT seção 4) |

**Audit conclusão:** Os 2 callsites UI declarados no CONTEXT (TabelaDividas + DetalheDivida) são exatamente os que sofrem do bug "Calculando...". App.jsx:3767 e DetalheContrato consumers seguem patterns diferentes (raw sum / agregação) e estão fora de escopo conforme CONTEXT seção 4.
</state_pattern_audit>

<memory_feedbacks_aplicados>
1. **`feedback_so_custas_ui_calculando_indefinido`** — origem do bug (audit 2026-04-25, gap pré-existente Phase 7.9). Pre-check dataset confirmado via UAT 2026-05-01 (~50% linhas).
2. **`feedback_custas_semantica_quitacao_dual`** — D-pre-5 NÃO filtra c.pago. c.pago em custas é dual-semântica ("advogado pagou ao judiciário" ≠ "devedor quitou dívida"). Filtrar zerar custa avulsa = erro semântico (advogado pediu ressarcimento ao devedor com correção INPC).
3. **`feedback_post_hoc_d_pre_must_be_validated_in_child_phase`** — TASK 0 com operador 2026-05-01 (screenshot + caso de uso real "decisão de cobrança") já consumida; D-pre desta phase nascem do briefing v2 confirmado.
4. **`feedback_discuss_walkthrough_fluxo_real`** — caso de uso real ("decisão de cobrança no módulo Dívidas") pré-validado; não há ambiguidade de Caminho A vs B.
5. **`feedback_memory_must_factcheck_codebase_before_claiming_severity`** — lição cumulative; CONTEXT.md já passou por catch pré-commit (memory `feedback_validate_phase_premise_with_user_use_case_first` substituído por 2 memories REAIS). Nenhuma referência fantasma neste PLAN.
6. **`feedback_gate_motor_grep_use_name_only`** — Task 4 dual gate D-01 usa `git log -1 --format="" --name-only` (não `--stat`), evitando false positives em commit messages. Plus `git diff -U0` working tree pre-commit.
7. **`feedback_grep_state_pattern_paralelo_em_componentes_relacionados`** — `<state_pattern_audit>` acima cobre TODOS consumers de `_so_custas` (não apenas os 2 explicitamente declarados em CONTEXT) e justifica por que os outros estão fora de escopo. Audit prevent surprise post-execute.
</memory_feedbacks_aplicados>

<invariantes_inviolaveis>
- **D-01 herdada (D-pre-4)** — `src/utils/devedorCalc.js` INTOCADO trivialmente (helper externo). Dual gate Task 4: working tree (`git diff -U0`) + post-commit (`git log -1 --format="" --name-only`).
- **Schema** — `dividas._so_custas BOOLEAN` migration 002 INTOCADA.
- **Service layer** — `src/services/contratos.js` D-23 INTOCADO. `src/services/pagamentos.js` + `src/services/dividas.js` INTOCADOS.
- **Cache hook** — `src/hooks/useSaldoAtualizadoCache.js` D-06 INTOCADO.
- **`src/utils/masks.js`** — INTOCADO (D-01 cumulative).
- **D-22/23/24/25** — shape de `custas[]` (`c.data` legacy, dívida-fantasma sempre criada via D-23, `c.pago` dual-semântica) — preservados.
- **D-pre-5** — helper NÃO filtra `c.pago` (espelhamento exato motor L479-491).
- **D-pre-6** — campo `c.data` (não `c.data_pagamento`).
- **D-pre-7** — parity test obrigatório (helper === motor, ±1e-9).
- **`autonomous: false`** — Task 5 é PAUSA humana UAT.
- **Build + 34/34 motor regression PASS** — gate efetivo via `prebuild` hook (`npm run build` cobre `npm run test:regressao` automaticamente).
- **ZERO push** até execute completo + UAT humano PASS + tag (orchestrator faz tag/push fora deste plan).
</invariantes_inviolaveis>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 — Criar src/utils/custasAvulsas.js + tests unitários (D-pre-1, D-pre-5, D-pre-6, D-pre-7)</name>
  <read_first>
    - `src/mr-3/mr-cobrancas/src/utils/devedorCalc.js` L479-491 (loop motor — REFERÊNCIA EXATA pra espelhar)
    - `src/mr-3/mr-cobrancas/src/utils/correcao.js` L419-421 (`calcularFatorCorrecao` signature — `(indexador, dataInicio, dataFim) → number`)
    - `src/mr-3/mr-cobrancas/src/utils/__tests__/parseInputBR.test.js` (pattern de test existente — vitest, `import { describe, it, expect } from "vitest"`)
    - `src/mr-3/mr-cobrancas/package.json` (verificar `"test": "vitest run"` + `"test:regressao"` = 8 files / 34 tests; `prebuild: npm run test:regressao`)
  </read_first>
  <files>
    - `src/mr-3/mr-cobrancas/src/utils/custasAvulsas.js` (NOVO)
    - `src/mr-3/mr-cobrancas/src/utils/__tests__/custasAvulsas.test.js` (NOVO)
  </files>
  <behavior>
    - Test 1 — Custa única SEM `data` → soma `parseFloat(c.valor)` puro (sem fator INPC).
    - Test 2 — Custa única COM `c.data` < `hoje` → soma `vc * calcularFatorCorrecao("inpc", c.data, hoje)`.
    - Test 3 — Custas múltiplas (mix com/sem `data`) → soma cumulativa correta.
    - Test 4 — Custa com `valor` inválido (`""`, `null`, `undefined`, `"abc"`) → ignora (`parseFloat || 0` garante NaN→0; total não NaN).
    - Test 5 — Custas com `c.pago === true` → contribuem normal (D-pre-5: NÃO filtra).
    - Test 6 — Array vazio `[]` ou `null` ou `undefined` ou não-Array → retorna 0 (early return).
    - Test 7 (parity D-pre-7) — Mesmo input `custas[]` feed em (a) helper direto e (b) motor `calcularDetalheEncargos` (lendo `result.custas.atualizado`, computado a partir das entradas `_so_custas`) → igualdade `±1e-9`. Fixture: 2-3 custas com mix datas + um c.pago=true.
  </behavior>
  <action>
    **A) Criar `src/utils/custasAvulsas.js` com VERBATIM pattern (D-pre-1):**

    ```js
    import { calcularFatorCorrecao } from "./correcao.js";

    /**
     * Phase 7.10.bug2.sub1 — espelhamento UI do loop motor devedorCalc.js:479-491.
     * D-pre-5: NÃO filtra c.pago (precedente Phase 7.9 P3 + dual-semântica advogado/devedor).
     * D-pre-6: usa c.data (legacy shape D-22), não c.data_pagamento.
     * D-pre-7: parity gate (helper === motor) em test fixture.
     */
    export function calcularValorAtualizadoCustasAvulsas(custas, hoje) {
      if (!Array.isArray(custas) || custas.length === 0) return 0;
      let total = 0;
      for (const c of custas) {
        const vc = parseFloat(c.valor) || 0;
        if (c.data && c.data < hoje) {
          total += vc * calcularFatorCorrecao("inpc", c.data, hoje);
        } else {
          total += vc;
        }
      }
      return total;
    }
    ```

    **B) Criar `src/utils/__tests__/custasAvulsas.test.js` com 7 testes (vitest):**

    - Imports: `import { describe, it, expect } from "vitest";` + `import { calcularValorAtualizadoCustasAvulsas } from "../custasAvulsas.js";` + (para Test 7) `import { calcularDetalheEncargos } from "../devedorCalc.js";` (read-only — motor não tocado).
    - Test 7 (parity) constrói `devedor` mock com `dividas: [{ id: "x", _so_custas: true, custas: [...] }]`, chama `calcularDetalheEncargos(devedor, [], hoje)`, lê `result.custas.atualizado` (shape motor L513), compara com `calcularValorAtualizadoCustasAvulsas(custas, hoje)` — `expect(Math.abs(motor - helper)).toBeLessThan(1e-9)`.
    - `hoje` fixture: `"2026-05-01"`. `c.data` fixture: `"2024-01-15"` (garante fator INPC > 1).

    NÃO importar de `./db.js`, `./supabase.js` ou outros — helper é pure function.

    NÃO modificar `package.json` — `npm run test -- src/utils/__tests__/custasAvulsas.test.js` funciona com `vitest run` script já existente.
  </action>
  <verify>
    <automated>
      cd src/mr-3/mr-cobrancas && \
      grep -c "^export function calcularValorAtualizadoCustasAvulsas" src/utils/custasAvulsas.js && \
      grep -c "^import { calcularFatorCorrecao } from \"\\./correcao\\.js\"" src/utils/custasAvulsas.js && \
      npm run test -- src/utils/__tests__/custasAvulsas.test.js
    </automated>
  </verify>
  <done>
    - `src/utils/custasAvulsas.js` existe com EXATAMENTE 1 export named `calcularValorAtualizadoCustasAvulsas` (grep `^export function calcularValorAtualizadoCustasAvulsas` count = 1).
    - Helper importa `calcularFatorCorrecao` de `./correcao.js` (grep count ≥ 1).
    - `src/utils/__tests__/custasAvulsas.test.js` existe com ≥ 7 `it(` blocks (grep `^\\s*it(` count ≥ 7).
    - `npm run test -- src/utils/__tests__/custasAvulsas.test.js` exit 0 (todos os 7 testes PASS).
    - Test 7 (parity) explicitamente compara helper === motor com tolerance 1e-9.
    - Motor `devedorCalc.js` ZERO modificações (verificado em Task 4 dual gate).
  </done>
  <pause>
    **PAUSA #1 — orchestrator review:**
    - Diff `src/utils/custasAvulsas.js` (deve ser ~15 linhas verbatim do motor).
    - Diff `src/utils/__tests__/custasAvulsas.test.js` (≥ 7 testes incluindo parity).
    - Output `npm run test -- custasAvulsas.test.js` (todos PASS).
    - Confirmar D-pre-1 (signature exata), D-pre-5 (sem filtro c.pago + Test 5 explícito), D-pre-6 (c.data), D-pre-7 (parity test PASS).
    Resume signal: "Task 1 OK, prosseguir Task 2".
  </pause>
</task>

<task type="auto" tdd="false">
  <name>Task 2 — Branch _so_custas em TabelaDividas.jsx L94-96 (D-pre-2)</name>
  <read_first>
    - `src/mr-3/mr-cobrancas/src/components/TabelaDividas.jsx` (FULL file — confirmar imports topo + estrutura visivel block L80-140 + qualquer outro consumer de `d._so_custas` no arquivo).
    - Confirmar import existente de `calcularSaldosPorDivida` no topo (provavelmente `import { calcularSaldosPorDivida } from "../utils/devedorCalc.js";`).
  </read_first>
  <files>
    - `src/mr-3/mr-cobrancas/src/components/TabelaDividas.jsx` (MOD — adicionar import + branch)
  </files>
  <action>
    **A) Adicionar import no topo (próximo aos imports existentes de `../utils/`):**

    ```jsx
    import { calcularValorAtualizadoCustasAvulsas } from "../utils/custasAvulsas.js";
    ```

    **B) Substituir bloco L93-96 BEFORE:**

    ```jsx
    const obj = buildDevedorObjParaSaldo(d, devedores, allPagamentos);
    const saldosMap = obj ? calcularSaldosPorDivida(obj.devedor, obj.pagamentos, hoje) : null;
    const saldo = saldosMap != null ? (saldosMap[String(d.id)] ?? null) : null;
    const saldoExibido = d.saldo_quitado === true ? 0 : saldo;
    ```

    **POR (D-pre-2 pattern verbatim do CONTEXT):**

    ```jsx
    const obj = buildDevedorObjParaSaldo(d, devedores, allPagamentos);
    const saldosMap = obj ? calcularSaldosPorDivida(obj.devedor, obj.pagamentos, hoje) : null;
    const saldo = saldosMap != null ? (saldosMap[String(d.id)] ?? null) : null;
    // Phase 7.10.bug2.sub1 D-pre-2 — branch _so_custas (motor filtra L172-176, helper espelha L479-491).
    const saldoCustas = d._so_custas
      ? calcularValorAtualizadoCustasAvulsas(d.custas || [], hoje)
      : null;
    const saldoExibido = d.saldo_quitado === true
      ? 0
      : (d._so_custas ? saldoCustas : saldo);
    ```

    Comportamento das dívidas regulares (não-`_so_custas`) preservado bit-a-bit (mesma branch original `saldo` quando `d._so_custas` é falsy).

    NÃO tocar L132-136 (label "Calculando..." consumer) — ela continua existindo para edge case `saldoExibido == null` em dívidas regulares onde `saldosMap` ainda não calculou (loading legítimo).

    NÃO modificar header de tabela, devedor lookup, credor lookup, badge contrato, status, atraso, ações — todos preservados bit-a-bit.
  </action>
  <verify>
    <automated>
      cd src/mr-3/mr-cobrancas && \
      grep -c "calcularValorAtualizadoCustasAvulsas" src/components/TabelaDividas.jsx && \
      grep -c "d\\._so_custas" src/components/TabelaDividas.jsx && \
      grep -c "Phase 7\\.10\\.bug2\\.sub1 D-pre-2" src/components/TabelaDividas.jsx && \
      npm run build
    </automated>
  </verify>
  <done>
    - `grep -c "calcularValorAtualizadoCustasAvulsas" src/components/TabelaDividas.jsx` ≥ 2 (1 import + 1 chamada em saldoCustas).
    - `grep -c "d\\._so_custas" src/components/TabelaDividas.jsx` ≥ 2 (saldoCustas branch + saldoExibido ternary).
    - `grep -c "Phase 7\\.10\\.bug2\\.sub1 D-pre-2" src/components/TabelaDividas.jsx` = 1 (audit trail comment).
    - `npm run build` exit 0 (cobre `prebuild` → `npm run test:regressao` 34/34 motor regression PASS automaticamente + Vite build PASS).
    - Block L132-136 ("Calculando..." label) PRESERVADO bit-a-bit (`saldoExibido == null` ainda existe para edge case loading legítimo).
    - Estrutura `<tr>...<td>` preservada.
  </done>
  <pause>
    **PAUSA #2 — orchestrator review:**
    - Diff `src/components/TabelaDividas.jsx` (esperado ~6-8 linhas adicionadas: 1 import + comment + 3 linhas saldoCustas + 3 linhas saldoExibido reescrito).
    - Output `npm run build` (PASS — inclui motor regression 34/34).
    - Confirmar D-pre-2 (branch antes saldoExibido) + comment audit trail "Phase 7.10.bug2.sub1 D-pre-2".
    - Confirmar zero modificações fora do block L93-96 + 1 import.
    Resume signal: "Task 2 OK, prosseguir Task 3".
  </pause>
</task>

<task type="auto" tdd="false">
  <name>Task 3 — Branch _so_custas em DetalheDivida.jsx L79-86 (D-pre-3)</name>
  <read_first>
    - `src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx` (FULL file — confirmar imports topo L7 + bloco L73-88 + onde `saldoAtual` é consumido posteriormente — typically render de label "Calculando..." ou rendering condicional).
    - Confirmar `divida.custas` é acessível (single divida prop, não array de dividas como TabelaDividas).
  </read_first>
  <files>
    - `src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx` (MOD — adicionar import + branch)
  </files>
  <action>
    **A) Adicionar import no topo (após L7 import de `devedorCalc.js`):**

    ```jsx
    import { calcularValorAtualizadoCustasAvulsas } from "../utils/custasAvulsas.js";
    ```

    **B) Substituir bloco L79-86 BEFORE:**

    ```jsx
    const saldosMap = devedor ? calcularSaldosPorDivida(devedor, pagamentosDoDevedor, hoje) : null;
    const saldoDivida = saldosMap != null ? (saldosMap[String(divida.id)] ?? null) : null;
    // saldoDividaLocal: sobrescreve saldoDivida quando PagamentosDivida reporta novo saldo via onSaldoChange
    const [saldoDividaLocal, setSaldoDividaLocal] = useState(null);
    const [totalPagoDivida, setTotalPagoDivida] = useState(null);
    // Valor efetivo: se PagamentosDivida já propagou um saldo (saldoDividaLocal !== null), usar esse;
    // senão usar o calculado pelos pagamentos_parciais do devedor (saldoDivida).
    const saldoAtual = saldoDividaLocal !== null ? saldoDividaLocal : saldoDivida;
    ```

    **POR (D-pre-3 — adapta single divida em vez de array, branch _so_custas curto-circuita PagamentosDivida porque fantasma não tem pagamentos parciais):**

    ```jsx
    const saldosMap = devedor ? calcularSaldosPorDivida(devedor, pagamentosDoDevedor, hoje) : null;
    const saldoDivida = saldosMap != null ? (saldosMap[String(divida.id)] ?? null) : null;
    // Phase 7.10.bug2.sub1 D-pre-3 — branch _so_custas (motor filtra L172-176, helper espelha L479-491).
    // Para _so_custas, saldoDividaLocal NÃO se aplica — fantasma não tem pagamentos parciais (PagamentosDivida não opera).
    const saldoCustas = divida._so_custas
      ? calcularValorAtualizadoCustasAvulsas(divida.custas || [], hoje)
      : null;
    // saldoDividaLocal: sobrescreve saldoDivida quando PagamentosDivida reporta novo saldo via onSaldoChange
    const [saldoDividaLocal, setSaldoDividaLocal] = useState(null);
    const [totalPagoDivida, setTotalPagoDivida] = useState(null);
    // Valor efetivo:
    //  - _so_custas → saldoCustas (helper INPC)
    //  - regular: saldoDividaLocal (PagamentosDivida) ou saldoDivida (motor)
    const saldoAtual = divida._so_custas
      ? saldoCustas
      : (saldoDividaLocal !== null ? saldoDividaLocal : saldoDivida);
    ```

    Comportamento de dívidas regulares (não-`_so_custas`) PRESERVADO bit-a-bit: `saldoDividaLocal !== null ? saldoDividaLocal : saldoDivida` continua sendo o caminho ativo.

    NÃO tocar `useState`s, `pagoPorDividaMap`, `totalPago`, `handleRemovePrincipalWarning`, `handleConfirmarRemoverPrincipal`, `handleCancelarRemoverPrincipal` — todos preservados bit-a-bit.

    NÃO tocar `PagamentosDivida` rendering — fica como está (motor agnóstico; `_so_custas` simplesmente curto-circuita upstream).
  </action>
  <verify>
    <automated>
      cd src/mr-3/mr-cobrancas && \
      grep -c "calcularValorAtualizadoCustasAvulsas" src/components/DetalheDivida.jsx && \
      grep -c "divida\\._so_custas" src/components/DetalheDivida.jsx && \
      grep -c "Phase 7\\.10\\.bug2\\.sub1 D-pre-3" src/components/DetalheDivida.jsx && \
      npm run build
    </automated>
  </verify>
  <done>
    - `grep -c "calcularValorAtualizadoCustasAvulsas" src/components/DetalheDivida.jsx` ≥ 2 (1 import + 1 chamada em saldoCustas).
    - `grep -c "divida\\._so_custas" src/components/DetalheDivida.jsx` ≥ 2 (saldoCustas branch + saldoAtual ternary).
    - `grep -c "Phase 7\\.10\\.bug2\\.sub1 D-pre-3" src/components/DetalheDivida.jsx` = 1 (audit trail comment).
    - `npm run build` exit 0 (motor regression 34/34 PASS via prebuild + Vite build PASS).
    - `useState(null)` declarations PRESERVADOS bit-a-bit (saldoDividaLocal + totalPagoDivida).
    - `pagoPorDividaMap` + `totalPago` PRESERVADOS bit-a-bit.
  </done>
  <pause>
    **PAUSA #3 — orchestrator review:**
    - Diff `src/components/DetalheDivida.jsx` (esperado ~8-10 linhas adicionadas: 1 import + comment + 3 linhas saldoCustas + 3-4 linhas saldoAtual reescrito).
    - Output `npm run build` (PASS).
    - Confirmar D-pre-3 (branch antes saldoAtual) + comment audit trail "Phase 7.10.bug2.sub1 D-pre-3".
    - Confirmar zero modificações fora do block L79-86 + 1 import.
    - Confirmar branch curto-circuita `saldoDividaLocal`/`saldoDivida` para `_so_custas` (PagamentosDivida não aplicável a fantasma).
    Resume signal: "Task 3 OK, prosseguir Task 4".
  </pause>
</task>

<task type="auto" tdd="false">
  <name>Task 4 — Build + test:regressao + dual gate D-01 + commit atomic submódulo</name>
  <read_first>
    - `src/mr-3/mr-cobrancas/package.json` (confirmar `prebuild: npm run test:regressao` ainda existe — gate efetivo).
    - Working tree status submódulo (`git status` em `src/mr-3/mr-cobrancas`) — confirmar APENAS 4 arquivos modificados/novos: `src/utils/custasAvulsas.js`, `src/utils/__tests__/custasAvulsas.test.js`, `src/components/TabelaDividas.jsx`, `src/components/DetalheDivida.jsx`.
  </read_first>
  <files>
    - (nenhum arquivo de source modificado nesta task — apenas gates + commit)
  </files>
  <action>
    **A) Pre-commit gates (working tree):**

    1. `cd src/mr-3/mr-cobrancas && npm run build` — exit 0 esperado (cobre `prebuild` → `npm run test:regressao` automaticamente: 8 files / 34 tests motor regression PASS + Vite build PASS).

    2. `cd src/mr-3/mr-cobrancas && npm run test -- src/utils/__tests__/custasAvulsas.test.js` — exit 0 esperado (7 testes do helper PASS).

    3. **Dual gate D-01 working tree (D-pre-4 + lição `feedback_gate_motor_grep_use_name_only`):**
       ```bash
       cd src/mr-3/mr-cobrancas
       # Gate 4a — diff -U0 working tree (zero linhas em motor + service layer + masks):
       test "$(git diff -U0 src/utils/devedorCalc.js | wc -l)" = "0"
       test "$(git diff -U0 src/services/pagamentos.js | wc -l)" = "0"
       test "$(git diff -U0 src/services/dividas.js | wc -l)" = "0"
       test "$(git diff -U0 src/utils/masks.js | wc -l)" = "0"
       ```

    4. Confirmar staging contém APENAS:
       - `src/utils/custasAvulsas.js` (NOVO)
       - `src/utils/__tests__/custasAvulsas.test.js` (NOVO)
       - `src/components/TabelaDividas.jsx` (MOD)
       - `src/components/DetalheDivida.jsx` (MOD)

       Adicionar specificamente (não usar `git add .` — lição cumulative):
       ```bash
       git add src/utils/custasAvulsas.js \
               src/utils/__tests__/custasAvulsas.test.js \
               src/components/TabelaDividas.jsx \
               src/components/DetalheDivida.jsx
       ```

    **B) Commit atomic submódulo:**

    ```bash
    git commit -m "$(cat <<'EOF'
    feat(07.10.bug2.sub1): helper INPC custas avulsas + branches TabelaDividas + DetalheDivida

    Phase 7.10.bug2.sub1 — Helper INPC para `_so_custas` (UI "Calculando..." indefinido).

    Motivo: motor `devedorCalc.js:172-176` filtra `_so_custas:true` antes do
    cálculo → `saldosMap[id] === undefined` → UI consumers exibem
    "Calculando..." indefinido (gap pré-existente Phase 7.9; ~50% linhas
    afetadas em prod confirmado UAT 2026-05-01).

    Decisão Caminho A (Caminho 1 do briefing): helper externo
    `utils/custasAvulsas.js` espelha EXATAMENTE motor `devedorCalc.js:479-491`.
    UI consumers (TabelaDividas L94-96 + DetalheDivida L79-86) ganham
    branch `_so_custas` antes do fallback null.

    D-pre-1 helper signature `calcularValorAtualizadoCustasAvulsas(custas, hoje) → number`.
    D-pre-2 branch TabelaDividas (saldoExibido com d._so_custas).
    D-pre-3 branch DetalheDivida (saldoAtual com divida._so_custas — curto-circuita PagamentosDivida pra fantasma).
    D-pre-4 D-01 motor INTOCADO (dual gate verbatim grep + diff -U0).
    D-pre-5 NÃO filtra c.pago (precedente Phase 7.9 P3 + dual-semântica advogado/devedor).
    D-pre-6 c.data legacy shape (não c.data_pagamento).
    D-pre-7 test parity helper === motor (gate drift, ±1e-9).

    Files:
    - NOVO src/utils/custasAvulsas.js (~15 linhas)
    - NOVO src/utils/__tests__/custasAvulsas.test.js (7 testes)
    - MOD src/components/TabelaDividas.jsx (+import +6-8 linhas)
    - MOD src/components/DetalheDivida.jsx (+import +8-10 linhas)

    Gates: build OK + 34/34 motor regression PASS + 7/7 helper tests PASS
    + dual gate D-01 (devedorCalc.js + services/pagamentos.js + services/dividas.js + masks.js zero modifications).

    Severidade BAIXA. Risco BAIXO. autonomous:false (Task 5 UAT humano pendente).
    EOF
    )"
    ```

    **C) Gate 4b post-commit (if-then-else, lição `feedback_gate_motor_grep_use_name_only`):**

    ```bash
    cd src/mr-3/mr-cobrancas
    if git log -1 --format="" --name-only | grep -qE "(devedorCalc\.js|services/pagamentos\.js|services/dividas\.js|utils/masks\.js)"; then
      echo "FAIL Gate 4b — D-01 violado: motor/service/masks tocado no commit"
      exit 1
    else
      echo "PASS Gate 4b — D-01 INTOCADO no commit"
    fi
    ```

    **D) NÃO push, NÃO chore bump no pai, NÃO tag.** Orchestrator faz pós Task 5 UAT.
  </action>
  <verify>
    <automated>
      cd src/mr-3/mr-cobrancas && \
      npm run build && \
      ! git log -1 --format="" --name-only | grep -qE "(devedorCalc\\.js|services/pagamentos\\.js|services/dividas\\.js|utils/masks\\.js)" && \
      git log -1 --pretty=format:"%s" | grep -qE "^feat\\(07\\.10\\.bug2\\.sub1\\):"
    </automated>
  </verify>
  <done>
    - `npm run build` exit 0 (cobre `prebuild` → `test:regressao` 34/34 + Vite build).
    - `npm run test -- src/utils/__tests__/custasAvulsas.test.js` exit 0 (7 testes PASS).
    - Gate 4a working tree: `git diff -U0 src/utils/devedorCalc.js | wc -l` = 0; idem para `services/pagamentos.js`, `services/dividas.js`, `utils/masks.js`.
    - Commit submódulo presente com mensagem `feat(07.10.bug2.sub1): helper INPC custas avulsas + branches TabelaDividas + DetalheDivida`.
    - Gate 4b post-commit: `git log -1 --format="" --name-only` NÃO contém `devedorCalc.js`, `services/pagamentos.js`, `services/dividas.js`, `utils/masks.js`.
    - Commit contém EXATAMENTE 4 arquivos (2 NOVOS + 2 MOD).
    - Pai (`mr-cobrancas-vercel_1/mr-cobrancas`) NÃO recebe chore bump nesta task (orchestrator faz pós-UAT).
  </done>
  <pause>
    **PAUSA #4 — orchestrator review:**
    - `git log -1 --stat` no submódulo (esperado: 4 files changed, ~25-30 insertions, 0 deletions OU as deletions exatas das 8 linhas BEFORE substituídas).
    - Output `npm run build` (PASS).
    - Output Gate 4a + Gate 4b (D-01 INTOCADO confirmado dual).
    - Confirmar mensagem commit completa (D-pre 1-7 listadas + lista de files + gates).
    Resume signal: "Task 4 OK, prosseguir UAT humano Task 5".
  </pause>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5 — UAT humano CHECKPOINT (5 SCs do CONTEXT seção 9)</name>
  <what-built>
    Helper `calcularValorAtualizadoCustasAvulsas(custas, hoje)` em `src/utils/custasAvulsas.js` espelha motor `devedorCalc.js:479-491`. Branches `_so_custas` em `TabelaDividas.jsx` (L94-96) e `DetalheDivida.jsx` (L79-86) consomem o helper antes do fallback null. Motor INTOCADO (D-01 dual gate PASS). 34/34 motor regression PASS. 7/7 helper tests PASS (incluindo parity helper === motor).

    Visualmente: linhas `_so_custas:true` (criadas por D-23 da Phase 7.9; valor_total=0; ~50% das linhas em prod) deixam de exibir "Calculando..." e passam a exibir o valor INPC-corrigido das custas.
  </what-built>
  <how-to-verify>
    Pré-condição: `cd src/mr-3/mr-cobrancas && npm run dev` em terminal local OU push pra Vercel (orchestrator decide; recomenda local primeiro pra evitar push pré-UAT — invariante "ZERO push até UAT PASS").

    **SC1 — Módulo Dívidas: linhas `_so_custas` exibem valor (não "Calculando...")**
    1. Login em `http://localhost:5173/` (ou URL local equivalente).
    2. Navegar para módulo "Dívidas" (sidebar).
    3. Identificar linhas com Valor Original = R$ 0,00 (assinatura `_so_custas:true`).
    4. ✅ ESPERADO: coluna "Saldo Atualizado" exibe valor monetário (ex: R$ 1.234,56), NÃO "Calculando...".
    5. ❌ FAIL se: ainda exibe "Calculando..." em linhas `_so_custas:true`.

    **SC2 — Clicar Ver Detalhe → DetalheDivida exibe mesmo valor**
    1. Em uma linha `_so_custas:true` do SC1 que mostra valor X, clicar (abre DetalheDivida).
    2. ✅ ESPERADO: tela DetalheDivida exibe valor X (mesmo valor que TabelaDividas).
    3. ❌ FAIL se: DetalheDivida exibe valor diferente, ou "Calculando...", ou null/undefined renderizado.

    **SC3 — Console F12 limpo**
    1. Abrir DevTools (F12) → aba Console.
    2. Recarregar /dividas e abrir DetalheDivida de uma linha `_so_custas:true`.
    3. ✅ ESPERADO: zero erros novos, zero warnings novos relacionados a `calcularValorAtualizadoCustasAvulsas` ou `_so_custas`.
    4. ❌ FAIL se: TypeError, ReferenceError, ou warning de prop tipo errado.

    **SC4 — Dívidas regulares (não-`_so_custas`) preservam comportamento**
    1. Em /dividas, identificar linhas com Valor Original > R$ 0,00 (dívidas regulares).
    2. ✅ ESPERADO: Saldo Atualizado igual ao que era ANTES desta phase (motor calcula via `calcularSaldosPorDivida` como sempre).
    3. ✅ ESPERADO: clicar Ver Detalhe → DetalheDivida funciona normalmente, PagamentosDivida funcional, saldoDividaLocal sobrescreve quando há pagamento parcial.
    4. ❌ FAIL se: regressão visual ou funcional em dívidas regulares.

    **SC5 — Cross-check valor parity (D-pre-7 in vivo)**
    1. Para uma dívida `_so_custas:true` específica (anotar `divida.id` via inspeção React DevTools ou DB):
       - Valor exibido em TabelaDividas (X1).
       - Valor exibido em DetalheDivida (X2).
       - Valor que o motor agrega em `result.custas.atualizado` (via React DevTools inspect de `calcularDetalheEncargos` retorno, OU via console.log temporário se necessário; OU validar via teste fixture local com mesma `custas[]`).
    2. ✅ ESPERADO: X1 ≡ X2 ≡ X3 (helper === motor in vivo, confirma parity test D-pre-7 não diverge em prod).
    3. ❌ FAIL se: divergência entre X1, X2, X3 (drift helper vs motor — bug crítico, parity test deveria pegar mas pode ter caso edge não coberto).

    **NOTA UAT:** se prod tem 0 linhas `_so_custas:true` (improvável dado UAT 2026-05-01), criar custa avulsa de teste via DetalheContrato → +Adicionar Documento → custa avulsa (fluxo D-23 Phase 7.9), depois rodar SCs.
  </how-to-verify>
  <resume-signal>
    "UAT 7.10.bug2.sub1 PASS" (todos os 5 SCs PASS — orchestrator prossegue pra SUMMARY + chore bump pai + tag `v1.4-phase7.10.bug2.sub1`).

    OU

    "UAT FAIL [SC#] [motivo]" (pelo menos 1 SC FAIL — orchestrator decide rollback vs fix-forward; típico fix-forward é Task adicional cobrindo edge case revelado).
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→helper pure | Input UI (`d.custas[]`) flui pra helper pure function client-side. |
| helper→correcao | Helper consome `calcularFatorCorrecao` (read-only). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7.10.bug2.sub1-01 | Tampering | `custas[]` array shape | accept | Schema dividas._so_custas BOOLEAN + custas JSONB já validados motor-side via D-22; helper usa mesmo shape (D-pre-6). |
| T-7.10.bug2.sub1-02 | Information Disclosure | Helper drift vs motor | mitigate | D-pre-7 parity test (helper === motor, ±1e-9) + dual gate D-01 (motor INTOCADO grep verbatim). |
| T-7.10.bug2.sub1-03 | Denial of Service | parseFloat(c.valor) com input gigante | accept | Custas inseridas via UI Phase 7.9 D-23 (fluxo controlado); array typically 1-5 items por dívida-fantasma. |
| T-7.10.bug2.sub1-04 | Elevation of Privilege | N/A | accept | Phase UI-only (sem service layer touch, sem schema touch, sem auth touch). |
| T-7.10.bug2.sub1-05 | Repudiation | Audit trail comments | mitigate | Comentário "Phase 7.10.bug2.sub1 D-pre-2/3" em cada callsite + commit message com 7 D-pre listadas. |
| T-7.10.bug2.sub1-06 | Spoofing | N/A | accept | Helper não autentica nem autoriza — pure function. |
</threat_model>

<verification>
## Plan-level gates

1. **Build + 34/34 motor regression**: `npm run build` no submódulo (cobre `prebuild` → `npm run test:regressao` 8 files / 34 tests motor) — Tasks 2, 3, 4.
2. **7/7 helper tests**: `npm run test -- src/utils/__tests__/custasAvulsas.test.js` — Task 1, 4.
3. **Parity test D-pre-7**: helper === motor (±1e-9) — Task 1 Test 7 (gate de drift).
4. **D-01 dual gate (D-pre-4)**:
   - Gate 4a working tree: `git diff -U0 {devedorCalc.js, services/pagamentos.js, services/dividas.js, utils/masks.js}` = 0 linhas — Task 4.
   - Gate 4b post-commit: `git log -1 --format="" --name-only` NÃO contém esses 4 paths — Task 4.
5. **Audit trail comments**: `Phase 7.10.bug2.sub1 D-pre-2` em TabelaDividas + `Phase 7.10.bug2.sub1 D-pre-3` em DetalheDivida (1 cada).
6. **UAT humano 5 SCs**: SC1 + SC2 + SC3 + SC4 + SC5 PASS — Task 5.
7. **Estado pós-execução submódulo**: 1 commit atomic, 4 files (2 NOVOS + 2 MOD), ~25-30 linhas adicionadas.
8. **Estado pós-execução pai**: ZERO modificação no pai durante este plan (chore bump + tag fora do escopo, orchestrator pós-UAT).
</verification>

<success_criteria>
- [ ] `src/utils/custasAvulsas.js` existe com export `calcularValorAtualizadoCustasAvulsas` (helper espelha motor L479-491 verbatim).
- [ ] `src/utils/__tests__/custasAvulsas.test.js` existe com ≥ 7 testes PASS (incluindo parity test D-pre-7).
- [ ] `src/components/TabelaDividas.jsx` consome helper via branch `_so_custas` em saldoExibido (D-pre-2 + audit comment).
- [ ] `src/components/DetalheDivida.jsx` consome helper via branch `_so_custas` em saldoAtual (D-pre-3 + audit comment).
- [ ] Motor `devedorCalc.js` ZERO modificações (D-01 dual gate PASS).
- [ ] `services/pagamentos.js`, `services/dividas.js`, `utils/masks.js` ZERO modificações (cumulative D-01).
- [ ] `npm run build` exit 0 (cobre `prebuild` → 34/34 motor regression).
- [ ] 1 commit atomic submódulo com message `feat(07.10.bug2.sub1):` + 7 D-pre listadas.
- [ ] UAT humano 5 SCs PASS REAL (operador local ou prod).
- [ ] Pai ZERO modificações até pós-UAT (chore bump fora deste plan).
- [ ] ZERO push até UAT PASS + tag (orchestrator post-this-plan).
</success_criteria>

<output>
Pós Task 5 UAT PASS, orchestrator (fora deste plan) cria:

1. `.planning/phases/07.10-bug2-sub1-custas-helper-inpc/07.10-bug2-sub1-01-SUMMARY.md` — D-pre 1-7 validation table, 5 SCs UAT result, lições.
2. `.planning/phases/07.10-bug2-sub1-custas-helper-inpc/07.10-bug2-sub1-LEARNINGS.md` (opcional, se ≥ 1 lição transferível).
3. STATE.md + ROADMAP.md updates → "SHIPPED 2026-05-01" entries.
4. Pai chore bump (`chore(07.10.bug2.sub1): bump submódulo — feat helper INPC custas avulsas`) + tag `v1.4-phase7.10.bug2.sub1` no commit do pai.
5. `git push origin master` + `git push origin v1.4-phase7.10.bug2.sub1` (apenas pai, lição `feedback_tag_push_apenas_no_pai_chore_bump`).
</output>
