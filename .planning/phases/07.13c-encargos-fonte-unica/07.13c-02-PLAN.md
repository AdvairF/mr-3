---
phase: 07.13c-encargos-fonte-unica
plan: 02
type: execute
wave: 2
depends_on: ["07.13c-01"]
files_modified:
  - src/mr-3/mr-cobrancas/src/utils/devedorCalc.js
  - src/mr-3/mr-cobrancas/src/App.jsx
  - src/mr-3/mr-cobrancas/src/hooks/useSaldoAtualizadoCache.js
  - src/mr-3/mr-cobrancas/src/services/filaDevedor.js
  - src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx
  - src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx
  - src/mr-3/mr-cobrancas/src/services/__tests__/calculos.test.js
  - src/mr-3/mr-cobrancas/src/services/__tests__/saldoAtualizadoCache.test.js
  - src/mr-3/mr-cobrancas/src/services/__tests__/custasCache.test.js
autonomous: false
requirements: []
must_haves:
  truths:
    - "Adapter calcularDetalheEncargosContrato recebe contrato como 1º arg e sobrescreve 6 campos de encargo (D-pre-1, despesas excluído pois motor não consome — CONTEXT.md L101) no pseudoDevedor.dividas ANTES de delegar ao motor"
    - "calcularSaldoContratoAtualizado repassa contrato adiante (assinatura 4 args)"
    - "dividasMap populator (App.jsx:8418-8428) enriquece dividas com encargos do contrato via lookup em allContratos por div.contrato_id (Estratégia A — D-pre-3.2)"
    - "Branch back-compat: divida sem contrato_id mantém encargos próprios (D-pre-3.1) em ambas estratégias"
    - "Os 4 callsites do adapter recebem contrato como 1º arg explícito (D-pre-3.3)"
    - "Test shield equivalência: caso degenerado zero drift retorna saldo idêntico ao motor direto"
    - "Test novo drift sintético: contrato.multa_percentual=2 vs divida.multa_pct=30 → adapter retorna saldo do contrato"
    - "Motor calcularDetalheEncargos (devedorCalc.js:365-521) zero diff cumulative (D-01)"
    - "services/pagamentos.js + services/dividas.js + masks.js zero diff cumulative"
    - "Smoke local cruzado: UI exibe valores que SQL Plan 01 NEW previu como 'Depois' per contrato afetado"
  artifacts:
    - path: "src/mr-3/mr-cobrancas/src/utils/devedorCalc.js"
      provides: "Adapter Path A puro com sobrescrita 6 campos D-pre-1 antes de delegar ao motor INTOCADO"
      contains: "function calcularDetalheEncargosContrato\\(contrato"
    - path: "src/mr-3/mr-cobrancas/src/App.jsx"
      provides: "dividasMap populator enriquecido (Estratégia A) com lookup allContratosMap"
      contains: "allContratosMap|contratoOwner"
    - path: "src/mr-3/mr-cobrancas/src/services/__tests__/calculos.test.js"
      provides: "Test drift sintético + shield equivalência adapter↔motor"
      contains: "drift|equivalência|equivalencia"
  key_links:
    - from: "calcularDetalheEncargosContrato (1º arg contrato)"
      to: "calcularDetalheEncargos (motor INTOCADO)"
      via: "pseudoDevedor.dividas com 6 campos sobrescritos"
      pattern: "pseudoDevedor|dividasComEncargosDoContrato"
    - from: "useSaldoAtualizadoCache.js:recomputeEntry"
      to: "calcularDetalheEncargosContrato(contrato, ...)"
      via: "contrato repassado de useSaldoContrato/kickoffBatch para recomputeEntry"
      pattern: "recomputeEntry\\(.*contrato"
    - from: "App.jsx:8418-8428 dividasMap populator"
      to: "calcularDetalheEncargos via App.jsx:2040 (Pessoas/Detalhe-Devedor)"
      via: "lookup allContratosMap por div.contrato_id + sobrescrita 6 campos"
      pattern: "allContratosMap"
---

<objective>
Refatorar `calcularDetalheEncargosContrato` para Path A puro (recebe contrato como 1º arg, sobrescreve 6 campos de encargo D-pre-1 antes de delegar ao motor INTOCADO) + atualizar 4 callsites adapter + populator `dividasMap` em App.jsx + 3 test files existentes + adicionar 2 tests novos (drift sintético + shield equivalência).

**Purpose:** Aplicação direta de `schema_adapter_long_short_form_consumer_replication` — adapter DB↔motor reaplicado em TODOS 5 consumers (1 motor direto via populator + 4 callsites adapter). `shields_wrapper_must_test_equivalence`: shield obrigatório de equivalência adapter↔motor pro caso degenerado zero drift. `strategy_a_shared_wrapper_delegation`: populator centraliza Pessoas + adapter cobre 4 callsites; sem duplicação. D-01 motor INTOCADO cumulative (devedorCalc.js:365-521 zero diff).

**Output:** 6 arquivos produção modificados + 3 test files atualizados + 2 tests novos. Smoke local cruzado UI↔SQL PASS per contrato afetado. ZERO push (deploy é Plan 03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.13c-encargos-fonte-unica/07.13c-CONTEXT.md
@.planning/phases/07.13c-encargos-fonte-unica/07.13c-AUDIT-PROD.md
@.planning/phases/07.13c-encargos-fonte-unica/07.13c-COMPARISON.md
@.planning/phases/07.13c-encargos-fonte-unica/07.13c-01-SUMMARY.md
@src/mr-3/mr-cobrancas/src/utils/devedorCalc.js

<interfaces>
ATUAL (devedorCalc.js:849-867 — HEAD):
```js
export function calcularDetalheEncargosContrato(dividasDoContrato, allPagamentosDivida, hoje) { ... }
export const calcularSaldoContratoAtualizado = (dividasDoContrato, allPagamentosDivida, hoje) => ...;
```

TARGET Path A puro (4 args com contrato 1º):
```js
export function calcularDetalheEncargosContrato(contrato, dividasDoContrato, allPagamentosDivida, hoje) {
  const dividaIds = new Set((dividasDoContrato || []).map(d => String(d.id)));
  const pagamentosFiltrados = (allPagamentosDivida || []).filter(p => dividaIds.has(String(p.divida_id)));
  // D-pre-3.1 back-compat: contrato=null/undefined → adapter passa-thru.
  const dividasComEncargosDoContrato = contrato
    ? (dividasDoContrato || []).map(d => ({
        ...d,
        // D-pre-1: 6 campos sobrescritos
        indexador:      contrato.indice_correcao,
        juros_tipo:     contrato.juros_tipo,
        juros_am:       contrato.juros_am_percentual,
        multa_pct:      contrato.multa_percentual,
        honorarios_pct: contrato.honorarios_percentual,
        art523_opcao:   contrato.art523_opcao,
        // D-pre-2: NÃO sobrescrever data_inicio_atualizacao, custas, valor_total, data_vencimento.
      }))
    : (dividasDoContrato || []);
  const pseudoDevedor = { dividas: dividasComEncargosDoContrato };
  return calcularDetalheEncargos(pseudoDevedor, pagamentosFiltrados, hoje);
}

export const calcularSaldoContratoAtualizado = (contrato, dividasDoContrato, allPagamentosDivida, hoje) =>
  calcularDetalheEncargosContrato(contrato, dividasDoContrato, allPagamentosDivida, hoje).saldoAtualizado;
```

Motor INTOCADO (devedorCalc.js:365-521): zero diff em toda a phase.

Mapeamento 5 callsites:
1. App.jsx:2040 `calcularDetalheEncargos(sel, ...)` — NÃO MUDA (consome dividas já enriquecidas pelo populator).
2. App.jsx:8418-8428 populator dividasMap — ENRIQUECER (Estratégia A — D-pre-3.2).
3. useSaldoAtualizadoCache.js:64 — recomputeEntry recebe contrato + 3 callsites internos (L107, L117, L179) atualizados.
4. filaDevedor.js:148 — `c` no .map scope, trivial.
5. filaDevedor.js:241 — `contrato` no scope L237, trivial.
6. FilaDevedor.jsx:480 — `contrato` é prop, trivial + deps useMemo.
7. DetalheContrato.jsx:589 — `contrato` é prop principal, trivial + deps useMemo.
</interfaces>

<test_paths_descobertos_via_grep>
- `src/mr-3/mr-cobrancas/src/services/__tests__/calculos.test.js` linhas 21, 205, 207, 236
- `src/mr-3/mr-cobrancas/src/services/__tests__/custasCache.test.js` linhas 19, 100, 126, 135
- `src/mr-3/mr-cobrancas/src/services/__tests__/saldoAtualizadoCache.test.js` linhas 20, 24, 114
</test_paths_descobertos_via_grep>

<memory_feedbacks_aplicaveis>
- `schema_adapter_long_short_form_consumer_replication` — adapter DB↔motor reaplicado em TODOS 5 consumers (Path A é aplicação direta)
- `shields_wrapper_must_test_equivalence` — shield obrigatório `wrapper === adapterDireto(input)` para caso degenerado zero drift
- `strategy_a_shared_wrapper_delegation` — populator + adapter sem duplicação
- `gate_motor_grep_use_name_only` — `git log -1 --format="" --name-only`, NÃO `--stat` (lição 7.8.2a)
- `grep_global_antes_de_fix_pontual` — grep global em TODOS arquivos da phase + leitura de invariantes D-XX
</memory_feedbacks_aplicaveis>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor adapter calcularDetalheEncargosContrato + calcularSaldoContratoAtualizado (Path A puro)</name>
  <read_first>
    - src/mr-3/mr-cobrancas/src/utils/devedorCalc.js (motor 365-521 + adapter 849-867 ANTES de tocar)
    - .planning/phases/07.13c-encargos-fonte-unica/07.13c-CONTEXT.md (D-pre-1, D-pre-2, D-pre-3.1)
    - .planning/phases/07.13c-encargos-fonte-unica/07.13c-COMPARISON.md (resultado Plan 01 NEW)
    - $HOME/.claude/projects/.../memory/feedback_schema_adapter_long_short_form_consumer_replication.md
  </read_first>
  <behavior>
    - Test 1 (zero drift equivalência): contrato.indice_correcao = "inpc" + dividas[0].indexador = "inpc" + multas/honor/juros idênticos → `adapter(c, divs, pgts, hoje).saldoAtualizado === motor(pseudoDev, pgtsFilt, hoje).saldoAtualizado`
    - Test 2 (drift sintético): contrato.multa_percentual=2 + dividas[0].multa_pct=30 → adapter retorna saldo computado com multa 2%, NÃO 30%
    - Test 3 (back-compat D-pre-3.1): `adapter(null, divs, pgts, hoje)` → passa-thru, encargos próprios da divida (sem sobrescrita)
  </behavior>
  <files>src/mr-3/mr-cobrancas/src/utils/devedorCalc.js (somente linhas 849-867)</files>
  <action>
1. Editar APENAS linhas 849-867 substituindo a função e a const pelo código TARGET (ver `<interfaces>` block acima — código completo lá).

2. Verificar imediatamente motor INTOCADO (linhas 365-521):
```bash
cd C:/Users/advai/Downloads/mr-cobrancas-vercel_1/mr-cobrancas/src/mr-3/mr-cobrancas
git diff src/utils/devedorCalc.js -U0 | grep "^@@"
```
Confirmar visualmente que só hunks dentro do range linhas 849-867 aparecem. Se algum hunk com `@@ -3` ou `@@ -4` ou `@@ -5` aparecer: ABORT.

3. PAUSA #1 — orchestrator revisa diff isolado de devedorCalc.js. Confirmar:
   - 6 campos sobrescritos: indice_correcao→indexador, juros_tipo, juros_am_percentual→juros_am, multa_percentual→multa_pct, honorarios_percentual→honorarios_pct, art523_opcao
   - 4 campos NÃO sobrescritos documentados em comentário: data_inicio_atualizacao, custas, valor_total, data_vencimento
   - Branch back-compat presente: ternário `contrato ? ... : (dividasDoContrato || [])`
   - Motor `calcularDetalheEncargos` linhas 365-521 ZERO diff

4. NÃO commitar — agrupar com Tasks 2-5.
  </action>
  <verify>
    <automated>cd src/mr-3/mr-cobrancas &amp;&amp; grep -E "function calcularDetalheEncargosContrato\(contrato," src/utils/devedorCalc.js &amp;&amp; grep -E "calcularSaldoContratoAtualizado = \(contrato," src/utils/devedorCalc.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "function calcularDetalheEncargosContrato(contrato," src/utils/devedorCalc.js` ≥ 1
    - `grep -c "calcularSaldoContratoAtualizado = (contrato," src/utils/devedorCalc.js` ≥ 1
    - 6 campos D-pre-1 presentes: `grep -c "indexador:.*contrato\.indice_correcao"`, `grep -c "juros_tipo:.*contrato\.juros_tipo"`, `grep -c "juros_am:.*contrato\.juros_am_percentual"`, `grep -c "multa_pct:.*contrato\.multa_percentual"`, `grep -c "honorarios_pct:.*contrato\.honorarios_percentual"`, `grep -c "art523_opcao:.*contrato\.art523_opcao"` — todos ≥ 1
    - Branch back-compat: `grep -E "contrato\s+\?" src/utils/devedorCalc.js` retorna ≥ 1 (confirma ternário back-compat D-pre-3.1)
    - Motor `calcularDetalheEncargos` zero diff: `git diff src/utils/devedorCalc.js -U0 | grep "^@@"` mostra apenas hunks em torno linhas 849-867
    - PAUSA #1 executada
  </acceptance_criteria>
  <done>
    - Adapter refatorado Path A puro
    - Motor INTOCADO confirmado
    - PAUSA #1 OK, ZERO commit ainda
  </done>
</task>

<task type="auto">
  <name>Task 2: Atualizar populator dividasMap em App.jsx (Estratégia A — D-pre-3.2)</name>
  <read_first>
    - src/mr-3/mr-cobrancas/src/App.jsx linhas 8380-8460 (allContratos state + populator dividasMap atual)
    - src/mr-3/mr-cobrancas/src/App.jsx linhas 2030-2055 (callsite L2040 — beneficiário enriquecimento)
    - .planning/phases/07.13c-encargos-fonte-unica/07.13c-CONTEXT.md (D-pre-3.2 + D-pre-3.1)
  </read_first>
  <files>src/mr-3/mr-cobrancas/src/App.jsx (somente bloco populator linhas ~8413-8429)</files>
  <action>
1. Editar APENAS o bloco populator. Substituir o forEach atual pelo código abaixo (`contratos` é a variável local de `carregarTudo()` da L8408 `setAllContratos(...)`):

```js
const documentosMap = new Map((documentos || []).map(d => [String(d.id), d.tipo]));
// Phase 7.13c D-pre-3.2 — Estratégia A: enriquecer dividas com encargos do contrato (single source).
// D-pre-3.1 back-compat: divida sem contrato_id (custas avulsas Phase 7.9, forenses 7.10) → encargos próprios.
const allContratosMap = new Map((contratos || []).map(c => [String(c.id), c]));
const dividasMap = new Map();
(divs || []).forEach(div => {
  const k = String(div.devedor_id);
  if (!dividasMap.has(k)) dividasMap.set(k, []);
  const contratoOwner = div.contrato_id ? allContratosMap.get(String(div.contrato_id)) : null;
  dividasMap.get(k).push({
    ...div,
    parcelas: parseJ(div.parcelas),
    custas: parseJ(div.custas),
    descricao: div.observacoes,
    // D-pre-1: 6 campos sobrescritos quando contrato existe; senão fallback aos campos próprios da divida.
    indexador:      contratoOwner ? contratoOwner.indice_correcao        : div.indice_correcao,
    juros_tipo:     contratoOwner ? contratoOwner.juros_tipo              : div.juros_tipo,
    juros_am:       contratoOwner ? contratoOwner.juros_am_percentual     : div.juros_am_percentual,
    multa_pct:      contratoOwner ? contratoOwner.multa_percentual        : div.multa_percentual,
    honorarios_pct: contratoOwner ? contratoOwner.honorarios_percentual   : div.honorarios_percentual,
    art523_opcao:   contratoOwner ? contratoOwner.art523_opcao            : div.art523_opcao,
    _contrato_tipo: div.documento_id ? (documentosMap.get(String(div.documento_id)) ?? null) : null,
  });
});
```

   Notas:
   - Os campos `juros_tipo` e `art523_opcao` que faltavam no enriquecimento HEAD (consumidos pelo motor mas não populados) agora ESTÃO presentes — fix latente. Documentar no commit message.
   - `contratos` é variável local do `carregarTudo()` (L8408). Sem hit de state assíncrono.

2. Verificar callsite L2040 ZERO diff:
```bash
cd C:/Users/advai/Downloads/mr-cobrancas-vercel_1/mr-cobrancas/src/mr-3/mr-cobrancas
git diff src/App.jsx -U0 | grep "^@@"
```
Confirmar único hunk é o populator (linhas ~8413-8429).

3. NÃO commitar — agrupar com Tasks 1, 3, 4.
  </action>
  <verify>
    <automated>cd src/mr-3/mr-cobrancas &amp;&amp; grep -c "allContratosMap" src/App.jsx &amp;&amp; grep -c "contratoOwner" src/App.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "allContratosMap" src/App.jsx` ≥ 1
    - `grep -c "contratoOwner" src/App.jsx` ≥ 7 (1 declaração + 6 usos no enriquecimento)
    - `grep -c "art523_opcao:.*contratoOwner" src/App.jsx` ≥ 1 (campo NOVO no populator pós-Path A)
    - `grep -c "juros_tipo:.*contratoOwner" src/App.jsx` ≥ 1 (campo NOVO no populator pós-Path A)
    - `git diff src/App.jsx -U0 | grep "^@@"` mostra HUNKs só em torno linha ~8413-8429
    - Callsite App.jsx:2040 ZERO diff
  </acceptance_criteria>
  <done>
    - Populator enriquecido com 6 campos D-pre-1
    - Branch back-compat D-pre-3.1 funcional
    - App.jsx:2040 NÃO modificado
    - ZERO commit ainda
  </done>
</task>

<task type="auto">
  <name>Task 3: Atualizar 4 callsites adapter (D-pre-3.3)</name>
  <read_first>
    - src/mr-3/mr-cobrancas/src/hooks/useSaldoAtualizadoCache.js (toda — recomputeEntry + useSaldoContrato + kickoffBatch)
    - src/mr-3/mr-cobrancas/src/services/filaDevedor.js linhas 1-260 (callsites L148 + L241; scope `c` e `contrato`)
    - src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx linhas 460-490 (props + useMemo)
    - src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx linhas 580-600 (contrato prop + useMemo deps)
    - .planning/phases/07.13c-encargos-fonte-unica/07.13c-CONTEXT.md (D-pre-3.3)
    - $HOME/.claude/projects/.../memory/feedback_grep_global_antes_de_fix_pontual.md
  </read_first>
  <files>
    src/mr-3/mr-cobrancas/src/hooks/useSaldoAtualizadoCache.js,
    src/mr-3/mr-cobrancas/src/services/filaDevedor.js,
    src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx,
    src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx
  </files>
  <action>
1. **useSaldoAtualizadoCache.js** — atualizar `recomputeEntry` (L59) para receber `contrato`:
```js
function recomputeEntry(contratoId, contrato, dividasDoContrato, allPagamentosDivida, hoje) {
  // ... (corpo igual, mudando só o callsite do adapter):
  const detalhe = calcularDetalheEncargosContrato(contrato, dividasDoContrato || [], allPagamentosDivida || [], hoje);
  // ... (resto igual)
}
```
   Atualizar 3 callsites de `recomputeEntry`:
   - L107 (MISS dentro de useSaldoContrato): `setTimeout(() => recomputeEntry(contrato.id, contrato, divs, allPags, dataHoje), 0);`
   - L117 (drift fingerprint dentro de useSaldoContrato): `setTimeout(() => recomputeEntry(contrato.id, contrato, divs, allPags, dataHoje), 0);`
   - L179 (kickoffBatch — `c` é o contrato no forEach): `recomputeEntry(c.id, c, dv, allPagamentosDivida, dataHoje);`

2. **filaDevedor.js** — atualizar 2 callsites:
   - L148 (dentro `.map((c) =>`): `const saldoAtualizado = calcularSaldoContratoAtualizado(c, dividasDoContrato, allPagamentosDivida, hoje);`
   - L241 (`contrato` no scope L237): `const saldo = calcularSaldoContratoAtualizado(contrato, dividas, allPagamentos, hoje);`

3. **FilaDevedor.jsx** — atualizar callsite L480 + deps useMemo:
```js
const detalheEncargos = useMemo(() => {
  if (!dividas?.length) return null;
  const hoje = new Date().toISOString().slice(0, 10);
  return calcularDetalheEncargosContrato(contrato, dividas, allPagamentosDivida, hoje);
}, [contrato, dividas, allPagamentosDivida]);
```

4. **DetalheContrato.jsx** — atualizar callsite L588-591 + deps useMemo:
```js
const detalheEncargosContrato = useMemo(
  () => calcularDetalheEncargosContrato(contrato, dividas || [], allPagamentosDivida || [], hoje),
  [contrato, dividas, allPagamentosDivida, hoje]
);
```

5. Aplicar `grep_global_antes_de_fix_pontual` — grep global pra detectar callsite legacy esquecido:
```bash
cd C:/Users/advai/Downloads/mr-cobrancas-vercel_1/mr-cobrancas/src/mr-3/mr-cobrancas
# Total de ocorrências (incluindo imports):
grep -rn "calcularDetalheEncargosContrato\|calcularSaldoContratoAtualizado" src/ --include='*.js' --include='*.jsx'
# Esperado ≥ 7 ocorrências em arquivos não-test, não-devedorCalc:
# - 4 callsites adapter (useSaldoAtualizadoCache.js + filaDevedor.js × 2 + FilaDevedor.jsx + DetalheContrato.jsx)
# - 2 imports (filaDevedor.js L3 + useSaldoAtualizadoCache.js L18 + FilaDevedor.jsx L23 + DetalheContrato.jsx L15)
# Sanity: nenhuma chamada com 3 args legacy (sem contrato como 1º):
grep -rE "calcularDetalheEncargosContrato\(\s*[a-zA-Z]" src/ --include='*.js' --include='*.jsx' | grep -v "__tests__" | grep -v "devedorCalc.js" | grep -v "(contrato\|(c,\|(null,"
# Esperado: 0 linhas (nenhum callsite legacy)
```
Se algum callsite com assinatura antiga aparecer: corrigir antes de prosseguir.

6. NÃO commitar — agrupar com Task 4 (tests).
  </action>
  <verify>
    <automated>cd src/mr-3/mr-cobrancas &amp;&amp; grep -c "calcularDetalheEncargosContrato(contrato," src/components/DetalheContrato.jsx &amp;&amp; grep -c "calcularDetalheEncargosContrato(contrato," src/components/FilaDevedor.jsx &amp;&amp; grep -c "calcularSaldoContratoAtualizado(c," src/services/filaDevedor.js &amp;&amp; grep -c "calcularSaldoContratoAtualizado(contrato," src/services/filaDevedor.js &amp;&amp; grep -c "recomputeEntry(.*contrato" src/hooks/useSaldoAtualizadoCache.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "calcularDetalheEncargosContrato(contrato," src/components/DetalheContrato.jsx` ≥ 1
    - `grep -c "calcularDetalheEncargosContrato(contrato," src/components/FilaDevedor.jsx` ≥ 1
    - `grep -c "calcularSaldoContratoAtualizado(c," src/services/filaDevedor.js` ≥ 1
    - `grep -c "calcularSaldoContratoAtualizado(contrato," src/services/filaDevedor.js` ≥ 1
    - `grep -c "recomputeEntry(.*contrato" src/hooks/useSaldoAtualizadoCache.js` ≥ 4 (1 definição + 3 callsites internos)
    - useMemo deps em FilaDevedor.jsx L481 e DetalheContrato.jsx L590 incluem `contrato`
    - Grep global confirma ZERO callsite legacy 3-args fora de tests
  </acceptance_criteria>
  <done>
    - 4 callsites adapter atualizados pra Path A
    - useMemo deps atualizados (contrato adicionado)
    - Grep global confirma ZERO callsite legacy
    - ZERO commit ainda
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Atualizar 3 test files existentes + adicionar 2 tests novos (drift sintético + shield equivalência)</name>
  <read_first>
    - src/mr-3/mr-cobrancas/src/services/__tests__/calculos.test.js (linhas 21, 205, 207, 236 — describe "calcularDetalheEncargosContrato")
    - src/mr-3/mr-cobrancas/src/services/__tests__/saldoAtualizadoCache.test.js (linhas 20, 24, 114 — fixtures)
    - src/mr-3/mr-cobrancas/src/services/__tests__/custasCache.test.js (linhas 19, 100, 126, 135 — chamadas)
    - src/mr-3/mr-cobrancas/src/utils/devedorCalc.js (motor + adapter atualizado pós-Task 1)
    - $HOME/.claude/projects/.../memory/feedback_shields_wrapper_must_test_equivalence.md
  </read_first>
  <files>
    src/mr-3/mr-cobrancas/src/services/__tests__/calculos.test.js,
    src/mr-3/mr-cobrancas/src/services/__tests__/saldoAtualizadoCache.test.js,
    src/mr-3/mr-cobrancas/src/services/__tests__/custasCache.test.js
  </files>
  <action>
1. **calculos.test.js** — atualizar 2 chamadas existentes pra nova assinatura:
   - L207: `const r = calcularDetalheEncargosContrato(null, [], [], "2026-04-24");` (back-compat null)
   - L236: `const r = calcularDetalheEncargosContrato(null, dividas, pagamentosAllContratos, "2026-04-24");` (caso original sem contrato — passa-thru)

   Adicionar 2 tests NOVOS dentro do mesmo describe `calcularDetalheEncargosContrato`:

   ```js
   it("Path A — contrato sobrescreve encargos da divida (drift sintético)", () => {
     // Phase 7.13c D-pre-1 + memory feedback schema_adapter_long_short_form_consumer_replication
     const contrato = {
       id: "c1",
       indice_correcao: "inpc",
       juros_tipo: "sem_juros",
       juros_am_percentual: 0,
       multa_percentual: 2,    // contrato 2%
       honorarios_percentual: 10,
       art523_opcao: "nao_aplicar",
     };
     const dividas = [{
       id: "d1", contrato_id: "c1",
       valor_total: 1000,
       data_vencimento: "2025-01-01",
       data_inicio_atualizacao: "2025-01-01",
       indexador: "inpc",
       juros_tipo: "sem_juros",
       juros_am: 0,
       multa_pct: 30,    // divida 30% (drift!)
       honorarios_pct: 0,
       art523_opcao: "nao_aplicar",
     }];
     const r = calcularDetalheEncargosContrato(contrato, dividas, [], "2026-04-24");
     // Saldo deve usar multa 2% (contrato), NÃO 30% (divida).
     // Saldo bruto teórico: 1000 + correcao + multa(2%) + honor(10%); validar via reconstrução manual:
     const detalhe1 = r.detalhePorDivida[0];
     expect(detalhe1.multaPct).toBe(2);    // do contrato
     expect(detalhe1.honorariosPct).toBe(10);  // do contrato
     // Multa absoluta deve refletir 2% sobre principal corrigido (não 30%):
     const principalCorrigido = 1000 + detalhe1.correcao;
     expect(detalhe1.multa).toBeCloseTo(principalCorrigido * 0.02, 2);
   });

   it("SHIELD equivalência — caso degenerado zero drift retorna saldo idêntico ao motor direto", () => {
     // Phase 7.13c D-pre-1 + memory feedback shields_wrapper_must_test_equivalence
     // Quando dividas.*_percentual === contratos_dividas.*_percentual, adapter Path A
     // deve produzir mesmo resultado que motor calcularDetalheEncargos invocado diretamente.
     const contrato = {
       id: "c1",
       indice_correcao: "inpc",
       juros_tipo: "sem_juros",
       juros_am_percentual: 0,
       multa_percentual: 2,
       honorarios_percentual: 10,
       art523_opcao: "nao_aplicar",
     };
     const dividas = [{
       id: "d1", contrato_id: "c1",
       valor_total: 1000,
       data_vencimento: "2025-01-01",
       data_inicio_atualizacao: "2025-01-01",
       indexador: "inpc",          // === contrato
       juros_tipo: "sem_juros",     // === contrato
       juros_am: 0,                 // === contrato
       multa_pct: 2,                // === contrato (zero drift)
       honorarios_pct: 10,          // === contrato (zero drift)
       art523_opcao: "nao_aplicar", // === contrato
     }];
     const pgts = [];
     const hoje = "2026-04-24";

     // Adapter Path A (com contrato):
     const adapterResult = calcularDetalheEncargosContrato(contrato, dividas, pgts, hoje);

     // Motor direto (sem adapter — pseudoDevedor com mesmos dados):
     const dividaIds = new Set(dividas.map(d => String(d.id)));
     const pgtsFiltrados = pgts.filter(p => dividaIds.has(String(p.divida_id)));
     const motorResult = calcularDetalheEncargos({ dividas }, pgtsFiltrados, hoje);

     // Shield: saldo IDÊNTICO (não só "aproximado").
     expect(adapterResult.saldoAtualizado).toBe(motorResult.saldoAtualizado);
     expect(adapterResult.totalEncargos).toBe(motorResult.totalEncargos);
     expect(adapterResult.multa.valor).toBe(motorResult.multa.valor);
     expect(adapterResult.juros.valor).toBe(motorResult.juros.valor);
     expect(adapterResult.honorarios.valor).toBe(motorResult.honorarios.valor);
     expect(adapterResult.correcao.valor).toBe(motorResult.correcao.valor);
   });
   ```

   Adicionar import `calcularDetalheEncargos` no topo do file se ainda não estiver importado (provável, pois é o motor — verificar grep `import.*calcularDetalheEncargos[^C]` em calculos.test.js):
   ```js
   import { calcularSaldoDevedorAtualizado, calcularPlanilhaCompleta, calcularDetalheEncargosContrato, calcularDetalheEncargos } from "../../utils/devedorCalc.js";
   ```

2. **saldoAtualizadoCache.test.js** — atualizar fixture L114:
   - L114: `const direto = calcularDetalheEncargosContrato(null, dividas, [], hoje);` (zero contrato pra back-compat — adapter passa-thru, mantém comportamento HEAD do test)
   - OR adicionar fixture `contrato` no escopo do test e passar adiante. Decisão: orchestrator escolhe pela leitura do contexto do test (se test depende de encargos específicos: passar `contrato` mockado; se test só verifica fingerprint/cache: usar `null`).

3. **custasCache.test.js** — atualizar 3 chamadas (L100, L126, L135):
   - L100: `const direto = calcularDetalheEncargosContrato(null, div, [], "2026-04-24");`
   - L126: `const detIPCA = calcularDetalheEncargosContrato(null, divIPCA, [], hoje);`
   - L135: `const detIGPM = calcularDetalheEncargosContrato(null, divIGPM, [], hoje);`

   Justificativa null: tests de custasCache focam em correção monetária baseada em encargos das dividas (sem contrato no escopo). Passar `null` ativa back-compat D-pre-3.1 — adapter passa-thru, comportamento idêntico ao HEAD.

4. Rodar suite completa:
```bash
cd C:/Users/advai/Downloads/mr-cobrancas-vercel_1/mr-cobrancas/src/mr-3/mr-cobrancas
npm test -- --testPathPattern=calculos
npm test -- --testPathPattern=saldoAtualizadoCache
npm test -- --testPathPattern=custasCache
# E o regressão completo do motor:
npm test
```
   Esperado: 34/34 motor regression PASS + tests novos PASS + tests existentes atualizados PASS.

5. PAUSA #2 — orchestrator revisa os 2 tests novos (drift sintético + shield equivalência) ANTES de prosseguir.

6. NÃO commitar — agrupar no commit consolidado da Task 5.
  </action>
  <verify>
    <automated>cd src/mr-3/mr-cobrancas &amp;&amp; npm test -- --testPathPattern=calculos 2&gt;&amp;1 | grep -E "Tests:.*passed" &amp;&amp; grep -c "drift sintético\|drift sintetico" src/services/__tests__/calculos.test.js &amp;&amp; grep -c "SHIELD equivalência\|SHIELD equivalencia" src/services/__tests__/calculos.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "drift sintético\|drift sintetico" src/services/__tests__/calculos.test.js` ≥ 1
    - `grep -c "SHIELD equivalência\|SHIELD equivalencia" src/services/__tests__/calculos.test.js` ≥ 1
    - `npm test -- --testPathPattern=calculos` exit 0 e logs incluem `Tests: ... passed` (zero failed)
    - `npm test -- --testPathPattern=saldoAtualizadoCache` exit 0
    - `npm test -- --testPathPattern=custasCache` exit 0
    - `npm test` (full suite) exit 0 com 34/34 motor regression PASS (`grep -c "calcularDetalheEncargos" src/services/__tests__/calculos.test.js` mantém o describe original do motor)
    - PAUSA #2 executada (orchestrator revisou os 2 tests novos)
  </acceptance_criteria>
  <done>
    - 3 test files atualizados pra nova assinatura
    - 2 tests novos adicionados (drift sintético + shield equivalência)
    - Suite completa PASS
    - PAUSA #2 OK, ZERO commit ainda
  </done>
</task>

<task type="auto">
  <name>Task 5: Build + lint + dual gate D-01 motor INTOCADO + commit consolidado submódulo</name>
  <read_first>
    - $HOME/.claude/projects/.../memory/feedback_gate_motor_grep_use_name_only.md
    - .planning/phases/07.13c-encargos-fonte-unica/07.13c-CONTEXT.md (seção 7 Invariantes universais)
  </read_first>
  <files>(commit submódulo apenas — sem novo arquivo)</files>
  <action>
1. Build + lint:
```bash
cd C:/Users/advai/Downloads/mr-cobrancas-vercel_1/mr-cobrancas/src/mr-3/mr-cobrancas
npm run build
npm run lint
```
Esperado: ambos exit 0.

2. **Dual gate D-01 motor INTOCADO** (memory feedback `gate_motor_grep_use_name_only`):

   **Gate (a)** — diff exato verbatim do motor:
   ```bash
   # Confirmar que linhas 365-521 do devedorCalc.js NÃO mudaram.
   # Estratégia: extrair os hunks do git diff e validar que nenhum cobre o range 365-521.
   git diff src/utils/devedorCalc.js -U0 | grep "^@@"
   # Esperado: hunks só com `@@ -849,...` ou `@@ -86X,...` (linhas do adapter).
   # CRÍTICO: NENHUM hunk com `@@ -3...` `@@ -4...` `@@ -5...` (range do motor).
   ```

   **Gate (b)** — `git log --name-only` NÃO contém pagamentos.js / dividas.js (D-01 cumulative + invariantes 7.14):
   ```bash
   # Após commit consolidado (passo 4), verificar:
   git log -1 --format="" --name-only
   # Esperado: lista NÃO inclui src/services/pagamentos.js NEM src/services/dividas.js NEM src/utils/masks.js.
   ```

3. PAUSA #3 — orchestrator aprova diff completo do submódulo:
```bash
cd C:/Users/advai/Downloads/mr-cobrancas-vercel_1/mr-cobrancas/src/mr-3/mr-cobrancas
git status
git diff --stat
git diff
```
Esperado:
   - Arquivos modificados: 6 produção (devedorCalc.js + App.jsx + useSaldoAtualizadoCache.js + filaDevedor.js + FilaDevedor.jsx + DetalheContrato.jsx) + 3 tests (calculos.test.js + saldoAtualizadoCache.test.js + custasCache.test.js) = 9 total
   - Arquivos NÃO modificados (cumulative invariants): services/pagamentos.js, services/dividas.js, masks.js
   - devedorCalc.js: hunks só em torno de linhas 849-867
   - App.jsx: hunks só em torno populator (~8413-8429)

4. Commit consolidado no submódulo:
```bash
cd C:/Users/advai/Downloads/mr-cobrancas-vercel_1/mr-cobrancas/src/mr-3/mr-cobrancas
git add src/utils/devedorCalc.js src/App.jsx src/hooks/useSaldoAtualizadoCache.js src/services/filaDevedor.js src/components/FilaDevedor.jsx src/components/DetalheContrato.jsx src/services/__tests__/calculos.test.js src/services/__tests__/saldoAtualizadoCache.test.js src/services/__tests__/custasCache.test.js
git commit -m "$(cat <<'EOF'
feat(07.13c-02): Path A — contrato como fonte única de encargos

Phase 7.13c — Refactor adapter calcularDetalheEncargosContrato para receber
contrato como 1º arg e sobrescrever 6 campos D-pre-1 antes de delegar ao motor.

Changes:
- src/utils/devedorCalc.js linhas 849-867 — adapter recebe contrato (Path A)
- src/App.jsx populator dividasMap (linhas ~8413-8429) — Estratégia A D-pre-3.2,
  enriquece com lookup allContratosMap por div.contrato_id; back-compat D-pre-3.1
  preservado (divida sem contrato_id mantém encargos próprios). Fix latente:
  juros_tipo + art523_opcao agora populados no enriquecimento.
- src/hooks/useSaldoAtualizadoCache.js — recomputeEntry recebe contrato + 3 callsites
- src/services/filaDevedor.js — 2 callsites (L148 c, L241 contrato no scope)
- src/components/FilaDevedor.jsx — useMemo deps atualizados (contrato)
- src/components/DetalheContrato.jsx — useMemo deps atualizados (contrato)
- 3 test files atualizados + 2 tests novos (drift sintético + shield equivalência)

D-01 motor INTOCADO cumulative (calcularDetalheEncargos linhas 365-521 zero diff).
services/pagamentos.js + services/dividas.js + masks.js INTOCADOS cumulative.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

5. Re-rodar dual gate D-01 pós-commit:
```bash
git log -1 --format="" --name-only
```
Confirmar lista NÃO contém `services/pagamentos.js`, `services/dividas.js`, `utils/masks.js`.

6. ZERO push (push é Plan 03).
  </action>
  <verify>
    <automated>cd src/mr-3/mr-cobrancas &amp;&amp; npm run build &amp;&amp; npm run lint &amp;&amp; git log -1 --format="" --name-only | (! grep -E "services/pagamentos\.js|services/dividas\.js|utils/masks\.js")</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exit 0
    - `npm run lint` exit 0
    - `git log -1 --format="" --name-only` lista 9 arquivos (6 prod + 3 tests)
    - `git log -1 --format="" --name-only` NÃO contém `src/services/pagamentos.js`
    - `git log -1 --format="" --name-only` NÃO contém `src/services/dividas.js`
    - `git log -1 --format="" --name-only` NÃO contém `src/utils/masks.js`
    - `git log -1 --format="" --name-only` NÃO contém `src/services/contratos.js` (D-pre-6 EDT-04 reconciliação — `editarContrato` zero diff confirma "encargos é template na escrita, contrato na leitura")
    - PAUSA #3 executada (orchestrator aprovou diff completo)
    - Commit `feat(07.13c-02)` criado no submódulo, ZERO push
  </acceptance_criteria>
  <done>
    - Build + lint PASS
    - Dual gate D-01 confirmado (motor zero diff + name-only sem invariantes)
    - 1 commit consolidado no submódulo
    - ZERO push
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6 (CHECKPOINT): Smoke local cruzado UI ↔ SQL Plan 01 NEW</name>
  <what-built>
    Submódulo com 9 arquivos modificados (1 commit `feat(07.13c-02)`):
    - 6 produção: devedorCalc.js + App.jsx + useSaldoAtualizadoCache.js + filaDevedor.js + FilaDevedor.jsx + DetalheContrato.jsx
    - 3 tests: calculos.test.js (2 novos) + saldoAtualizadoCache.test.js + custasCache.test.js

    Path A puro implementado: adapter recebe contrato como 1º arg, sobrescreve 6 campos D-pre-1, delega ao motor INTOCADO.
  </what-built>
  <how-to-verify>
1. Subir app local (dev server):
```bash
cd C:/Users/advai/Downloads/mr-cobrancas-vercel_1/mr-cobrancas/src/mr-3/mr-cobrancas
npm run dev
```

2. Abrir app local no navegador (geralmente http://localhost:5173 ou conforme config Vite/CRA).

3. Para CADA um dos 3 contratos com drift do `07.13c-COMPARISON.md`, verificar UI exibe valor "Depois" do COMPARISON.md (memory feedback `uat_humano_pega_bugs_que_gates_automatizados_nao_pegam`):

   **Tela 1 — DetalheContrato (contrato afetado, ex.: M L FRIOS):**
   - Abrir contrato M L FRIOS (UUID 335a2ad2)
   - Conferir Resumo Financeiro: saldo bate com `total_depois` da linha M L FRIOS no COMPARISON.md (±R$ 1 de tolerância por arredondamento de fator INPC)
   - Conferir Decomposição: multa 2% + honor 10% (do contrato), NÃO multa 30% + honor 0% (das dividas)

   **Tela 2 — Pessoas / Detalhe-Devedor (devedor associado ao contrato afetado):**
   - Abrir devedor da M L FRIOS
   - Conferir saldo da divida individual exibe os mesmos encargos D-pre-1 (multa 2%, honor 10%) — populator funcionando

   **Tela 3 — FilaPainel:**
   - Verificar contrato M L FRIOS aparece na fila (se status apropriado) com `_saldo_atualizado` correto
   - Conferir score reflete saldo correto

   **Tela 4 — Carteira Total Dashboard:**
   - Conferir Carteira Total reflete soma corrigida (delta de R$ ΔT do COMPARISON.md aplicado nos 3 contratos)

4. Para o **contrato sem drift (controle)** do COMPARISON.md:
   - Verificar saldo idêntico ao Antes (Δ R$ 0,00 — confirma populator não introduz drift onde não havia)

5. Console F12 limpo (zero erros, zero warnings novos).

6. Decisão final:
   - **PASS:** UI bate com SQL Plan 01 NEW per contrato afetado + controle ZERO drift mantém saldo + console limpo. Plan 03 LIBERADO.
   - **FAIL fix-forward (deviation menor):** UI difere por arredondamento ≤ R$ 1 ou bug isolado em 1 callsite. Orchestrator decide se fix-forward direto OU re-pausar.
   - **FAIL ABORT (delta inesperado):** UI bate aproximadamente mas magnitude global do Δ TOTAL não é coerente com o que orchestrator esperou. ABORT — phase recicla pra discuss revisado.
  </how-to-verify>
  <resume-signal>
    Type "SMOKE LOCAL PASS — PLAN 03 LIBERADO" para continuar pro deploy.
    Type "SMOKE LOCAL FAIL — fix-forward: <descrever issue>" pra fix-forward antes de Plan 03.
    Type "SMOKE LOCAL FAIL — ABORT" pra reciclar phase pra /gsd-discuss-phase.
  </resume-signal>
  <acceptance_criteria>
    - Orchestrator declarou EXPLICITAMENTE no log "SMOKE LOCAL PASS" OU "FAIL fix-forward" OU "FAIL ABORT"
    - Se PASS: declaração inclui per-contrato a confirmação de UI bate com COMPARISON.md "Depois" (com tolerância documentada)
    - Console F12 limpo (zero erros novos vs HEAD pre-Plan 02 NEW)
    - Contrato sem drift (controle) mantém saldo idêntico
  </acceptance_criteria>
  <done>
    - Decisão final: PASS / FAIL fix-forward / FAIL ABORT
    - Se PASS: Plan 03 liberado
    - Se FAIL fix-forward: novo commit dentro do submódulo (consolidar com `git commit --amend` OU commit fix-forward separado — orchestrator decide)
    - Se FAIL ABORT: phase reciclada
  </done>
</task>

</tasks>

<verification>
**Quality gates do plano:**

- [ ] Adapter Path A puro implementado (devedorCalc.js linhas 849-867)
- [ ] Motor INTOCADO confirmado (devedorCalc.js linhas 365-521 zero diff)
- [ ] Populator App.jsx enriquecido com Estratégia A (lookup allContratosMap + 6 campos D-pre-1)
- [ ] 4 callsites adapter atualizados (useSaldoAtualizadoCache + filaDevedor × 2 + FilaDevedor + DetalheContrato)
- [ ] App.jsx:2040 (callsite motor direto) ZERO diff
- [ ] 3 test files atualizados + 2 tests novos (drift sintético + shield equivalência)
- [ ] `npm test` full suite PASS (incluindo 34/34 motor regression)
- [ ] `npm run build` exit 0
- [ ] `npm run lint` exit 0
- [ ] Dual gate D-01: motor zero diff + git log --name-only sem invariantes (services/pagamentos.js + services/dividas.js + masks.js)
- [ ] PAUSAs #1 (diff devedorCalc.js), #2 (tests novos), #3 (diff completo submódulo) executadas
- [ ] Smoke local cruzado: UI bate com COMPARISON.md "Depois" per contrato afetado
- [ ] Console F12 limpo
- [ ] 1 commit consolidado no submódulo, ZERO push

**Invariantes inviolaveis cumulative cumpridas:**
- D-01 motor INTOCADO (cumulative com 7.1, 7.5.1, 7.7, 7.8.1, 7.8.2a, 7.9, 7.10a, 7.10.bug, 7.10bcd, 7.13, 7.14, 7.13b)
- services/pagamentos.js + services/dividas.js INTOCADOS
- masks.js INTOCADO (D-pre-3 RESTRITO compliance cumulative desde 7.14)
- autonomous: false (PAUSAs internas + checkpoint final)
- ZERO push
- ZERO modificação fora do escopo declarado
</verification>

<success_criteria>
- 6 arquivos produção + 3 tests modificados num commit consolidado
- 2 tests novos (drift sintético + shield equivalência) PASS
- 34/34 motor regression PASS
- Dual gate D-01 motor INTOCADO confirmado
- Smoke local cruzado: UI exibe valores "Depois" do COMPARISON.md per contrato afetado
- Console F12 limpo
- ZERO push (deploy é Plan 03)
</success_criteria>

<output>
Após completar o plan, criar `.planning/phases/07.13c-encargos-fonte-unica/07.13c-02-SUMMARY.md` com:
- Header (data, plan ID, status SHIPPED ou ABORTED)
- Tasks executadas (1-6) com timestamps
- Diff stat do submódulo (9 arquivos, X insertions, Y deletions)
- Dual gate D-01 confirmação (motor zero diff + name-only sem invariantes)
- Smoke local cruzado per contrato afetado (UI ↔ SQL "Depois" — magnitudes documentadas)
- Memory feedbacks aplicados (schema_adapter + shields_wrapper + strategy_a + gate_motor_grep + grep_global)
- Deviations (se houver fix-forward — descrever causa raiz)
- SHA do commit `feat(07.13c-02)` no submódulo
</output>
