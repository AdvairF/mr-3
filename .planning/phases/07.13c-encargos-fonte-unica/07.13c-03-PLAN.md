---
phase: 07.13c-encargos-fonte-unica
plan: 03
type: execute
wave: 3
depends_on: ["07.13c-02"]
files_modified:
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/phases/07.13c-encargos-fonte-unica/07.13c-LEARNINGS.md
  - .planning/phases/07.13c-encargos-fonte-unica/07.13c-03-SUMMARY.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "Submódulo bumped no pai com chore(07.13c) commit"
    - "Push pai com TODOS commits stacked acumulados"
    - "Tag v1.4-phase7.13c criada e pushed APENAS no pai (chore bump)"
    - "Smoke prod mrcobrancas.com.br: 4 telas exibem saldos corretos para os 3 contratos com drift"
    - "Contrato M L FRIOS exibe multa 2%/honor 10% (Path A — fonte contrato), NÃO 30%/0% (HEAD pre-7.13c)"
    - "Console F12 limpo + Drift PROD vs LOCAL ZERO"
    - "STATE.md + ROADMAP.md atualizados — Phase 7.13c SHIPPED + remoção warning DECISÕES FINANCEIRAS RETROATIVAS"
    - "LEARNINGS.md + SUMMARY 03 gerados"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Snapshot Phase 7.13c SHIPPED listando TODOS commits da phase (memory feedback state_snapshot_must_list_all_phase_commits)"
      contains: "Phase 7.13c.*SHIPPED|v1.4-phase7.13c"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 7.13c entry SHIPPED + warning removido"
      contains: "Phase 7.13c.*SHIPPED"
    - path: ".planning/phases/07.13c-encargos-fonte-unica/07.13c-LEARNINGS.md"
      provides: "Lições transferíveis + memory feedbacks aplicados"
      contains: "D-pre|memory feedback"
  key_links:
    - from: "submódulo commit feat(07.13c-02)"
      to: "pai chore bump + tag v1.4-phase7.13c"
      via: "git add src/mr-3/mr-cobrancas + git commit + git tag"
      pattern: "v1\\.4-phase7\\.13c"
    - from: "tag v1.4-phase7.13c"
      to: "smoke prod 4 telas mrcobrancas.com.br"
      via: "git push origin master + push tag (Vercel auto-deploy)"
      pattern: "smoke prod"
---

<objective>
Deploy Phase 7.13c para produção: bump submódulo no pai, push pai com tag `v1.4-phase7.13c`, smoke prod 4 telas, atualizar STATE.md + ROADMAP.md + gerar LEARNINGS.md, remover warning DECISÕES FINANCEIRAS RETROATIVAS.

**Purpose:** Memory feedback `tag_push_apenas_no_pai_chore_bump` (pai+submódulo compartilham remote URL → tag namespace global; push tag APENAS no pai chore bump). Memory feedback `state_snapshot_must_list_all_phase_commits` (STATE.md status field deve listar TODOS commits da phase). ABORT GATE secundário pode disparar mesmo no Plan 03 se smoke prod reprovar.

**Output:** Deploy bem-sucedido com tag v1.4-phase7.13c em prod + smoke 4 telas PASS + drift PROD↔LOCAL zero + STATE/ROADMAP/LEARNINGS atualizados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.13c-encargos-fonte-unica/07.13c-CONTEXT.md
@.planning/phases/07.13c-encargos-fonte-unica/07.13c-AUDIT-PROD.md
@.planning/phases/07.13c-encargos-fonte-unica/07.13c-COMPARISON.md
@.planning/phases/07.13c-encargos-fonte-unica/07.13c-01-SUMMARY.md
@.planning/phases/07.13c-encargos-fonte-unica/07.13c-02-SUMMARY.md

<commits_stacked_pendentes>
**Sessão atual no pai (cumulativos pendentes pra push pós-7.13c):**
- bf080a2 docs(references) planilha SosCálculos
- dc29203 docs(roadmap) rebaixa 7.10.bug2
- 03e6b65 docs(07.13c) CONTEXT + AUDIT-PROD
- (commits adicionais Plan 01 NEW + Plan 02 NEW + chore bump 7.13c)

Verificar com `git log origin/master..HEAD --oneline` antes de push.
</commits_stacked_pendentes>

<memory_feedbacks_aplicaveis>
- `tag_push_apenas_no_pai_chore_bump` — push tag APENAS no pai chore bump (lição 7.13b Plan 03 Etapa 9)
- `state_snapshot_must_list_all_phase_commits` — STATE.md lista TODOS commits da phase (lição 7.13b Plan 03 handoff)
</memory_feedbacks_aplicaveis>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump submódulo no pai + chore commit + criação tag local</name>
  <read_first>
    - .planning/phases/07.13c-encargos-fonte-unica/07.13c-02-SUMMARY.md (SHA do commit submódulo Plan 02 NEW)
    - $HOME/.claude/projects/.../memory/feedback_tag_push_apenas_no_pai_chore_bump.md
  </read_first>
  <files>(commit pai apenas — bump submódulo + chore message)</files>
  <action>
1. Verificar estado:
```bash
cd C:/Users/advai/Downloads/mr-cobrancas-vercel_1/mr-cobrancas
git status
git submodule status
cd src/mr-3/mr-cobrancas && git log -1 --oneline
```
Esperado: submódulo HEAD = SHA do commit `feat(07.13c-02)`; pai working tree mostra submódulo modified.

2. Stage bump:
```bash
cd C:/Users/advai/Downloads/mr-cobrancas-vercel_1/mr-cobrancas
git add src/mr-3/mr-cobrancas
git status
```
Confirmar único staged change é o submódulo.

3. Chore commit:
```bash
git commit -m "$(cat <<'EOF'
chore(07.13c): bump submodule mr-3 — Phase 7.13c SHIPPED (Path A puro)

Phase 7.13c — Encargos: Contrato como Fonte Única.
Adapter calcularDetalheEncargosContrato refatorado pra Path A puro:
recebe contrato como 1º arg, sobrescreve 7 campos D-pre-1 antes de delegar
ao motor INTOCADO (devedorCalc.js:365-521 zero diff cumulative).

5 consumers cobertos:
- App.jsx populator dividasMap (Estratégia A — D-pre-3.2)
- useSaldoAtualizadoCache.js (recomputeEntry recebe contrato)
- filaDevedor.js (L148 + L241)
- FilaDevedor.jsx (useMemo deps contrato)
- DetalheContrato.jsx (useMemo deps contrato)

Resolve drift sistêmico contratos_dividas vs dividas (3 contratos / 147 dívidas
audit prod 2026-04-29). DECISÕES FINANCEIRAS RETROATIVAS warning removido.

D-01 motor INTOCADO cumulative.
services/pagamentos.js + services/dividas.js + masks.js INTOCADOS cumulative.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

4. Criar tag (memory feedback `tag_push_apenas_no_pai_chore_bump`):
```bash
git tag v1.4-phase7.13c
git tag --list 'v1.4-phase7.13c'
```

5. PAUSA #1 — orchestrator confirma SHA + tag:
```bash
git log -1 --oneline
git rev-parse HEAD
```

6. ZERO push ainda.
  </action>
  <verify>
    <automated>cd C:/Users/advai/Downloads/mr-cobrancas-vercel_1/mr-cobrancas &amp;&amp; git tag --list 'v1.4-phase7.13c' | grep -q "v1.4-phase7.13c" &amp;&amp; git log -1 --format="%s" | grep -q "chore(07.13c)"</automated>
  </verify>
  <acceptance_criteria>
    - `git tag --list 'v1.4-phase7.13c'` retorna `v1.4-phase7.13c`
    - HEAD do pai contém `chore(07.13c)` no subject
    - HEAD do pai inclui apenas mudança no submódulo (`git diff HEAD~1 HEAD --stat` mostra apenas `src/mr-3/mr-cobrancas`)
    - PAUSA #1 executada
  </acceptance_criteria>
  <done>
    - Chore bump commitado no pai
    - Tag v1.4-phase7.13c local
    - PAUSA #1 OK, ZERO push
  </done>
</task>

<task type="auto">
  <name>Task 2: Push pai master + push tag (Vercel auto-deploy)</name>
  <read_first>
    - $HOME/.claude/projects/.../memory/feedback_tag_push_apenas_no_pai_chore_bump.md
    - $HOME/.claude/projects/.../memory/feedback_state_snapshot_must_list_all_phase_commits.md
  </read_first>
  <files>(operação git remota — sem alteração de arquivo)</files>
  <action>
1. Listar commits stacked pendentes (memory feedback `state_snapshot_must_list_all_phase_commits`):
```bash
cd C:/Users/advai/Downloads/mr-cobrancas-vercel_1/mr-cobrancas
git log origin/master..HEAD --oneline
```
Esperado: ≥ 6 commits (commits anteriores + Plan 01 NEW + Plan 02 NEW + chore bump).

2. PAUSA #2 — orchestrator confirma todos commits stacked corretos:
   - bf080a2 + dc29203 (commits anteriores não-7.13c)
   - 03e6b65 (CONTEXT + AUDIT-PROD)
   - commits Plan 01 NEW (header + comparison report)
   - commits Plan 02 NEW (plans + summary 01)
   - chore bump 7.13c (Task 1 deste plan)

3. Push pai com tag (memory feedback `tag_push_apenas_no_pai_chore_bump`):
```bash
git push origin master
git push origin v1.4-phase7.13c
```

4. Verificar push remoto:
```bash
git log origin/master --oneline -10
git ls-remote --tags origin | grep v1.4-phase7.13c
```
Esperado: `origin/master` contém HEAD chore bump + tag listada no remote.

5. **NÃO push tag no submódulo** — pai+submódulo compartilham remote URL → conflito tag namespace:
```bash
cd src/mr-3/mr-cobrancas
# Tag local-only é dev-convenience opcional. Orchestrator escolhe se cria.
git tag v1.4-phase7.13c HEAD  # OPCIONAL local-only
# DO NOT: git push origin v1.4-phase7.13c (conflito com tag pai)
```

6. Aguardar deploy Vercel (~1-3 min):
```bash
# Vercel detecta push pra master e dispara deploy.
# Orchestrator monitora https://vercel.com/<workspace>/mr-cobrancas/deployments até "Ready".
```
PAUSA #3 — orchestrator confirma deploy "Ready" antes de Task 3 smoke.
  </action>
  <verify>
    <automated>cd C:/Users/advai/Downloads/mr-cobrancas-vercel_1/mr-cobrancas &amp;&amp; git ls-remote --tags origin | grep -q "refs/tags/v1.4-phase7.13c" &amp;&amp; git log origin/master -1 --format="%s" | grep -q "chore(07.13c)"</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-remote --tags origin` contém `v1.4-phase7.13c`
    - `git log origin/master -1 --format="%s"` retorna chore bump (sincronizado)
    - Deploy Vercel "Ready" (orchestrator confirma manualmente)
    - PAUSA #2 + PAUSA #3 executadas
  </acceptance_criteria>
  <done>
    - Pai pushed com TODOS commits stacked
    - Tag v1.4-phase7.13c no remote
    - Deploy Vercel "Ready"
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 (CHECKPOINT): Smoke prod 4 telas + console F12 limpo</name>
  <what-built>
    Deploy Vercel "Ready" com:
    - Pai master sincronizado (chore bump 7.13c)
    - Submódulo apontando pra commit `feat(07.13c-02)` (Path A puro)
    - Tag v1.4-phase7.13c no remote
    - Comparison report `07.13c-COMPARISON.md` disponível como referência
  </what-built>
  <how-to-verify>
1. Abrir mrcobrancas.com.br + F12 console.
2. Force refresh duro (Ctrl+Shift+R).
3. **Tela 1 — DetalheContrato M L FRIOS** (UUID 335a2ad2):
   - Login + buscar contrato
   - Resumo Financeiro: saldo bate com `total_depois` da linha M L FRIOS no COMPARISON.md (±R$ 1 tolerância arredondamento INPC)
   - Decomposição: multa 2% + honor 10% (do contrato), NÃO 30%/0% (HEAD pre-7.13c)
4. **Tela 2 — FilaPainel:**
   - Verificar 3 contratos com drift refletem saldo correto
5. **Tela 3 — Pessoas/Detalhe-Devedor:**
   - Buscar devedor da M L FRIOS
   - Drawer dividas: encargos D-pre-1 (multa 2%, honor 10%) — populator funcionando
6. **Tela 4 — Carteira Total Dashboard:**
   - Soma reflete delta agregado dos 3 contratos com drift
7. **Console F12:**
   - Zero erros novos
   - Zero warnings sobre `calcularDetalheEncargosContrato`/`calcularSaldoContratoAtualizado`
   - Network tab: requests Supabase 200 OK
8. **Drift PROD vs LOCAL:**
```bash
cd C:/Users/advai/Downloads/mr-cobrancas-vercel_1/mr-cobrancas
git status   # working tree clean
git fetch origin
git log master..origin/master --oneline   # zero linhas
```
9. Decisão final:
   - **PASS:** 4 telas PASS + console limpo + drift zero. Plan 03 SHIPPED. Continuar Task 4.
   - **FAIL fix-forward:** UI difere por arredondamento mínimo OU bug isolado em 1 callsite.
   - **FAIL ABORT:** delta surpreende em prod. Revert chore bump + tag + reciclar phase.
  </how-to-verify>
  <resume-signal>
    Type "SMOKE PROD PASS — Phase 7.13c SHIPPED" pra continuar Task 4.
    Type "SMOKE PROD FAIL — fix-forward: <descrever issue>" pra fix-forward.
    Type "SMOKE PROD FAIL — ABORT" pra reverter + reciclar phase.
  </resume-signal>
  <acceptance_criteria>
    - Orchestrator declarou "SMOKE PROD PASS" OU "FAIL fix-forward" OU "FAIL ABORT"
    - Se PASS: declaração inclui per-tela confirmação visual (4 telas)
    - Console F12 limpo (zero erros novos)
    - `git status` clean working tree
    - `git log master..origin/master --oneline` retorna zero linhas
  </acceptance_criteria>
  <done>
    - Decisão final: PASS / FAIL fix-forward / FAIL ABORT
    - Se PASS: prosseguir Task 4
    - Se FAIL: fix-forward OU revert+reciclar
  </done>
</task>

<task type="auto">
  <name>Task 4: Update STATE.md + ROADMAP.md + gerar LEARNINGS.md + SUMMARY 03 + commit docs final</name>
  <read_first>
    - .planning/STATE.md (frontmatter + Roadmap Evolution)
    - .planning/ROADMAP.md (linha 423 entry Phase 7.13c)
    - .planning/phases/07.13c-encargos-fonte-unica/07.13c-CONTEXT.md (D-pre-1..D-pre-7)
    - .planning/phases/07.13c-encargos-fonte-unica/07.13c-COMPARISON.md (delta financeiro factual)
    - .planning/phases/07.13c-encargos-fonte-unica/07.13c-01-SUMMARY.md + 07.13c-02-SUMMARY.md
    - $HOME/.claude/projects/.../memory/feedback_state_snapshot_must_list_all_phase_commits.md
  </read_first>
  <files>
    .planning/STATE.md,
    .planning/ROADMAP.md,
    .planning/phases/07.13c-encargos-fonte-unica/07.13c-LEARNINGS.md (criação),
    .planning/phases/07.13c-encargos-fonte-unica/07.13c-03-SUMMARY.md (criação)
  </files>
  <action>
1. **STATE.md** — atualizar frontmatter `status` field listando TODOS commits da phase (memory feedback `state_snapshot_must_list_all_phase_commits`):
   - Substituir bloco `status:` por descrição "Phase 7.13c SHIPPED — Encargos: Contrato como Fonte Única (Path A puro). Tag v1.4-phase7.13c em <SHA-chore-bump> pai. Smoke prod mrcobrancas.com.br PASS — 4 telas. Console F12 limpo. Drift PROD vs LOCAL: ZERO. <N> commits no pai listados (orchestrator preenche SHAs): <SHA1> CONTEXT+AUDIT, <SHA2> Plan 01 NEW header, <SHA3> Plan 01 NEW comparison, <SHA4> Plan 02 NEW plans, <SHA5> Plan 03 plan, <SHA-chore-bump> chore bump, <SHA-docs-final> docs final. ⚠ DECISÕES FINANCEIRAS RETROATIVAS — REMOVIDO. 9 memory feedbacks aplicados. D-01 motor INTOCADO cumulative confirmed."
   - Atualizar `last_updated: "2026-04-29"` e `last_activity:` com sumário breve.
   - Adicionar linha em `## Roadmap Evolution`:
     ```
     - Phase 7.13c SHIPPED 2026-04-29 — tag v1.4-phase7.13c em <SHA> pai (chore bump). Smoke prod mrcobrancas.com.br PASS — 4 telas. Path A puro implementado: adapter calcularDetalheEncargosContrato refatorado pra contrato como 1º arg + sobrescrever 7 campos D-pre-1 antes de delegar ao motor INTOCADO. 5 consumers cobertos. 9 memory feedbacks aplicados. ⚠ DECISÕES FINANCEIRAS RETROATIVAS — REMOVIDO.
     ```

2. **ROADMAP.md** (linha 423) — atualizar entry Phase 7.13c:
   - Mudar header `(INSERTED)` para `(SHIPPED)`
   - Remover bloco `⚠ DECISÕES FINANCEIRAS RETROATIVAS` (warning não mais ativo)
   - Atualizar `**Status:**` para `**SHIPPED** 2026-04-29 — tag v1.4-phase7.13c em <SHA> pai (chore bump). Submódulo <SHA-Plan02> (Plan 02 NEW). Smoke prod mrcobrancas.com.br PASS — 4 telas (DetalheContrato M L FRIOS multa 2%/honor 10% confirmado + FilaPainel + Pessoas + Carteira Total). Console F12 limpo. Drift PROD vs LOCAL: ZERO. Path A puro entregue: adapter recebe contrato como 1º arg + sobrescreve 7 campos D-pre-1 + delega ao motor INTOCADO. 5 consumers cobertos. 9 memory feedbacks aplicados. D-01 motor INTOCADO cumulative confirmed.`
   - Atualizar lista `**Tags v1.4 ativas**` para incluir 7.13c (16 tags totais).

3. **Criar LEARNINGS.md** em `.planning/phases/07.13c-encargos-fonte-unica/07.13c-LEARNINGS.md` com:
   - Header (status SHIPPED, tag, data)
   - Tabela D-pre lockeds resumo (D-pre-1..D-pre-7.3 conforme CONTEXT.md)
   - Lições transferíveis (5+):
     1. Fail-fast SQL antes de refactor (reordenação Plan 01 NEW SQL+ABORT antes de Plan 02 NEW refactor)
     2. SQL puro como fonte de verdade auditável (D-pre-7.1)
     3. Path A puro escala melhor que Paths B/C (Q1+Q2 audit destrava)
     4. Populator + adapter sem duplicação (Strategy A delegation pattern)
     5. Shield equivalência adapter↔motor obrigatório (caso degenerado zero drift)
   - Patterns reutilizáveis (2+):
     1. Pre-execute SELECT prévio de pagamentos (decisão Art.354)
     2. Controle ZERO drift no comparison report (sanity check intrínseco do SQL)
   - Memory feedbacks aplicados (9 listados):
     schema_adapter_long_short_form_consumer_replication, uat_humano_pega_bugs_que_gates_automatizados_nao_pegam, memory_must_factcheck_codebase_before_claiming_severity, shields_wrapper_must_test_equivalence, strategy_a_shared_wrapper_delegation, gate_motor_grep_use_name_only, grep_global_antes_de_fix_pontual, tag_push_apenas_no_pai_chore_bump, state_snapshot_must_list_all_phase_commits
   - Surprises (orchestrator preenche pós-execução)

4. **Criar SUMMARY 03** em `.planning/phases/07.13c-encargos-fonte-unica/07.13c-03-SUMMARY.md` com:
   - Header (status, tag, pai SHA, submódulo SHA)
   - Tasks executadas (1-4) com timestamps + decisões
   - Smoke prod 4 telas (per-tela confirmação visual)
   - Drift PROD vs LOCAL ZERO confirmação
   - Memory feedbacks Plan 03 aplicados
   - Eventuais deviations / fix-forwards (se houver)

5. Commit final docs no pai:
```bash
cd C:/Users/advai/Downloads/mr-cobrancas-vercel_1/mr-cobrancas
git add .planning/STATE.md .planning/ROADMAP.md .planning/phases/07.13c-encargos-fonte-unica/07.13c-LEARNINGS.md .planning/phases/07.13c-encargos-fonte-unica/07.13c-03-SUMMARY.md
git commit -m "$(cat <<'EOF'
docs(07.13c-03): SUMMARY 03 + LEARNINGS + STATE/ROADMAP — Phase 7.13c CLOSED

Phase 7.13c SHIPPED 2026-04-29 — Encargos: Contrato como Fonte Única (Path A puro).
Smoke prod mrcobrancas.com.br PASS — 4 telas. Drift PROD vs LOCAL: ZERO.
Tag v1.4-phase7.13c. DECISÕES FINANCEIRAS RETROATIVAS warning removido.

9 memory feedbacks aplicados.
D-01 motor INTOCADO cumulative confirmed (linhas 365-521 zero diff).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

6. Push final:
```bash
git push origin master
git log origin/master -1 --format="%s"
```

7. PAUSA #4 — orchestrator confirma push docs final + STATE/ROADMAP corretos.
  </action>
  <verify>
    <automated>cd C:/Users/advai/Downloads/mr-cobrancas-vercel_1/mr-cobrancas &amp;&amp; test -f .planning/phases/07.13c-encargos-fonte-unica/07.13c-LEARNINGS.md &amp;&amp; test -f .planning/phases/07.13c-encargos-fonte-unica/07.13c-03-SUMMARY.md &amp;&amp; grep -q "Phase 7.13c.*SHIPPED" .planning/STATE.md &amp;&amp; grep -q "SHIPPED" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/07.13c-encargos-fonte-unica/07.13c-LEARNINGS.md` existe
    - `.planning/phases/07.13c-encargos-fonte-unica/07.13c-03-SUMMARY.md` existe
    - `grep -q "Phase 7.13c.*SHIPPED" .planning/STATE.md` retorna 0
    - `grep "Phase 7.13c" .planning/ROADMAP.md` mostra status SHIPPED (não INSERTED)
    - `grep "DECISÕES FINANCEIRAS RETROATIVAS" .planning/ROADMAP.md` retorna 0 linhas (warning removido)
    - LEARNINGS.md lista 9 memory feedbacks aplicados (`grep -c "memory feedback\|memory_feedback\|feedback_" .planning/phases/07.13c-encargos-fonte-unica/07.13c-LEARNINGS.md` ≥ 9)
    - Commit `docs(07.13c-03)` no pai pushed
    - PAUSA #4 executada
  </acceptance_criteria>
  <done>
    - STATE.md + ROADMAP.md atualizados
    - LEARNINGS.md + SUMMARY 03 criados
    - Commit docs final pushed
    - Phase 7.13c CLOSED
  </done>
</task>

</tasks>

<verification>
**Quality gates do plano:**

- [ ] Submódulo bumped no pai com `chore(07.13c)` commit
- [ ] Tag `v1.4-phase7.13c` criada no pai chore bump (memory feedback `tag_push_apenas_no_pai_chore_bump`)
- [ ] Push pai com TODOS commits stacked acumulados (`git log origin/master..HEAD --oneline` zero linhas pós-push)
- [ ] Tag `v1.4-phase7.13c` listada no remote (`git ls-remote --tags origin | grep v1.4-phase7.13c`)
- [ ] Tag NÃO pushed no submódulo (convenção projeto)
- [ ] Smoke prod 4 telas PASS (DetalheContrato + FilaPainel + Pessoas + Carteira Total Dashboard)
- [ ] Console F12 limpo (zero erros novos)
- [ ] Drift PROD vs LOCAL ZERO (`git status` clean + `git log master..origin/master` zero linhas)
- [ ] STATE.md atualizado (Phase 7.13c SHIPPED + commits listados — memory feedback `state_snapshot_must_list_all_phase_commits`)
- [ ] ROADMAP.md atualizado (status SHIPPED + warning DECISÕES FINANCEIRAS RETROATIVAS REMOVIDO)
- [ ] LEARNINGS.md gerado (D-pre cumulative + lições + 9 memory feedbacks)
- [ ] SUMMARY 03 gerado
- [ ] PAUSAs #1, #2, #3, #4 executadas

**Invariantes inviolaveis cumulative cumpridas:**
- D-01 motor INTOCADO (cumulative com 7.1, 7.5.1, 7.7, 7.8.1, 7.8.2a, 7.9, 7.10a, 7.10.bug, 7.10bcd, 7.13, 7.14, 7.13b, 7.13c)
- services/pagamentos.js + services/dividas.js + masks.js INTOCADOS
- autonomous: false (PAUSAs internas + checkpoint smoke prod)
- Push só ao final (Plan 03), não em Plans 01 NEW + 02 NEW

**ABORT GATE secundário:** smoke prod reprovação cíclica → revert chore bump + tag + phase recicla pra `/gsd-discuss-phase`.
</verification>

<success_criteria>
- Tag v1.4-phase7.13c em prod
- Smoke prod 4 telas PASS
- Console F12 limpo
- Drift PROD vs LOCAL ZERO
- STATE.md + ROADMAP.md atualizados
- LEARNINGS.md gerado com 9 memory feedbacks aplicados
- SUMMARY 03 gerado
- DECISÕES FINANCEIRAS RETROATIVAS warning REMOVIDO
- Phase 7.13c CLOSED
</success_criteria>

<output>
Após completar o plan, `.planning/phases/07.13c-encargos-fonte-unica/07.13c-03-SUMMARY.md` deve conter:
- Header (data 2026-04-29, plan ID 07.13c-03, status SHIPPED, tag v1.4-phase7.13c, pai SHA, submódulo SHA)
- Tasks executadas (1-4) com timestamps
- Smoke prod 4 telas (per-tela: DetalheContrato M L FRIOS, FilaPainel, Pessoas, Dashboard) com confirmação visual
- Drift PROD vs LOCAL ZERO (git status clean + zero divergence)
- Memory feedbacks Plan 03 aplicados (tag_push_apenas_no_pai_chore_bump + state_snapshot_must_list_all_phase_commits)
- Lista commits da phase (Plan 01 NEW + Plan 02 NEW + chore bump + docs final)
- Eventuais deviations / fix-forwards
- Decisão final: SHIPPED ou ABORTED
</output>
