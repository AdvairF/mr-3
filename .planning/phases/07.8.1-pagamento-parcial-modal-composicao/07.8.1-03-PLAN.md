---
phase: 07.8.1-pagamento-parcial-modal-composicao
plan: 02
type: execute
wave: 2
depends_on:
  - 07.8.1-01
files_modified:
  - src/mr-3/mr-cobrancas
autonomous: false
requirements: []
tags:
  - uat
  - submodule-bump
  - commit-pai
  - checkpoint
  - diff-review

must_haves:
  truths:
    - "UAT SC-1 (contrato com pagamento → linha aparece em vermelho entre Valor Original e Correção) validado pelo usuário em localhost"
    - "UAT SC-2 (Copiar inclui a linha no clipboard + Imprimir mostra a linha em vermelho no A4) validado pelo usuário em localhost"
    - "UAT SC-3 (contrato sem pagamento → linha NÃO aparece) validado pelo usuário em localhost"
    - "Commit `chore(07.8.1): bump submodule mr-3 — pagamento parcial no modal (07.8.1-01)` existe no pai master"
    - "Tag `v1.4-phase7.8.1` NÃO é criada nesta phase (padrão 7.5.1/7.6/7.7/7.8)"
    - "Zero `git push` executado sem autorização explícita em PAUSA #3"
  artifacts:
    - path: ".planning/phases/07.8.1-pagamento-parcial-modal-composicao/07.8.1-02-SUMMARY.md"
      provides: "Resultado de UAT 3 SCs + hash do bump no pai + registro de autorização do push"
      contains: "SC-1"
      contains_bump: "chore(07.8.1)"
  key_links:
    - from: "Submódulo mr-3 branch main commit feat(07.8.1-01)"
      to: "Pai master commit chore(07.8.1) via `git add src/mr-3/mr-cobrancas`"
      via: "git submodule update + git commit no pai"
      pattern: "chore\\(07\\.8\\.1\\): bump submodule mr-3 — pagamento parcial"
    - from: "UAT SC-1..SC-3 reportado pelo usuário"
      to: "07.8.1-02-SUMMARY.md com PASS/FAIL por SC"
      via: "PAUSA #1 interactive"
      pattern: "SC-[123]: (PASS|FAIL)"
---

<objective>
Validar em localhost os 3 cenários de UAT (D-14), revisar o diff consolidado do commit do submódulo (PAUSA #2), revisar e autorizar o bump no pai master (PAUSA #3). Sem tag nesta phase (D-16 — tag fica para ação separada pós-UAT prod).

Purpose: Garantir que a mudança cirúrgica aplicada em 07.8.1-01 produz comportamento correto no navegador (render React + Clipboard + Print A4) e não quebra cenários sem pagamento. O bump do pai só acontece APÓS UAT verde — se algum SC falhar, o plan PARA e gera gap closure. Esta phase espelha o pattern 7.5.1/7.8 de UAT + bump duplo checkpoint, com adaptação para 3 pausas (escopo cirúrgico não justifica 5 pausas de modo cansado).

Output:
- Confirmação do usuário (PAUSA #1) de SC-1, SC-2, SC-3 em localhost
- Confirmação do usuário (PAUSA #2) sobre o diff consolidado do commit do submódulo
- Autorização do usuário (PAUSA #3) para commitar o bump no pai
- Commit `chore(07.8.1): bump submodule mr-3 — pagamento parcial no modal (07.8.1-01)` no pai master
- Zero `git push` executado sem confirmação explícita
- Zero tag criada nesta phase
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07.8.1-pagamento-parcial-modal-composicao/07.8.1-CONTEXT.md
@.planning/phases/07.8.1-pagamento-parcial-modal-composicao/07.8.1-01-SUMMARY.md
@src/mr-3/mr-cobrancas/src/components/DecomposicaoSaldoModal.jsx
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: PAUSA #1 — UAT localhost (SC-1, SC-2, SC-3)</name>

  <what-built>
    Plan 07.8.1-01 adicionou ao `DecomposicaoSaldoModal.jsx`:
    - Extração `const totalPago = Number(d.totalPago || 0)` no bloco de extrações
    - Push condicional `if (totalPago > 0) rows.push({ label: "Pagamento parcial", value: -totalPago, signed: true, bold: false })` entre "Valor Original" e "Correção"
    - `totalPago` adicionado às deps do `useMemo` de `linhas`

    Comportamento esperado:
    - Modal de composição renderiza nova linha "Pagamento parcial" em vermelho (`#dc2626`) entre "Valor Original" e "Correção" QUANDO totalPago > 0
    - Linha some quando contrato não tem pagamento (`totalPago === 0`)
    - Botão "📋 Copiar" inclui a linha no texto formatado
    - Botão "🖨️ Imprimir" renderiza a linha em vermelho no A4 (via `bodyRowsHtml` existente)
  </what-built>

  <read_first>
    Antes da PAUSA, executar `cd src/mr-3/mr-cobrancas && npm run dev` (ou instruir o usuário a executar se já estiver rodando). Capturar URL localhost (geralmente http://localhost:5173).
  </read_first>

  <how-to-verify>
    **Preparar ambiente:**
    ```bash
    cd src/mr-3/mr-cobrancas && npm run dev
    ```
    Abrir http://localhost:5173 (ou URL que o Vite imprimir) no navegador, autenticar, navegar até a lista de contratos.

    **SC-1 — Contrato COM pagamento (linha aparece vermelha):**
    1. Selecionar um contrato que tenha 1+ pagamento registrado em `pagamentos_divida` (pode usar contrato de teste TJGO ou qualquer contrato com histórico de pagamento parcial)
    2. Abrir a modal "Composição do Saldo Atualizado" clicando na 4ª coluna do Resumo Financeiro
    3. Verificar:
       - [ ] Linha **"Pagamento parcial"** aparece na tabela de decomposição
       - [ ] Posição: IMEDIATAMENTE abaixo de "Valor Original"
       - [ ] Posição: IMEDIATAMENTE acima de "Correção" (ou de Multa/Juros se não houver Correção)
       - [ ] Valor exibido no formato `- R$ X,XX` (prefixo `- ` com espaço)
       - [ ] Cor do valor é vermelha (tom `#dc2626`)
       - [ ] Label está sem parênteses ou símbolos (`(−)`, `(-)` NÃO podem aparecer)

    **SC-2 — Copiar + Imprimir (propagação para clipboard e A4):**
    1. Com modal aberto (contrato do SC-1), clicar em **"📋 Copiar"**
    2. Verificar toast "Copiado!" aparece
    3. Colar (Ctrl+V) em qualquer editor de texto (Bloco de Notas, VSCode)
    4. Verificar:
       - [ ] Linha presente no texto: `+ Pagamento parcial: - R$ X,XX`
       - [ ] Linha aparece ENTRE "Valor Original" e "Correção" (ou próximo componente) no texto
    5. Fechar editor, voltar à modal, clicar em **"🖨️ Imprimir"**
    6. Nova aba abre com versão A4 (Times New Roman, @page A4)
    7. Verificar:
       - [ ] Linha "Pagamento parcial" visível na tabela do A4
       - [ ] Valor renderizado em vermelho (`color: #dc2626` inline)
       - [ ] Label sem parênteses
       - [ ] Ctrl+P abre print dialog mostrando preview A4 (opcional, sanity check)
    8. Fechar aba do print

    **SC-3 — Contrato SEM pagamento (hide zero-value):**
    1. Selecionar um contrato SEM pagamentos registrados (ou criar um novo de teste)
    2. Abrir a modal "Composição do Saldo Atualizado"
    3. Verificar:
       - [ ] Linha "Pagamento parcial" **NÃO aparece**
       - [ ] Ordem da tabela é: Valor Original → Correção → Multa → Juros → Honorários → ... → Saldo Atualizado (fluxo pré-07.8.1 preservado)
    4. Clicar em "📋 Copiar" e colar em editor
       - [ ] Texto NÃO contém "Pagamento parcial"
    5. Clicar em "🖨️ Imprimir" e inspecionar A4
       - [ ] A4 NÃO contém linha "Pagamento parcial"

    **Reportar resultado:**
    Para CADA SC, responder PASS ou FAIL. Se FAIL, descrever o que foi observado vs. o esperado (screenshot opcional mas recomendado).
  </how-to-verify>

  <resume-signal>
    Responder com:
    - **"SC-1: PASS, SC-2: PASS, SC-3: PASS — seguir para PAUSA #2"** (caminho feliz — prosseguir para Task 2)
    - **"SC-X: FAIL — [descrição]"** (plan PARA; gerar gap closure via `/gsd-plan-phase --gaps` ou edição direta no plan 07.8.1-01)

    Não prosseguir sem os 3 SCs reportados explicitamente.
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: PAUSA #2 — Revisão do diff consolidado do commit do submódulo</name>

  <what-built>
    Plan 07.8.1-01 produziu exatamente 1 commit no submódulo `src/mr-3/mr-cobrancas` branch main:
    - `feat(07.8.1-01): linha Pagamento parcial no DecomposicaoSaldoModal`

    Esta PAUSA revisa o diff consolidado desse único commit antes de autorizar o bump no pai. Serve como último sanity check de escopo (D-13: exatamente 1 arquivo, ~10-15 linhas).
  </what-built>

  <read_first>
    Executar para preparar a revisão:
    ```bash
    cd src/mr-3/mr-cobrancas
    git log -1 --pretty="%h %s"
    git show HEAD --stat
    git show HEAD -- src/components/DecomposicaoSaldoModal.jsx
    cd ../../..
    ```
    Imprimir a saída completa ao usuário ANTES de pedir aprovação.
  </read_first>

  <how-to-verify>
    **Diff consolidado do submódulo** (output de `git show HEAD`):

    Usuário revisa:
    1. **Subject line** — deve ser EXATAMENTE: `feat(07.8.1-01): linha Pagamento parcial no DecomposicaoSaldoModal`
       - [ ] Typo-free, com prefixo correto
    2. **Escopo de arquivos** — deve ser EXATAMENTE 1 arquivo: `src/components/DecomposicaoSaldoModal.jsx`
       - [ ] `git show HEAD --stat` lista 1 file changed
       - [ ] Nenhum arquivo extra (devedorCalc.js, pagamentos.js, Modal.jsx, test files NÃO aparecem)
    3. **Linhas adicionadas** — deve estar na faixa ~10 a ~20 linhas adicionadas (esperado: ~12-15 novas linhas entre extração, push block, deps)
       - [ ] Sem linhas removidas substanciais (só o `}, [...]);` da deps com append)
    4. **Conteúdo do diff** — confirmar visualmente:
       - [ ] `+  const totalPago = Number(d.totalPago || 0);` presente
       - [ ] `+  if (totalPago > 0) { rows.push({ label: "Pagamento parcial", value: -totalPago, signed: true, bold: false }); }` presente (expandido em múltiplas linhas)
       - [ ] Array de deps do useMemo com `totalPago` incluído
       - [ ] Nenhuma mudança fora do `DecomposicaoSaldoModal.jsx`
       - [ ] Nenhuma reformatação de linhas adjacentes (só adições limpas)

    **Decisão do usuário:**
    Se o diff estiver OK → aprovar PAUSA #2
    Se houver problema → descrever e retornar para 07.8.1-01 (gap fix)
  </how-to-verify>

  <resume-signal>
    Responder com:
    - **"PAUSA #2 aprovada — prosseguir para bump no pai"** (Task 3 executa)
    - **"PAUSA #2 reprovada: [razão]"** (plan PARA; fix no submódulo)
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Criar commit de bump no pai master (sem push)</name>
  <files>src/mr-3/mr-cobrancas (gitlink updated)</files>

  <read_first>
    Do diretório raiz do pai, confirmar estado do submódulo ANTES do commit:
    ```bash
    git status -s src/mr-3/mr-cobrancas
    # esperado: " M src/mr-3/mr-cobrancas" (gitlink pendente)
    git diff src/mr-3/mr-cobrancas
    # esperado: linhas tipo "-Subproject commit OLD" / "+Subproject commit NEW"
    ```
    Capturar o novo hash do submódulo:
    ```bash
    cd src/mr-3/mr-cobrancas && git rev-parse HEAD && cd ../../..
    ```
  </read_first>

  <action>
    Do diretório raiz do repo pai (NÃO do submódulo), executar EXATAMENTE:

    ```bash
    git add src/mr-3/mr-cobrancas
    git commit -m "chore(07.8.1): bump submodule mr-3 — pagamento parcial no modal (07.8.1-01)"
    ```

    Validar pós-commit:
    ```bash
    git log -1 --pretty="%h %s"
    # esperado: <hash> chore(07.8.1): bump submodule mr-3 — pagamento parcial no modal (07.8.1-01)
    git show HEAD --stat
    # esperado: 1 file changed -> src/mr-3/mr-cobrancas | 2 ++-
    ```

    **NÃO executar `git push`** — push requer PAUSA #3 de autorização explícita.
    **NÃO criar tag `v1.4-phase7.8.1`** (D-16 — tag fica para ação separada pós-UAT prod).
    **NÃO executar `git tag`, `git push --tags`, `gh release`** — zero tags nesta phase.
  </action>

  <acceptance_criteria>
    1. **Commit pai existe com subject exato**:
       ```bash
       git log -1 --pretty=%s
       # esperado: chore(07.8.1): bump submodule mr-3 — pagamento parcial no modal (07.8.1-01)
       ```

    2. **Escopo do commit = apenas gitlink do submódulo**:
       ```bash
       git show HEAD --stat | grep -c "src/mr-3/mr-cobrancas"
       # esperado: >= 1 (linha do submódulo)
       git show HEAD --name-only | grep -v "^commit\|^Author\|^Date\|^$\|^    "
       # esperado: apenas "src/mr-3/mr-cobrancas" (ou equivalente git submodule path)
       ```

    3. **Gitlink HEAD do submódulo = hash do commit de 07.8.1-01**:
       ```bash
       git ls-tree HEAD src/mr-3/mr-cobrancas
       # formato: 160000 commit <SUB_HASH>  src/mr-3/mr-cobrancas
       # comparar com: cd src/mr-3/mr-cobrancas && git rev-parse HEAD && cd ../../..
       # devem ser IGUAIS
       ```

    4. **Zero push executado**:
       ```bash
       git log @{upstream}..HEAD --oneline
       # esperado: 1 ou mais linhas (inclui o novo bump + qualquer commit anterior ainda não pushado)
       # Qualquer saída vazia significaria push já foi feito (BUG).
       ```

    5. **Zero tag criada para esta phase**:
       ```bash
       git tag --list "v1.4-phase7.8.1*"
       # esperado: saída vazia
       ```

    6. **Working tree limpo após commit**:
       ```bash
       git status -s src/mr-3/mr-cobrancas
       # esperado: saída vazia (gitlink já commitado)
       ```
  </acceptance_criteria>

  <verify>
    <automated>git log -1 --pretty=%s | grep -q "chore(07.8.1): bump submodule mr-3 — pagamento parcial no modal (07.8.1-01)" &amp;&amp; git tag --list "v1.4-phase7.8.1*" | wc -l | grep -q "^0$" &amp;&amp; git status -s src/mr-3/mr-cobrancas | wc -l | grep -q "^0$"</automated>
  </verify>

  <done>
    - Commit `chore(07.8.1): bump submodule mr-3 — pagamento parcial no modal (07.8.1-01)` existe em `HEAD` do pai master
    - Gitlink do submódulo aponta para o hash de 07.8.1-01
    - Working tree limpo (sem dirty no submódulo)
    - Zero tag `v1.4-phase7.8.1*` criada
    - Zero `git push` executado
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: PAUSA #3 — Revisão do bump do pai + autorização de push (ou hold)</name>

  <what-built>
    Task 3 criou o commit `chore(07.8.1): bump submodule mr-3 — pagamento parcial no modal (07.8.1-01)` no pai master. Esta PAUSA permite o usuário revisar o estado final (submódulo commit + pai bump) e decidir se autoriza o push para o remoto (`origin main` no submódulo + `origin master` no pai) ou mantém tudo apenas local por enquanto.

    Dado que esta é a 7.8.1 (sub-phase cirúrgica) e o precedente 7.5.1 deployed OK, a autorização de push é provável — mas NUNCA automática.
  </what-built>

  <read_first>
    Executar para mostrar o estado final consolidado ao usuário:
    ```bash
    # Submódulo
    cd src/mr-3/mr-cobrancas
    echo "=== Submódulo HEAD ==="
    git log -1 --pretty="%h %s"
    echo "=== Submódulo commits à frente do upstream ==="
    git log @{upstream}..HEAD --oneline
    cd ../../..

    # Pai
    echo "=== Pai HEAD ==="
    git log -1 --pretty="%h %s"
    echo "=== Pai commits à frente do upstream ==="
    git log @{upstream}..HEAD --oneline

    # Tags
    echo "=== Tags 7.8.1 ==="
    git tag --list "*7.8.1*"
    cd src/mr-3/mr-cobrancas && git tag --list "*7.8.1*" && cd ../../..
    ```
    Imprimir todas as saídas ao usuário.
  </read_first>

  <how-to-verify>
    **Revisão do estado final:**

    Usuário confirma:
    1. **Subject do commit do submódulo** (pai + filho):
       - [ ] Submódulo: `feat(07.8.1-01): linha Pagamento parcial no DecomposicaoSaldoModal`
       - [ ] Pai: `chore(07.8.1): bump submodule mr-3 — pagamento parcial no modal (07.8.1-01)`
    2. **Ambos commits à frente do upstream** (nada pushado ainda):
       - [ ] Submódulo tem N commits em `@{upstream}..HEAD` (N >= 1)
       - [ ] Pai tem N commits em `@{upstream}..HEAD` (N >= 1)
    3. **Zero tag `v1.4-phase7.8.1` criada** (D-16):
       - [ ] `git tag --list "*7.8.1*"` retorna vazio em ambos os repos
    4. **Working tree limpo** (nada pendente):
       - [ ] `git status -s` no pai = vazio
       - [ ] `git status -s` no submódulo = vazio

    **Decisão do usuário (3 caminhos):**

    **Caminho A — Autorizar push AGORA:**
    Usuário responde: `"autorizar push — submódulo primeiro, depois pai"`.
    Agente executa EXATAMENTE:
    ```bash
    cd src/mr-3/mr-cobrancas && git push origin main && cd ../../..
    git push origin master
    ```
    Após push, confirmar no output:
    - Submódulo: nenhum erro, novo hash remoto aponta para o commit de 07.8.1-01
    - Pai: nenhum erro, novo hash remoto aponta para o bump

    **Caminho B — Hold push (deixar apenas local):**
    Usuário responde: `"segurar push — UAT prod depois"`.
    Agente NÃO executa push. Registra em summary que push está pendente de autorização posterior.

    **Caminho C — Rollback:**
    Usuário responde: `"rollback do bump"`.
    Agente executa:
    ```bash
    git reset --hard HEAD~1
    ```
    E abre gap closure para revisar o plan.

    **Em todos os 3 caminhos:** NÃO criar tag `v1.4-phase7.8.1` (D-16).
  </how-to-verify>

  <resume-signal>
    Responder com EXATAMENTE uma das três:
    - **"autorizar push — submódulo primeiro, depois pai"** (Caminho A — push em ordem)
    - **"segurar push — UAT prod depois"** (Caminho B — apenas local)
    - **"rollback do bump"** (Caminho C — reset --hard HEAD~1 no pai)
  </resume-signal>
</task>

</tasks>

<verification>
**Ao fim do plan, os seguintes invariantes devem valer:**

1. **UAT SC-1..SC-3** — reportados PASS pelo usuário em PAUSA #1 (registrados no summary).

2. **Diff do submódulo aprovado** — PAUSA #2 resolvida em OK (registrado no summary).

3. **Commit do pai existe com subject exato**:
   ```bash
   git log -1 --pretty=%s
   # esperado: chore(07.8.1): bump submodule mr-3 — pagamento parcial no modal (07.8.1-01)
   ```

4. **Gitlink coerente** (submódulo HEAD = bump target):
   ```bash
   PAIHASH=$(git ls-tree HEAD src/mr-3/mr-cobrancas | awk '{print $3}')
   SUBHASH=$(cd src/mr-3/mr-cobrancas && git rev-parse HEAD && cd ../../..)
   [ "$PAIHASH" = "$SUBHASH" ] && echo OK || echo MISMATCH
   # esperado: OK
   ```

5. **Tag NÃO criada** (D-16):
   ```bash
   git tag --list "v1.4-phase7.8.1*"
   cd src/mr-3/mr-cobrancas && git tag --list "v1.4-phase7.8.1*" && cd ../../..
   # ambos: saída vazia
   ```

6. **Push state conforme decisão de PAUSA #3** (caminho A/B/C documentado no summary).

7. **Working tree limpo**:
   ```bash
   git status -s
   # esperado: vazia (ou apenas arquivos não relacionados a esta phase)
   ```
</verification>

<success_criteria>
- [x] UAT SC-1 reportado pelo usuário (PASS ou FAIL)
- [x] UAT SC-2 reportado pelo usuário (PASS ou FAIL)
- [x] UAT SC-3 reportado pelo usuário (PASS ou FAIL)
- [x] PAUSA #2 aprovada (diff consolidado do submódulo) OU rollback executado
- [x] Commit pai `chore(07.8.1): bump submodule mr-3 — pagamento parcial no modal (07.8.1-01)` criado
- [x] Gitlink HEAD do submódulo = hash de 07.8.1-01 (match verificável via `git ls-tree`)
- [x] PAUSA #3 resolvida com uma das 3 decisões (autorizar push / segurar / rollback)
- [x] Tag `v1.4-phase7.8.1` NÃO criada nesta phase (D-16)
- [x] Zero `git push` não autorizado
- [x] Working tree limpo ao final
</success_criteria>

<output>
Após completar, criar `.planning/phases/07.8.1-pagamento-parcial-modal-composicao/07.8.1-02-SUMMARY.md` com:
- **UAT report**: SC-1, SC-2, SC-3 com status PASS/FAIL e observações do usuário
- **PAUSA #2 outcome**: aprovado ou reprovado (+ razão)
- **Commit do pai**: hash curto + subject exato + output de `git show HEAD --stat`
- **Gitlink check**: hash do submódulo no pai HEAD vs. hash no HEAD do submódulo (deve match)
- **PAUSA #3 outcome**: Caminho A (push executado com hashes remotos) / B (hold, pendente UAT prod) / C (rollback)
- **Tag check**: `git tag --list "*7.8.1*"` no pai e no submódulo (esperado vazio)
- **Phase status final**: "Shipped — aguarda UAT prod" (A) ou "Local only — aguarda autorização" (B) ou "Rollback — retornou a 07.8.1-01 gap" (C)
- **Próximos passos**: tag `v1.4-phase7.8.1` criada em ação separada pós-UAT prod (D-16); Phase 7.8.2 (Saldo na Listagem) segue no backlog
</output>
