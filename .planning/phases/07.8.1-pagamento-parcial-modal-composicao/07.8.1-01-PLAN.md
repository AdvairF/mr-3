---
phase: 07.8.1-pagamento-parcial-modal-composicao
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mr-3/mr-cobrancas/src/components/DecomposicaoSaldoModal.jsx
autonomous: false   # PAUSA #0: diff review pré-commit — padrão histórico 7.2/7.3/7.5/7.6/7.7/7.8 (escopo cirúrgico não elimina risco de bug sutil; +1min review humano)
requirements: []
tags:
  - ui
  - modal
  - composicao-divida
  - pagamento-parcial
  - art354
  - submodule-mr-3

must_haves:
  truths:
    - "Quando contrato tem pagamento (totalPago > 0), o modal mostra linha 'Pagamento parcial' com valor negativo em vermelho entre 'Valor Original' e 'Correção'"
    - "Quando contrato não tem pagamento (totalPago = 0), a linha 'Pagamento parcial' NÃO aparece"
    - "O motor Art.354 (devedorCalc.js), o adapter (pagamentos.js) e o Modal.jsx base permanecem intocados"
    - "A regressão do motor (10/10 testes, 9 TJGO + 1 shield 7.8-01) continua verde"
    - "O build Vite do submódulo mr-3 continua exit 0"
  artifacts:
    - path: "src/mr-3/mr-cobrancas/src/components/DecomposicaoSaldoModal.jsx"
      provides: "Linha 'Pagamento parcial' com valor -totalPago, signed=true, condicional totalPago>0"
      contains: "const totalPago = Number(d.totalPago"
      contains_label: '"Pagamento parcial"'
      contains_condicional: "if (totalPago > 0)"
      contains_value: "value: -totalPago"
  key_links:
    - from: "DecomposicaoSaldoModal.jsx: const totalPago"
      to: "detalhe.totalPago (shape de calcularDetalheEncargosContrato linha 495/516 devedorCalc.js)"
      via: "destruct/numerização em bloco de extrações (~linhas 45-58)"
      pattern: "d\\.totalPago"
    - from: "push condicional if (totalPago > 0)"
      to: "array rows entre 'Valor Original' (linha 85) e 'if (correcaoValor !== 0)' (linha 87)"
      via: "rows.push({ label: 'Pagamento parcial', value: -totalPago, signed: true })"
      pattern: "rows\\.push\\(\\{ label: \"Pagamento parcial\""
    - from: "useMemo deps de linhas"
      to: "totalPago (novo item na array)"
      via: "append em [valorOriginal, correcaoValor, ...]"
      pattern: "totalPago"
    - from: "bodyRowsHtml do handleImprimir (linha 160, existente)"
      to: "linha Pagamento parcial com color=#dc2626 inline"
      via: "l.signed && Number(l.value) < 0 ? '#dc2626' : '#000' (já existente)"
      pattern: "color = l.signed && Number\\(l.value\\) < 0 \\? \"#dc2626\""
    - from: "textoFormatado do handleCopiar (linha 127, existente)"
      to: "linha '+ Pagamento parcial: - R$ X' no clipboard"
      via: "linhas.forEach com prefix '+ ' e fmtBRLSigned (já existente)"
      pattern: "linhas\\.forEach"
---

<objective>
Adicionar linha "Pagamento parcial" (valor negativo, cor vermelha via helpers existentes) no array `rows` do `useMemo` de `linhas` em `DecomposicaoSaldoModal.jsx` — entre "Valor Original" e "Correção" — para que o advogado veja quanto já foi deduzido do principal antes dos encargos.

Purpose: UX gap descoberto em UAT prod da Phase 7.8 — o modal só mostrava o saldo FINAL; agora mostra a amortização explícita. Consumo puro de `detalhe.totalPago` já exposto pelo motor Art.354 (linha 495/516 `devedorCalc.js`). Zero mudança no motor, zero mudança no adapter `calcularDetalheEncargosContrato`, zero mudança no Modal.jsx base. Escopo cirúrgico: 1 arquivo, ~10-15 linhas adicionadas, `autonomous: true` porque baixo risco e gates automatizáveis 100%.

Output:
- `src/mr-3/mr-cobrancas/src/components/DecomposicaoSaldoModal.jsx` — extração `totalPago` + push condicional + useMemo deps atualizado
- Commit no submódulo mr-3 branch main: `feat(07.8.1-01): linha Pagamento parcial no DecomposicaoSaldoModal`
- Sem push (autorização explícita em plan 07.8.1-02)
- Sem bump no pai (plan 07.8.1-02)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07.8.1-pagamento-parcial-modal-composicao/07.8.1-CONTEXT.md

<!-- Código fonte a modificar -->
@src/mr-3/mr-cobrancas/src/components/DecomposicaoSaldoModal.jsx

<!-- Motor e adapter (APENAS LEITURA — NÃO MODIFICAR) -->
<!-- devedorCalc.js linha 495: const totalPago = (pagamentos || []).reduce((s,p)=>s+(parseFloat(p.valor)||0),0) -->
<!-- devedorCalc.js linha 516: totalPago é retornado no shape de calcularDetalheEncargos -->
<!-- pagamentos.js: calcularDetalheEncargosContrato passa pagamentos_divida direto -> totalPago = soma dos valores -->

<interfaces>
<!-- Shape relevante do detalhe (entrada do componente) -->
<!-- Consumido via prop `detalhe` de calcularDetalheEncargosContrato -->

Shape parcial de `detalhe` (já existente — não criar):
```typescript
{
  valorOriginal: number;       // já extraído (linha 49)
  totalPago: number;           // NOVO consumo — expor no bloco de extrações
  correcao: { valor: number }; // já extraído (linha 50)
  multa:    { valor: number }; // já extraído (linha 51)
  juros:    { valor: number }; // já extraído (linha 52)
  honorarios: { valor: number }; // já extraído (linha 53)
  art523: { multa: number; honorarios: number; total: number }; // (54-56)
  custas: { atualizado: number }; // (57)
  saldoAtualizado: number;     // (58)
}
```

Helpers existentes (NÃO criar duplicata):
```javascript
// DecomposicaoSaldoModal.jsx:14-21
function fmtBRLSigned(v) {
  // retorna "- R$ X" para v < 0, senão fmtBRL(v)
}
// DecomposicaoSaldoModal.jsx:24-26
function valueColor(v) {
  return Number(v) < 0 ? "#dc2626" : "#0f172a";
}
```

Shape de item de `rows` (contrato implícito já em uso):
```javascript
{ label: string, value: number, signed: boolean, bold: boolean, sempre?: boolean }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extrair totalPago, adicionar push condicional "Pagamento parcial", atualizar useMemo deps</name>
  <files>src/mr-3/mr-cobrancas/src/components/DecomposicaoSaldoModal.jsx</files>

  <read_first>
    Ler `src/mr-3/mr-cobrancas/src/components/DecomposicaoSaldoModal.jsx` inteiro (277 linhas) antes de editar. Observar:
    - Linhas 45-58: bloco de extrações de `detalhe` (`valorOriginal`, `correcaoValor`, ..., `saldoAtualizado`). É onde adicionar `const totalPago = Number(d.totalPago || 0);`.
    - Linhas 83-119: `useMemo` que monta `rows[]`. Linha 85 empurra "Valor Original". Linha 87 abre `if (correcaoValor !== 0)`. É entre essas DUAS linhas que vai o novo push.
    - Linha 119: array de deps do useMemo — adicionar `totalPago` ao fim (ou em posição coerente com o resto).
    - Linhas 127-135: `textoFormatado.useMemo` (clipboard). **NÃO tocar** — a nova linha propaga via `linhas.forEach`.
    - Linhas 158-164: `bodyRowsHtml` do `handleImprimir`. **NÃO tocar** — a nova linha propaga via `linhas.map` e o `color` inline já contempla `signed && value < 0 → #dc2626`.
  </read_first>

  <action>
    Realizar EXATAMENTE três edições em `src/mr-3/mr-cobrancas/src/components/DecomposicaoSaldoModal.jsx`:

    **Edição #1 — Extração de `totalPago` (D-02, D-10)**:
    Após a linha `const valorOriginal = Number(d.valorOriginal || 0);` (linha 49) inserir:
    ```javascript
      const totalPago         = Number(d.totalPago || 0);
    ```
    Alinhamento com o bloco existente (mesmo padrão de indentação `const X = Number(...)`).

    **Edição #2 — Push condicional entre "Valor Original" e "Correção" (D-04, D-05, D-06, D-07)**:
    Dentro do `useMemo` de `linhas` (linhas 83-119), logo APÓS a linha:
    ```javascript
    rows.push({ label: "Valor Original", value: valorOriginal, signed: false, bold: false, sempre: true });
    ```
    (linha 85) e ANTES da linha:
    ```javascript
    if (correcaoValor !== 0) {
    ```
    (linha 87), inserir o bloco condicional:
    ```javascript

        if (totalPago > 0) {
          rows.push({
            label: "Pagamento parcial",
            value: -totalPago,
            signed: true,
            bold: false,
          });
        }
    ```
    Notas importantes:
    - Label EXATA: `"Pagamento parcial"` (D-04) — SEM parênteses, SEM `(−)`, SEM `(-)`, SEM prefixo Unicode. Só a string.
    - `value: -totalPago` (D-07 — sinal no valor; `fmtBRLSigned` renderiza `"- R$ X"`).
    - `signed: true` (D-07 — dispara `fmtBRLSigned` + `valueColor` → `#dc2626`).
    - `bold: false` (não é linha total).
    - Sem `sempre: true` (a linha É condicional — hide quando zero, D-06).
    - Indentação respeita o restante do `useMemo` (2 espaços a mais que `const rows = []`).

    **Edição #3 — useMemo deps (D-10)**:
    Localizar a linha 119 (atual):
    ```javascript
    }, [valorOriginal, correcaoValor, indexador, multaValor, jurosValor, honorariosValor, art523Total, art523Multa, art523Hon, custasAtualizado, saldoAtualizado]);
    ```
    Adicionar `totalPago` à lista. Novo conteúdo:
    ```javascript
    }, [valorOriginal, totalPago, correcaoValor, indexador, multaValor, jurosValor, honorariosValor, art523Total, art523Multa, art523Hon, custasAtualizado, saldoAtualizado]);
    ```
    Posição recomendada: DEPOIS de `valorOriginal`, ANTES de `correcaoValor` (espelha ordem de aparição no array `rows`, mas qualquer posição é correta desde que presente — só é importante estar na array).

    **NÃO TOCAR** (regressão e invariantes):
    - `src/mr-3/mr-cobrancas/src/utils/devedorCalc.js` (motor Art.354 — D-01)
    - `src/mr-3/mr-cobrancas/src/services/pagamentos.js` (adapter — D-02)
    - `src/mr-3/mr-cobrancas/src/services/__tests__/calculos.test.js` (testes regressão — D-01)
    - `src/mr-3/mr-cobrancas/src/components/ui/Modal.jsx` (Modal base — D-03)
    - `textoFormatado` (linhas 121-135) — propaga via `linhas.forEach` (D-09)
    - `bodyRowsHtml` (linhas 158-164) — propaga via `linhas.map` (D-08)
    - JSX do render (linhas 218-243) — propaga via `linhas.map` com `valueColor` existente

    Após edições, do diretório raiz do repo pai, executar o commit NO SUBMÓDULO (não no pai):
    ```bash
    cd src/mr-3/mr-cobrancas
    git add src/components/DecomposicaoSaldoModal.jsx
    git commit -m "feat(07.8.1-01): linha Pagamento parcial no DecomposicaoSaldoModal"
    cd ../../..
    ```

    **NÃO fazer `git push` neste plan** — autorização em plan 07.8.1-02 (D-15).
    **NÃO fazer bump no pai neste plan** — bump em plan 07.8.1-02 (D-15).
  </action>

  <acceptance_criteria>
    Executar cada grep do diretório raiz do repo pai e validar o valor esperado. TODAS as checks devem passar antes de commitar:

    1. **Extração presente** (exatamente 1 ocorrência):
       ```bash
       grep -c "const totalPago = Number(d\.totalPago" src/mr-3/mr-cobrancas/src/components/DecomposicaoSaldoModal.jsx
       # esperado: 1
       ```

    2. **Label exata "Pagamento parcial"** (exatamente 1 ocorrência, sem parênteses nem símbolos):
       ```bash
       grep -c '"Pagamento parcial"' src/mr-3/mr-cobrancas/src/components/DecomposicaoSaldoModal.jsx
       # esperado: 1
       grep -c 'Pagamento parcial (−)' src/mr-3/mr-cobrancas/src/components/DecomposicaoSaldoModal.jsx
       # esperado: 0
       grep -c 'Pagamento parcial (-)' src/mr-3/mr-cobrancas/src/components/DecomposicaoSaldoModal.jsx
       # esperado: 0
       ```

    3. **Condicional hide zero-value** (exatamente 1 ocorrência):
       ```bash
       grep -c "if (totalPago > 0)" src/mr-3/mr-cobrancas/src/components/DecomposicaoSaldoModal.jsx
       # esperado: 1
       ```

    4. **Valor negativo do tipo exato `-totalPago`** (exatamente 1 ocorrência):
       ```bash
       grep -c "value: -totalPago" src/mr-3/mr-cobrancas/src/components/DecomposicaoSaldoModal.jsx
       # esperado: 1
       ```

    5. **Ordem correta — "Pagamento parcial" ENTRE "Valor Original" e `if (correcaoValor !== 0)`**:
       ```bash
       awk '/"Valor Original"/,/if \(correcaoValor !== 0\)/' src/mr-3/mr-cobrancas/src/components/DecomposicaoSaldoModal.jsx | grep -c '"Pagamento parcial"'
       # esperado: 1 (linha do push aparece DENTRO do intervalo)
       ```
       Se valor = 0, a ordem foi violada. Se > 1, existe duplicação.

    6. **useMemo deps incluem `totalPago`**:
       ```bash
       grep -n "}, \[valorOriginal" src/mr-3/mr-cobrancas/src/components/DecomposicaoSaldoModal.jsx | grep -c "totalPago"
       # esperado: 1 (a linha de fechamento do useMemo cita totalPago)
       ```

    7. **Motor Art.354 intocado** (diff vazio):
       ```bash
       cd src/mr-3/mr-cobrancas && git diff HEAD -- src/utils/devedorCalc.js src/services/pagamentos.js src/services/__tests__/calculos.test.js src/components/ui/Modal.jsx && cd ../../..
       # esperado: saída vazia (stdout empty)
       ```

    8. **Escopo cirúrgico — exatamente 1 arquivo modificado no submódulo**:
       ```bash
       cd src/mr-3/mr-cobrancas && git diff HEAD --name-only && cd ../../..
       # esperado (ANTES do commit): exatamente a linha `src/components/DecomposicaoSaldoModal.jsx`
       # Após commit: `git show HEAD --stat` deve mostrar 1 file changed
       ```

    9. **Regressão 10/10 verde**:
       ```bash
       cd src/mr-3/mr-cobrancas && npm run test:regressao && cd ../../..
       # esperado: exit 0, "10 passed" (ou equivalente do reporter)
       ```

    10. **Build Vite exit 0**:
        ```bash
        cd src/mr-3/mr-cobrancas && npm run build && cd ../../..
        # esperado: exit 0, sem erros de sintaxe/import
        ```

    11. **Commit message exato** (após commitar):
        ```bash
        cd src/mr-3/mr-cobrancas && git log -1 --pretty=%s && cd ../../..
        # esperado: feat(07.8.1-01): linha Pagamento parcial no DecomposicaoSaldoModal
        ```

    12. **Zero push** — garantir que `git push` NÃO foi executado neste plan. Verificar por ausência de commits remotos novos:
        ```bash
        cd src/mr-3/mr-cobrancas && git log @{upstream}..HEAD --oneline && cd ../../..
        # esperado: 1 linha (o commit novo está à frente do upstream)
        ```

    13. **Pai NÃO bumpado ainda** — submódulo aparece como dirty no pai (HEAD do submódulo avançou), mas nenhum commit novo no pai:
        ```bash
        git status -s src/mr-3/mr-cobrancas
        # esperado: linha com " M src/mr-3/mr-cobrancas" (submódulo com novo HEAD)
        git log HEAD..HEAD --oneline
        # esperado: vazio (pai sem commits novos)
        ```
  </acceptance_criteria>

  <verify>
    <automated>cd src/mr-3/mr-cobrancas &amp;&amp; npm run test:regressao &amp;&amp; npm run build &amp;&amp; cd ../../.. &amp;&amp; grep -c '"Pagamento parcial"' src/mr-3/mr-cobrancas/src/components/DecomposicaoSaldoModal.jsx &amp;&amp; grep -c "if (totalPago > 0)" src/mr-3/mr-cobrancas/src/components/DecomposicaoSaldoModal.jsx &amp;&amp; grep -c "value: -totalPago" src/mr-3/mr-cobrancas/src/components/DecomposicaoSaldoModal.jsx &amp;&amp; grep -c "const totalPago = Number(d\.totalPago" src/mr-3/mr-cobrancas/src/components/DecomposicaoSaldoModal.jsx</automated>
  </verify>

  <done>
    - 3 edições executadas no arquivo: extração `totalPago`, push condicional "Pagamento parcial", `totalPago` nas deps do useMemo
    - 13 acceptance criteria acima retornam valores esperados
    - Motor Art.354, adapter `pagamentos.js`, test suite `calculos.test.js` e `Modal.jsx` base permanecem intocados (git diff vazio nesses paths)
    - Regressão `npm run test:regressao` passa 10/10 (9 TJGO + 1 shield 7.8-01)
    - `npm run build` retorna exit 0
    - Commit `feat(07.8.1-01): linha Pagamento parcial no DecomposicaoSaldoModal` criado no submódulo `src/mr-3/mr-cobrancas` branch main
    - NÃO há commit no pai (master) — bump fica para plan 07.8.1-02
    - NÃO há `git push` executado (autorização em plan 07.8.1-02)
  </done>
</task>

</tasks>

<verification>
**Checks automáticos (executar após Task 1, todos devem passar):**

1. **Regressão motor** (invariante D-01):
   ```bash
   cd src/mr-3/mr-cobrancas && npm run test:regressao
   # exit 0, 10/10 passing
   ```

2. **Build Vite** (invariante sintaxe/import):
   ```bash
   cd src/mr-3/mr-cobrancas && npm run build
   # exit 0
   ```

3. **Arquivos intocados** (invariante D-01, D-02, D-03):
   ```bash
   cd src/mr-3/mr-cobrancas && git diff HEAD~1 -- src/utils/devedorCalc.js src/services/pagamentos.js src/services/__tests__/calculos.test.js src/components/ui/Modal.jsx
   # saída: vazia
   ```

4. **Escopo cirúrgico** (invariante D-13):
   ```bash
   cd src/mr-3/mr-cobrancas && git show HEAD --stat
   # saída: 1 file changed (src/components/DecomposicaoSaldoModal.jsx), ~10-20 linhas adicionadas
   ```

5. **Grep AC consolidado**:
   ```bash
   for pattern in \
     '"Pagamento parcial"' \
     'if (totalPago > 0)' \
     'value: -totalPago' \
     'const totalPago = Number(d\.totalPago'; do
     echo "=== $pattern ==="
     grep -c "$pattern" src/mr-3/mr-cobrancas/src/components/DecomposicaoSaldoModal.jsx
   done
   # cada um: 1
   ```

**Sem checkpoints nesta fase** — `autonomous: true`. UAT visual fica no plan 07.8.1-02.
</verification>

<success_criteria>
- [x] `DecomposicaoSaldoModal.jsx` extrai `totalPago` de `detalhe` (grep = 1)
- [x] Push condicional `if (totalPago > 0)` adiciona linha "Pagamento parcial" entre "Valor Original" e "Correção" (grep + awk range = 1)
- [x] Label EXATA = `"Pagamento parcial"` (grep = 1, variantes = 0)
- [x] Value = `-totalPago` (grep = 1)
- [x] `signed: true` na nova linha (dentro do bloco)
- [x] `useMemo` de `linhas` inclui `totalPago` nas deps
- [x] Motor Art.354 + adapter + test suite + Modal base intocados (diff vazio nesses paths)
- [x] `npm run test:regressao` 10/10 verde
- [x] `npm run build` exit 0
- [x] Commit `feat(07.8.1-01): linha Pagamento parcial no DecomposicaoSaldoModal` no submódulo mr-3 main
- [x] Zero `git push` neste plan
- [x] Bump do pai fica para plan 07.8.1-02
</success_criteria>

<output>
Após completar, criar `.planning/phases/07.8.1-pagamento-parcial-modal-composicao/07.8.1-01-SUMMARY.md` com:
- Hash do commit do submódulo (`cd src/mr-3/mr-cobrancas && git rev-parse HEAD`)
- Diff line-count do commit (`git show HEAD --stat`)
- Confirmação de paths intocados (outputs de git diff)
- Output de `npm run test:regressao` (resumo)
- Output de `npm run build` (resumo)
- Listagem dos 4 greps AC principais
- Estado pendente: bump no pai + UAT visual → plan 07.8.1-02
</output>
