---
plan: 07.13-01
phase: 7.13
phase_name: "Múltiplos Devedores por Contrato (Solidariedade Passiva)"
type: execute
wave: 1
depends_on: []
autonomous: false
requirements: []
source: "ROADMAP.md L380-387 + 07.13-CONTEXT.md (D-pre-1..D-pre-14 + invariantes herdadas D-01..D-34 + implementation_plan §187-216 + UAT §218-244). Phase has NO REQ-IDs (suporte legal a co-obrigados — solidariedade passiva CC art. 264; INSERTED após 7.10bcd)."
estimated_tasks: 7

files_modified:
  - src/mr-3/mr-cobrancas/src/services/devedoresDividas.js                                 # MOD (commit 07.13a — 5 helpers novos: listarDevedoresDoContrato, criarVinculoContratoDevedor, removerVinculoContratoDevedor, alterarPapelContratoDevedor, promoverParaPrincipalComDemocao, seedDevedoresDoContrato)
  - src/mr-3/mr-cobrancas/src/services/__tests__/devedoresDividasContrato.test.js          # NEW (commit 07.13a — test trivial 3 it() pra helpers contrato-level)
  - src/mr-3/mr-cobrancas/package.json                                                     # MOD (commit 07.13a — script test:regressao estende com devedoresDividasContrato.test.js)
  - src/mr-3/mr-cobrancas/src/services/contratos.js                                        # MOD (commit 07.13b — adicionarDocumento L294-298 troca seedPrincipal por seedDevedoresDoContrato)
  - src/mr-3/mr-cobrancas/src/components/DevedoresDoContrato.jsx                           # NEW (commit 07.13c — componente multi-devedor por contrato; clone adaptado de DevedoresDaDivida.jsx)
  - src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx                               # MOD (commit 07.13c — substitui dropdown único L253/L285/L571 por DevedoresDoContrato)
  - src/mr-3/mr-cobrancas/src/components/DevedoresDaDivida.jsx                             # MOD (commit 07.13d — prop readOnly={true} esconde botões adicionar/remover/promover/✕)
  - src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx                                 # MOD (commit 07.13d — passa readOnly={true} pra DevedoresDaDivida L195)
  - src/mr-3/mr-cobrancas/src/App.jsx                                                      # MOD (commits 07.13d + 07.13e + 07.13f — L3889 readOnly; case "contratos" L8493 query expandida; case "devedores" L8468 helper paralelo)
  - src/mr-3/mr-cobrancas/src/components/TabelaContratos.jsx                               # MOD (commit 07.13e — coluna Devedor render inline "Nome (Papel), Nome (Papel)" + PRINCIPAL primeiro)
  - src/mr-3/mr-cobrancas/src/utils/agruparPagamentosPorDevedorIncluindoSolidarios.js      # NEW (commit 07.13f — helper paralelo fan-out 1:N via junction)
  - src/mr-3/mr-cobrancas/src/services/__tests__/agruparPagamentosPorDevedorIncluindoSolidarios.test.js  # NEW (commit 07.13f — test trivial 2 it())

must_haves:
  truths:
    - "Helper layer em devedoresDividas.js (commit 07.13a): 5 helpers novos contrato-level — listarDevedoresDoContrato (DISTINCT por contrato), criarVinculoContratoDevedor (N inserts fan-out), removerVinculoContratoDevedor (N deletes), alterarPapelContratoDevedor (N PATCHes simples sem mudar PRINCIPAL — bloqueia papel='PRINCIPAL'), promoverParaPrincipalComDemocao (wizard D-pre-13 — N PATCHes pareados atomicamente reexecutáveis), seedDevedoresDoContrato (estende seedPrincipal pra TODOS os devedores do contrato)"
    - "Test trivial 3 it() blocks PASS para helpers contrato-level (commit 07.13a): (a) criarVinculoContratoDevedor com 12 dívidas → 12 rows inserted; (b) listarDevedoresDoContrato retorna devedores DISTINCT por contrato com papel; (c) seedDevedoresDoContrato em nova dívida copia TODOS os devedores existentes do contrato"
    - "package.json test:regressao estendido (commit 07.13a): 28/28 esperado (25 base 7.10bcd + 3 helpers contrato-level)"
    - "Cascade nova dívida em adicionarDocumento (commit 07.13b): após cada dbInsert(\"dividas\", p) chama seedDevedoresDoContrato(novaDiv.id, contratoId) — substitui seedPrincipal isolado. Contrato existente continua funcionando (junction já populada via seed retrocompat 002 §7); nova parcela em contrato com N devedores recebe N rows na junction (não só PRINCIPAL legado)"
    - "Componente DevedoresDoContrato.jsx (commit 07.13c — NEW): clone adaptado de DevedoresDaDivida.jsx com signatures contratoId + dividas (não dividaId). PAPEL_META + RESP_LABELS reusados verbatim. Modal AdicionarDevedorContratoModal segue pattern AdicionarParticipanteModal (clone com lógica salvar trocada de 1 insert → N inserts via criarVinculoContratoDevedor). Modal wizard D-pre-13 promoção a PRINCIPAL com PRINCIPAL anterior — dropdown 5 papéis sem default; usuário escolhe papel do anterior; N PATCHes pareados via promoverParaPrincipalComDemocao. Modal warning bloqueio remoção último PRINCIPAL via Modal.jsx existente — D-05 LOCKED, sem window.confirm"
    - "DetalheContrato.jsx (commit 07.13c — MOD): substitui dropdown único devedor_id em 3 pontos (L253/L285/L571) por componente DevedoresDoContrato. Validação devedorAlterado L253 vira validação set diff (qualquer mudança no set de devedores cascateia); snapshot histórico L285 vira array de devedor_ids; render L571 substitui devedor.find por devedoresDoContrato"
    - "DetalheDivida read-only mode (commit 07.13d): DevedoresDaDivida.jsx ganha prop readOnly={true} que esconde botões adicionar/remover/promover/✕ — só renderiza chips de papel + nome + cpf. DetalheDivida.jsx L195 + App.jsx L3889 passam readOnly={true}. Edição cascateia do contrato; dívida individual não permite override"
    - "ModuloContratos listagem inline (commit 07.13e): App.jsx case 'contratos' L8493 query expandida (PostgREST embed: contratos_dividas?select=*,dividas(devedores_dividas(devedor_id,papel))) OU batch separado pré-render (devedores_dividas?divida_id=in.(...)) consolidado em Map<contratoId, [{devedor,papel}]>. TabelaContratos.jsx coluna 'Devedor' render 'Nome1 (Papel1), Nome2 (Papel2)' com PRINCIPAL primeiro (sort: PRINCIPAL → outros), papel formatado primeira-maiúscula PT-BR via PAPEL_META.label"
    - "Pessoas saldo cheio para não-PRINCIPAL (commit 07.13f): novo helper utils/agruparPagamentosPorDevedorIncluindoSolidarios.js fan-out 1:N via junction (cada pagamento_divida vai pra TODOS os devedores da dívida, não só PRINCIPAL). App.jsx L2993 Pessoas Map troca chamada agruparPagamentosPorDevedor → agruparPagamentosPorDevedorIncluindoSolidarios. Dashboard L568 (totalCarteira/recebidoPeriodo) e L8434 (parent App pgtosPorDevedorCarteira) INTOCADOS — preservam dedupe via principaisDividaIds papel='PRINCIPAL' (D-pre-6)"
    - "Final Gates Task (Task 7 — NÃO produz commit): Shields D-02 strict imports `^import .* from \"...\"` em todos componentes novos/modificados; shield equivalência seedDevedoresDoContrato (≡ N seedPrincipal por dívida + N adicionarParticipante por papel); shield equivalência promoverParaPrincipalComDemocao (N PATCHes pareados onde PRINCIPAL anterior recebe novoPapelDoAnterior); shield equivalência criarVinculoContratoDevedor (≡ N adicionarParticipante por dívida do contrato); Gate motor C2 cumulativo `git -C src/mr-3 log f00cf2c..HEAD --format=\"\" --name-only` confirma devedorCalc.js + pagamentos.js + dividas.js AUSENTES em todos 6 commits anteriores; Gate motor C1 cumulativo `git -C src/mr-3 diff f00cf2c..HEAD -- src/utils/devedorCalc.js src/services/pagamentos.js src/services/dividas.js` retorna VAZIO; D-30 commit count: EXATAMENTE 6 commits sequenciais com headers verbatim feat(07.13a/b/c/d/e/f)"
    - "Motor Art.354 (devedorCalc.js + pagamentos.js + dividas.js) INTOCADO em todos 6 commits — D-01 estrito cumulativo desde baseline f00cf2c (post-7.10bcd HEAD); dual gate C1 (diff) + C2 (--name-only) ambos verdes em Final Gates Task"
    - "Schema zero-diff: ZERO migration nesta phase. Reusa devedores_dividas (Migrações 001+002) — 6 papéis preservados (D-pre-7), responsabilidade modelada mas não exposta default SOLIDARIA (D-pre-8), UNIQUE INDEX (papel='PRINCIPAL', divida_id) preservado (D-pre-12), UNIQUE INDEX (divida_id, devedor_id) preservado"
    - "Dashboard Carteira Total NÃO MUDA: anti-dupla-contagem via principaisDividaIds papel='PRINCIPAL' filter (App.jsx L597-608 + L8434) preservada exatamente. Pre-7.13 e Pós-7.13 retornam mesmo valor (validado em SC7 do Plan 02)"
    - "2 checkpoints internos no Plan 01 (autonomous:false): após Task 3 (cadastro+wizard funcional — usuário valida UI manualmente em localhost) e após Task 6 (Pessoas saldo cheio — usuário valida fiador R$ 36.652,56 cheio sem dobrar Carteira Total)"
    - "D-30 commits atômicos cumulativos bisect-able: cada commit deixa estado verde (build + regressão 28/28). Sequência verbatim: feat(07.13a) → feat(07.13b) → feat(07.13c) → feat(07.13d) → feat(07.13e) → feat(07.13f). Final Gates Task NÃO produz commit (verificação)"
  artifacts:
    - path: "src/mr-3/mr-cobrancas/src/services/devedoresDividas.js"
      provides: "Service layer estendido — 5 helpers contrato-level (listarDevedoresDoContrato, criarVinculoContratoDevedor, removerVinculoContratoDevedor, alterarPapelContratoDevedor, promoverParaPrincipalComDemocao) + seedDevedoresDoContrato substitui seedPrincipal em adicionarDocumento. Existentes (listarParticipantes, adicionarParticipante, alterarPapel, removerParticipante, seedPrincipal, demoverPrincipalAtual) PRESERVADOS — back-compat com DevedoresDaDivida.jsx (read-only mode 7.13d ainda usa listarParticipantes via useDevedoresDividas)"
      contains: "export async function seedDevedoresDoContrato"
    - path: "src/mr-3/mr-cobrancas/src/services/__tests__/devedoresDividasContrato.test.js"
      provides: "Test trivial 3 it() — defesa contra regressão silenciosa do helper compartilhado (lição feedback_shields_wrapper_must_test_equivalence.md)"
      contains: "criarVinculoContratoDevedor"
    - path: "src/mr-3/mr-cobrancas/src/services/contratos.js"
      provides: "adicionarDocumento estendido — após cada dbInsert(dividas) chama seedDevedoresDoContrato (substitui seedPrincipal isolado)"
      contains: "seedDevedoresDoContrato"
    - path: "src/mr-3/mr-cobrancas/src/components/DevedoresDoContrato.jsx"
      provides: "Componente multi-devedor por contrato — lista vertical + chips de papel + Modal AdicionarDevedorContratoModal + Modal wizard D-pre-13 (promoção a PRINCIPAL) + Modal warning último PRINCIPAL"
      contains: "function DevedoresDoContrato"
    - path: "src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx"
      provides: "3 substituições do dropdown único (L253 validação, L285 snapshot, L571 render) — agora usa array de devedores via componente DevedoresDoContrato"
      contains: "DevedoresDoContrato"
    - path: "src/mr-3/mr-cobrancas/src/components/DevedoresDaDivida.jsx"
      provides: "Prop readOnly={true} — esconde botões adicionar/remover/promover/✕; só renderiza chips (D-pre-9)"
      contains: "readOnly"
    - path: "src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx"
      provides: "L195 passa readOnly={true} pra DevedoresDaDivida — listagem read-only por dívida"
      contains: "readOnly={true}"
    - path: "src/mr-3/mr-cobrancas/src/components/TabelaContratos.jsx"
      provides: "Coluna 'Devedor' render inline 'Nome (Papel), Nome (Papel)' com PRINCIPAL primeiro — D-pre-2 + D-pre-14"
      contains: "Principal"
    - path: "src/mr-3/mr-cobrancas/src/utils/agruparPagamentosPorDevedorIncluindoSolidarios.js"
      provides: "Helper paralelo fan-out 1:N via junction — pagamento de dívida com N devedores aparece em TODOS N (D-pre-6 saldo cheio); usado em Pessoas (App.jsx L2993)"
      contains: "agruparPagamentosPorDevedorIncluindoSolidarios"
  key_links:
    - from: "src/utils/agruparPagamentosPorDevedorIncluindoSolidarios.js (helper)"
      to: "App.jsx L2993 Pessoas Map (commit 07.13f)"
      via: "import + chamada `agruparPagamentosPorDevedorIncluindoSolidarios(devedores, allPagamentos, devedoresDividasJunction)` em 1 callsite"
      pattern: "agruparPagamentosPorDevedorIncluindoSolidarios\\("
    - from: "src/services/devedoresDividas.js seedDevedoresDoContrato"
      to: "contratos.js adicionarDocumento (commit 07.13b)"
      via: "Substituição de seedPrincipal — após cada dbInsert(dividas) cascade copia TODOS devedores existentes do contrato (não só PRINCIPAL legado)"
      pattern: "seedDevedoresDoContrato\\("
    - from: "DevedoresDoContrato.jsx (componente)"
      to: "DetalheContrato.jsx L253/L285/L571 (commit 07.13c)"
      via: "3 substituições do dropdown único — array de devedores via componente"
      pattern: "<DevedoresDoContrato"
    - from: "DevedoresDaDivida.jsx prop readOnly"
      to: "DetalheDivida.jsx L195 + App.jsx L3889"
      via: "Prop readOnly={true} esconde botões — D-pre-9 read-only"
      pattern: "readOnly=\\{true\\}"
    - from: "TabelaContratos.jsx coluna Devedor"
      to: "Query expandida (PostgREST embed OU batch) consolidada em Map<contratoId, [{devedor,papel}]>"
      via: "Sort PRINCIPAL primeiro + render 'Nome (Papel), Nome (Papel)' inline expandido (D-pre-2)"
      pattern: "Principal.*Fiador|principal.*fiador"
    - from: "Final Gates Task (Task 7)"
      to: "D-01 cumulativo + D-30 6 commits + Shields equivalência"
      via: "Verificação automática SEM commit — gate dual motor C1+C2 + 3 shields equivalência + count commits"
      pattern: "git log f00cf2c..HEAD"
---

<objective>
Implementar suporte a múltiplos devedores por contrato (solidariedade passiva CC art. 264) via fan-out na junction `devedores_dividas` existente — **schema zero-diff**, 6 commits atômicos sequenciais bisect-able no submódulo + 1 Final Gates Task de verificação. Reusa `devedores_dividas` (Migrações 001+002, 6 papéis), `seedPrincipal`, `demoverPrincipalAtual`, `useDevedoresDividas`, `Modal.jsx`, padrão de cascade `cascatearCredorDevedor` (Phase 6 EDT-03), helper compartilhado pattern (7.10bcd D-31).

Purpose:
- **D-pre-1 NÍVEL CONTRATO semanticamente / N rows operacionalmente** — solidariedade aplicada a TODAS as dívidas do contrato via N rows na junction (não tabela `contrato_devedores` nova).
- **D-pre-3 phase grande única, 6 commits atômicos bisect-able** — sequência `feat(07.13a): service` → `feat(07.13b): cascade nova dívida` → `feat(07.13c): UI cadastro + wizard` → `feat(07.13d): DetalheDivida read-only` → `feat(07.13e): ModuloContratos inline` → `feat(07.13f): Pessoas saldo cheio`. Final Gates Task (Task 7) NÃO produz commit — verificação cumulativa absorve trabalho do antigo "07.13g".
- **D-pre-9 UI cadastro só por contrato** — DetalheDivida read-only (botões escondidos via prop). Edição cascateia do contrato; dívida individual não permite override (menor superfície de inconsistência).
- **D-pre-10 cascade fan-out** — vincular = N inserts; remover = N deletes; alterar papel sem mudar PRINCIPAL = N PATCHes; promoção a PRINCIPAL com PRINCIPAL anterior = wizard D-pre-13 (N PATCHes pareados). Atomicidade reexecutável (padrão 7.2 D-04 — failure mid-loop deixa estado parcial recuperável via re-execução).
- **D-pre-11 seedDevedoresDoContrato** — cascade existente `seedPrincipal` estendida para popular TODOS os devedores do contrato em nova dívida. Helper-first (lição 7.9 feedback_db_integration_gate_missing.md).
- **D-pre-12 validações app-level** — ≥1 PRINCIPAL ao salvar (primeiro auto via seedPrincipal); bloqueio remoção último PRINCIPAL via Modal warning (D-05 LOCKED — sem window.confirm); UNIQUE INDEX papel=PRINCIPAL preservado.
- **D-pre-13 wizard 2-steps obrigatório** — promover não-PRINCIPAL → PRINCIPAL quando contrato JÁ TEM PRINCIPAL: Modal "Contrato já tem PRINCIPAL ([nome]). Promover [novo] a PRINCIPAL e demover [nome] a:" + dropdown 5 papéis SEM DEFAULT. Confirma → N PATCHes pareados via `promoverParaPrincipalComDemocao(contratoId, novoDevedorId, novoPapelDoAnterior)`. NUNCA chamar `demoverPrincipalAtual` silencioso direto da UI.
- **D-pre-14 ModuloContratos query expandida** — listagem inline `"Nome1 (Papel1), Nome2 (Papel2)"` com PRINCIPAL primeiro; PostgREST embed OU batch separado.
- **D-pre-6 Pessoas saldo cheio** — fiador aparece R$ 36.652,56 cheio (perspectiva de cobrança individual). Helper paralelo `agruparPagamentosPorDevedorIncluindoSolidarios` faz fan-out 1:N via junction. Dashboard Carteira Total INTOCADO — anti-dupla-contagem via `principaisDividaIds` papel='PRINCIPAL' (App.jsx L597-608 + L8434).
- **D-01 motor INTOCADO** — devedorCalc.js + pagamentos.js + dividas.js zero diff cumulativo desde f00cf2c (post-7.10bcd HEAD). Cálculo por dívida individual não muda — múltiplos devedores não afetam saldo da dívida em si.
- **2 checkpoints internos** (Tasks 3 e 6 são `checkpoint:human-verify` blocking) — defesa proporcional ao risco. Phase autonomous:false.

Output:
- 6 commits atômicos sequenciais no submódulo: feat(07.13a) → feat(07.13b) → feat(07.13c) → feat(07.13d) → feat(07.13e) → feat(07.13f)
- 13 arquivos cumulativos modificados (5 NEW + 8 MOD)
- Regressão 28/28 PASS por commit (25 base 7.10bcd + 3 helpers contrato-level)
- Final Gates Task verificação cumulativa SEM commit
- `.planning/phases/07.13-multiplos-devedores-contrato-solidariedade-passiva/07.13-01-SUMMARY.md` (criado em última task, left uncommitted no pai até Plan 02 Task 6)

Fora de escopo:
- Auto-povoar polo passivo ProcessosJudiciais (deferred — phase futura)
- Ativação UI de responsabilidade=SUBSIDIARIA (schema pronto, deferred)
- Mudança de papéis em UI Pessoas cross-contract (deferred — só DetalheContrato edita)
- Decomposição/petição multi-devedor (v1.5)
- Deprecação de `dividas.devedor_id` (preservada como denormalização — phase futura)
- Tag `v1.4-phase7.13` (Plan 02 Task 6 pós-UAT prod)
- Bump pai / push / Vercel deploy (Plan 02 Task 4)
- UAT 10 SCs + 2 SC-iso (Plan 02 Task 2)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/PROJECT.md
@.planning/phases/07.13-multiplos-devedores-contrato-solidariedade-passiva/07.13-CONTEXT.md
@.planning/phases/07.10bcd-migracao-pagamentos-divida-tres-consumidores/07.10bcd-CONTEXT.md

<!-- Service layer junction (REUSAR — D-pre-4) -->
@src/mr-3/mr-cobrancas/src/services/devedoresDividas.js
@src/mr-3/mr-cobrancas/src/services/useDevedoresDividas.js

<!-- Schema (zero-diff — D-pre-4) -->
@src/mr-3/mr-cobrancas/src/services/migrations/002_dividas_tabela.sql

<!-- UI existente (clone adaptado — D-pre-9) -->
@src/mr-3/mr-cobrancas/src/components/DevedoresDaDivida.jsx
@src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx
@src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx
@src/mr-3/mr-cobrancas/src/components/ModuloContratos.jsx
@src/mr-3/mr-cobrancas/src/components/TabelaContratos.jsx

<!-- Service layer contratos (cascade — D-pre-11) -->
@src/mr-3/mr-cobrancas/src/services/contratos.js

<!-- Pessoas / Dashboard (helper paralelo — D-pre-6) -->
@src/mr-3/mr-cobrancas/src/App.jsx
@src/mr-3/mr-cobrancas/src/utils/agruparPagamentosPorDevedor.js
@src/mr-3/mr-cobrancas/src/services/__tests__/agruparPagamentosPorDevedor.test.js

<!-- Motor protegido (D-01 estrito — ZERO edição em qualquer um, cumulativo desde f00cf2c) -->
@src/mr-3/mr-cobrancas/src/utils/devedorCalc.js
@src/mr-3/mr-cobrancas/src/services/pagamentos.js
@src/mr-3/mr-cobrancas/src/services/dividas.js

<!-- Modal pattern (D-05 LOCKED — sem window.confirm em fluxos de PRINCIPAL) -->
@src/mr-3/mr-cobrancas/src/components/Modal.jsx

<!-- Memory feedbacks aplicados pre-execute -->
<!-- feedback_db_integration_gate_missing.md (helper-first) -->
<!-- feedback_grep_global_before_pointwise_fix.md (grep dividas[].devedor_id antes de mudar agregação) -->
<!-- feedback_grep_shield_strict_imports.md (^import .* from "...") -->
<!-- feedback_shields_wrapper_must_test_equivalence.md (helper precisa shield equivalência) -->
<!-- feedback_d01_relaxation_protocol.md (D-01 NÃO relaxa nesta phase) -->
<!-- feedback_proc_judiciais_create_bug_preexisting.md (smoke pré-execute em Plan 02 Task 1) -->
<!-- feedback_cross_entity_uat_isolation.md (SC-iso obrigatório no Plan 02) -->

<interfaces>
Key contracts the executor needs (extracted from codebase) — NO codebase exploration needed.

**Submódulo baseline pré-7.13:** SHA `f00cf2c` (post-7.10bcd HEAD = pré-7.13 baseline).
- Cumulative motor diff `git -C src/mr-3 diff f00cf2c..HEAD -- src/utils/devedorCalc.js src/services/pagamentos.js src/services/dividas.js` deve ser VAZIO ao final do plan (D-01 cumulativo).
- Cumulative motor name-only `git -C src/mr-3 log f00cf2c..HEAD --format="" --name-only | grep -E "(devedorCalc|pagamentos|dividas)\.js"` deve retornar VAZIO (D-01 dual gate).
- Cumulative commit count `git -C src/mr-3 log f00cf2c..HEAD --oneline | wc -l` deve ser EXATAMENTE 6 ao final (D-30).
- Cumulative regressão final esperada: 28/28 (25 base 7.10bcd + 3 helpers contrato-level).

**Schema devedores_dividas (verbatim — Migration 002 §6):**
```sql
CREATE TABLE devedores_dividas (
  id               UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  devedor_id       BIGINT NOT NULL REFERENCES devedores(id) ON DELETE CASCADE,
  divida_id        UUID NOT NULL REFERENCES dividas(id) ON DELETE CASCADE,
  papel            TEXT NOT NULL DEFAULT 'PRINCIPAL'
                   CHECK (papel IN ('PRINCIPAL','COOBRIGADO','AVALISTA','FIADOR','CONJUGE','OUTRO')),
  responsabilidade TEXT NOT NULL DEFAULT 'SOLIDARIA'
                   CHECK (responsabilidade IN ('SOLIDARIA','SUBSIDIARIA','DIVISIVEL')),
  observacao       TEXT,
  created_at       TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at       TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
CREATE UNIQUE INDEX devedores_dividas_principal_unique
  ON devedores_dividas (divida_id) WHERE papel = 'PRINCIPAL';
CREATE UNIQUE INDEX devedores_dividas_unico
  ON devedores_dividas (divida_id, devedor_id);
```

**Service existente devedoresDividas.js (PRESERVAR — back-compat):**
- `listarParticipantes(dividaId)` — usado por useDevedoresDividas hook
- `adicionarParticipante({ devedorId, dividaId, papel, responsabilidade, observacao })` — auto-demote PRINCIPAL silencioso (NÃO usar diretamente da UI 7.13 — substituído por criarVinculoContratoDevedor + promoverParaPrincipalComDemocao)
- `alterarPapel(rowId, novoPapel, dividaId)` — auto-demote PRINCIPAL silencioso (mesma observação)
- `removerParticipante(rowId)` — DELETE simples
- `seedPrincipal(devedorId, dividaId)` — cascade nova dívida (substituído em adicionarDocumento por seedDevedoresDoContrato)
- `demoverPrincipalAtual(dividaId, exceptRowId)` — auxiliar interno (PRESERVAR — usado por adicionarParticipante/alterarPapel back-compat)

**adicionarDocumento (contratos.js L256-301 — alvo commit 07.13b):**
```javascript
// L294-298 trecho atual (alvo da edição):
const rows = [];
for (const p of parcelasPayload) {
  const r = await dbInsert("dividas", p);
  rows.push(Array.isArray(r) ? r[0] : r);
  // ← ANTES: nada. NOVO commit 07.13b:
  //   await seedDevedoresDoContrato(novaDiv.id, contratoId);
  //   substitui qualquer chamada legacy a seedPrincipal (que vivia em App.jsx L3265 fluxo manual de criação de dívida)
}
await recalcularTotaisContrato(contratoId);
```

**App.jsx L3260-3268 fluxo legacy de adicionar dívida manual (alvo commit 07.13b — substituir seedPrincipal por seedDevedoresDoContrato se contrato_id presente; senão manter seedPrincipal pra dívidas standalone sem contrato):**
```javascript
// L3262-3268 trecho atual:
try {
  const { seedPrincipal } = await import("./services/devedoresDividas.js");
  await seedPrincipal(sel.id, novaDiv.id);
} catch (seedErr) {
  console.warn("seedPrincipal failed (non-critical):", seedErr);
}
// NOVO commit 07.13b:
//   if (novaDiv.contrato_id) {
//     await seedDevedoresDoContrato(novaDiv.id, novaDiv.contrato_id);
//   } else {
//     await seedPrincipal(sel.id, novaDiv.id);  // dívida standalone — sem contrato
//   }
```

**DetalheContrato.jsx 3 pontos do dropdown único (alvo commit 07.13c):**
- L240-253 (validação `devedorAlterado` baseada em payload.devedor_id): vira validação set diff (set de devedores antes vs depois)
- L282-285 (snapshot histórico em registrarEvento): `devedor_id` vira array `devedor_ids` no snapshot, ou novo tipo_evento (ver D-pre-9 L43)
- L571 (`const devedor = devedores.find(d => String(d.id) === String(contrato.devedor_id))`): substituir por `const devedoresDoContrato = await listarDevedoresDoContrato(contrato.id)` ou consumir de prop/state pré-carregado em ModuloContratos

**DevedoresDaDivida.jsx pattern atual (clone adaptado em DevedoresDoContrato.jsx — commit 07.13c):**
- L5-12 PAPEL_META (REUSAR verbatim)
- L14-18 RESP_LABELS (REUSAR verbatim)
- L20-158 componente principal (CLONE, signature mudará: `dividaId` → `contratoId` + carregamento via novo helper listarDevedoresDoContrato)
- L160-340 AdicionarParticipanteModal (CLONE → AdicionarDevedorContratoModal, lógica salvar troca: 1 insert → N inserts via criarVinculoContratoDevedor)
- L88-117 botão remover (auto-demote silencioso → wizard D-pre-13: ao clicar promover, abre Modal de confirmação)

**App.jsx L2993 Pessoas Map atual (alvo commit 07.13f):**
```javascript
// Phase 7.10bcd:
const pgtosPorDevedor = useMemo(
  () => agruparPagamentosPorDevedor(devedores, allPagamentos),
  [devedores, allPagamentos]
);
// NOVO commit 07.13f:
const pgtosPorDevedor = useMemo(
  () => agruparPagamentosPorDevedorIncluindoSolidarios(devedores, allPagamentos, devedoresDividasJunction),
  [devedores, allPagamentos, devedoresDividasJunction]
);
// devedoresDividasJunction = state novo carregado em carregarTudo (sb("devedores_dividas?select=devedor_id,divida_id,papel"))
```

**App.jsx L8493 case "contratos" (alvo commit 07.13e — query expandida):**
- Opção A (PostgREST embed): `dbGet("contratos_dividas", "select=*,dividas(id,devedores_dividas(devedor_id,papel))")` — 1 query, response aninhado
- Opção B (batch separado): `dbGet("devedores_dividas", "select=devedor_id,divida_id,papel")` em parallel com contratos_dividas + consolidação client-side em Map<contratoId, [{devedor_id,papel}]>
- DECISÃO PLANNER: **Opção B (batch separado)** — match de pattern atual de App.jsx (cargas paralelas em carregarTudo); response shape consistente com Devedores Map L2993

**App.jsx L597-608 Dashboard `principaisDividaIds` (PRESERVAR EXATO — D-pre-6):**
```javascript
const [principaisDividaIds, setPrincipaisDividaIds] = useState(null);
useEffect(() => {
  async function load() {
    const { sb } = await import("./config/supabase.js");
    const rows = await sb("devedores_dividas?papel=eq.PRINCIPAL&select=devedor_id,divida_id");
    if (Array.isArray(rows)) {
      const m = new Map();
      rows.forEach(r => m.set(String(r.divida_id), String(r.devedor_id)));
      setPrincipaisDividaIds(m);
    }
  }
  load();
}, []);
```
Esta query CONTINUA filtrando por `papel=eq.PRINCIPAL` — anti-dupla-contagem preservada em 7.13. Pessoas usa helper paralelo SEM filtro de papel.

**Existing PAPEL_META (DevedoresDaDivida.jsx L5-12 — REUSAR verbatim em DevedoresDoContrato.jsx):**
```javascript
const PAPEL_META = {
  PRINCIPAL:  { label: "Principal",  bg: "#fef3c7", cor: "#92400e" },
  COOBRIGADO: { label: "Coobrigado", bg: "#ede9fe", cor: "#4c1d95" },
  AVALISTA:   { label: "Avalista",   bg: "#dbeafe", cor: "#1e3a8a" },
  FIADOR:     { label: "Fiador",     bg: "#dcfce7", cor: "#14532d" },
  CONJUGE:    { label: "Cônjuge",    bg: "#fce7f3", cor: "#831843" },
  OUTRO:      { label: "Outro",      bg: "#f1f5f9", cor: "#334155" },
};
```
</interfaces>

<open_questions>
**Q1 — Helper signatures: assinaturas exatas dos 5 helpers contrato-level**

→ **DECISÃO PLANNER (lockada na Task 1):**
```javascript
export async function listarDevedoresDoContrato(contratoId)
  // → Array<{ devedor_id, papel, responsabilidade }> DISTINCT (não 12x repetido)
export async function criarVinculoContratoDevedor(contratoId, devedorId, papel, responsabilidade = 'SOLIDARIA')
  // → N inserts (uma por dívida do contrato), todas mesmo (devedor_id, papel, responsabilidade)
  // Bloqueio: se papel='PRINCIPAL' e contrato JÁ TEM PRINCIPAL → throw new Error("USE_WIZARD_PROMOCAO")
export async function removerVinculoContratoDevedor(contratoId, devedorId)
  // → N deletes (uma por dívida do contrato)
  // Bloqueio: se devedorId é último PRINCIPAL → throw new Error("LAST_PRINCIPAL_BLOCKED")
export async function alterarPapelContratoDevedor(contratoId, devedorId, novoPapel)
  // → N PATCHes (uma por dívida do contrato), todas mudam papel
  // Bloqueio: se novoPapel='PRINCIPAL' e contrato JÁ TEM OUTRO PRINCIPAL → throw new Error("USE_WIZARD_PROMOCAO")
export async function promoverParaPrincipalComDemocao(contratoId, novoDevedorId, novoPapelDoAnterior)
  // → 2N PATCHes pareados (N para promover novo, N para demover anterior ao papel escolhido)
  // novoPapelDoAnterior obrigatório, valida ∈ {COOBRIGADO,AVALISTA,FIADOR,CONJUGE,OUTRO} (5 opções, não inclui PRINCIPAL)
export async function seedDevedoresDoContrato(dividaId, contratoId)
  // → N inserts (uma por devedor do contrato), copiando papel + responsabilidade existentes
  // Se contrato sem devedores na junction (header-only criando primeira dívida) → fallback seedPrincipal(contrato.devedor_id, dividaId)
```

**Q2 — Helper paralelo Pessoas: branch interno OR arquivo separado?**

CONTEXT.md L70-72 marca como Claude's Discretion: "Reuso vs novo helper agruparPagamentosPorDevedor para Pessoas (1:N branch interno OU helper paralelo agruparPagamentosPorDevedorIncluindoSolidarios)."

→ **DECISÃO PLANNER: arquivo separado** — `utils/agruparPagamentosPorDevedorIncluindoSolidarios.js` NEW. Razões:
  (a) Helper 7.10bcd `agruparPagamentosPorDevedor` consumido em Dashboard L568 + parent App L8434 — esses NÃO devem mudar comportamento (preservam dedupe via principaisDividaIds);
  (b) Branch interno requer flag callsite-by-callsite — viola single-responsibility;
  (c) Test trivial fica isolado, não polui suite do helper compartilhado;
  (d) Pattern 7.10bcd D-31: helper-first quando 2+ callsites diferentes precisam de comportamentos divergentes (Dashboard PRINCIPAL-only vs Pessoas all-roles).

**Q3 — Substituir seedPrincipal em App.jsx L3265 ou só em adicionarDocumento?**

→ **DECISÃO PLANNER (commit 07.13b):** **substituir em ambos lugares**:
  - `contratos.js adicionarDocumento` L294-298 → SEMPRE chama seedDevedoresDoContrato (contrato existe por construção)
  - `App.jsx L3262-3268` (fluxo legacy de adicionar dívida manual via DividaForm) → if (novaDiv.contrato_id) seedDevedoresDoContrato; else seedPrincipal (preserva back-compat para dívidas standalone sem contrato — pattern raro mas existente)

**Q4 — Snapshot histórico em registrarEvento (DetalheContrato.jsx L285) com array vs string?**

→ **DECISÃO PLANNER (commit 07.13c):** **array `devedor_ids: [id1, id2, ...]`** no snapshot_campos JSONB. Compat com schema existente (snapshot_campos é JSONB livre); diff entries em DetalheContrato exibe array sem precisar mudar render (apenas formatador opcional). Tipo evento mantém `assuncao_divida` ou `outros` conforme lógica L307-313 atual.

**Q5 — UAT smoke pre-execute (lição feedback_proc_judiciais_create_bug_preexisting.md)?**

→ **DECISÃO PLANNER:** smoke executado em **Plan 02 Task 1** (não em Plan 01 Task 1 pra evitar confusão de ordem). Plan 02 Task 1 = Pre-UAT smoke obrigatório: criar contrato → adicionar documento → vincular devedor extra → remover. Se falhar em prod (não-regressão), PAUSA UAT e abre backlog antes de continuar.
</open_questions>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Service layer multi-devedor por contrato + test trivial 3 it() + estender package.json (commit feat(07.13a))</name>
  <read_first>
    - `.planning/phases/07.13-multiplos-devedores-contrato-solidariedade-passiva/07.13-CONTEXT.md` (D-pre-1, D-pre-4, D-pre-10, D-pre-11, D-pre-12, D-pre-13 — service layer specs)
    - `src/mr-3/mr-cobrancas/src/services/devedoresDividas.js` (existing helpers — PRESERVAR back-compat)
    - `src/mr-3/mr-cobrancas/src/services/__tests__/agruparPagamentosPorDevedor.test.js` (analog vitest style — 7.10bcd precedente)
    - `src/mr-3/mr-cobrancas/package.json` (script test:regressao atual após 7.10bcd inclui 5 test files)
    - `src/mr-3/mr-cobrancas/src/config/supabase.js` (sb helper signature — REST PostgREST)
  </read_first>
  <behavior>
    - **it #1 — criarVinculoContratoDevedor com 12 dívidas**: mock `dbGet("dividas", "contrato_id=eq.X")` retorna 12 rows; helper faz 12 `sb(POST devedores_dividas)` chamadas, todas com mesmo (devedor_id, papel='FIADOR', responsabilidade='SOLIDARIA'). Assert 12 calls + payload shape.
    - **it #2 — listarDevedoresDoContrato retorna DISTINCT**: mock `sb("devedores_dividas?divida_id=in.(...)&select=devedor_id,papel,responsabilidade")` retorna 24 rows (12 dívidas × 2 devedores: PRINCIPAL + FIADOR); helper consolida client-side e retorna 2 entries DISTINCT.
    - **it #3 — seedDevedoresDoContrato em nova dívida copia TODOS**: mock listarDevedoresDoContrato retorna 2 devedores (Mendes PRINCIPAL + João FIADOR); helper faz 2 `sb(POST)` chamadas para a nova dividaId, preservando papel + responsabilidade.
  </behavior>
  <files>
    - src/mr-3/mr-cobrancas/src/services/devedoresDividas.js (MOD — 5 helpers novos + seedDevedoresDoContrato; NÃO remover/alterar helpers existentes)
    - src/mr-3/mr-cobrancas/src/services/__tests__/devedoresDividasContrato.test.js (NEW — 3 it() com vi.mock pra sb)
    - src/mr-3/mr-cobrancas/package.json (MOD — script test:regressao acrescenta devedoresDividasContrato.test.js)
  </files>
  <action>
    **Parte A — Estender `src/mr-3/mr-cobrancas/src/services/devedoresDividas.js`** (adicionar ao FINAL do arquivo, NÃO modificar funções existentes):

    ```javascript
    // ─── PHASE 7.13 — HELPERS CONTRATO-LEVEL (D-pre-10, D-pre-11, D-pre-13) ───

    /**
     * Lista devedores DISTINCT de um contrato com seu papel.
     * Query: SELECT DISTINCT ON (devedor_id) devedor_id, papel, responsabilidade
     *        FROM devedores_dividas WHERE divida_id IN (dividas do contrato)
     * Implementação: 1 query em devedores_dividas filtrando por divida_id IN (...) +
     *                consolidação DISTINCT client-side por devedor_id (papel deve ser
     *                consistente entre as N dívidas do contrato — invariante D-pre-1).
     */
    export async function listarDevedoresDoContrato(contratoId) {
      const { dbGet } = await import("../config/supabase.js");
      const dividas = await dbGet("dividas", `contrato_id=eq.${encodeURIComponent(contratoId)}&select=id`);
      const dividaIds = (Array.isArray(dividas) ? dividas : []).map(d => d.id);
      if (dividaIds.length === 0) return [];
      const inList = dividaIds.map(encodeURIComponent).join(",");
      const rows = await sb(
        `${TABLE}?divida_id=in.(${inList})&select=devedor_id,papel,responsabilidade`
      );
      const seen = new Map();
      (Array.isArray(rows) ? rows : []).forEach(r => {
        const key = String(r.devedor_id);
        if (!seen.has(key)) seen.set(key, { devedor_id: r.devedor_id, papel: r.papel, responsabilidade: r.responsabilidade });
      });
      return Array.from(seen.values());
    }

    /**
     * Vincula devedor X com papel Y a TODAS as dívidas do contrato (D-pre-10 fan-out N inserts).
     * Bloqueio (D-pre-13): se papel='PRINCIPAL' e contrato JÁ TEM outro PRINCIPAL → throw "USE_WIZARD_PROMOCAO".
     * UI deve invocar promoverParaPrincipalComDemocao no lugar.
     */
    export async function criarVinculoContratoDevedor(contratoId, devedorId, papel, responsabilidade = 'SOLIDARIA') {
      const { dbGet } = await import("../config/supabase.js");
      const dividas = await dbGet("dividas", `contrato_id=eq.${encodeURIComponent(contratoId)}&select=id`);
      const dividasArr = Array.isArray(dividas) ? dividas : [];
      if (papel === 'PRINCIPAL' && dividasArr.length > 0) {
        const existingPrincipal = await sb(
          `${TABLE}?divida_id=eq.${encodeURIComponent(dividasArr[0].id)}&papel=eq.PRINCIPAL&select=devedor_id&limit=1`
        );
        if (Array.isArray(existingPrincipal) && existingPrincipal.length > 0
            && String(existingPrincipal[0].devedor_id) !== String(devedorId)) {
          throw new Error("USE_WIZARD_PROMOCAO");
        }
      }
      const results = [];
      for (const d of dividasArr) {
        const r = await sb(TABLE, "POST", {
          devedor_id: devedorId,
          divida_id: String(d.id),
          papel,
          responsabilidade,
          observacao: "",
        });
        results.push(r);
      }
      return results;
    }

    /**
     * Remove vínculo de devedor X de TODAS as dívidas do contrato (D-pre-10 fan-out N deletes).
     * Bloqueio (D-pre-12): se devedorId é o ÚNICO PRINCIPAL → throw "LAST_PRINCIPAL_BLOCKED".
     * UI deve exibir Modal warning (D-05 LOCKED, sem window.confirm).
     */
    export async function removerVinculoContratoDevedor(contratoId, devedorId) {
      const { dbGet } = await import("../config/supabase.js");
      const dividas = await dbGet("dividas", `contrato_id=eq.${encodeURIComponent(contratoId)}&select=id`);
      const dividasArr = Array.isArray(dividas) ? dividas : [];
      if (dividasArr.length === 0) return [];
      // Check: devedor é PRINCIPAL? Se sim, é o único?
      const minhasRows = await sb(
        `${TABLE}?divida_id=eq.${encodeURIComponent(dividasArr[0].id)}&devedor_id=eq.${devedorId}&select=papel&limit=1`
      );
      const ehPrincipal = Array.isArray(minhasRows) && minhasRows.length > 0 && minhasRows[0].papel === 'PRINCIPAL';
      if (ehPrincipal) {
        const principaisCount = await sb(
          `${TABLE}?divida_id=eq.${encodeURIComponent(dividasArr[0].id)}&papel=eq.PRINCIPAL&select=devedor_id`
        );
        if (Array.isArray(principaisCount) && principaisCount.length === 1) {
          throw new Error("LAST_PRINCIPAL_BLOCKED");
        }
      }
      const results = [];
      for (const d of dividasArr) {
        const r = await sb(
          `${TABLE}?divida_id=eq.${encodeURIComponent(d.id)}&devedor_id=eq.${devedorId}`,
          "DELETE"
        );
        results.push(r);
      }
      return results;
    }

    /**
     * Altera papel de devedor X em TODAS as dívidas do contrato (D-pre-10 fan-out N PATCHes).
     * Bloqueio (D-pre-13): se novoPapel='PRINCIPAL' e contrato JÁ TEM outro PRINCIPAL → throw "USE_WIZARD_PROMOCAO".
     * UI deve invocar promoverParaPrincipalComDemocao no lugar.
     */
    export async function alterarPapelContratoDevedor(contratoId, devedorId, novoPapel) {
      const { dbGet } = await import("../config/supabase.js");
      const dividas = await dbGet("dividas", `contrato_id=eq.${encodeURIComponent(contratoId)}&select=id`);
      const dividasArr = Array.isArray(dividas) ? dividas : [];
      if (novoPapel === 'PRINCIPAL' && dividasArr.length > 0) {
        const existingPrincipal = await sb(
          `${TABLE}?divida_id=eq.${encodeURIComponent(dividasArr[0].id)}&papel=eq.PRINCIPAL&select=devedor_id&limit=1`
        );
        if (Array.isArray(existingPrincipal) && existingPrincipal.length > 0
            && String(existingPrincipal[0].devedor_id) !== String(devedorId)) {
          throw new Error("USE_WIZARD_PROMOCAO");
        }
      }
      const results = [];
      for (const d of dividasArr) {
        const r = await sb(
          `${TABLE}?divida_id=eq.${encodeURIComponent(d.id)}&devedor_id=eq.${devedorId}`,
          "PATCH",
          { papel: novoPapel, updated_at: new Date().toISOString() }
        );
        results.push(r);
      }
      return results;
    }

    /**
     * WIZARD D-pre-13: promove novoDevedor a PRINCIPAL e demove o anterior ao papel escolhido pelo usuário.
     * 2N PATCHes pareados — N pra promover, N pra demover. Atomicidade reexecutável (padrão 7.2 D-04).
     *
     * @param contratoId UUID do contrato
     * @param novoDevedorId BIGINT id do devedor que será PRINCIPAL
     * @param novoPapelDoAnterior 'COOBRIGADO'|'AVALISTA'|'FIADOR'|'CONJUGE'|'OUTRO' (5 opções, NÃO 'PRINCIPAL')
     */
    export async function promoverParaPrincipalComDemocao(contratoId, novoDevedorId, novoPapelDoAnterior) {
      const VALID_PAPEIS = new Set(['COOBRIGADO', 'AVALISTA', 'FIADOR', 'CONJUGE', 'OUTRO']);
      if (!VALID_PAPEIS.has(novoPapelDoAnterior)) {
        throw new Error(`Papel inválido para anterior: ${novoPapelDoAnterior}. Esperado: ${[...VALID_PAPEIS].join(', ')}.`);
      }
      const { dbGet } = await import("../config/supabase.js");
      const dividas = await dbGet("dividas", `contrato_id=eq.${encodeURIComponent(contratoId)}&select=id`);
      const dividasArr = Array.isArray(dividas) ? dividas : [];
      if (dividasArr.length === 0) return { promovido: [], demovido: [] };

      // Identifica PRINCIPAL atual (1 por dívida; D-pre-1 garante consistência cross-dívidas)
      const principalRow = await sb(
        `${TABLE}?divida_id=eq.${encodeURIComponent(dividasArr[0].id)}&papel=eq.PRINCIPAL&select=devedor_id&limit=1`
      );
      const anteriorDevedorId = (Array.isArray(principalRow) && principalRow.length > 0)
        ? principalRow[0].devedor_id
        : null;
      if (anteriorDevedorId === null) {
        throw new Error("NO_PRINCIPAL_TO_DEMOTE");
      }
      if (String(anteriorDevedorId) === String(novoDevedorId)) {
        return { promovido: [], demovido: [] };  // no-op
      }

      // Demove anterior PRIMEIRO (libera UNIQUE INDEX papel='PRINCIPAL') — N PATCHes
      const demovido = [];
      for (const d of dividasArr) {
        const r = await sb(
          `${TABLE}?divida_id=eq.${encodeURIComponent(d.id)}&devedor_id=eq.${anteriorDevedorId}`,
          "PATCH",
          { papel: novoPapelDoAnterior, updated_at: new Date().toISOString() }
        );
        demovido.push(r);
      }
      // Promove novo — N PATCHes
      const promovido = [];
      for (const d of dividasArr) {
        const r = await sb(
          `${TABLE}?divida_id=eq.${encodeURIComponent(d.id)}&devedor_id=eq.${novoDevedorId}`,
          "PATCH",
          { papel: 'PRINCIPAL', updated_at: new Date().toISOString() }
        );
        promovido.push(r);
      }
      return { promovido, demovido, anteriorDevedorId };
    }

    /**
     * Estende seedPrincipal: copia TODOS os devedores existentes do contrato para a nova dívida,
     * preservando papel + responsabilidade (D-pre-11). Substitui seedPrincipal isolado em
     * adicionarDocumento (commit 07.13b) e em App.jsx fluxo legacy (apenas se novaDiv.contrato_id).
     *
     * Fallback (contrato header-only, primeira dívida sendo criada): se listarDevedoresDoContrato
     * retorna [] → chama seedPrincipal(contrato.devedor_id, dividaId) usando legacy denormalização.
     */
    export async function seedDevedoresDoContrato(dividaId, contratoId) {
      const devedoresDoContrato = await listarDevedoresDoContrato(contratoId);
      if (devedoresDoContrato.length === 0) {
        // Header-only contract (primeira dívida): fallback usando dividas.devedor_id legacy
        const { dbGet } = await import("../config/supabase.js");
        const contratoArr = await dbGet("contratos_dividas", `id=eq.${encodeURIComponent(contratoId)}&select=devedor_id&limit=1`);
        const contrato = Array.isArray(contratoArr) ? contratoArr[0] : null;
        if (contrato?.devedor_id) {
          return [await seedPrincipal(contrato.devedor_id, dividaId)];
        }
        return [];
      }
      const results = [];
      for (const dev of devedoresDoContrato) {
        const r = await sb(TABLE, "POST", {
          devedor_id: dev.devedor_id,
          divida_id: String(dividaId),
          papel: dev.papel,
          responsabilidade: dev.responsabilidade,
          observacao: "",
        });
        results.push(r);
      }
      return results;
    }
    ```

    **Parte B — Criar `src/mr-3/mr-cobrancas/src/services/__tests__/devedoresDividasContrato.test.js`** (3 it() com vi.mock):

    ```javascript
    /**
     * Phase 7.13 — Test trivial helpers contrato-level (D-pre-10, D-pre-11, D-pre-13)
     *
     * Defesa contra regressão silenciosa de cascade fan-out + wizard pareado
     * (lição feedback_shields_wrapper_must_test_equivalence.md da 7.8.2a).
     *
     * 3 it() blocks:
     *   1. criarVinculoContratoDevedor: 12 dívidas → 12 inserts
     *   2. listarDevedoresDoContrato: 24 rows (12 dívidas × 2 devedores) → 2 entries DISTINCT
     *   3. seedDevedoresDoContrato: copia TODOS os devedores existentes do contrato pra nova dívida
     */

    import { describe, it, expect, vi, beforeEach } from 'vitest';

    // Mocks de sb + dbGet (ambos exportados de config/supabase.js — fix audit deviation)
    const mockSb = vi.fn();
    const mockDbGet = vi.fn();

    vi.mock('../../config/supabase.js', () => ({
      sb: (...args) => mockSb(...args),
      dbGet: (...args) => mockDbGet(...args),
    }));

    describe('devedoresDividas — helpers contrato-level (Phase 7.13 D-pre-10/11/13)', () => {
      beforeEach(() => {
        mockSb.mockReset();
        mockDbGet.mockReset();
      });

      it('criarVinculoContratoDevedor: 12 dívidas → 12 inserts (FIADOR, SOLIDARIA)', async () => {
        const { criarVinculoContratoDevedor } = await import('../devedoresDividas.js');
        const dividas = Array.from({ length: 12 }, (_, i) => ({ id: `div-${i + 1}` }));
        mockDbGet.mockResolvedValueOnce(dividas);   // listar dividas do contrato
        // não-PRINCIPAL: skip pre-check de PRINCIPAL existente
        mockSb.mockResolvedValue([{ id: 'inserted' }]);

        await criarVinculoContratoDevedor('contrato-X', 999, 'FIADOR');

        // 12 chamadas POST devedores_dividas
        const postCalls = mockSb.mock.calls.filter(c => c[1] === 'POST');
        expect(postCalls).toHaveLength(12);
        postCalls.forEach((call, i) => {
          expect(call[0]).toBe('devedores_dividas');
          expect(call[2]).toMatchObject({
            devedor_id: 999,
            divida_id: `div-${i + 1}`,
            papel: 'FIADOR',
            responsabilidade: 'SOLIDARIA',
          });
        });
      });

      it('listarDevedoresDoContrato: 24 rows (12 dívidas × 2 devedores) → 2 entries DISTINCT', async () => {
        const { listarDevedoresDoContrato } = await import('../devedoresDividas.js');
        mockDbGet.mockResolvedValueOnce(
          Array.from({ length: 12 }, (_, i) => ({ id: `div-${i + 1}` }))
        );
        const rows = [];
        for (let i = 1; i <= 12; i++) {
          rows.push({ devedor_id: 100, papel: 'PRINCIPAL', responsabilidade: 'SOLIDARIA' });
          rows.push({ devedor_id: 200, papel: 'FIADOR', responsabilidade: 'SOLIDARIA' });
        }
        mockSb.mockResolvedValueOnce(rows);

        const result = await listarDevedoresDoContrato('contrato-X');
        expect(result).toHaveLength(2);
        expect(result.find(r => r.devedor_id === 100).papel).toBe('PRINCIPAL');
        expect(result.find(r => r.devedor_id === 200).papel).toBe('FIADOR');
      });

      it('seedDevedoresDoContrato: copia TODOS os devedores existentes pra nova dívida', async () => {
        const { seedDevedoresDoContrato } = await import('../devedoresDividas.js');
        // listarDevedoresDoContrato retornará 2 devedores
        mockDbGet.mockResolvedValueOnce([{ id: 'div-existente-1' }]);   // dividas do contrato
        mockSb.mockResolvedValueOnce([
          { devedor_id: 100, papel: 'PRINCIPAL', responsabilidade: 'SOLIDARIA' },
          { devedor_id: 200, papel: 'FIADOR', responsabilidade: 'SOLIDARIA' },
        ]);   // listar rows distinct
        mockSb.mockResolvedValue([{ id: 'inserted' }]);   // inserts subsequentes

        await seedDevedoresDoContrato('div-NOVA', 'contrato-X');

        // 2 chamadas POST (uma por devedor)
        const postCalls = mockSb.mock.calls.filter(c => c[1] === 'POST');
        expect(postCalls).toHaveLength(2);
        expect(postCalls[0][2]).toMatchObject({ devedor_id: 100, divida_id: 'div-NOVA', papel: 'PRINCIPAL' });
        expect(postCalls[1][2]).toMatchObject({ devedor_id: 200, divida_id: 'div-NOVA', papel: 'FIADOR' });
      });
    });
    ```

    **Parte C — Atualizar `src/mr-3/mr-cobrancas/package.json`** (script test:regressao estende):

    Localizar script atual (post-7.10bcd):
    ```json
    "test:regressao": "vitest run src/services/__tests__/calculos.test.js src/services/__tests__/saldoAtualizadoCache.test.js src/services/__tests__/custasCache.test.js src/services/__tests__/processosJudiciaisFilter.test.js src/services/__tests__/agruparPagamentosPorDevedor.test.js"
    ```

    Substituir por (acrescenta `devedoresDividasContrato.test.js` no FIM):
    ```json
    "test:regressao": "vitest run src/services/__tests__/calculos.test.js src/services/__tests__/saldoAtualizadoCache.test.js src/services/__tests__/custasCache.test.js src/services/__tests__/processosJudiciaisFilter.test.js src/services/__tests__/agruparPagamentosPorDevedor.test.js src/services/__tests__/devedoresDividasContrato.test.js"
    ```

    **Parte D — Run regressão + commit:**
    ```bash
    cd src/mr-3/mr-cobrancas
    npm run test:regressao   # esperado 28/28 PASS (25 base + 3 novos)
    npm run build            # esperado exit 0
    cd ..
    git add src/mr-cobrancas/src/services/devedoresDividas.js src/mr-cobrancas/src/services/__tests__/devedoresDividasContrato.test.js src/mr-cobrancas/package.json
    git commit -m "feat(07.13a): service layer multi-devedor por contrato

    Helpers contrato-level (D-pre-10, D-pre-11, D-pre-13):
    - listarDevedoresDoContrato (DISTINCT por contrato)
    - criarVinculoContratoDevedor (N inserts fan-out, bloqueio USE_WIZARD_PROMOCAO)
    - removerVinculoContratoDevedor (N deletes, bloqueio LAST_PRINCIPAL_BLOCKED)
    - alterarPapelContratoDevedor (N PATCHes simples sem mudar PRINCIPAL)
    - promoverParaPrincipalComDemocao (wizard 2-steps, N PATCHes pareados)
    - seedDevedoresDoContrato (estende seedPrincipal pra TODOS devedores; fallback header-only)

    Test trivial 3 it() (regressão 28/28 cumulativa).
    Helpers existentes PRESERVADOS — back-compat com DevedoresDaDivida.jsx (read-only mode em 07.13d).
    Schema zero-diff: reusa devedores_dividas (Migrações 001+002, D-pre-4)."
    ```
  </action>
  <verify>
    <automated>
      cd src/mr-3/mr-cobrancas && npm run test:regressao 2>&1 | grep -E "Tests.*28 passed" || exit 1
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export async function listarDevedoresDoContrato" src/mr-3/mr-cobrancas/src/services/devedoresDividas.js` exit 0
    - `grep -q "export async function criarVinculoContratoDevedor" src/mr-3/mr-cobrancas/src/services/devedoresDividas.js` exit 0
    - `grep -q "export async function removerVinculoContratoDevedor" src/mr-3/mr-cobrancas/src/services/devedoresDividas.js` exit 0
    - `grep -q "export async function alterarPapelContratoDevedor" src/mr-3/mr-cobrancas/src/services/devedoresDividas.js` exit 0
    - `grep -q "export async function promoverParaPrincipalComDemocao" src/mr-3/mr-cobrancas/src/services/devedoresDividas.js` exit 0
    - `grep -q "export async function seedDevedoresDoContrato" src/mr-3/mr-cobrancas/src/services/devedoresDividas.js` exit 0
    - `grep -q "USE_WIZARD_PROMOCAO" src/mr-3/mr-cobrancas/src/services/devedoresDividas.js` exit 0 (bloqueio explícito)
    - `grep -q "LAST_PRINCIPAL_BLOCKED" src/mr-3/mr-cobrancas/src/services/devedoresDividas.js` exit 0 (bloqueio explícito)
    - Helpers existentes PRESERVADOS: `grep -E "^export (async )?function (listarParticipantes|adicionarParticipante|alterarPapel|removerParticipante|seedPrincipal)" src/mr-3/mr-cobrancas/src/services/devedoresDividas.js | wc -l` >= 5
    - `cd src/mr-3/mr-cobrancas && npm run test:regressao` exit 0 + output mostra "28 passed"
    - `cd src/mr-3/mr-cobrancas && npm run build` exit 0
    - **D-01 motor untouched (gate por commit):** `git -C src/mr-3 show HEAD --name-only | grep -E "(devedorCalc|pagamentos|dividas)\.js"` exit 1 (motor files AUSENTES neste commit; nota: pagamentos.js arquivo ≠ pagamentos_divida tabela)
    - Commit criado com header verbatim `feat(07.13a): service layer multi-devedor por contrato` — `git -C src/mr-3 log -1 --format=%s | grep -q "^feat(07.13a):"` exit 0
  </acceptance_criteria>
  <done>
    - Service layer estendido com 5 helpers contrato-level + seedDevedoresDoContrato
    - Test trivial 3 it() PASS (regressão 28/28 cumulativa)
    - package.json estendido
    - Commit `feat(07.13a)` criado no submódulo
    - D-01 motor intocado neste commit (verificado via grep --name-only)
    - Helpers existentes preservados (back-compat DevedoresDaDivida.jsx via useDevedoresDividas)
  </done>
</task>

<task type="auto">
  <name>Task 2: Cascade nova dívida → seedDevedoresDoContrato (commit feat(07.13b))</name>
  <read_first>
    - `src/mr-3/mr-cobrancas/src/services/contratos.js` (L256-301 adicionarDocumento — alvo da edição)
    - `src/mr-3/mr-cobrancas/src/App.jsx` L3262-3268 (fluxo legacy seedPrincipal — alvo se novaDiv.contrato_id)
    - `src/mr-3/mr-cobrancas/src/services/devedoresDividas.js` (seedDevedoresDoContrato criado em Task 1)
    - `.planning/phases/07.13-multiplos-devedores-contrato-solidariedade-passiva/07.13-CONTEXT.md` (D-pre-11 cascade)
  </read_first>
  <files>
    - src/mr-3/mr-cobrancas/src/services/contratos.js (MOD — adicionarDocumento L294-298 + import seedDevedoresDoContrato)
    - src/mr-3/mr-cobrancas/src/App.jsx (MOD — L3262-3268 conditional seedDevedoresDoContrato vs seedPrincipal)
  </files>
  <action>
    **Parte A — Editar `src/mr-3/mr-cobrancas/src/services/contratos.js`:**

    1. Adicionar no topo do arquivo (junto com outros imports):
       ```javascript
       import { seedDevedoresDoContrato } from "./devedoresDividas.js";
       ```

    2. Substituir loop L294-298 atual de `adicionarDocumento`:
       ```javascript
       // ANTES (L294-298):
       const rows = [];
       for (const p of parcelasPayload) {
         const r = await dbInsert("dividas", p);
         rows.push(Array.isArray(r) ? r[0] : r);
       }
       await recalcularTotaisContrato(contratoId);

       // DEPOIS (commit 07.13b):
       const rows = [];
       for (const p of parcelasPayload) {
         const r = await dbInsert("dividas", p);
         const novaDivida = Array.isArray(r) ? r[0] : r;
         rows.push(novaDivida);
         // Phase 7.13 D-pre-11: cascade fan-out — popula junction com TODOS os devedores
         // do contrato (não só PRINCIPAL legado). Substitui seedPrincipal isolado.
         if (novaDivida?.id) {
           await seedDevedoresDoContrato(novaDivida.id, contratoId);
         }
       }
       await recalcularTotaisContrato(contratoId);
       ```

    **Parte B — Editar `src/mr-3/mr-cobrancas/src/App.jsx` L3262-3268** (fluxo legacy de adicionar dívida manual):

    ```javascript
    // ANTES (L3262-3268):
    try {
      const { seedPrincipal } = await import("./services/devedoresDividas.js");
      await seedPrincipal(sel.id, novaDiv.id);
    } catch (seedErr) {
      console.warn("seedPrincipal failed (non-critical):", seedErr);
    }

    // DEPOIS (commit 07.13b):
    try {
      if (novaDiv.contrato_id) {
        // Phase 7.13 D-pre-11: dívida em contrato → cascade copia TODOS os devedores existentes
        const { seedDevedoresDoContrato } = await import("./services/devedoresDividas.js");
        await seedDevedoresDoContrato(novaDiv.id, novaDiv.contrato_id);
      } else {
        // Dívida standalone (sem contrato) — preserva back-compat seedPrincipal
        const { seedPrincipal } = await import("./services/devedoresDividas.js");
        await seedPrincipal(sel.id, novaDiv.id);
      }
    } catch (seedErr) {
      console.warn("seed devedores failed (non-critical):", seedErr);
    }
    ```

    **Parte C — Run regressão + commit:**
    ```bash
    cd src/mr-3/mr-cobrancas
    npm run test:regressao   # esperado 28/28 PASS (não mexe em testes; sanity)
    npm run build            # esperado exit 0
    cd ..
    git add src/mr-cobrancas/src/services/contratos.js src/mr-cobrancas/src/App.jsx
    git commit -m "feat(07.13b): cascade nova divida → seedDevedoresDoContrato

    contratos.js adicionarDocumento (L296-298): após cada dbInsert(dividas)
    chama seedDevedoresDoContrato(novaDiv.id, contratoId) — substitui seedPrincipal
    isolado, popula junction com TODOS os devedores existentes do contrato (D-pre-11).

    App.jsx L3262-3268 (fluxo legacy adicionar dívida manual): conditional —
    if (novaDiv.contrato_id) seedDevedoresDoContrato; else seedPrincipal (back-compat
    dívidas standalone sem contrato).

    Contrato existente: zero impacto (junction já populada via seed retrocompat 002 §7).
    Nova parcela em contrato com N devedores: N rows na junction (não só PRINCIPAL legado)."
    ```
  </action>
  <verify>
    <automated>
      cd src/mr-3/mr-cobrancas && npm run test:regressao 2>&1 | grep -E "Tests.*28 passed" || exit 1
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "import { seedDevedoresDoContrato }" src/mr-3/mr-cobrancas/src/services/contratos.js` exit 0
    - `grep -q "await seedDevedoresDoContrato(novaDivida.id, contratoId)" src/mr-3/mr-cobrancas/src/services/contratos.js` exit 0
    - `grep -q "await seedDevedoresDoContrato(novaDiv.id, novaDiv.contrato_id)" src/mr-3/mr-cobrancas/src/App.jsx` exit 0
    - `grep -q "if (novaDiv.contrato_id)" src/mr-3/mr-cobrancas/src/App.jsx` exit 0 (conditional preservada)
    - `cd src/mr-3/mr-cobrancas && npm run test:regressao` exit 0 + "28 passed"
    - `cd src/mr-3/mr-cobrancas && npm run build` exit 0
    - **D-01 motor untouched (gate por commit):** `git -C src/mr-3 show HEAD --name-only | grep -E "src/utils/devedorCalc\.js|src/services/pagamentos\.js$|src/services/dividas\.js$"` exit 1
    - Commit criado: `git -C src/mr-3 log -1 --format=%s | grep -q "^feat(07.13b):"` exit 0
  </acceptance_criteria>
  <done>
    - adicionarDocumento estende cascade pra TODOS os devedores do contrato
    - App.jsx fluxo legacy preserva seedPrincipal como fallback (back-compat dívidas standalone)
    - Commit `feat(07.13b)` criado, regressão 28/28 PASS, build verde, D-01 motor intocado
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UI DetalheContrato componente multi-devedor + Modal wizard D-pre-13 + Modal warning último PRINCIPAL (commit feat(07.13c)) + CHECKPOINT INTERNO #1</name>
  <read_first>
    - `src/mr-3/mr-cobrancas/src/components/DevedoresDaDivida.jsx` (clone adaptado — PAPEL_META, AdicionarParticipanteModal, lista vertical pattern)
    - `src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx` L240-300 (validação devedorAlterado + snapshot histórico) + L571 (render devedor.find)
    - `src/mr-3/mr-cobrancas/src/components/Modal.jsx` (pattern de Modal warning — D-05)
    - `src/mr-3/mr-cobrancas/src/services/devedoresDividas.js` (helpers Task 1: criarVinculoContratoDevedor, removerVinculoContratoDevedor, alterarPapelContratoDevedor, promoverParaPrincipalComDemocao, listarDevedoresDoContrato)
    - `.planning/phases/07.13-multiplos-devedores-contrato-solidariedade-passiva/07.13-CONTEXT.md` (D-pre-9, D-pre-12, D-pre-13)
    - `memory/feedback_d01_relaxation_protocol.md` (D-01 NÃO relaxa nesta phase)
  </read_first>
  <what-built>
    Componente NOVO `DevedoresDoContrato.jsx` (clone adaptado de DevedoresDaDivida.jsx) substitui dropdown único de devedor em DetalheContrato.jsx (L253, L285, L571). Inclui:
    - Lista vertical com chip de papel + nome + cpf (PAPEL_META reutilizado verbatim)
    - Botão "+ Adicionar Devedor" → Modal AdicionarDevedorContratoModal (busca + select papel, fan-out N inserts via criarVinculoContratoDevedor)
    - Botão remover ✕ → confirma + chama removerVinculoContratoDevedor (N deletes); se LAST_PRINCIPAL_BLOCKED throw → exibe Modal warning (D-05, sem window.confirm)
    - Botão promover 👑 → SE contrato já tem PRINCIPAL (qualquer outro devedor) abre **Modal wizard D-pre-13**: "Contrato já tem PRINCIPAL ([nome]). Promover [novo] a PRINCIPAL e demover [nome] a:" + dropdown 5 papéis SEM DEFAULT. Confirmar → promoverParaPrincipalComDemocao(contratoId, novoDevedorId, papelEscolhido)

    DetalheContrato.jsx muda em 3 pontos:
    - L240-253: validação `devedorAlterado` legacy (single-FK) → SEMPRE recarrega componente DevedoresDoContrato após edição header (sem cascade automático — cascade é via componente standalone)
    - L282-285: snapshot `devedor_id` em registrarEvento → array `devedor_ids: [...]` no JSONB campos
    - L571: `const devedor = devedores.find(...)` → componente DevedoresDoContrato renderizado em seção dedicada após Header / Encargos
  </what-built>
  <how-to-verify>
    **CHECKPOINT INTERNO #1 — Validação visual + funcional do componente multi-devedor.**

    O executor já criou o commit `feat(07.13c)`. Antes de prosseguir para Task 4, usuário precisa validar manualmente em localhost:

    **Preparação:**
    ```bash
    cd src/mr-3/mr-cobrancas
    npm install   # se necessário
    npm run dev   # inicia Vite em http://localhost:5173
    ```
    Abrir `http://localhost:5173`, logar.

    **Validações (marcar PASS/FAIL em cada):**

    1. **Render limpo da nova seção em DetalheContrato:**
       - Abrir contrato MENDES (ou qualquer contrato existente). Após Header/Encargos, deve aparecer seção "Devedores do Contrato" com 1 chip "Mendes (Principal)" (junction já populada via seed retrocompat).
       - PASS [ ] / FAIL [ ]

    2. **Adicionar devedor extra (FIADOR):**
       - Clicar "+ Adicionar Devedor". Modal abre. Buscar "João" (ou cadastrar novo). Selecionar. Papel="Fiador". Confirmar.
       - Toast "Devedor adicionado". Lista atualiza para 2 chips: "Mendes (Principal)" + "João (Fiador)".
       - Verificar Supabase console: SELECT count(*) FROM devedores_dividas WHERE divida_id IN (SELECT id FROM dividas WHERE contrato_id = '<MENDES>') AND devedor_id = <JOÃO> → deve ser N (= num parcelas do contrato MENDES).
       - PASS [ ] / FAIL [ ]   N inserts: ____

    3. **Remover devedor extra (não-PRINCIPAL):**
       - Clicar ✕ em "João (Fiador)". Confirmação. Confirma.
       - Toast "Removido". Lista volta a 1 chip "Mendes (Principal)".
       - PASS [ ] / FAIL [ ]

    4. **Tentar remover último PRINCIPAL (Modal warning D-05):**
       - Clicar ✕ em "Mendes (Principal)" (único PRINCIPAL).
       - Esperado: **Modal warning customizado** (NÃO window.confirm) com texto tipo "Promova outro devedor a Principal antes de remover este — não é possível remover o último PRINCIPAL". Botão "Entendido" fecha. Bloqueado, sem DELETE.
       - PASS [ ] / FAIL [ ]

    5. **Wizard D-pre-13 — promover não-PRINCIPAL com PRINCIPAL existente:**
       - Adicionar João como FIADOR (passo 2 novamente).
       - Clicar 👑 em "João (Fiador)" (promover).
       - Esperado: **Modal wizard** com texto "Contrato já tem PRINCIPAL (Mendes). Promover João a PRINCIPAL e demover Mendes a:" + dropdown com 5 opções (COOBRIGADO, AVALISTA, FIADOR, CONJUGE, OUTRO) **sem nada selecionado por default**. Botões "Confirmar" (desabilitado se sem seleção) + "Cancelar". (Schema CHECK só permite estes 5 + PRINCIPAL.)
       - Selecionar "Coobrigado". Confirmar.
       - Toast "Promoção concluída". Lista atualiza para "João (Principal)" + "Mendes (Coobrigado)".
       - PASS [ ] / FAIL [ ]

    6. **Verificar UNIQUE INDEX preservado pós-wizard:**
       - SELECT divida_id, count(*) FROM devedores_dividas WHERE divida_id IN (SELECT id FROM dividas WHERE contrato_id = '<MENDES>') AND papel='PRINCIPAL' GROUP BY divida_id → deve ser exatamente 1 por dívida (UNIQUE INDEX papel='PRINCIPAL' por divida_id).
       - PASS [ ] / FAIL [ ]

    7. **Revert estado para próximas tasks:**
       - Promover Mendes de volta a PRINCIPAL (mesmo wizard) com João → FIADOR.
       - Remover João.
       - Estado final: 1 chip "Mendes (Principal)" como inicial.
       - PASS [ ] / FAIL [ ]

    **Build/test gates (executor já validou, usuário pode re-rodar pra confirmar):**
    ```bash
    cd src/mr-3/mr-cobrancas
    npm run test:regressao   # 28/28 PASS
    npm run build            # exit 0
    ```

    **D-01 motor untouched (gate por commit):**
    ```bash
    git -C src/mr-3 show HEAD --name-only | grep -E "src/utils/devedorCalc\.js|src/services/pagamentos\.js$|src/services/dividas\.js$"
    # Esperado: exit 1 (zero matches)
    ```

    **Se algum item FAIL:** responder `FAIL: <item-num>: <sintoma>`. Executor revisa em modo gap-closure.
  </how-to-verify>
  <resume-signal>
    Digite `APROVADO` se TODOS os 7 items PASS. Caso contrário, responda `FAIL: <item>: <sintoma>` e executor abre gap-closure no Plan 01.
  </resume-signal>
  <acceptance_criteria>
    - Arquivo NEW: `test -f src/mr-3/mr-cobrancas/src/components/DevedoresDoContrato.jsx` exit 0
    - `grep -q "function DevedoresDoContrato" src/mr-3/mr-cobrancas/src/components/DevedoresDoContrato.jsx` exit 0
    - `grep -q "function AdicionarDevedorContratoModal" src/mr-3/mr-cobrancas/src/components/DevedoresDoContrato.jsx` exit 0
    - `grep -q "function PromoverPrincipalWizardModal\|wizard\|promoverParaPrincipalComDemocao" src/mr-3/mr-cobrancas/src/components/DevedoresDoContrato.jsx` exit 0 (wizard D-pre-13)
    - `grep -c "window.confirm" src/mr-3/mr-cobrancas/src/components/DevedoresDoContrato.jsx` retorna 0 (D-05 LOCKED — sem window.confirm em fluxos de PRINCIPAL)
    - `grep -q "DevedoresDoContrato" src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx` exit 0 (componente integrado)
    - `grep -q "PAPEL_META" src/mr-3/mr-cobrancas/src/components/DevedoresDoContrato.jsx` exit 0 (reusa dicionário visual)
    - **Shield D-02 strict imports** (lição feedback_grep_shield_strict_imports.md): `grep -E "^import .* from \"\\./" src/mr-3/mr-cobrancas/src/components/DevedoresDoContrato.jsx | wc -l` retorna >= 2 (imports de devedoresDividas + Modal)
    - `cd src/mr-3/mr-cobrancas && npm run test:regressao` exit 0 + "28 passed"
    - `cd src/mr-3/mr-cobrancas && npm run build` exit 0
    - **D-01 motor untouched (cumulativo via gate por commit):** `git -C src/mr-3 show HEAD --name-only | grep -E "src/utils/devedorCalc\.js|src/services/pagamentos\.js$|src/services/dividas\.js$"` exit 1
    - Commit criado: `git -C src/mr-3 log -1 --format=%s | grep -q "^feat(07.13c):"` exit 0
    - Usuário respondeu `APROVADO` em PAUSA #1 (todos 7 items visuais PASS)
  </acceptance_criteria>
  <done>
    - Componente DevedoresDoContrato.jsx criado com 3 modais (Adicionar, Wizard promoção, Warning último PRINCIPAL)
    - DetalheContrato.jsx substituiu dropdown único em 3 pontos (L253/L285/L571)
    - Sem window.confirm em fluxos de PRINCIPAL (D-05 LOCKED)
    - Wizard D-pre-13 funcional com dropdown 5 papéis SEM default
    - Modal warning bloqueia remoção último PRINCIPAL
    - Commit `feat(07.13c)` criado, regressão 28/28 PASS, build verde, D-01 motor intocado
    - PAUSA #1 APROVADO pelo usuário
  </done>
</task>

<task type="auto">
  <name>Task 4: DetalheDivida read-only mode (commit feat(07.13d))</name>
  <read_first>
    - `src/mr-3/mr-cobrancas/src/components/DevedoresDaDivida.jsx` (L20 signature + L36-42 botão Adicionar + L88-117 botão remover + L119-134 botão promover)
    - `src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx` L191-201 (embed atual de DevedoresDaDivida)
    - `src/mr-3/mr-cobrancas/src/App.jsx` L3889-3893 (embed adicional em fluxo de criação de dívida)
    - `.planning/phases/07.13-multiplos-devedores-contrato-solidariedade-passiva/07.13-CONTEXT.md` (D-pre-9 read-only)
  </read_first>
  <files>
    - src/mr-3/mr-cobrancas/src/components/DevedoresDaDivida.jsx (MOD — prop readOnly + 3 conditional renders)
    - src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx (MOD — L195 passa readOnly={true})
    - src/mr-3/mr-cobrancas/src/App.jsx (MOD — L3889 passa readOnly={true})
  </files>
  <action>
    **Parte A — Editar `src/mr-3/mr-cobrancas/src/components/DevedoresDaDivida.jsx`:**

    1. Atualizar signature L20:
       ```javascript
       // ANTES:
       export default function DevedoresDaDivida({ dividaId, devedores = [], devedorAtualId, onRemovePrincipal }) {
       // DEPOIS:
       export default function DevedoresDaDivida({ dividaId, devedores = [], devedorAtualId, onRemovePrincipal, readOnly = false }) {
       ```

    2. Conditional render do botão "+ Adicionar" L36-42:
       ```javascript
       // ANTES:
       <button onClick={() => setShowModal(true)} ...>+ Adicionar</button>
       // DEPOIS (envolver em conditional):
       {!readOnly && (
         <button onClick={() => setShowModal(true)} ...>+ Adicionar</button>
       )}
       ```

    3. Conditional render do botão remover ✕ L89-118:
       ```javascript
       // ANTES:
       {!isAtual && (
         <button onClick={async () => { ... }} ...>✕</button>
       )}
       // DEPOIS:
       {!readOnly && !isAtual && (
         <button onClick={async () => { ... }} ...>✕</button>
       )}
       ```

    4. Conditional render do botão promover 👑 L119-134:
       ```javascript
       // ANTES:
       {p.papel !== "PRINCIPAL" && (
         <button title="Promover a Principal" onClick={...}>👑</button>
       )}
       // DEPOIS:
       {!readOnly && p.papel !== "PRINCIPAL" && (
         <button title="Promover a Principal" onClick={...}>👑</button>
       )}
       ```

    5. Conditional render do AdicionarParticipanteModal L139-155:
       ```javascript
       // ANTES:
       {showModal && (<AdicionarParticipanteModal ... />)}
       // DEPOIS:
       {!readOnly && showModal && (<AdicionarParticipanteModal ... />)}
       ```

    **Parte B — Editar `src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx` L195:**
    ```javascript
    // ANTES:
    <DevedoresDaDivida
      dividaId={divida.id}
      devedores={devedores}
      devedorAtualId={null}
      onRemovePrincipal={handleRemovePrincipalWarning}
    />

    // DEPOIS (commit 07.13d — D-pre-9 read-only):
    <DevedoresDaDivida
      dividaId={divida.id}
      devedores={devedores}
      devedorAtualId={null}
      onRemovePrincipal={handleRemovePrincipalWarning}
      readOnly={true}
    />
    ```

    **Parte C — Editar `src/mr-3/mr-cobrancas/src/App.jsx` L3889-3893:**
    ```javascript
    // ANTES:
    <DevedoresDaDivida
      dividaId={String(div.id)}
      devedores={devedores}
      devedorAtualId={sel.id}
    />

    // DEPOIS (commit 07.13d):
    <DevedoresDaDivida
      dividaId={String(div.id)}
      devedores={devedores}
      devedorAtualId={sel.id}
      readOnly={true}
    />
    ```

    **Parte D — Run regressão + commit:**
    ```bash
    cd src/mr-3/mr-cobrancas
    npm run test:regressao   # 28/28 PASS
    npm run build            # exit 0
    cd ..
    git add src/mr-cobrancas/src/components/DevedoresDaDivida.jsx src/mr-cobrancas/src/components/DetalheDivida.jsx src/mr-cobrancas/src/App.jsx
    git commit -m "feat(07.13d): DetalheDivida read-only mode

    DevedoresDaDivida.jsx ganha prop readOnly={true} — esconde botões adicionar/
    remover/promover/✕ via 4 conditional renders. Componente preserva render
    de chips + nome + cpf + papel.

    DetalheDivida.jsx L195 + App.jsx L3889 passam readOnly={true} (D-pre-9 —
    UI cadastro só por contrato; dívida individual não edita devedores).

    Hook useDevedoresDividas continua chamado (lê listarParticipantes — só leitura).
    Edição cascateia do contrato via DevedoresDoContrato (commit 07.13c)."
    ```
  </action>
  <verify>
    <automated>
      cd src/mr-3/mr-cobrancas && npm run test:regressao 2>&1 | grep -E "Tests.*28 passed" || exit 1
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "readOnly = false" src/mr-3/mr-cobrancas/src/components/DevedoresDaDivida.jsx` exit 0 (signature)
    - `grep -c "!readOnly" src/mr-3/mr-cobrancas/src/components/DevedoresDaDivida.jsx` retorna >= 4 (4 conditional renders: + Adicionar, ✕, 👑, Modal)
    - `grep -q "readOnly={true}" src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx` exit 0
    - `grep -q "readOnly={true}" src/mr-3/mr-cobrancas/src/components/DevedoresDoContrato.jsx 2>/dev/null && exit 1; true` (NÃO deve estar em DevedoresDoContrato — só DevedoresDaDivida)
    - `grep -A 5 "<DevedoresDaDivida" src/mr-3/mr-cobrancas/src/App.jsx | grep -q "readOnly={true}"` exit 0
    - `cd src/mr-3/mr-cobrancas && npm run test:regressao` exit 0 + "28 passed"
    - `cd src/mr-3/mr-cobrancas && npm run build` exit 0
    - **D-01 motor untouched (gate por commit):** `git -C src/mr-3 show HEAD --name-only | grep -E "src/utils/devedorCalc\.js|src/services/pagamentos\.js$|src/services/dividas\.js$"` exit 1
    - Commit criado: `git -C src/mr-3 log -1 --format=%s | grep -q "^feat(07.13d):"` exit 0
  </acceptance_criteria>
  <done>
    - DevedoresDaDivida.jsx com prop readOnly funcional (4 conditional renders)
    - DetalheDivida.jsx + App.jsx fluxo legacy passam readOnly={true}
    - Commit `feat(07.13d)` criado, regressão 28/28 PASS, build verde, D-01 motor intocado
  </done>
</task>

<task type="auto">
  <name>Task 5: ModuloContratos listagem inline "Nome (Papel), Nome (Papel)" (commit feat(07.13e))</name>
  <read_first>
    - `src/mr-3/mr-cobrancas/src/App.jsx` L8493-8504 (case "contratos" — props pra ModuloContratos)
    - `src/mr-3/mr-cobrancas/src/components/ModuloContratos.jsx` L17-47 (signature + state)
    - `src/mr-3/mr-cobrancas/src/components/TabelaContratos.jsx` L50-88 (coluna Devedor — ponto de mudança)
    - `src/mr-3/mr-cobrancas/src/components/DevedoresDaDivida.jsx` L5-12 PAPEL_META (reusar para label)
    - `.planning/phases/07.13-multiplos-devedores-contrato-solidariedade-passiva/07.13-CONTEXT.md` (D-pre-2 inline expandido + D-pre-14 query expandida)
  </read_first>
  <files>
    - src/mr-3/mr-cobrancas/src/App.jsx (MOD — carregarTudo carrega devedoresDividasJunction state; case "contratos" passa prop)
    - src/mr-3/mr-cobrancas/src/components/ModuloContratos.jsx (MOD — recebe devedoresDividasJunction prop e passa pra TabelaContratos)
    - src/mr-3/mr-cobrancas/src/components/TabelaContratos.jsx (MOD — coluna Devedor render inline com PRINCIPAL primeiro)
  </files>
  <action>
    **Parte A — Editar `src/mr-3/mr-cobrancas/src/App.jsx`:**

    1. Adicionar state global ao lado de `allPagamentos`/`allPagamentosDivida` (typically near L8325):
       ```javascript
       const [devedoresDividasJunction, setDevedoresDividasJunction] = useState([]);
       ```

    2. Em `carregarTudo` (typically L8344), adicionar fetch paralelo:
       ```javascript
       // ANTES (carregarTudo paralelo, exemplo):
       const [contratos, dividas, pagsParciais, pagsDivida, ...] = await Promise.all([
         dbGet("contratos_dividas", "order=created_at.desc"),
         dbGet("dividas", "order=data_vencimento.asc"),
         dbGet("pagamentos_parciais", ""),
         dbGet("pagamentos_divida", ""),
         // ...
       ]);

       // DEPOIS (commit 07.13e — fetch paralelo de junction):
       const [contratos, dividas, pagsParciais, pagsDivida, junctionRows, ...] = await Promise.all([
         dbGet("contratos_dividas", "order=created_at.desc"),
         dbGet("dividas", "order=data_vencimento.asc"),
         dbGet("pagamentos_parciais", ""),
         dbGet("pagamentos_divida", ""),
         dbGet("devedores_dividas", "select=devedor_id,divida_id,papel"),
         // ...
       ]);
       setDevedoresDividasJunction(Array.isArray(junctionRows) ? junctionRows : []);
       ```

    3. case "contratos" L8493-8504 passa prop:
       ```javascript
       case "contratos": return (
         <ModuloContratos
           allContratos={allContratos}
           allDividas={allDividas}
           devedores={devedores}
           credores={credores}
           allPagamentos={allPagamentos}
           allPagamentosDivida={allPagamentosDivida}
           devedoresDividasJunction={devedoresDividasJunction}   // NEW commit 07.13e
           hoje={hoje_app}
           onCarregarTudo={carregarTudo}
           setTab={setTab}
         />
       );
       ```

    **Parte B — Editar `src/mr-3/mr-cobrancas/src/components/ModuloContratos.jsx` L17-28** (signature + pass-through):

    ```javascript
    export default function ModuloContratos({
      allContratos,
      allDividas,
      devedores,
      credores,
      allPagamentos,
      allPagamentosDivida,
      devedoresDividasJunction = [],   // NEW
      hoje,
      onCarregarTudo,
      setTab,
      devedorPreSelecionado,
    }) {
      // ... (corpo existente até <TabelaContratos>)

      // Map<contratoId, [{devedor_id, papel, devedor_nome}]> consolidado client-side
      // PRINCIPAL primeiro, depois ordem original (D-pre-2/D-pre-14)
      const devedoresPorContrato = useMemo(() => {
        const dividaToContrato = new Map();
        (allDividas || []).forEach(d => {
          if (d.contrato_id) dividaToContrato.set(String(d.id), String(d.contrato_id));
        });
        const out = new Map();
        (devedoresDividasJunction || []).forEach(r => {
          const contratoId = dividaToContrato.get(String(r.divida_id));
          if (!contratoId) return;
          if (!out.has(contratoId)) out.set(contratoId, new Map());
          // DISTINCT por devedor_id (uma entrada por devedor, não 12x)
          out.get(contratoId).set(String(r.devedor_id), { devedor_id: r.devedor_id, papel: r.papel });
        });
        // Converte Map<id,{...}> → Array com PRINCIPAL primeiro
        const result = new Map();
        for (const [contratoId, devMap] of out.entries()) {
          const arr = Array.from(devMap.values()).sort((a, b) => {
            if (a.papel === 'PRINCIPAL' && b.papel !== 'PRINCIPAL') return -1;
            if (b.papel === 'PRINCIPAL' && a.papel !== 'PRINCIPAL') return 1;
            return 0;
          });
          result.set(contratoId, arr);
        }
        return result;
      }, [devedoresDividasJunction, allDividas]);

      // ... e passa pra TabelaContratos via prop:
      // <TabelaContratos ... devedoresPorContrato={devedoresPorContrato} />
    }
    ```

    **Parte C — Editar `src/mr-3/mr-cobrancas/src/components/TabelaContratos.jsx` L50-88:**

    1. Adicionar imports no topo (se não existir):
       ```javascript
       const PAPEL_LABEL = {
         PRINCIPAL: 'Principal', COOBRIGADO: 'Coobrigado', AVALISTA: 'Avalista',
         FIADOR: 'Fiador', CONJUGE: 'Cônjuge', OUTRO: 'Outro',
       };
       ```

    2. Receber prop `devedoresPorContrato` na signature L13.

    3. Substituir L60 + L82-84 (coluna Devedor):
       ```javascript
       // ANTES (L60):
       const devedor = devedores.find(d => String(d.id) === String(c.devedor_id));
       // ...
       // L82-84:
       <td style={{ ...td, fontWeight: 600, color: "#0f172a" }}>
         {devedor?.nome || "—"}
       </td>

       // DEPOIS (commit 07.13e):
       const devedoresInline = devedoresPorContrato?.get(String(c.id)) || [];
       const devedoresStr = devedoresInline
         .map(({ devedor_id, papel }) => {
           const dev = devedores.find(d => String(d.id) === String(devedor_id));
           const nome = dev?.nome || `#${devedor_id}`;
           return `${nome} (${PAPEL_LABEL[papel] || papel})`;
         })
         .join(', ');
       const fallbackDevedor = devedores.find(d => String(d.id) === String(c.devedor_id));
       // ...
       // L82-84 substituição:
       <td style={{ ...td, fontWeight: 600, color: "#0f172a" }}>
         {devedoresStr || fallbackDevedor?.nome || "—"}
       </td>
       ```

    **Parte D — Run regressão + commit:**
    ```bash
    cd src/mr-3/mr-cobrancas
    npm run test:regressao   # 28/28 PASS
    npm run build            # exit 0
    cd ..
    git add src/mr-cobrancas/src/App.jsx src/mr-cobrancas/src/components/ModuloContratos.jsx src/mr-cobrancas/src/components/TabelaContratos.jsx
    git commit -m "feat(07.13e): ModuloContratos listagem inline 'Nome (Papel), Nome (Papel)'

    App.jsx carregarTudo: fetch paralelo de devedores_dividas (select=devedor_id,
    divida_id,papel). State devedoresDividasJunction global, passado via prop.

    ModuloContratos.jsx consolida client-side em Map<contratoId, [{devedor_id,papel}]>
    DISTINCT (uma entrada por devedor, não 12x). Sort PRINCIPAL primeiro, depois
    ordem original (D-pre-2/D-pre-14).

    TabelaContratos.jsx coluna 'Devedor' render 'Nome1 (Papel1), Nome2 (Papel2)'
    inline expandido (PT-BR, papel primeira-maiúscula via PAPEL_LABEL). Fallback
    para devedor.find(c.devedor_id) em contrato sem rows na junction (header-only)."
    ```
  </action>
  <verify>
    <automated>
      cd src/mr-3/mr-cobrancas && npm run test:regressao 2>&1 | grep -E "Tests.*28 passed" || exit 1
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "devedoresDividasJunction" src/mr-3/mr-cobrancas/src/App.jsx` exit 0 (state global)
    - `grep -q 'dbGet("devedores_dividas"' src/mr-3/mr-cobrancas/src/App.jsx` exit 0 (fetch paralelo)
    - `grep -q "devedoresDividasJunction" src/mr-3/mr-cobrancas/src/components/ModuloContratos.jsx` exit 0
    - `grep -q "devedoresPorContrato" src/mr-3/mr-cobrancas/src/components/ModuloContratos.jsx` exit 0
    - `grep -q "PRINCIPAL.*PRINCIPAL\|sort.*papel" src/mr-3/mr-cobrancas/src/components/ModuloContratos.jsx` exit 0 (sort PRINCIPAL primeiro)
    - `grep -q "devedoresPorContrato" src/mr-3/mr-cobrancas/src/components/TabelaContratos.jsx` exit 0
    - `grep -q "PAPEL_LABEL\|Principal.*Coobrigado" src/mr-3/mr-cobrancas/src/components/TabelaContratos.jsx` exit 0 (label dictionary)
    - `cd src/mr-3/mr-cobrancas && npm run test:regressao` exit 0 + "28 passed"
    - `cd src/mr-3/mr-cobrancas && npm run build` exit 0
    - **D-01 motor untouched (gate por commit):** `git -C src/mr-3 show HEAD --name-only | grep -E "src/utils/devedorCalc\.js|src/services/pagamentos\.js$|src/services/dividas\.js$"` exit 1
    - Commit criado: `git -C src/mr-3 log -1 --format=%s | grep -q "^feat(07.13e):"` exit 0
  </acceptance_criteria>
  <done>
    - App.jsx fetch paralelo de devedores_dividas + state global devedoresDividasJunction
    - ModuloContratos consolida em Map<contratoId, [...]> client-side com PRINCIPAL primeiro
    - TabelaContratos render inline "Nome (Papel), Nome (Papel)" (D-pre-2/D-pre-14)
    - Commit `feat(07.13e)` criado, regressão 28/28 PASS, build verde, D-01 motor intocado
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Pessoas listagem com saldo cheio para não-PRINCIPAL — helper paralelo (commit feat(07.13f)) + CHECKPOINT INTERNO #2</name>
  <read_first>
    - `src/mr-3/mr-cobrancas/src/utils/agruparPagamentosPorDevedor.js` (helper 7.10bcd existente — pattern de fan-out via lookup)
    - `src/mr-3/mr-cobrancas/src/services/__tests__/agruparPagamentosPorDevedor.test.js` (analog test trivial)
    - `src/mr-3/mr-cobrancas/src/App.jsx` L2969-2996 (Pessoas state pgtosPorDevedor — alvo da edição)
    - `src/mr-3/mr-cobrancas/src/App.jsx` L562-720 (Dashboard — INTOCADO, helper original preservado)
    - `src/mr-3/mr-cobrancas/src/App.jsx` L8425-8440 (parent App pgtosPorDevedorCarteira — INTOCADO)
    - `.planning/phases/07.13-multiplos-devedores-contrato-solidariedade-passiva/07.13-CONTEXT.md` (D-pre-6 saldo cheio + Dashboard preservado)
    - `memory/feedback_grep_global_before_pointwise_fix.md` (grep `agruparPagamentosPorDevedor\(` ANTES de mudar — precisa garantir só Pessoas troca)
  </read_first>
  <what-built>
    Novo helper paralelo `src/utils/agruparPagamentosPorDevedorIncluindoSolidarios.js` (fan-out 1:N via junction): cada pagamento_divida vai pra TODOS os devedores da dívida (não só PRINCIPAL). Pessoas (App.jsx L2993) troca chamada `agruparPagamentosPorDevedor` → `agruparPagamentosPorDevedorIncluindoSolidarios`. Test trivial 2 it() blocks.

    Dashboard (App.jsx L568) e parent App (L8434) **INTOCADOS** — preservam dedupe via `principaisDividaIds` papel='PRINCIPAL' filter (L597-608). Carteira Total não muda valor (validação SC7 do Plan 02).

    Resultado UAT: João FIADOR aparece R$ 36.652,56 em Pessoas (saldo cheio do contrato MENDES); Carteira Total continua R$ 36.652,56 (não dobra) — D-pre-6 "perspectiva de cobrança individual".
  </what-built>
  <how-to-verify>
    **CHECKPOINT INTERNO #2 — Validação visual de Pessoas saldo cheio + Dashboard intacto.**

    O executor já criou o commit `feat(07.13f)`. Antes de prosseguir para Task 7 (Final Gates), usuário valida manualmente:

    **Preparação:**
    ```bash
    cd src/mr-3/mr-cobrancas && npm run dev
    ```

    **Validações (marcar PASS/FAIL):**

    1. **Pre-state — capturar Carteira Total ANTES de adicionar fiador:**
       - Dashboard → card "Carteira Total" (header global). Anotar valor: R$ ____________
       - Pessoas → MENDES → coluna saldo. Anotar: R$ ____________

    2. **Adicionar João como FIADOR no contrato MENDES** (re-fluxo Task 3 SC2):
       - DetalheContrato MENDES → "+ Adicionar Devedor" → João → FIADOR. Confirmar.
       - PASS [ ] / FAIL [ ]

    3. **Pessoas — verificar saldo cheio para João (D-pre-6):**
       - Aba Pessoas → buscar João. Valor da coluna saldo deve ser **IGUAL** ao MENDES (saldo cheio do contrato).
       - Esperado: João R$ 36.652,56 (ou valor real do MENDES) — saldo cheio.
       - PASS [ ] / FAIL [ ]   João: R$ ____________   MENDES: R$ ____________   Drift: R$ ______

    4. **Dashboard Carteira Total — verificar dedupe preservado:**
       - Dashboard → Carteira Total (header global). Valor deve ser **IDÊNTICO** ao pre-state (ZERO mudança).
       - Esperado: mesmo valor de pre-state. Sem dobrar (NÃO 2x R$ 36.652,56).
       - PASS [ ] / FAIL [ ]   Carteira PRE: R$ ______   Carteira PÓS: R$ ______   Drift: R$ ______ (esperado 0,00)

    5. **Cross-check ModuloContratos inline (sanity Task 5):**
       - ModuloContratos → linha MENDES → coluna "Devedor". Deve mostrar `"Mendes (Principal), João (Fiador)"`.
       - PASS [ ] / FAIL [ ]

    6. **Cross-check DetalheDivida read-only (sanity Task 4):**
       - Abrir 1 dívida do contrato MENDES (ModuloDividas → linha → DetalheDivida).
       - Seção "Pessoas Vinculadas" deve mostrar 2 chips (Mendes Principal + João Fiador) **SEM** botões adicionar/remover/promover/✕.
       - PASS [ ] / FAIL [ ]

    7. **Revert estado:**
       - DetalheContrato MENDES → remover João.
       - Pessoas: João desaparece da listagem (ou volta a saldo zero/sem dívidas).
       - PASS [ ] / FAIL [ ]

    **Build/test gates (executor já validou):**
    ```bash
    cd src/mr-3/mr-cobrancas
    npm run test:regressao   # 30/30 PASS (28 anteriores + 2 helper paralelo)
    npm run build            # exit 0
    ```

    **D-01 motor untouched (gate por commit):**
    ```bash
    git -C src/mr-3 show HEAD --name-only | grep -E "src/utils/devedorCalc\.js|src/services/pagamentos\.js$|src/services/dividas\.js$"
    # Esperado: exit 1 (zero matches)
    ```

    **Se algum item FAIL:** responder `FAIL: <item-num>: <sintoma>`. Executor revisa em modo gap-closure.
  </how-to-verify>
  <resume-signal>
    Digite `APROVADO` se TODOS os 7 items PASS. Caso contrário, `FAIL: <item>: <sintoma>` e executor abre gap-closure.
  </resume-signal>
  <acceptance_criteria>
    - Arquivo NEW: `test -f src/mr-3/mr-cobrancas/src/utils/agruparPagamentosPorDevedorIncluindoSolidarios.js` exit 0
    - `grep -q "export function agruparPagamentosPorDevedorIncluindoSolidarios" src/mr-3/mr-cobrancas/src/utils/agruparPagamentosPorDevedorIncluindoSolidarios.js` exit 0
    - Arquivo NEW: `test -f src/mr-3/mr-cobrancas/src/services/__tests__/agruparPagamentosPorDevedorIncluindoSolidarios.test.js` exit 0
    - `grep -c "it(" src/mr-3/mr-cobrancas/src/services/__tests__/agruparPagamentosPorDevedorIncluindoSolidarios.test.js` retorna >= 2 (2 it blocks)
    - `grep -q "agruparPagamentosPorDevedorIncluindoSolidarios" src/mr-3/mr-cobrancas/src/App.jsx` exit 0 (Pessoas Map L2993 swap)
    - **Grep global garante Dashboard preservado** (lição feedback_grep_global_before_pointwise_fix.md): `grep -n "agruparPagamentosPorDevedor\b" src/mr-3/mr-cobrancas/src/App.jsx | wc -l` retorna >= 2 (Dashboard L568 + parent L8434 PRESERVADAS — não trocadas)
    - `grep -q "papel=eq.PRINCIPAL" src/mr-3/mr-cobrancas/src/App.jsx` exit 0 (principaisDividaIds dedupe Dashboard preservado)
    - package.json estende test:regressao: `grep -q "agruparPagamentosPorDevedorIncluindoSolidarios.test.js" src/mr-3/mr-cobrancas/package.json` exit 0
    - `cd src/mr-3/mr-cobrancas && npm run test:regressao` exit 0 + "30 passed"
    - `cd src/mr-3/mr-cobrancas && npm run build` exit 0
    - **D-01 motor untouched (gate por commit):** `git -C src/mr-3 show HEAD --name-only | grep -E "src/utils/devedorCalc\.js|src/services/pagamentos\.js$|src/services/dividas\.js$"` exit 1
    - Commit criado: `git -C src/mr-3 log -1 --format=%s | grep -q "^feat(07.13f):"` exit 0
    - Usuário respondeu `APROVADO` em PAUSA #2 (todos 7 items visuais PASS, drift Carteira Total = 0,00)
  </acceptance_criteria>
  <done>
    - Helper paralelo agruparPagamentosPorDevedorIncluindoSolidarios.js criado + test 2 it()
    - Pessoas (App.jsx L2993) troca para helper paralelo
    - Dashboard L568 + parent L8434 PRESERVADOS (helper original)
    - Commit `feat(07.13f)` criado, regressão 30/30 PASS, build verde, D-01 motor intocado
    - PAUSA #2 APROVADO pelo usuário (saldo cheio João funcional, Dashboard sem regressão)
  </done>
</task>

<task type="auto">
  <name>Task 7 — Final Gates Task: Shields equivalência + D-01 cumulativo + D-30 6 commits (NÃO produz commit)</name>
  <read_first>
    - `.planning/phases/07.13-multiplos-devedores-contrato-solidariedade-passiva/07.13-CONTEXT.md` (invariantes locked + D-01 estrito + D-30 atomicidade)
    - `memory/feedback_gate_motor_grep_use_name_only.md` (gate motor com --name-only, não --stat)
    - `memory/feedback_shields_wrapper_must_test_equivalence.md` (helper precisa shield equivalência)
    - `memory/feedback_grep_shield_strict_imports.md` (^import .* from "...")
    - `src/mr-3/mr-cobrancas/src/services/devedoresDividas.js` (helpers — equivalência)
    - `src/mr-3/mr-cobrancas/src/components/DevedoresDoContrato.jsx` (Shields D-02 strict imports)
    - Plan 01 SUMMARY draft (em curso de criação nesta task)
  </read_first>
  <files>
    - .planning/phases/07.13-multiplos-devedores-contrato-solidariedade-passiva/07.13-01-SUMMARY.md (NEW — registra gates verdes; left uncommitted, será commitado em Plan 02 Task 6 junto com SUMMARY 02 + STATE)
  </files>
  <action>
    **Esta task NÃO produz commit no submódulo. É verificação cumulativa final que absorve o que seria o commit "07.13g".**

    **Parte A — D-01 cumulativo dual gate (motor INTOCADO em todos 6 commits desde f00cf2c):**

    ```bash
    # C1 — diff cumulativo motor files (esperado VAZIO)
    cd src/mr-3
    git diff f00cf2c..HEAD -- src/mr-cobrancas/src/utils/devedorCalc.js src/mr-cobrancas/src/services/pagamentos.js src/mr-cobrancas/src/services/dividas.js
    # Esperado: 0 bytes output

    # C2 — name-only cumulativo (esperado: motor files AUSENTES em todos 6 commits)
    git log f00cf2c..HEAD --format="" --name-only | grep -E "src/mr-cobrancas/src/utils/devedorCalc\.js|src/mr-cobrancas/src/services/pagamentos\.js|src/mr-cobrancas/src/services/dividas\.js"
    # Esperado: exit 1 (zero matches)
    ```

    **Parte B — D-30 EXATAMENTE 6 commits sequenciais com headers verbatim:**
    ```bash
    cd src/mr-3
    git log f00cf2c..HEAD --oneline
    # Esperado: 6 linhas, headers verbatim na ordem inversa cronológica:
    #   <SHA-f> feat(07.13f): Pessoas listagem com saldo cheio ...
    #   <SHA-e> feat(07.13e): ModuloContratos listagem inline ...
    #   <SHA-d> feat(07.13d): DetalheDivida read-only mode
    #   <SHA-c> feat(07.13c): UI DetalheContrato componente multi-devedor + wizard D-pre-13
    #   <SHA-b> feat(07.13b): cascade nova divida → seedDevedoresDoContrato
    #   <SHA-a> feat(07.13a): service layer multi-devedor por contrato

    # Validação programática:
    COUNT=$(git log f00cf2c..HEAD --oneline | wc -l)
    test "$COUNT" -eq 6 || { echo "FAIL: $COUNT commits, esperado 6"; exit 1; }

    HEADERS=$(git log f00cf2c..HEAD --format=%s | sort)
    EXPECTED=$(printf "feat(07.13a): service layer multi-devedor por contrato\nfeat(07.13b): cascade nova divida → seedDevedoresDoContrato\nfeat(07.13c): UI DetalheContrato componente multi-devedor + wizard D-pre-13\nfeat(07.13d): DetalheDivida read-only mode\nfeat(07.13e): ModuloContratos listagem inline 'Nome (Papel), Nome (Papel)'\nfeat(07.13f): Pessoas listagem com saldo cheio para não-PRINCIPAL\n" | sort)
    # Comparação aceita variação de subject após "feat(07.13X):" — só o prefixo é estrito
    git log f00cf2c..HEAD --format=%s | grep -cE "^feat\(07\.13[a-f]\):" | awk '$1 != 6 { exit 1 }'
    ```

    **Parte C — Shield equivalência seedDevedoresDoContrato (≡ N seedPrincipal por dívida + N adicionarParticipante por papel):**

    ```bash
    # Verificação manual no código fonte (não shell test) — registrar no SUMMARY:
    # seedDevedoresDoContrato faz:
    #   1. listarDevedoresDoContrato(contratoId) → DISTINCT [{devedor_id, papel, responsabilidade}]
    #   2. Para cada devedor: sb(POST devedores_dividas, {devedor_id, divida_id: novaDivida, papel, responsabilidade})
    # Equivalência:
    #   - Para PRINCIPAL: ≡ seedPrincipal(devedor_id, novaDivida) com papel='PRINCIPAL'
    #   - Para outros papéis: ≡ adicionarParticipante({devedorId, dividaId, papel, responsabilidade}) SEM auto-demote (porque não é PRINCIPAL)
    # Test trivial Task 1 it #3 valida o comportamento (3 inserts pra 2 devedores em 1 nova dívida).

    grep -A 20 "export async function seedDevedoresDoContrato" src/mr-3/mr-cobrancas/src/services/devedoresDividas.js | grep -q "papel: dev.papel" || { echo "FAIL: seedDevedoresDoContrato não copia papel"; exit 1; }
    grep -A 20 "export async function seedDevedoresDoContrato" src/mr-3/mr-cobrancas/src/services/devedoresDividas.js | grep -q "responsabilidade: dev.responsabilidade" || { echo "FAIL: seedDevedoresDoContrato não copia responsabilidade"; exit 1; }
    ```

    **Parte D — Shield equivalência promoverParaPrincipalComDemocao (N PATCHes pareados):**

    ```bash
    # promoverParaPrincipalComDemocao faz:
    #   1. Demove anterior PRIMEIRO: N PATCHes (devedor_id=anterior, papel=novoPapelDoAnterior) — libera UNIQUE INDEX
    #   2. Promove novo: N PATCHes (devedor_id=novoDevedorId, papel='PRINCIPAL')
    # Total: 2N PATCHes pareados. UNIQUE INDEX papel=PRINCIPAL preservado em todo momento (demoção PRIMEIRO).

    grep -B 2 -A 30 "export async function promoverParaPrincipalComDemocao" src/mr-3/mr-cobrancas/src/services/devedoresDividas.js | grep -q "Demove anterior PRIMEIRO" || { echo "FAIL: comentário ordem não presente"; exit 1; }
    grep -A 50 "export async function promoverParaPrincipalComDemocao" src/mr-3/mr-cobrancas/src/services/devedoresDividas.js | grep -c '"PATCH"' | awk '$1 < 2 { exit 1 }'   # mínimo 2 chamadas PATCH
    ```

    **Parte E — Shield equivalência criarVinculoContratoDevedor (≡ N adicionarParticipante por dívida):**

    ```bash
    grep -A 30 "export async function criarVinculoContratoDevedor" src/mr-3/mr-cobrancas/src/services/devedoresDividas.js | grep -q 'sb(TABLE, "POST"' || { echo "FAIL: criarVinculoContratoDevedor não usa POST"; exit 1; }
    grep -A 30 "export async function criarVinculoContratoDevedor" src/mr-3/mr-cobrancas/src/services/devedoresDividas.js | grep -q "USE_WIZARD_PROMOCAO" || { echo "FAIL: bloqueio wizard ausente"; exit 1; }
    ```

    **Parte F — Shields D-02 strict imports** (lição feedback_grep_shield_strict_imports.md — `^import .* from "..."`, NÃO substring que casa em prose PT-BR):

    ```bash
    # DevedoresDoContrato.jsx — imports estritos:
    grep -E '^import .* from "\\./DevedoresDoContrato\\.jsx"' src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx | wc -l   # esperado >= 1

    # devedoresDividas helpers consumidos em DevedoresDoContrato.jsx:
    grep -E '^import .* from "\\.\\./services/devedoresDividas\\.js"' src/mr-3/mr-cobrancas/src/components/DevedoresDoContrato.jsx | wc -l   # esperado >= 1

    # Helper paralelo Pessoas:
    grep -E '^import .* from "\\./utils/agruparPagamentosPorDevedorIncluindoSolidarios\\.js"' src/mr-3/mr-cobrancas/src/App.jsx | wc -l   # esperado >= 1
    ```

    **Parte G — Final regressão cumulativa:**

    ```bash
    cd src/mr-3/mr-cobrancas
    npm run test:regressao   # esperado 30/30 PASS (25 base 7.10bcd + 3 helpers contrato + 2 helper paralelo)
    npm run build            # esperado exit 0
    ```

    **Parte H — Criar SUMMARY 01 (left uncommitted no pai até Plan 02):**

    ```bash
    cat > .planning/phases/07.13-multiplos-devedores-contrato-solidariedade-passiva/07.13-01-SUMMARY.md <<'EOF'
    ---
    phase: 7.13
    plan: 07.13-01
    status: implemented (gates green, awaiting Plan 02 UAT + bump)
    commits_submodule:
      - feat(07.13a): service layer multi-devedor por contrato
      - feat(07.13b): cascade nova divida → seedDevedoresDoContrato
      - feat(07.13c): UI DetalheContrato componente multi-devedor + wizard D-pre-13
      - feat(07.13d): DetalheDivida read-only mode
      - feat(07.13e): ModuloContratos listagem inline
      - feat(07.13f): Pessoas listagem com saldo cheio para não-PRINCIPAL
    baseline_pre_phase: f00cf2c (post-7.10bcd HEAD)
    cumulative_commit_count: 6
    regressao_final: 30/30 PASS
    motor_d01_cumulative_diff: VAZIO (devedorCalc.js + pagamentos.js + dividas.js intocados)
    motor_d01_cumulative_name_only: zero matches
    files_modified_cumulative: 13 (5 NEW + 8 MOD)
    schema_diff: ZERO migration (D-pre-4 — reusa devedores_dividas existente)
    pausas_internas: 2 APROVADAS (#1 após Task 3 cadastro+wizard funcional; #2 após Task 6 Pessoas saldo cheio)
    ---

    # Phase 7.13 Plan 01 — SUMMARY (left uncommitted; será commitado em Plan 02 Task 6)

    ## Gates verdes na Final Gates Task (Task 7)

    - **D-01 dual gate cumulativo (f00cf2c..HEAD)**: motor diff VAZIO + name-only zero matches em devedorCalc.js + pagamentos.js + dividas.js (3 motor files)
    - **D-30 commit count cumulativo**: EXATAMENTE 6 commits sequenciais com headers verbatim feat(07.13a/b/c/d/e/f)
    - **Shield equivalência seedDevedoresDoContrato**: copia papel + responsabilidade verbatim (≡ N adicionarParticipante por papel)
    - **Shield equivalência promoverParaPrincipalComDemocao**: demove PRIMEIRO + promove DEPOIS (preserva UNIQUE INDEX papel=PRINCIPAL)
    - **Shield equivalência criarVinculoContratoDevedor**: bloqueio USE_WIZARD_PROMOCAO funcional
    - **Shields D-02 strict imports**: DevedoresDoContrato + helpers contrato-level + helper paralelo todos importados via padrão `^import .* from "..."`
    - **Regressão final**: 30/30 PASS (25 base 7.10bcd + 3 helpers contrato + 2 helper paralelo)
    - **Build**: exit 0

    ## Estado pós-Plan 01

    - Submódulo: 6 commits atômicos sequenciais bisect-able criados sobre baseline f00cf2c
    - Pai: AINDA NÃO BUMPADO (Plan 02 Task 4)
    - Push: NÃO executado
    - Tag v1.4-phase7.13: NÃO criada (Plan 02 Task 6)

    ## Próximo passo

    Plan 02 — UAT humano + bump tag (Task 1 Pre-UAT smoke + Task 2 UAT 10 SCs + 2 SC-iso + Task 3 resolução divergências + Task 4 push+bump+deploy + Task 5 UAT prod + Task 6 tag).
    EOF
    ```

    **NÃO COMMITAR neste task.** Plan 02 Task 6 commita SUMMARY 01 + SUMMARY 02 + STATE.md juntos no pai.
  </action>
  <verify>
    <automated>
      cd src/mr-3 && \
      git diff f00cf2c..HEAD -- src/mr-cobrancas/src/utils/devedorCalc.js src/mr-cobrancas/src/services/pagamentos.js src/mr-cobrancas/src/services/dividas.js | wc -c | awk '$1 != 0 { print "FAIL: D-01 motor diff cumulativo NOT EMPTY"; exit 1 }' && \
      git log f00cf2c..HEAD --format="" --name-only | grep -E "src/mr-cobrancas/src/utils/devedorCalc\.js|src/mr-cobrancas/src/services/pagamentos\.js|src/mr-cobrancas/src/services/dividas\.js" | wc -l | awk '$1 != 0 { print "FAIL: D-01 motor name-only cumulativo HAS MATCHES"; exit 1 }' && \
      COUNT=$(git log f00cf2c..HEAD --oneline | wc -l) && test "$COUNT" -eq 6 && \
      cd mr-cobrancas && npm run test:regressao 2>&1 | grep -E "Tests.*30 passed"
    </automated>
  </verify>
  <acceptance_criteria>
    - **D-01 cumulativo C1 (diff)**: `git -C src/mr-3 diff f00cf2c..HEAD -- src/mr-cobrancas/src/utils/devedorCalc.js src/mr-cobrancas/src/services/pagamentos.js src/mr-cobrancas/src/services/dividas.js | wc -c` retorna `0`
    - **D-01 cumulativo C2 (name-only)**: `git -C src/mr-3 log f00cf2c..HEAD --format="" --name-only | grep -E "src/mr-cobrancas/src/utils/devedorCalc\.js|src/mr-cobrancas/src/services/pagamentos\.js|src/mr-cobrancas/src/services/dividas\.js" | wc -l` retorna `0`
    - **D-30 EXATAMENTE 6 commits**: `git -C src/mr-3 log f00cf2c..HEAD --oneline | wc -l` retorna `6`
    - **D-30 headers verbatim feat(07.13X)**: `git -C src/mr-3 log f00cf2c..HEAD --format=%s | grep -cE "^feat\\(07\\.13[a-f]\\):"` retorna `6`
    - **Shield equivalência seedDevedoresDoContrato**: copia `papel` + `responsabilidade` verbatim (assert via grep no source)
    - **Shield equivalência promoverParaPrincipalComDemocao**: ordem demove→promove documentada (assert via grep "Demove anterior PRIMEIRO" no comentário)
    - **Shield equivalência criarVinculoContratoDevedor**: bloqueio `USE_WIZARD_PROMOCAO` presente (assert via grep)
    - **Shields D-02 strict imports**: `grep -cE '^import .* from "\\./DevedoresDoContrato\\.jsx"' src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx` retorna >= 1
    - **Shields D-02 strict imports helper paralelo**: `grep -cE '^import .* from "\\./utils/agruparPagamentosPorDevedorIncluindoSolidarios\\.js"' src/mr-3/mr-cobrancas/src/App.jsx` retorna >= 1
    - **Regressão cumulativa final**: `cd src/mr-3/mr-cobrancas && npm run test:regressao` exit 0 + "30 passed"
    - **Build cumulativo final**: `cd src/mr-3/mr-cobrancas && npm run build` exit 0
    - **NENHUM commit novo no submódulo**: `git -C src/mr-3 log f00cf2c..HEAD --oneline | wc -l` permanece em `6` (Final Gates Task NÃO produz commit)
    - SUMMARY 01 criado em `.planning/phases/07.13.../07.13-01-SUMMARY.md` (left uncommitted)
  </acceptance_criteria>
  <done>
    - Todos os shields/gates cumulativos verdes
    - D-01 motor 100% intocado em todos 6 commits cumulativos
    - D-30 EXATAMENTE 6 commits sequenciais com headers verbatim
    - Regressão 30/30 PASS, build exit 0
    - SUMMARY 01 criado (left uncommitted — Plan 02 Task 6 commita junto)
    - Plan 01 pronto pra Plan 02 (UAT + bump + tag)
  </done>
</task>

</tasks>

<verification>
**Verificação overall do Plan 01 (após Task 7):**
- 6 commits atômicos sequenciais no submódulo (feat(07.13a) → feat(07.13f))
- 13 arquivos cumulativos modificados (5 NEW + 8 MOD)
- D-01 motor INTOCADO cumulativamente (devedorCalc.js + pagamentos.js + dividas.js zero diff desde f00cf2c)
- Regressão 30/30 PASS por commit
- Build exit 0 por commit
- 2 PAUSAs internas APROVADAS (#1 após Task 3 / #2 após Task 6)
- SUMMARY 01 criado, left uncommitted no pai (Plan 02 Task 6 commita)
- Pai NÃO BUMPADO; push NÃO executado; tag v1.4-phase7.13 NÃO criada

**Final Gates Task substitui o que seria commit "07.13g" (verificação cumulativa sem commit).**
</verification>

<success_criteria>
- All 7 tasks PASS (6 commits + Final Gates Task)
- Cumulative regressão 30/30 PASS
- D-01 motor 100% intocado (dual gate C1+C2 cumulativo)
- D-30 EXATAMENTE 6 commits com headers verbatim feat(07.13a/b/c/d/e/f)
- D-02 strict imports verificados em DevedoresDoContrato + helper paralelo
- 2 PAUSAs humanas APROVADAS internamente
- Schema zero-diff (D-pre-4)
- SUMMARY 01 criado (left uncommitted)
- Pronto para Plan 02 (UAT + bump + tag)
</success_criteria>

<output>
Após completar Task 7, executor verifica todos acceptance_criteria e move para Plan 02 (`/gsd-execute-phase 7.13` continua com 07.13-02-PLAN.md).
</output>
