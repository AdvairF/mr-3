---
plan: 07.13-02
phase: 7.13
phase_name: "Múltiplos Devedores por Contrato (Solidariedade Passiva)"
type: execute
wave: 2
depends_on: ["07.13-01"]
autonomous: false
requirements: []
source: "ROADMAP.md L380-387 + 07.13-CONTEXT.md (UAT §218-244 — 10 SCs + 2 SC-iso + Pre-existing feature check) + invariantes herdadas D-01..D-34. Phase has NO REQ-IDs."
estimated_tasks: 6

files_modified: []   # Plan 02 não edita código-fonte — só PAUSAs humanas + bump no pai (submódulo já commitado em 6 commits no Plan 01) + commit docs SUMMARY 01 + 02 + STATE.md + tag
files_touched:
  - src/mr-3                                                                                                        # bump no pai (git add src/mr-3 no repo pai)
  - .planning/phases/07.13-multiplos-devedores-contrato-solidariedade-passiva/07.13-01-SUMMARY.md                    # commitado neste plan (criado em Plan 01 Task 7)
  - .planning/phases/07.13-multiplos-devedores-contrato-solidariedade-passiva/07.13-02-SUMMARY.md                    # novo — SUMMARY após UAT + bump
  - .planning/STATE.md                                                                                                # update — phase 7.13 SHIPPED

must_haves:
  truths:
    - "Pre-UAT smoke obrigatório (lição feedback_proc_judiciais_create_bug_preexisting.md): criar contrato → adicionar documento → vincular devedor extra → remover. PASS antes de continuar com UAT 10 SCs. Se FAIL não-regressão (bug pré-existente), PAUSA UAT e abre backlog ANTES de prosseguir"
    - "UAT 10 SCs (SC1-SC10) executado em prod local OU prod direto com PASS explícito em TODOS — drift de centavo = FAIL atomic. SCs verbatim do CONTEXT.md L218-232 (SC1 criar contrato 2 devedores; SC2 adicionar João FIADOR ao MENDES; SC3 remover João; SC4 bloqueio último PRINCIPAL warning; SC5 wizard D-pre-13 promoção; SC6 Pessoas saldo cheio; SC7 Dashboard Carteira preservado; SC8 ModuloContratos inline; SC9 DetalheDivida read-only; SC10 nova parcela cascade)"
    - "UAT cross-entity isolation (SC-iso-1 + SC-iso-2 — lição feedback_cross_entity_uat_isolation.md): SC-iso-1 — após adicionar João a MENDES, contrato 'advair' NÃO mostra João; SC-iso-2 — excluir contrato MENDES sem pagamentos cascateia DELETE em devedores_dividas via FK (advair preservado)"
    - "Drift de centavo em qualquer 1 dos 10 SCs ou 2 SC-iso = FAIL atomic — usuário decide entre fix-forward <5min ou rollback total `git -C src/mr-3 reset --hard f00cf2c` (volta ao state pré-7.13). Phase aborta inteira se rollback; nunca ship parcial"
    - "Diff consolidado do submódulo aprovado pelo usuário (PAUSA #2 explícita) antes do bump pai: 13 files cumulativos (5 NEW + 8 MOD) + D-01 motor diff vazio cumulativo desde f00cf2c + EXATAMENTE 6 commits sequenciais com headers feat(07.13a/b/c/d/e/f) + Shields equivalência todos OK + regressão 30/30"
    - "Bump submódulo no pai com mensagem `chore(07.13): bump submodule mr-3 — múltiplos devedores por contrato (solidariedade passiva)`"
    - "SHA-bump no pai === SHA do último commit submódulo (07.13f) aprovado em PAUSA #2 (cross-check verificado em Task 4 `<verify>` automatizado — substitui PAUSA #3 dedicada — pattern 7.10a/7.10bcd)"
    - "Push humano executado pelo planner em Task 4 — `git -C src/mr-3 push origin main` (6 commits) seguido de `git push origin master` (bump pai). Vercel auto-build dispara"
    - "UAT prod humano em mrcobrancas.com.br pós-deploy (Task 5) — SCs principais re-validados em ambiente real"
    - "Smoke não-regressão funcional das 4 telas migradas em 7.10a + 7.10bcd (Task 5 sub-itens 7a/b/c/d): TabelaDividas (D-27 Set lookup intocado) + ProcessosJudiciais polo passivo (junction `processos_devedores` independente, deferred D-pre-14 — devedores manuais NÃO apagados) + Dashboard Carteira Total drift R$ 0,00 absoluto vs pre-7.13 (D-pre-6 preserva pattern `papel=PRINCIPAL`) + Pessoas filtros funcionais. Drift inesperado em qualquer 1 = FAIL phase, abre backlog `Phase 7.13.prod-fix` antes de tag (lições cumulativas feedback_proc_judiciais_create_bug_preexisting.md + feedback_grep_global_before_pointwise_fix.md)"
    - "Tag `v1.4-phase7.13` criada em Task 6 SE UAT prod APROVADO + 4 smokes não-regressão APROVADOS — tag no pai + tag no submódulo (pattern 7.10bcd ce22a05)"
    - "Commit docs consolidado (Task 6): `docs(07.13): PLAN, SUMMARY consolidados + STATE phase 7.13 SHIPPED + tag v1.4-phase7.13`. Inclui SUMMARY 01 (criado em Plan 01 Task 7) + SUMMARY 02 + STATE.md atualizado"
    - "Total Phase 7.13 = 4 humanos checkpoints (D-pre-3 phase grande proporcional ao risco): 2 sub-PAUSAs intra-Plan-01 (#1 após Task 3 cadastro+wizard; #2 após Task 6 Pessoas saldo cheio) + 2 PAUSAs Plan 02 (Pre-UAT smoke + diff consolidado pré-bump). UAT 10 SCs e UAT prod são PAUSAs blocking adicionais — total 6 checkpoints proporcional ao escopo grande"
  artifacts:
    - path: ".planning/phases/07.13-multiplos-devedores-contrato-solidariedade-passiva/07.13-02-SUMMARY.md"
      provides: "Registro Pre-UAT smoke + UAT 10 SCs + 2 SC-iso + SHA-bump submódulo↔pai + UAT prod + tag v1.4-phase7.13"
      contains: "SC1.*PASS"
    - path: ".planning/phases/07.13-multiplos-devedores-contrato-solidariedade-passiva/07.13-01-SUMMARY.md"
      provides: "SUMMARY 01 (criado em Plan 01 Task 7, commitado em Plan 02 Task 6 junto com SUMMARY 02 + STATE.md + tag)"
      contains: "D-01 ESTRITO cumulativo"
    - path: ".planning/STATE.md"
      provides: "STATE.md atualizado last_activity + status referenciando Phase 7.13 SHIPPED"
      contains: "Phase 7.13 SHIPPED"
  key_links:
    - from: "Plan 02 PAUSA #1 (Pre-UAT smoke)"
      to: "Plan 02 Task 2 (UAT 10 SCs)"
      via: "Sem APROVADO no smoke (cadastro de contrato funciona em prod), não autoriza UAT — bug pré-existente isola backlog antes"
      pattern: "PAUSA #1.*APROVADO"
    - from: "Plan 02 Task 2 (UAT 10 SCs + 2 SC-iso)"
      to: "Plan 02 Task 3 (resolução divergências)"
      via: "Drift PROD vs LOCAL → debug ou backlog; APROVADO = TODOS PASS"
      pattern: "SC.*PASS"
    - from: "Plan 02 PAUSA #2 (diff consolidado pré-bump)"
      to: "Plan 02 Task 4 (push submódulo + bump pai + push pai + Vercel deploy)"
      via: "Sem OK diff consolidado submódulo + 6 commits + 13 files + motor vazio cumulativo, não bumpa nem autoriza push"
      pattern: "PAUSA #2.*APROVADO"
    - from: "Plan 02 Task 5 (UAT prod APROVADO)"
      to: "Plan 02 Task 6 (tag v1.4-phase7.13 + commit docs)"
      via: "UAT prod humano em mrcobrancas.com.br precede tag — tag só após confirmação real prod"
      pattern: "v1.4-phase7.13"
    - from: "Rollback fallback"
      to: "git -C src/mr-3 reset --hard f00cf2c"
      via: "FAIL UAT em qualquer 1 dos 10 SCs ou 2 SC-iso (não-trivial) → rollback total dos 6 commits → phase aborta inteira"
      pattern: "git reset --hard f00cf2c"
---

<objective>
Executar Pre-UAT smoke + UAT cross-check de 10 SCs + 2 SC-iso (CONTEXT.md L218-244 — verbatim) em prod local ou prod direto, bumpar submódulo no pai com 6 commits, push humano + Vercel deploy, UAT prod humano, criar tag `v1.4-phase7.13`, e documentar SUMMARY 02 + STATE SHIPPED + commit docs consolidado. Os 6 commits do submódulo já foram criados em Plan 01 Tasks 1-6.

Purpose:
- **PAUSA #1 (Pre-UAT smoke obrigatório — lição feedback_proc_judiciais_create_bug_preexisting.md)** impede UAT contra bug pré-existente. Smoke cobre fluxo essencial não-7.13: criar contrato → adicionar documento → vincular devedor extra → remover. Se falhar em prod (não-regressão), PAUSA UAT e abre backlog ANTES de continuar.
- **Task 2 — UAT 10 SCs + 2 SC-iso (D-pre-3 phase grande, gates UAT proporcionais)**:
  - SC1-SC10 cobrem fluxo completo (cadastro → adição → remoção → wizard → Pessoas → Dashboard → ModuloContratos → DetalheDivida → cascade nova parcela)
  - SC-iso-1 + SC-iso-2 cross-entity isolation (lição feedback_cross_entity_uat_isolation.md — feature com state global SEMPRE precisa cenário cross-entity)
- **Task 3 — Resolução de divergências**: qualquer drift PROD vs LOCAL → debug pontual ou backlog phase futura. Atualização CONTEXT/SUMMARY com observações.
- **PAUSA #2 (diff consolidado pré-bump)** verifica antes do push: 13 files cumulativos + D-01 motor vazio cumulativo + 6 commits sequenciais headers verbatim + Shields equivalência verdes. Cross-check SHA submódulo↔pai automatizado em Task 4 `<verify>` (substitui PAUSA dedicada — pattern 7.10a/7.10bcd).
- **Task 5 UAT prod humano** em mrcobrancas.com.br — re-validar SCs principais (SC2 João adicionado, SC5 wizard, SC6 Pessoas saldo cheio, SC7 Dashboard preservado) com dados reais.
- **Task 6 — Tag `v1.4-phase7.13` + commit docs consolidado**: tag no pai + submódulo após APROVADO UAT prod; commit `docs(07.13)` reúne SUMMARY 01 + SUMMARY 02 + STATE.md atualizado.

**Por que 2 PAUSAs blocking + 2 PAUSAs internas Plan 01 + UAT humano = 6 checkpoints total** (D-pre-3 phase grande, ~3-4 sessões em 2-3 dias). Proporcional ao escopo (13 files, 6 commits, 2 fluxos novos críticos: wizard D-pre-13 + Pessoas saldo cheio).

Output:
- Pre-UAT smoke PASS registrado
- UAT 10 SCs + 2 SC-iso PASS registrado (matriz completa com PROD/LOCAL valores cross-check)
- Bump submódulo no pai
- Push do submódulo (6 commits) + push do pai
- UAT prod humano APROVADO em mrcobrancas.com.br
- Tag `v1.4-phase7.13` criada (pai + submódulo)
- `07.13-02-SUMMARY.md` documentando UAT 10+2 SCs + SHAs + 4 PAUSAs (incluindo internas) + drift D-28 observação
- `07.13-01-SUMMARY.md` (criado em Plan 01 Task 7) commitado junto neste plan
- `.planning/STATE.md` atualizado — Phase 7.13 SHIPPED
- Commit docs consolidado: `docs(07.13): PLAN, SUMMARY consolidados + STATE phase 7.13 SHIPPED`

Fora de escopo:
- Auto-povoamento polo passivo ProcessosJudiciais (deferred — phase futura)
- Ativação UI responsabilidade=SUBSIDIARIA (deferred)
- Mudança de papéis em Pessoas cross-contract (deferred)
- Decomposição/petição multi-devedor (v1.5)
- Deprecação de dividas.devedor_id (preservada — phase futura)
- Qualquer edit de código-fonte (Plan 01 já executou tudo em 6 commits)
- Smoke test independente em prod por outras fontes (UAT prod em Task 5 cobre)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/PROJECT.md
@.planning/phases/07.13-multiplos-devedores-contrato-solidariedade-passiva/07.13-CONTEXT.md
@.planning/phases/07.13-multiplos-devedores-contrato-solidariedade-passiva/07.13-01-PLAN.md
@.planning/phases/07.13-multiplos-devedores-contrato-solidariedade-passiva/07.13-01-SUMMARY.md

<!-- Plan 01 criou os 6 commits no submódulo + SUMMARY 01 (left uncommitted no pai); este plan executa Pre-UAT smoke + UAT 10+2 SCs + bumpa no pai após APROVADO + push humano + UAT prod + tag + commita SUMMARY 01 + 02 + STATE juntos. -->

<!-- Memory feedbacks aplicados em UAT design -->
<!-- feedback_proc_judiciais_create_bug_preexisting.md → Pre-UAT smoke obrigatório (Task 1) -->
<!-- feedback_cross_entity_uat_isolation.md → SC-iso-1 + SC-iso-2 obrigatórios (Task 2) -->
<!-- feedback_db_integration_gate_missing.md → SC1 + SC10 cobrem fan-out + cascade nova parcela com INSERT real -->
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1 — PAUSA #1: Pre-UAT smoke obrigatório (lição feedback_proc_judiciais_create_bug_preexisting.md)</name>
  <read_first>
    - `.planning/phases/07.13-multiplos-devedores-contrato-solidariedade-passiva/07.13-CONTEXT.md` (L241-243 Pre-existing feature check)
    - `.planning/phases/07.13-multiplos-devedores-contrato-solidariedade-passiva/07.13-01-SUMMARY.md` (confirmar 6 commits criados + Plan 01 gates finais cumulativos verdes)
    - `memory/feedback_proc_judiciais_create_bug_preexisting.md` (lição 7.10a — pre-check feature alvo utilizável em prod)
  </read_first>
  <what-built>
    Plan 01 Tasks 1-7 criou 6 commits atômicos sequenciais no submódulo (cadeia D-30 bisect-able) sobre baseline f00cf2c:
    - **commit 07.13a**: service layer (5 helpers contrato-level + seedDevedoresDoContrato + test 3 it())
    - **commit 07.13b**: cascade nova dívida → seedDevedoresDoContrato (contratos.js adicionarDocumento + App.jsx fluxo legacy)
    - **commit 07.13c**: UI DetalheContrato componente multi-devedor + Modal wizard D-pre-13 + Modal warning último PRINCIPAL
    - **commit 07.13d**: DetalheDivida read-only mode (prop readOnly + 4 conditional renders)
    - **commit 07.13e**: ModuloContratos listagem inline "Nome (Papel), Nome (Papel)"
    - **commit 07.13f**: Pessoas listagem com saldo cheio para não-PRINCIPAL (helper paralelo + Dashboard preservado)

    Final Gates Task 7 verificou todos shields cumulativos verdes (D-01 motor intocado + D-30 6 commits + Shields equivalência + regressão 30/30 + build verde + 2 PAUSAs internas APROVADAS).

    O pai AINDA NÃO foi bumpado. O localhost (ou prod via deploy preview se habilitado) deve rodar com o submódulo no SHA do último commit (`feat(07.13f)`).
  </what-built>
  <how-to-verify>
    **Preparação:**
    ```bash
    cd src/mr-3/mr-cobrancas
    npm install   # se necessário
    npm run dev   # inicia Vite em http://localhost:5173
    ```
    Abrir `http://localhost:5173` (ou porta exibida), logar.

    **Alternativa**: testar em prod (`mrcobrancas.com.br`) usando dados existentes — apenas se deploy preview do submódulo estiver habilitado E SHA atual de prod for compatível. Pra esta phase, RECOMENDADO localhost (não há urgência de drift centavo, focused em fluxo).

    **Pre-UAT smoke obrigatório (lição feedback_proc_judiciais_create_bug_preexisting.md):**

    Confirmar que fluxos NÃO-7.13 funcionam em prod ANTES de testar 7.13:

    1. **Criar contrato novo (smoke A — cadastro de contrato funciona em prod):**
       - Aba Contratos → "+ Novo Contrato" → preencher (Credor, Devedor, Referência mínima) → salvar.
       - Esperado: contrato criado, navega pra DetalheContrato.
       - PASS [ ] / FAIL [ ]

    2. **Adicionar documento ao contrato (smoke B — fluxo de documento funciona):**
       - Em DetalheContrato → "+ Adicionar Documento" → preencher (NF/CV, valor, num_parcelas, data_emissao) → salvar.
       - Esperado: parcelas geradas, junction populada para o PRINCIPAL.
       - PASS [ ] / FAIL [ ]

    3. **Vincular devedor extra (smoke C — feature 7.13 funciona em casos básicos):**
       - Em DetalheContrato → seção "Devedores do Contrato" → "+ Adicionar Devedor" → buscar/selecionar qualquer pessoa → papel "Coobrigado" → confirmar.
       - Esperado: chip novo aparece "Pessoa (Coobrigado)" + N rows na junction.
       - PASS [ ] / FAIL [ ]

    4. **Remover devedor extra (smoke D — feature 7.13 reverte limpo):**
       - Clicar ✕ no chip recém-adicionado → confirmar.
       - Esperado: chip desaparece, N rows DELETED da junction.
       - PASS [ ] / FAIL [ ]

    **Regra de aprovação:** TODOS os 4 smokes A/B/C/D devem passar. Se A ou B falhar = bug pré-existente NÃO-regressão da 7.13 → PAUSA, abrir backlog "Phase 7.13.preUAT-fix" e fix antes de continuar UAT 10 SCs. Se C ou D falhar = bug REGRESSÃO da 7.13 → tratar como FAIL UAT (Task 2 mesmo decision tree).

    **Se algum SMOKE FAIL não-7.13 (A ou B):** responder `FAIL: SMOKE-A: <sintoma>` ou `FAIL: SMOKE-B: <sintoma>`. Executor abre backlog antes de Task 2.

    **Se SMOKE-C ou SMOKE-D FAIL:** responder `FAIL: SMOKE-C/D: <sintoma>` — bug regressão 7.13. Decidir entre fix-forward <5min ou rollback total `git -C src/mr-3 reset --hard f00cf2c`.
  </how-to-verify>
  <resume-signal>
    Digite `APROVADO` se TODOS os 4 smokes (A/B/C/D) PASS. Caso contrário:
    - `FAIL: SMOKE-A: <sintoma>` (cadastro de contrato bug pré-existente — não-regressão)
    - `FAIL: SMOKE-B: <sintoma>` (fluxo de documento bug pré-existente — não-regressão)
    - `FAIL: SMOKE-C: <sintoma>` (vinculação 7.13 falhou — regressão; usuário decide fix-forward ou rollback)
    - `FAIL: SMOKE-D: <sintoma>` (remoção 7.13 falhou — regressão; usuário decide fix-forward ou rollback)

    Em FAIL não-regressão (A/B), executor abre backlog "Phase 7.13.preUAT-fix" e PAUSA UAT 10 SCs até resolver.
  </resume-signal>
  <acceptance_criteria>
    - Usuário respondeu explicitamente `APROVADO`
    - Smoke A (criar contrato) = PASS
    - Smoke B (adicionar documento) = PASS
    - Smoke C (vincular devedor extra) = PASS
    - Smoke D (remover devedor extra) = PASS
    - Se FAIL não-regressão: backlog "Phase 7.13.preUAT-fix" registrado em SUMMARY 02 + STATE.md
    - Se FAIL regressão: rollback OU fix-forward decidido + executado
  </acceptance_criteria>
  <done>
    - Pre-UAT smoke completo APROVADO
    - Bug pré-existente isolado em backlog se houver
    - Pronto pra Task 2 (UAT 10 SCs)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 — UAT 10 SCs + 2 SC-iso cross-entity (CONTEXT.md L218-244 verbatim)</name>
  <read_first>
    - `.planning/phases/07.13-multiplos-devedores-contrato-solidariedade-passiva/07.13-CONTEXT.md` (§uat_scenarios L218-244 — **verbatim** — 10 SCs + 2 SC-iso + Pre-existing feature check)
    - `.planning/phases/07.13-multiplos-devedores-contrato-solidariedade-passiva/07.13-01-SUMMARY.md` (gates Plan 01 verdes confirmados)
    - `memory/feedback_cross_entity_uat_isolation.md` (lição 7.8 — feature com state global SEMPRE precisa cross-entity)
  </read_first>
  <what-built>
    Mesmo state pós-Plan 01 do Task 1 (6 commits no submódulo, pai não bumpado, localhost com SHA do feat(07.13f)).
  </what-built>
  <how-to-verify>
    **Continuar em http://localhost:5173 do Task 1 (Pre-UAT smoke já PASS).**

    **Executar os 10 SCs + 2 SC-iso (verbatim CONTEXT.md L218-244). Marcar PASS/FAIL em cada um. TODOS MANDATÓRIOS:**

    ---

    **SC1 — Criar contrato novo com 2 devedores (1 PRINCIPAL + 1 FIADOR) durante cadastro** [MANDATÓRIO]
    - Passos: "+ Novo Contrato" → preencher header → salvar → DetalheContrato → "+ Adicionar Documento" → preencher → salvar (12 parcelas, ex.) → seção "Devedores do Contrato" mostra 1 chip PRINCIPAL → "+ Adicionar Devedor" → selecionar pessoa → papel "Fiador" → confirmar.
    - Esperado: 2 chips visíveis ("Mendes (Principal)" + "João (Fiador)"); junction tem 24 rows (12 parcelas × 2 devedores). UNIQUE INDEX (papel='PRINCIPAL', divida_id) preservado: 1 PRINCIPAL por dívida.
    - Resultado: PASS [ ] / FAIL [ ]   Junction count: ____ (esperado 24)

    ---

    **SC2 — Adicionar João como FIADOR ao contrato MENDES existente (12 parcelas)** [MANDATÓRIO]
    - Passos: aba Contratos → linha MENDES → DetalheContrato → "+ Adicionar Devedor" → buscar João → papel "Fiador" → confirmar.
    - Esperado: 12 inserts em devedores_dividas (papel='FIADOR', responsabilidade='SOLIDARIA'); UI atualiza listagem chip "João (Fiador)".
    - Verificar Supabase console: SELECT count(*) FROM devedores_dividas WHERE divida_id IN (SELECT id FROM dividas WHERE contrato_id = '<MENDES>') AND devedor_id = <JOÃO> → deve ser 12.
    - Resultado: PASS [ ] / FAIL [ ]   Inserts: ____ (esperado 12)

    ---

    **SC3 — Remover João do contrato MENDES** [MANDATÓRIO]
    - Passos: DetalheContrato MENDES → ✕ no chip "João (Fiador)" → confirmar.
    - Esperado: 12 deletes em devedores_dividas; PRINCIPAL Mendes preservado; toast "Removido".
    - Resultado: PASS [ ] / FAIL [ ]   Deletes: ____ (esperado 12)

    ---

    **SC4 — Tentar remover Mendes (último PRINCIPAL do contrato MENDES) — Modal warning D-05** [MANDATÓRIO]
    - Passos: DetalheContrato MENDES → ✕ no chip "Mendes (Principal)".
    - Esperado: **Modal warning customizado** (NÃO window.confirm) com texto tipo "Promova outro devedor a Principal antes de remover este — não é possível remover o último PRINCIPAL". Bloqueado, sem DELETE em devedores_dividas. Botão "Entendido" fecha.
    - Anti-pattern check: `grep -r "window.confirm" src/mr-3/mr-cobrancas/src/components/DevedoresDoContrato.jsx` exit 1 (zero matches — D-05 LOCKED).
    - Resultado: PASS [ ] / FAIL [ ]   Modal exibido: ___ (sim/não)   DELETE bloqueado: ___ (sim/não)

    ---

    **SC5 — Promover João FIADOR → PRINCIPAL no contrato MENDES (com Mendes ainda PRINCIPAL) — wizard D-pre-13** [MANDATÓRIO]
    - Passos: re-adicionar João como FIADOR (SC2) → 👑 no chip "João (Fiador)".
    - Esperado: **Modal wizard de confirmação** (D-pre-13) com texto verbatim: "Contrato já tem PRINCIPAL (Mendes). Promover João a PRINCIPAL e demover Mendes a:" + dropdown com 5 opções (COOBRIGADO, AVALISTA, FIADOR, CONJUGE, OUTRO) **sem nada selecionado por default**. Botão "Confirmar" desabilitado até seleção. (Schema CHECK constraint só permite estes 5 + PRINCIPAL — SOLIDARIO NÃO é papel; solidariedade é via coluna `responsabilidade='SOLIDARIA'`.)
    - Selecionar "Coobrigado" → "Confirmar".
    - Esperado: 24 PATCHes em devedores_dividas (12 rows devedor_id=João papel='FIADOR'→'PRINCIPAL' + 12 rows devedor_id=Mendes papel='PRINCIPAL'→'COOBRIGADO'). UNIQUE INDEX (papel='PRINCIPAL', divida_id) preservado: 1 PRINCIPAL por dívida em todo momento (demoção PRIMEIRO via promoverParaPrincipalComDemocao). Validação D-pre-12 mantida: ≥1 PRINCIPAL preservado (João assumiu).
    - Verificar Supabase: SELECT divida_id, count(*) FROM devedores_dividas WHERE divida_id IN (...) AND papel='PRINCIPAL' GROUP BY divida_id → 1 por dívida.
    - Resultado: PASS [ ] / FAIL [ ]   Wizard exibido: ___   Sem default: ___   PATCHes: ____ (esperado 24)   UNIQUE preservado: ___

    ---

    **SC6 — Pessoas listagem após SC2 (saldo cheio para FIADOR — D-pre-6)** [MANDATÓRIO]
    - **Reverter SC5**: promover Mendes de volta a PRINCIPAL com João → FIADOR (mesmo wizard). Mantém João como FIADOR.
    - Passos: aba Pessoas → buscar Mendes (PRINCIPAL) e João (FIADOR).
    - Esperado: ambos com chip de papel; **AMBOS com saldo cheio do contrato** (R$ 36.652,56 ou valor real do MENDES). João aparece R$ 36.652,56 (perspectiva de cobrança individual D-pre-6).
    - Resultado: PASS [ ] / FAIL [ ]   Mendes: R$ ________   João: R$ ________   Cross-check (devem ser iguais): ___

    ---

    **SC7 — Dashboard Carteira Total após SC2 (preservado, NÃO dobra)** [MANDATÓRIO — drift centavo = FAIL]
    - Passos: Dashboard → card "Carteira Total" (header global).
    - Esperado: R$ 36.652,56 (ou valor real do MENDES) — **preservado, NÃO dobra para R$ 73.305,12**. Recuperado preserva pattern (sem dupla-contagem); Dashboard intocado (App.jsx L597-608 anti-dupla-contagem via `papel=PRINCIPAL` filter via `principaisDividaIds`).
    - Drift de centavo = FAIL.
    - Resultado: PASS [ ] / FAIL [ ]   Carteira: R$ ________   Drift: R$ ______ (esperado 0,00)

    ---

    **SC8 — ModuloContratos listagem inline após SC2** [MANDATÓRIO — D-pre-2/D-pre-14]
    - Passos: aba Contratos → linha do contrato MENDES → coluna "Devedor".
    - Esperado: render inline `"Mendes (Principal), João (Fiador)"` — PRINCIPAL primeiro; sem badge "+1"; PT-BR primeira-maiúscula via PAPEL_LABEL.
    - Resultado: PASS [ ] / FAIL [ ]   String render: ____________________

    ---

    **SC9 — DetalheDivida (qualquer parcela do contrato MENDES) após SC2 (read-only — D-pre-9)** [MANDATÓRIO]
    - Passos: aba Dívidas → buscar parcela do MENDES → DetalheDivida → seção "Pessoas Vinculadas".
    - Esperado: 2 chips (Mendes Principal + João Fiador) read-only — **SEM** botões "+ Adicionar", ✕ remover, 👑 promover. Apenas chips com chip de papel + nome + cpf.
    - Resultado: PASS [ ] / FAIL [ ]   Botões escondidos: ___ (sim/não)

    ---

    **SC10 — Adicionar nova parcela (Documento novo) ao contrato MENDES após SC2 (cascade D-pre-11)** [MANDATÓRIO]
    - Passos: DetalheContrato MENDES (com Mendes PRINCIPAL + João FIADOR) → "+ Adicionar Documento" → 6 parcelas → salvar.
    - Esperado: cascade `seedDevedoresDoContrato` → junction ganha 12 rows novas (6 parcelas × 2 devedores: Mendes PRINCIPAL + João FIADOR); papéis e responsabilidade preservados; UI atualiza.
    - Verificar Supabase: SELECT count(*) FROM devedores_dividas WHERE divida_id IN (SELECT id FROM dividas WHERE documento_id = '<doc_novo>') → deve ser 12 (6 × 2).
    - Resultado: PASS [ ] / FAIL [ ]   Inserts: ____ (esperado 12)

    ---

    **SC-iso-1 — Cross-entity isolation (lição feedback_cross_entity_uat_isolation.md)** [MANDATÓRIO]
    - Pre-state: contrato MENDES tem João FIADOR (após SC2).
    - Passos: aba Contratos → linha contrato `advair` → DetalheContrato → seção "Devedores do Contrato".
    - Esperado: listagem advair NÃO mostra João — apenas devedores DO contrato advair (PRINCIPAL legítimo). Pessoas: João aparece (saldo do MENDES) mas advair não tem João vinculado.
    - Resultado: PASS [ ] / FAIL [ ]   advair contém João: ___ (esperado: NÃO)

    ---

    **SC-iso-2 — Cascade DELETE preserva isolamento entre contratos** [MANDATÓRIO]
    - Pre-state: contrato MENDES com Mendes PRINCIPAL + João FIADOR; contrato advair preservado.
    - Passos (CUIDADO — operação destrutiva — usar contrato de teste se MENDES tem dados de uso real): excluir um contrato de teste sem pagamentos via "Excluir Contrato" (Phase 7.2).
    - Esperado: `devedores_dividas` rows do contrato caem via FK CASCADE em `dividas` (CASCADE definido em Migration 002 §6 L161 `divida_id UUID NOT NULL REFERENCES dividas(id) ON DELETE CASCADE`); contrato advair PRESERVADO (devedores e dívidas intactos).
    - Verificar Supabase: SELECT count(*) FROM devedores_dividas WHERE divida_id IN (dividas do contrato excluído) → 0.
    - Resultado: PASS [ ] / FAIL [ ]   advair preservado: ___ (sim/não)   junction MENDES limpa: ___

    ---

    **Matriz consolidada — preencher TODOS antes de aprovar:**
    ```
    Pre-UAT smoke (Task 1):                    [ ] PASS já confirmado
    SC1  — Criar contrato 2 devedores:         [ ] PASS  [ ] FAIL  Junction: ______
    SC2  — Adicionar João FIADOR ao MENDES:    [ ] PASS  [ ] FAIL  Inserts: ______
    SC3  — Remover João:                       [ ] PASS  [ ] FAIL  Deletes: ______
    SC4  — Bloqueio último PRINCIPAL warning:  [ ] PASS  [ ] FAIL  Modal: ___
    SC5  — Wizard D-pre-13 promoção:           [ ] PASS  [ ] FAIL  PATCHes: ______ (24)
    SC6  — Pessoas saldo cheio:                [ ] PASS  [ ] FAIL  Drift: R$ ______
    SC7  — Dashboard Carteira preservado:      [ ] PASS  [ ] FAIL  Drift: R$ ______ (0,00)
    SC8  — ModuloContratos inline:             [ ] PASS  [ ] FAIL  String: __________
    SC9  — DetalheDivida read-only:            [ ] PASS  [ ] FAIL
    SC10 — Cascade nova parcela:               [ ] PASS  [ ] FAIL  Inserts: ______ (12)
    SC-iso-1 — advair NÃO contém João:         [ ] PASS  [ ] FAIL
    SC-iso-2 — Cascade DELETE preserva isolam: [ ] PASS  [ ] FAIL
    ```

    **Regra de aprovação:** PASS em **TODOS os 10 SCs + 2 SC-iso** é MANDATÓRIO. Drift de centavo no SC7 = FAIL.

    **Se algum SC = FAIL:**
    - **(A) Bug trivial fixável <5min**: fix-forward dentro da phase (executor revisita Plan 01 em modo gap-closure; novo commit feat(07.13X.fix)); re-rodar SC FAIL.
    - **(B) Bug não-trivial OU múltiplos SCs FAIL**: ROLLBACK TOTAL D-34:
      ```bash
      cd src/mr-3 && git reset --hard f00cf2c
      ```
      (descarta os 6 commits 07.13a/b/c/d/e/f). Phase aborta inteira. Documentar em SUMMARY 02 + STATE.md como "Phase 7.13 ABORTED — UAT FAIL em SC<x>: <sintoma>".
  </how-to-verify>
  <resume-signal>
    Digite `APROVADO` (ou `APROVADO com ressalva: <nota>`) SE TODOS os 10 SCs + 2 SC-iso passaram. Caso contrário:
    - `FAIL: <SCx>: <sintoma>` (1-2 SCs FAIL; usuário decide fix-forward <5min ou rollback)
    - `FAIL: ROLLBACK: <SCs falhos>` (rollback total `git -C src/mr-3 reset --hard f00cf2c`)

    Em FAIL, executor aborta SEM bumpar pai e SEM autorizar push.
  </resume-signal>
  <acceptance_criteria>
    - Usuário respondeu `APROVADO`
    - Matriz SC1-SC10 + SC-iso-1 + SC-iso-2 preenchida com PASS em cada item
    - SC1 PASS (criar contrato 2 devedores)
    - SC2 PASS (adicionar João FIADOR — 12 inserts)
    - SC3 PASS (remover João — 12 deletes)
    - SC4 PASS (Modal warning D-05 bloqueia último PRINCIPAL — sem window.confirm)
    - SC5 PASS (wizard D-pre-13 com dropdown sem default + 24 PATCHes pareados)
    - SC6 PASS (Pessoas João saldo cheio = MENDES saldo)
    - SC7 PASS (Dashboard Carteira Total drift R$ 0,00 — não dobra)
    - SC8 PASS (ModuloContratos inline "Mendes (Principal), João (Fiador)")
    - SC9 PASS (DetalheDivida read-only — botões escondidos)
    - SC10 PASS (cascade nova parcela — 12 inserts pra 6 parcelas × 2 devedores)
    - SC-iso-1 PASS (advair não contém João)
    - SC-iso-2 PASS (cascade DELETE preserva advair)
    - Se FAIL: rollback OU fix-forward decidido + executado
  </acceptance_criteria>
  <done>
    - UAT 10 SCs + 2 SC-iso APROVADO
    - Pronto pra Task 3 (resolução divergências) ou rollback se FAIL
  </done>
</task>

<task type="auto">
  <name>Task 3 — Resolução de divergências + atualização CONTEXT/SUMMARY</name>
  <read_first>
    - Output da PAUSA #1 (Pre-UAT smoke) e PAUSA Task 2 (UAT 10+2)
    - `.planning/phases/07.13-multiplos-devedores-contrato-solidariedade-passiva/07.13-CONTEXT.md` (drift D-28 referência se aplicável)
    - `.planning/phases/07.13-multiplos-devedores-contrato-solidariedade-passiva/07.13-01-SUMMARY.md` (gates Plan 01)
  </read_first>
  <files>
    - .planning/phases/07.13-multiplos-devedores-contrato-solidariedade-passiva/07.13-02-SUMMARY.md (NEW — draft inicial; será completado em Task 6 com SHA-bump + UAT prod + tag)
  </files>
  <action>
    **Caso 1 — Todos PASS (caminho feliz):**
    Criar draft inicial do SUMMARY 02 com matriz UAT completa registrada (PASS em todos):
    ```bash
    cat > .planning/phases/07.13-multiplos-devedores-contrato-solidariedade-passiva/07.13-02-SUMMARY.md <<'EOF'
    ---
    phase: 7.13
    plan: 07.13-02
    status: uat-approved (pre-bump, draft)
    pre_uat_smoke: APPROVED (4 smokes A/B/C/D PASS)
    uat_matrix:
      SC1: PASS
      SC2: PASS
      SC3: PASS
      SC4: PASS
      SC5: PASS
      SC6: PASS
      SC7: PASS
      SC8: PASS
      SC9: PASS
      SC10: PASS
      SC-iso-1: PASS
      SC-iso-2: PASS
    ---

    # Phase 7.13 Plan 02 — SUMMARY (draft inicial; SHA-bump + UAT prod + tag a serem preenchidos)

    ## Pre-UAT smoke (Task 1)
    - Smoke A (criar contrato): PASS
    - Smoke B (adicionar documento): PASS
    - Smoke C (vincular devedor extra): PASS
    - Smoke D (remover devedor extra): PASS

    ## UAT 10 SCs + 2 SC-iso (Task 2)
    [Matriz completa será preenchida com valores reais coletados]
    - SC1 - SC10: TODOS PASS
    - SC-iso-1 + SC-iso-2: TODOS PASS
    - Drift de centavo SC7 (Dashboard Carteira): R$ 0,00 (preservado)

    ## Drifts observados
    [Nenhum / D-28 nota se aplicável]

    ## Próximo passo
    Task 4 — PAUSA #2 (diff consolidado pré-bump) + bump pai + push + Vercel deploy.
    EOF
    ```

    **Caso 2 — Algum SC FAIL com fix-forward:**
    Documentar o fix em SUMMARY 02 (qual SC falhou, qual commit fix foi feito, novo SHA). Atualizar baseline f00cf2c..HEAD count para 7+ commits (6 originais + commits fix).

    **Caso 3 — Algum drift PROD vs LOCAL (ex: testou só localhost):**
    Marcar em SUMMARY 02 como "drift LOCAL-only — re-validar em PROD em Task 5 (UAT prod humano)". Se for drift D-28 esperado (dados antigos via UI legacy), documentar e prosseguir.

    **Caso 4 — Algum item FAIL não-trivial (rollback):**
    Não chega aqui — Task 2 já abortou. SUMMARY 02 = "Phase 7.13 ABORTED" + STATE.md atualizado.
  </action>
  <verify>
    <automated>
      test -f .planning/phases/07.13-multiplos-devedores-contrato-solidariedade-passiva/07.13-02-SUMMARY.md || exit 1
    </automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY 02 draft criado com matriz UAT preenchida
    - Drift D-28 documentado se observado
    - Fix-forward commits documentados se houve
    - Pronto pra Task 4 (PAUSA #2 + bump)
  </acceptance_criteria>
  <done>
    - Divergências resolvidas (debug pontual, drift documentado, fix-forward, ou rollback)
    - SUMMARY 02 draft criado
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4 — PAUSA #2: Diff consolidado pré-bump + push submódulo + bump pai + push pai + Vercel deploy</name>
  <read_first>
    - `.planning/phases/07.13-multiplos-devedores-contrato-solidariedade-passiva/07.13-01-SUMMARY.md` (Plan 01 gates verdes)
    - `.planning/phases/07.13-multiplos-devedores-contrato-solidariedade-passiva/07.13-02-SUMMARY.md` draft (UAT APROVADO)
    - `.planning/STATE.md` (last_activity atual = Phase 7.10bcd SHIPPED)
  </read_first>
  <what-built>
    Plan 01 6 commits + UAT 10 SCs + 2 SC-iso APROVADO. Próximo passo: bump pai com 6 commits do submódulo + push humano + Vercel auto-deploy.
  </what-built>
  <how-to-verify>
    **PAUSA #2 — Diff consolidado pré-bump (verificação automatizada de 10 pontos antes do bump):**

    ```bash
    cd src/mr-3

    # 1. EXATAMENTE 6 commits cumulativos
    COUNT=$(git log f00cf2c..HEAD --oneline | wc -l)
    echo "1. Commits cumulativos: $COUNT (esperado: 6)"

    # 2. Headers verbatim feat(07.13a/b/c/d/e/f)
    HEADERS=$(git log f00cf2c..HEAD --format=%s | grep -cE "^feat\(07\.13[a-f]\):")
    echo "2. Headers feat(07.13X) válidos: $HEADERS (esperado: 6)"

    # 3. D-01 motor diff cumulativo VAZIO
    DIFF_LEN=$(git diff f00cf2c..HEAD -- src/mr-cobrancas/src/utils/devedorCalc.js src/mr-cobrancas/src/services/pagamentos.js src/mr-cobrancas/src/services/dividas.js | wc -c)
    echo "3. D-01 motor diff cumulativo: $DIFF_LEN bytes (esperado: 0)"

    # 4. D-01 motor name-only cumulativo VAZIO
    NAME_MATCHES=$(git log f00cf2c..HEAD --format="" --name-only | grep -cE "src/mr-cobrancas/src/utils/devedorCalc\.js|src/mr-cobrancas/src/services/pagamentos\.js|src/mr-cobrancas/src/services/dividas\.js" || true)
    echo "4. D-01 motor name-only matches: $NAME_MATCHES (esperado: 0)"

    # 5. Files cumulativos modificados
    FILES=$(git log f00cf2c..HEAD --format="" --name-only | sort -u | grep -v "^$" | wc -l)
    echo "5. Files cumulativos: $FILES (esperado: ~13)"

    # 6. Working tree limpo
    DIRTY=$(git status --short | wc -l)
    echo "6. Working tree dirty count: $DIRTY (esperado: 0)"

    # 7. Build cumulativo
    cd mr-cobrancas
    npm run build 2>&1 > /dev/null && echo "7. Build: PASS" || echo "7. Build: FAIL"

    # 8. Regressão cumulativa 30/30
    npm run test:regressao 2>&1 | grep -E "Tests.*30 passed" && echo "8. Regressão 30/30: PASS" || echo "8. Regressão: FAIL"
    cd ..

    # 9. SHA submódulo HEAD (a ser bumpado no pai)
    SUBMODULE_HEAD=$(git rev-parse HEAD)
    echo "9. Submódulo HEAD SHA: $SUBMODULE_HEAD"
    cd ..

    # 10. Estado do pai (submódulo dirty)
    git status src/mr-3
    # Esperado: "modified content, untracked content" ou "modified" indicando que pai vê o submódulo em SHA diferente
    ```

    **Regra de aprovação PAUSA #2:** TODOS os 8 pontos automatizados devem passar (1-8). Pontos 9-10 são informativos.

    Se passar APROVADO em PAUSA #2, executor procede com **push humano** + **bump no pai** + **push do pai** (Task continua):

    ```bash
    # Push humano do submódulo
    git -C src/mr-3 push origin main
    # Esperado: 6 commits enviados para origin/main do submódulo mr-3

    # Bump no pai
    git add src/mr-3
    git commit -m "chore(07.13): bump submodule mr-3 — múltiplos devedores por contrato (solidariedade passiva)

    6 commits atômicos sequenciais bisect-able no submódulo:
    - feat(07.13a): service layer multi-devedor por contrato (5 helpers + seedDevedoresDoContrato)
    - feat(07.13b): cascade nova divida → seedDevedoresDoContrato
    - feat(07.13c): UI DetalheContrato componente multi-devedor + wizard D-pre-13
    - feat(07.13d): DetalheDivida read-only mode
    - feat(07.13e): ModuloContratos listagem inline 'Nome (Papel), Nome (Papel)'
    - feat(07.13f): Pessoas listagem com saldo cheio para não-PRINCIPAL

    Schema zero-diff (D-pre-4 — reusa devedores_dividas Migrações 001+002).
    D-01 motor INTOCADO (devedorCalc.js + pagamentos.js + dividas.js zero diff cumulativo).
    Regressão 30/30 PASS. UAT 10 SCs + 2 SC-iso APROVADO."

    # Push do pai
    git push origin master
    # Esperado: bump SHA enviado pra origin/master, Vercel auto-build dispara
    ```

    **Cross-check SHA submódulo↔pai (substitui PAUSA #3 dedicada — pattern 7.10a/7.10bcd):**

    ```bash
    SUBMODULE_HEAD_PUSHED=$(git -C src/mr-3 rev-parse HEAD)
    PARENT_BUMP=$(git ls-tree HEAD src/mr-3 | awk '{print $3}')
    test "$SUBMODULE_HEAD_PUSHED" = "$PARENT_BUMP" && echo "SHA cross-check OK" || { echo "FAIL: SHA mismatch — submódulo $SUBMODULE_HEAD_PUSHED vs pai $PARENT_BUMP"; exit 1; }
    ```

    **Aguardar Vercel deploy** (1-3min) — verificar dashboard Vercel ou abrir mrcobrancas.com.br após deploy.

    **Se algum ponto 1-8 do diff consolidado FAIL:** responder `FAIL: ponto-N: <sintoma>`. NÃO bumpar.
  </how-to-verify>
  <resume-signal>
    Digite `APROVADO` se 1-8 PASS + push submódulo + bump pai + push pai + Vercel build acionado executados sem erro. Caso contrário, `FAIL: ponto-N: <sintoma>` ou `FAIL: push: <erro>` (executor não bumpa).
  </resume-signal>
  <acceptance_criteria>
    - **Diff consolidado 1-8**: todos PASS (6 commits / headers válidos / D-01 motor zero / files ~13 / working tree clean / build PASS / regressão 30/30 PASS)
    - **Push submódulo**: `git -C src/mr-3 push origin main` exit 0
    - **Bump pai**: commit `chore(07.13): bump submodule mr-3...` criado no pai
    - **Push pai**: `git push origin master` exit 0
    - **Cross-check SHA**: SHA submódulo HEAD pushed === SHA bump no pai
    - **Vercel build acionado**: usuário confirma via dashboard Vercel ou mrcobrancas.com.br responde com novo build
    - Usuário respondeu `APROVADO`
  </acceptance_criteria>
  <done>
    - Diff consolidado 1-8 verificado verde
    - Push submódulo + bump pai + push pai executados
    - SHA cross-check OK
    - Vercel deploy disparado
    - Pronto pra Task 5 (UAT prod)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5 — UAT prod humano em mrcobrancas.com.br (cenários principais re-validados)</name>
  <read_first>
    - `.planning/phases/07.13-multiplos-devedores-contrato-solidariedade-passiva/07.13-02-SUMMARY.md` (UAT local APROVADO em Task 2)
    - `.planning/phases/07.13-multiplos-devedores-contrato-solidariedade-passiva/07.13-CONTEXT.md` (UAT scenarios verbatim)
  </read_first>
  <what-built>
    Vercel deploy concluído (1-3min após Task 4). mrcobrancas.com.br responde com build novo (SHA bump pai recente).
  </what-built>
  <how-to-verify>
    **UAT prod humano — re-validar cenários PRINCIPAIS em mrcobrancas.com.br:**

    Abrir `https://mrcobrancas.com.br`, logar com credenciais reais, F5 (cache headers Phase 7.4 garantem build novo).

    **SCs principais a re-validar (subset crítico — não todos os 10 — por velocidade UAT prod):**

    1. **SC2 prod (adicionar devedor extra real):**
       - Aba Contratos → contrato MENDES (ou outro com 12+ parcelas) → DetalheContrato → "+ Adicionar Devedor" → buscar devedor real → papel "Fiador" → confirmar.
       - Esperado: chip aparece, junction populada (cross-check Supabase prod via dashboard).
       - PASS [ ] / FAIL [ ]

    2. **SC5 prod (wizard D-pre-13 em prod):**
       - DetalheContrato → 👑 promover → wizard exibido com dropdown sem default → escolher papel anterior → confirmar.
       - Esperado: promoção atômica, UI atualiza com PRINCIPAL trocado.
       - PASS [ ] / FAIL [ ]

    3. **SC6 prod (Pessoas saldo cheio em prod):**
       - aba Pessoas → fiador real adicionado → coluna saldo deve ser cheio (= PRINCIPAL).
       - PASS [ ] / FAIL [ ]

    4. **SC7 prod (Dashboard Carteira Total preservado em prod):**
       - Capturar Carteira Total ANTES de adicionar fiador → adicionar fiador → re-capturar → drift deve ser R$ 0,00.
       - PASS [ ] / FAIL [ ]   Drift PROD: R$ ______

    5. **SC8 prod (ModuloContratos inline em prod):**
       - aba Contratos → linha contrato com fiador → render inline "Nome (Papel), Nome (Papel)".
       - PASS [ ] / FAIL [ ]

    6. **Sanity: Console limpo (sem erros JS):**
       - DevTools Console → navegação por todas as abas (Dashboard, Devedores, Contratos, Dívidas, Processos, Pessoas) → ZERO errors em vermelho. Warnings ignoráveis.
       - PASS [ ] / FAIL [ ]

    7. **Smoke não-regressão funcional das 4 telas migradas em 7.10a + 7.10bcd** [MANDATÓRIO — drift inesperado em qualquer = FAIL phase, abrir backlog "Phase 7.13.prod-fix"]:

       Estas 4 telas foram migradas para `pagamentos_divida` em phases anteriores (7.10a + 7.10bcd). Phase 7.13 NÃO altera o pattern delas, mas REUSA conceitos próximos (helper paralelo, Set lookup). Validar funcionalidade em prod **antes da tag** evita regressão silenciosa descoberta só pós-ship (lição feedback_proc_judiciais_create_bug_preexisting.md cumulativa com feedback_grep_global_before_pointwise_fix.md).

       7a. **TabelaDividas (D-27 Set lookup intocado):**
       - Abrir aba Dívidas → TabelaDividas listagem → confirmar parcelas listadas com coluna "Saldo Atualizado" preenchida (não "Calculando...").
       - Cross-check 1 dívida: clicar para abrir DetalheDivida → saldo individual exibido = saldo na tabela ± centavo.
       - Set lookup pattern (D-27) preservado: pagamento aparece no devedor correto.
       - PASS [ ] / FAIL [ ]   Print/observação: ____________

       7b. **ProcessosJudiciais polo passivo (junction `processos_devedores` independente, deferred D-pre-14):**
       - Abrir aba Processos → selecionar processo existente com polo passivo cadastrado (MENDES ou outro) → tab "Polo Passivo".
       - Esperado: devedores manuais cadastrados via `processos_devedores` permanecem visíveis (NÃO foram apagados nem auto-povoados a partir de devedores do contrato).
       - tab "Dívidas" do processo: saldo por devedor do polo calculado corretamente (pattern 7.10a Set lookup divida_id ∈ dividas-do-devedor).
       - **Se polo passivo vazio onde antes tinha devedores OU saldo zerado em devedor que tinha dívida = FAIL crítico.**
       - PASS [ ] / FAIL [ ]   Print/observação: ____________

       7c. **Dashboard Carteira Total drift R$ 0,00 vs pre-7.13 (sem fiadores ainda cadastrados em prod):**
       - Antes de adicionar QUALQUER fiador via Phase 7.13 em prod: capturar `Carteira Total` em mrcobrancas.com.br (valor pré-7.13).
       - Após deploy 7.13 (mas SEM ter adicionado fiador novo ainda): re-capturar `Carteira Total`. **Esperado: drift R$ 0,00 absoluto** (não centavo).
       - Justificativa: schema zero-diff (D-pre-4) + Dashboard intocado (D-pre-6 preserva pattern `papel=PRINCIPAL` filter App.jsx L597-608 + L8434).
       - **Se drift > R$ 0,00 = FAIL crítico** (regressão Dashboard introduzida em algum dos 6 commits).
       - PASS [ ] / FAIL [ ]   Carteira pré: R$ ________ · pós: R$ ________ · drift: R$ ________

       7d. **Pessoas filtros/buscas funcionais (não só listagem passiva):**
       - aba Pessoas → buscar por nome (campo busca) → filtrar resultado.
       - Filtro por status (em cobrança / quitado / etc se houver).
       - Helper paralelo `agruparPagamentosPorDevedorIncluindoSolidarios` (D-pre-6 commit 07.13f) deve preservar funcionamento de filtros — se filtro quebrou, regressão de pattern de iteração.
       - PASS [ ] / FAIL [ ]   Print/observação: ____________

       **Cada um dos 4 itens (7a/b/c/d) produz print ou observação textual. Drift inesperado em QUALQUER 1 = FAIL phase. Abrir backlog "Phase 7.13.prod-fix" antes de Task 6 (tag).**

    **Limpar estado de teste:** se adicionou devedor real para o teste, remover ao final pra não poluir prod data.

    **Se algum FAIL em prod (mas LOCAL passou no Task 2):** investigar drift D-28 ou bug de ambiente; abrir backlog "Phase 7.13.prod-fix" se não-trivial. Rollback é opção pesada — preferir fix-forward em prod.
  </how-to-verify>
  <resume-signal>
    Digite `APROVADO` se todos os 6 SCs principais + 4 smokes não-regressão (7a/b/c/d) PASS. Caso contrário, `FAIL: prod-N: <sintoma>` ou `FAIL: smoke-7X: <sintoma>` (executor abre backlog ou decide fix-forward urgente).
  </resume-signal>
  <acceptance_criteria>
    - Usuário respondeu `APROVADO`
    - Todos 6 SCs principais PROD = PASS
    - 4 smokes não-regressão (7a TabelaDividas / 7b ProcessosJudiciais polo / 7c Dashboard drift R$ 0,00 / 7d Pessoas filtros) = PASS
    - Console limpo (zero JS errors)
    - Estado de teste limpo (devedor de teste removido)
  </acceptance_criteria>
  <done>
    - UAT prod APROVADO em mrcobrancas.com.br
    - Pronto pra Task 6 (tag + commit docs)
  </done>
</task>

<task type="auto">
  <name>Task 6 — Tag v1.4-phase7.13 (pai + submódulo) + commit docs consolidado SUMMARY 01 + 02 + STATE.md</name>
  <read_first>
    - `.planning/phases/07.13-multiplos-devedores-contrato-solidariedade-passiva/07.13-01-SUMMARY.md` (criado em Plan 01 Task 7, left uncommitted)
    - `.planning/phases/07.13-multiplos-devedores-contrato-solidariedade-passiva/07.13-02-SUMMARY.md` draft (criado em Task 3, atualizar com SHA bump + UAT prod APROVADO + tag SHA)
    - `.planning/STATE.md` (atualizar last_activity + status Phase 7.13 SHIPPED)
    - `.planning/ROADMAP.md` (linha 386 status atual `Planned 2026-04-26 — awaiting /gsd-plan-phase 7.13` → `Shipped <data>`)
  </read_first>
  <files>
    - .planning/phases/07.13-multiplos-devedores-contrato-solidariedade-passiva/07.13-02-SUMMARY.md (UPDATE — completar com SHA bump + UAT prod + tag)
    - .planning/STATE.md (UPDATE — last_activity + progress Phase 7.13 SHIPPED)
    - .planning/ROADMAP.md (UPDATE — Phase 7.13 status: Shipped <data> + tag v1.4-phase7.13)
  </files>
  <action>
    **Parte A — Criar tags (pai + submódulo):**

    ```bash
    # Tag no submódulo (no SHA do feat(07.13f) último commit)
    git -C src/mr-3 tag v1.4-phase7.13
    git -C src/mr-3 push origin v1.4-phase7.13

    # Tag no pai (no SHA do bump commit chore(07.13): bump submodule mr-3...)
    git tag v1.4-phase7.13
    git push origin v1.4-phase7.13
    ```

    **Parte B — Atualizar SUMMARY 02 com dados finais:**

    Adicionar ao SUMMARY 02 (substituir draft de Task 3):
    ```markdown
    ## SHA-bump cross-check
    - Submódulo HEAD pushed: <SHA_07.13f>
    - Pai bump commit SHA: <SHA_chore_07.13>
    - Cross-check: OK (SHA submódulo === ls-tree pai)

    ## UAT prod (Task 5)
    - SC2 prod (adicionar devedor real): PASS
    - SC5 prod (wizard D-pre-13): PASS
    - SC6 prod (Pessoas saldo cheio): PASS
    - SC7 prod (Dashboard Carteira drift R$ 0,00): PASS
    - SC8 prod (ModuloContratos inline): PASS
    - Console limpo: PASS

    ## Tag
    - v1.4-phase7.13 (pai): <SHA>
    - v1.4-phase7.13 (submódulo): <SHA_07.13f>

    ## 4 PAUSAs humanas Plan 02 (proporcionais ao escopo grande)
    - PAUSA #1 (Pre-UAT smoke): APROVADO
    - PAUSA UAT 10+2 SCs (Task 2): APROVADO
    - PAUSA #2 (diff consolidado pré-bump): APROVADO
    - UAT prod (Task 5): APROVADO

    ## Total Phase 7.13 = 6 humanos checkpoints (D-pre-3 phase grande)
    Plan 01: 2 sub-PAUSAs internas (após Tasks 3 e 6) + Plan 02: 4 PAUSAs (acima)
    ```

    **Parte C — Atualizar STATE.md:**

    Adicionar ao topo da seção Phase 7.13 ou atualizar `current_plan` + `status` + `last_activity` + `progress`:

    ```yaml
    current_plan: COMPLETE
    status: Phase 7.13 SHIPPED — múltiplos devedores por contrato (solidariedade passiva). 6 commits atômicos no submódulo (feat 07.13a-f) + bump pai + tag v1.4-phase7.13. UAT 10 SCs + 2 SC-iso PASS local + UAT prod 6 SCs PASS em mrcobrancas.com.br. D-01 motor INTOCADO cumulativo desde f00cf2c. Schema zero-diff (reusa devedores_dividas). Wizard D-pre-13 + Pessoas saldo cheio (D-pre-6) + ModuloContratos inline (D-pre-14) + DetalheDivida read-only (D-pre-9) entregues.
    last_updated: "<DATA-HORA>"
    last_activity: <data> — Phase 7.13 SHIPPED. Submódulo chain de 6 commits atômicos bisect-able sobre baseline f00cf2c (07.13a service → 07.13b cascade → 07.13c UI cadastro+wizard → 07.13d read-only → 07.13e ModuloContratos inline → 07.13f Pessoas saldo cheio). Pai bump <SHA>. Tag v1.4-phase7.13 pai + submódulo. UAT prod APROVADO. 4 PAUSAs Plan 02 + 2 PAUSAs Plan 01 = 6 checkpoints total proporcional ao escopo grande (D-pre-3). Schema zero-diff: reusa devedores_dividas (Migrações 001+002). D-01 estrito cumulativo (motor 100% intocado). Helper paralelo agruparPagamentosPorDevedorIncluindoSolidarios pra Pessoas (D-pre-6); Dashboard L568 + parent App L8434 PRESERVADOS via principaisDividaIds papel='PRINCIPAL' filter (anti-dupla-contagem D-pre-6).
    ```

    **Parte D — Atualizar ROADMAP.md L386 (Phase 7.13 status):**

    ```markdown
    # ANTES:
    **Status**: Planned 2026-04-26 — awaiting /gsd-plan-phase 7.13

    # DEPOIS:
    **Status**: Shipped <DATA> — tag v1.4-phase7.13 (pai + submódulo). 6 commits atômicos no submódulo. UAT 10 SCs + 2 SC-iso PASS local + UAT prod 6 SCs PASS em mrcobrancas.com.br.
    ```

    **Parte E — Commit docs consolidado:**

    ```bash
    git add .planning/phases/07.13-multiplos-devedores-contrato-solidariedade-passiva/07.13-01-SUMMARY.md
    git add .planning/phases/07.13-multiplos-devedores-contrato-solidariedade-passiva/07.13-02-SUMMARY.md
    git add .planning/STATE.md
    git add .planning/ROADMAP.md
    git commit -m "docs(07.13): PLAN, SUMMARY consolidados + STATE phase 7.13 SHIPPED + tag v1.4-phase7.13

    Phase 7.13 (Múltiplos Devedores por Contrato — Solidariedade Passiva) completa:
    - 6 commits atômicos no submódulo (feat 07.13a-f): service layer + cascade + UI cadastro+wizard + read-only + ModuloContratos inline + Pessoas saldo cheio
    - Schema zero-diff (D-pre-4 — reusa devedores_dividas Migrações 001+002)
    - D-01 motor INTOCADO cumulativo desde f00cf2c (devedorCalc.js + pagamentos.js + dividas.js zero diff)
    - Regressão 30/30 PASS (25 base 7.10bcd + 3 helpers contrato + 2 helper paralelo)
    - UAT 10 SCs + 2 SC-iso PASS em local + UAT prod 6 SCs PASS em mrcobrancas.com.br
    - 6 humanos checkpoints (D-pre-3 phase grande proporcional ao risco)
    - Tag v1.4-phase7.13 (pai + submódulo)

    Decisões locked D-pre-1..D-pre-14 entregues:
    - D-pre-1 NÍVEL CONTRATO semanticamente / N rows operacionalmente
    - D-pre-9 UI cadastro só por contrato; DetalheDivida read-only
    - D-pre-11 seedDevedoresDoContrato cascade nova dívida
    - D-pre-13 wizard 2-steps obrigatório com dropdown 5 papéis sem default (sobrepõe demoção silenciosa)
    - D-pre-6 Pessoas saldo cheio (perspectiva de cobrança individual); Dashboard preservado via principaisDividaIds papel='PRINCIPAL'

    Próximas phases backlog:
    - Auto-povoar polo passivo ProcessosJudiciais a partir devedores do contrato (deferred)
    - Ativação UI responsabilidade=SUBSIDIARIA (deferred)
    - Decomposição/petição multi-devedor (v1.5)"
    ```

    **NÃO push docs** — usuário decide se push imediato (já bumpado em Task 4) ou agrupa com próximas mudanças.
  </action>
  <verify>
    <automated>
      git tag | grep -q "^v1.4-phase7.13$" && git -C src/mr-3 tag | grep -q "^v1.4-phase7.13$" && git log -1 --format=%s | grep -q "^docs(07.13):"
    </automated>
  </verify>
  <acceptance_criteria>
    - Tag `v1.4-phase7.13` criada no submódulo: `git -C src/mr-3 tag | grep -q "^v1.4-phase7.13$"` exit 0
    - Tag `v1.4-phase7.13` criada no pai: `git tag | grep -q "^v1.4-phase7.13$"` exit 0
    - Tag pushed submódulo: `git -C src/mr-3 ls-remote --tags origin | grep -q "v1.4-phase7.13"` exit 0
    - Tag pushed pai: `git ls-remote --tags origin | grep -q "v1.4-phase7.13"` exit 0
    - SUMMARY 02 atualizado com SHA-bump cross-check + UAT prod + tag
    - STATE.md atualizado com `Phase 7.13 SHIPPED` + last_activity + progress
    - ROADMAP.md L386 status: `Shipped <data> — tag v1.4-phase7.13`
    - Commit docs criado: `git log -1 --format=%s | grep -q "^docs(07.13):"` exit 0
    - Working tree limpo após commit docs: `git status --short | wc -l` retorna `0`
  </acceptance_criteria>
  <done>
    - Tag v1.4-phase7.13 criada em pai + submódulo + pushed
    - SUMMARY 01 + SUMMARY 02 + STATE.md + ROADMAP.md commitados em docs(07.13)
    - Phase 7.13 SHIPPED ✓
  </done>
</task>

</tasks>

<verification>
**Verificação overall do Plan 02 (após Task 6):**
- 4 PAUSAs humanas blocking executadas e APROVADAS (Pre-UAT smoke / UAT 10+2 / diff pré-bump / UAT prod)
- Plus 2 PAUSAs internas Plan 01 (após Tasks 3 e 6) = 6 checkpoints total
- 6 commits do submódulo PUSHED para origin/main do mr-3
- Bump pai PUSHED para origin/master
- Vercel auto-deploy concluído + UAT prod APROVADO
- Tag v1.4-phase7.13 criada e pushed em pai + submódulo
- SUMMARY 01 + 02 + STATE.md + ROADMAP.md commitados em `docs(07.13): PLAN, SUMMARY consolidados + STATE phase 7.13 SHIPPED + tag v1.4-phase7.13`
- Working tree limpo
</verification>

<success_criteria>
- Pre-UAT smoke (4 sub-smokes A/B/C/D) APROVADO
- UAT 10 SCs + 2 SC-iso PASS local
- UAT prod 6 SCs PASS em mrcobrancas.com.br
- 4 PAUSAs Plan 02 APROVADAS
- Bump pai + push submódulo + push pai executados
- SHA cross-check submódulo↔pai OK (automático em Task 4)
- Tag v1.4-phase7.13 criada e pushed (pai + submódulo)
- Commit docs consolidado criado
- STATE.md + ROADMAP.md refletem Phase 7.13 SHIPPED
</success_criteria>

<output>
Após completar Task 6, executor confirma Phase 7.13 SHIPPED. Próximas ações backlog (não desta phase):
- `/gsd-extract-learnings 07.13` (extrair LEARNINGS — padrão pós-ship 7.10a/7.10bcd)
- Phase 7.13.prod-fix se algum drift PROD-only emergiu
- Phase futura: auto-povoar polo passivo ProcessosJudiciais (deferred D-pre)
- Phase futura: ativação UI responsabilidade=SUBSIDIARIA (deferred)
- Continuar milestone v1.4: Phase 8 PDF Demonstrativo
</output>
