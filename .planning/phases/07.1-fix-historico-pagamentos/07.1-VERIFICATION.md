---
phase: 07.1-fix-historico-pagamentos
verified: 2026-04-22T22:33:30Z
status: passed
score: 3/3 must-haves verified
overrides_applied: 0
gaps: []
human_verification:
  - test: "Abrir histórico de um contrato com evento pagamento_recebido ou pagamento_revertido e confirmar que Valor, Data e Parcelas aparecem"
    expected: "A linha do evento exibe 'Valor: R$ X.XXX,XX', 'Data: DD/MM/AAAA' e 'Parcelas: Parc. N/T (...  amortizada(s))' — sem nenhum '— → —'"
    why_human: "O ramo JSX isPagamento está corretamente wired e alimentado por snap.valor/snap.data_pagamento/snap.parcelas_ids, mas confirmar visualmente que o snapshot_campos salvo pela SP contém esses campos requer acesso ao banco de produção ou ambiente de teste com dado real"
  - test: "Criar um pagamento às 23h BRT (02h UTC do dia seguinte) e verificar a data exibida no histórico"
    expected: "A data exibida no timeline deve ser a data BRT (ex: 22/04/2026), não a data UTC (23/04/2026)"
    why_human: "fmtDataHora usa toLocaleDateString no browser — a correção de timezone só é validável com um evento cujo created_at caia em faixa UTC+0 = dia seguinte BRT, o que requer dado real no banco"
---

# Phase 7.1: Fix Histórico Pagamentos — Verification Report

**Phase Goal:** Corrigir dois bugs visuais confirmados no histórico de pagamentos de DetalheContrato.jsx — Bug A (isPagamento render branch) e Bug B (fmtDataHora timezone). Fechar os dois bugs reportados no UAT da Phase 7 antes de avançar para a Phase 8.
**Verified:** 2026-04-22T22:33:30Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Eventos de tipo pagamento_recebido e pagamento_revertido na timeline exibem Valor, Data e Parcelas — não exibem "— → —" | VERIFIED | `const isPagamento` declarado (L815); `diffEntries` guard inclui `&& !isPagamento` (L819); JSX branch `{isPagamento && (...)}` (L871–880) exibe `fmtBRL(snap.valor)`, `fmtData(snap.data_pagamento)`, `buildParcelasText(snap.parcelas_ids, dividas)` — diffEntries será `[]` para pagamentos, eliminando o "— → —" |
| 2 | O timestamp created_at de cada evento do histórico exibe a data correta no fuso horário de Brasília, não a data UTC deslocada | VERIFIED | `function fmtDataHora` inserida em L14 com `isNaN(d.getTime())` guard (L18) e `timeZone: "America/Sao_Paulo"` (L19); único call site `fmtDataHora(evento.created_at)` em L849; count de `fmtData(evento.created_at)` = 0 |
| 3 | Os 9/9 testes de regressão continuam passando após os fixes | VERIFIED | `npm run test:regressao` executado: `Tests 9 passed (9)`, exit code 0, duração 612ms |

**Score:** 3/3 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx` | `function fmtDataHora`, `const isPagamento`, `isPagamento && (`, `fmtDataHora(evento.created_at)`, `&& !isPagamento` | VERIFIED | Todos os 5 padrões confirmados via grep. Arquivo modificado e commitado em `5a0fc40` no submodule mr-3 |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `historico.map` callback (L813) | ramo JSX `isPagamento` (L871) | `const isPagamento` calculado em L815 antes de `diffEntries` | WIRED | `isPagamento` declarado em L815, consumido em L819 (guard) e L871 (JSX branch) — 3 ocorrências confirmadas |
| L849 — timestamp do evento | `fmtDataHora` | substituição direta do call site | WIRED | `fmtDataHora(evento.created_at)` em L849; zero ocorrências de `fmtData(evento.created_at)` restantes |
| ramo `isPagamento` JSX | `snap.valor`, `snap.data_pagamento`, `snap.parcelas_ids`, `snap.parcelas_amortizadas` | `evento.snapshot_campos || {}` (L817) | WIRED | `fmtBRL(snap.valor)` (L873), `fmtData(snap.data_pagamento)` (L874), `buildParcelasText(snap.parcelas_ids, dividas)` (L876), `snap.parcelas_amortizadas ?? snap.parcelas_ids?.length ?? 0` (L877) |
| `diffEntries` guard | exclusão de eventos de pagamento | `!isCriacao && !isPagamento` ternário (L819) | WIRED | Exatamente 1 ocorrência de `&& !isPagamento` em L819 — confirma que diffEntries = [] para pagamentos |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `DetalheContrato.jsx` — ramo `isPagamento` | `snap` (`evento.snapshot_campos`) | `listarHistorico(contrato.id)` → Supabase `contratos_historico` → `snapshot_campos JSONB` populado pela SP `registrar_pagamento_contrato` | Yes — SP registra `valor`, `data_pagamento`, `parcelas_ids`, `parcelas_amortizadas` no snapshot (documentado em CONTEXT.md e SUMMARY.md "Known Stubs: None") | FLOWING |
| `DetalheContrato.jsx` — timestamp L849 | `evento.created_at` | coluna TIMESTAMPTZ em `contratos_historico` — valor autêntico gerado pelo banco no insert | Yes — campo nativo do banco, nunca hardcoded | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| 9/9 regression tests pass | `cd src/mr-3/mr-cobrancas && npm run test:regressao` | `Tests 9 passed (9)`, exit code 0 | PASS |
| `fmtDataHora` defined at L14 with correct body | `grep -n "function fmtDataHora"` | Line 14 returned | PASS |
| `fmtDataHora(evento.created_at)` is sole call site | `grep -n "fmtDataHora(evento.created_at)"` + `grep -c "fmtData(evento.created_at)"` | L849 returned; count = 0 | PASS |
| `isPagamento` has 3 uses (declare, guard, JSX) | `grep -n "isPagamento"` | Lines 815, 819, 871 — exactly 3 | PASS |
| diffEntries guard updated | `grep -n "&& !isPagamento"` | Exactly 1 line (L819) | PASS |
| JSX branch order: isCriacao → isPagamento → diffEntries | `grep -n "isCriacao && (\|isPagamento && (\|!isCriacao && diffEntries"` | Lines 853, 871, 882 — correct order | PASS |
| submodule mr-3 commit | `git log --oneline -1` in mr-3 | `5a0fc40 fix(07.1): corrigir renderização pagamentos no histórico e timezone created_at` | PASS |
| parent bump commit | `git log --oneline` in parent | `c4f6b9f chore(07.1): bump submodule mr-3 (fix histórico pagamentos + timezone)` confirmed | PASS |

### Requirements Coverage

No `requirements:` field in PLAN frontmatter — phase is a UAT gap-closure with its own success criteria in the plan rather than a REQUIREMENTS.md reference. All success criteria from the plan are verified above.

### Anti-Patterns Found

| File | Pattern | Severity | Impact |
|------|---------|----------|--------|
| — | None found | — | No TODOs, placeholders, empty returns, or hardcoded stubs in the modified sections |

`fmtData` other call sites (`pagamento.data_pagamento` L599, `doc.data_emissao` L680, `p.data_vencimento` L717, `snap.data_pagamento` L874) are all DATE columns — correctly preserved with slice-based fmtData. Not stubs.

### Human Verification Required

#### 1. isPagamento render branch displays Valor/Data/Parcelas in production

**Test:** Open `DetalheContrato` for a contract that has at least one `pagamento_recebido` or `pagamento_revertido` event in the history. Expand the "Histórico" section. Locate the payment event row.
**Expected:** The event shows three lines — "Valor: R$ X.XXX,XX", "Data: DD/MM/AAAA", "Parcelas: Parc. N/T (N amortizada(s))". No "— → —" pattern appears.
**Why human:** The JSX branch and wiring are code-verified, but confirming `snapshot_campos` actually contains the expected fields (valor, data_pagamento, parcelas_ids, parcelas_amortizadas) requires a real database event created by `registrar_pagamento_contrato`.

#### 2. Timezone display for late-night payments

**Test:** Register a payment at 23h BRT (which is 02h UTC the following day). Expand "Histórico" and inspect the date next to the "Pagamento recebido" event label.
**Expected:** The date shown is the BRT date (e.g., 22/04/2026), not the UTC date (23/04/2026).
**Why human:** `fmtDataHora` with `timeZone: "America/Sao_Paulo"` is correct code, but actual timezone correction can only be confirmed with a real `created_at` TIMESTAMPTZ value that straddles UTC midnight.

### Gaps Summary

No gaps. Both Bug A and Bug B are fully implemented and wired in `DetalheContrato.jsx`. The regression suite (9/9) confirms no regressions. The two human verification items are standard UAT confirmations for a presentational fix — they are not blocking gaps but post-merge smoke tests.

---

_Verified: 2026-04-22T22:33:30Z_
_Verifier: Claude (gsd-verifier)_
