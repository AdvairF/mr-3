---
phase: 07.1-fix-historico-pagamentos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Eventos de tipo pagamento_recebido e pagamento_revertido na timeline exibem Valor, Data e Parcelas — não exibem '— → —'"
    - "O timestamp created_at de cada evento do histórico exibe a data correta no fuso horário de Brasília, não a data UTC deslocada"
    - "Os 9/9 testes de regressão continuam passando após os fixes"
  artifacts:
    - path: "src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx"
      provides: "função fmtDataHora, constante isPagamento, ramo JSX isPagamento, substituição fmtData→fmtDataHora em created_at"
      contains:
        - "function fmtDataHora"
        - "const isPagamento"
        - "isPagamento && ("
        - "fmtDataHora(evento.created_at)"
        - "&& !isPagamento"
  key_links:
    - from: "historico.map callback (linha ~806)"
      to: "ramo JSX isPagamento"
      via: "constante isPagamento calculada antes de diffEntries"
      pattern: "isPagamento && \\("
    - from: "linha 840 — timestamp do evento"
      to: "fmtDataHora"
      via: "substituição direta do call site"
      pattern: "fmtDataHora\\(evento\\.created_at\\)"
---

<objective>
Corrigir dois bugs visuais confirmados no histórico de pagamentos de DetalheContrato.jsx.

Bug A: Eventos pagamento_recebido / pagamento_revertido renderizam "— → —" em vez dos detalhes do pagamento (valor, data, parcelas). Fix: adicionar const isPagamento, modificar guarda de diffEntries, inserir ramo JSX dedicado.

Bug B: fmtData(evento.created_at) usa .slice(0,10) em coluna TIMESTAMPTZ — o offset UTC pode deslocar a data para o dia anterior. Fix: adicionar fmtDataHora que usa toLocaleDateString com fuso America/Sao_Paulo e substituir o único call site.

Purpose: Fechar os dois bugs reportados no UAT da Phase 7 antes de avançar para a Phase 8.
Output: DetalheContrato.jsx com 4 alterações cirúrgicas; commit no submodule mr-3 (sem push).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1-fix-historico-pagamentos/07.1-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Adicionar fmtDataHora (linha 13) e substituir call site created_at (linha 840)</name>

  <read_first>
    src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx
    — Ler antes de qualquer edição para ver o estado atual exato das linhas 12-14 e 838-842.
    — NUNCA assumir que o conteúdo é exatamente o do plano; confirmar via leitura.
  </read_first>

  <files>src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx</files>

  <action>
PASSO 1 — Inserir fmtDataHora imediatamente após fmtData (linha 13, antes da linha em branco).

A linha 13 atual é:
```
function fmtData(iso) { if (!iso) return "—"; const d = iso.slice(0, 10).split("-"); return `${d[2]}/${d[1]}/${d[0]}`; }
```

Após essa linha, inserir (na linha 14, empurrando o restante):
```js
function fmtDataHora(iso) {
  if (!iso) return "—";
  try {
    const d = new Date(iso);
    if (isNaN(d.getTime())) return "—";
    return d.toLocaleDateString("pt-BR", { timeZone: "America/Sao_Paulo" });
  } catch { return "—"; }
}
```

Notas de implementação (D-05):
- O guard `isNaN(d.getTime())` é obrigatório além do try/catch — `new Date(null)` retorna epoch sem lançar exceção.
- Retorna APENAS a data (sem hora) via toLocaleDateString — consistente com o resto da UI.
- NÃO alterar fmtData nem nenhuma de suas chamadas existentes (D-07).

PASSO 2 — Substituir o único call site de fmtData(evento.created_at).

Localizar a linha que contém `fmtData(evento.created_at)` (atualmente linha ~840, pode deslocar após inserção no passo 1).
Substituir:
```
{fmtData(evento.created_at)}
```
por:
```
{fmtDataHora(evento.created_at)}
```

Esta é a ÚNICA ocorrência de `fmtData(evento.created_at)` no arquivo — confirmar com grep antes de salvar.
Todas as demais chamadas a fmtData (data_pagamento, data_vencimento, data_emissao) NÃO devem ser alteradas (D-07).
  </action>

  <verify>
    <automated>
grep -n "function fmtDataHora" src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx
grep -n "fmtDataHora(evento.created_at)" src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx
grep -c "fmtData(evento.created_at)" src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx
    </automated>
  </verify>

  <acceptance_criteria>
    - DetalheContrato.jsx contém `function fmtDataHora` (grep retorna linha)
    - DetalheContrato.jsx contém `if (isNaN(d.getTime())) return "—";` dentro de fmtDataHora
    - DetalheContrato.jsx contém `timeZone: "America/Sao_Paulo"` dentro de fmtDataHora
    - DetalheContrato.jsx contém `fmtDataHora(evento.created_at)` (grep retorna linha)
    - `grep -c 'fmtData(evento.created_at)' DetalheContrato.jsx` retorna 0 (call site antigo removido)
    - fmtData na linha 13 permanece inalterada (grep confirma que `iso.slice(0, 10)` ainda existe na definição de fmtData)
  </acceptance_criteria>

  <done>fmtDataHora definida imediatamente após fmtData; único call site created_at usa fmtDataHora; fmtData e todos seus outros call sites intactos.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Adicionar isPagamento, corrigir diffEntries e inserir ramo JSX de pagamento</name>

  <read_first>
    src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx
    — Ler linhas 805-885 para ver o estado atual do bloco historico.map antes de editar.
    — Confirmar posição atual de isCriacao, diffEntries, bloco isCriacao JSX e bloco diffEntries JSX.
    — Confirmar que buildParcelasText existe na linha ~385 (não redefinir).
  </read_first>

  <files>src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx</files>

  <action>
Dentro do callback de `historico.map`, fazer três alterações cirúrgicas (D-01, D-02, D-03):

---

ALTERAÇÃO 1 — Adicionar const isPagamento logo após const isCriacao (D-01).

Localizar:
```js
const isCriacao = evento.tipo_evento === "criacao";
```
Adicionar imediatamente após:
```js
const isPagamento = ['pagamento_recebido', 'pagamento_revertido'].includes(evento.tipo_evento);
```

---

ALTERAÇÃO 2 — Modificar guarda ternária de diffEntries (D-02).

Localizar o bloco atual:
```js
const diffEntries = !isCriacao
  ? Object.entries(snap).map(([campo, val]) => ({
      campo,
      antes:  String(val?.antes  ?? ""),
      depois: String(val?.depois ?? ""),
    }))
  : [];
```
Substituir por:
```js
const diffEntries = !isCriacao && !isPagamento
  ? Object.entries(snap).map(([campo, val]) => ({
      campo,
      antes:  String(val?.antes  ?? ""),
      depois: String(val?.depois ?? ""),
    }))
  : [];
```
Única mudança: `!isCriacao` → `!isCriacao && !isPagamento`.

---

ALTERAÇÃO 3 — Inserir ramo JSX {isPagamento && ...} (D-03).

Localizar o bloco JSX de criacao que termina com:
```jsx
        )}

                    {!isCriacao && diffEntries.length > 0 && (
```
Entre o fechamento do bloco isCriacao e a abertura do bloco diffEntries, inserir:

```jsx
                    {isPagamento && (
                      <div style={{ fontSize: 13, color: "#374151", lineHeight: 1.7 }}>
                        <div>Valor: {fmtBRL(snap.valor)}</div>
                        <div>Data: {fmtData(snap.data_pagamento)}</div>
                        <div>
                          Parcelas: {buildParcelasText(snap.parcelas_ids, dividas)}
                          {" "}({snap.parcelas_amortizadas ?? snap.parcelas_ids?.length ?? 0} amortizada(s))
                        </div>
                      </div>
                    )}
```

Notas de implementação (D-03):
- `snap.data_pagamento` é coluna DATE → usar `fmtData` (correto, NÃO fmtDataHora).
- `snap.parcelas_amortizadas` existe só em pagamento_recebido, ausente em pagamento_revertido → fallback `?? snap.parcelas_ids?.length ?? 0`.
- `buildParcelasText(snap.parcelas_ids, dividas)` — reutilizar a função existente na linha ~385; NÃO redefinir.
- O bloco `{!isCriacao && diffEntries.length > 0 && ...}` (D-04) NÃO precisa de alteração — com a guarda !isPagamento em diffEntries (Alteração 2), diffEntries já será [] para pagamentos, então diffEntries.length > 0 será false automaticamente.

Ordem final dos três ramos JSX após as alterações:
1. `{isCriacao && (...)}`   — exibe dados de criação (inalterado)
2. `{isPagamento && (...)}` — exibe detalhes do pagamento (NOVO)
3. `{!isCriacao && diffEntries.length > 0 && (...)}` — exibe diff de edição (inalterado)
  </action>

  <verify>
    <automated>
grep -n "isPagamento" src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx
grep -n "&& !isPagamento" src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx
grep -n "isPagamento && (" src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx
grep -n "snap.data_pagamento" src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx
grep -n "snap.parcelas_amortizadas" src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx
    </automated>
  </verify>

  <acceptance_criteria>
    - DetalheContrato.jsx contém `const isPagamento = ['pagamento_recebido', 'pagamento_revertido'].includes(evento.tipo_evento);`
    - DetalheContrato.jsx contém `&& !isPagamento` na linha de diffEntries (grep retorna exatamente 1 linha)
    - DetalheContrato.jsx contém `{isPagamento && (` (grep retorna linha)
    - DetalheContrato.jsx contém `fmtBRL(snap.valor)` dentro do ramo isPagamento
    - DetalheContrato.jsx contém `fmtData(snap.data_pagamento)` dentro do ramo isPagamento
    - DetalheContrato.jsx contém `buildParcelasText(snap.parcelas_ids, dividas)` dentro do ramo isPagamento
    - DetalheContrato.jsx contém `snap.parcelas_amortizadas ?? snap.parcelas_ids?.length ?? 0`
    - `grep -n 'isPagamento' DetalheContrato.jsx` retorna pelo menos 3 linhas (declaração + guarda diffEntries + ramo JSX)
  </acceptance_criteria>

  <done>isPagamento declarado; diffEntries guarda inclui !isPagamento; ramo JSX {isPagamento && ...} inserido entre os blocos isCriacao e diffEntries; buildParcelasText reutilizado sem redefinição.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Executar regressão e commitar no submodule mr-3</name>

  <read_first>
    src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx
    — Leitura rápida das linhas modificadas para confirmar integridade antes de rodar testes.
  </read_first>

  <files>src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx</files>

  <action>
PASSO 1 — Executar suíte de regressão (D-10).

```bash
cd src/mr-3/mr-cobrancas && npm run test:regressao
```

Resultado esperado: 9/9 testes passando, exit code 0.
Se algum teste falhar, NÃO commitar — inspecionar o output e corrigir antes de prosseguir.

PASSO 2 — Commitar no submodule mr-3 (D-09).

```bash
cd src/mr-3/mr-cobrancas
git add src/components/DetalheContrato.jsx
git commit -m "fix(07.1): corrigir renderização pagamentos no histórico e timezone created_at"
```

O commit message deve descrever os dois fixes:
- Bug A: ramo isPagamento + guarda diffEntries
- Bug B: fmtDataHora para created_at

PASSO 3 — Bump do submodule no repositório pai (D-09).

```bash
cd ../../../
git add src/mr-3
git commit -m "chore(07.1): bump submodule mr-3 (fix histórico pagamentos + timezone)"
```

CRITICAL (D-09): NÃO executar `git push` em nenhum dos dois repositórios. Push somente após autorização explícita do usuário.
  </action>

  <verify>
    <automated>
cd src/mr-3/mr-cobrancas && npm run test:regressao
    </automated>
  </verify>

  <acceptance_criteria>
    - `npm run test:regressao` sai com código 0 e reporta 9/9 testes passando
    - `git log --oneline -1` no submodule mr-3 mostra commit com mensagem "fix(07.1):"
    - `git log --oneline -1` no repositório pai mostra commit com mensagem "chore(07.1):"
    - `git status` no repositório pai não mostra arquivos staged pendentes
    - `git remote -v` não foi executado para push (push bloqueado por D-09)
  </acceptance_criteria>

  <done>Regressão 9/9 verde; commit no mr-3; bump no pai; nenhum push executado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend → Frontend props | `evento.snapshot_campos` (JSONB) vem do Supabase via listarHistorico — dado já autenticado e imutável neste componente |
| Frontend render | DetalheContrato.jsx renderiza snapshot_campos para usuário autenticado — view only, sem novos inputs do usuário |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.1-01 | Information Disclosure | fmtDataHora — exposição de fuso horário do servidor | accept | A função usa `toLocaleDateString` no browser do cliente, não expõe dados do servidor; fuso é de apresentação |
| T-07.1-02 | Tampering | snap.valor / snap.parcelas_ids renderizados diretamente | accept | Dados vêm de contratos_historico (insert feito pela SP autenticada); componente é read-only para este snapshot; sem novo surface de input |
| T-07.1-03 | Denial of Service | snap.parcelas_ids array longo via buildParcelasText | accept | buildParcelasText já existe e é usado na seção Pagamentos Recebidos; sem novo risco introduzido |
| T-07.1-04 | Spoofing | N/A — nenhum novo fluxo de autenticação ou autorização | accept | Fix puramente presentacional em view autenticada; nenhuma boundary de auth afetada |

**Resumo de segurança:** Esta phase não introduz novos attack surfaces. Todas as alterações são lógica de apresentação em um componente já autenticado. Nenhum input novo do usuário é processado. ASVS L1 não requer mitigação adicional para changes puramente de display dentro de view autenticada.
</threat_model>

<verification>
Verificações completas após execução dos três tasks:

```bash
# 1. fmtDataHora definida
grep -n "function fmtDataHora" src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx
# Esperado: retorna linha com "function fmtDataHora"

# 2. Call site substituído
grep -n "fmtDataHora(evento.created_at)" src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx
# Esperado: retorna 1 linha

# 3. Call site antigo removido
grep -c "fmtData(evento.created_at)" src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx
# Esperado: 0

# 4. isPagamento declarado e usado
grep -n "isPagamento" src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx
# Esperado: >= 3 linhas (declaração, guarda diffEntries, ramo JSX)

# 5. Guarda diffEntries corrigida
grep -n "&& !isPagamento" src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx
# Esperado: 1 linha

# 6. Regressão verde
cd src/mr-3/mr-cobrancas && npm run test:regressao
# Esperado: exit 0, 9/9 testes passando
```
</verification>

<success_criteria>
- `function fmtDataHora` existe em DetalheContrato.jsx, imediatamente após fmtData
- `fmtDataHora` contém guard `isNaN(d.getTime())` e `timeZone: "America/Sao_Paulo"`
- `fmtDataHora(evento.created_at)` substitui `fmtData(evento.created_at)` no único call site do historico
- `fmtData` permanece inalterada; todos seus demais call sites intactos
- `const isPagamento = ['pagamento_recebido', 'pagamento_revertido'].includes(evento.tipo_evento)` declarado após isCriacao
- `diffEntries` calcula com guarda `!isCriacao && !isPagamento`
- Ramo JSX `{isPagamento && (...)}` inserido entre blocos isCriacao e diffEntries, exibindo Valor (fmtBRL), Data (fmtData) e Parcelas (buildParcelasText)
- `npm run test:regressao` → 9/9 testes passando
- Commit no submodule mr-3 realizado; bump no repositório pai realizado; zero pushes
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/07.1-fix-historico-pagamentos/07.1-01-SUMMARY.md` com:
- Alterações realizadas (4 mudanças cirúrgicas em DetalheContrato.jsx)
- Resultado dos testes (9/9)
- Hash do commit no mr-3 e hash do bump no pai
- Confirmação de que nenhum push foi executado
</output>
