---
plan: 07.14-01
phase: 7.14
title: Soft delete devedor (deleted_at TIMESTAMPTZ + 3 callsites filtro + 1 exceção forense)
wave: 1
depends_on: []
autonomous: false
files_modified:
  - src/mr-3/mr-cobrancas/src/services/migrations/003_devedores_soft_delete.sql
  - src/mr-3/mr-cobrancas/src/App.jsx
  - src/mr-3/mr-cobrancas/src/services/filaDevedor.js
  - src/mr-3/mr-cobrancas/src/services/filaDevedor.test.js
  - src/mr-3/mr-cobrancas/src/services/processosJudiciais.js
  - src/mr-3/mr-cobrancas/src/components/ProcessosJudiciais.jsx
requirements: []
---

# Plan 07.14-01: Soft delete devedor

<objective>
Substitui DELETE físico de `devedores` por soft delete via coluna `deleted_at TIMESTAMPTZ`, bypassando o bug bloqueante de produção `eventos_andamento_devedor_id_fkey` (FK sem CASCADE) sem precisar fixar 12+ FKs uma a uma. Aplica filtro `deleted_at=is.null` em 3 callsites (D-pre-9), preserva exceção forense em ProcessosJudiciais com badge "Inativo" cinza (D-pre-11), e mantém o motor Art.354 (`devedorCalc.js`, `services/pagamentos.js`, `services/dividas.js`) **INTOCADO** sob gate dual `git log --name-only` (D-01 cumulativo).
</objective>

<must_haves>
- Migration 003 SQL existe em `src/mr-3/mr-cobrancas/src/services/migrations/003_devedores_soft_delete.sql` com `ALTER TABLE devedores ADD COLUMN IF NOT EXISTS deleted_at TIMESTAMPTZ`, índice parcial `devedores_deleted_at_idx WHERE deleted_at IS NULL`, e `pg_notify('pgrst', 'reload schema')`
- Handler `excluirDevedor` em `App.jsx:3497` chama `dbUpdate("devedores", d.id, { deleted_at: new Date().toISOString() })` em vez de `dbDelete("devedores", d.id)` — audit log e state filter inalterados
- 3 callsites de leitura de `devedores` recebem filtro `deleted_at=is.null`: (1) `App.jsx:8392` carga global `dbGet("devedores")`; (2) `services/filaDevedor.js:586` query `id=in.(...)`; (3) `services/filaDevedor.test.js:9` test fixture
- 1 exceção forense documentada inline em `services/processosJudiciais.js:71` (JOIN aninhado `devedor:devedores(...)`) — D-pre-11 preserva devedor soft-deleted no polo passivo histórico, comentário cita ID da decisão
- Componente `ProcessosJudiciais.jsx` renderiza badge cinza "Inativo" no chip do polo passivo quando `dev?.deleted_at` truthy, ao lado do `<BadgePapel>` em `~line 367`
- D-01 motor Art.354 INTOCADO cumulativo: `devedorCalc.js`, `services/pagamentos.js`, `services/dividas.js` zero modificações nesta phase (gate `git log -1 --name-only` empty)
- Smoke gate verifica que migration 003 SQL contém todas as 5 strings críticas + nenhum motor file aparece no diff cumulativo desta phase
- Build (`npm run build`) e testes (`npm test`) PASSAM após cada commit atômico (existing 34 tests verde — incluindo `filaDevedor.test.js` ajustado)
</must_haves>

---

<task id="01-01">
<title>Migration 003 — devedores.deleted_at TIMESTAMPTZ + partial index + pg_notify</title>
<read_first>
- src/mr-3/mr-cobrancas/src/services/migrations/001_devedores_dividas.sql (RLS pattern e header style — seções comentadas)
- src/mr-3/mr-cobrancas/src/services/migrations/002_dividas_tabela.sql (RLS pattern e `pg_notify('pgrst', 'reload schema')` final)
- .planning/phases/07.14-blindagens-de-integridade-de-dados/07.14-CONTEXT.md (D-pre-4 SQL block exato + D-pre-1 escopo MÍNIMO)
</read_first>
<action>
Crie o arquivo `src/mr-3/mr-cobrancas/src/services/migrations/003_devedores_soft_delete.sql` com o conteúdo EXATO abaixo (copiar literal, sem alterar):

```sql
-- ────────────────────────────────────────────────────
-- Migration 003: devedores soft delete
-- Adds deleted_at TIMESTAMPTZ NULL to devedores so that
-- DELETE devedor is replaced by UPDATE deleted_at = NOW().
-- Bypasses bug bloqueante de produção:
--   eventos_andamento_devedor_id_fkey (FK NOT CASCADE)
--   blocking DELETE devedores em uso real.
-- Soft delete contorna 12+ FKs sem precisar fixar uma a uma.
--
-- D-pre-1 (escopo mínimo): SOMENTE devedores. Credores/contratos/
-- processos NÃO entram nesta phase.
-- D-pre-4 (schema TIMESTAMPTZ): NÃO é boolean ativo — TIMESTAMPTZ
-- preserva informação temporal e permite restauração via SQL
-- (UPDATE devedores SET deleted_at=NULL WHERE id=X).
-- D-pre-5 (sem UI restauração): restauração via SQL apenas — YAGNI.
-- D-pre-9 (strategy b): 3 callsites recebem filtro deleted_at=is.null
-- + 1 exceção forense (services/processosJudiciais.js:71 D-pre-11).
--
-- Run this MANUALLY in Supabase SQL Editor (mesmo padrão das
-- Migrations 001/002 — não há framework npm de migration neste projeto).
-- ────────────────────────────────────────────────────

ALTER TABLE devedores ADD COLUMN IF NOT EXISTS deleted_at TIMESTAMPTZ;

CREATE INDEX IF NOT EXISTS devedores_deleted_at_idx
  ON devedores (deleted_at)
  WHERE deleted_at IS NULL;

SELECT pg_notify('pgrst', 'reload schema');
```

Nota: este arquivo é executado MANUALMENTE pelo usuário no Supabase SQL Editor — não há script `npm run migrate`. Deixe explícito no header (já incluído acima) para auditoria futura.

NÃO toque em mais nada nesta task. Não rode SQL — apenas crie o arquivo. A execução do SQL é responsabilidade do usuário durante UAT (será gate da Task 01-08 smoke).
</action>
<acceptance_criteria>
- File `src/mr-3/mr-cobrancas/src/services/migrations/003_devedores_soft_delete.sql` exists
- File contains literal string `ALTER TABLE devedores ADD COLUMN IF NOT EXISTS deleted_at TIMESTAMPTZ`
- File contains literal string `CREATE INDEX IF NOT EXISTS devedores_deleted_at_idx`
- File contains literal string `WHERE deleted_at IS NULL`
- File contains literal string `pg_notify('pgrst', 'reload schema')`
- File header references D-pre-1, D-pre-4, D-pre-5, D-pre-9 (audit trail)
- Verify via grep: `grep -c "ALTER TABLE devedores ADD COLUMN IF NOT EXISTS deleted_at TIMESTAMPTZ" src/mr-3/mr-cobrancas/src/services/migrations/003_devedores_soft_delete.sql` returns 1
- Verify via grep: `grep -c "deleted_at_idx" src/mr-3/mr-cobrancas/src/services/migrations/003_devedores_soft_delete.sql` returns at least 1
- Verify via grep: `grep -c "pg_notify('pgrst'" src/mr-3/mr-cobrancas/src/services/migrations/003_devedores_soft_delete.sql` returns 1
- `git status --short src/mr-3/mr-cobrancas/src/services/migrations/003_devedores_soft_delete.sql` shows new file
</acceptance_criteria>
</task>

---

<task id="01-02">
<title>Convert excluirDevedor handler — dbDelete → dbUpdate({ deleted_at })</title>
<read_first>
- src/mr-3/mr-cobrancas/src/App.jsx (read lines 3490-3515 for full handler context — `excluirDevedor` at L3497, `logAudit` call at L3505, state filter at L3506, `fecharModal()` at L3507)
- src/mr-3/mr-cobrancas/src/config/supabase.js (verify `dbUpdate` signature — should be `dbUpdate(table, id, patch)`)
- .planning/phases/07.14-blindagens-de-integridade-de-dados/07.14-CONTEXT.md (D-pre-1 escopo, "Callsite UI excluir devedor" seção)
</read_first>
<action>
Em `src/mr-3/mr-cobrancas/src/App.jsx`, localize a função `excluirDevedor` (linha 3497). O bloco atual é:

```js
async function excluirDevedor(d) {
  if (!await confirm(`Excluir "${d.nome}"?`)) return;
  try {
    await dbDelete("devedores", d.id);
  } catch (e) {
    toast.error("Erro ao excluir devedor: " + (e?.message || e));
    return;
  }
  logAudit("Excluiu devedor", "devedores", { id: d.id, nome: d.nome, cpf_cnpj: d.cpf_cnpj });
  setDevedores(prev => prev.filter(x => x.id !== d.id));
  fecharModal();
}
```

Substitua APENAS a linha `await dbDelete("devedores", d.id);` por:

```js
    await dbUpdate("devedores", d.id, { deleted_at: new Date().toISOString() });
```

(Mantenha indentação de 4 espaços dentro do `try`.)

Mantenha INTOCADOS:
- Texto do `confirm()` em L3498 (`Excluir "${d.nome}"?`) — NÃO alterar (D-pre-12 spirit: light touch, mensagem opcional, low priority — pular para evitar surface extra)
- `toast.error(...)` no catch
- `logAudit("Excluiu devedor", ...)` (string "Excluiu devedor" preservada — audit history integridade)
- `setDevedores(prev => prev.filter(x => x.id !== d.id))` (state filter ainda some o devedor da listagem local — devedor recarregado virá filtrado por D-pre-9 callsite 1 na próxima `carregarTudo`)
- `fecharModal()`

Verifique que `dbUpdate` já está importado no topo do arquivo (App.jsx). Se não estiver, ADICIONE-o ao import existente de `./config/supabase` (não crie novo import). Comum: `import { dbGet, dbInsert, dbUpdate, dbDelete } from "./config/supabase.js"` — caso `dbUpdate` falte, adicione na lista nesse mesmo import.

NÃO REMOVA `dbDelete` do import — pode estar sendo usado em outros handlers (excluir contrato, excluir documento). Apenas adicione `dbUpdate` se faltar.
</action>
<acceptance_criteria>
- `grep -n 'dbUpdate("devedores", d.id, { deleted_at: new Date().toISOString() })' src/mr-3/mr-cobrancas/src/App.jsx` returns at least 1 match
- `grep -n 'dbDelete("devedores", d.id)' src/mr-3/mr-cobrancas/src/App.jsx` returns 0 matches (old call removed)
- `grep -nE '^\s*async function excluirDevedor\(d\)' src/mr-3/mr-cobrancas/src/App.jsx` returns 1 match (function still exists, name preserved)
- `grep -n 'logAudit("Excluiu devedor"' src/mr-3/mr-cobrancas/src/App.jsx` returns at least 1 match (audit string preserved)
- `grep -n 'setDevedores(prev => prev.filter(x => x.id !== d.id))' src/mr-3/mr-cobrancas/src/App.jsx` returns at least 1 match (state filter preserved)
- `grep -nE '^import .* dbUpdate.* from ["'\'']\./config/supabase' src/mr-3/mr-cobrancas/src/App.jsx` returns at least 1 match (dbUpdate imported, strict import per `feedback_grep_shield_strict_imports`)
- `npm run build` PASSES (no syntax errors)
</acceptance_criteria>
</task>

---

<task id="01-03">
<title>Filtro callsite 1 — App.jsx:8392 carga global dbGet("devedores")</title>
<read_first>
- src/mr-3/mr-cobrancas/src/App.jsx (lines 8385-8410 — bloco `carregarTudo` Promise.all que faz fetch de devedores junto com credores/processos/etc; `dbGet("devedores")` em L8392)
- .planning/phases/07.14-blindagens-de-integridade-de-dados/07.14-CONTEXT.md (D-pre-9 callsite 1)
</read_first>
<action>
Em `src/mr-3/mr-cobrancas/src/App.jsx` linha 8392, substitua:

```js
        dbGet("devedores"),
```

por:

```js
        dbGet("devedores", "deleted_at=is.null"),
```

(Mantenha indentação atual — 8 espaços de leading whitespace.)

NÃO altere outras chamadas `dbGet` adjacentes (credores L8393, processos L8394, andamentos L8395, regua_cobranca L8396, lembretes L8397, pagamentos_parciais L8398, dividas L8399 — todas inalteradas). Apenas a linha do `devedores`.

Não toque nas junction queries `devedores_dividas` (App.jsx:8403, App.jsx:2983, App.jsx:2990) — D-pre-9 explicita: "Junction queries não recebem filtro direto — herdam consistência via Set lookup pattern (D-27)".

NOTA: Variável de retorno `devs` (L8391 destructure) carregará apenas devedores com `deleted_at IS NULL`. State global `setDevedores(devs)` herda esse filtro automaticamente. Listagens consumidoras (Pessoas, ModuloContratos, Devedores tabela) ficam consistentes sem outras alterações.
</action>
<acceptance_criteria>
- `grep -nE 'dbGet\("devedores", *"deleted_at=is\.null"\)' src/mr-3/mr-cobrancas/src/App.jsx` returns at least 1 match
- `grep -cE '^\s*dbGet\("devedores"\),\s*$' src/mr-3/mr-cobrancas/src/App.jsx` returns 0 (old unfiltered call removed)
- Adjacent calls preserved: `grep -n 'dbGet("credores")' src/mr-3/mr-cobrancas/src/App.jsx` returns at least 1 match (credores intocado)
- Adjacent calls preserved: `grep -n 'dbGet("processos")' src/mr-3/mr-cobrancas/src/App.jsx` returns at least 1 match (processos intocado)
- Adjacent calls preserved: `grep -n 'dbGet("dividas")' src/mr-3/mr-cobrancas/src/App.jsx` returns at least 1 match (dividas intocado)
- `npm run build` PASSES
</acceptance_criteria>
</task>

---

<task id="01-04">
<title>Filtro callsite 2 — services/filaDevedor.js:586 query id=in.(...)</title>
<read_first>
- src/mr-3/mr-cobrancas/src/services/filaDevedor.js (lines 580-595 — bloco `Promise.all` que faz fetch parcial de contratos e devedores por id; query devedores em L586)
- .planning/phases/07.14-blindagens-de-integridade-de-dados/07.14-CONTEXT.md (D-pre-9 callsite 3)
</read_first>
<action>
Em `src/mr-3/mr-cobrancas/src/services/filaDevedor.js` linha 586, substitua:

```js
      devedorIds.length > 0 ? dbGet("devedores", `id=in.(${devedorIds.join(",")})`) : Promise.resolve([]),
```

por:

```js
      devedorIds.length > 0 ? dbGet("devedores", `id=in.(${devedorIds.join(",")})&deleted_at=is.null`) : Promise.resolve([]),
```

(Note o `&deleted_at=is.null` ANTES do template literal fechar. Sintaxe PostgREST: múltiplos filtros concatenados com `&`.)

NÃO altere a chamada adjacente de contratos em L585 (`dbGet("contratos", \`id=in.(${contratoIds.join(",")})\`)`) — D-pre-1 escopo MÍNIMO restringe filtros a `devedores` apenas.

NOTA: o resultado `devedorMap` (L590) não conterá entries para ids soft-deleted, e `enriched` (L592) terá `devedor: null` nesses casos — comportamento consistente com filtro carga global D-pre-9 callsite 1. Caller (provavelmente UI Fila Devedor) já trata `devedor === null` ou cai no fallback `#${devedor_id}`.
</action>
<acceptance_criteria>
- `grep -nE 'id=in\.\(\$\{devedorIds\.join\(","\)\}\)&deleted_at=is\.null' src/mr-3/mr-cobrancas/src/services/filaDevedor.js` returns at least 1 match
- `grep -cE 'dbGet\("devedores", *\`id=in\.\(\$\{devedorIds\.join\(","\)\}\)\`\)' src/mr-3/mr-cobrancas/src/services/filaDevedor.js` returns 0 (old unfiltered query removed)
- Contratos query preserved: `grep -n 'id=in.(${contratoIds.join(","' src/mr-3/mr-cobrancas/src/services/filaDevedor.js` returns at least 1 match without `deleted_at` (escopo D-pre-1: somente devedores filtrados)
- `npm run build` PASSES
</acceptance_criteria>
</task>

---

<task id="01-05">
<title>Filtro callsite 3 — services/filaDevedor.test.js:9 test fixture</title>
<read_first>
- src/mr-3/mr-cobrancas/src/services/filaDevedor.test.js (lines 1-30 — função setup() carrega devedor existente para lookup; query em L9 `dbGet("devedores", "select=id&limit=1")`)
- .planning/phases/07.14-blindagens-de-integridade-de-dados/07.14-CONTEXT.md (D-pre-9 callsite 4)
</read_first>
<action>
Em `src/mr-3/mr-cobrancas/src/services/filaDevedor.test.js` linha 9, substitua:

```js
  const devedores = await dbGet("devedores", "select=id&limit=1");
```

por:

```js
  const devedores = await dbGet("devedores", "select=id&limit=1&deleted_at=is.null");
```

(Sintaxe PostgREST: filtros concatenados com `&`, sem reorganizar a ordem dos parâmetros existentes.)

Mantenha INTOCADO o resto do arquivo:
- Comentário acima em L8 (`// 1. Buscar um devedor existente para lookup (nao criar — tabela legada BIGINT)`) — preservado
- Validação `if (!devedores.length) throw new Error("Nenhum devedor encontrado para teste");` em L10 — preservada (se zero devedores ativos em prod, test ainda falha apropriadamente)
- `testIds.devedor = devedores[0].id;` em L11 — preservada
- Resto do setup (operador, contrato, parcelas) — INTOCADO (foco em devedores apenas, D-pre-1)

NOTA: o teste assume que existe pelo menos 1 devedor com `deleted_at IS NULL` em prod. Investigação SQL prod confirma 6 devedores total (banco minúsculo, todos ativos hoje, zero soft-deleted antes desta phase) — pré-condição satisfeita.
</action>
<acceptance_criteria>
- `grep -nE 'dbGet\("devedores", "select=id&limit=1&deleted_at=is\.null"\)' src/mr-3/mr-cobrancas/src/services/filaDevedor.test.js` returns at least 1 match
- `grep -cE 'dbGet\("devedores", *"select=id&limit=1"\)' src/mr-3/mr-cobrancas/src/services/filaDevedor.test.js` returns 0 (old fixture call removed)
- Other test setup preserved: `grep -n 'dbInsert("operadores"' src/mr-3/mr-cobrancas/src/services/filaDevedor.test.js` returns at least 1 match (operador setup intocado)
- Other test setup preserved: `grep -n 'numero_contrato: "TEST-FILA-"' src/mr-3/mr-cobrancas/src/services/filaDevedor.test.js` returns at least 1 match (contrato setup intocado)
- `npm test` PASSES (existing 34 tests still green; if filaDevedor test runs against prod DB and passes, fixture filter works)
</acceptance_criteria>
</task>

---

<task id="01-06">
<title>Exceção forense — comentário inline services/processosJudiciais.js:71 (NÃO filtrar)</title>
<read_first>
- src/mr-3/mr-cobrancas/src/services/processosJudiciais.js (lines 60-85 — bloco contendo `listarDevedores` em L69 com JOIN aninhado `devedor:devedores(...)` em L71)
- .planning/phases/07.14-blindagens-de-integridade-de-dados/07.14-CONTEXT.md (D-pre-11 exceção forense detalhada)
</read_first>
<action>
Em `src/mr-3/mr-cobrancas/src/services/processosJudiciais.js`, localize a função `listarDevedores` (linha 69). O bloco atual é:

```js
export async function listarDevedores(processoId) {
  return sb(
    `${TABLE_DEV}?processo_id=eq.${processoId}&select=*,devedor:devedores(id,nome,cpf_cnpj,telefone,email)&order=created_at.asc`
  );
}
```

**NÃO ADICIONE FILTRO `deleted_at=is.null` no JOIN aninhado** — esta é a única exceção forense do projeto (D-pre-11). Processos judiciais já cadastrados preservam devedor soft-deleted no polo passivo (advogado pode reabrir caso, defender ação revisional, calcular prescrição, gerar petições retroativas).

A única alteração desta task é ADICIONAR um comentário inline ACIMA do JOIN string, documentando a exceção. Substitua o bloco atual por:

```js
export async function listarDevedores(processoId) {
  // D-pre-11 (Phase 7.14): exceção forense — JOIN aninhado devedor:devedores(...)
  // intencionalmente SEM filtro deleted_at=is.null. Processo judicial já cadastrado
  // preserva devedor soft-deleted no polo passivo (histórico forense para reabertura,
  // ação revisional, prescrição). UI ProcessosJudiciais.jsx exibe badge "Inativo" cinza
  // no chip quando dev.deleted_at é truthy. Criação NOVA de processo aplica filtro
  // normal via lookup global (App.jsx:8392 D-pre-9 callsite 1) — não permite adicionar
  // inativo como novo réu.
  return sb(
    `${TABLE_DEV}?processo_id=eq.${processoId}&select=*,devedor:devedores(id,nome,cpf_cnpj,telefone,email)&order=created_at.asc`
  );
}
```

Mantenha INTOCADAS as outras funções do arquivo:
- `vincularDevedor` (L~50)
- `removerDevedor` (L65)
- `atualizarCitacao` (L75)
- `listarPorDevedor` (L83)

Apenas `listarDevedores` (L69) recebe o comentário. A query string em si fica IDÊNTICA — zero filtro, zero alteração de PostgREST syntax.
</action>
<acceptance_criteria>
- `grep -nE '^\s*// D-pre-11 \(Phase 7\.14\): exceção forense' src/mr-3/mr-cobrancas/src/services/processosJudiciais.js` returns at least 1 match
- `grep -n 'devedor:devedores(id,nome,cpf_cnpj,telefone,email)' src/mr-3/mr-cobrancas/src/services/processosJudiciais.js` returns at least 1 match (JOIN string preservada)
- `grep -nE 'devedor:devedores\(.*\).*deleted_at=is\.null' src/mr-3/mr-cobrancas/src/services/processosJudiciais.js` returns 0 matches (filtro NÃO foi adicionado por engano)
- Comentário menciona "D-pre-11", "exceção forense", "badge", "Inativo": `grep -c "D-pre-11" src/mr-3/mr-cobrancas/src/services/processosJudiciais.js` returns at least 1
- `grep -c "Inativo" src/mr-3/mr-cobrancas/src/services/processosJudiciais.js` returns at least 1
- Other functions preserved: `grep -n 'export async function removerDevedor' src/mr-3/mr-cobrancas/src/services/processosJudiciais.js` returns at least 1 match
- `npm run build` PASSES
</acceptance_criteria>
</task>

---

<task id="01-07">
<title>UI badge "Inativo" — ProcessosJudiciais.jsx polo passivo render</title>
<read_first>
- src/mr-3/mr-cobrancas/src/components/ProcessosJudiciais.jsx (lines 358-420 — bloco `devedoresProcesso.map(row => { ... })`; render do chip do devedor com `<BadgePapel papel={row.papel} />` em L367; variável `dev = row.devedor || {}` em L359)
- .planning/phases/07.14-blindagens-de-integridade-de-dados/07.14-CONTEXT.md (D-pre-11 exato style do badge)
</read_first>
<action>
Em `src/mr-3/mr-cobrancas/src/components/ProcessosJudiciais.jsx`, localize o bloco do polo passivo render (linha 358 abre `devedoresProcesso.map(row => { ... })`, linha 359 define `const dev = row.devedor || {};`, linha 367 renderiza `<BadgePapel papel={row.papel} />`).

Adicione um badge cinza "Inativo" IMEDIATAMENTE APÓS `<BadgePapel papel={row.papel} />` em L367, condicionalmente quando `dev.deleted_at` é truthy.

O bloco atual em ~L367 é:

```jsx
                    <BadgePapel papel={row.papel} />
```

Substitua por:

```jsx
                    <BadgePapel papel={row.papel} />
                    {dev.deleted_at && (
                      <span style={{ background: "#f1f5f9", color: "#64748b", fontSize: 10, fontWeight: 700, padding: "2px 8px", borderRadius: 99 }}>
                        Inativo
                      </span>
                    )}
```

(Mantenha indentação atual — alinhada com `<BadgePapel>` no JSX, 20 espaços leading whitespace conforme padrão circundante.)

Estilos EXATOS conforme D-pre-11:
- `background: "#f1f5f9"` (cinza claro)
- `color: "#64748b"` (cinza escuro slate-500)
- `fontSize: 10`
- `fontWeight: 700`
- `padding: "2px 8px"`
- `borderRadius: 99` (pill shape consistente com `<BadgePapel>`)

Não altere o resto do bloco (status_citacao select, data_citacao input, botões Ver/📞/WA/✕, fallback `dev.nome || \`Devedor #${row.devedor_id}\``). O `dev` já vem do `row.devedor || {}` em L359 — `dev.deleted_at` será `undefined` (falsy) quando devedor está ativo (campo NULL na DB) ou quando `row.devedor` está vazio fallback `{}`. Render é seguro nos dois casos.

NÃO toque na aba "Dívidas" (linha 425+, `dividasData.map`) — escopo D-pre-11 menciona apenas chip do polo passivo (aba "Polo Passivo"). Aba "Dívidas" é tabela compacta sem chip render — adicionar badge ali expande surface.
</action>
<acceptance_criteria>
- `grep -n 'dev.deleted_at &&' src/mr-3/mr-cobrancas/src/components/ProcessosJudiciais.jsx` returns at least 1 match
- `grep -nF 'background: "#f1f5f9"' src/mr-3/mr-cobrancas/src/components/ProcessosJudiciais.jsx` returns at least 1 match
- `grep -nF 'color: "#64748b"' src/mr-3/mr-cobrancas/src/components/ProcessosJudiciais.jsx` returns at least 1 match
- `grep -nE '>\s*Inativo\s*<' src/mr-3/mr-cobrancas/src/components/ProcessosJudiciais.jsx` returns at least 1 match
- `grep -nF 'borderRadius: 99' src/mr-3/mr-cobrancas/src/components/ProcessosJudiciais.jsx` returns at least 1 match
- BadgePapel rendering preserved: `grep -n '<BadgePapel papel={row.papel} />' src/mr-3/mr-cobrancas/src/components/ProcessosJudiciais.jsx` returns at least 1 match (não foi removido — badge "Inativo" é ADICIONAL, não substituto)
- `npm run build` PASSES (no JSX syntax errors)
</acceptance_criteria>
</task>

---

<task id="01-08">
<title>D-01 motor INTOCADO shield + smoke gate cumulativo (Phase 7.14)</title>
<read_first>
- .planning/phases/07.14-blindagens-de-integridade-de-dados/07.14-CONTEXT.md (Invariantes herdadas: D-01 motor Art.354 INTOCADO + lição `gate_motor_grep_use_name_only`)
- src/mr-3/mr-cobrancas/src/utils/devedorCalc.js (verify file exists — não ler conteúdo, apenas confirmar existência)
- src/mr-3/mr-cobrancas/src/services/pagamentos.js (verify file exists)
- src/mr-3/mr-cobrancas/src/services/dividas.js (verify file exists)
</read_first>
<action>
Esta é uma **gate task** — não modifica código. Executa shields automatizados de invariantes.

**Pré-condição:** todos os commits desta phase 7.14-01 (Tasks 01-01 a 01-07) já foram criados (cada task com seu commit atômico, ou consolidados conforme decisão executor). O range de comparação é o baseline pré-7.14 contra HEAD.

Execute os 3 shields abaixo e exija que os 3 retornem PASS:

**Shield 1 — D-01 motor INTOCADO (`--name-only`, lição `gate_motor_grep_use_name_only`):**

```bash
cd src/mr-3/mr-cobrancas
git log --name-only --pretty=format: <BASELINE>..HEAD -- src/utils/devedorCalc.js src/services/pagamentos.js src/services/dividas.js
```

Onde `<BASELINE>` = SHA do último commit do submódulo antes de iniciar Phase 7.14 (registrar no SUMMARY desta task; o último estado conhecido pré-7.14 é o submódulo `b6fbe20` referenciado em STATE.md).

**Resultado esperado: saída VAZIA** (zero linhas, zero arquivos motor modificados nesta phase).

Se a saída listar QUALQUER um dos 3 arquivos motor, FAIL — investigar e reverter mudança não-autorizada antes de prosseguir.

**Shield 2 — Migration 003 SQL contém todas as 5 strings críticas:**

```bash
grep -c "ALTER TABLE devedores ADD COLUMN IF NOT EXISTS deleted_at TIMESTAMPTZ" src/services/migrations/003_devedores_soft_delete.sql
grep -c "CREATE INDEX IF NOT EXISTS devedores_deleted_at_idx" src/services/migrations/003_devedores_soft_delete.sql
grep -c "WHERE deleted_at IS NULL" src/services/migrations/003_devedores_soft_delete.sql
grep -c "pg_notify('pgrst'" src/services/migrations/003_devedores_soft_delete.sql
grep -c "D-pre-1" src/services/migrations/003_devedores_soft_delete.sql
```

**Resultado esperado:** todos os 5 retornam `>= 1`. Se algum retorna `0`, FAIL — re-rodar Task 01-01.

**Shield 3 — Filtro `deleted_at=is.null` aplicado em 3 callsites + exceção D-pre-11 documentada:**

```bash
# Callsite 1 (App.jsx carga global)
grep -cE 'dbGet\("devedores", *"deleted_at=is\.null"\)' src/App.jsx

# Callsite 2 (filaDevedor.js id=in)
grep -cE 'id=in\.\(\$\{devedorIds\.join\(","\)\}\)&deleted_at=is\.null' src/services/filaDevedor.js

# Callsite 3 (filaDevedor.test.js fixture)
grep -cE 'dbGet\("devedores", "select=id&limit=1&deleted_at=is\.null"\)' src/services/filaDevedor.test.js

# Exceção D-pre-11 documentada inline
grep -c "D-pre-11" src/services/processosJudiciais.js
```

**Resultado esperado:** todos os 4 retornam `>= 1`.

Adicional shield 4 (anti-vazamento exceção forense — D-pre-11 NÃO deve adicionar filtro):

```bash
grep -cE 'devedor:devedores\([^)]*\).*deleted_at=is\.null' src/services/processosJudiciais.js
```

**Resultado esperado: `0`** (filtro NÃO foi adicionado por engano no JOIN forense).

**Shield 5 — Build + tests verde:**

```bash
npm run build
npm test
```

**Resultado esperado:** ambos PASSAM. Build sem erros JSX/syntax. 34/34 tests verde.

Se TODOS os 5 shields PASSAM, esta task está completa. Se qualquer shield FALHA, NÃO prossiga — corrigir e re-rodar antes do commit final.

NÃO modifique código motor (`devedorCalc.js`, `pagamentos.js`, `dividas.js`) nesta task. Esta task é puramente de verificação automatizada.
</action>
<acceptance_criteria>
- `cd src/mr-3/mr-cobrancas && git log --name-only --pretty=format: <BASELINE>..HEAD -- src/utils/devedorCalc.js src/services/pagamentos.js src/services/dividas.js` returns empty output (zero motor files modified cumulatively in 7.14-01)
- All 5 grep counts in Shield 2 return >= 1 (migration 003 has all critical strings + D-pre-1 reference)
- All 4 grep counts in Shield 3 return >= 1 (3 filtros + 1 comentário forense)
- Shield 4 grep returns 0 (filtro NOT adicionado no JOIN forense por engano)
- `cd src/mr-3/mr-cobrancas && npm run build` exits 0 (PASSES)
- `cd src/mr-3/mr-cobrancas && npm test` exits 0 (PASSES — 34/34 tests verde)
- SUMMARY documenta: BASELINE SHA usado, output dos 5 shields, screenshot ou log inline da PASS de cada um
</acceptance_criteria>
</task>

---

<verification>
After all 8 tasks complete:

**Code-level (automated, executor-side):**
- `npm run build` PASSES (submodule mr-3)
- `npm test` PASSES (existing 34 tests still green; `filaDevedor.test.js` ajustado em Task 01-05 ainda PASS)
- D-01 motor INTOCADO via Shield 1 (`git log --name-only` empty diff em `devedorCalc.js`, `pagamentos.js`, `dividas.js` cumulativo nesta phase)
- Migration 003 SQL existe com todas as 5 strings críticas
- Handler `excluirDevedor` em App.jsx:3497 chama `dbUpdate(deleted_at)` não `dbDelete`
- 3 callsites recebem filtro `deleted_at=is.null` (App.jsx:8392, filaDevedor.js:586, filaDevedor.test.js:9)
- Exceção D-pre-11 documentada inline em `processosJudiciais.js:71` (comentário menciona "D-pre-11", "exceção forense", "Inativo")
- Filtro NÃO foi adicionado no JOIN forense por engano (Shield 4 zero)
- Badge "Inativo" cinza renderizado em `ProcessosJudiciais.jsx` quando `dev.deleted_at` truthy

**SQL execution gate (manual, usuário):**
- Usuário executou Migration 003 manualmente no Supabase SQL Editor (mesmo pattern Migrations 001/002)
- Após execução SQL, verificar via Supabase SQL: `SELECT column_name FROM information_schema.columns WHERE table_name='devedores' AND column_name='deleted_at';` retorna 1 row
- Verificar partial index: `SELECT indexname, indexdef FROM pg_indexes WHERE tablename='devedores' AND indexname='devedores_deleted_at_idx';` retorna 1 row com `WHERE (deleted_at IS NULL)` no `indexdef`

**UAT humano (cenários SC1-SC6 + SC22 do CONTEXT.md sub-item 1):**
- SC1: excluir devedor com `eventos_andamento` em prod — toast sucesso, devedor some da listagem, ZERO FK violation (root cause bug fix)
- SC2: cross-entity isolation — excluir devedor X não afeta devedor Y (lição `cross_entity_uat_isolation`)
- SC3: junction Set lookup — devedor soft-deleted some das dívidas no front-end
- SC4: `filaDevedor.js` query por id retorna lista sem soft-deleted
- SC5: ProcessosJudiciais existente — chip do devedor soft-deleted renderiza badge "Inativo" cinza, devedor permanece visível no polo (preserva histórico forense)
- SC6: restauração SQL — `UPDATE devedores SET deleted_at=NULL WHERE id=X` → devedor reaparece em todas as listas após reload
- SC22: criação NOVA processo — modal "Adicionar Réu" NÃO exibe devedor inativo no dropdown (D-pre-9 callsite 1 aplicado em criação NOVA — distingue de exceção D-pre-11 que vale só para processo já cadastrado)

**Cross-cutting shields (Cross-cutting SCs aplicáveis a este plan):**
- SC18: D-01 motor INTOCADO via `git log --name-only` (Shield 1 desta task)
- SC19: build + unit tests verde após cada commit atômico
- SC20: smoke prod paths — Pessoas listing, ModuloContratos render, Dashboard agregação, Devedores listing renderizam sem console errors após Migration 003 SQL aplicada e código deployed
</verification>

<success_criteria>
1. Migration 003 SQL file existe e contém ALTER + INDEX parcial + pg_notify (Task 01-01 acceptance)
2. Handler `excluirDevedor` em App.jsx:3497 usa `dbUpdate({ deleted_at })` não `dbDelete` (Task 01-02 acceptance)
3. 3 callsites de leitura recebem filtro `deleted_at=is.null` (Tasks 01-03, 01-04, 01-05 acceptance)
4. 1 exceção forense documentada inline em `processosJudiciais.js:71` SEM filtro adicionado (Task 01-06 acceptance)
5. Badge "Inativo" cinza renderizado em `ProcessosJudiciais.jsx` quando `dev.deleted_at` truthy (Task 01-07 acceptance)
6. D-01 motor Art.354 INTOCADO cumulativo via Shield 1 (`git log --name-only` empty) — `devedorCalc.js`, `pagamentos.js`, `dividas.js` zero modificações nesta phase (Task 01-08 acceptance)
7. Build + tests PASSAM (`npm run build` e `npm test` exit 0; 34/34 tests verde — incluindo `filaDevedor.test.js` ajustado)
8. UAT humano SC1 (root cause bug fix) PASS em prod — DELETE devedor que antes falhava por FK violation agora funciona via UPDATE deleted_at
</success_criteria>
