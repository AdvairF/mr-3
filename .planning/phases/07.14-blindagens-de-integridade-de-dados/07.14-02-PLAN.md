---
plan: 07.14-02
phase: 7.14
title: Remover botão "+ Nova Dívida" do ModuloDividas + DELETE NovaDivida.jsx
wave: 1
depends_on: []
autonomous: false
files_modified:
  - src/mr-3/mr-cobrancas/src/components/ModuloDividas.jsx
files_deleted:
  - src/mr-3/mr-cobrancas/src/components/NovaDivida.jsx
files_preserved:
  - src/mr-3/mr-cobrancas/src/components/DividaForm.jsx (D-pre-12 — App.jsx:3947 consumer)
  - src/mr-3/mr-cobrancas/src/App.jsx (D-pre-12 — :3947 drawer Pessoas preservado)
requirements:
  - D-pre-6
  - D-pre-12
tags:
  - cleanup
  - dead-code
  - module-dividas
---

# Plan 07.14-02: Remover botão "+ Nova Dívida" + DELETE NovaDivida.jsx

<objective>
Eliminar caminho legacy obsoleto de criação de dívida em ModuloDividas (botão "+ Nova Dívida" + view `nova` + handlers + import) e DELETAR o componente standalone `NovaDivida.jsx` cujo único consumer é o próprio ModuloDividas (grep audit 2026-04-27 confirmado, ver `<grep_audit>`). Preservar `DividaForm.jsx` (consumer adicional em `App.jsx:3947` drawer Pessoas — D-pre-12 caminho legacy aceitável: herda `contrato_id` do contexto, não cria órfã). Trivial em escopo (1 arquivo editado, 1 arquivo deletado), baixo risco (zero órfãos `dividas.contrato_id IS NULL = 0` confirmado em prod), zero motor.
</objective>

<grep_audit>
**Executado 2026-04-27, antes deste plano:**

```
$ grep -rn "NovaDivida" src/mr-3/mr-cobrancas/src
src/mr-3/mr-cobrancas/src/components/NovaDivida.jsx:24:export default function NovaDivida({ ... })
src/mr-3/mr-cobrancas/src/components/ModuloDividas.jsx:5:import NovaDivida from "./NovaDivida.jsx";
src/mr-3/mr-cobrancas/src/components/ModuloDividas.jsx:93:        <NovaDivida ...

$ grep -rn "handleNovaDivida" src/mr-3/mr-cobrancas/src
src/mr-3/mr-cobrancas/src/components/ModuloDividas.jsx:41:  function handleNovaDivida() { setView("nova"); }
src/mr-3/mr-cobrancas/src/components/ModuloDividas.jsx:58:            <Btn onClick={handleNovaDivida} ...

$ grep -rn "DividaForm" src/mr-3/mr-cobrancas/src
src/mr-3/mr-cobrancas/src/App.jsx:32:import DividaForm from "./components/DividaForm.jsx";
src/mr-3/mr-cobrancas/src/App.jsx:3947:                <DividaForm
src/mr-3/mr-cobrancas/src/components/DividaForm.jsx:18:export default function DividaForm({ ... })
src/mr-3/mr-cobrancas/src/components/NovaDivida.jsx:5:import DividaForm from "./DividaForm.jsx";
src/mr-3/mr-cobrancas/src/components/NovaDivida.jsx:229:      <DividaForm ...
```

**Conclusão:**
- `NovaDivida.jsx` consumer único = `ModuloDividas.jsx` (será removido) → **PODE DELETAR**
- `DividaForm.jsx` consumer adicional = `App.jsx:32,3947` (drawer Pessoas) → **MANTER** (D-pre-12)
- `handleNovaDivida` local a ModuloDividas → cleanup junto
- Backup files `App.13-04-backup.js/.jsx` contêm `resetDividaForm` (helper interno, nome diferente, não consome o componente `DividaForm.jsx`) — sem impacto.
</grep_audit>

<must_haves>
- Botão "+ Nova Dívida" ausente em `ModuloDividas.jsx` (zero matches `+ Nova Dívida`)
- View `nova` switcher removida (linhas 92-99 originais)
- Handlers `handleNovaDivida` e `handleVoltarDaNova` removidos (linhas 41-42 originais)
- Import `NovaDivida` removido (linha 5 original)
- Estado `view` e `setView` continuam funcionando para os 2 modos restantes (`lista` e `detalhe`)
- File `NovaDivida.jsx` DELETADO (test -f retorna falso)
- `DividaForm.jsx` PRESERVADO (test -f retorna verdadeiro, App.jsx:3947 ainda usa)
- `App.jsx` INTOCADO (zero modificações em `App.jsx`, especialmente linha 3947 callsite `<DividaForm>` preservado)
- D-01 motor INTOCADO cumulativo (`devedorCalc.js`, `services/pagamentos.js`, `services/dividas.js` zero modificações nesta phase 7.14)
- `npm run build` PASS (sem erros de import quebrado, especialmente o import removido em ModuloDividas)
- `npm test` PASS (nenhum test quebrado)
- Grep zero references: `NovaDivida` aparece SOMENTE em arquivos backup `App.13-04-backup.*` (já existentes, não criados aqui) e em SUMMARYs/PLANs históricos (.planning/) — zero ocorrências em `src/mr-3/mr-cobrancas/src/` exceto backups
</must_haves>

<task id="02-01">
**Title:** Cleanup ModuloDividas.jsx — remover botão, view nova, handlers, import

**Type:** auto

<read_first>
- `.planning/phases/07.14-blindagens-de-integridade-de-dados/07.14-CONTEXT.md` (especialmente D-pre-6 e D-pre-12)
- `src/mr-3/mr-cobrancas/src/components/ModuloDividas.jsx` (arquivo a editar)
</read_first>

<files>
src/mr-3/mr-cobrancas/src/components/ModuloDividas.jsx
</files>

<action>
Edição cirúrgica de 4 trechos no arquivo `src/mr-3/mr-cobrancas/src/components/ModuloDividas.jsx`. Use a ferramenta Edit (não rewrite total). Cada trecho abaixo cita LINHA EXATA e CÓDIGO EXATO no estado atual (verificado 2026-04-27).

**1) Remover linha 5 — import NovaDivida:**

Trecho atual (linha 5):
```jsx
import NovaDivida from "./NovaDivida.jsx";
```

Substituir por: (DELETAR linha — remover por completo, sem deixar linha em branco no lugar)

Após edição, as importações ficam:
```jsx
import { useState } from "react";
import FiltroDividas from "./FiltroDividas.jsx";
import TabelaDividas from "./TabelaDividas.jsx";
import DetalheDivida from "./DetalheDivida.jsx";
import Btn from "./ui/Btn.jsx";
```

**2) Remover linhas 41-42 — handlers handleNovaDivida e handleVoltarDaNova:**

Trecho atual (linhas 41-42):
```jsx
  function handleNovaDivida() { setView("nova"); }
  function handleVoltarDaNova() { setView("lista"); }
```

Substituir por: (DELETAR ambas as linhas)

Após edição, a função `handleVoltar()` (linha 36-39 original) é a última helper antes do `return`.

**3) Remover linha 58 — botão `+ Nova Dívida`:**

Trecho atual (linha 58):
```jsx
            <Btn onClick={handleNovaDivida} color="#059669" sm>+ Nova Dívida</Btn>
```

Substituir por: (DELETAR linha completa)

Após edição, o `<div>` flex continua existindo com apenas o título "Dívidas" + chip "em cobrança". Isso é OK — o flex-justify-between dispara o layout para alinhar o título à esquerda; chip "em cobrança" continua aninhado ao título no inner flex-gap-10.

OPCIONAL (cosmético, recomendado): se a remoção deixar `justify-content: space-between` esquisito visualmente (só um child), pode trocar para `justify-content: flex-start`. Mas isso é safe-default — o flex se ajusta naturalmente. Não exigido pelo plano. Manter `space-between` é aceito (degrada graciosamente para flex-start visualmente quando há um único child).

**Decisão:** manter `space-between` (sem alteração de estilo). Reduz superfície da edição. Layout não quebra.

**4) Remover linhas 92-99 — bloco view `nova`:**

Trecho atual (linhas 92-99):
```jsx
      {view === "nova" && (
        <NovaDivida
          devedores={devedores}
          credores={credores}
          onCarregarTudo={onCarregarTudo}
          onVoltar={handleVoltarDaNova}
        />
      )}
```

Substituir por: (DELETAR bloco completo, incluindo linha em branco antes se houver)

Após edição, o JSX retorna apenas 2 views: `view === "lista"` e `view === "detalhe"`. O state `view` permanece (`useState("lista")`) e nunca recebe o valor `"nova"` (já que handler removido).

**Não tocar:**
- `App.jsx` (especialmente linha 3947 `<DividaForm>` callsite — D-pre-12 preservado)
- `DividaForm.jsx` (D-pre-12 preservado)
- Qualquer outro arquivo
- D-01 motor: `devedorCalc.js`, `services/pagamentos.js`, `services/dividas.js`
- `src/utils/masks.js` (D-pre-3 preservado)

Executar `npm run build` localmente após edição para confirmar zero erros de import quebrado (especialmente: `NovaDivida` removido do escopo).
</action>

<acceptance_criteria>
Todos os comandos abaixo devem retornar o resultado esperado:

```bash
# 1) Botão removido
! grep -n "+ Nova Dívida" src/mr-3/mr-cobrancas/src/components/ModuloDividas.jsx
# Esperado: zero matches (grep -n com ! negado retorna sucesso)

# 2) Import removido
! grep -n "import NovaDivida" src/mr-3/mr-cobrancas/src/components/ModuloDividas.jsx
# Esperado: zero matches

# 3) Handler removido
! grep -n "handleNovaDivida" src/mr-3/mr-cobrancas/src/components/ModuloDividas.jsx
! grep -n "handleVoltarDaNova" src/mr-3/mr-cobrancas/src/components/ModuloDividas.jsx
# Esperado: zero matches

# 4) View "nova" removida (na string literal usada no setView)
! grep -nE 'setView\("nova"\)' src/mr-3/mr-cobrancas/src/components/ModuloDividas.jsx
! grep -nE 'view === "nova"' src/mr-3/mr-cobrancas/src/components/ModuloDividas.jsx
# Esperado: zero matches

# 5) Componente <NovaDivida ainda referenciado? (deve ser zero)
! grep -nE "<NovaDivida" src/mr-3/mr-cobrancas/src/components/ModuloDividas.jsx
# Esperado: zero matches

# 6) Imports remanescentes corretos
grep -nE '^import .+ from ".+"' src/mr-3/mr-cobrancas/src/components/ModuloDividas.jsx
# Esperado: 5 linhas (useState, FiltroDividas, TabelaDividas, DetalheDivida, Btn)

# 7) Views remanescentes ainda funcionam
grep -nE 'view === "(lista|detalhe)"' src/mr-3/mr-cobrancas/src/components/ModuloDividas.jsx
# Esperado: 2 matches (view === "lista" e view === "detalhe")

# 8) Build passa
cd src/mr-3/mr-cobrancas && npm run build
# Esperado: PASS, zero erros
```
</acceptance_criteria>

<commit>
```
fix(07.14-02): remove botão +Nova Dívida e view nova de ModuloDividas (D-pre-6)

- Remove import NovaDivida (linha 5)
- Remove handlers handleNovaDivida + handleVoltarDaNova (linhas 41-42)
- Remove botão "+ Nova Dívida" (linha 58)
- Remove bloco view "nova" do switcher (linhas 92-99)

NovaDivida.jsx será deletado em commit subsequente (Task 02-02).
DividaForm.jsx PRESERVADO (App.jsx:3947 consumer — D-pre-12).
App.jsx INTOCADO (D-pre-12).
D-01 motor INTOCADO (gate em Task 02-03).
```
</commit>

</task>

<task id="02-02">
**Title:** Deletar arquivo NovaDivida.jsx

**Type:** auto

<read_first>
- `.planning/phases/07.14-blindagens-de-integridade-de-dados/07.14-CONTEXT.md` (D-pre-6 — autorização explícita "DELETAR arquivo `components/NovaDivida.jsx`")
- Output da Task 02-01 (build PASS confirmado, import removido)
</read_first>

<files>
src/mr-3/mr-cobrancas/src/components/NovaDivida.jsx (DELETE)
</files>

<action>
Pré-condição (verificar antes de deletar):

```bash
# Confirmar que Task 02-01 completou — sem este check, deletar quebra o build
! grep -n "import NovaDivida" src/mr-3/mr-cobrancas/src/components/ModuloDividas.jsx
# Esperado: zero matches. Se algum match aparecer, ABORT — Task 02-01 não foi aplicada.
```

Se pré-condição falhar, parar e reportar ao orquestrador. Não deletar o arquivo.

Se pré-condição passar:

```bash
# Deletar fisicamente o arquivo
rm src/mr-3/mr-cobrancas/src/components/NovaDivida.jsx
# Equivalente Windows/PowerShell: Remove-Item ...
# Equivalente git: git rm src/mr-3/mr-cobrancas/src/components/NovaDivida.jsx (preferido para git tracking)
```

**Preferir** `git rm` (não `rm` raw) para manter o staging consistente com o commit subsequente:

```bash
git rm src/mr-3/mr-cobrancas/src/components/NovaDivida.jsx
```

**Não tocar:**
- `DividaForm.jsx` — D-pre-12 preservado (consumer App.jsx:3947 ainda usa)
- `App.jsx` — D-pre-12 preservado, especialmente linha 3947 `<DividaForm>` callsite
- Qualquer outro arquivo

Executar `npm run build` após deleção para confirmar zero erros — `NovaDivida` não deve mais aparecer em grafo de imports.
</action>

<acceptance_criteria>
```bash
# 1) Arquivo deletado
! test -f src/mr-3/mr-cobrancas/src/components/NovaDivida.jsx
# Esperado: arquivo não existe

# 2) DividaForm.jsx PRESERVADO (D-pre-12)
test -f src/mr-3/mr-cobrancas/src/components/DividaForm.jsx
# Esperado: arquivo existe

# 3) App.jsx PRESERVADO (D-pre-12), especialmente :3947
grep -n "DividaForm" src/mr-3/mr-cobrancas/src/App.jsx
# Esperado: matches em linha 32 (import) e linha 3947 (callsite). Zero remoções.

# 4) Zero referências a NovaDivida em código produtivo (excluindo backups e .planning)
! grep -rn "NovaDivida" src/mr-3/mr-cobrancas/src --include="*.jsx" --include="*.js" \
    --exclude="App.13-04-backup.*"
# Esperado: zero matches. Backups históricos (`App.13-04-backup.*`) ficam intocados.

# 5) Build passa após deleção
cd src/mr-3/mr-cobrancas && npm run build
# Esperado: PASS, zero erros de "module not found"

# 6) Tests passam
cd src/mr-3/mr-cobrancas && npm test
# Esperado: PASS
```
</acceptance_criteria>

<commit>
```
chore(07.14-02): DELETE src/mr-3/mr-cobrancas/src/components/NovaDivida.jsx (D-pre-6)

Consumer único (ModuloDividas.jsx) removido em commit anterior (07.14-02 task 01).
Grep audit 2026-04-27 confirmou zero outros consumers em código produtivo.

DividaForm.jsx PRESERVADO — consumer adicional em App.jsx:3947 (drawer Pessoas)
mantido como caminho legacy aceitável (D-pre-12: herda contrato_id do contexto,
não cria órfã).

App.jsx INTOCADO. D-01 motor INTOCADO (gate em commit subsequente).
```
</commit>

</task>

<task id="02-03">
**Title:** Gate task — D-01 motor shield + grep zero references + build + tests + smoke prod

**Type:** checkpoint:human-verify

**Gate:** blocking

<read_first>
- `.planning/phases/07.14-blindagens-de-integridade-de-dados/07.14-CONTEXT.md` (Invariantes herdadas — D-01 motor INTOCADO + D-pre-12 App.jsx:3947 preservado)
- Lições MEMORY: `gate_motor_grep_use_name_only`, `grep_global_before_pointwise_fix`
</read_first>

<files>
(verification only — no file edits)
</files>

<action>
Executar todos os shields automatizados abaixo, em sequência. Qualquer falha é blocker; reportar ao orquestrador para revisão manual antes de marcar a phase como complete.

**Shield 1 — D-01 motor INTOCADO cumulativo (lição `gate_motor_grep_use_name_only`):**

```bash
# Identificar primeiro commit da phase 7.14 — substituir <BASELINE_SHA> pela SHA do commit
# imediatamente anterior ao primeiro commit 7.14 (HEAD do master pré-7.14).
# Se baseline ainda não foi calculado, derivar:
#   BASELINE_SHA=$(git log --format=%H -- .planning/phases/07.14-blindagens-de-integridade-de-dados/07.14-CONTEXT.md | tail -1)^

cd src/mr-3/mr-cobrancas

# Listar arquivos motor modificados desde baseline (--name-only para evitar falso positivo em commit message — lição 7.8.2a commit 608c124)
git log <BASELINE_SHA>..HEAD --format="" --name-only -- \
    src/utils/devedorCalc.js \
    src/services/pagamentos.js \
    src/services/dividas.js | sort -u

# Esperado: output VAZIO (zero linhas). Se aparecer algum arquivo motor: ABORT — D-01 violado.
```

**Shield 2 — D-pre-12 App.jsx:3947 preservado:**

```bash
# Garantir que App.jsx não foi modificado nesta task
git log <BASELINE_07.14-02_SHA>..HEAD --format="" --name-only -- \
    src/mr-3/mr-cobrancas/src/App.jsx | sort -u

# Esperado: VAZIO. Outros plans (07.14-01, 07.14-03, 07.14-04) podem tocar App.jsx,
# mas 07.14-02 NÃO. Se App.jsx aparece nos commits da Task 02-01 ou 02-02: ABORT.

# Confirmar callsite preservado
grep -n "<DividaForm" src/mr-3/mr-cobrancas/src/App.jsx
# Esperado: pelo menos 1 match em torno da linha 3947 (linha exata pode variar com edits
# de outros plans, mas o callsite deve continuar existindo).

grep -n "import DividaForm" src/mr-3/mr-cobrancas/src/App.jsx
# Esperado: 1 match em torno da linha 32.
```

**Shield 3 — DividaForm.jsx PRESERVADO:**

```bash
test -f src/mr-3/mr-cobrancas/src/components/DividaForm.jsx
# Esperado: existe

git log <BASELINE_07.14-02_SHA>..HEAD --format="" --name-only -- \
    src/mr-3/mr-cobrancas/src/components/DividaForm.jsx | sort -u
# Esperado: VAZIO (não modificado nesta task).
```

**Shield 4 — NovaDivida.jsx DELETADO + zero referências em código produtivo:**

```bash
! test -f src/mr-3/mr-cobrancas/src/components/NovaDivida.jsx
# Esperado: arquivo não existe

# Grep global em código produtivo (excluindo backups históricos e .planning)
! grep -rn "NovaDivida" src/mr-3/mr-cobrancas/src \
    --include="*.jsx" --include="*.js" \
    --exclude="App.13-04-backup.*" \
    --exclude-dir=".planning"
# Esperado: zero matches. Backups (`App.13-04-backup.*`) e .planning/ não são código produtivo.
```

**Shield 5 — masks.js INTOCADO (D-pre-3 cumulativo, lição `blindagens_integridade`):**

```bash
git log <BASELINE_07.14-02_SHA>..HEAD --format="" --name-only -- \
    src/mr-3/mr-cobrancas/src/utils/masks.js | sort -u
# Esperado: VAZIO. Phase 7.14-04 toca paste-friendly mas masks.js NUNCA é tocado em 7.14.
```

**Shield 6 — Build verde:**

```bash
cd src/mr-3/mr-cobrancas && npm run build
# Esperado: exit code 0, "build successful" ou equivalente, zero warnings de import broken.
```

**Shield 7 — Tests verdes:**

```bash
cd src/mr-3/mr-cobrancas && npm test -- --run
# Esperado: exit code 0, todos os testes existentes PASS. Nenhum teste novo é exigido neste plan.
```

**Smoke UAT humano (checkpoint:human-verify):**

Pedir ao usuário:

> "Por favor abra a aplicação local (`npm run dev` no submódulo `src/mr-3/mr-cobrancas`), navegue para o módulo **Dívidas** e confirme:
>
> 1. O botão verde "+ Nova Dívida" no topo direito **NÃO aparece mais**.
> 2. A listagem de dívidas continua renderizando normalmente (header "Dívidas", chip "X em cobrança", filtros, tabela).
> 3. Clicar em uma dívida ainda abre `DetalheDivida` (view `detalhe` continua funcionando).
> 4. O console do browser não mostra nenhum erro de import quebrado nem render error.
> 5. (Cross-check D-pre-12) Vá em **Pessoas** → abra um devedor → confirme que o drawer ainda mostra a seção que permite "+ Salvar Dívida" via `DividaForm` (D-pre-12 preservado, esse caminho continua existindo)."
>
> **Resume signal:** "approved" se todos os 5 itens passaram, ou descreva o que falhou.

Após "approved" do usuário, gate complete. Se algum item falhar, reportar ao orquestrador (provavelmente revision mode em outro plan, não neste).
</action>

<acceptance_criteria>
Todos os shields acima retornam o resultado esperado **E** o usuário responde "approved" ao smoke UAT cross-check.

Cumulative invariants (verificáveis ad-hoc):
```bash
# D-01 motor zero modificações em toda a phase 7.14
git log <BASELINE_PRE_7.14_SHA>..HEAD --format="" --name-only -- \
    src/mr-3/mr-cobrancas/src/utils/devedorCalc.js \
    src/mr-3/mr-cobrancas/src/services/pagamentos.js \
    src/mr-3/mr-cobrancas/src/services/dividas.js | sort -u
# Esperado: VAZIO

# masks.js zero modificações em toda a phase 7.14 (D-pre-3)
git log <BASELINE_PRE_7.14_SHA>..HEAD --format="" --name-only -- \
    src/mr-3/mr-cobrancas/src/utils/masks.js | sort -u
# Esperado: VAZIO
```
</acceptance_criteria>

<resume_signal>
Type "approved" se smoke UAT humano + todos os 7 shields automatizados passaram. Se alguma coisa falhou, descreva o item (ex: "Shield 1 falhou — devedorCalc.js apareceu no diff").
</resume_signal>

</task>

<verification>
**Plan-level verification (após Task 02-03 approved):**

1. `npm run build` PASS
2. `npm test` PASS
3. Grep audit:
   - `! grep -n "+ Nova Dívida" src/mr-3/mr-cobrancas/src/components/ModuloDividas.jsx`
   - `! grep -n "handleNovaDivida" src/mr-3/mr-cobrancas/src/components/ModuloDividas.jsx`
   - `! grep -n "import NovaDivida" src/mr-3/mr-cobrancas/src/components/ModuloDividas.jsx`
   - `! test -f src/mr-3/mr-cobrancas/src/components/NovaDivida.jsx`
   - `test -f src/mr-3/mr-cobrancas/src/components/DividaForm.jsx`
   - `grep -n "<DividaForm" src/mr-3/mr-cobrancas/src/App.jsx` retorna pelo menos 1 match (idealmente em torno da linha 3947, mas pode shift por outros plans 7.14)
4. D-01 motor cumulative shield: zero modificações em `devedorCalc.js`, `services/pagamentos.js`, `services/dividas.js` desde baseline pre-7.14
5. masks.js cumulative shield: zero modificações desde baseline pre-7.14
6. UAT humano (SC7 + SC8 da CONTEXT.md):
   - **SC7**: Acessar ModuloDividas → botão "+ Nova Dívida" ausente; listagem continua funcionando normal. ✓
   - **SC8** (caminho substituto): criar dívida via `DetalheContrato → "+ Adicionar Documento"` continua funcionando idêntico. ✓
   - **Bonus D-pre-12**: drawer Pessoas/Devedores ainda permite "💾 Salvar Dívida" via DividaForm. ✓

**Output esperado:** ModuloDividas com 5 imports, 2 views (`lista` e `detalhe`), zero referência a `NovaDivida`. Arquivo `NovaDivida.jsx` deletado. `DividaForm.jsx` + `App.jsx:3947` preservados.

**Total commits esperados:** 2 (um por Task 02-01 e Task 02-02; Task 02-03 é gate-only, não comita).
</verification>

<rollback>
Se Task 02-03 reprovar (smoke UAT identifica regressão visual ou shield falha):

```bash
# Reverter ambos os commits da phase 02 sem afetar plans 01/03/04
git revert <SHA_TASK_02-02> <SHA_TASK_02-01> --no-edit
# Resultado: NovaDivida.jsx restaurado, ModuloDividas.jsx restaurado ao estado pré-02
```

Comunicar ao orquestrador para revision mode no plan 02 antes de re-tentar.
</rollback>
