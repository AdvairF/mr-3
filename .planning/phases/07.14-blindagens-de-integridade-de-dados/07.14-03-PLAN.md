---
plan: 07.14-03
phase: 7.14
title: Validação criação contrato — UI guard + service throw (defense in depth)
wave: 1
depends_on: []
autonomous: false
files_modified:
  - src/mr-3/mr-cobrancas/src/components/NovoContrato.jsx
  - src/mr-3/mr-cobrancas/src/services/contratos.js
  - src/mr-3/mr-cobrancas/src/services/__tests__/criarContratoGuard.test.js
requirements: []
must_haves:
  truths:
    - "NovoContrato.handleSalvar bloqueia submit se !devedor_id (toast 'Selecione o devedor')"
    - "NovoContrato.handleSalvar bloqueia submit se !credor_id (toast 'Selecione o credor') — NOVO"
    - "criarContrato(payload) em services/contratos.js throwa Error semântico se !payload.devedor_id"
    - "criarContrato(payload) em services/contratos.js throwa Error semântico se !payload.credor_id"
    - "Schema contratos_dividas.devedor_id e credor_id permanecem nullable (D-pre-2)"
    - "DetalheContrato.handleSalvar (linha 238 — UPDATE) intocado"
    - "Motor D-01 (devedorCalc.js + services/pagamentos.js + services/dividas.js) intocado cumulativo"
    - "npm run build PASS, npm test PASS (testes existentes + 2 novos guards)"
  artifacts:
    - path: "src/mr-3/mr-cobrancas/src/components/NovoContrato.jsx"
      provides: "UI guard credor_id antes de POST (linha 44 handleSalvar)"
      contains: "Selecione o credor"
    - path: "src/mr-3/mr-cobrancas/src/services/contratos.js"
      provides: "Service throw guards no topo de criarContrato (linha 111)"
      contains: "criarContrato: devedor_id obrigatório"
    - path: "src/mr-3/mr-cobrancas/src/services/__tests__/criarContratoGuard.test.js"
      provides: "Unit tests dos 2 guards do service"
      min_lines: 30
  key_links:
    - from: "NovoContrato.handleSalvar"
      to: "criarContrato(payload)"
      via: "import { criarContrato } from '../services/contratos.js' (linha 5)"
      pattern: "criarContrato\\(payload\\)"
    - from: "criarContrato"
      to: "dbInsert(TABLE, payload)"
      via: "throw guards interceptam ANTES do INSERT"
      pattern: "throw new Error\\(\"criarContrato:"
---

# Plan 07.14-03: Validação criação contrato (UI + service guard)

<objective>
Implementar defense in depth para criação de contrato (D-pre-7 + D-pre-10 da Phase 7.14): UI valida ANTES do submit (UX), service-layer guarda ANTES do INSERT (integridade). Hoje `NovoContrato.handleSalvar` valida apenas `!devedor_id`; falta validação `!credor_id`. E `criarContrato` em `services/contratos.js` aceita payload com nulls silenciosamente. Lição feedback `db_integration_gate_missing` (4 bugs em série na 7.9 SC1) motiva o service guard: previne futuros consumers (scripts, tests, novos componentes) de criar contratos órfãos. Schema permanece nullable (D-pre-2 — legacy preservado, sem migration NOT NULL agressiva).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.14-blindagens-de-integridade-de-dados/07.14-CONTEXT.md
@src/mr-3/mr-cobrancas/src/components/NovoContrato.jsx
@src/mr-3/mr-cobrancas/src/services/contratos.js

<interfaces>
<!-- Contratos extraídos do código existente — executor usa direto, sem exploração -->

From src/mr-3/mr-cobrancas/src/components/NovoContrato.jsx (linha 44, handleSalvar atual):
```js
async function handleSalvar() {
  if (!devedor_id) { toast("Selecione o devedor.", { icon: "⚠️" }); return; }
  setSalvando(true);
  try {
    const payload = { credor_id: credor_id || null, devedor_id, /* ... */ };
    const res = await criarContrato(payload);
    // ...
  } catch (e) {
    toast.error("Erro ao criar contrato: " + e.message);
  } finally {
    setSalvando(false);
  }
}
```

Toast import: `import toast from "react-hot-toast";` (linha 2 já presente).

From src/mr-3/mr-cobrancas/src/services/contratos.js (linha 111, criarContrato atual):
```js
export async function criarContrato(payload) {
  const res = await dbInsert(TABLE, payload);
  const contrato = Array.isArray(res) ? res[0] : res;
  if (contrato?.id) {
    registrarEvento(contrato.id, "criacao", { /* snapshot */ }).catch(() => {});
  }
  return res;
}
```

TABLE constant: `const TABLE = "contratos_dividas";` (linha 68).
dbInsert import: `import { dbGet, dbInsert, dbUpdate, dbDelete, sb } from "../config/supabase.js";` (linha 64).

From src/mr-3/mr-cobrancas/src/services/__tests__/devedoresDividasContrato.test.js (test framework reference):
```js
import { describe, it, expect, vi, beforeEach } from 'vitest';
const mockSb = vi.fn();
const mockDbGet = vi.fn();
vi.mock('../../config/supabase.js', () => ({
  sb: (...args) => mockSb(...args),
  dbGet: (...args) => mockDbGet(...args),
}));
```

Test framework: **vitest 4.1.4** (confirmado em `src/mr-3/mr-cobrancas/package.json` devDependencies).
Test runner: `npm test` (script: `vitest run`) — submódulo `src/mr-3/mr-cobrancas/`.
</interfaces>

<constraints>
- **D-pre-2 (LOCKED):** schema `contratos_dividas.devedor_id` e `credor_id` PERMANECEM nullable. NÃO criar migration NOT NULL.
- **D-pre-7 (LOCKED):** `DetalheContrato.handleSalvar` (linha 238) é UPDATE (chama `editarContrato`, não `criarContrato`). NÃO TOCAR.
- **D-pre-10 (LOCKED):** UI guard E service guard ambos throwam mensagem semântica. Defense in depth.
- **D-01 cumulativo (LOCKED):** `src/mr-3/mr-cobrancas/src/utils/devedorCalc.js`, `src/mr-3/mr-cobrancas/src/services/pagamentos.js`, `src/mr-3/mr-cobrancas/src/services/dividas.js` — INTOCADOS. Gate task no final valida via `git log -1 --format="" --name-only HEAD`.
- **Scope (LOCKED):** apenas `NovoContrato.handleSalvar` + `criarContrato` + 1 arquivo de teste novo. Nenhum outro arquivo modificado.
</constraints>
</context>

<tasks>

<task id="03-01" type="auto">
  <name>Task 03-01: NovoContrato UI guard — adicionar validação credor_id antes do POST</name>
  <files>src/mr-3/mr-cobrancas/src/components/NovoContrato.jsx</files>
  <read_first>
    - src/mr-3/mr-cobrancas/src/components/NovoContrato.jsx (handleSalvar linha 44)
    - .planning/phases/07.14-blindagens-de-integridade-de-dados/07.14-CONTEXT.md (D-pre-7, D-pre-10)
  </read_first>
  <action>
Editar `handleSalvar` (linha 44) para adicionar validação de `credor_id` ANTES do `setSalvando(true)`. Inserir uma única linha entre o guard atual de `devedor_id` (linha 45) e `setSalvando(true)` (linha 46).

**Diff exato (verbatim D-pre-7):**

ANTES (linhas 44-46):
```js
async function handleSalvar() {
  if (!devedor_id) { toast("Selecione o devedor.", { icon: "⚠️" }); return; }
  setSalvando(true);
```

DEPOIS:
```js
async function handleSalvar() {
  if (!devedor_id) { toast("Selecione o devedor.", { icon: "⚠️" }); return; }
  if (!credor_id)  { toast("Selecione o credor.",  { icon: "⚠️" }); return; }
  setSalvando(true);
```

**NÃO alterar:**
- `podesSalvar = !!devedor_id` (linha 38) — permanece como está. O botão `Btn` em linha 163 usa `disabled={!podesSalvar || salvando}`. Não tornar `podesSalvar` dependente de `credor_id` (mudança comportamental fora do escopo D-pre-7); o toast cobre o caso de submit por Enter ou edge case.
- O resto do `handleSalvar` (try/catch, payload, await criarContrato, etc.) — inalterado.
- Imports — `toast` já importado linha 2.
- O `<select>` do credor (linha 90) — opção "— Sem credor" (linha 91) permanece visível na UI; o guard cobre caso runtime.

Justificativa do não-mexer em `podesSalvar`: D-pre-7 especifica APENAS o guard runtime no `handleSalvar`. Tornar o botão disabled também muda UX (botão fica permanente desabilitado até credor selecionado), expandindo escopo. Toast é suficiente como defesa imediata.
  </action>
  <verify>
    <automated>
      cd src/mr-3/mr-cobrancas && grep -n "Selecione o credor" src/components/NovoContrato.jsx
    </automated>
    Espera linha matched: `if (!credor_id)  { toast("Selecione o credor.",  { icon: "⚠️" }); return; }`
  </verify>
  <acceptance_criteria>
    - `grep -n "Selecione o credor" src/mr-3/mr-cobrancas/src/components/NovoContrato.jsx` retorna 1 match
    - `grep -n "Selecione o devedor" src/mr-3/mr-cobrancas/src/components/NovoContrato.jsx` retorna 2 matches (1 no handleSalvar + 1 no `title` do span linha 162 — pré-existente)
    - `grep -n "if (!credor_id)" src/mr-3/mr-cobrancas/src/components/NovoContrato.jsx` retorna 1 match
    - `git diff src/mr-3/mr-cobrancas/src/components/NovoContrato.jsx` mostra apenas +1 linha adicionada (sem outras alterações)
    - `cd src/mr-3/mr-cobrancas && npm run build` PASS
  </acceptance_criteria>
  <done>
    UI guard credor_id presente; submit bloqueia com toast quando credor_id é null; nenhuma outra mudança em NovoContrato.jsx. Build passa.
  </done>
</task>

<task id="03-02" type="auto">
  <name>Task 03-02: criarContrato service guard — throw new Error semântico no topo da função</name>
  <files>src/mr-3/mr-cobrancas/src/services/contratos.js</files>
  <read_first>
    - src/mr-3/mr-cobrancas/src/services/contratos.js (criarContrato linha 111)
    - .planning/phases/07.14-blindagens-de-integridade-de-dados/07.14-CONTEXT.md (D-pre-7, D-pre-10)
  </read_first>
  <action>
Editar `criarContrato(payload)` em `services/contratos.js` (linha 111) para adicionar 2 throw guards no TOPO da função, ANTES do `dbInsert`.

**Diff exato (verbatim D-pre-7):**

ANTES (linhas 111-130):
```js
export async function criarContrato(payload) {
  const res = await dbInsert(TABLE, payload);
  const contrato = Array.isArray(res) ? res[0] : res;
  if (contrato?.id) {
    // HIS-01: fire-and-forget — do not block or fail contract creation if history insert fails
    registrarEvento(contrato.id, "criacao", { /* ... snapshot ... */ }).catch(() => {});
  }
  return res;
}
```

DEPOIS:
```js
export async function criarContrato(payload) {
  if (!payload.devedor_id) throw new Error("criarContrato: devedor_id obrigatório");
  if (!payload.credor_id)  throw new Error("criarContrato: credor_id obrigatório");
  const res = await dbInsert(TABLE, payload);
  const contrato = Array.isArray(res) ? res[0] : res;
  if (contrato?.id) {
    // HIS-01: fire-and-forget — do not block or fail contract creation if history insert fails
    registrarEvento(contrato.id, "criacao", { /* ... snapshot ... */ }).catch(() => {});
  }
  return res;
}
```

**NÃO alterar:**
- O resto do `criarContrato` (dbInsert, registrarEvento, return) — inalterado.
- Imports (linha 64) — `dbInsert` já importado.
- Nenhuma outra função do arquivo (`registrarEvento`, `editarContrato`, `cascatearCredorDevedor`, `criarCusta`, etc.) — INTOCADAS.
- A constante `TABLE` (linha 68) — inalterada.

**ATENÇÃO callsite NovoContrato.jsx linha 49:**
O payload atual em `NovoContrato.handleSalvar` faz `credor_id: credor_id || null`. Após Task 03-01 o UI guard impede `credor_id` falsy de chegar aqui. Mas o serviço deve ser robusto a chamadas diretas (futuros scripts, devtools, etc.) — daí o throw é a defesa correta. **O comportamento de NovoContrato em runtime fica:** UI bloqueia ANTES (Task 03-01); se algum caller bypassar (script), service throwa. Defense in depth.

**Implicação semântica para callsites futuros (NÃO ALTERAR NESTA TASK, apenas documentar):**
`criarContrato({ credor_id: null, ... })` deixa de funcionar (throwa). Hoje em prod só `NovoContrato.jsx:61` chama `criarContrato`. Após Task 03-01, esse callsite NUNCA passa null (UI guard intercepta). Logo, comportamento prod inalterado. Confirmar via grep no acceptance abaixo.
  </action>
  <verify>
    <automated>
      cd src/mr-3/mr-cobrancas && grep -n "criarContrato:" src/services/contratos.js && grep -rn "import.*criarContrato\|from.*contratos" src/components/ src/services/ | grep -v "__tests__"
    </automated>
    Espera linhas matched:
    - `if (!payload.devedor_id) throw new Error("criarContrato: devedor_id obrigatório");`
    - `if (!payload.credor_id)  throw new Error("criarContrato: credor_id obrigatório");`
    - apenas 1 callsite consumer: `src/components/NovoContrato.jsx:5` (`import { criarContrato } from "../services/contratos.js";`)
  </verify>
  <acceptance_criteria>
    - `grep -n "criarContrato: devedor_id obrigatório" src/mr-3/mr-cobrancas/src/services/contratos.js` retorna 1 match
    - `grep -n "criarContrato: credor_id obrigatório" src/mr-3/mr-cobrancas/src/services/contratos.js` retorna 1 match
    - `grep -rn "criarContrato" src/mr-3/mr-cobrancas/src/components/ src/mr-3/mr-cobrancas/src/services/ | grep -v "__tests__\|contratos.js"` retorna apenas 2 matches em `NovoContrato.jsx` (linha 5 import + linha 61 invocation) — confirma que nenhum outro caller é afetado
    - `git diff src/mr-3/mr-cobrancas/src/services/contratos.js` mostra +2 linhas adicionadas (apenas os 2 throws); nenhuma outra alteração
    - `cd src/mr-3/mr-cobrancas && npm run build` PASS
  </acceptance_criteria>
  <done>
    Service throws presentes; comportamento runtime de prod inalterado (NovoContrato após Task 03-01 nunca passa nulls); defense in depth ativa para futuros consumers.
  </done>
</task>

<task id="03-03" type="auto">
  <name>Task 03-03: Unit tests para criarContrato guards — vitest com vi.mock dbInsert</name>
  <files>src/mr-3/mr-cobrancas/src/services/__tests__/criarContratoGuard.test.js</files>
  <read_first>
    - src/mr-3/mr-cobrancas/src/services/contratos.js (criarContrato pós Task 03-02)
    - src/mr-3/mr-cobrancas/src/services/__tests__/devedoresDividasContrato.test.js (pattern vi.mock vitest)
    - .planning/phases/07.14-blindagens-de-integridade-de-dados/07.14-CONTEXT.md (D-pre-10 cenário SC11)
  </read_first>
  <action>
Criar arquivo NOVO `src/mr-3/mr-cobrancas/src/services/__tests__/criarContratoGuard.test.js` com 4 testes cobrindo os 2 guards de `criarContrato`.

**Conteúdo completo do arquivo (verbatim — não interpretar):**

```js
/**
 * Phase 7.14 — Sub-item 3 (D-pre-7 + D-pre-10)
 *
 * Testes dos guards de criarContrato em services/contratos.js:
 *   - guard 1: throwa se !payload.devedor_id
 *   - guard 2: throwa se !payload.credor_id
 *
 * Defense in depth — service-layer protege INSERT mesmo quando UI bypassed.
 *
 * Pattern reference: devedoresDividasContrato.test.js (Phase 7.13).
 * Test framework: vitest 4.1.4.
 */

import { describe, it, expect, vi, beforeEach } from 'vitest';

const mockDbInsert = vi.fn();
const mockSb = vi.fn();
const mockDbGet = vi.fn();
const mockDbUpdate = vi.fn();
const mockDbDelete = vi.fn();

vi.mock('../../config/supabase.js', () => ({
  dbInsert: (...args) => mockDbInsert(...args),
  sb: (...args) => mockSb(...args),
  dbGet: (...args) => mockDbGet(...args),
  dbUpdate: (...args) => mockDbUpdate(...args),
  dbDelete: (...args) => mockDbDelete(...args),
}));

// Mock dependências indiretas de contratos.js (atualizarDivida + seedDevedoresDoContrato).
// Não são exercitadas nos testes de guard, mas vi.mock precisa retornar export válido
// para evitar import resolution error.
vi.mock('../dividas.js', () => ({
  atualizarDivida: vi.fn(),
}));
vi.mock('../devedoresDividas.js', () => ({
  seedDevedoresDoContrato: vi.fn(),
}));

describe('criarContrato — service guards (Phase 7.14 D-pre-7/D-pre-10)', () => {
  beforeEach(() => {
    mockDbInsert.mockReset();
    mockSb.mockReset();
    mockDbGet.mockReset();
    mockDbUpdate.mockReset();
    mockDbDelete.mockReset();
  });

  it('throwa Error semântico se devedor_id é null', async () => {
    const { criarContrato } = await import('../contratos.js');
    await expect(
      criarContrato({ devedor_id: null, credor_id: 1 })
    ).rejects.toThrow(/devedor_id obrigatório/);
    expect(mockDbInsert).not.toHaveBeenCalled();
  });

  it('throwa Error semântico se credor_id é null', async () => {
    const { criarContrato } = await import('../contratos.js');
    await expect(
      criarContrato({ devedor_id: 1, credor_id: null })
    ).rejects.toThrow(/credor_id obrigatório/);
    expect(mockDbInsert).not.toHaveBeenCalled();
  });

  it('throwa Error semântico se devedor_id é undefined', async () => {
    const { criarContrato } = await import('../contratos.js');
    await expect(
      criarContrato({ credor_id: 1 })
    ).rejects.toThrow(/devedor_id obrigatório/);
    expect(mockDbInsert).not.toHaveBeenCalled();
  });

  it('payload válido (ambos ids presentes) NÃO throwa e chama dbInsert', async () => {
    mockDbInsert.mockResolvedValueOnce([{ id: 'contrato-novo-1' }]);
    const { criarContrato } = await import('../contratos.js');
    const res = await criarContrato({ devedor_id: 1, credor_id: 2, referencia: 'teste' });
    expect(mockDbInsert).toHaveBeenCalledTimes(1);
    expect(mockDbInsert).toHaveBeenCalledWith(
      'contratos_dividas',
      expect.objectContaining({ devedor_id: 1, credor_id: 2 })
    );
    // mockDbInsert é chamado também por registrarEvento (fire-and-forget) DEPOIS do return.
    // Não validamos chamada de registrarEvento aqui — está fora do escopo dos guards.
    expect(res).toEqual([{ id: 'contrato-novo-1' }]);
  });
});
```

**Pontos de atenção:**

1. **Test framework é vitest** (não jest). Confirmado em `src/mr-3/mr-cobrancas/package.json`. Sintaxe: `import { describe, it, expect, vi, beforeEach } from 'vitest';`.

2. **Path do mock é `'../../config/supabase.js'`** — relativo ao test file em `__tests__/`. Confirmado em `devedoresDividasContrato.test.js:19`.

3. **Mock de `dividas.js` e `devedoresDividas.js`** é necessário porque `contratos.js` importa de ambos (linhas 64-66). Sem mock, vitest falha em resolver as deps em ambiente test.

4. **Não validamos `registrarEvento` (fire-and-forget):** após `dbInsert` retornar com `contrato.id`, o código (linha 116-127) chama `registrarEvento` que internamente chama `dbInsert(HIST_TABLE, ...)`. Como `mockDbInsert` é uma única função, ela seria chamada 2 vezes. O teste foca APENAS no guard — usa `toHaveBeenCalledTimes(1)` na entrada para verificar que dbInsert foi chamado pra `contratos_dividas` mas NÃO contamos quantas vezes total (registrarEvento é async fire-and-forget com `.catch(() => {})`, pode ou não ter executado quando o `await criarContrato` retorna). **Solução adotada:** assert `toHaveBeenCalledTimes(1)` com `expect.objectContaining({ devedor_id: 1, credor_id: 2 })` para confirmar a chamada DO contrato; assertion não falha mesmo se registrarEvento adicionar uma 2ª chamada antes do `return res` (fire-and-forget Promise não-awaited).

   **Alternativa se assertion `toHaveBeenCalledTimes(1)` falhar:** trocar para `toHaveBeenCalled()` simples + `toHaveBeenCalledWith(...)` na primeira chamada. Test runner determinará. Se falhar, executor revisa para `expect(mockDbInsert.mock.calls[0]).toEqual(['contratos_dividas', expect.objectContaining({ devedor_id: 1, credor_id: 2 })]);`.

5. **NÃO criar diretório novo** — `__tests__` já existe (`devedoresDividasContrato.test.js` etc. já lá).

6. **Adicionar este teste ao `test:regressao` script?** — NÃO modificar `package.json` nesta task. `npm test` (vitest run) já roda TODOS os arquivos `*.test.js` por default. O script `test:regressao` é um subset explícito do `prebuild`. Phase futura pode adicionar; YAGNI.
  </action>
  <verify>
    <automated>
      cd src/mr-3/mr-cobrancas && npm test 2>&1 | tail -60
    </automated>
    Espera output contendo:
    - `criarContrato — service guards (Phase 7.14 D-pre-7/D-pre-10)` no test report
    - 4 tests passing dentro desse describe
    - `Tests  X passed` no rodapé (X >= 4 + tests pré-existentes)
  </verify>
  <acceptance_criteria>
    - Arquivo `src/mr-3/mr-cobrancas/src/services/__tests__/criarContratoGuard.test.js` existe (`ls` retorna match)
    - `grep -n "throwa Error semântico se devedor_id é null" src/mr-3/mr-cobrancas/src/services/__tests__/criarContratoGuard.test.js` retorna 1 match
    - `grep -n "throwa Error semântico se credor_id é null" src/mr-3/mr-cobrancas/src/services/__tests__/criarContratoGuard.test.js` retorna 1 match
    - `cd src/mr-3/mr-cobrancas && npm test` PASS — 4 novos testes do describe `criarContrato — service guards (Phase 7.14 D-pre-7/D-pre-10)` verdes; testes pré-existentes (devedoresDividasContrato, calculos, etc.) continuam passando.
    - Nenhuma alteração em arquivos fora do escopo (`git status` mostra apenas o arquivo novo .test.js).
  </acceptance_criteria>
  <done>
    4 unit tests cobrindo guards do service; vitest run PASS sem regressão; pattern alinhado com `devedoresDividasContrato.test.js`.
  </done>
</task>

<task id="03-04" type="checkpoint:human-verify" gate="blocking">
  <name>Task 03-04: Gate D-01 motor + invariantes + build/test final + UAT smoke</name>
  <files>(verificação only — sem modificações)</files>
  <read_first>
    - .planning/phases/07.14-blindagens-de-integridade-de-dados/07.14-CONTEXT.md (Invariantes herdadas, SC9, SC10, SC11, SC18, SC19)
  </read_first>
  <what-built>
    Plan 07.14-03 entrega: (1) UI guard credor_id em `NovoContrato.handleSalvar:44`; (2) Service guards em `criarContrato:111` (`devedor_id` + `credor_id` throws); (3) 4 unit tests vitest cobrindo guards do service.
    Schema permanece nullable (D-pre-2). DetalheContrato UPDATE intocado (D-pre-7). Motor D-01 intocado cumulativo.
  </what-built>
  <action>
**Gate automatizado executado pelo executor ANTES da pausa humana:**

```bash
cd src/mr-3/mr-cobrancas

# 1. D-01 motor INTOCADO no commit deste plano (cumulativo desde 7.13).
git log -1 --format="" --name-only HEAD | grep -E "(devedorCalc\.js|services/pagamentos\.js|services/dividas\.js)$" && echo "FAIL: motor tocado no commit" && exit 1
# Esperado: zero matches → gate verde.

# 2. Apenas os 3 arquivos do escopo modificados.
git diff --name-only HEAD~1 HEAD | sort > /tmp/diff_files.txt
cat <<EOF | sort > /tmp/expected_files.txt
src/mr-3/mr-cobrancas/src/components/NovoContrato.jsx
src/mr-3/mr-cobrancas/src/services/__tests__/criarContratoGuard.test.js
src/mr-3/mr-cobrancas/src/services/contratos.js
EOF
diff /tmp/expected_files.txt /tmp/diff_files.txt && echo "PASS: scope clean" || echo "FAIL: arquivos extras modificados"

# 3. Greps de invariante.
grep -n "Selecione o credor" src/components/NovoContrato.jsx || (echo "FAIL: UI guard ausente" && exit 1)
grep -n "criarContrato: devedor_id obrigatório" src/services/contratos.js || (echo "FAIL: service guard devedor ausente" && exit 1)
grep -n "criarContrato: credor_id obrigatório" src/services/contratos.js || (echo "FAIL: service guard credor ausente" && exit 1)

# 4. D-pre-2: schema NÃO modificado (zero migrations adicionadas).
git diff --name-only HEAD~1 HEAD | grep -E "(\.sql$|migration)" && echo "FAIL: schema modificado" && exit 1

# 5. Build + test full.
npm run build || (echo "FAIL: build" && exit 1)
npm test || (echo "FAIL: test" && exit 1)
```

**Pausa para UAT humano:**

Após gates automatizados verdes, pausa e usuário valida cenários SC9, SC10, SC11 (CONTEXT.md):
  </action>
  <how-to-verify>
    1. **SC9 — UI guard devedor:** Abrir app local (`npm start` no submódulo). Pessoas → escolher devedor → "+ Novo Contrato" → NÃO selecionar devedor → tentar Criar → toast "Selecione o devedor" aparece, nada gravado.
    2. **SC10 — UI guard credor (NOVO):** Mesmo fluxo, selecionar devedor mas NÃO selecionar credor (manter "— Sem credor" no select) → tentar Criar → toast "Selecione o credor" aparece, nada gravado em `contratos_dividas`. Console limpo.
    3. **SC11 — Service guard via console:** No DevTools console:
       ```js
       const { criarContrato } = await import("/src/services/contratos.js");
       await criarContrato({ devedor_id: null, credor_id: 1 }).catch(e => e.message);
       // Esperado: "criarContrato: devedor_id obrigatório"
       await criarContrato({ devedor_id: 1, credor_id: null }).catch(e => e.message);
       // Esperado: "criarContrato: credor_id obrigatório"
       ```
    4. **SC18 cumulativo — D-01 motor intocado:** `git log --since="7.13 ship" --name-only -- src/mr-3/mr-cobrancas/src/utils/devedorCalc.js src/mr-3/mr-cobrancas/src/services/pagamentos.js src/mr-3/mr-cobrancas/src/services/dividas.js` retorna ZERO commits.
    5. **SC19 — Build + tests verde:** Output do executor mostra `npm run build` PASS e `npm test` PASS (todos vitest tests, incluindo os 4 novos do plan 03 + testes pré-existentes da 7.13).
    6. **Smoke — caminho feliz:** Selecionar devedor + selecionar credor real do dropdown + Criar Contrato → contrato criado com sucesso, redirect para "+ Adicionar Documento" funciona idêntico ao baseline pré-7.14.
  </how-to-verify>
  <resume-signal>Type "approved" if SC9, SC10, SC11, SC18, SC19 + smoke happy path verdes. Type "issues: <details>" se algo falha.</resume-signal>
  <acceptance_criteria>
    - Gates automatizados (D-01 motor, scope clean, greps, schema intocado, build, test) todos PASS antes do checkpoint.
    - Usuário aprova SC9, SC10, SC11, SC18, SC19, smoke happy path.
    - `git status` clean entre execução e checkpoint (apenas 3 arquivos modificados).
  </acceptance_criteria>
  <done>
    Plan 07.14-03 SHIPPED dentro da phase 7.14. Defense in depth ativa em criação de contrato. D-pre-2 (nullable preservado), D-pre-7 (DetalheContrato UPDATE intocado), D-pre-10 (UI + service duplos), D-01 motor cumulativo — todos mantidos.
  </done>
</task>

</tasks>

<verification>

## Final verification (todos os gates)

```bash
cd src/mr-3/mr-cobrancas

# Gate 1 — UI guard credor presente
grep -n "Selecione o credor" src/components/NovoContrato.jsx
# Expected: 1 match em handleSalvar

# Gate 2 — Service guards presentes
grep -n "criarContrato: devedor_id obrigatório" src/services/contratos.js
grep -n "criarContrato: credor_id obrigatório" src/services/contratos.js
# Expected: 1 match cada

# Gate 3 — D-01 motor INTOCADO cumulativo (lição gate_motor_grep_use_name_only)
git log -1 --format="" --name-only HEAD | grep -E "(devedorCalc\.js|services/pagamentos\.js|services/dividas\.js)$"
# Expected: zero matches

# Gate 4 — Schema NÃO modificado (D-pre-2)
git diff --name-only HEAD~1 HEAD | grep -E "(\.sql$|migration)"
# Expected: zero matches

# Gate 5 — Scope strict (apenas 3 arquivos)
git diff --name-only HEAD~1 HEAD
# Expected exatamente:
#   src/mr-3/mr-cobrancas/src/components/NovoContrato.jsx
#   src/mr-3/mr-cobrancas/src/services/__tests__/criarContratoGuard.test.js
#   src/mr-3/mr-cobrancas/src/services/contratos.js

# Gate 6 — DetalheContrato.handleSalvar (linha 238) intocado (D-pre-7)
git diff HEAD~1 HEAD -- src/components/DetalheContrato.jsx
# Expected: empty (sem alterações)

# Gate 7 — Build + Test full
npm run build
npm test
# Expected: ambos PASS, 4 novos tests do describe "criarContrato — service guards" verdes
```
</verification>

<success_criteria>
- UI bloqueia submit em NovoContrato sem credor selecionado (toast "Selecione o credor")
- Service criarContrato throwa Error semântico se payload inválido (devedor_id ou credor_id null/undefined)
- 4 unit tests novos verdes em vitest, isolados em criarContratoGuard.test.js
- DetalheContrato.handleSalvar (UPDATE) intocado
- Schema permanece nullable (zero migrations adicionadas)
- D-01 motor (devedorCalc.js, pagamentos.js, dividas.js) intocado cumulativo
- npm run build + npm test PASS
- UAT humano confirma SC9, SC10, SC11, SC18, SC19 + smoke happy path
</success_criteria>

<output>
After completion, create `.planning/phases/07.14-blindagens-de-integridade-de-dados/07.14-03-SUMMARY.md` documenting:
- Diff exato aplicado em NovoContrato.jsx (1 linha) e contratos.js (2 linhas)
- Conteúdo do criarContratoGuard.test.js novo
- Resultado dos gates (build, test, greps)
- UAT cenários verdes (SC9/SC10/SC11/SC18/SC19/smoke)
- Confirmação invariantes (D-pre-2, D-pre-7, D-01 motor, schema)
- Sub-itens 1, 2, 4 da phase 7.14 — status atual (planejados em outros plans, não executados aqui)
</output>
