# Plan 07.14-02 — Remover +Nova Dívida — SUMMARY

**Phase:** 7.14 — Blindagens de Integridade de Dados
**Plan goal:** Remover botão "+ Nova Dívida" obsoleto pós-7.5/7.6/7.13 + DELETE arquivo `NovaDivida.jsx` (consumer único era ModuloDividas, removido). PRESERVAR `DividaForm.jsx` + `App.jsx:3947` callsite (D-pre-12 caminho legacy aceitável).
**Status:** SHIPPED LOCAL 2026-04-27
**Tasks:** 3 (2 commits + 1 verification gate)

## Commits no submódulo (2 atômicos)

| # | SHA | Mensagem |
|---|-----|----------|
| 02-01 | `8422922` | refactor: remove botão +Nova Dívida e view nova de ModuloDividas (D-pre-6) |
| 02-02 | `11069b9` | chore: DELETE src/components/NovaDivida.jsx (D-pre-6) |
| 02-03 | n/a | gate task — 7 shields verification, no commit |

**Plan baseline → HEAD:** `b6fbe20` → `11069b9` (primeira execução da phase)
**Plan diff:** 2 files, **440 linhas deletadas** (14 ModuloDividas + 426 NovaDivida)

## Files modificados

- `src/components/ModuloDividas.jsx` — 4 surgical edits:
  - L5: removed `import NovaDivida from "./NovaDivida.jsx";`
  - L41-42: removed `handleNovaDivida()` and `handleVoltarDaNova()` handlers
  - L58: removed `<Btn onClick={handleNovaDivida} ...>+ Nova Dívida</Btn>`
  - L92-99: removed `view === "nova"` block

## Files deletados

- `src/components/NovaDivida.jsx` — 426 lines (consumer único era ModuloDividas, agora removido)

## Files preservados (D-pre-12)

- `src/components/DividaForm.jsx` — consumer adicional em `App.jsx:3947` (drawer Pessoas)
- `src/App.jsx:3947` `<DividaForm>` callsite — caminho legacy aceitável (não cria órfã, herda contrato_id do contexto)

## Deviations registradas

**1. Layout cosmetic decision (D-pre-6 application):** Removendo o botão deixa o outer flex container com one child + `justify-content: space-between`. Decisão: leave the style alone (graceful degradation to flex-start) rather than expand surface. Documentado em Task 02-01 action.

**2. Pre-condition guard em Task 02-02:** Adicionado guard `grep "import NovaDivida"` returning zero matches BEFORE `git rm` para prevenir out-of-order execution from breaking the build.

**3. Backup file exclusion baked into shields:** `--exclude=App.13-04-backup.*` no cumulative grep shield para evitar false-positive em backup files contendo string literal `NovaDivida` (none found in current grep, defensive).

**4. `git rm` preferido sobre `rm`** para atomic staging consistency com o deletion commit.

**5. `<BASELINE_SHA>` placeholder** em shields rather than hard-coded SHA — executor resolveu em runtime (since 7.14 baseline `b6fbe20` foi fluid até primeiro commit lander).

## Lições aplicadas pre-execute

- `gate_motor_grep_use_name_only` (Shield 1 usa `--name-only`, não `--stat`)
- `grep_shield_strict_imports` (shields anchored em `^import .* from ["']...["']`)
- Backup files exclusion para evitar false positives em cumulative greps

## UAT resultados

| SC | Resultado | Nota |
|----|-----------|------|
| SC7 | **PASS** | Botão verde "+ Nova Dívida" no topo direito do Módulo Dívidas SUMIU. Listagem renderiza normal (header "Dívidas", chip "X em cobrança", filtros, tabela). Console limpo. |
| SC8 | **PASS** | DetalheContrato → "+ Adicionar Documento" continua funcionando idêntico ao pré-7.14. |
| Cross-check D-pre-12 | **PASS PRESERVADO** | Drawer Pessoas/Devedores → advair → aba Dívidas mostra "+ Nova Dívida" + DividaForm intactos (App.jsx:3947 não tocado). |

**Conclusão UAT:** Plan 02 trivial baixo risco entregue conforme planejado. Caminho legacy D-pre-12 preservado integralmente.

## Lessons learned

1. **Grep audit pré-insert revela consumers ocultos** — grep audit em sessão de discuss (2026-04-27) encontrou consumer adicional `DividaForm` em `App.jsx:3947` que briefing original tinha como "consumer único ModuloDividas". Revelou necessidade de D-pre-12 NEW para preservar caminho legacy aceitável. Lição: sempre executar grep audit antes de propor DELETE arquivos.

2. **Princípio anti-mid-flight** — quando UAT visual revelou que `App.jsx:3947` callsite contradiz spirit D-pre-6 ("dívida sempre via DetalheContrato"), decisão foi NÃO expandir surface da phase ativa. Backlog 7.14b criado para removal futuro após Phase 7.14 SHIP em prod + 1-2 dias de uso real. Lição transferível: scope changes mid-flight aumentam risco de regressão; defer to follow-up phase.

3. **Backup files exclusion em shields cumulativos** — projetos com arquivos `App.13-04-backup.{js,jsx}` no histórico precisam de `--exclude` em greps cumulativos para evitar false positives. Pattern reutilizável para outras phases.

## Próximas referências

- Phase 7.14b (backlog) — remover callsite legacy `App.jsx:3947` `<DividaForm>` após 7.14 SHIP em prod e usuário usar por 1-2 dias
