---
phase: 07.7-excluir-documento-individual
plan: 03
type: execute
wave: 3
depends_on:
  - 07.7-01
  - 07.7-02
files_modified:
  - src/mr-3 (bump do submodule pointer no repo pai branch master)
autonomous: false   # D-11: modo cansado — 3 pausas humanas nesta plan (#3 UAT, #4 diff consolidado, #5 bump commit). SEM PUSH.
requirements: []    # Gap closure — sem REQ-IDs formais. Contrato de verificação = SC-1..SC-5 em 07.7-CONTEXT.md §5.

must_haves:
  truths:
    - "SC-1: Excluir doc sem pagamentos em contrato com 2 docs → sucesso; contrato.valor_total e num_documentos recalculam (D-05 recalcularTotaisContrato); outro doc intocado; parcelas do doc excluído somem"
    - "SC-2: Excluir doc com ≥1 pagamentos_divida manual → toast vermelho bloqueante; doc e parcelas preservados no banco (D-03)"
    - "SC-3: Excluir doc com parcelas amortizadas por SP registrar_pagamento_contrato → toast vermelho (SP-generated via pagamentos_divida, D-06)"
    - "SC-4: Excluir ÚNICO doc do contrato → contrato vazio (num_documentos=0), sem crash, DetalheContrato renderiza normal, botão 'Adicionar documento' disponível (D-08)"
    - "SC-5: Excluir doc que está sendo editado (editingDocId === doc.id) → cleanup funciona, sem crash de render pós-delete (D-07)"
    - "PAUSA #3 (UAT): usuário reporta PASS/FAIL de cada SC-1..SC-5 no chat antes de avançar"
    - "PAUSA #4 (diff consolidado review): agente mostra 2 commits do submodule (git diff HEAD~2) + confirma motor Art.354 diff vazio antes de bumpar o pai"
    - "PAUSA #5 (bump commit review): agente confirma branch master + src/mr-3 stageado + mensagem EXATA antes do commit final no pai"
    - "SC-bump: 1 commit no pai branch master com subject EXATO `chore(07.7): bump submodule mr-3 — excluir documento individual (07.7-01 + 07.7-02)` (D-14)"
    - "SC-no-push: zero git push em toda a phase. git log origin/main..main ≥2 no submodule. git log origin/master..master ≥1 no pai."
    - "SC-no-tag: tag v1.4-phase7.7 NÃO é criada nesta phase — ação separada pós-UAT prod (padrão 7.5.1/7.6)"
    - "Flag 7.5.2 latente (D-13): se UAT reportar encargos stale em doc(s) restante(s), NÃO é regressão 7.7 — documentar mas não bloquear ship"
  artifacts:
    - path: "src/mr-3 (submodule pointer no pai)"
      provides: "Bump do ponteiro para o tip do submodule main (SHA do commit 07.7-02). Encapsula os 2 commits da phase 7.7 em 1 commit atômico no pai."
      contains: "bump submodule mr-3"
    - path: ".planning/phases/07.7-excluir-documento-individual/07.7-03-SUMMARY.md"
      provides: "Evidência: SHAs (3), resultado UAT (SC-1..SC-5), autorizações das 3 PAUSAs, invariante motor, zero push, próximos passos"
      contains: "SC-1|SC-2|SC-3|SC-4|SC-5"
  key_links:
    - from: "Task 1 (checkpoint UAT = PAUSA #3)"
      to: "localhost npm run dev + 5 cenários manuais SC-1..SC-5 reportados pelo usuário"
      via: "gate humano antes de qualquer bump do pai"
      pattern: "SC-1|SC-2|SC-3|SC-4|SC-5"
    - from: "Task 2 (checkpoint duplo gate = PAUSA #4 + PAUSA #5)"
      to: "commit bump submodule mr-3 no pai branch master"
      via: "PAUSA #4 diff consolidado review + PAUSA #5 bump commit review — SEM PUSH em nenhum momento"
      pattern: "PAUSA #4|PAUSA #5"
    - from: "Tag v1.4-phase7.7"
      to: "Ação separada do usuário pós-UAT prod — NÃO criar nesta plan"
      via: "padrão 7.5.1/7.6 — tag só após smoke test em produção"
      pattern: "NÃO criar tag"
---

<objective>
Fechamento da Phase 7.7: UAT manual em localhost dos 5 cenários SC-1..SC-5 (D-12) + duplo checkpoint humano no bump do submodule no repo pai. **SEM PUSH em qualquer momento**. **Tag `v1.4-phase7.7` NÃO é criada nesta phase** — ação separada pós-UAT de produção (padrão 7.5.1/7.6).

Estado esperado no início desta plan:
- Submodule `src/mr-3/mr-cobrancas` branch `main` com **2 commits locais** pendentes de push:
  - **Commit A (07.7-01):** `feat(07.7-01): service excluirDocumento em contratos.js` — 1 arquivo (`src/services/contratos.js`).
  - **Commit B (07.7-02):** `feat(07.7-02): botão excluir documento + handleExcluirDocumento em DetalheContrato` — 1 arquivo (`src/components/DetalheContrato.jsx`).
- Repo pai branch `master`: `src/mr-3` marcado como modified (bump pendente). Pode haver outros arquivos modificados de sessões anteriores (`.planning/STATE.md`, etc) — **NÃO** fazem parte desta phase, ficam unstaged.

Esta plan executa:

1. **Task 1 (checkpoint:human-verify = PAUSA #3):** UAT manual dos 5 cenários SC-1..SC-5 em localhost. Usuário executa e reporta PASS/FAIL de cada. Agente aguarda autorização explícita para avançar.

2. **Task 2 (checkpoint:human-verify com DUPLO gate = PAUSA #4 + PAUSA #5):**
   - **PAUSA #4:** agente mostra os 2 commits do submodule (`git log origin/main..main --stat` + diff consolidado `git diff HEAD~2`) + confirma motor Art.354 intocado. Aguarda `approved` / `bump it`.
   - **Passo intermediário:** cd pra raiz do pai, confirma branch `master`, `src/mr-3` modified.
   - **PAUSA #5:** reporta SHAs + mensagem EXATA proposta. Aguarda autorização SEPARADA.
   - **Commit bump no pai:** `git add src/mr-3 && git commit -m "chore(07.7): bump submodule mr-3 — excluir documento individual (07.7-01 + 07.7-02)"`.
   - Reporta SHAs finais + confirma zero push, zero tag.

Padrão idêntico ao seguido nas Phases 7.2 / 7.3 / 7.4 / 7.5 / 7.5.1 / 7.6 (D-14 de 07.7-CONTEXT.md + precedente 07.6-03-PLAN.md Task 2).

Os commits das Plans 07.7-01 e 07.7-02 NÃO são undone, amendados ou rebase-ados. Esta plan só **verifica + aprova (3x) + bumpa o pai**.

Purpose: Advogado faz smoke test em localhost dos 5 cenários críticos (incluindo edge cases D-07 render-after-delete e D-08 contrato vazio) e confirma que o motor Art.354 continua 9/9 verde. Commit do bump fica local no pai até o usuário decidir push em sessão separada (padrão 7.5.1 — push de 2026-04-23 pós-UAT).

Output:
- Log de execução dos 5 cenários UAT (reportado inline no chat, resumido no SUMMARY).
- 1 commit local no pai branch `master` com mensagem EXATA `chore(07.7): bump submodule mr-3 — excluir documento individual (07.7-01 + 07.7-02)` e apenas `src/mr-3` stageado.
- SUMMARY registrando: SHAs do submodule (x2) + SHA do pai (x1) + UAT result + 3 autorizações humanas + invariante motor + confirmação sem push + sem tag.
- **NENHUMA TAG criada.** Tag `v1.4-phase7.7` em ação separada pós-UAT prod.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07.7-excluir-documento-individual/07.7-CONTEXT.md

<!-- 2 plans predecessoras (Tasks dependem de ambas completas): -->
@.planning/phases/07.7-excluir-documento-individual/07.7-01-PLAN.md
@.planning/phases/07.7-excluir-documento-individual/07.7-02-PLAN.md

<!-- TEMPLATE literal do padrão duplo-checkpoint UAT+bump+SEM PUSH (seguir estrutura passo-a-passo): -->
@.planning/phases/07.6-parcelas-n-grande/07.6-03-PLAN.md

<!-- Precedentes adicionais do mesmo padrão commit+bump+SEM PUSH: -->
<!-- .planning/phases/07.2-excluir-contrato/07.2-01-PLAN.md Task 3 (precursor, 2 pausas) -->
<!-- .planning/phases/07.5.1-sincronizacao-saldosmap-ux-input-valor/07.5.1-01-PLAN.md Task 3 (template 10-passo) -->

<interfaces>
<!-- Pré-condições verificáveis desta plan. Executor checa ANTES de abrir Task 1. -->

**Pré-condições (se FALHAREM, abortar plan e reportar ao usuário):**

```bash
cd src/mr-3/mr-cobrancas
git rev-parse --abbrev-ref HEAD                                       # esperado: main
git log -2 --format="%H %s" origin/main..main
# esperado EXATAMENTE 2 linhas, mais recente primeiro:
# <SHA2>  feat(07.7-02): botão excluir documento + handleExcluirDocumento em DetalheContrato
# <SHA1>  feat(07.7-01): service excluirDocumento em contratos.js

git diff HEAD~2 -- src/utils/devedorCalc.js src/services/pagamentos.js src/services/dividas.js
# esperado: vazio (invariante motor D-01 de ponta a ponta nas 2 plans anteriores)
```

Se o output não bater EXATAMENTE → PARAR e reportar `git log -5 --format="%H %s" origin/main..main` completo pro usuário diagnosticar (e.g., plans 07.7-01 ou 07.7-02 não foram commitadas corretamente).

**Estado do pai (repo master) ANTES desta plan:**
```bash
cd <raiz do pai>
git rev-parse --abbrev-ref HEAD                                       # esperado: master
git status --short
# esperado: `src/mr-3` aparece como modified (bump pendente)
# ATENÇÃO: podem existir entries pré-existentes (.planning/STATE.md, .planning/ROADMAP.md
# modificados ou deletados em sessões anteriores — vide gitStatus inicial do planning_context).
# Essas entries NÃO fazem parte desta phase. O commit do Passo 8 adiciona APENAS `src/mr-3`.
```

**Textos EXATOS das PAUSAs (replicar literal no chat quando cada gate for atingido):**

---

**PAUSA #3 (UAT — Task 1, Passo 7):**
> ```
> ## UAT Phase 7.7 — 5 cenários SC-1..SC-5 em localhost
> Dev server: <URL capturada do npm run dev>
>
> Por favor execute os 5 cenários abaixo no browser e reporte PASS / FAIL de cada.
> Todos devem passar antes de eu prosseguir para o bump do pai.
>
> ### SC-1 — Excluir doc SEM pagamentos, contrato com 2 docs (happy path + recalcularTotaisContrato)
> 1. Criar contrato novo (ou abrir existente sem pagamentos).
> 2. Adicionar 2 documentos (NF R$1000 3x + NF R$2000 2x — valores arbitrários).
> 3. Confirmar: contrato.valor_total = R$3000, num_documentos = 2, num_parcelas_total = 5 (via UI ou SQL Editor).
> 4. Expandir card do 1º doc. Clicar "Excluir documento" (botão vermelho outline, ao lado de "Editar parcelas").
> 5. Confirmar no window.confirm.
> 6. Esperado:
>    - Toast verde "Documento excluído com sucesso".
>    - Card do 1º doc some da tela.
>    - 2º doc CONTINUA visível, intacto.
>    - Após refresh automático (onCarregarTudo): contrato.valor_total = R$2000, num_documentos = 1, num_parcelas_total = 2.
>    - Usuário continua na tela do contrato (NÃO volta pra lista — SC-4).
> Pass/Fail:
>
> ### SC-2 — Excluir doc com ≥1 pagamento MANUAL registrado (pre-check D-03)
> 1. Criar contrato + 1 documento com 3 parcelas.
> 2. Expandir doc, abrir uma parcela (DetalheDivida) e registrar 1 pagamento manual parcial.
> 3. Voltar pra DetalheContrato, expandir card do doc.
> 4. Clicar "Excluir documento" → Confirmar.
> 5. Esperado:
>    - Toast vermelho com texto EXATO "Este documento possui pagamentos registrados — não pode ser excluído. Exclua os pagamentos primeiro."
>    - Card do doc CONTINUA na tela. Parcelas preservadas. Nenhum DELETE executou.
>    - (Opcional: conferir no SQL Editor `SELECT * FROM dividas WHERE documento_id=:id` retorna as 3 parcelas.)
> Pass/Fail:
>
> ### SC-3 — Excluir doc com parcelas amortizadas por SP registrar_pagamento_contrato (D-06)
> 1. Criar contrato + 1 documento com 3 parcelas.
> 2. Registrar pagamento NO NÍVEL DO CONTRATO (botão "Registrar Pagamento" no DetalheContrato — SP registrar_pagamento_contrato dispara e amortiza as parcelas automaticamente, escrevendo em pagamentos_divida).
> 3. Expandir card do doc.
> 4. Clicar "Excluir documento" → Confirmar.
> 5. Esperado:
>    - Toast vermelho com texto EXATO "Este documento possui pagamentos registrados — não pode ser excluído. Exclua os pagamentos primeiro." (mesma mensagem de SC-2 — pre-check único em pagamentos_divida captura SP-generated; D-06 confirmado).
>    - Card do doc preservado.
> Pass/Fail:
>
> ### SC-4 — Excluir ÚNICO doc do contrato (contrato fica vazio, D-08)
> 1. Criar contrato + 1 único documento.
> 2. Excluir o documento. Confirmar no window.confirm.
> 3. Esperado:
>    - Toast verde "Documento excluído com sucesso".
>    - Usuário CONTINUA na DetalheContrato (NÃO navega pra lista — diferença vs excluirContrato).
>    - DetalheContrato renderiza normal:
>      * Seção "Documentos" vazia (ou com mensagem "Nenhum documento adicionado ainda" se houver).
>      * Botão "Adicionar documento" disponível.
>      * Contrato header com valor_total = R$0, num_documentos = 0.
>    - Sem crash / sem erro no console do browser.
> Pass/Fail:
>
> ### SC-5 — Excluir documento cujo editingDocId aponta pra ele — cleanup D-07
> **Setup:** Criar contrato + 2 documentos sem pagamentos (ou usar existente). Expandir card do doc A, clicar "Editar parcelas" (editingDocId vira A.id, abre TabelaParcelasEditaveis inline).
>
> **Cenário:** Sem fechar o editor, tentar clicar em "Excluir Documento" do MESMO card A.
> - Se o botão estiver visível durante edição: clicar direto.
> - Se o botão estiver disabled/hidden durante edição (comportamento atual: o JSX usa `{editingDocId !== doc.id && (...)}`, então o botão NÃO renderiza no card em edição): **cancelar o editor primeiro** (botão "Cancelar" da TabelaParcelasEditaveis) — isso seta editingDocId=null — depois clicar "Excluir documento". Neste path, o cleanup D-07 para `editingDocId` não é exercitado diretamente, mas o cleanup para `expandedDoc` é (card A ainda está expandido quando clicamos Excluir).
>
> **Esperado:**
> - Toast verde "Documento excluído com sucesso".
> - Após o delete, sem crash de render: DetalheContrato re-renderiza limpo após onCarregarTudo.
> - `editingDocId` === null (limpo pelo handler antes do service call — verificado no grep do plan 07.7-02) OU continua null se já estava.
> - `expandedDoc` === null (limpo pelo handler antes do service call se apontava pro doc deletado).
> - Console do browser: zero erros React, zero warnings novos.
>
> **Nota:** O cleanup de `editingDocId` em D-07 é defensivo (blinda contra mudanças futuras no JSX onde o botão fique visível durante edição). Hoje é coberto por grep estático no plan 07.7-02; UAT runtime cobre o path `expandedDoc` via este SC-5.
> Pass/Fail:
>
> ---
> Quando responder, confirme também:
> - Nenhum erro no console do browser (devtools > Console).
> - Nenhum warning React novo (ignorar warnings pré-existentes).
> - Regressão 9/9: `cd src/mr-3/mr-cobrancas && npm run test:regressao` exit 0 (já verificado nos gates determinísticos, mas pode re-rodar).
>
> **Nota sobre D-13 (flag 7.5.2 latente):** se você notar que os encargos (multa/juros/honorários/correção) dos doc(s) restante(s) parecem stale pós-delete (e.g., valores financeiros não batem com o esperado), **isso é pendência pré-existente da Phase 7.5.2, não regressão da 7.7**. Anote mas não bloqueia ship.
> ```

---

**PAUSA #4 (Task 2, Passo 1):**
> ```
> ## PAUSA #4 (07.7-03) — diff consolidado review antes do bump no pai
>
> UAT localhost: 5 cenários SC-1..SC-5 reportados pelo usuário na Task 1.
>
> Gates determinísticos:
> - Regressão 9/9: PASS
> - Build: exit 0
> - Motor Art.354 (devedorCalc.js + pagamentos.js + dividas.js) diff HEAD~2: VAZIO ✓ (invariante D-01)
>
> Submodule src/mr-3/mr-cobrancas @ main tem 2 commits locais pendentes de push:
> - <SHA2_short> feat(07.7-02): botão excluir documento + handleExcluirDocumento em DetalheContrato (1 arquivo: src/components/DetalheContrato.jsx)
> - <SHA1_short> feat(07.7-01): service excluirDocumento em contratos.js (1 arquivo: src/services/contratos.js)
>
> Diff consolidado dos 2 commits mostrado acima.
>
> Autoriza bumpar o submodule no pai branch `master`?
> Responda **'approved'** ou **'bump it'** para prosseguir.
> Qualquer outra coisa cancela o bump — os 2 commits do submodule continuam intactos, apenas
> o ponteiro no pai fica pendente (pode bumpar em sessão futura).
> ```

---

**PAUSA #5 (Task 2, Passo 3):**
> ```
> ## PAUSA #5 (07.7-03) — bump commit review antes do commit final no pai
>
> Commits do submodule autorizados (PAUSA #4 OK).
> Pai (raiz do repo) em branch `master`. Status:
> - `src/mr-3` modified (bump do ponteiro pendente) ✓
> - (Outros arquivos pré-existentes podem aparecer como modified — NÃO serão stageados,
>    ficam unstaged conforme planning_context.)
>
> SHAs a apontar:
> - <SHA1_short> feat(07.7-01) + <SHA2_short> feat(07.7-02) (tip: <SHA2_full>)
>
> Autoriza commit no pai com mensagem EXATA:
> `chore(07.7): bump submodule mr-3 — excluir documento individual (07.7-01 + 07.7-02)`
> ?
>
> Responda **'bump it'** ou **'approved'** para prosseguir.
> Esta autorização é SEPARADA da PAUSA #4 — qualquer outra coisa cancela o bump
> (submodule continua commitado localmente, só o bump no pai fica pendente).
> ```

---

**Mensagem EXATA do commit do pai (D-14):**
```
chore(07.7): bump submodule mr-3 — excluir documento individual (07.7-01 + 07.7-02)

Submodule src/mr-3 @ branch main: 2 commits cirúrgicos da Phase 7.7 (gap closure descoberto em uso real pelo advogado — até hoje corrigir doc cadastrado errado exigia apagar e recriar contrato inteiro).

- feat(07.7-01): service excluirDocumento em contratos.js — espelho estrutural de excluirContrato (7.2) com filtro documento_id + recalcularTotaisContrato OBRIGATÓRIO pós-delete.
- feat(07.7-02): botão excluir documento + handleExcluirDocumento em DetalheContrato — vermelho outline sibling do "Editar parcelas" no card expandido; cleanup D-07 ANTES do service call; NÃO chama onVoltar (D-08 — contrato vazio é estado válido).

Pre-check único em pagamentos_divida bloqueia delete com pagamentos registrados (manuais ou SP-generated via registrar_pagamento_contrato, D-06). Ordem DELETE invariante: dividas → documentos_contrato (D-04). Pós-delete recalcularTotaisContrato(contratoId) mantém contrato.valor_total / num_documentos / num_parcelas_total coerentes (D-05).

Motor Art.354 (devedorCalc.js + pagamentos.js + dividas.js) LITERALMENTE intocado — invariante estrutural D-01 preservada nos 2 commits. Regressão 9/9 verde. Build exit 0. UAT localhost dos 5 cenários SC-1..SC-5 (CONTEXT D-12) reportado pelo usuário.

Evento forense em contratos_historico deferido (D-09, padrão 7.5.1). Flag 7.5.2 latente (encargos em doc(s) restante(s) pós-delete) permanece pendência pré-existente independente.

Tag v1.4-phase7.7 pendente — só após UAT de produção em ação separada (padrão 7.5.1/7.6).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

</interfaces>
</context>

<tasks>

<!--
  2 tasks em 1 wave SEQUENCIAL:
  - Task 1: UAT manual dos 5 cenários SC-1..SC-5 + gates regressão/build/motor. Bloqueia até usuário confirmar.
  - Task 2: Duplo checkpoint (PAUSA #4 diff consolidado + PAUSA #5 bump review) + commit final no pai.
  Task 2 SÓ executa após Task 1 autorizada. Task 2 tem 2 gates humanos próprios.
  NENHUMA das 2 tasks faz push. Tag é ação separada do usuário, fora desta plan.
-->

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: PAUSA #3 — UAT localhost dos 5 cenários SC-1..SC-5 + gates determinísticos (regressão + build + motor diff)</name>
  <files>
    (nenhum arquivo modificado nesta task — é UAT manual + gates determinísticos)
  </files>

  <what-built>
    Plans 07.7-01 e 07.7-02 já commitaram localmente no submodule `src/mr-3/mr-cobrancas` branch `main`:
    - **Commit A (07.7-01):** `feat(07.7-01): service excluirDocumento em contratos.js` — 1 arquivo.
    - **Commit B (07.7-02):** `feat(07.7-02): botão excluir documento + handleExcluirDocumento em DetalheContrato` — 1 arquivo.

    Esta Task 1 executa os 4 gates determinísticos + UAT manual (D-12):
    1. **Gate regressão:** `npm run test:regressao` 9/9 no submodule (motor Art.354 não tocado).
    2. **Gate build:** `npm run build` exit 0.
    3. **Gate invariante motor:** `git diff HEAD~2 -- src/utils/devedorCalc.js src/services/pagamentos.js src/services/dividas.js` → vazio.
    4. **Gate escopo:** `git diff --name-only HEAD~2` retorna APENAS `src/services/contratos.js` e `src/components/DetalheContrato.jsx` (nenhum outro arquivo mexido pela phase).
    5. **UAT manual (5 cenários D-12):** SC-1 happy path, SC-2 pre-check manual, SC-3 pre-check SP-generated, SC-4 contrato vazio, SC-5 cleanup D-07.

    Agente reporta resultado dos gates + apresenta UAT e **aguarda confirmação explícita do usuário (PAUSA #3)** antes de abrir Task 2.
  </what-built>

  <read_first>
    - `.planning/phases/07.7-excluir-documento-individual/07.7-CONTEXT.md` §4 D-01 (INV motor), D-03 (pre-check), D-06 (pagamentos_contrato excluído do pre-check), D-07 (UI cleanup), D-08 (contrato vazio OK), D-11 (5 pausas — esta é a #3), D-12 (SC-1..SC-5 EXATOS), D-13 (flag 7.5.2 latente), §5 Success Criteria
    - `.planning/phases/07.7-excluir-documento-individual/07.7-01-PLAN.md` + `07.7-02-PLAN.md` — confirmar SHAs esperados dos commits no submodule
    - `.planning/phases/07.6-parcelas-n-grande/07.6-03-PLAN.md` Task 1 "PARTE A" e "PARTE B" — **template literal** do padrão de gates determinísticos pré-UAT + formato do prompt de UAT
  </read_first>

  <how-to-verify>

### PARTE A — Gates determinísticos (agente executa ANTES de abrir UAT manual)

**Pré-requisito:** cwd = raiz do repo pai.

**Passo 1 — Confirmar branch e 2 commits locais no submodule main:**
```bash
cd src/mr-3/mr-cobrancas
git rev-parse --abbrev-ref HEAD
# esperado: main

git log -2 --format="%H %s" origin/main..main
# esperado: EXATAMENTE 2 linhas, mais recente primeiro:
# <SHA2> feat(07.7-02): botão excluir documento + handleExcluirDocumento em DetalheContrato
# <SHA1> feat(07.7-01): service excluirDocumento em contratos.js
```

Se output não bater: **PARAR** e reportar `git log -5 --format="%H %s" origin/main..main` completo.

**Passo 2 — Gate regressão (bloqueante, D-01 invariante):**
```bash
cd src/mr-3/mr-cobrancas
npm run test:regressao
```
Esperado: `9/9 PASS`. Se falhar, **PARAR** — diagnóstico:
```bash
git diff HEAD~2 -- src/utils/devedorCalc.js src/services/pagamentos.js src/services/dividas.js
```
Se qualquer byte de diff → motor foi tocado indevidamente em 07.7-01 ou 07.7-02; reverter via `git revert <SHA_ofensor>` ou solicitar rollback manual do usuário antes de prosseguir.

**Passo 3 — Gate build (bloqueante):**
```bash
cd src/mr-3/mr-cobrancas
npm run build
```
Exit 0 obrigatório. Se falhar, reportar saída completa. Causas prováveis em ordem:
- Import quebrado em DetalheContrato.jsx (`excluirDocumento` não exportado pelo service? Verificar `grep -c "export async function excluirDocumento" src/services/contratos.js` = 1).
- JSX mal fechado na região do botão novo (linha ~765).
- Prop `style` não aceita pelo Btn (fallback documentado em 07.7-02 action: usar `<span style={{marginRight:8}}>` wrapper).

**Passo 4 — Gate invariante motor Art.354 (bloqueante, D-01):**
```bash
cd src/mr-3/mr-cobrancas
git diff HEAD~2 -- src/utils/devedorCalc.js
git diff HEAD~2 -- src/services/pagamentos.js
git diff HEAD~2 -- src/services/dividas.js
```
**Esperado:** todos vazios (0 bytes). Qualquer byte → reportar EXATAMENTE o diff ao usuário e pedir orientação (revert do commit ofensor, ou confirmar se é whitespace acidental — default é bloquear).

**Passo 5 — Gate escopo (só 2 arquivos da phase):**
```bash
cd src/mr-3/mr-cobrancas
git diff --name-only HEAD~2
```
Esperado: EXATAMENTE 2 linhas:
- `src/components/DetalheContrato.jsx`
- `src/services/contratos.js`

Ordem pode variar. Qualquer outro arquivo → reportar e aguardar confirmação.

**Passo 6 — Sanidade grep pós-commit (verificação do trabalho das 2 plans anteriores):**
```bash
cd src/mr-3/mr-cobrancas

# Plan 07.7-01 artefatos:
grep -c "export async function excluirDocumento" src/services/contratos.js                 # esperado: 1
grep -c "export async function excluirContrato" src/services/contratos.js                  # esperado: 1 (preservada da 7.2)
grep -c "await recalcularTotaisContrato(contratoId)" src/services/contratos.js             # esperado: ≥2 (1 existente em adicionarDocumento + 1 novo em excluirDocumento)
grep -cE 'documento_id=eq' src/services/contratos.js                                        # esperado: ≥3 (lookup + listar parcelas + DELETE dividas)
grep -c "Este documento possui pagamentos registrados" src/services/contratos.js           # esperado: 1

# Plan 07.7-02 artefatos:
grep -c "async function handleExcluirDocumento" src/components/DetalheContrato.jsx         # esperado: 1
grep -c 'window.confirm("Tem certeza? Esta ação não pode ser desfeita.")' src/components/DetalheContrato.jsx    # esperado: 2 (7.2 + 7.7)
grep -c "excluirDocumento" src/components/DetalheContrato.jsx                              # esperado: ≥3 (import + chamada + handler name)
grep -c "excluindoDocumentoId, setExcluindoDocumentoId" src/components/DetalheContrato.jsx # esperado: 1

# Ordem crítica D-07 (cleanup ANTES do service call) — awk restrito ao scope da função nova:
awk '/async function handleExcluirDocumento/,/^  \}/' src/components/DetalheContrato.jsx | \
  grep -nE 'setEditingDocId\(null\)|setExpandedDoc\(null\)|await excluirDocumento'
# esperado: 3 linhas; as 2 do cleanup aparecem ANTES da linha com `await excluirDocumento`.

# Garantia NEGATIVA: handleExcluirDocumento não chama onVoltar (D-08):
awk '/async function handleExcluirDocumento/,/^  \}/' src/components/DetalheContrato.jsx | \
  grep -cE 'onVoltar\s*\('
# esperado: 0
```

Se QUALQUER gate falhar → reportar ao usuário e **NÃO** prosseguir para Parte B.

---

### PARTE B — UAT manual dos 5 cenários SC-1..SC-5 (usuário executa no browser localhost)

**Passo 7 — Iniciar dev server:**
```bash
cd src/mr-3/mr-cobrancas
npm run dev
```
(Rodar em background — Bash tool com `run_in_background: true`. Capturar URL do output — tipicamente `http://localhost:5173/`.)

**Passo 8 — Reportar os 5 cenários ao usuário com formato EXATO (texto integral da PAUSA #3, ver <interfaces> acima — replicar literal substituindo `<URL capturada>` pelo URL real do dev server).**

**AGUARDAR resposta do usuário.** NÃO prosseguir para Task 2 sem confirmação explícita de que os 5 cenários passaram (ou que falhas são conhecidas/aceitáveis).

Diagnóstico de falhas no UAT:
- **SC-1 FAIL (valor_total não recalcula):** verificar `grep -n "await recalcularTotaisContrato(contratoId)" src/services/contratos.js` dentro de `excluirDocumento`. Se ausente, bug no 07.7-01.
- **SC-2/SC-3 FAIL (pre-check não dispara):** verificar `grep -n 'pagamentos_divida' src/services/contratos.js` dentro de excluirDocumento. Se o pre-check foi pulado, bug no 07.7-01.
- **SC-4 FAIL (navega pra lista):** verificar handler em DetalheContrato.jsx; se contém `onVoltar()`, bug no 07.7-02 (violação D-08).
- **SC-5 FAIL (crash pós-delete):** verificar ordem textual do cleanup D-07 no handler. Se `setExpandedDoc(null)` vem DEPOIS do `await excluirDocumento`, bug no 07.7-02.
- **Flag 7.5.2 latente (D-13):** se o usuário reportar "encargos do doc restante parecem estranhos", **NÃO é regressão da 7.7** — é pendência pré-existente da 7.5.2 (memory: `project_phase752_backlog_encargos.md`). Documentar no SUMMARY, não bloqueia ship.

Em qualquer FAIL bloqueante (não latente): reverter o commit ofensor via `git revert` no submodule e NÃO prosseguir; abrir novo ciclo.

  </how-to-verify>

  <acceptance_criteria>
    - `cd src/mr-3/mr-cobrancas && git log -2 --format=%s origin/main..main` retorna EXATAMENTE 2 linhas:
      1. `feat(07.7-02): botão excluir documento + handleExcluirDocumento em DetalheContrato`
      2. `feat(07.7-01): service excluirDocumento em contratos.js`
    - `cd src/mr-3/mr-cobrancas && npm run test:regressao` exit 0 (9/9 PASS)
    - `cd src/mr-3/mr-cobrancas && npm run build` exit 0
    - `cd src/mr-3/mr-cobrancas && git diff HEAD~2 -- src/utils/devedorCalc.js` retorna **vazio** (INV motor D-01)
    - `cd src/mr-3/mr-cobrancas && git diff HEAD~2 -- src/services/pagamentos.js` retorna **vazio** (INV motor D-01)
    - `cd src/mr-3/mr-cobrancas && git diff HEAD~2 -- src/services/dividas.js` retorna **vazio** (INV motor D-01)
    - `cd src/mr-3/mr-cobrancas && git diff --name-only HEAD~2` retorna EXATAMENTE 2 arquivos (contratos.js + DetalheContrato.jsx)
    - Todos os greps do Passo 6 retornam os contadores esperados
    - Usuário reportou resultado dos 5 cenários SC-1..SC-5 no chat (texto contendo "SC-1", "SC-2", "SC-3", "SC-4", "SC-5" ou equivalente) e indicou aprovação para prosseguir
    - **NENHUM** comando `git push` executado em Parte A nem durante Parte B
    - Texto do chat contém marcador `PAUSA #3` ou equivalente referenciando UAT Phase 7.7
  </acceptance_criteria>

  <resume-signal>
    Para prosseguir:
    - **`approved`** / **`next`** / **`ship it`** → UAT passou, agente abre Task 2 (PAUSA #4 + PAUSA #5).
    - **`FAIL SC-N: descrição`** → agente investiga, aplica fix local se possível (re-inicia gate de regressão+build+motor), ou solicita novo ciclo de plan se fix não-trivial.
    - **`defer 7.5.2: <detalhe>`** → user aceita flag latente D-13 (encargos stale), agente anota no SUMMARY e prossegue pra Task 2.
  </resume-signal>

  <done>
    Gates determinísticos PASS (regressão 9/9 + build exit 0 + motor diff vazio + escopo 2 arquivos). UAT dos 5 cenários SC-1..SC-5 reportado pelo usuário via PAUSA #3 e aprovado explicitamente. Safe para avançar para Task 2 (PAUSA #4 + #5 + commit bump no pai).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: PAUSA #4 (diff consolidado review) + PAUSA #5 (bump commit review) + commit bump submodule no pai branch master (SEM PUSH)</name>
  <files>
    src/mr-3 (submodule pointer no repo pai — bump)
  </files>

  <what-built>
    Após UAT da Task 1 autorizado (PAUSA #3), executa o protocolo duplo-checkpoint do padrão 7.2/7.3/7.4/7.5/7.5.1/7.6 (D-14 + D-11):

    - **PAUSA #4:** mostrar os 2 commits do submodule (`git log origin/main..main --stat` + diff consolidado via `git diff HEAD~2 -- src/services/contratos.js src/components/DetalheContrato.jsx`) + confirmar motor intocado. Aguardar autorização explícita.
    - **Passo intermediário:** cd pra raiz do pai, confirmar `master` + `src/mr-3` modified.
    - **PAUSA #5:** reportar SHAs + mensagem EXATA do commit proposto. Aguardar autorização SEPARADA.
    - **Commit único no pai:** `git add src/mr-3 && git commit` com mensagem EXATA da D-14.
    - Reportar SHA final + confirmação zero push + próximos passos (push + tag em ação separada).

    NENHUM dos commits do submodule é undone, amendado ou rebase-ado nesta task.
  </what-built>

  <read_first>
    - `.planning/phases/07.7-excluir-documento-individual/07.7-CONTEXT.md` §4 D-11 (pausa #4 e #5), D-14 (mensagem EXATA + push separado + tag separada)
    - `.planning/phases/07.6-parcelas-n-grande/07.6-03-PLAN.md` Task 2 Passos 1-7 — **template literal** PAUSA #1 (equivale nossa PAUSA #4) + PAUSA #2 (equivale nossa PAUSA #5) + commit bump
    - `.planning/phases/07.5.1-sincronizacao-saldosmap-ux-input-valor/07.5.1-01-PLAN.md` Task 3 — precedente do 10-passo template
    - `.planning/STATE.md` `last_activity` — confirma padrão "commits locais, sem push" desde 7.1
  </read_first>

  <how-to-verify>

**Passo 1 — PAUSA #4 (checkpoint pré-bump, diff consolidado review dos 2 commits do submodule):**

Rodar e mostrar ao usuário:
```bash
cd src/mr-3/mr-cobrancas
git status --short
git log origin/main..main --stat
git log -2 --format="=== %h %s ===" origin/main..main

git diff HEAD~2 -- src/services/contratos.js
git diff HEAD~2 -- src/components/DetalheContrato.jsx

git diff HEAD~2 -- src/utils/devedorCalc.js        # DEVE SER VAZIO
git diff HEAD~2 -- src/services/pagamentos.js      # DEVE SER VAZIO
git diff HEAD~2 -- src/services/dividas.js         # DEVE SER VAZIO
```

Capturar SHAs:
```bash
SHA1=$(git log origin/main..main --reverse --format=%H | head -1)     # feat(07.7-01)
SHA2=$(git log origin/main..main --format=%H | head -1)               # feat(07.7-02) — tip
SHA1_short=$(echo $SHA1 | cut -c1-7)
SHA2_short=$(echo $SHA2 | cut -c1-7)
```

Reportar (replicar texto EXATO da PAUSA #4 do <interfaces> acima, substituindo `<SHA1_short>` e `<SHA2_short>`).

**AGUARDAR resposta do usuário.** NÃO prosseguir sem `approved` / `bump it` explícito.

Se usuário apontar correção nos commits:
- Correção no 07.7-02 (tip): preferível 3º commit de correção (amend quebra SHA já reportado). Re-rodar Task 1 gates + PAUSA #4.
- Correção no 07.7-01: 3º commit de correção preferível (rebase interativo destrutivo, requer autorização separada).

---

**Passo 2 — Após PAUSA #4 autorizada: cd pra raiz do repo pai e verificar estado:**

```bash
cd ../../..                                                # volta pra raiz do pai (mr-cobrancas-vercel_1/mr-cobrancas)
pwd                                                        # confirmar path do pai
git rev-parse --abbrev-ref HEAD                           # esperado: master
git status --short
```

Esperado:
- `pwd` imprime path do pai (raiz do repo parent).
- Branch = `master`.
- `git status` mostra `src/mr-3` como modified (bump pendente). **Pode** listar entries pré-existentes (`.planning/STATE.md`, `.planning/ROADMAP.md` etc — vide gitStatus inicial do planning_context). Essas NÃO fazem parte desta phase.

**IMPORTANTE (hygiene do commit):** O próximo `git add src/mr-3` adiciona APENAS o bump. **NÃO** usar `git add -A` / `git add .` / `git add -u`. Outros arquivos modificados ficam unstaged e NÃO entram no commit.

---

**Passo 3 — PAUSA #5 (checkpoint pré-commit do pai, SEPARADA da PAUSA #4):**

Reportar (replicar texto EXATO da PAUSA #5 do <interfaces> acima, substituindo `<SHA1_short>`, `<SHA2_short>`, `<SHA2_full>`).

**AGUARDAR resposta do usuário.** NÃO prosseguir sem `bump it` / `approved` explícito. A autorização da PAUSA #4 **NÃO** cobre a PAUSA #5 (D-11 — 2 gates distintos).

---

**Passo 4 — Após PAUSA #5 autorizada: commit do bump no pai:**

```bash
# Continuamos na raiz do pai.
git add src/mr-3                                           # APENAS o bump, nada mais
git status --short                                         # confirmar SOMENTE `M src/mr-3` stageado (outros ficam unstaged)

git commit -m "$(cat <<'EOF'
chore(07.7): bump submodule mr-3 — excluir documento individual (07.7-01 + 07.7-02)

Submodule src/mr-3 @ branch main: 2 commits cirúrgicos da Phase 7.7 (gap closure descoberto em uso real pelo advogado — até hoje corrigir doc cadastrado errado exigia apagar e recriar contrato inteiro).

- feat(07.7-01): service excluirDocumento em contratos.js — espelho estrutural de excluirContrato (7.2) com filtro documento_id + recalcularTotaisContrato OBRIGATÓRIO pós-delete.
- feat(07.7-02): botão excluir documento + handleExcluirDocumento em DetalheContrato — vermelho outline sibling do "Editar parcelas" no card expandido; cleanup D-07 ANTES do service call; NÃO chama onVoltar (D-08 — contrato vazio é estado válido).

Pre-check único em pagamentos_divida bloqueia delete com pagamentos registrados (manuais ou SP-generated via registrar_pagamento_contrato, D-06). Ordem DELETE invariante: dividas → documentos_contrato (D-04). Pós-delete recalcularTotaisContrato(contratoId) mantém contrato.valor_total / num_documentos / num_parcelas_total coerentes (D-05).

Motor Art.354 (devedorCalc.js + pagamentos.js + dividas.js) LITERALMENTE intocado — invariante estrutural D-01 preservada nos 2 commits. Regressão 9/9 verde. Build exit 0. UAT localhost dos 5 cenários SC-1..SC-5 (CONTEXT D-12) reportado pelo usuário.

Evento forense em contratos_historico deferido (D-09, padrão 7.5.1). Flag 7.5.2 latente (encargos em doc(s) restante(s) pós-delete) permanece pendência pré-existente independente.

Tag v1.4-phase7.7 pendente — só após UAT de produção em ação separada (padrão 7.5.1/7.6).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"

git log -1 --format=%H                                    # capturar SHA pai full
git log -1 --format=%h                                    # SHA pai short
```

---

**Passo 5 — Reportar SHAs finais + estado "zero push" + próximos passos:**

Formato EXATO no chat:
```
## Phase 7.7 shipped LOCALLY (SEM PUSH)

Submodule src/mr-3/mr-cobrancas @ main:
- <SHA1_short> feat(07.7-01): service excluirDocumento em contratos.js
- <SHA2_short> feat(07.7-02): botão excluir documento + handleExcluirDocumento em DetalheContrato

Parent repo master:
- <SHA_pai_short> chore(07.7): bump submodule mr-3 — excluir documento individual (07.7-01 + 07.7-02)

Invariante motor Art.354: git diff HEAD~2 -- src/utils/devedorCalc.js src/services/pagamentos.js src/services/dividas.js → VAZIO. Regressão 9/9 preservada. Build exit 0.

UAT localhost 5 cenários SC-1..SC-5: reportados pelo usuário na Task 1.

3 autorizações humanas registradas: PAUSA #3 (UAT), PAUSA #4 (diff consolidado), PAUSA #5 (bump commit).
(Pausas #1 e #2 já autorizadas nos plans 07.7-01 e 07.7-02 respectivamente.)

Próximos passos (ação separada do usuário, NÃO executar agora):
1. **Push quando autorizar:** `cd src/mr-3/mr-cobrancas && git push origin main` + `cd <raiz_pai> && git push origin master` (NESSA ORDEM — submodule primeiro, pai depois).
2. **Deploy Vercel:** auto-deploy após push do pai (cache headers da 7.4 garantem F5 pós-deploy).
3. **UAT produção:** testar exclusão em mrcobrancas.com.br com contrato real.
4. **Tag v1.4-phase7.7:** criar SOMENTE após UAT prod OK (`git tag v1.4-phase7.7 <SHA_pai>` + push da tag).

Status: phase 7.7 pronta para push — zero push executado nesta sessão. Zero tag criada.
```

---

**Passo 6 — Criar SUMMARY da phase:**

Criar `.planning/phases/07.7-excluir-documento-individual/07.7-03-SUMMARY.md` com:
- SHAs: 2 do submodule + 1 do pai (full + short)
- Output literal do UAT 5 cenários (copiar report do usuário da Task 1)
- Output dos 5 gates determinísticos (regressão + build + motor diff x3 + escopo)
- Texto literal das 3 autorizações do usuário (PAUSA #3 + PAUSA #4 + PAUSA #5)
- Evidência invariante motor Art.354 (confirmação explícita que os 3 arquivos do motor não mudaram)
- Próximos passos EXATOS (push + deploy + UAT prod + tag)
- **Flag 7.5.2 latente (D-13):** se usuário mencionou anomalia em encargos no UAT, registrar como pendência pré-existente (não regressão da 7.7)
- Confirmação final: "zero push executado, zero tag criada"

**NÃO** atualizar `.planning/STATE.md` ou `.planning/ROADMAP.md` nesta task — é ação de `/gsd-close-phase` ou `/gsd-complete-phase`.

---

**Passo 7 — Confirmar SEM PUSH:**

```bash
cd src/mr-3/mr-cobrancas
git log origin/main..main --oneline
# esperado: 2 linhas (07.7-02 + 07.7-01)
cd ../../..
git log origin/master..master --oneline
# esperado: ≥1 linha (o commit de bump desta phase + possivelmente commits de phases anteriores ainda não pushed como 07.5.1/07.6)
```

Se alguma linha 0 → push foi executado indevidamente; investigar crítico. (Não deveria acontecer — nenhum `git push` foi chamado.)

  </how-to-verify>

  <acceptance_criteria>
    **PAUSA #4 autorizada:**
    - Agente executou `git log origin/main..main --stat` + `git diff HEAD~2 -- src/services/contratos.js src/components/DetalheContrato.jsx` e mostrou ao usuário
    - Agente reportou `git diff HEAD~2` vazio nos 3 arquivos do motor (devedorCalc.js + pagamentos.js + dividas.js) como evidência INV motor D-01
    - Usuário respondeu `approved`, `bump it`, ou equivalente explícito ANTES do commit no pai
    - Texto do chat contém marcador `PAUSA #4` ou equivalente

    **PAUSA #5 autorizada:**
    - Agente cd pra raiz do pai e confirmou `git rev-parse --abbrev-ref HEAD` = `master`
    - Agente reportou SHAs EXATOS (`<SHA1_short>` + `<SHA2_short>`) ANTES do commit
    - Agente reportou mensagem EXATA do commit proposto: `chore(07.7): bump submodule mr-3 — excluir documento individual (07.7-01 + 07.7-02)`
    - Usuário respondeu `bump it`, `approved`, ou equivalente explícito (autorização SEPARADA da PAUSA #4)
    - Texto do chat contém marcador `PAUSA #5` ou equivalente

    **Commit do pai correto:**
    - `git rev-parse --abbrev-ref HEAD` na raiz do pai retorna `master`
    - `git log -1 --format=%s` retorna EXATAMENTE `chore(07.7): bump submodule mr-3 — excluir documento individual (07.7-01 + 07.7-02)`
    - `git log -1 --name-only --format=` lista EXATAMENTE 1 entrada: `src/mr-3` (ou `Submodule src/mr-3` no stat view)
    - `git show --stat HEAD` mostra 1 submodule pointer atualizado (`Subproject commit <SHA_antigo> → <SHA_novo>`)
    - `git log -1 --format=%b` contém: `Phase 7.7`, `excluirDocumento`, `D-01`, `D-05`, `D-07`, `D-08`, `recalcularTotaisContrato`, `SC-1..SC-5`, `Tag v1.4-phase7.7 pendente`, `Co-Authored-By: Claude Opus 4.7`

    **Zero push:**
    - `cd src/mr-3/mr-cobrancas && git log origin/main..main --oneline` retorna **exatamente 2 linhas** (07.7-01 + 07.7-02 locais)
    - `cd <raiz do pai> && git log origin/master..master --oneline` retorna **≥1 linha** (novo commit de bump)
    - **NENHUM** comando `git push` executado em toda a Task 2 (histórico do shell deve confirmar)
    - **NENHUMA** tag criada: `git tag -l "v1.4-phase7.7*"` retorna vazio

    **SUMMARY criado:**
    - `.planning/phases/07.7-excluir-documento-individual/07.7-03-SUMMARY.md` existe e contém:
      * SHAs (submodule x2 + pai x1)
      * Report literal do UAT SC-1..SC-5
      * Output dos gates determinísticos
      * Texto literal das 3 autorizações (PAUSA #3 + PAUSA #4 + PAUSA #5)
      * Evidência invariante motor Art.354
      * Próximos passos (push + deploy + UAT prod + tag)
      * Confirmação explícita: "zero push, zero tag"
      * Flag 7.5.2 latente (se aplicável ao UAT reportado)

    **Nenhum arquivo fora de escopo foi commitado:**
    - `git show HEAD --stat` no pai mostra EXATAMENTE 1 entrada: `src/mr-3`
    - Arquivos `.planning/STATE.md`, `.planning/ROADMAP.md` etc pré-existentes como modified NÃO aparecem no commit de bump (permanecem unstaged)
  </acceptance_criteria>

  <resume-signal>
    Esta task tem 2 gates humanos internos (PAUSA #4 + PAUSA #5). Cada gate espera autorização SEPARADA (`approved` / `bump it`). Agente NÃO prossegue sem ambas.

    Após Passo 5 reportado: task completa. Usuário pode:
    - Encerrar sessão e autorizar push em sessão separada (padrão 7.5.1).
    - Pedir `/gsd-close-phase 07.7` pra atualizar STATE.md + ROADMAP.md (fora do escopo desta task).
    - Autorizar push imediato: agente executa `cd src/mr-3/mr-cobrancas && git push origin main && cd ../../.. && git push origin master` APENAS se user disser explicitamente `push agora` / `autoriza push`.
  </resume-signal>

  <done>
    Commit `chore(07.7): bump submodule mr-3 — excluir documento individual (07.7-01 + 07.7-02)` existe no repo pai branch `master` com APENAS `src/mr-3` stageado. 3 SHAs finais reportados (2 submodule + 1 pai). 3 autorizações humanas registradas (PAUSA #3 + #4 + #5; pausas #1 e #2 foram nas plans 07.7-01 e 07.7-02). SUMMARY criado. Zero push, zero tag criada. Próximos passos (push + deploy + UAT prod + tag) documentados para ação separada do usuário.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| Agente → Git CLI do submodule e do pai | Comandos locais (git add/commit, npm). Sem boundary de rede — zero push. |
| Agente → Usuário (checkpoints humanos) | 3 PAUSAs bloqueantes (PAUSA #3 UAT + #4 diff + #5 bump). Sem fallback automatizado — autorização explícita obrigatória. |

## STRIDE Threat Register

| Threat ID | Categoria | Componente | Disposição | Mitigação |
|-----------|-----------|------------|------------|-----------|
| T-07.7-15 | Tampering (push acidental) | Agente invoca `git push` inadvertidamente | mitigate | Acceptance criteria negativa explícita: "NENHUM comando git push". Passo 7 conta commits pendentes (`git log origin/*..*`); se count = 0, push ocorreu e é crítico. |
| T-07.7-16 | Tampering (git add -A resgata arquivos fora de escopo) | Commit do pai inclui `.planning/STATE.md` etc | mitigate | `git add src/mr-3` EXPLICITAMENTE (nunca `git add -A` / `.`). Passo 4 confirma `git status --short` mostra SÓ `M src/mr-3` stageado. AC: `git show HEAD --stat` = 1 arquivo. |
| T-07.7-17 | Elevation of Privilege (usuário fatigado autoriza sem revisar) | Modo cansado D-11, 5 pausas totais | accept | D-11 EXPLICITAMENTE adiciona pausas para mitigar fadiga. Autorização explícita exigida em cada gate. Se usuário auto-aprova cegamente, risco aceito — plano não pode forçar revisão profunda. |
| T-07.7-18 | Repudiation (commit message inconsistente) | Mensagem do bump diverge do padrão | mitigate | Texto literal no commit message (HEREDOC), verificado por `git log -1 --format=%b | grep "Phase 7.7|D-05|Tag.*pendente"`. AC inclui grep de strings-chave. |
| T-07.7-19 | DoS (dev server não sobe, bloqueia UAT) | npm run dev falha | accept | Unlikely (build já passou); se acontecer, usuário reporta, agente diagnostica sem urgência. |
| T-07.7-20 | Tag criada prematuramente | `git tag v1.4-phase7.7` disparado | mitigate | Acceptance criteria EXPLICITAMENTE verifica `git tag -l "v1.4-phase7.7*"` = vazio. D-14 + CONTEXT §5 SC-11 anexa "Tag v1.4-phase7.7 NÃO é criada nesta phase". |
| T-07.7-21 | Information Disclosure (commit body vaza estado interno) | Mensagem bump reporta SHAs, paths, etc | accept | Todos os dados expostos são intencionais e úteis pra auditoria futura. Sem secrets. |
</threat_model>

<verification>
Verificação final da plan 07.7-03 (após Task 2 completa):

1. **Submodule main (estado final):**
   - `cd src/mr-3/mr-cobrancas && git log origin/main..main --oneline` → 2 linhas (07.7-01 + 07.7-02)
   - `cd src/mr-3/mr-cobrancas && git diff HEAD~2 -- src/utils/devedorCalc.js src/services/pagamentos.js src/services/dividas.js` → vazio
   - `cd src/mr-3/mr-cobrancas && npm run test:regressao` → 9/9 PASS
   - `cd src/mr-3/mr-cobrancas && npm run build` → exit 0

2. **Pai master (estado final):**
   - `cd <raiz> && git log -1 --format=%s` → `chore(07.7): bump submodule mr-3 — excluir documento individual (07.7-01 + 07.7-02)`
   - `cd <raiz> && git log -1 --name-only --format=` → `src/mr-3` (1 linha apenas)
   - `cd <raiz> && git log origin/master..master --oneline` → inclui o commit novo

3. **Zero push:**
   - Ambos `git log origin/*..` retornam ≥1 commit local. Nenhum `git push` executado.

4. **Sem tag:**
   - `cd <raiz> && git tag -l "v1.4-phase7.7*"` → vazio.

5. **UAT:**
   - 5 cenários SC-1..SC-5 reportados pelo usuário (Task 1 PAUSA #3), referenciados no SUMMARY.

6. **3 autorizações humanas registradas:**
   - PAUSA #3 (UAT), PAUSA #4 (diff consolidado), PAUSA #5 (bump commit) — todas com texto do usuário no chat.

7. **SUMMARY criado** com: SHAs (3), UAT report, gates, 3 autorizações, invariante motor, zero push/tag, próximos passos, flag 7.5.2 latente se aplicável.

8. **Flag 7.5.2 latente (D-13) respeitada:**
   - Se UAT mencionou anomalia em encargos de docs restantes: registrado no SUMMARY como pendência pré-existente, NÃO como regressão da 7.7.
</verification>

<success_criteria>
Plan 07.7-03 completa quando:

1. ✅ Task 1: gates determinísticos PASS (regressão 9/9, build exit 0, motor diff vazio nos 3 arquivos, escopo = 2 arquivos)
2. ✅ Task 1: UAT 5 cenários SC-1..SC-5 reportados pelo usuário (PAUSA #3) e aprovados (ou com flags latentes D-13 anotadas)
3. ✅ Task 2 PAUSA #4: diff consolidado autorizado explicitamente (`approved` / `bump it`)
4. ✅ Task 2 PAUSA #5: bump commit autorizado explicitamente (SEPARADA da PAUSA #4)
5. ✅ Task 2: commit no pai branch master com subject EXATO e APENAS `src/mr-3` stageado
6. ✅ Zero push em toda a phase (ambos `git log origin/*..` ≥1 commit local)
7. ✅ Zero tag criada (`git tag -l "v1.4-phase7.7*"` vazio)
8. ✅ SUMMARY criado em `.planning/phases/07.7-excluir-documento-individual/07.7-03-SUMMARY.md`
9. ✅ Invariante motor Art.354 preservada de ponta a ponta (verificada em todas as 3 plans da phase)
10. ✅ 5 pausas totais registradas (D-11): pausa #1 (07.7-01 Task 3), pausa #2 (07.7-02 Task 3), pausa #3 (07.7-03 Task 1), pausa #4 + pausa #5 (07.7-03 Task 2) — todas com autorização explícita do usuário.
</success_criteria>

<output>
Após conclusão da Task 2, criar `.planning/phases/07.7-excluir-documento-individual/07.7-03-SUMMARY.md` contendo:

- **SHAs:** 2 do submodule (07.7-01 + 07.7-02) + 1 do pai (bump), full + short
- **UAT:** resultado literal dos 5 cenários SC-1..SC-5 (copiar report do usuário da Task 1)
- **Gates determinísticos:** output de regressão (9/9), build (exit 0), motor diff (vazio nos 3 arquivos), escopo (2 arquivos)
- **Autorizações humanas (3):** texto literal do usuário em PAUSA #3 + PAUSA #4 + PAUSA #5
- **Invariante motor Art.354:** confirmação explícita que `devedorCalc.js` + `pagamentos.js` + `dividas.js` não mudaram (diff HEAD~2 vazio)
- **Flag 7.5.2 latente (D-13):** se usuário reportou anomalia em encargos, registrar como pendência pré-existente
- **Zero push + zero tag:** confirmação explícita
- **Próximos passos:**
  1. Push quando autorizar: `cd src/mr-3/mr-cobrancas && git push origin main && cd <raiz> && git push origin master`
  2. Deploy Vercel automático pós-push (cache 7.4 OK)
  3. UAT produção em mrcobrancas.com.br
  4. Tag `v1.4-phase7.7` APENAS após UAT prod OK, em ação separada autorizada

O SUMMARY é o artefato de fechamento da phase — `/gsd-close-phase` usa para atualizar STATE.md e ROADMAP.md.
</output>
</content>
</invoke>