---
phase: 07.7-excluir-documento-individual
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mr-3/mr-cobrancas/src/services/contratos.js
autonomous: false   # D-11: modo cansado — pausa #1 (diff review pré-commit) antes do feat(07.7-01) no submodule main. SEM PUSH.
requirements: []    # Gap closure — sem REQ-IDs formais. Contrato de verificação = SC-1..SC-5 em 07.7-CONTEXT.md §5 + D-02..D-06.

must_haves:
  truths:
    - "excluirDocumento(documentoId) existe em src/services/contratos.js, é exportada async, retorna { ok: true } ou { ok: false, motivo: string } — espelho estrutural de excluirContrato (D-02)"
    - "Pre-check bloqueia delete quando parcelas do documento têm ≥1 linha em pagamentos_divida — toast vermelho bloqueante no caller (D-03)"
    - "Ordem DELETE invariante: dividas WHERE documento_id ANTES de documentos_contrato WHERE id — NO CASCADE em dividas.documento_id (D-04)"
    - "recalcularTotaisContrato(contratoId) OBRIGATÓRIO pós-DELETEs e ANTES do return { ok: true } — senão contrato.valor_total/num_documentos/num_parcelas_total ficam stale (D-05)"
    - "pagamentos_contrato NÃO entra no pre-check (pagamentos_divida já captura SP-generated amortizations) (D-06)"
    - "Motor Art.354 intocado — git diff HEAD -- src/utils/devedorCalc.js src/services/pagamentos.js retorna vazio (D-01)"
    - "npm run test:regressao 9/9 verde após commit (motor intocado)"
    - "npm run build exit 0 após commit"
    - "Zero UI modificada, zero side-effect fora do service — Plan 07.7-02 trata UI"
    - "1 commit local submodule mr-3 branch main subject exato `feat(07.7-01): service excluirDocumento em contratos.js` (D-14) — SEM PUSH"
  artifacts:
    - path: "src/mr-3/mr-cobrancas/src/services/contratos.js"
      provides: "excluirDocumento(documentoId) — pre-check pagamentos_divida + DELETE dividas + DELETE documentos_contrato + recalcularTotaisContrato(contratoId) + return { ok, motivo }"
      contains: "export async function excluirDocumento"
  key_links:
    - from: "excluirDocumento (pre-check D-03)"
      to: "pagamentos_divida"
      via: "dbGet em pagamentos_divida WHERE divida_id IN (parcelas do documento)"
      pattern: "dbGet\\(['\"]pagamentos_divida['\"]"
    - from: "excluirDocumento (cascade D-04)"
      to: "DELETE dividas WHERE documento_id → DELETE documentos_contrato WHERE id"
      via: "sb('dividas', 'DELETE', null, '?documento_id=eq.*') → dbDelete('documentos_contrato', id) — NESSA ORDEM"
      pattern: "sb\\(['\"]dividas['\"].*DELETE.*dbDelete\\(['\"]documentos_contrato['\"]"
    - from: "excluirDocumento (pós-delete D-05)"
      to: "recalcularTotaisContrato(contratoId)"
      via: "chamada OBRIGATÓRIA antes do return { ok: true }"
      pattern: "await recalcularTotaisContrato\\("
    - from: "excluirDocumento"
      to: "contratoId (lookup)"
      via: "SELECT contrato_id FROM documentos_contrato WHERE id — precisa do contrato_id pra chamar recalcularTotaisContrato DEPOIS do DELETE"
      pattern: "dbGet\\(['\"]documentos_contrato['\"]"
---

<objective>
Adicionar ao service `src/mr-3/mr-cobrancas/src/services/contratos.js` a função `excluirDocumento(documentoId)` — espelho estrutural de `excluirContrato` (Phase 7.2, linhas 329-371 do arquivo) com apenas 2 diferenças:

1. **Filtro muda**: `documento_id=eq.*` em vez de `contrato_id=eq.*` no SELECT parcelas + DELETE dividas + DELETE documentos_contrato.
2. **Pós-delete adiciona** `await recalcularTotaisContrato(contratoId)` ANTES do return `{ ok: true }` — helper já existente em `contratos.js:219-226`, usado por `adicionarDocumento:283`.

Purpose: Advogado consegue apagar UM documento (NF/DA/DM cadastrado errado) sem precisar deletar e recriar o contrato inteiro. Service layer puro — zero UI, zero side-effect fora do service. Plan 07.7-02 trata botão + handler em DetalheContrato.jsx.

Output:
- `excluirDocumento(documentoId)` exportada async de `src/mr-3/mr-cobrancas/src/services/contratos.js` — `{ ok: true }` ou `{ ok: false, motivo: "..." }`.
- 1 commit local no submodule `src/mr-3/mr-cobrancas` branch `main` com subject EXATO `feat(07.7-01): service excluirDocumento em contratos.js` (D-14).
- **Zero commits no repo pai nesta phase — o bump acontece em 07.7-03 Task 2 após UAT.**
- **Zero push em qualquer momento.** Zero tag criada.

Contexto de conformidade:
- D-01 Motor Art.354 intocado — função nova, não altera `services/dividas.js`, `utils/devedorCalc.js`, `services/pagamentos.js`.
- D-02 Template `excluirContrato` (contratos.js:329-371) copiado 1:1 em estrutura — só muda filtro e adiciona `recalcularTotaisContrato`.
- D-03 Pre-check em `pagamentos_divida` bloqueia delete — toast vermelho no caller (caller é responsabilidade de 07.7-02).
- D-04 Ordem DELETE invariante: `dividas` ANTES de `documentos_contrato` (NO CASCADE em `dividas.documento_id`).
- D-05 `recalcularTotaisContrato(contratoId)` OBRIGATÓRIO pós-delete.
- D-06 `pagamentos_contrato` NÃO entra no pre-check (redundante — pagamentos_divida já cobre SP-generated).
- D-10 Convenções: `sb()` / `dbGet` / `dbDelete` (NÃO rpc), submodule main, zero push sem autorização.
- D-11 Modo cansado: plan `autonomous: false` — Task 3 é checkpoint humano de diff review antes do commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07.7-excluir-documento-individual/07.7-CONTEXT.md

<!-- Template literal Phase 7.2 (excluirContrato) — mesma estrutura, só muda filtro + adiciona recalcularTotaisContrato: -->
@.planning/phases/07.2-excluir-contrato/07.2-01-PLAN.md

<interfaces>
<!-- Interfaces-chave do codebase atual. Executor usa DIRETO sem explorar. -->

From src/mr-3/mr-cobrancas/src/config/supabase.js (helpers já disponíveis, re-usar como na 7.2):
```javascript
export async function sb(path, method = "GET", body = null, extra = "");
export const dbGet    = (t, q = "") => sb(t, "GET",    null, q ? `?${q}` : "?order=id.asc");
export const dbDelete = (t, id)     => sb(t, "DELETE", null, `?id=eq.${id}`);
```

From src/mr-3/mr-cobrancas/src/services/contratos.js — imports e helpers EXISTENTES (reutilizar, não adicionar novos imports):
```javascript
import { dbGet, dbInsert, dbUpdate, sb, dbDelete } from "../config/supabase.js";
const TABLE = "contratos_dividas";
```

From src/mr-3/mr-cobrancas/src/services/contratos.js:219-226 — helper JÁ EXISTENTE que DEVE ser chamado pós-delete (D-05):
```javascript
export async function recalcularTotaisContrato(contratoId) {
  const docs = await listarDocumentosPorContrato(contratoId);
  const arr = Array.isArray(docs) ? docs : [];
  const valor_total = parseFloat(arr.reduce((s, d) => s + parseFloat(d.valor || 0), 0).toFixed(2));
  const num_documentos = arr.length;
  const num_parcelas_total = arr.reduce((s, d) => s + (d.num_parcelas || 0), 0);
  return dbUpdate(TABLE, contratoId, { valor_total, num_documentos, num_parcelas_total });
}
```

From src/mr-3/mr-cobrancas/src/services/contratos.js:329-371 — TEMPLATE LITERAL `excluirContrato` (D-02, copiar 1:1 a estrutura, mudar só filtro e adicionar recalcularTotaisContrato):
```javascript
export async function excluirContrato(contratoId) {
  // Pré-check 1: pagamentos no nível do contrato
  const pagsContrato = await dbGet(
    "pagamentos_contrato",
    `contrato_id=eq.${encodeURIComponent(contratoId)}&select=id&limit=1`
  );
  if (Array.isArray(pagsContrato) && pagsContrato.length > 0) {
    return { ok: false, motivo: "Este contrato possui pagamentos registrados — não pode ser excluído. Exclua os pagamentos primeiro." };
  }

  // Pré-check 2: pagamentos no nível das parcelas (pagamentos_divida)
  const parcelas = await dbGet(
    "dividas",
    `contrato_id=eq.${encodeURIComponent(contratoId)}&select=id`
  );
  const parcelaIds = Array.isArray(parcelas) ? parcelas.map(p => p.id) : [];
  if (parcelaIds.length > 0) {
    const inList = parcelaIds.map(encodeURIComponent).join(",");
    const pagsDivida = await dbGet(
      "pagamentos_divida",
      `divida_id=in.(${inList})&select=id&limit=1`
    );
    if (Array.isArray(pagsDivida) && pagsDivida.length > 0) {
      return { ok: false, motivo: "Este contrato possui pagamentos registrados — não pode ser excluído. Exclua os pagamentos primeiro." };
    }
  }

  // Cascata manual
  await sb("dividas", "DELETE", null, `?contrato_id=eq.${encodeURIComponent(contratoId)}`);
  await sb("documentos_contrato", "DELETE", null, `?contrato_id=eq.${encodeURIComponent(contratoId)}`);
  await dbDelete(TABLE, contratoId);

  return { ok: true };
}
```

Schema FK analysis (confirmado no schema Supabase — CONTEXT.md §7):

| Tabela.coluna | Target | ON DELETE |
|---|---|---|
| `documentos_contrato.contrato_id` | `contratos_dividas(id)` | NO CASCADE |
| `dividas.documento_id` | `documentos_contrato(id)` | **NO CASCADE** (→ dividas PRIMEIRO) |
| `dividas.contrato_id` | `contratos_dividas(id)` | NO CASCADE |
| `pagamentos_divida.divida_id` | `dividas(id)` | **CASCADE ✓** (cai junto com dividas) |
| `devedores_dividas.divida_id` | `dividas(id)` | **CASCADE ✓** (cai junto com dividas) |

Consequência para a função nova:
- DELETE `dividas WHERE documento_id` primeiro → `pagamentos_divida` + `devedores_dividas` caem via CASCADE.
- DELETE `documentos_contrato WHERE id` depois → falharia com FK violation se dividas ainda existissem.
- D-06: `pagamentos_contrato` NÃO checado — SP-generated amortizations já estão em `pagamentos_divida`, capturadas pelo pre-check único.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implementar excluirDocumento(documentoId) em src/services/contratos.js</name>
  <files>src/mr-3/mr-cobrancas/src/services/contratos.js</files>

  <read_first>
    <!-- OBRIGATÓRIO ler antes de qualquer escrita -->
    - `src/mr-3/mr-cobrancas/src/services/contratos.js` linhas 1-40 (imports existentes, constantes `TABLE`, `HIST_TABLE`, `PAG_TABLE` — confirmar que `sb`, `dbGet`, `dbDelete` já estão importados em linha ~64; NÃO adicionar novos imports)
    - `src/mr-3/mr-cobrancas/src/services/contratos.js` linhas 219-226 (`recalcularTotaisContrato` — helper OBRIGATÓRIO pós-delete D-05; confirmar assinatura `recalcularTotaisContrato(contratoId)`)
    - `src/mr-3/mr-cobrancas/src/services/contratos.js` linhas 329-371 (`excluirContrato` — TEMPLATE LITERAL D-02; o código novo é isomorfo em estrutura, troca `contrato_id=eq.*` por `documento_id=eq.*` e adiciona `recalcularTotaisContrato(contratoId)` ao final)
    - `.planning/phases/07.7-excluir-documento-individual/07.7-CONTEXT.md` §4 D-02..D-06 (invariantes), §5 SC-1..SC-5, §7 (schema FK analysis + template Phase 7.2)
  </read_first>

  <action>
    **Localização da nova função:** Inserir ANTES do comentário `// ─── TOTAIS NOMINAIS DO CONTRATO (Phase 7.3) ─────────────────────────────────` (linha 373 atual) e DEPOIS da chave `}` que fecha `excluirContrato` (linha 371 atual). Adicionar seu próprio header de seção: `// ─── EXCLUIR DOCUMENTO INDIVIDUAL (Phase 7.7) ────────────────────────────────`.

    **Código a inserir (LITERAL — espelho 1:1 de `excluirContrato` com 2 diferenças marcadas):**

    ```javascript
    // ─── EXCLUIR DOCUMENTO INDIVIDUAL (Phase 7.7) ────────────────────────────────

    /**
     * Exclui UM documento de um contrato com hard delete em cascata manual.
     *
     * Phase 7.7 — espelho estrutural de excluirContrato (Phase 7.2, linhas 329-371
     * deste arquivo) com 2 diferenças (D-02):
     *   1. Filtro: documento_id=eq.* em vez de contrato_id=eq.* nos DELETEs.
     *   2. Pós-delete OBRIGATÓRIO: recalcularTotaisContrato(contratoId) (D-05) —
     *      senão contrato.valor_total, num_documentos, num_parcelas_total ficam
     *      stale (mesmo helper que adicionarDocumento:283 usa).
     *
     * Pré-checagem (D-03, D-06):
     *  - rejeita se existir alguma linha em pagamentos_divida apontando pra
     *    qualquer parcela (divida_id) do documento.
     *  - pagamentos_contrato NÃO é checado separadamente — SP-generated
     *    amortizations via registrar_pagamento_contrato já escrevem em
     *    pagamentos_divida, capturadas pelo pre-check único (D-06).
     *
     * Sequência dos DELETEs (D-04, ordem obrigatória por FK dividas.documento_id
     * sem CASCADE):
     *  1. DELETE FROM dividas WHERE documento_id = :id
     *     → pagamentos_divida + devedores_dividas caem via CASCADE.
     *  2. DELETE FROM documentos_contrato WHERE id = :id
     *  3. recalcularTotaisContrato(contratoId) (D-05)
     *
     * Contrato vazio (num_documentos=0) pós-delete do último doc é estado válido
     * (D-08) — sem tratamento especial.
     *
     * Sem try/catch interno: exceções técnicas (rede/RLS) propagam pro caller
     * (padrão de excluirContrato).
     *
     * @param {string} documentoId  UUID do documento (documentos_contrato.id)
     * @returns {Promise<{ ok: true } | { ok: false, motivo: string }>}
     */
    export async function excluirDocumento(documentoId) {
      // Lookup do contrato_id (necessário no final pra recalcularTotaisContrato).
      // Também valida que o documento existe — se não existe, retornamos ok:false
      // em vez de propagar erro opaco de banco.
      const docs = await dbGet(
        "documentos_contrato",
        `id=eq.${encodeURIComponent(documentoId)}&select=id,contrato_id&limit=1`
      );
      if (!Array.isArray(docs) || docs.length === 0) {
        return { ok: false, motivo: "Documento não encontrado." };
      }
      const contratoId = docs[0].contrato_id;

      // Pre-check (D-03, D-06): pagamentos_divida apontando pra qualquer parcela do doc.
      // Passo 1: listar parcelas (dividas) do documento.
      const parcelas = await dbGet(
        "dividas",
        `documento_id=eq.${encodeURIComponent(documentoId)}&select=id`
      );
      const parcelaIds = Array.isArray(parcelas) ? parcelas.map(p => p.id) : [];
      if (parcelaIds.length > 0) {
        const inList = parcelaIds.map(encodeURIComponent).join(",");
        const pagsDivida = await dbGet(
          "pagamentos_divida",
          `divida_id=in.(${inList})&select=id&limit=1`
        );
        if (Array.isArray(pagsDivida) && pagsDivida.length > 0) {
          return {
            ok: false,
            motivo: "Este documento possui pagamentos registrados — não pode ser excluído. Exclua os pagamentos primeiro."
          };
        }
      }

      // Cascata manual (D-04) — ordem OBRIGATÓRIA:
      // Passo 1: dividas WHERE documento_id — pagamentos_divida + devedores_dividas caem via CASCADE.
      await sb("dividas", "DELETE", null, `?documento_id=eq.${encodeURIComponent(documentoId)}`);
      // Passo 2: o documento em si.
      await dbDelete("documentos_contrato", documentoId);

      // Pós-delete OBRIGATÓRIO (D-05): recalcula totais do contrato pai.
      // Sem isso, contrato.valor_total / num_documentos / num_parcelas_total ficam stale.
      await recalcularTotaisContrato(contratoId);

      return { ok: true };
    }
    ```

    **Detalhes obrigatórios:**
    - Nome literal: `excluirDocumento` (export nomeado, async).
    - Return shape EXATO: `{ ok: true }` em sucesso; `{ ok: false, motivo: "Documento não encontrado." }` para doc inexistente; `{ ok: false, motivo: "Este documento possui pagamentos registrados — não pode ser excluído. Exclua os pagamentos primeiro." }` em rejeição por pre-check.
    - Usar `encodeURIComponent(documentoId)` e `encodeURIComponent(parcelaId)` nas querystrings (padrão do arquivo — ver `excluirContrato` linha 333).
    - NÃO adicionar `try/catch` interno — exceções técnicas (rede/RLS) propagam pro caller (simetria com `excluirContrato`, D-04 Phase 7.2).
    - NÃO adicionar novos imports — `sb`, `dbGet`, `dbDelete`, `recalcularTotaisContrato` já existem no arquivo.
    - NÃO tocar `excluirContrato` (linhas 329-371), `recalcularTotaisContrato` (linhas 219-226), `adicionarDocumento` (linhas 240-285), nem qualquer outra função existente.
    - NÃO tocar `services/dividas.js`, `utils/devedorCalc.js`, `services/pagamentos.js` (invariante motor Art.354 D-01).
    - NÃO registrar evento em `contratos_historico` — evento forense deferido (D-09, precedente 7.5.1).
  </action>

  <verify>
    <automated>cd src/mr-3/mr-cobrancas &amp;&amp; node -e "import('./src/services/contratos.js').then(m =&gt; { if (typeof m.excluirDocumento !== 'function') { console.error('FAIL: excluirDocumento não exportada'); process.exit(1); } console.log('OK: excluirDocumento exportada e async'); })"</automated>
  </verify>

  <acceptance_criteria>
    <!-- Cada item é grep-verificável. Rodar da raiz do submodule src/mr-3/mr-cobrancas. -->
    - `grep -c "export async function excluirDocumento" src/services/contratos.js` retorna **1**
    - `grep -c "export async function excluirContrato" src/services/contratos.js` retorna **1** (função da 7.2 preservada — não deletada, não alterada)
    - `grep -n "documento_id=eq" src/services/contratos.js` retorna ≥3 ocorrências (lookup do doc, listar parcelas, DELETE dividas WHERE documento_id)
    - `grep -c 'sb("dividas", "DELETE"' src/services/contratos.js` retorna ≥2 (1 da 7.2 + 1 da 7.7)
    - `grep -n 'sb("dividas", "DELETE".*documento_id=eq' src/services/contratos.js` retorna **1 linha** (novo DELETE por documento_id — distinto do DELETE por contrato_id da 7.2)
    - `grep -c 'dbDelete("documentos_contrato"' src/services/contratos.js` retorna **1** (dentro de excluirDocumento — 7.2 usa `sb("documentos_contrato", "DELETE", ...)` com filtro `?contrato_id=eq`, NÃO dbDelete)
    - `grep -c "await recalcularTotaisContrato(contratoId)" src/services/contratos.js` retorna ≥2 (1 em `adicionarDocumento:283` existente + 1 em `excluirDocumento` novo)
    - `grep -c "Este documento possui pagamentos registrados" src/services/contratos.js` retorna **1** (mensagem literal da rejeição por pre-check)
    - `grep -c "Documento não encontrado" src/services/contratos.js` retorna **1** (mensagem literal de doc inexistente)
    - `grep -c 'dbGet(\s*"pagamentos_divida"' src/services/contratos.js` OU `grep -cE '"pagamentos_divida"' src/services/contratos.js` retorna ≥2 (1 em excluirContrato + 1 em excluirDocumento)
    - **Motor Art.354 invariante (D-01):** `git diff HEAD -- src/utils/devedorCalc.js src/services/pagamentos.js src/services/dividas.js` retorna **vazio** (0 bytes)
    - **Escopo do diff:** `git diff --name-only HEAD` retorna EXATAMENTE `src/services/contratos.js` (nenhum outro arquivo do submodule modificado)
    - `npm run test:regressao` exit 0 (9/9 PASS — motor intocado, função nova é additiva)
    - `npm run build` exit 0
  </acceptance_criteria>

  <done>
    `excluirDocumento(documentoId)` existe em `src/services/contratos.js`, exportada async, espelho estrutural de `excluirContrato` (D-02) com: pre-check único em `pagamentos_divida` (D-03, D-06); DELETE dividas → DELETE documentos_contrato na ordem correta (D-04); `recalcularTotaisContrato(contratoId)` pós-delete (D-05); returns `{ ok: true }` ou `{ ok: false, motivo: "..." }`. Zero UI. Zero novos imports. Motor Art.354 intocado. Regressão 9/9 verde, build exit 0. **Ainda não commitado — Task 3 tem o gate humano de diff review antes do commit (D-11 pausa #1).**
  </done>
</task>

<task type="auto">
  <name>Task 2: Gates determinísticos — regressão 9/9 + build exit 0 + invariante motor (antes da pausa de diff review)</name>
  <files>
    (nenhum arquivo modificado — só gates)
  </files>

  <read_first>
    - `.planning/phases/07.7-excluir-documento-individual/07.7-CONTEXT.md` §4 D-01 (INV motor), D-11 (5 pausas totais — Task 3 desta plan é a pausa #1)
    - `.planning/phases/07.6-parcelas-n-grande/07.6-03-PLAN.md` Task 1 "PARTE A" — padrão de gates determinísticos pré-UAT (re-usar estrutura)
  </read_first>

  <action>
    Rodar os 3 gates determinísticos na raiz do submodule. Se QUALQUER gate falhar, NÃO avançar para Task 3 e reportar ao usuário com o diff/output completo do gate ofensor.

    ```bash
    cd src/mr-3/mr-cobrancas

    # Gate 1 — Regressão 9/9 (invariante motor Art.354, D-01):
    npm run test:regressao
    # Esperado: 9/9 PASS, exit 0.
    # Se falhar: reportar qual teste quebrou. Causa provável única: edição indevida em utils/devedorCalc.js,
    # services/pagamentos.js ou services/dividas.js na Task 1. Executor NÃO tocou esses arquivos —
    # se falhou, algo foi editado fora de escopo. Rodar `git diff HEAD` pra auditar.

    # Gate 2 — Build limpo:
    npm run build
    # Esperado: exit 0. Se falhar: syntax error em contratos.js (provável causa única).

    # Gate 3 — Invariante motor Art.354 (D-01):
    git diff HEAD -- src/utils/devedorCalc.js src/services/pagamentos.js src/services/dividas.js
    # Esperado: vazio (0 bytes). Se qualquer byte → reportar EXATAMENTE o diff e ABORTAR.

    # Gate 4 — Escopo do diff (garantir que só contratos.js foi mexido no submodule):
    git diff --name-only HEAD
    # Esperado: EXATAMENTE "src/services/contratos.js".
    # Qualquer outro arquivo → reportar e pedir confirmação do usuário antes de avançar.
    ```
  </action>

  <verify>
    <automated>cd src/mr-3/mr-cobrancas &amp;&amp; npm run test:regressao &amp;&amp; npm run build &amp;&amp; [ -z "$(git diff HEAD -- src/utils/devedorCalc.js src/services/pagamentos.js src/services/dividas.js)" ] &amp;&amp; echo "ALL GATES PASS"</automated>
  </verify>

  <acceptance_criteria>
    - `cd src/mr-3/mr-cobrancas && npm run test:regressao` exit 0 (9/9 PASS)
    - `cd src/mr-3/mr-cobrancas && npm run build` exit 0
    - `cd src/mr-3/mr-cobrancas && git diff HEAD -- src/utils/devedorCalc.js src/services/pagamentos.js src/services/dividas.js` retorna **vazio** (0 bytes)
    - `cd src/mr-3/mr-cobrancas && git diff --name-only HEAD` retorna EXATAMENTE `src/services/contratos.js` (1 linha)
  </acceptance_criteria>

  <done>
    Todos os gates determinísticos passaram. Safe para apresentar diff review ao usuário (Task 3 pausa #1).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: PAUSA #1 — diff review antes do commit submodule (D-11 modo cansado)</name>
  <files>
    src/mr-3/mr-cobrancas/src/services/contratos.js (commit local no submodule main — SEM PUSH)
  </files>

  <what-built>
    Task 1 adicionou `excluirDocumento(documentoId)` em `src/services/contratos.js`. Task 2 confirmou gates determinísticos verdes (regressão 9/9, build exit 0, motor intocado, diff only contratos.js).

    Esta Task 3 é a **pausa #1 das 5 pausas totais do modo cansado (D-11)**:

    1. Agente mostra ao usuário: `git status --short` + `git diff --stat HEAD` + `git diff HEAD -- src/services/contratos.js` no submodule.
    2. Agente confirma verbalmente: motor Art.354 intocado (gate 3 da Task 2 foi vazio), regressão 9/9, build exit 0, diff escopo correto.
    3. Aguarda autorização explícita do usuário (`approved` / `commit it` / equivalente).
    4. Após autorização: `git add` + `git commit` local no submodule `main` com mensagem EXATA `feat(07.7-01): service excluirDocumento em contratos.js`.
    5. **Zero `git push`** em qualquer ponto. Zero commits no pai (bump é 07.7-03 Task 2).
    6. Reportar SHA do commit no final e encerrar a plan.
  </what-built>

  <read_first>
    - `.planning/phases/07.7-excluir-documento-individual/07.7-CONTEXT.md` §4 D-11 (5 pausas; esta é a pausa #1), D-14 (mensagem EXATA do commit)
    - `.planning/phases/07.2-excluir-contrato/07.2-01-PLAN.md` Task 3 Passos 2-3 — template literal do padrão commit-no-submodule-SEM-PUSH
    - `.planning/STATE.md` (confirmar padrão histórico "commits locais, aguarda autorização")
  </read_first>

  <how-to-verify>

**Passo 1 — Mostrar diff completo ao usuário (PAUSA #1):**

```bash
cd src/mr-3/mr-cobrancas
git status --short
# esperado: "M src/services/contratos.js"

git diff --stat HEAD
# esperado: 1 file changed, +N insertions, 0 deletions (N ≈ 70-90 linhas com comentário JSDoc)

git diff HEAD -- src/services/contratos.js
# diff completo da nova função excluirDocumento + header de seção.

git diff HEAD -- src/utils/devedorCalc.js src/services/pagamentos.js src/services/dividas.js
# DEVE SER VAZIO (gate invariante motor D-01, já confirmado na Task 2).

git rev-parse --abbrev-ref HEAD
# esperado: main

git log origin/main..main --oneline
# esperado: vazio antes desta task (nenhum commit local pendente da phase 7.7 ainda).
# ATENÇÃO: pode haver commits pendentes de phases anteriores que ainda não foram pushed — OK.
# A task verifica o DELTA desta plan, não o estado absoluto.
```

**Passo 2 — Reportar ao usuário (texto EXATO):**

> ```
> ## PAUSA #1 (07.7-01) — diff review antes do commit no submodule
>
> Gates determinísticos:
> - Regressão: 9/9 PASS
> - Build: exit 0
> - Motor Art.354 (devedorCalc.js + pagamentos.js + dividas.js) diff HEAD: VAZIO ✓ (invariante D-01)
> - Escopo do diff: APENAS src/services/contratos.js ✓
>
> Função adicionada: excluirDocumento(documentoId) — espelho estrutural de excluirContrato
> (D-02), com filtro documento_id em vez de contrato_id, pre-check único em pagamentos_divida
> (D-03/D-06), ordem DELETE dividas → documentos_contrato (D-04), e
> recalcularTotaisContrato(contratoId) OBRIGATÓRIO pós-delete (D-05).
>
> Diff completo acima.
>
> Autoriza commit no submodule src/mr-3/mr-cobrancas branch main com subject EXATO:
> `feat(07.7-01): service excluirDocumento em contratos.js` ?
>
> Responda **'approved'** ou **'commit it'** para prosseguir.
> Qualquer outra coisa cancela — contratos.js continua modificado localmente sem commit.
> ```

**AGUARDAR resposta do usuário.** NÃO prosseguir sem autorização explícita.

Se o usuário apontar correção: aplicar, re-rodar gates da Task 2, voltar a PAUSA #1. Se pedir abort: deixar `contratos.js` modificado localmente sem commit e reportar.

**Passo 3 — Após autorização: commit no submodule:**

```bash
cd src/mr-3/mr-cobrancas
git checkout main                                       # garantir que está em main (não detached HEAD)
git add src/services/contratos.js                       # APENAS este arquivo (não usar git add -A)
git status --short                                       # confirmar SOMENTE "M src/services/contratos.js" stageado

git commit -m "$(cat <<'EOF'
feat(07.7-01): service excluirDocumento em contratos.js

Phase 7.7 — permite excluir UM documento de um contrato sem precisar apagar e
recriar o contrato inteiro. Service layer puro, zero UI nesta plan.

Espelho estrutural de excluirContrato (Phase 7.2, contratos.js:329-371) com 2
diferenças (D-02):
  1. Filtro muda de contrato_id=eq.* para documento_id=eq.* nos DELETEs.
  2. Pós-delete OBRIGATÓRIO chama recalcularTotaisContrato(contratoId) (D-05),
     senão contrato.valor_total / num_documentos / num_parcelas_total ficam
     stale (mesmo helper que adicionarDocumento:283 usa).

Pre-check (D-03, D-06): rejeita se pagamentos_divida tiver alguma row apontando
pra parcela do documento. pagamentos_contrato NÃO é checado separadamente — SPs
de pagamento já escrevem em pagamentos_divida, capturado pelo pre-check único.

Ordem DELETE (D-04, por FK dividas.documento_id sem CASCADE):
  1. DELETE dividas WHERE documento_id = :id (pagamentos_divida e
     devedores_dividas caem via CASCADE).
  2. DELETE documentos_contrato WHERE id = :id.
  3. await recalcularTotaisContrato(contratoId) (D-05).

Contrato vazio pós-delete (num_documentos=0) é estado válido (D-08).
Evento forense em contratos_historico deferido (D-09 — precedente 7.5.1).

Motor Art.354 LITERALMENTE intocado — invariante estrutural D-01 (devedorCalc.js,
pagamentos.js, dividas.js sem diff). Regressão 9/9 verde. Build exit 0.

UI (botão + handler em DetalheContrato.jsx) vem em feat(07.7-02). Bump no pai
e UAT vêm em 07.7-03. Zero push nesta phase.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"

git log -1 --format=%H           # capturar SHA full
git log -1 --format=%h           # capturar SHA short (7 chars)
```

**Passo 4 — Reportar SHA final + estado "zero push":**

Formato EXATO no chat:

```
## 07.7-01 commitado LOCALMENTE no submodule (SEM PUSH)

Submodule src/mr-3/mr-cobrancas @ main:
- <SHA_short> feat(07.7-01): service excluirDocumento em contratos.js

Invariante motor Art.354: preservada (git diff HEAD vazio nos 3 arquivos do motor).
Regressão 9/9 verde. Build exit 0.

Próximo: Plan 07.7-02 (UI button + handler em DetalheContrato.jsx).
Bump no pai + UAT ficam em Plan 07.7-03 (pausas #4 e #5).
Zero push executado. Zero tag criada.
```

**Passo 5 — Confirmar SEM PUSH:**

```bash
cd src/mr-3/mr-cobrancas
git log origin/main..main --oneline | head -5
# esperado: ≥1 linha (o novo commit local).
# Se retornar 0 → algo fez push sem autorização. PARAR e reportar crítico.
```

**NÃO** executar `git push` em nenhum momento.
**NÃO** tocar no repo pai (bump é responsabilidade de 07.7-03 Task 2).

  </how-to-verify>

  <acceptance_criteria>
    **PAUSA #1 autorizada:**
    - Agente executou `git status --short` + `git diff --stat HEAD` + `git diff HEAD -- src/services/contratos.js` e mostrou ao usuário
    - Agente reportou os 4 gates da Task 2 (regressão 9/9, build exit 0, motor diff vazio, escopo diff = contratos.js)
    - Usuário respondeu `approved`, `commit it`, ou equivalente explícito ANTES do commit
    - Texto do chat contém marcador `PAUSA #1` ou equivalente

    **Commit correto:**
    - `cd src/mr-3/mr-cobrancas && git rev-parse --abbrev-ref HEAD` retorna `main`
    - `cd src/mr-3/mr-cobrancas && git log -1 --format=%s` retorna EXATAMENTE `feat(07.7-01): service excluirDocumento em contratos.js`
    - `cd src/mr-3/mr-cobrancas && git log -1 --name-only --format=` lista EXATAMENTE `src/services/contratos.js`
    - `cd src/mr-3/mr-cobrancas && git status --porcelain` retorna **vazio** (nada uncommitted)
    - `cd src/mr-3/mr-cobrancas && git log -1 --format=%b` contém as strings: `Phase 7.7`, `D-02`, `D-03`, `D-04`, `D-05`, `D-01`, `recalcularTotaisContrato`, `Co-Authored-By: Claude Opus 4.7`

    **Zero push:**
    - `cd src/mr-3/mr-cobrancas && git log origin/main..main --oneline` retorna ≥1 linha (commit local presente)
    - **NENHUM** comando `git push` foi executado em toda a plan
    - **NENHUMA** tag criada (`git tag -l "v1.4-phase7.7*"` retorna vazio)

    **Pai intocado nesta plan:**
    - No repo pai: `git log -1 --format=%s` NÃO contém `chore(07.7)` (bump é responsabilidade da plan 07.7-03)
    - No repo pai: `git status --short` pode mostrar `src/mr-3` como modified (ponteiro pendente) — **isso é esperado e intencional**
  </acceptance_criteria>

  <resume-signal>
    Usuário responde:
    - **`approved`** / **`commit it`** → agente executa commit no submodule e reporta SHA.
    - **`fix: ...`** → agente aplica correção na função, re-roda Task 2 gates, volta a apresentar PAUSA #1.
    - **`abort`** → contratos.js fica modificado localmente sem commit; reportar estado e encerrar.
  </resume-signal>

  <done>
    Commit `feat(07.7-01): service excluirDocumento em contratos.js` existe localmente no submodule `src/mr-3/mr-cobrancas` branch `main`. Zero push, zero bump no pai, zero tag. Pronto para a plan 07.7-02 (UI).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| Browser → Supabase REST via `sb()`/`dbGet`/`dbDelete` | Requests autenticados via anon key. RLS USING(true) WITH CHECK(true) permite qualquer row (D-10). Trust vem da camada de auth+RLS, não desta função. |
| Caller (UI handler) → `excluirDocumento(documentoId)` | `documentoId` vem de UI state — assumido não-tamperado (usuário só pode clicar em docs do contrato aberto). |

## STRIDE Threat Register

| Threat ID | Categoria | Componente | Disposição | Mitigação |
|-----------|-----------|------------|------------|-----------|
| T-07.7-01 | Tampering | `documentoId` arg | mitigate | Lookup prévio em `documentos_contrato` valida existência — doc inválido retorna `{ ok: false, motivo: "Documento não encontrado." }` sem executar DELETEs. `encodeURIComponent` em todos filtros previne PostgREST injection. |
| T-07.7-02 | Denial of Service | DELETE em massa via loop sem limite | accept | Função opera sobre UM documento (+ parcelas). Sem multiplicador — escala O(1) com N_parcelas pequeno (tipicamente 1-360). |
| T-07.7-03 | Information Disclosure | Exception de Supabase vaza schema interno via stack trace no toast | mitigate | Plan 07.7-02 Task responsibility — caller usa toast.error com texto genérico no catch em vez de `e.message`. Esta plan não expõe erros (service apenas retorna shape `{ ok, motivo }` em rejeições esperadas; exceções técnicas propagam pro caller). |
| T-07.7-04 | Elevation of Privilege | User apaga doc de contrato de outro usuário via ID manipulado | accept | RLS `USING(true) WITH CHECK(true)` permite qualquer user apagar qualquer row (D-10 — convenção herdada). Mitigação futura (não desta phase) requer revisão de RLS + concept de `owner_id` nas tables. Aceito porque escopo atual = 1 advogado/1 banco. |
| T-07.7-05 | Repudiation | Doc é apagado sem registro forense | accept | Evento em `contratos_historico` deferido (D-09, precedente 7.5.1 que deferiu `parcelas_editadas`). Backlog "Rastreabilidade forense de exclusões" — não bloqueia 7.7. |
| T-07.7-06 | Inconsistência de estado | Process crasha entre DELETE dividas e DELETE documentos_contrato | accept | Sem SP atômica (D-02). Estado reexecutável: próxima tentativa com o mesmo `documentoId` retorna `{ ok: true }` (DELETE idempotente) ou `"Documento não encontrado."`. recalcularTotaisContrato idempotente também. |
| T-07.7-07 | Inconsistência de totais | `recalcularTotaisContrato` falha silenciosamente após DELETE bem-sucedido | mitigate | Exceção propaga pro caller (sem try/catch) — UI mostra erro técnico e pode disparar `onCarregarTudo` pra sincronizar state. Estado no banco: doc foi apagado mas contrato.valor_total stale; próxima chamada de adicionarDocumento/excluirDocumento ou reload força recálculo. Aceito — não vaza segredo, só UI transitoriamente stale. |
</threat_model>

<verification>
Verificação final da plan 07.7-01:

1. **Função exportada:**
   - `grep -c "export async function excluirDocumento" src/mr-3/mr-cobrancas/src/services/contratos.js` = 1

2. **Pre-check único (D-03, D-06):**
   - `grep` confirma dbGet de `pagamentos_divida` por `divida_id=in.*` em excluirDocumento
   - `grep` confirma AUSÊNCIA de dbGet `pagamentos_contrato` em excluirDocumento (mas presença em excluirContrato da 7.2 preservada)

3. **Ordem DELETE (D-04):**
   - Linha do `sb("dividas", "DELETE", ..., documento_id=eq)` aparece ANTES da linha do `dbDelete("documentos_contrato", ...)` no arquivo

4. **Pós-delete (D-05):**
   - `grep -c "await recalcularTotaisContrato(contratoId)" src/mr-3/mr-cobrancas/src/services/contratos.js` ≥ 2 (1 existente em adicionarDocumento + 1 novo em excluirDocumento)

5. **Return shape:**
   - Arquivo contém `return { ok: true }` dentro do bloco de excluirDocumento
   - Arquivo contém `motivo: "Este documento possui pagamentos registrados — não pode ser excluído. Exclua os pagamentos primeiro."` EXATO
   - Arquivo contém `motivo: "Documento não encontrado."` EXATO

6. **Invariante motor Art.354 (D-01):**
   - `git diff HEAD~1 -- src/utils/devedorCalc.js src/services/pagamentos.js src/services/dividas.js` (no submodule, após commit) = vazio

7. **Testes + build:**
   - `npm run test:regressao` 9/9 PASS
   - `npm run build` exit 0

8. **Commit:**
   - `git log -1 --format=%s` no submodule main = `feat(07.7-01): service excluirDocumento em contratos.js`
   - `git log origin/main..main --oneline` ≥ 1 linha

9. **Zero push:**
   - Nenhum `git push` executado. Zero tag criada.
</verification>

<success_criteria>
Plan 07.7-01 completa quando:

1. Função `excluirDocumento(documentoId)` existe em `src/mr-3/mr-cobrancas/src/services/contratos.js`, exportada async, com estrutura espelhada de `excluirContrato` (D-02) + `recalcularTotaisContrato(contratoId)` pós-delete (D-05).
2. Pre-check único em `pagamentos_divida` implementado (D-03/D-06).
3. Ordem DELETE: `dividas` ANTES de `documentos_contrato` (D-04).
4. Return shape: `{ ok: true }` | `{ ok: false, motivo: "Este documento possui pagamentos registrados — não pode ser excluído. Exclua os pagamentos primeiro." }` | `{ ok: false, motivo: "Documento não encontrado." }`.
5. Motor Art.354 intocado — `git diff HEAD~1 -- src/utils/devedorCalc.js src/services/pagamentos.js src/services/dividas.js` vazio (D-01).
6. Regressão 9/9 verde, build exit 0 antes do commit (Task 2 gates).
7. PAUSA #1 autorizada pelo usuário explicitamente (`approved`/`commit it`) antes do commit (D-11).
8. 1 commit local no submodule `src/mr-3/mr-cobrancas` branch `main` com subject EXATO `feat(07.7-01): service excluirDocumento em contratos.js` (D-14).
9. Zero commits no repo pai nesta plan (bump é responsabilidade de 07.7-03 Task 2).
10. Zero `git push` executado. Zero tag criada.
</success_criteria>

<output>
Após completar a Task 3, criar `.planning/phases/07.7-excluir-documento-individual/07.7-01-SUMMARY.md` com:
- SHA full + short do commit feat(07.7-01) no submodule main
- Output dos 4 gates (regressão, build, motor diff, escopo diff)
- Texto literal da autorização do usuário na PAUSA #1
- Linha de código ou contador de linhas da função `excluirDocumento` adicionada
- Confirmação explícita: "zero push, zero bump no pai, zero tag"
- Ponteiro: próximo passo = Plan 07.7-02 (UI button + handler)
</output>
</content>
</invoke>