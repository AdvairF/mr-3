---
phase: 07.7-excluir-documento-individual
plan: 02
type: execute
wave: 2
depends_on:
  - 07.7-01
files_modified:
  - src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx
autonomous: false   # D-11: modo cansado — pausa #2 (diff review pré-commit) antes do feat(07.7-02). SEM PUSH.
requirements: []    # Gap closure — sem REQ-IDs formais. Contrato de verificação = SC-1..SC-5 em 07.7-CONTEXT.md §5 (especialmente SC-4 e SC-5).

must_haves:
  truths:
    - "Botão vermelho outline 'Excluir documento' renderiza no MESMO <div> do 'Editar parcelas' (DetalheContrato.jsx:765) dentro do card expandido do documento"
    - "handleExcluirDocumento(doc) exibe window.confirm com texto EXATO 'Tem certeza? Esta ação não pode ser desfeita.' (mesma string da Phase 7.2) — cancelar não executa nada"
    - "UI state cleanup acontece ANTES do service call (D-07): se editingDocId === doc.id → setEditingDocId(null); se expandedDoc === doc.id → setExpandedDoc(null). Previne render crash quando onCarregarTudo retorna lista sem o doc"
    - "Handler chama excluirDocumento(doc.id); rejeição `{ ok: false }` dispara toast.error(motivo); sucesso dispara toast.success + onCarregarTudo + refresh local de documentos (NÃO chama onVoltar — SC-4 fica no contrato pós-delete)"
    - "Handler NÃO chama onVoltar() — diferença proposital de handleExcluirContrato (doc some mas contrato permanece aberto, padrão handleDocumentoAdicionado:404-408)"
    - "Import de excluirDocumento adicionado em linha 8-9 junto ao bloco existente de imports de ../services/contratos.js"
    - "Motor Art.354 intocado — git diff HEAD -- src/utils/devedorCalc.js src/services/pagamentos.js src/services/dividas.js vazio (D-01)"
    - "npm run test:regressao 9/9 verde (motor intocado)"
    - "npm run build exit 0 (JSX válido, imports resolvem)"
    - "1 commit local submodule mr-3 branch main subject exato `feat(07.7-02): botão excluir documento + handleExcluirDocumento em DetalheContrato` (D-14) — SEM PUSH"
  artifacts:
    - path: "src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx"
      provides: "Botão 'Excluir documento' + handler + UI state cleanup + toast feedback"
      contains: "handleExcluirDocumento"
  key_links:
    - from: "DetalheContrato.jsx (handleExcluirDocumento)"
      to: "services/contratos.js (excluirDocumento da Plan 07.7-01)"
      via: "import nomeado na linha 8-9 + chamada dentro do handler"
      pattern: "excluirDocumento\\(doc\\.id\\)"
    - from: "handleExcluirDocumento (UI state cleanup ANTES do service call, D-07)"
      to: "setEditingDocId(null) + setExpandedDoc(null)"
      via: "condicional `if (editingDocId === doc.id)` + `if (expandedDoc === doc.id)` ANTES do await excluirDocumento"
      pattern: "editingDocId === doc\\.id.*expandedDoc === doc\\.id.*await excluirDocumento"
    - from: "handleExcluirDocumento (success path)"
      to: "onCarregarTudo + refresh local de documentos"
      via: "await onCarregarTudo() seguido de listarDocumentosPorContrato(contrato.id).then(setDocumentos) — padrão handleDocumentoAdicionado:404-408, SEM onVoltar()"
      pattern: "await onCarregarTudo\\(\\);?\\s*(listarDocumentosPorContrato|\\/\\/)"
    - from: "Botão 'Excluir documento' vermelho outline"
      to: "onClick={() => handleExcluirDocumento(doc)}"
      via: "sibling do 'Editar parcelas' no mesmo <div style={marginBottom: 8, textAlign: right}> da linha ~765"
      pattern: "Excluir documento"
---

<objective>
Adicionar em `src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx`:

1. **Import** de `excluirDocumento` (da Plan 07.7-01) no bloco de imports de `../services/contratos.js` (linha 8-9 do arquivo atual).
2. **Handler `handleExcluirDocumento(doc)`** com:
   - `window.confirm("Tem certeza? Esta ação não pode ser desfeita.")` — texto EXATO, mesma string da Phase 7.2 D-06.
   - **UI state cleanup ANTES do service call (D-07)**: `if (editingDocId === doc.id) setEditingDocId(null);` + `if (expandedDoc === doc.id) setExpandedDoc(null);`. Previne render crash quando `onCarregarTudo` retorna lista sem o doc (card expandido apontaria pra ID inexistente).
   - Chamada `excluirDocumento(doc.id)` → rejeição `{ ok: false }` dispara `toast.error(result.motivo)`; sucesso dispara `toast.success("Documento excluído com sucesso")`.
   - Refresh de estado: `await onCarregarTudo()` + `listarDocumentosPorContrato(contrato.id).then(setDocumentos)` (padrão literal de `handleDocumentoAdicionado:404-408`).
   - **NÃO chama `onVoltar()`** — diferença proposital vs `handleExcluirContrato` (SC-4: contrato vazio é estado válido, usuário fica no contrato).
   - Catch técnico: `toast.error("Erro ao excluir documento. Tente novamente.")` (texto genérico, não expor `e.message`).
3. **Botão "Excluir documento"** vermelho outline small, sibling do "Editar parcelas" EXISTENTE em `DetalheContrato.jsx:766-774`, dentro do mesmo `<div style={{ marginBottom: 8, textAlign: "right" }}>` da linha 765. Padrão visual da Phase 7.2 (`color="#dc2626"`, `sm`, `outline`).

Purpose: Advogado enxerga o botão "Excluir documento" no card expandido do documento, clica, confirma, doc some, contrato permanece aberto. Integra o service da Plan 07.7-01 na UI.

Output:
- 1 commit local no submodule `src/mr-3/mr-cobrancas` branch `main` com subject EXATO `feat(07.7-02): botão excluir documento + handleExcluirDocumento em DetalheContrato` (D-14).
- Zero commits no repo pai nesta plan (bump é 07.7-03 Task 2).
- Zero `git push`. Zero tag.

Contexto de conformidade:
- D-01 Motor Art.354 intocado — só UI + handler.
- D-02/D-03 (Phase 7.2) Template `handleExcluirContrato` (DetalheContrato.jsx:385-402) é referência estrutural, com 2 diferenças: (a) argumento `doc` em vez de fechado sobre `contrato`, (b) sem `onVoltar()` no sucesso.
- D-07 UI state cleanup ANTES do service call — regra invariante.
- D-08 Contrato vazio pós-delete é estado válido — não navega.
- D-09 Sem evento `contratos_historico`.
- D-10 Convenções: `toast` (já importado — ver `handleExcluirContrato:391/398`), submodule main, zero push sem autorização.
- D-11 Modo cansado: plan `autonomous: false` — Task 3 é pausa #2 de diff review.
- D-13 Flag 7.5.2 latente (encargos): se UAT da 07.7-03 detectar anomalia nos encargos de docs restantes, é backlog 7.5.2 PRÉ-EXISTENTE — não regressão desta plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07.7-excluir-documento-individual/07.7-CONTEXT.md

<!-- Plan predecessora (import do service vem daqui): -->
@.planning/phases/07.7-excluir-documento-individual/07.7-01-PLAN.md

<!-- Template literal de handler excluir + commit submodule SEM PUSH: -->
@.planning/phases/07.2-excluir-contrato/07.2-01-PLAN.md

<interfaces>
<!-- Interfaces-chave do codebase atual. Executor usa DIRETO sem re-explorar. -->

From src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx — ESTADO ATUAL (linhas verificadas pelo planner):

```javascript
// Linha 1: imports de React (useState já presente)
import { useState, useEffect, useMemo } from "react";

// Linhas 8-9: bloco de imports de services/contratos.js (estender com excluirDocumento):
import { listarDocumentosPorContrato, editarContrato, cascatearCredorDevedor, registrarEvento, listarHistorico,
         registrarPagamentoContrato, excluirPagamentoContrato, listarPagamentosContrato, excluirContrato } from "../services/contratos.js";
// ATENÇÃO: excluirContrato (adicionado em 7.2) já está lá. ADICIONAR excluirDocumento ao final da lista.

// Linhas 121-145: bloco de useState do componente (onde declarar novo state):
//   const [documentos, setDocumentos] = useState([]);                         // 121
//   const [loadingDocumentos, setLoadingDocumentos] = useState(false);        // 122
//   const [expandedDoc, setExpandedDoc] = useState(null);                     // 123 (USADO no cleanup D-07)
//   const [saldosMap, setSaldosMap] = useState({});                           // 124
//   ...
//   const [excluindoContrato, setExcluindoContrato] = useState(false);        // 141 (pattern 7.2)
//   const [editingDocId, setEditingDocId] = useState(null);                   // 142 (USADO no cleanup D-07)
//   const [savingParcelas, setSavingParcelas] = useState(false);              // 143
//   ...
// INSERIR depois da linha 141 (junto dos states de exclusão, pattern projeto):
//   const [excluindoDocumentoId, setExcluindoDocumentoId] = useState(null);   // NOVO — UUID do doc sendo excluído (pattern de excluindoPagamentoId linha 140)

// Linhas 385-402: handleExcluirContrato — TEMPLATE ESTRUTURAL (D-02 Phase 7.2):
async function handleExcluirContrato() {
  if (!window.confirm("Tem certeza? Esta ação não pode ser desfeita.")) return;
  setExcluindoContrato(true);
  try {
    const result = await excluirContrato(contrato.id);
    if (!result.ok) {
      toast.error(result.motivo);
      return;
    }
    toast.success("Contrato excluído com sucesso");
    await onCarregarTudo();
    onVoltar();                    // ← ESTE onVoltar() é o QUE NÃO REPRODUZIR no handler novo (D-08)
  } catch (e) {
    toast.error("Erro ao excluir contrato. Tente novamente.");
  } finally {
    setExcluindoContrato(false);
  }
}

// Linhas 404-408: handleDocumentoAdicionado — TEMPLATE para refresh local de documentos no sucesso:
async function handleDocumentoAdicionado() {
  setAdicionandoDocumento(false);
  await onCarregarTudo();
  listarDocumentosPorContrato(contrato.id)
    .then(docs => setDocumentos(Array.isArray(docs) ? docs : []));
}
// USO NO NOVO HANDLER: após toast.success, replicar await onCarregarTudo() +
// listarDocumentosPorContrato(contrato.id).then(setDocumentos) — garante que
// state local `documentos` reflete a ausência do doc sem depender de remount.

// Linhas 765-776: botão "Editar parcelas" EXISTENTE (Phase 7.5 D-04) — LOCALIZAÇÃO DO NOVO BOTÃO:
{editingDocId !== doc.id && (
  <div style={{ marginBottom: 8, textAlign: "right" }}>
    <Btn
      color="#4f46e5"
      sm
      outline
      onClick={() => setEditingDocId(doc.id)}
      disabled={savingParcelas}
    >
      Editar parcelas
    </Btn>
    {/* ← INSERIR AQUI o segundo <Btn> como sibling dentro do MESMO <div>, NÃO criar div novo */}
  </div>
)}
```

**Iterador do card expandido (para referência do scope onde doc está no lexical scope):**
O bloco `{editingDocId !== doc.id && (...)}` da linha 764 está DENTRO de um `.map(doc => ...)` que itera sobre `documentos`. Por isso `doc` está disponível no `onClick` do novo botão — é o mesmo contexto léxico onde `onClick={() => setEditingDocId(doc.id)}` já funciona.

**Btn component signature (padrão do projeto):** `<Btn color="..." sm outline onClick={...} disabled={...}>label</Btn>` — boolean flags posicionais. Estar importado no topo do arquivo. Padrão 7.2 para ações destrutivas = `color="#dc2626" sm outline`.

**Toast**: já importado no topo do arquivo (usado em `handleExcluirContrato:391`, `handleSalvarParcelasCustom:418/425` etc) — NÃO adicionar novo import.

Schema FKs (mesmo contexto da 07.7-01 — leitura pro handler): ver CONTEXT.md §7. O handler não manipula DB diretamente; delega ao service.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Estender imports + adicionar useState excluindoDocumentoId + implementar handleExcluirDocumento + renderizar botão "Excluir documento"</name>
  <files>src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx</files>

  <read_first>
    <!-- OBRIGATÓRIO ler antes de qualquer escrita -->
    - `src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx` linhas 1-20 (imports — localizar linha 8-9 com lista de imports de `../services/contratos.js`; confirmar que `excluirContrato` está presente da Phase 7.2 e que `toast` está importado no topo)
    - `src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx` linhas 100-150 (bloco de `useState` — confirmar `excludingContrato` em 141, `editingDocId` em 142, `expandedDoc` em 123, `excluindoPagamentoId` em 140 como pattern literal para o novo `excluindoDocumentoId`)
    - `src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx` linhas 383-410 (handlers `handleExcluirContrato` + `handleDocumentoAdicionado` — template ESTRUTURAL)
    - `src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx` linhas 760-800 (card expandido com botão "Editar parcelas" — alvo de inserção do novo botão)
    - `.planning/phases/07.7-excluir-documento-individual/07.7-CONTEXT.md` §4 D-07 (cleanup order OBRIGATÓRIA), D-08 (sem onVoltar), §5 SC-4 e SC-5 (edge cases que esta implementação cobre)
    - `.planning/phases/07.7-excluir-documento-individual/07.7-01-PLAN.md` (contrato exato de `excluirDocumento` — return shape, motivos de rejeição)
  </read_first>

  <action>
    **1. Estender import de services/contratos.js (linhas 8-9):**

    Estado atual (linhas 8-9 — copiado da leitura do planner; executor confirma o estado exato ao ler):
    ```javascript
    import { listarDocumentosPorContrato, editarContrato, cascatearCredorDevedor, registrarEvento, listarHistorico,
             registrarPagamentoContrato, excluirPagamentoContrato, listarPagamentosContrato, excluirContrato } from "../services/contratos.js";
    ```

    Trocar para (adicionar `excluirDocumento` ao final da lista, sem remover nada):
    ```javascript
    import { listarDocumentosPorContrato, editarContrato, cascatearCredorDevedor, registrarEvento, listarHistorico,
             registrarPagamentoContrato, excluirPagamentoContrato, listarPagamentosContrato, excluirContrato, excluirDocumento } from "../services/contratos.js";
    ```

    (Se a lista exata de imports divergir do estado mostrado — outra ordem ou quebra de linha — EXECUTOR deve preservar a ordem existente e apenas **apêndar** `excluirDocumento` antes do `} from "../services/contratos.js";`. O essencial: o símbolo `excluirDocumento` deve estar no bloco de imports desse service.)

    **2. Adicionar state de loading no bloco de useState (inserir IMEDIATAMENTE depois do `excluindoContrato` na linha 141):**

    Inserir NOVA linha depois da linha 141 (`const [excluindoContrato, setExcluindoContrato] = useState(false);`) e antes da linha 142 (`const [editingDocId, setEditingDocId] = useState(null);`):
    ```javascript
      const [excluindoDocumentoId,  setExcluindoDocumentoId]  = useState(null);   // Phase 7.7: UUID do doc sendo excluído (mesmo pattern de excluindoPagamentoId linha 140)
    ```

    **3. Adicionar handler `handleExcluirDocumento`:**

    Inserir IMEDIATAMENTE DEPOIS da função `handleExcluirContrato` (que termina na linha 402 atual com `}`) e ANTES de `async function handleDocumentoAdicionado()` (linha 404 atual).

    Código EXATO:
    ```javascript
      // Phase 7.7 — exclui UM documento específico. Cleanup de UI state ANTES do service
      // (D-07) previne render crash quando onCarregarTudo retorna lista sem o doc. Não
      // navega (SC-4: contrato vazio pós-delete é estado válido, usuário fica no contrato).
      async function handleExcluirDocumento(doc) {
        if (!window.confirm("Tem certeza? Esta ação não pode ser desfeita.")) return;

        // UI state cleanup ANTES do service call (D-07) — ordem OBRIGATÓRIA:
        // se o doc sendo excluído é o que está sendo editado ou expandido, limpar
        // antes do delete senão o próximo render aponta pra ID inexistente.
        if (editingDocId === doc.id) setEditingDocId(null);
        if (expandedDoc === doc.id) setExpandedDoc(null);

        setExcluindoDocumentoId(doc.id);
        try {
          const result = await excluirDocumento(doc.id);
          if (!result.ok) {
            toast.error(result.motivo);
            return;
          }
          toast.success("Documento excluído com sucesso");
          // Refresh de estado: global (App.jsx) + local (this component).
          // Pattern literal de handleDocumentoAdicionado:404-408.
          await onCarregarTudo();
          listarDocumentosPorContrato(contrato.id)
            .then(docs => setDocumentos(Array.isArray(docs) ? docs : []));
          // NÃO chama onVoltar() — diferença proposital vs handleExcluirContrato.
          // Contrato vazio pós-delete é estado válido (D-08); usuário fica na tela
          // do DetalheContrato para poder clicar "Adicionar documento" de novo.
        } catch (e) {
          toast.error("Erro ao excluir documento. Tente novamente.");
        } finally {
          setExcluindoDocumentoId(null);
        }
      }
    ```

    **4. Renderizar botão "Excluir documento" sibling do "Editar parcelas":**

    Localizar o bloco (linhas 764-776 do estado atual):
    ```jsx
    {editingDocId !== doc.id && (
      <div style={{ marginBottom: 8, textAlign: "right" }}>
        <Btn
          color="#4f46e5"
          sm
          outline
          onClick={() => setEditingDocId(doc.id)}
          disabled={savingParcelas}
        >
          Editar parcelas
        </Btn>
      </div>
    )}
    ```

    Trocar para (adicionar segundo `<Btn>` SIBLING, dentro do MESMO `<div>`, ANTES do "Editar parcelas" para alinhar à esquerda do flex, com gap implícito via `marginRight` no primeiro botão):
    ```jsx
    {editingDocId !== doc.id && (
      <div style={{ marginBottom: 8, textAlign: "right" }}>
        <Btn
          color="#dc2626"
          sm
          outline
          onClick={() => handleExcluirDocumento(doc)}
          disabled={excluindoDocumentoId === doc.id}
          style={{ marginRight: 8 }}
        >
          {excluindoDocumentoId === doc.id ? "Excluindo…" : "Excluir documento"}
        </Btn>
        <Btn
          color="#4f46e5"
          sm
          outline
          onClick={() => setEditingDocId(doc.id)}
          disabled={savingParcelas}
        >
          Editar parcelas
        </Btn>
      </div>
    )}
    ```

    **Nota sobre `style={{ marginRight: 8 }}` no Btn novo:** Se o componente `Btn` do projeto não aceita prop `style` (verificar via grep rápido em `src/components/Btn.jsx` ou similar — mas o padrão de handleExcluirPagamento e outros Btn inline no arquivo sugere que aceita), usar `<span style={{ marginRight: 8, display: "inline-block" }}>` envolvendo o primeiro Btn em vez de passar `style` ao Btn. Default: passar `style` no Btn — se build quebrar por prop não suportada, reverter para span wrapper.

    **Detalhes obrigatórios:**
    - `window.confirm` texto EXATO: `"Tem certeza? Esta ação não pode ser desfeita."` (mesma string da Phase 7.2 D-06).
    - UI state cleanup ANTES do `setExcluindoDocumentoId(...)` e ANTES do `await excluirDocumento` — ordem textual no código é **obrigatória** (D-07): linhas `if (editingDocId === doc.id) setEditingDocId(null);` + `if (expandedDoc === doc.id) setExpandedDoc(null);` aparecem ANTES da linha `setExcluindoDocumentoId(doc.id)` e da linha `const result = await excluirDocumento(doc.id)`.
    - `toast.error(result.motivo)` para rejeição `{ ok: false }` (mensagem vem do service, D-03 da Plan 07.7-01).
    - `toast.success("Documento excluído com sucesso")` para caminho feliz (string literal exata — checkable via grep).
    - `toast.error("Erro ao excluir documento. Tente novamente.")` no catch técnico (texto genérico — NÃO expor `e.message`).
    - Ordem do sucesso: `toast.success` → `await onCarregarTudo()` → `listarDocumentosPorContrato(contrato.id).then(setDocumentos)`. **Nunca** adicionar `onVoltar()` nessa ordem.
    - Botão novo: `color="#dc2626"` (vermelho literal, pattern 7.2), `sm`, `outline`, `onClick={() => handleExcluirDocumento(doc)}`, `disabled={excluindoDocumentoId === doc.id}`.
    - Label dinâmica: `{excluindoDocumentoId === doc.id ? "Excluindo…" : "Excluir documento"}` — previne double-click durante o service call.
    - NÃO tocar `handleExcluirContrato`, `handleExcluirPagamento`, `handleDocumentoAdicionado`, `handleSalvarParcelasCustom`, nem qualquer outro handler existente.
    - NÃO tocar `src/services/dividas.js`, `src/utils/devedorCalc.js`, `src/services/pagamentos.js` (invariante motor D-01).
    - NÃO usar `alert()` — projeto inteiro usa toast.
  </action>

  <verify>
    <automated>cd src/mr-3/mr-cobrancas &amp;&amp; npm run build 2&gt;&amp;1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    <!-- Cada item é grep-verificável. Rodar da raiz do submodule src/mr-3/mr-cobrancas. -->

    **Import estendido:**
    - `grep -c 'excluirDocumento' src/components/DetalheContrato.jsx` retorna ≥4 (1 no import + 1 chamada + 1 menção no JSDoc/comment opcional + 1 usage como nome de função do handler via grep de `handleExcluirDocumento` — mínimo 4 ocorrências do literal `excluirDocumento` no arquivo)
    - `grep -nE 'import \{[^}]*excluirDocumento[^}]*\} from "\.\./services/contratos\.js"' src/components/DetalheContrato.jsx` retorna **≥1 linha** (excluirDocumento presente no bloco de import do service)
    - Import do service original PRESERVADO: `grep -c 'excluirContrato' src/components/DetalheContrato.jsx` retorna ≥1 (Phase 7.2 não foi removida)

    **State novo:**
    - `grep -c 'excluindoDocumentoId, setExcluindoDocumentoId' src/components/DetalheContrato.jsx` retorna **1** (declaração única)
    - `grep -c 'setExcluindoDocumentoId(' src/components/DetalheContrato.jsx` retorna ≥2 (set doc.id + setNull no finally)

    **Handler correto:**
    - `grep -c 'async function handleExcluirDocumento' src/components/DetalheContrato.jsx` retorna **1**
    - `grep -c 'window.confirm("Tem certeza? Esta ação não pode ser desfeita.")' src/components/DetalheContrato.jsx` retorna **2** (handleExcluirContrato da 7.2 + handleExcluirDocumento novo)
    - `grep -c 'await excluirDocumento(doc.id)' src/components/DetalheContrato.jsx` retorna **1**
    - `grep -c 'toast.success("Documento excluído com sucesso")' src/components/DetalheContrato.jsx` retorna **1**
    - `grep -c 'toast.error("Erro ao excluir documento. Tente novamente.")' src/components/DetalheContrato.jsx` retorna **1**
    - `grep -c 'toast.error(result.motivo)' src/components/DetalheContrato.jsx` retorna ≥2 (handleExcluirContrato + handleExcluirDocumento — padrão comum)

    **UI state cleanup ANTES do service (D-07 — ordem textual):**
    - Padrão multiline obrigatório dentro de `handleExcluirDocumento`:
      `grep -nE 'editingDocId === doc\.id.*setEditingDocId\(null\)' src/components/DetalheContrato.jsx` retorna ≥1
      `grep -nE 'expandedDoc === doc\.id.*setExpandedDoc\(null\)' src/components/DetalheContrato.jsx` retorna ≥1
    - **Ordem verificação de linha** (executor roda awk/grep com numeração):
      `awk '/async function handleExcluirDocumento/,/^  \}/' src/components/DetalheContrato.jsx | grep -nE 'setEditingDocId\(null\)|setExpandedDoc\(null\)|await excluirDocumento'` retorna AS 3 LINHAS, com as 2 do cleanup ANTES da linha do `await excluirDocumento`

    **Refresh pattern (handleDocumentoAdicionado mirror):**
    - `grep -nA1 'await onCarregarTudo()' src/components/DetalheContrato.jsx | grep -c 'listarDocumentosPorContrato(contrato.id)'` retorna ≥2 (um em handleDocumentoAdicionado existente + um em handleExcluirDocumento novo — ou equivalente via awk no scope da função nova)
    - `awk '/async function handleExcluirDocumento/,/^  \}/' src/components/DetalheContrato.jsx | grep -c 'listarDocumentosPorContrato(contrato.id)'` retorna **1**

    **SEM onVoltar() no handler novo (D-08, SC-4):**
    - `awk '/async function handleExcluirDocumento/,/^  \}/' src/components/DetalheContrato.jsx | grep -c 'onVoltar()'` retorna **0** (handler NÃO navega pós-delete)
    - Garantia NEGATIVA: `awk '/async function handleExcluirDocumento/,/^  \}/' src/components/DetalheContrato.jsx | grep -cE 'onVoltar\s*\(' ` retorna **0**

    **Botão renderizado:**
    - `grep -c '>Excluir documento<' src/components/DetalheContrato.jsx` OU `grep -c '"Excluir documento"' src/components/DetalheContrato.jsx` retorna ≥1
    - `grep -c 'onClick={() => handleExcluirDocumento(doc)}' src/components/DetalheContrato.jsx` retorna **1**
    - `grep -c 'color="#dc2626"' src/components/DetalheContrato.jsx` retorna ≥2 (pattern 7.2 já tem 1 no header read-mode + 1 novo no card do doc)
    - `grep -c 'disabled={excluindoDocumentoId === doc.id}' src/components/DetalheContrato.jsx` retorna **1**

    **Motor Art.354 invariante (D-01):**
    - `cd src/mr-3/mr-cobrancas && git diff HEAD -- src/utils/devedorCalc.js src/services/pagamentos.js src/services/dividas.js` retorna **vazio** (0 bytes)

    **Escopo do diff:**
    - `cd src/mr-3/mr-cobrancas && git diff --name-only HEAD` retorna EXATAMENTE `src/components/DetalheContrato.jsx` (1 linha)

    **Build + regressão:**
    - `cd src/mr-3/mr-cobrancas && npm run build` exit 0
    - `cd src/mr-3/mr-cobrancas && npm run test:regressao` exit 0 (9/9 — só UI+handler, motor intocado)
  </acceptance_criteria>

  <done>
    Em `DetalheContrato.jsx`: import estendido com `excluirDocumento`, state `excluindoDocumentoId` declarado, handler `handleExcluirDocumento` implementado com cleanup ANTES do service (D-07), NÃO chamando `onVoltar` (D-08), botão vermelho outline "Excluir documento" renderizado como sibling do "Editar parcelas". Build limpo, regressão 9/9, motor intocado. **Ainda não commitado — Task 3 tem a pausa #2 de diff review.**
  </done>
</task>

<task type="auto">
  <name>Task 2: Gates determinísticos pós-implementação (build + regressão + motor diff + escopo)</name>
  <files>
    (nenhum arquivo modificado — só gates)
  </files>

  <read_first>
    - `.planning/phases/07.7-excluir-documento-individual/07.7-CONTEXT.md` §4 D-01 (INV motor), D-11 (pausa #2 é a Task 3 desta plan)
    - `.planning/phases/07.7-excluir-documento-individual/07.7-01-PLAN.md` Task 2 — mesma estrutura de gates
  </read_first>

  <action>
    Rodar os 4 gates determinísticos. Se QUALQUER falhar, NÃO avançar para Task 3. Reportar ao usuário com output completo do gate ofensor.

    ```bash
    cd src/mr-3/mr-cobrancas

    # Gate 1 — Regressão 9/9 (invariante motor Art.354 D-01):
    npm run test:regressao
    # Esperado: 9/9 PASS, exit 0.
    # Se falhar: diagnóstico — git diff HEAD pra auditar arquivos mexidos. Se motor foi tocado
    # (utils/devedorCalc.js, services/pagamentos.js, services/dividas.js) → reverter edição.

    # Gate 2 — Build limpo:
    npm run build
    # Esperado: exit 0. Se falhar: syntax error JSX ou import quebrado.
    # Checagem comum de falha: "excluirDocumento is not exported" → Plan 07.7-01 Task 3 não foi
    # commitada ainda? Sim está. Ou import mal estendido? Verificar linha 8-9.

    # Gate 3 — Invariante motor Art.354 (D-01):
    git diff HEAD -- src/utils/devedorCalc.js src/services/pagamentos.js src/services/dividas.js
    # Esperado: vazio (0 bytes). Qualquer byte → reportar e ABORTAR.

    # Gate 4 — Escopo do diff (APENAS DetalheContrato.jsx desta plan):
    git diff --name-only HEAD
    # Esperado: EXATAMENTE "src/components/DetalheContrato.jsx".
    # Qualquer outro arquivo → reportar e aguardar confirmação.
    # ATENÇÃO: `git diff HEAD` mostra só o que MUDOU desde o último commit.
    # O commit da 07.7-01 (em contratos.js) já foi feito no HEAD~1 — NÃO deve aparecer aqui.
    ```
  </action>

  <verify>
    <automated>cd src/mr-3/mr-cobrancas &amp;&amp; npm run test:regressao &amp;&amp; npm run build &amp;&amp; [ -z "$(git diff HEAD -- src/utils/devedorCalc.js src/services/pagamentos.js src/services/dividas.js)" ] &amp;&amp; [ "$(git diff --name-only HEAD)" = "src/components/DetalheContrato.jsx" ] &amp;&amp; echo "ALL GATES PASS"</automated>
  </verify>

  <acceptance_criteria>
    - `cd src/mr-3/mr-cobrancas && npm run test:regressao` exit 0 (9/9 PASS)
    - `cd src/mr-3/mr-cobrancas && npm run build` exit 0
    - `cd src/mr-3/mr-cobrancas && git diff HEAD -- src/utils/devedorCalc.js src/services/pagamentos.js src/services/dividas.js` retorna **vazio** (0 bytes)
    - `cd src/mr-3/mr-cobrancas && git diff --name-only HEAD` retorna EXATAMENTE `src/components/DetalheContrato.jsx` (1 linha)
    - `cd src/mr-3/mr-cobrancas && git log -1 --format=%s` retorna `feat(07.7-01): service excluirDocumento em contratos.js` (o commit anterior, da Plan 07.7-01, está íntegro)
  </acceptance_criteria>

  <done>
    Todos os gates determinísticos passaram. Safe para apresentar diff review ao usuário na PAUSA #2 (Task 3).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: PAUSA #2 — diff review antes do commit submodule (D-11 modo cansado)</name>
  <files>
    src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx (commit local no submodule main — SEM PUSH)
  </files>

  <what-built>
    Task 1 estendeu imports, adicionou state `excluindoDocumentoId`, handler `handleExcluirDocumento`, e botão "Excluir documento" em `DetalheContrato.jsx`. Task 2 confirmou 4 gates verdes.

    Esta Task 3 é a **pausa #2 das 5 pausas totais do modo cansado (D-11)**:

    1. Agente mostra ao usuário: `git status --short` + `git diff --stat HEAD` + `git diff HEAD -- src/components/DetalheContrato.jsx` no submodule.
    2. Agente confirma: motor Art.354 intocado, regressão 9/9, build exit 0, diff escopo correto.
    3. Aguarda autorização explícita do usuário (`approved` / `commit it`).
    4. Após autorização: commit local no submodule `main` com mensagem EXATA `feat(07.7-02): botão excluir documento + handleExcluirDocumento em DetalheContrato`.
    5. **Zero `git push`**. Zero commit no pai (bump é 07.7-03 Task 2).
    6. Reportar SHA + encerrar plan.
  </what-built>

  <read_first>
    - `.planning/phases/07.7-excluir-documento-individual/07.7-CONTEXT.md` §4 D-11 (pausa #2), D-14 (mensagem EXATA)
    - `.planning/phases/07.7-excluir-documento-individual/07.7-01-PLAN.md` Task 3 — template da pausa #1, mesma estrutura
    - `.planning/phases/07.2-excluir-contrato/07.2-01-PLAN.md` Task 3 — padrão commit-submodule-SEM-PUSH
  </read_first>

  <how-to-verify>

**Passo 1 — Mostrar diff completo ao usuário (PAUSA #2):**

```bash
cd src/mr-3/mr-cobrancas
git status --short
# esperado: "M src/components/DetalheContrato.jsx"

git diff --stat HEAD
# esperado: 1 file changed, +N insertions, 0 deletions (N ≈ 35-50 linhas: ~25 handler + ~8 JSX botão + 1 import + 1 useState)

git diff HEAD -- src/components/DetalheContrato.jsx
# diff completo da extensão (import + useState + handler + botão).

git diff HEAD -- src/utils/devedorCalc.js src/services/pagamentos.js src/services/dividas.js
# DEVE SER VAZIO (invariante motor D-01, já confirmado na Task 2).

git log --oneline origin/main..main | head -5
# esperado: ≥1 linha, a mais recente sendo `feat(07.7-01): service excluirDocumento em contratos.js` (commit da plan anterior pendente de push).

git rev-parse --abbrev-ref HEAD
# esperado: main
```

**Passo 2 — Reportar ao usuário (texto EXATO):**

> ```
> ## PAUSA #2 (07.7-02) — diff review antes do commit no submodule
>
> Gates determinísticos:
> - Regressão: 9/9 PASS
> - Build: exit 0
> - Motor Art.354 (devedorCalc.js + pagamentos.js + dividas.js) diff HEAD: VAZIO ✓ (invariante D-01)
> - Escopo do diff: APENAS src/components/DetalheContrato.jsx ✓
>
> Mudanças:
> - Import: excluirDocumento adicionado ao bloco de imports de ../services/contratos.js (linha 8-9)
> - useState novo: excluindoDocumentoId (UUID do doc sendo excluído; pattern de excluindoPagamentoId)
> - Handler: handleExcluirDocumento(doc) com window.confirm, cleanup ANTES do service (D-07:
>   setEditingDocId(null) se matching + setExpandedDoc(null) se matching), toast success/error,
>   refresh via onCarregarTudo + listarDocumentosPorContrato. SEM onVoltar (D-08 — contrato
>   vazio é estado válido, usuário fica no contrato).
> - Botão: <Btn color="#dc2626" sm outline ...> sibling de "Editar parcelas" no card expandido.
>
> Commit pendente no submodule main:
> - HEAD: feat(07.7-01): service excluirDocumento em contratos.js (já commitado na plan 07.7-01)
> - A commitar: feat(07.7-02): botão excluir documento + handleExcluirDocumento em DetalheContrato
>
> Autoriza commit no submodule src/mr-3/mr-cobrancas branch main com subject EXATO:
> `feat(07.7-02): botão excluir documento + handleExcluirDocumento em DetalheContrato` ?
>
> Responda **'approved'** ou **'commit it'** para prosseguir.
> Qualquer outra coisa cancela — DetalheContrato.jsx continua modificado localmente sem commit.
> ```

**AGUARDAR resposta do usuário.** NÃO prosseguir sem autorização explícita.

Se usuário apontar correção: aplicar, re-rodar Task 2 gates, voltar a PAUSA #2.

**Passo 3 — Após autorização: commit no submodule:**

```bash
cd src/mr-3/mr-cobrancas
git checkout main                                                  # garantir main
git add src/components/DetalheContrato.jsx                         # APENAS este arquivo (não usar git add -A)
git status --short                                                  # confirmar SOMENTE "M src/components/DetalheContrato.jsx" stageado

git commit -m "$(cat <<'EOF'
feat(07.7-02): botão excluir documento + handleExcluirDocumento em DetalheContrato

Phase 7.7 — UI layer da funcionalidade "excluir documento individual".
Integra o service excluirDocumento da feat(07.7-01).

Mudanças em src/components/DetalheContrato.jsx:
1. Import: excluirDocumento adicionado ao bloco de imports de ../services/contratos.js.
2. useState novo: excluindoDocumentoId (UUID do doc em delete em andamento; mesmo
   pattern de excluindoPagamentoId — permite disabled no botão do card correto
   quando múltiplos docs existem).
3. Handler handleExcluirDocumento(doc):
   - window.confirm com texto EXATO "Tem certeza? Esta ação não pode ser desfeita."
     (mesma string da Phase 7.2 D-06).
   - UI state cleanup ANTES do service call (D-07, ordem OBRIGATÓRIA):
       if (editingDocId === doc.id) setEditingDocId(null);
       if (expandedDoc === doc.id) setExpandedDoc(null);
     Previne render crash quando onCarregarTudo retorna lista sem o doc (card
     expandido apontaria pra ID inexistente).
   - Rejeição { ok: false } → toast.error(result.motivo).
   - Sucesso → toast.success + await onCarregarTudo + refresh local via
     listarDocumentosPorContrato (pattern de handleDocumentoAdicionado:404-408).
   - NÃO chama onVoltar (D-08) — contrato vazio pós-delete é estado válido;
     usuário fica no DetalheContrato, podendo clicar "Adicionar documento".
   - Catch técnico: toast.error genérico, sem expor e.message.
4. Botão "Excluir documento" (vermelho outline #dc2626 sm) como sibling do
   "Editar parcelas" existente no card expandido (linha ~765). Label dinâmica
   "Excluindo…" / "Excluir documento" previne double-click.

Motor Art.354 (devedorCalc.js + pagamentos.js + dividas.js) LITERALMENTE intocado
— invariante estrutural D-01 preservada.
Contrato de onVoltar/onCarregarTudo/setDocumentos preservado.
Convenções de toast mantidas (nenhum alert() novo).
Regressão 9/9 verde. Build exit 0.

Bump no pai + UAT localhost (SC-1..SC-5) ficam em 07.7-03. Zero push nesta phase.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"

git log -1 --format=%H         # SHA full
git log -1 --format=%h         # SHA short
git log origin/main..main --oneline | head -5
# esperado: 2 linhas (07.7-02 mais recente, 07.7-01 em seguida)
```

**Passo 4 — Reportar SHA final + estado:**

```
## 07.7-02 commitado LOCALMENTE no submodule (SEM PUSH)

Submodule src/mr-3/mr-cobrancas @ main (2 commits locais pendentes de push):
- <SHA_07.7-02_short> feat(07.7-02): botão excluir documento + handleExcluirDocumento em DetalheContrato
- <SHA_07.7-01_short> feat(07.7-01): service excluirDocumento em contratos.js

Invariante motor Art.354: preservada em ambos os commits (git diff HEAD~2 vazio nos 3 arquivos do motor).
Regressão 9/9 verde. Build exit 0.

Próximo: Plan 07.7-03 — UAT localhost (SC-1..SC-5) + PAUSA #3 (UAT gate humano) + PAUSA #4 (diff consolidado review) + PAUSA #5 (bump commit review). Zero push executado. Zero tag.
```

**Passo 5 — Confirmar SEM PUSH:**

```bash
cd src/mr-3/mr-cobrancas
git log origin/main..main --oneline
# esperado: exatamente 2 linhas (07.7-02 + 07.7-01).
# Se retornar 0 ou 1 → algo está errado. Reportar.
```

**NÃO** executar `git push`.
**NÃO** tocar no repo pai (bump é 07.7-03).

  </how-to-verify>

  <acceptance_criteria>
    **PAUSA #2 autorizada:**
    - Agente executou `git status --short` + `git diff --stat HEAD` + `git diff HEAD -- src/components/DetalheContrato.jsx` e mostrou ao usuário
    - Agente reportou os 4 gates verdes da Task 2
    - Usuário respondeu `approved`, `commit it`, ou equivalente explícito ANTES do commit
    - Texto do chat contém marcador `PAUSA #2` ou equivalente

    **Commit correto:**
    - `cd src/mr-3/mr-cobrancas && git rev-parse --abbrev-ref HEAD` retorna `main`
    - `cd src/mr-3/mr-cobrancas && git log -1 --format=%s` retorna EXATAMENTE `feat(07.7-02): botão excluir documento + handleExcluirDocumento em DetalheContrato`
    - `cd src/mr-3/mr-cobrancas && git log -1 --name-only --format=` lista EXATAMENTE `src/components/DetalheContrato.jsx`
    - `cd src/mr-3/mr-cobrancas && git log -2 --format=%s origin/main..main` retorna EXATAMENTE 2 linhas:
      1. `feat(07.7-02): botão excluir documento + handleExcluirDocumento em DetalheContrato`
      2. `feat(07.7-01): service excluirDocumento em contratos.js`
    - `cd src/mr-3/mr-cobrancas && git status --porcelain` retorna **vazio**
    - `cd src/mr-3/mr-cobrancas && git log -1 --format=%b` contém as strings: `Phase 7.7`, `D-07`, `D-08`, `D-01`, `handleExcluirDocumento`, `Co-Authored-By: Claude Opus 4.7`

    **Zero push:**
    - `cd src/mr-3/mr-cobrancas && git log origin/main..main --oneline | wc -l` retorna **2** (exatamente 2 commits locais pendentes)
    - **NENHUM** comando `git push` foi executado
    - **NENHUMA** tag criada (`git tag -l "v1.4-phase7.7*"` retorna vazio)

    **Pai intocado nesta plan:**
    - No repo pai: `git log -1 --format=%s` NÃO contém `chore(07.7)`
    - No repo pai: `git status --short` pode mostrar `src/mr-3` como modified (esperado; bump é 07.7-03 Task 2)
  </acceptance_criteria>

  <resume-signal>
    Usuário responde:
    - **`approved`** / **`commit it`** → agente executa commit e reporta SHA.
    - **`fix: ...`** → agente aplica correção, re-roda Task 2 gates, volta a PAUSA #2.
    - **`abort`** → DetalheContrato.jsx fica modificado localmente sem commit.
  </resume-signal>

  <done>
    Commit `feat(07.7-02): botão excluir documento + handleExcluirDocumento em DetalheContrato` existe no submodule `src/mr-3/mr-cobrancas` branch `main`. 2 commits locais da Phase 7.7 (07.7-01 + 07.7-02) pendentes de push. Zero push, zero bump no pai, zero tag. Pronto para a plan 07.7-03 (UAT + bump + double checkpoint).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| Usuário (browser) → handleExcluirDocumento(doc) | `doc` vem do `.map` do state `documentos` local do componente. Não é input externo direto — user só pode disparar o handler clicando em um doc já renderizado. |
| handleExcluirDocumento → excluirDocumento(doc.id) | Delega validação e DB ops ao service (Plan 07.7-01 já tem trust model cobrindo isso). |
| handleExcluirDocumento → onCarregarTudo + setDocumentos | Confia no contrato de props injetado pelo App.jsx (pattern consolidado — Phase 7.2 já usa handleExcluirContrato sem revalidação). |

## STRIDE Threat Register

| Threat ID | Categoria | Componente | Disposição | Mitigação |
|-----------|-----------|------------|------------|-----------|
| T-07.7-08 | Tampering (race condition) | User clica "Excluir" em 2 docs diferentes em sequência rápida | mitigate | `excluindoDocumentoId === doc.id` é por-doc (UUID específico). Botões de docs diferentes operam independentemente. Duplo-click no MESMO doc: `disabled={excluindoDocumentoId === doc.id}` bloqueia até finally limpar. |
| T-07.7-09 | Denial of Service | User spam-clica "Excluir documento" | accept | `disabled` no botão bloqueia. Se user bypassar via devtools: service é idempotente (2a chamada retorna `Documento não encontrado` = `{ ok: false }`, UI mostra toast.error). Sem amplificação. |
| T-07.7-10 | Information Disclosure | Catch técnico vaza detalhes de Supabase via `e.message` | mitigate | Uso EXPLÍCITO de texto genérico `"Erro ao excluir documento. Tente novamente."` em vez de `e.message`. Console do browser ainda tem o erro — acessível ao dev, não ao user casual. |
| T-07.7-11 | Render crash pós-delete | `onCarregarTudo` refresca mas state local `editingDocId`/`expandedDoc` ainda aponta pra doc deletado | mitigate | **Cleanup ANTES do service call (D-07)** — ver grep de ordem textual nas acceptance criteria. Cobre SC-5 do UAT. |
| T-07.7-12 | Repudiation | Sem evento em `contratos_historico` | accept | D-09: deferido, precedente 7.5.1. Backlog "Rastreabilidade forense de exclusões". |
| T-07.7-13 | Bug visual (D-13 latente) | Docs restantes mostram encargos stale pós-delete | accept | Flag 7.5.2 latente: se UAT 07.7-03 reproduzir, é backlog pré-existente (memory: `project_phase752_backlog_encargos.md`). NÃO regressão desta phase. |
| T-07.7-14 | CSS break | Dois Btn no mesmo <div> com textAlign:right sem gap explícito | mitigate | `style={{ marginRight: 8 }}` no primeiro Btn garante spacing. Se Btn não aceita `style`, fallback: `<span style={{ marginRight: 8 }}>…</span>` wrapper (documentado no action). |
</threat_model>

<verification>
Verificação final da plan 07.7-02:

1. **Import estendido:**
   - `grep` confirma `excluirDocumento` no bloco de imports de `../services/contratos.js` (linha 8-9 do arquivo estendida).

2. **State novo:**
   - `grep -c 'excluindoDocumentoId' DetalheContrato.jsx` ≥ 4 (declaração + set(doc.id) + set(null) + dependency na prop `disabled`).

3. **Handler completo:**
   - `handleExcluirDocumento` existe com cleanup D-07 ANTES do service call (ordem textual verificável via `awk /from/,/to/` no arquivo).
   - NÃO contém `onVoltar()` (grep negativo no scope da função).

4. **Botão renderizado:**
   - `>Excluir documento<` ou `"Excluir documento"` presente; `#dc2626` presente (≥2 total no arquivo).

5. **Invariante motor Art.354 (D-01):**
   - `git diff HEAD~2 -- src/utils/devedorCalc.js src/services/pagamentos.js src/services/dividas.js` (no submodule, após commit das plans 07.7-01 e 07.7-02) = vazio.

6. **Testes + build:**
   - `npm run test:regressao` 9/9 PASS.
   - `npm run build` exit 0.

7. **Commit:**
   - `git log -1 --format=%s` no submodule main = `feat(07.7-02): botão excluir documento + handleExcluirDocumento em DetalheContrato`.
   - `git log origin/main..main --oneline` = 2 linhas (07.7-02 + 07.7-01).

8. **Zero push + zero tag + zero bump no pai nesta plan.**
</verification>

<success_criteria>
Plan 07.7-02 completa quando:

1. `DetalheContrato.jsx` tem import estendido com `excluirDocumento`, state `excluindoDocumentoId`, handler `handleExcluirDocumento`, e botão "Excluir documento" vermelho outline sibling do "Editar parcelas".
2. Handler:
   - usa `window.confirm("Tem certeza? Esta ação não pode ser desfeita.")` EXATO (D-06 da 7.2);
   - faz UI state cleanup ANTES do service call (D-07) — `editingDocId` + `expandedDoc` checked e limpos ANTES de `setExcluindoDocumentoId` e `await excluirDocumento`;
   - NÃO chama `onVoltar()` no success path (D-08);
   - refresca via `await onCarregarTudo()` + `listarDocumentosPorContrato(contrato.id).then(setDocumentos)` (pattern handleDocumentoAdicionado);
   - trata rejeição com `toast.error(result.motivo)`, sucesso com `toast.success("Documento excluído com sucesso")`, catch com `toast.error("Erro ao excluir documento. Tente novamente.")`.
3. Motor Art.354 intocado (D-01): `git diff HEAD -- src/utils/devedorCalc.js src/services/pagamentos.js src/services/dividas.js` vazio.
4. Regressão 9/9 verde, build exit 0.
5. PAUSA #2 autorizada pelo usuário explicitamente (D-11).
6. 1 commit local no submodule `main` com subject EXATO `feat(07.7-02): botão excluir documento + handleExcluirDocumento em DetalheContrato` (D-14).
7. 2 commits totais pendentes de push no submodule main (07.7-01 + 07.7-02).
8. Zero commit no repo pai nesta plan. Zero `git push`. Zero tag.
</success_criteria>

<output>
Após completar a Task 3, criar `.planning/phases/07.7-excluir-documento-individual/07.7-02-SUMMARY.md` com:
- SHA full + short do commit feat(07.7-02) no submodule main
- Recap dos 4 gates (regressão, build, motor diff, escopo diff)
- Texto literal da autorização do usuário na PAUSA #2
- Lista dos 2 commits pendentes de push no submodule main (07.7-01 + 07.7-02)
- Confirmação: "zero push, zero bump no pai, zero tag"
- Ponteiro: próximo passo = Plan 07.7-03 (UAT SC-1..SC-5 + PAUSA #3 + PAUSA #4 + PAUSA #5 + bump pai)
</output>
</content>
</invoke>