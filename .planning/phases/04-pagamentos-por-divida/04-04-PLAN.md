---
phase: 4
plan: 4
type: execute
wave: 3
depends_on: [04-01, 04-02, 04-03]
gap_closure: true
files_modified:
  - src/mr-3/mr-cobrancas/src/components/PagamentosDivida.jsx
  - src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx
autonomous: true
requirements: [PAG-05, PAG-07]

must_haves:
  truths:
    - "useEffect de PagamentosDivida chama recalcularESincronizar após listarPagamentos no mount"
    - "Saldo Atualizado em DetalheDivida reflete pagamentos_divida ao abrir a tela sem mutação prévia"
    - "saldo_quitado no banco atualiza automaticamente ao abrir tela de dívida com pagamentos suficientes"
    - "Total Pago em DetalheDivida exibe soma de pagamentos_divida da dívida (não pagamentos_parciais)"
    - "devedorCalc.js não foi modificado"
    - "pagamentos_parciais não foi alterado"
  artifacts:
    - path: "src/mr-3/mr-cobrancas/src/components/PagamentosDivida.jsx"
      provides: "recalcularESincronizar chamada no mount após listarPagamentos"
      contains: "recalcularESincronizar(lista)"
    - path: "src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx"
      provides: "Total Pago calculado a partir de pagamentos_divida via state local"
      contains: "totalPagoDivida"
  key_links:
    - from: "PagamentosDivida.useEffect"
      to: "recalcularESincronizar"
      via: "chamada no .then() do listarPagamentos, após setPagamentos"
    - from: "recalcularESincronizar"
      to: "onSaldoChange"
      via: "propaga novoSaldo para DetalheDivida.setSaldoDividaLocal no mount"
    - from: "PagamentosDivida"
      to: "DetalheDivida.totalPagoDivida"
      via: "prop callback onTotalPagoChange ou soma local no estado pagamentos"
---

<objective>
Corrigir dois bugs críticos de integração identificados no UAT (CR-03):

Bug 1 — PAG-05/PAG-07: `recalcularESincronizar` só era chamada após mutação, nunca no mount.
Resultado: ao abrir DetalheDivida, "Saldo Atualizado" mostrava o valor calculado pelos
`pagamentos_parciais` (motor antigo) e `saldo_quitado` nunca era sincronizado na carga inicial.

Bug 2 — PAG-05: "Total Pago" em DetalheDivida calculava via `calcularTotalPagoPorDivida`
sobre `pagamentos_parciais` — invisível para `pagamentos_divida`. Exibia R$ 0,00 mesmo com
pagamentos registrados na nova tabela.

Causa raiz: o plano 04-03 integrou o componente mas não fechou o ciclo de leitura inicial.
`recalcularESincronizar` foi corretamente chamada em mutações; faltou a chamada no useEffect.
`totalPago` em DetalheDivida nunca foi adaptado para ler `pagamentos_divida`.

NÃO tocar: `devedorCalc.js`, `pagamentos_parciais`, nenhuma tabela DB, nenhum service.
Fixes são puramente no lado React — ~15 linhas no total.

Output: PagamentosDivida.jsx e DetalheDivida.jsx corrigidos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-pagamentos-por-divida/04-CONTEXT.md
@.planning/phases/04-pagamentos-por-divida/04-02-SUMMARY.md
@.planning/phases/04-pagamentos-por-divida/04-03-SUMMARY.md

<interfaces>
<!-- Estado atual de PagamentosDivida.jsx — useEffect relevante (linhas 49-61) -->
```js
// ATUAL — não chama recalcularESincronizar no mount:
useEffect(() => {
  setLoading(true);
  listarPagamentos(divida.id)
    .then(data => {
      setPagamentos(Array.isArray(data) ? data : []);
    })
    .catch(e => toast.error("Erro ao carregar pagamentos: " + e.message))
    .finally(() => setLoading(false));
}, [divida.id]);

// CORRETO — adicionar chamada após setPagamentos:
useEffect(() => {
  setLoading(true);
  listarPagamentos(divida.id)
    .then(data => {
      const lista = Array.isArray(data) ? data : [];
      setPagamentos(lista);
      recalcularESincronizar(lista);   // Fix A: sincroniza saldo + saldo_quitado no mount
    })
    .catch(e => toast.error("Erro ao carregar pagamentos: " + e.message))
    .finally(() => setLoading(false));
}, [divida.id]);
```

<!-- Estado atual de DetalheDivida.jsx — Total Pago (linhas 84-85) -->
```js
// ATUAL — lê pagamentos_parciais via devedorCalc:
const pagoPorDividaMap = devedor ? calcularTotalPagoPorDivida(devedor, pagamentosDoDevedor, hoje) : {};
const totalPago = pagoPorDividaMap[String(divida.id)] ?? 0;

// CORRETO — Fix B: somar pagamentos_divida via state propagado de PagamentosDivida.
// Abordagem: adicionar prop onTotalPagoChange a PagamentosDivida que emite a soma
// toda vez que recalcularESincronizar roda (incluindo no mount).
// DetalheDivida adiciona state totalPagoDivida (null inicial) e usa-o quando não-null.
// Quando null (antes do primeiro carregamento), exibe "—" em vez de R$ 0,00.
```

<!-- Contrato de PagamentosDivida (atualizado com nova prop) -->
```js
// Props: { divida, hoje, onSaldoChange, onTotalPagoChange? }
// onTotalPagoChange: (totalPago: number) => void — opcional, chamado em recalcularESincronizar
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Fix A: Chamar recalcularESincronizar no mount em PagamentosDivida.jsx</name>
  <read_first>
    src/mr-3/mr-cobrancas/src/components/PagamentosDivida.jsx (arquivo completo)
    .planning/phases/04-pagamentos-por-divida/04-CONTEXT.md (D-02, D-07)
  </read_first>
  <files>src/mr-3/mr-cobrancas/src/components/PagamentosDivida.jsx</files>
  <action>
Editar `PagamentosDivida.jsx` com duas mudanças cirúrgicas:

**MUDANÇA 1 — Adicionar prop `onTotalPagoChange` à assinatura da função:**

Localizar:
```js
export default function PagamentosDivida({ divida, hoje, onSaldoChange }) {
```

Substituir por:
```js
export default function PagamentosDivida({ divida, hoje, onSaldoChange, onTotalPagoChange }) {
```

**MUDANÇA 2 — Emitir total pago em `recalcularESincronizar`:**

Localizar a função `recalcularESincronizar`. Após `if (onSaldoChange) onSaldoChange(novoSaldo);`,
adicionar:
```js
if (onTotalPagoChange) {
  const total = (listaPagamentos || []).reduce((s, p) => s + (parseFloat(p.valor) || 0), 0);
  onTotalPagoChange(total);
}
```

A função completa deve ficar:
```js
async function recalcularESincronizar(listaPagamentos) {
  const novoSaldo = calcularSaldoPorDividaIndividual(divida, listaPagamentos, hoje);
  const quitado = novoSaldo <= 0;
  try {
    await atualizarSaldoQuitado(divida.id, quitado);
  } catch (e) {
    toast.error("Aviso: falha ao sincronizar status quitado — " + e.message);
  }
  if (onSaldoChange) onSaldoChange(novoSaldo);
  if (onTotalPagoChange) {
    const total = (listaPagamentos || []).reduce((s, p) => s + (parseFloat(p.valor) || 0), 0);
    onTotalPagoChange(total);
  }
}
```

**MUDANÇA 3 — Chamar recalcularESincronizar no mount (useEffect):**

Localizar o useEffect que chama `listarPagamentos`. Substituir o `.then` de:
```js
.then(data => {
  setPagamentos(Array.isArray(data) ? data : []);
})
```
Por:
```js
.then(data => {
  const lista = Array.isArray(data) ? data : [];
  setPagamentos(lista);
  recalcularESincronizar(lista);
})
```

**RESTRIÇÃO:** `recalcularESincronizar` já existe — apenas adicionar a chamada no mount e
o emit de `onTotalPagoChange`. Não alterar a lógica interna de cálculo. Não alterar handlers
de criar/editar/excluir (eles já chamam recalcularESincronizar corretamente).
  </action>
  <verify>
    <automated>grep -n "recalcularESincronizar\|onTotalPagoChange\|lista\b" src/mr-3/mr-cobrancas/src/components/PagamentosDivida.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "onTotalPagoChange" src/mr-3/mr-cobrancas/src/components/PagamentosDivida.jsx` retorna pelo menos 3 matches (prop, if-guard, chamada)
    - `grep "recalcularESincronizar(lista)" src/mr-3/mr-cobrancas/src/components/PagamentosDivida.jsx` retorna 1 match dentro do useEffect
    - `grep "setPagamentos(lista)" src/mr-3/mr-cobrancas/src/components/PagamentosDivida.jsx` retorna 1 match (lista, não data)
    - handleCriar, handleSalvarEdit e handleExcluir ainda chamam recalcularESincronizar (não removidos)
  </acceptance_criteria>
  <done>PagamentosDivida chama recalcularESincronizar no mount, propagando saldo e total pago para DetalheDivida imediatamente ao abrir a tela.</done>
</task>

<task type="auto">
  <name>Fix B: Substituir Total Pago em DetalheDivida para ler pagamentos_divida</name>
  <read_first>
    src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx (arquivo completo)
    .planning/phases/04-pagamentos-por-divida/04-CONTEXT.md (D-02, D-08)
  </read_first>
  <files>src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx</files>
  <action>
Editar `DetalheDivida.jsx` com duas mudanças cirúrgicas:

**MUDANÇA 1 — Adicionar state para total pago local (junto ao state saldoDividaLocal):**

Localizar:
```js
const [saldoDividaLocal, setSaldoDividaLocal] = useState(null);
```

Adicionar na linha seguinte:
```js
const [totalPagoDivida, setTotalPagoDivida] = useState(null);
```

**MUDANÇA 2 — Passar onTotalPagoChange para PagamentosDivida:**

Localizar o bloco que monta `<PagamentosDivida`:
```jsx
<PagamentosDivida
  divida={divida}
  hoje={hoje}
  onSaldoChange={(novoSaldo) => setSaldoDividaLocal(novoSaldo)}
/>
```

Adicionar a prop `onTotalPagoChange`:
```jsx
<PagamentosDivida
  divida={divida}
  hoje={hoje}
  onSaldoChange={(novoSaldo) => setSaldoDividaLocal(novoSaldo)}
  onTotalPagoChange={(total) => setTotalPagoDivida(total)}
/>
```

**MUDANÇA 3 — Substituir totalPago no card financeiro:**

Localizar onde `totalPago` é exibido no JSX (célula "Total Pago" do card financeiro).
A célula exibe `fmtBRL(totalPago)`. Substituir o valor exibido por:
```js
totalPagoDivida !== null ? fmtBRL(totalPagoDivida) : "—"
```

Isso exibe "—" antes do mount de PagamentosDivida propagar o total, e o valor correto
(soma de pagamentos_divida) depois. A variável `totalPago` existente (de pagamentos_parciais)
pode ser mantida no código mas não deve mais aparecer no card financeiro da dívida.

**RESTRIÇÃO:** Não remover `calcularTotalPagoPorDivida` do import nem a variável `totalPago` —
ela pode ser usada em outros contextos no componente. Apenas substituir o valor exibido no
card financeiro. Não alterar nenhuma outra linha do componente.
  </action>
  <verify>
    <automated>grep -n "totalPagoDivida\|onTotalPagoChange\|setTotalPagoDivida" src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "totalPagoDivida" src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx` retorna pelo menos 3 matches (useState, setter, uso no JSX)
    - `grep "onTotalPagoChange" src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx` retorna 1 match na prop de PagamentosDivida
    - Card financeiro exibe `totalPagoDivida !== null ? fmtBRL(totalPagoDivida) : "—"` (não `fmtBRL(totalPago)`)
    - `grep "useState" src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx` retorna matches incluindo o novo state
    - Arquivo ainda contém `calcularTotalPagoPorDivida` no import (não removido)
  </acceptance_criteria>
  <done>DetalheDivida exibe Total Pago somado diretamente dos pagamentos_divida via state local propagado por onTotalPagoChange. Exibe "—" antes do mount, valor correto depois.</done>
</task>

<task type="auto">
  <name>Verificação final: regressão + build</name>
  <read_first>
    package.json (confirmar scripts test:regressao e build)
  </read_first>
  <files></files>
  <action>
Executar em sequência a partir da raiz do submodule (src/mr-3/mr-cobrancas/):

```bash
npm run test:regressao
npm run build
```

**Critérios de aceite:**
- `test:regressao` passa 9/9 (ou o total vigente) sem falhas
- `build` termina sem erros (warnings de lint são aceitáveis)

Se algum teste falhar: investigar se a causa é um dos dois arquivos modificados.
Não tentar corrigir falhas em outros arquivos — reportar como blocker no SUMMARY.md.
  </action>
  <verify>
    <automated>cd src/mr-3/mr-cobrancas && npm run test:regressao 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Saída de test:regressao contém "passed" sem "failed"
    - Saída de build não contém "error" (case-insensitive, exceto strings em comentários ou nomes de variáveis)
  </acceptance_criteria>
  <done>Suite de regressão verde. Build limpo. Fix A e Fix B não quebraram nenhuma funcionalidade existente.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recalcularESincronizar no mount | Chama atualizarSaldoQuitado no banco ao abrir a tela — efeito colateral intencional, documentado em D-03 |
| onTotalPagoChange | Callback de filho para pai — valor numérico calculado localmente, sem I/O adicional |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | Tampering | recalcularESincronizar no mount escreve saldo_quitado=false em dívida sem pagamentos | accept | Comportamento correto: dívida sem pagamentos tem saldo > 0, saldo_quitado=false é o valor certo |
| T-04-12 | Denial of Service | mount dispara write desnecessário se saldo_quitado já está correto no banco | accept | Uma escrita extra por abertura de tela é aceitável no volume atual |
</threat_model>

<verification>
Após execução:
1. Abrir DetalheDivida de dívida com pagamentos em pagamentos_divida — "Saldo Atualizado" deve mostrar valor correto SEM precisar registrar um novo pagamento
2. "Total Pago" deve mostrar soma dos pagamentos_divida, não R$ 0,00
3. Se saldo ≤ 0, badge "Saldo quitado" aparece imediatamente ao abrir a tela
4. npm run test:regressao — 9/9 verde
5. npm run build — sem erros
</verification>

<success_criteria>
- PagamentosDivida.useEffect chama recalcularESincronizar no mount após listarPagamentos
- Saldo Atualizado em DetalheDivida reflete pagamentos_divida na carga inicial
- saldo_quitado sincronizado no banco ao abrir tela (não apenas após mutação)
- Total Pago exibe soma de pagamentos_divida (não pagamentos_parciais)
- devedorCalc.js intacto — zero modificações
- test:regressao verde, build sem erros
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/04-pagamentos-por-divida/04-04-SUMMARY.md`
</output>
