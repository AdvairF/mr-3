---
phase: 4
plan: 3
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified:
  - src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx
  - src/mr-3/mr-cobrancas/src/components/TabelaDividas.jsx
autonomous: true
requirements: [PAG-05, PAG-06, PAG-07, PAG-08]

must_haves:
  truths:
    - "PagamentosDivida aparece no final de DetalheDivida abaixo de Pessoas Vinculadas"
    - "Badge 'Saldo quitado' aparece ao lado de Saldo Atualizado em DetalheDivida quando saldo ≤ 0"
    - "Badge 'Saldo quitado' aparece na coluna Atraso de TabelaDividas quando divida.saldo_quitado === true"
    - "TabelaDividas lê divida.saldo_quitado da coluna (sem recalcular)"
    - "DetalheDivida recalcula saldo_quitado em tempo real via onSaldoChange"
  artifacts:
    - path: "src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx"
      provides: "Monta PagamentosDivida + badge + onSaldoChange handler"
      contains: "PagamentosDivida"
    - path: "src/mr-3/mr-cobrancas/src/components/TabelaDividas.jsx"
      provides: "Badge Saldo quitado na coluna Atraso lendo divida.saldo_quitado"
      contains: "saldo_quitado"
  key_links:
    - from: "DetalheDivida"
      to: "PagamentosDivida"
      via: "import + render com props divida, hoje, onSaldoChange"
    - from: "PagamentosDivida.onSaldoChange"
      to: "DetalheDivida.saldoDivida state"
      via: "useState + setSaldoDivida callback — atualiza o card financeiro sem re-fetch"
    - from: "TabelaDividas"
      to: "dividas[i].saldo_quitado"
      via: "leitura direta da propriedade, sem calcularSaldosPorDivida"
---

<objective>
Integrar o componente PagamentosDivida em DetalheDivida e adicionar o badge "Saldo quitado"
nos dois pontos especificados em D-08: no card financeiro de DetalheDivida (calculado em tempo
real via onSaldoChange) e na coluna de atraso de TabelaDividas (lendo dividas.saldo_quitado).

Purpose: Fecha o ciclo visual — o advogado vê imediatamente quando uma dívida está quitada
tanto ao abrir o detalhe quanto na listagem global.

Output: DetalheDivida.jsx e TabelaDividas.jsx atualizados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-pagamentos-por-divida/04-CONTEXT.md
@.planning/phases/04-pagamentos-por-divida/04-UI-SPEC.md
@.planning/phases/04-pagamentos-por-divida/04-01-SUMMARY.md
@.planning/phases/04-pagamentos-por-divida/04-02-SUMMARY.md

<interfaces>
<!-- DetalheDivida.jsx — estado atual completo (extraído acima) -->
<!-- Props: { divida, devedores, credores, allPagamentos, hoje, onVoltar, onCarregarTudo, setTab } -->
<!-- Renders: back button, header card, financial card, Art.523 card, Pessoas Vinculadas, Editar Dívida btn -->
<!-- saldoDivida calculado na linha 76 via calcularSaldosPorDivida — será usado como valor inicial -->

<!-- Badge "Saldo quitado" — spec exata de 04-UI-SPEC.md -->
```js
// Badge inline next to "Saldo Atualizado" value (DetalheDivida financial card):
<span
  role="status"
  style={{
    display: "inline-block",
    background: "#dcfce7",
    color: "#065f46",
    fontSize: 11,
    fontWeight: 700,
    padding: "3px 8px",
    borderRadius: 99,
    whiteSpace: "nowrap",
    marginTop: 4,
  }}
>
  Saldo quitado
</span>
// Renderizado condicionalmente: saldoAtual !== null && saldoAtual <= 0
```

<!-- TabelaDividas.jsx — coluna Atraso (linha 112) -->
```js
// Atual:  <td style={td}><AtrasoCell dataVencimento={d.data_vencimento} /></td>
// Novo:   se d.saldo_quitado === true, mostrar badge "Saldo quitado" EM VEZ de AtrasoCell
//         se d.saldo_quitado !== true, manter AtrasoCell como estava
```

<!-- PagamentosDivida.jsx — interface de integração (criado no plan 02) -->
```js
// <PagamentosDivida
//   divida={divida}
//   hoje={hoje}
//   onSaldoChange={(novoSaldo) => setSaldoDividaLocal(novoSaldo)}
// />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrar PagamentosDivida em DetalheDivida e adicionar badge no card financeiro</name>
  <read_first>
    src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx (arquivo completo — ver estado atual antes de editar)
    .planning/phases/04-pagamentos-por-divida/04-UI-SPEC.md (seção "Layout: DetalheDivida with PagamentosDivida" e badge spec)
    .planning/phases/04-pagamentos-por-divida/04-CONTEXT.md (D-04, D-07, D-08)
  </read_first>
  <files>src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx</files>
  <action>
Editar `DetalheDivida.jsx` com as seguintes mudanças cirúrgicas — NÃO alterar nada que não esteja
listado abaixo:

**MUDANÇA 1 — Adicionar import de PagamentosDivida (no topo, após imports existentes):**

```js
import PagamentosDivida from "./PagamentosDivida.jsx";
```

**MUDANÇA 2 — Adicionar state para saldo local (dentro da função DetalheDivida, antes do return):**

Localizar a linha que calcula `saldoDivida`:
```js
const saldoDivida = saldosMap != null ? (saldosMap[String(divida.id)] ?? null) : null;
```

Adicionar logo APÓS essa linha:
```js
// saldoDividaLocal: sobrescreve saldoDivida quando PagamentosDivida reporta novo saldo via onSaldoChange
const [saldoDividaLocal, setSaldoDividaLocal] = useState(null);
// Valor efetivo: se PagamentosDivida já propagou um saldo (saldoDividaLocal !== null), usar esse;
// senão usar o calculado pelos pagamentos_parciais do devedor (saldoDivida).
const saldoAtual = saldoDividaLocal !== null ? saldoDividaLocal : saldoDivida;
```

**MUDANÇA 3 — Substituir referências a `saldoDivida` no JSX por `saldoAtual`:**

No card financeiro (linha ~122), o array `.map` exibe `{ label: "Saldo Atualizado", value: saldoDivida != null ? fmtBRL(saldoDivida) : "—" }`.
Substituir por: `{ label: "Saldo Atualizado", value: saldoAtual != null ? fmtBRL(saldoAtual) : "—" }`.

**MUDANÇA 4 — Adicionar badge "Saldo quitado" no card financeiro:**

No card financeiro, encontrar o elemento `<div key={label} ...>` que renderiza cada célula do grid.
A célula "Saldo Atualizado" precisa exibir o badge abaixo do valor quando `saldoAtual <= 0`.

Localizar o bloco:
```js
[
  { label: "Valor Original",   value: fmtBRL(divida.valor_total) },
  { label: "Saldo Atualizado", value: saldoAtual != null ? fmtBRL(saldoAtual) : "—" },
  { label: "Total Pago",       value: fmtBRL(totalPago) },
].map(({ label, value }) => (
  <div key={label} ...>
    <p ...>{label}</p>
    <p ...>{value}</p>
  </div>
))
```

Como o `.map` atual não permite renderização condicional por célula, substituir o array .map por
três elementos JSX explícitos (ou adicionar um campo opcional `extra` ao objeto de cada célula).
A forma mais simples: quebrar o .map em 3 `<div>` explícitos e adicionar o badge na célula do meio.

Resultado esperado para a célula "Saldo Atualizado":
```jsx
<div style={{ background: "#fff", borderRadius: 12, padding: "12px 16px", border: "1px solid #e2e8f0", textAlign: "center" }}>
  <p style={{ fontSize: 11, fontWeight: 700, color: "#64748b", textTransform: "uppercase", letterSpacing: ".5px", marginBottom: 4 }}>
    Saldo Atualizado
  </p>
  <p style={{ fontSize: 22, fontWeight: 700, color: "#0f172a", fontFamily: "'Space Grotesk',sans-serif", margin: 0 }}>
    {saldoAtual != null ? fmtBRL(saldoAtual) : "—"}
  </p>
  {saldoAtual !== null && saldoAtual <= 0 && (
    <span
      role="status"
      style={{
        display: "inline-block",
        background: "#dcfce7",
        color: "#065f46",
        fontSize: 11,
        fontWeight: 700,
        padding: "3px 8px",
        borderRadius: 99,
        whiteSpace: "nowrap",
        marginTop: 4,
      }}
    >
      Saldo quitado
    </span>
  )}
</div>
```

As células "Valor Original" e "Total Pago" mantêm o estilo original sem badge.

**MUDANÇA 5 — Montar PagamentosDivida no final de DetalheDivida (D-04, D-07):**

Localizar o comentário `{/* 6. Editar Dívida button */}` (ou o botão "Editar Dívida").
Inserir o bloco `<PagamentosDivida>` ANTES do botão "Editar Dívida" e DEPOIS do card
"Pessoas Vinculadas" (comentário `{/* 5. Pessoas Vinculadas */}`):

```jsx
{/* 6. Pagamentos por Dívida — D-04 LOCKED: seção fixa no final */}
<PagamentosDivida
  divida={divida}
  hoje={hoje}
  onSaldoChange={(novoSaldo) => setSaldoDividaLocal(novoSaldo)}
/>
```

O botão "Editar Dívida" continua após PagamentosDivida (passa a ser o item 7).
O modal de aviso de remoção de principal continua no final, intacto.

**REGRA:** NÃO passar `allPagamentos` para PagamentosDivida — ela usa seu próprio useEffect
lazy para buscar `pagamentos_divida` da tabela separada (D-07). O `allPagamentos` existente
em DetalheDivida refere-se a `pagamentos_parciais` do devedor, escopo diferente (D-02).
  </action>
  <verify>
    <automated>grep -n "PagamentosDivida\|saldoDividaLocal\|saldoAtual\|Saldo quitado" src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "import PagamentosDivida" src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx` retorna 1 match
    - `grep "saldoDividaLocal" src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx` retorna pelo menos 3 matches (useState, setSaldoDividaLocal, saldoAtual cálculo)
    - `grep "saldoAtual" src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx` retorna pelo menos 3 matches (declaração + usos no JSX)
    - `grep '"Saldo quitado"' src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx` retorna 1 match
    - `grep 'role="status"' src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx` retorna 1 match
    - `grep "<PagamentosDivida" src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx` retorna 1 match com `divida={divida}` e `onSaldoChange`
    - `grep "allPagamentos.*PagamentosDivida\|PagamentosDivida.*allPagamentos" src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx` retorna 0 matches (allPagamentos NÃO passado para PagamentosDivida)
    - `grep "#dcfce7" src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx` retorna 1 match no badge
    - `grep "#065f46" src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx` retorna pelo menos 1 match (cor do badge)
    - Arquivo ainda contém `useState` importado (não removido acidentalmente)
    - Arquivo ainda contém `import DevedoresDaDivida` (não removido acidentalmente)
  </acceptance_criteria>
  <done>DetalheDivida monta PagamentosDivida com onSaldoChange. Badge "Saldo quitado" aparece ao lado do saldo calculado quando ≤ 0. Saldo local sobrescreve o calculado pelos pagamentos_parciais após cada operação de pagamento_divida.</done>
</task>

<task type="auto">
  <name>Task 2: Adicionar badge "Saldo quitado" em TabelaDividas lendo divida.saldo_quitado</name>
  <read_first>
    src/mr-3/mr-cobrancas/src/components/TabelaDividas.jsx (arquivo completo — ver estado atual)
    src/mr-3/mr-cobrancas/src/components/AtrasoCell.jsx (padrão visual de badge — ver estilos)
    .planning/phases/04-pagamentos-por-divida/04-UI-SPEC.md (seção "PAG-06 + PAG-08 — Badge Saldo quitado")
    .planning/phases/04-pagamentos-por-divida/04-CONTEXT.md (D-08)
  </read_first>
  <files>src/mr-3/mr-cobrancas/src/components/TabelaDividas.jsx</files>
  <action>
Editar `TabelaDividas.jsx` com uma única mudança cirúrgica na coluna "Atraso":

**MUDANÇA — Coluna Atraso (localizar linha ~112):**

Atual:
```jsx
<td style={td}><AtrasoCell dataVencimento={d.data_vencimento} /></td>
```

Substituir por:
```jsx
<td style={td}>
  {d.saldo_quitado === true
    ? (
      <span style={{
        display: "inline-block",
        background: "#dcfce7",
        color: "#065f46",
        fontSize: 11,
        fontWeight: 700,
        padding: "3px 8px",
        borderRadius: 99,
        whiteSpace: "nowrap",
      }}>
        Saldo quitado
      </span>
    )
    : <AtrasoCell dataVencimento={d.data_vencimento} />
  }
</td>
```

**REGRA D-08:** TabelaDividas NÃO recalcula saldo. Usa apenas o valor booleano de
`d.saldo_quitado` — que foi persistido por `atualizarSaldoQuitado` em `dividas.js`
após cada operação de pagamento em PagamentosDivida.

**NÃO remover nem alterar:** AtrasoCell import, calcularSaldosPorDivida import,
StatusBadgeDivida, paginação, demais colunas da tabela. Mudança cirúrgica apenas
na coluna Atraso.
  </action>
  <verify>
    <automated>grep -n "saldo_quitado\|Saldo quitado\|#dcfce7" src/mr-3/mr-cobrancas/src/components/TabelaDividas.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "saldo_quitado" src/mr-3/mr-cobrancas/src/components/TabelaDividas.jsx` retorna pelo menos 1 match (`d.saldo_quitado === true`)
    - `grep '"Saldo quitado"' src/mr-3/mr-cobrancas/src/components/TabelaDividas.jsx` retorna 1 match
    - `grep "#dcfce7" src/mr-3/mr-cobrancas/src/components/TabelaDividas.jsx` retorna 1 match (cor do badge)
    - `grep "#065f46" src/mr-3/mr-cobrancas/src/components/TabelaDividas.jsx` retorna 1 match (cor do texto)
    - `grep "AtrasoCell" src/mr-3/mr-cobrancas/src/components/TabelaDividas.jsx` retorna pelo menos 2 matches (import + uso condicional)
    - `grep "import AtrasoCell" src/mr-3/mr-cobrancas/src/components/TabelaDividas.jsx` retorna 1 match (import não removido)
    - `grep "calcularSaldosPorDivida\|StatusBadgeDivida" src/mr-3/mr-cobrancas/src/components/TabelaDividas.jsx` retorna matches existentes (não alterados)
    - Arquivo NÃO contém `calcularSaldoPorDividaIndividual` (TabelaDividas não deve recalcular — usa coluna persistida)
  </acceptance_criteria>
  <done>TabelaDividas exibe badge "Saldo quitado" na coluna Atraso quando divida.saldo_quitado === true, lendo o valor persistido no banco. AtrasoCell continua sendo exibida para dívidas não quitadas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dividas[] → TabelaDividas | `saldo_quitado` vem do banco via carregarTudo(); se banco retornar null/undefined, badge não aparece (falha segura) |
| onSaldoChange callback | Callback de PagamentosDivida para DetalheDivida — valor numérico calculado localmente, sem tráfego de rede adicional |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Tampering | saldo_quitado=true forçado no banco sem pagamentos reais | accept | App single-tenant; o advogado só manipula suas próprias dívidas; RLS bloqueia acesso de terceiros |
| T-04-09 | Information Disclosure | badge "Saldo quitado" visível para outros usuários da conta | accept | Single-tenant — não há múltiplos usuários com escopos separados em v1.1 |
| T-04-10 | Spoofing | onSaldoChange recebe valor fabricado pelo componente filho | accept | PagamentosDivida é código do próprio app; não é input externo; calcularSaldoPorDividaIndividual é determinístico |
</threat_model>

<verification>
Após execução:
1. `grep "PagamentosDivida" src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx` — import + render
2. `grep "Saldo quitado" src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx` — badge presente
3. `grep "saldo_quitado" src/mr-3/mr-cobrancas/src/components/TabelaDividas.jsx` — badge condicional presente
4. `grep "AtrasoCell" src/mr-3/mr-cobrancas/src/components/TabelaDividas.jsx` — import intacto + uso condicional
5. Verificar visualmente que DetalheDivida não passa allPagamentos para PagamentosDivida
</verification>

<success_criteria>
- DetalheDivida importa e monta PagamentosDivida com divida, hoje, onSaldoChange
- DetalheDivida tem state saldoDividaLocal que sobrescreve saldoDivida após onSaldoChange
- Badge "Saldo quitado" aparece em DetalheDivida quando saldoAtual <= 0 (tempo real)
- Badge "Saldo quitado" aparece em TabelaDividas quando d.saldo_quitado === true (coluna persistida)
- AtrasoCell intacta para dívidas não quitadas em TabelaDividas
- Nenhuma função existente em DetalheDivida ou TabelaDividas foi removida
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/04-pagamentos-por-divida/04-03-SUMMARY.md`
</output>
