---
phase: 4
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mr-3/mr-cobrancas/src/components/PagamentosDivida.jsx
autonomous: true
requirements: [PAG-01, PAG-02, PAG-03, PAG-04]

must_haves:
  truths:
    - "Usuário vê lista cronológica de pagamentos com data, valor e observação"
    - "Usuário vê formulário fixo de registro sempre visível (sem botão expandir)"
    - "Clicar Editar transforma a linha em campos inline sem abrir modal"
    - "Clicar Excluir mostra window.confirm e deleta após confirmação"
    - "Empty state exibe 'Nenhum pagamento registrado' em #94a3b8"
    - "Loading state exibe 'Carregando...' em #94a3b8"
  artifacts:
    - path: "src/mr-3/mr-cobrancas/src/components/PagamentosDivida.jsx"
      provides: "Componente autônomo com histórico, edição inline, exclusão e formulário de registro"
      exports: ["default PagamentosDivida"]
  key_links:
    - from: "PagamentosDivida"
      to: "pagamentos.js"
      via: "importa listarPagamentos, criarPagamento, atualizarPagamento, excluirPagamento, calcularSaldoPorDividaIndividual"
    - from: "PagamentosDivida"
      to: "dividas.js:atualizarSaldoQuitado"
      via: "importa e chama após cada mutação"
    - from: "PagamentosDivida.onSaldoChange"
      to: "DetalheDivida (plan 03)"
      via: "prop callback — propagará novo saldo para o header financeiro"
---

<objective>
Criar o componente `PagamentosDivida.jsx` completo: histórico cronológico com edição inline,
formulário de registro sempre visível, e lógica de recálculo de saldo após cada operação.

Purpose: Concentra toda a UI de pagamentos em um componente autônomo. DetalheDivida (plan 03)
simplesmente monta `<PagamentosDivida divida={divida} hoje={hoje} onSaldoChange={fn} />`.

Output: `src/components/PagamentosDivida.jsx` pronto para importação.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-pagamentos-por-divida/04-CONTEXT.md
@.planning/phases/04-pagamentos-por-divida/04-UI-SPEC.md

<interfaces>
<!-- Props do componente -->
```js
// PagamentosDivida({ divida, hoje, onSaldoChange })
// divida: objeto dívida com { id, valor_total, indexador, juros_tipo, juros_am,
//         multa_pct, honorarios_pct, art523_opcao, data_inicio_atualizacao|data_vencimento|data_origem }
// hoje: string "YYYY-MM-DD"
// onSaldoChange: (novoSaldo: number) => void  — chamado após cada mutação bem-sucedida
```

<!-- Imports disponíveis (extraídos do codebase) -->
```js
import { useState, useEffect } from "react";
import toast from "react-hot-toast";
import Btn from "./ui/Btn.jsx";
import {
  listarPagamentos,
  criarPagamento,
  atualizarPagamento,
  excluirPagamento,
  calcularSaldoPorDividaIndividual,
} from "../services/pagamentos.js";
import { atualizarSaldoQuitado } from "../services/dividas.js";
// Globals: dbGet, dbInsert, dbUpdate, dbDelete, toast — NÃO importar, já são globals
```

<!-- Padrão de card wrapper de DetalheDivida.jsx (linhas 117, 154) -->
```js
// Card wrapper exato para PagamentosDivida (legacy 20px, per UI-SPEC):
{ background: "#fff", borderRadius: 16, padding: "18px 20px", border: "1px solid #e8f0f7", marginBottom: 16 }
```

<!-- Padrão de edição inline de PessoasVinculadas.jsx -->
```js
// editandoId state controla qual linha está em modo edição
// iniciarEdicao(row) → setEditandoId(row.id) + setEditForm({...})
// handleSalvarEdit(row) → await atualizarX(row.id, editForm) + setEditandoId(null) + await carregar()
// Botão Editar: onClick → iniciarEdicao(row)
// Botão OK: onClick → handleSalvarEdit(row)
// Botão X: onClick → setEditandoId(null)
// Apenas uma linha em edição por vez (clicking Editar em outra linha sobrescreve editandoId)
```

<!-- Padrão de badge "Saldo quitado" de UI-SPEC -->
```js
// Badge inline — NÃO renderizar neste componente (plan 03 adiciona em DetalheDivida)
// Este componente apenas chama onSaldoChange(novoSaldo) para propagar o valor
```

<!-- Carregamento lazy (per D-07) -->
```js
// useEffect ao montar:
// useEffect(() => {
//   dbGet('pagamentos_divida', `divida_id=eq.${divida.id}&order=data_pagamento.asc`)
//     .then(setPagamentos);
// }, [divida.id]);
// MAS usar listarPagamentos(divida.id) do service em vez de dbGet direto
```

<!-- Estilos de tabela de TabelaDividas.jsx (lines 42-43) -->
```js
const th = { padding: "8px 10px", textAlign: "left", fontWeight: 700, color: "#64748b",
             fontSize: 11, textTransform: "uppercase", letterSpacing: ".5px", whiteSpace: "nowrap" };
const td = { padding: "9px 10px", color: "#374151", verticalAlign: "middle" };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar PagamentosDivida.jsx com histórico, edição inline e formulário</name>
  <read_first>
    src/mr-3/mr-cobrancas/src/components/PessoasVinculadas.jsx (padrão edição inline, linhas 110–128 e 180–260)
    src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx (card wrapper styles, linhas 117 e 154)
    src/mr-3/mr-cobrancas/src/components/ui/Btn.jsx (props disponíveis: sm, outline, danger)
    .planning/phases/04-pagamentos-por-divida/04-UI-SPEC.md (tabela de copywriting + interaction contract)
  </read_first>
  <files>src/mr-3/mr-cobrancas/src/components/PagamentosDivida.jsx</files>
  <action>
Criar `src/mr-3/mr-cobrancas/src/components/PagamentosDivida.jsx` do zero.

**ESTRUTURA INTERNA:**

```
PagamentosDivida({ divida, hoje, onSaldoChange })
  states:
    pagamentos[]          — lista carregada do banco
    loading bool          — true durante fetch inicial
    editandoId string|null — UUID do pagamento em edição inline (null = nenhum)
    editForm { data_pagamento, valor, observacao } — dados da linha em edição
    salvando bool         — true durante submit do formulário de registro
    novoForm { data_pagamento, valor, observacao } — dados do formulário de novo pagamento
```

**FUNÇÃO AUXILIAR formatação:**

```js
function fmtBRL(v) {
  if (v == null || v === "") return "—";
  return Number(v).toLocaleString("pt-BR", { style: "currency", currency: "BRL" });
}

function fmtData(iso) {
  if (!iso) return "—";
  const d = iso.slice(0, 10).split("-");
  return `${d[2]}/${d[1]}/${d[0]}`;
}
```

**CARREGAMENTO LAZY (D-07):**

```js
useEffect(() => {
  setLoading(true);
  listarPagamentos(divida.id)
    .then(data => {
      setPagamentos(Array.isArray(data) ? data : []);
    })
    .catch(e => toast.error("Erro ao carregar pagamentos: " + e.message))
    .finally(() => setLoading(false));
}, [divida.id]);
```

**FUNÇÃO HELPER — recalcular e sincronizar saldo_quitado após mutação:**

```js
async function recalcularESincronizar(listaPagamentos) {
  const novoSaldo = calcularSaldoPorDividaIndividual(divida, listaPagamentos, hoje);
  const quitado = novoSaldo <= 0;
  await atualizarSaldoQuitado(divida.id, quitado);
  if (onSaldoChange) onSaldoChange(novoSaldo);
}
```

**CRIAR PAGAMENTO (PAG-01):**

```js
async function handleCriar(e) {
  e.preventDefault();
  if (!novoForm.data_pagamento || !novoForm.valor) {
    toast.error("Preencha data e valor");
    return;
  }
  setSalvando(true);
  try {
    await criarPagamento({
      divida_id: divida.id,
      data_pagamento: novoForm.data_pagamento,
      valor: parseFloat(novoForm.valor),
      observacao: novoForm.observacao || null,
    });
    const lista = await listarPagamentos(divida.id);
    const listaAtual = Array.isArray(lista) ? lista : [];
    setPagamentos(listaAtual);
    await recalcularESincronizar(listaAtual);
    setNovoForm({ data_pagamento: "", valor: "", observacao: "" });
    toast.success("Pagamento registrado");
  } catch (e) {
    toast.error("Erro ao registrar pagamento: " + e.message);
  } finally {
    setSalvando(false);
  }
}
```

**EDIÇÃO INLINE — INICIAR (D-05):**

```js
function iniciarEdicao(row) {
  setEditandoId(row.id);
  setEditForm({
    data_pagamento: row.data_pagamento?.slice(0, 10) || "",
    valor: String(row.valor || ""),
    observacao: row.observacao || "",
  });
}
```

**EDIÇÃO INLINE — SALVAR (PAG-03):**

```js
async function handleSalvarEdit(row) {
  try {
    await atualizarPagamento(row.id, {
      data_pagamento: editForm.data_pagamento,
      valor: parseFloat(editForm.valor),
      observacao: editForm.observacao || null,
    });
    const lista = await listarPagamentos(divida.id);
    const listaAtual = Array.isArray(lista) ? lista : [];
    setPagamentos(listaAtual);
    await recalcularESincronizar(listaAtual);
    setEditandoId(null);
    toast.success("Pagamento atualizado");
  } catch (e) {
    toast.error("Erro ao atualizar: " + e.message);
  }
}
```

**EXCLUIR (PAG-04):**

```js
async function handleExcluir(row) {
  if (!window.confirm("Excluir este pagamento?")) return;
  try {
    await excluirPagamento(row.id);
    const lista = await listarPagamentos(divida.id);
    const listaAtual = Array.isArray(lista) ? lista : [];
    setPagamentos(listaAtual);
    await recalcularESincronizar(listaAtual);
    toast.success("Pagamento excluído");
  } catch (e) {
    toast.error("Erro ao excluir: " + e.message);
  }
}
```

**RENDER — estrutura JSX:**

Card wrapper com `{ background: "#fff", borderRadius: 16, padding: "18px 20px", border: "1px solid #e8f0f7", marginBottom: 16 }`.

Seção 1 — "Histórico de Pagamentos":
- Heading: `<p style={{ fontWeight: 700, fontSize: 13, color: "#0f172a", marginBottom: 12 }}>Histórico de Pagamentos</p>`
- Se loading: `<p style={{ color: "#94a3b8", fontSize: 13 }}>Carregando...</p>`
- Se !loading e pagamentos.length === 0: `<p style={{ color: "#94a3b8", fontSize: 13, textAlign: "center", padding: "16px 0" }}>Nenhum pagamento registrado</p>`
- Se !loading e pagamentos.length > 0: tabela com colunas Data (96px), Valor (112px), Observação (flex 1), Ações (80px)

Para cada linha da tabela:
  - Modo normal (editandoId !== row.id):
    - DATA: `fmtData(row.data_pagamento)`
    - VALOR: `fmtBRL(row.valor)`
    - OBS: `row.observacao || "—"` (truncado via `overflow: hidden, textOverflow: ellipsis, whiteSpace: nowrap`)
    - AÇÕES: `<Btn sm outline onClick={() => iniciarEdicao(row)}>Editar</Btn>` e
             `<Btn sm danger onClick={() => handleExcluir(row)} aria-label={`Excluir pagamento de ${fmtData(row.data_pagamento)}`}>Excluir</Btn>`
  - Modo edição inline (editandoId === row.id):
    - DATA: `<input type="date" value={editForm.data_pagamento} onChange={e => setEditForm(f => ({...f, data_pagamento: e.target.value}))} style={{ padding: "8px 10px", border: "1.5px solid #e2e8f0", borderRadius: 9, fontSize: 13 }} />`
    - VALOR: `<input type="number" step="0.01" value={editForm.valor} onChange={e => setEditForm(f => ({...f, valor: e.target.value}))} style={{ padding: "8px 10px", border: "1.5px solid #e2e8f0", borderRadius: 9, fontSize: 13, width: 100 }} />`
    - OBS: `<input type="text" value={editForm.observacao} onChange={e => setEditForm(f => ({...f, observacao: e.target.value}))} style={{ padding: "8px 10px", border: "1.5px solid #e2e8f0", borderRadius: 9, fontSize: 13, width: "100%" }} />`
    - AÇÕES: `<Btn sm outline onClick={() => handleSalvarEdit(row)} style={{ color: "#3d9970" }} aria-label="Confirmar edição">OK</Btn>` e
             `<Btn sm outline onClick={() => setEditandoId(null)} style={{ color: "#64748b" }} aria-label="Cancelar edição">✕</Btn>`

Seção 2 — `<hr style={{ margin: "16px 0", border: "none", borderTop: "1px solid #e2e8f0" }} />`

Seção 3 — "Registrar Pagamento":
- Heading: `<p style={{ fontWeight: 700, fontSize: 13, color: "#0f172a", marginBottom: 12 }}>Registrar Pagamento</p>`
- Form `onSubmit={handleCriar}`:
  - Layout flex row com gap 8px
  - Campo Data: label "DATA" (11px 700 #64748b uppercase) + `<input type="date" value={novoForm.data_pagamento} onChange={...} required />`
  - Campo Valor: label "VALOR" + `<input type="number" step="0.01" min="0.01" value={novoForm.valor} onChange={...} required />`
  - Campo Obs: label "OBSERVAÇÃO" + `<input type="text" value={novoForm.observacao} onChange={...} />`
  - Botão submit: `<Btn type="submit" disabled={salvando} style={{ background: "#3d9970", color: "#fff", border: "none" }}>{salvando ? "Salvando..." : "Salvar Pagamento"}</Btn>`

Acessibilidade:
- [Excluir] tem `aria-label={\`Excluir pagamento de ${fmtData(row.data_pagamento)}\`}`
- [OK] tem `aria-label="Confirmar edição"`
- [✕] tem `aria-label="Cancelar edição"`
  </action>
  <verify>
    <automated>grep -c "export default function PagamentosDivida" src/mr-3/mr-cobrancas/src/components/PagamentosDivida.jsx && grep -c "handleExcluir\|handleSalvarEdit\|handleCriar\|recalcularESincronizar" src/mr-3/mr-cobrancas/src/components/PagamentosDivida.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/mr-3/mr-cobrancas/src/components/PagamentosDivida.jsx` existe
    - `grep "export default function PagamentosDivida" src/.../PagamentosDivida.jsx` encontra 1 match
    - Arquivo contém `window.confirm("Excluir este pagamento?")` (D-06 exato)
    - Arquivo contém `toast.success("Pagamento registrado")` (copywriting contract exato)
    - Arquivo contém `toast.success("Pagamento atualizado")` (copywriting contract exato)
    - Arquivo contém `toast.success("Pagamento excluído")` (copywriting contract exato)
    - Arquivo contém `"Nenhum pagamento registrado"` (empty state exato)
    - Arquivo contém `"Carregando..."` (loading state exato)
    - Arquivo contém `"Salvar Pagamento"` (CTA exato)
    - Arquivo contém `"Salvando..."` (loading button state exato)
    - Arquivo contém `aria-label="Confirmar edição"` (acessibilidade)
    - Arquivo contém `aria-label="Cancelar edição"` (acessibilidade)
    - Arquivo contém `setEditandoId(null)` pelo menos 2 vezes (cancelar + pós-salvar)
    - Arquivo contém `recalcularESincronizar` chamado em handleCriar, handleSalvarEdit, handleExcluir
    - Arquivo contém `listarPagamentos` e `atualizarSaldoQuitado` nos imports
    - Arquivo contém `import { atualizarSaldoQuitado } from "../services/dividas.js"`
    - Arquivo contém `padding: "18px 20px"` (card wrapper legacy)
    - Arquivo NÃO contém `import.*dbGet` ou `import.*dbInsert` (globals, não importar)
  </acceptance_criteria>
  <done>PagamentosDivida.jsx criado com histórico cronológico, edição inline por linha, exclusão com confirm, formulário de registro fixo sempre visível. Recalcula saldo e persiste saldo_quitado após cada operação.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → componente | Campos de data, valor e observação recebem input direto do advogado |
| componente → pagamentos.js | Dados passados para CRUD sem sanitização adicional além do tipo de input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering | input type=number — valor negativo | mitigate | Adicionar `min="0.01"` no campo de valor do formulário de novo pagamento; para edição inline, parseFloat() + validação implícita do banco (NUMERIC NOT NULL) |
| T-04-06 | Tampering | input type=date — data futura intencional | accept | Não há regra de negócio vedando data futura em v1.1; o Art.354 opera corretamente com datas futuras |
| T-04-07 | Denial of Service | XSS via campo observação | accept | App single-tenant, usuário autenticado; React escapa strings em JSX automaticamente; sem `dangerouslySetInnerHTML` |
</threat_model>

<verification>
Após execução:
1. `grep "export default function PagamentosDivida" src/mr-3/mr-cobrancas/src/components/PagamentosDivida.jsx` — encontra declaração
2. `grep "window.confirm" src/mr-3/mr-cobrancas/src/components/PagamentosDivida.jsx` — encontra D-06
3. `grep "recalcularESincronizar" src/mr-3/mr-cobrancas/src/components/PagamentosDivida.jsx` — encontra em handleCriar, handleSalvarEdit, handleExcluir
4. `grep "aria-label" src/mr-3/mr-cobrancas/src/components/PagamentosDivida.jsx` — encontra 3+ atributos de acessibilidade
5. Checar visualmente que edição inline usa inputs (não modal) e form de registro é sempre visível
</verification>

<success_criteria>
- PagamentosDivida.jsx existe e exporta default function
- Todos os textos do copywriting contract estão presentes (verificados literalmente)
- Edição inline sem modal (D-05 honrado)
- window.confirm para exclusão (D-06 honrado)
- onSaldoChange chamado após cada mutação
- atualizarSaldoQuitado chamado após cada mutação (D-03 honrado)
- Carregamento lazy via useEffect (D-07 honrado)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/04-pagamentos-por-divida/04-02-SUMMARY.md`
</output>
