---
phase: 4
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mr-3/mr-cobrancas/src/services/pagamentos.js
  - src/mr-3/mr-cobrancas/src/services/dividas.js
autonomous: true
requirements: [PAG-01, PAG-02, PAG-03, PAG-04, PAG-05, PAG-07]

must_haves:
  truths:
    - "CRUD completo sobre pagamentos_divida existe como funções exportadas em pagamentos.js"
    - "atualizarSaldoQuitado(dividaUuid, bool) existe em dividas.js e persiste a coluna saldo_quitado"
    - "calcularSaldoPorDividaIndividual(divida, pagamentosDivida, hoje) retorna número correto pelo loop Art.354"
    - "As migrações SQL estão documentadas como comentários no topo de pagamentos.js para execução manual no Supabase"
  artifacts:
    - path: "src/mr-3/mr-cobrancas/src/services/pagamentos.js"
      provides: "CRUD sobre pagamentos_divida + helper de cálculo de saldo"
      exports: [listarPagamentos, criarPagamento, atualizarPagamento, excluirPagamento, calcularSaldoPorDividaIndividual]
    - path: "src/mr-3/mr-cobrancas/src/services/dividas.js"
      provides: "atualizarSaldoQuitado adicionado ao service existente"
      exports: [atualizarSaldoQuitado]
  key_links:
    - from: "pagamentos.js:calcularSaldoPorDividaIndividual"
      to: "devedorCalc.js:calcularSaldosPorDivida"
      via: "reutiliza lógica de loop Art.354, adaptada para receber apenas os pagamentos de uma dívida"
    - from: "pagamentos.js:criarPagamento/atualizarPagamento/excluirPagamento"
      to: "dividas.js:atualizarSaldoQuitado"
      via: "chamado internamente após cada mutação para sincronizar saldo_quitado"
---

<objective>
Criar a camada de dados completa para pagamentos por dívida: nova tabela `pagamentos_divida` no
Supabase, coluna `dividas.saldo_quitado`, service CRUD, e a função de cálculo de saldo individual
por dívida baseada no motor Art. 354 CC existente.

Purpose: Planos posteriores (UI + integração) dependem deste service layer. Tudo que toca o banco
fica aqui — os componentes React nunca chamam dbGet/dbInsert diretamente para pagamentos.

Output: `src/services/pagamentos.js` (novo), `src/services/dividas.js` (estendido com atualizarSaldoQuitado),
instruções SQL para migration manual.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-pagamentos-por-divida/04-CONTEXT.md
@.planning/phases/04-pagamentos-por-divida/04-UI-SPEC.md

<interfaces>
<!-- Padrão de service extraído de src/mr-3/mr-cobrancas/src/services/dividas.js -->
```js
// Globals disponíveis no runtime (não importar): dbGet, dbInsert, dbUpdate, dbDelete, toast
// sb() não está disponível neste arquivo — usar os utilitários globais conforme padrão abaixo.
// Exemplo de CRUD:
//   dbGet('tabela', 'coluna=eq.valor&order=campo.asc')
//   dbInsert('tabela', { campo: valor })
//   dbUpdate('tabela', 'id=eq.uuid', { campo: valor })
//   dbDelete('tabela', 'id=eq.uuid')

// EXCEÇÃO: dividas.js usa sb() do config/supabase.js — NÃO alterar essa importação.
// Ao adicionar atualizarSaldoQuitado em dividas.js, usar sb() como já existe:
//   sb(`dividas?id=eq.${dividaUuid}`, "PATCH", { saldo_quitado: bool, updated_at: new Date().toISOString() })
```

<!-- Motor Art.354 de devedorCalc.js (interface relevante) -->
```js
// calcularSaldosPorDivida(devedor, pagamentos, hoje) → { [dividaId]: number }
// Recebe: devedor (objeto com .dividas[]), pagamentos (array de pagamentos_parciais)
// ADAPTAÇÃO para pagamentos_divida:
//   - Construir um objeto devedor fictício com apenas a dívida em questão
//   - Passar os pagamentos_divida como array (mesmo shape que pagamentos_parciais:
//     { data_pagamento: "YYYY-MM-DD", valor: number, observacao?: string })
//   - O retorno será { [divida.id]: saldo }
//   - Extrair o saldo: result[String(divida.id)] ?? 0
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar service pagamentos.js com CRUD e helper de saldo</name>
  <read_first>
    src/mr-3/mr-cobrancas/src/services/dividas.js
    src/mr-3/mr-cobrancas/src/utils/devedorCalc.js
    .planning/phases/04-pagamentos-por-divida/04-CONTEXT.md (decisões D-01, D-02, D-03)
  </read_first>
  <files>src/mr-3/mr-cobrancas/src/services/pagamentos.js</files>
  <action>
Criar `src/mr-3/mr-cobrancas/src/services/pagamentos.js` do zero.

**PASSO 0 — Documentar migrations (comentário no topo do arquivo):**

```js
/**
 * pagamentos.js — Service CRUD para pagamentos_divida.
 *
 * MIGRATIONS NECESSÁRIAS — executar no Supabase SQL Editor antes de usar:
 *
 * -- Migration 1: nova tabela
 * CREATE TABLE public.pagamentos_divida (
 *   id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
 *   divida_id UUID NOT NULL REFERENCES public.dividas(id) ON DELETE CASCADE,
 *   data_pagamento DATE NOT NULL,
 *   valor NUMERIC(15,2) NOT NULL,
 *   observacao TEXT,
 *   created_at TIMESTAMPTZ DEFAULT NOW()
 * );
 * ALTER TABLE public.pagamentos_divida ENABLE ROW LEVEL SECURITY;
 * CREATE POLICY "Acesso autenticado" ON public.pagamentos_divida
 *   FOR ALL USING (auth.role() = 'authenticated');
 *
 * -- Migration 2: coluna saldo_quitado em dividas
 * ALTER TABLE public.dividas ADD COLUMN IF NOT EXISTS saldo_quitado BOOLEAN DEFAULT FALSE;
 */
```

**PASSO 1 — Importações:**

```js
import { calcularSaldosPorDivida } from "../utils/devedorCalc.js";
```

Não importar dbGet/dbInsert/dbUpdate/dbDelete — são globals disponíveis no runtime (injetados por App.jsx).

**PASSO 2 — Funções CRUD (seguindo exatamente o padrão de dividas.js):**

```js
const TABLE = "pagamentos_divida";

/**
 * Lista todos os pagamentos de uma dívida, ordenados por data_pagamento ASC.
 * @param {string} dividaId — UUID da dívida
 * @returns {Promise<Array>}
 */
export async function listarPagamentos(dividaId) {
  return dbGet(TABLE, `divida_id=eq.${dividaId}&order=data_pagamento.asc`);
}

/**
 * Cria um novo pagamento para uma dívida.
 * @param {{ divida_id: string, data_pagamento: string, valor: number, observacao?: string }} payload
 * @returns {Promise<Array>} — array com a row criada
 */
export async function criarPagamento(payload) {
  return dbInsert(TABLE, payload);
}

/**
 * Atualiza campos de um pagamento existente.
 * @param {string} pagamentoId — UUID do pagamento
 * @param {{ data_pagamento?: string, valor?: number, observacao?: string }} campos
 * @returns {Promise<Array>}
 */
export async function atualizarPagamento(pagamentoId, campos) {
  return dbUpdate(TABLE, `id=eq.${pagamentoId}`, campos);
}

/**
 * Exclui um pagamento pelo UUID.
 * @param {string} pagamentoId — UUID do pagamento
 * @returns {Promise<any>}
 */
export async function excluirPagamento(pagamentoId) {
  return dbDelete(TABLE, `id=eq.${pagamentoId}`);
}
```

**PASSO 3 — Helper de cálculo de saldo individual (per D-02):**

```js
/**
 * Calcula o saldo atualizado de uma dívida individual via Art. 354 CC.
 * Adapta o motor calcularSaldosPorDivida para receber pagamentos de uma
 * única dívida (pagamentos_divida), sem interferência de pagamentos_parciais.
 *
 * @param {object} divida — objeto dívida com: id, valor_total, indexador,
 *   juros_tipo, juros_am, multa_pct, honorarios_pct, art523_opcao,
 *   data_inicio_atualizacao|data_vencimento|data_origem
 * @param {Array}  pagamentosDivida — array de { data_pagamento, valor, observacao }
 * @param {string} hoje — "YYYY-MM-DD"
 * @returns {number} saldo final (≥ 0)
 */
export function calcularSaldoPorDividaIndividual(divida, pagamentosDivida, hoje) {
  // Constrói objeto devedor fictício com apenas esta dívida
  // O motor espera devedor.dividas[] — cada dívida precisa ter os campos de encargos
  const devedorFicticio = {
    dividas: [divida],
  };
  const saldosMap = calcularSaldosPorDivida(devedorFicticio, pagamentosDivida || [], hoje);
  return saldosMap[String(divida.id)] ?? 0;
}
```

O arquivo final tem: comentário de migrations, 1 import, constante TABLE, 4 funções CRUD exportadas, 1 helper exportado.
  </action>
  <verify>
    <automated>
      node -e "import('./src/mr-3/mr-cobrancas/src/services/pagamentos.js').catch(e => { if(e.message.includes('dbGet is not defined') || e.message.includes('Cannot use import')) process.exit(0); else { console.error(e.message); process.exit(1); } })" 2>/dev/null; grep -c "export async function" src/mr-3/mr-cobrancas/src/services/pagamentos.js
    </automated>
  </verify>
  <acceptance_criteria>
    - `src/mr-3/mr-cobrancas/src/services/pagamentos.js` existe
    - Arquivo contém a string `CREATE TABLE public.pagamentos_divida` (migration documentada)
    - Arquivo contém a string `ADD COLUMN IF NOT EXISTS saldo_quitado` (migration documentada)
    - `grep -c "^export" src/mr-3/mr-cobrancas/src/services/pagamentos.js` retorna 5 (4 CRUD + 1 helper)
    - Arquivo contém `import { calcularSaldosPorDivida } from "../utils/devedorCalc.js"`
    - Arquivo contém `const TABLE = "pagamentos_divida"`
    - Arquivo contém `export async function listarPagamentos(`
    - Arquivo contém `export async function criarPagamento(`
    - Arquivo contém `export async function atualizarPagamento(`
    - Arquivo contém `export async function excluirPagamento(`
    - Arquivo contém `export function calcularSaldoPorDividaIndividual(`
    - Arquivo contém `dividas: [divida]` (construção do devedorFicticio)
  </acceptance_criteria>
  <done>Service pagamentos.js criado com CRUD completo sobre pagamentos_divida e helper de cálculo Art.354 por dívida individual. Migrations documentadas no topo do arquivo.</done>
</task>

<task type="auto">
  <name>Task 2: Estender dividas.js com atualizarSaldoQuitado</name>
  <read_first>
    src/mr-3/mr-cobrancas/src/services/dividas.js
    .planning/phases/04-pagamentos-por-divida/04-CONTEXT.md (decisão D-03)
  </read_first>
  <files>src/mr-3/mr-cobrancas/src/services/dividas.js</files>
  <action>
Adicionar uma única função exportada ao final de `dividas.js`, sem alterar nenhuma das funções
existentes (`listarDividas`, `buscarDivida`, `criarDivida`, `atualizarDivida`, `excluirDivida`).

Adicionar APÓS a função `excluirDivida`:

```js
/**
 * Atualiza a coluna saldo_quitado de uma dívida.
 * Chamado após cada operação de pagamento (criar/editar/excluir).
 * TRUE quando saldo ≤ 0, FALSE quando saldo > 0 (per D-03).
 *
 * @param {string} dividaUuid — UUID da dívida
 * @param {boolean} quitado — true se saldo ≤ 0, false se saldo > 0
 * @returns {Promise<Array>}
 */
export async function atualizarSaldoQuitado(dividaUuid, quitado) {
  return sb(`${TABLE}?id=eq.${dividaUuid}`, "PATCH", {
    saldo_quitado: quitado,
    updated_at: new Date().toISOString(),
  });
}
```

Este é o único ponto do codebase que escreve em `dividas.saldo_quitado`. O componente
`PagamentosDivida.jsx` (plan 02) chamará esta função indiretamente através de
`pagamentos.js`, mas a responsabilidade de persistir o campo fica em `dividas.js`.
  </action>
  <verify>
    <automated>grep -n "atualizarSaldoQuitado" src/mr-3/mr-cobrancas/src/services/dividas.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep "export async function atualizarSaldoQuitado" src/mr-3/mr-cobrancas/src/services/dividas.js` retorna uma linha
    - Arquivo contém `saldo_quitado: quitado`
    - Arquivo contém `updated_at: new Date().toISOString()`
    - As 5 funções originais permanecem intactas: `grep -c "^export async function" src/mr-3/mr-cobrancas/src/services/dividas.js` retorna 6 (5 originais + 1 nova)
    - Arquivo ainda começa com `import { sb } from "../config/supabase.js"` (importação não alterada)
  </acceptance_criteria>
  <done>dividas.js estendido com atualizarSaldoQuitado. Todas as funções existentes permanecem intactas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Supabase REST | Todas as mutações em pagamentos_divida e dividas.saldo_quitado passam pela RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Spoofing | pagamentos_divida (RLS) | mitigate | RLS policy `auth.role() = 'authenticated'` bloqueará acesso não autenticado; documentada na migration no topo de pagamentos.js |
| T-04-02 | Tampering | atualizarPagamento — sem validação de ownership | accept | App single-tenant; o advogado só vê dividas do próprio tenant via filtro de UI; custo de validação server-side não justificado em v1.1 |
| T-04-03 | Information Disclosure | listarPagamentos com divida_id fabricado | mitigate | RLS por `auth.role()` garante que somente usuários autenticados acessam; Phase 4 não expõe endpoint público |
| T-04-04 | Elevation of Privilege | ON DELETE CASCADE em divida_id | accept | Cascade é intencional (D-01) — excluir uma dívida deve excluir seus pagamentos; não é vetor de escalação |
</threat_model>

<verification>
Após execução:
1. `grep "export" src/mr-3/mr-cobrancas/src/services/pagamentos.js` — lista 5 exports
2. `grep "atualizarSaldoQuitado" src/mr-3/mr-cobrancas/src/services/dividas.js` — encontra a função
3. `grep "CREATE TABLE public.pagamentos_divida" src/mr-3/mr-cobrancas/src/services/pagamentos.js` — encontra migration
4. `grep "ADD COLUMN IF NOT EXISTS saldo_quitado" src/mr-3/mr-cobrancas/src/services/pagamentos.js` — encontra migration
5. Revisar visualmente que dividas.js não teve funções existentes alteradas
</verification>

<success_criteria>
- pagamentos.js existe com 5 exports e migrations documentadas
- dividas.js tem 6 exports (5 originais + atualizarSaldoQuitado)
- Nenhuma função existente em dividas.js foi modificada
- calcularSaldoPorDividaIndividual usa devedorFicticio com divida.dividas = [divida]
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/04-pagamentos-por-divida/04-01-SUMMARY.md`
</output>
