---
phase: 7.10.bug3
plan: 01
title: "Limpeza Custas Órfãs + Bugfix excluirCusta cascade + Anti-Zero Validation"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mr-3/mr-cobrancas/src/services/contratos.js
  - "<form/handler client-side a identificar em Task 1>"
autonomous: false
estimated_lines: "30-50"
estimated_files: "0 NOVOS + 2-3 MOD"
requirements:
  - 7.10.bug3-D-pre-1   # Cleanup ESPECÍFICO (não genérico)
  - 7.10.bug3-D-pre-2   # Bugfix excluirCusta cascade DELETE
  - 7.10.bug3-D-pre-3   # Anti-zero validation valor > 0 (server + client)
  - 7.10.bug3-D-pre-4   # D-01 motor INTOCADO trivialmente
  - 7.10.bug3-D-pre-5   # Pre-cleanup COUNT global obrigatório (ABORT GATE)
  - 7.10.bug3-D-pre-6   # Cleanup escopado anti-blast-radius
must_haves:
  truths:
    - "Operador no Módulo Dívidas filtro 'mend' vê 13 linhas (não 20), sem R$ 0,00 / R$ 0,00, soma R$ 2.821,81"
    - "Operador no Módulo Dívidas filtro 'advair' não vê 3 linhas órfãs R$ 150,00"
    - "Excluir a última custa de uma dívida-fantasma deleta a dívida (linha some imediatamente do Módulo Dívidas)"
    - "Submeter formulário de criação de custa com valor=0 ou vazio é bloqueado com mensagem 'Valor da custa deve ser maior que zero'"
    - "Re-rodar Query 7+14 pós-cleanup confirma zero órfãs em todas as categorias"
    - "Motor Art.354 (devedorCalc.js + services/pagamentos.js + services/dividas.js + utils/masks.js) ZERO modificações no commit submódulo"
  artifacts:
    - path: "src/mr-3/mr-cobrancas/src/services/contratos.js"
      provides: "excluirCusta cascade DELETE + criarCusta anti-zero throw"
      contains: "Phase 7.10.bug3 D-pre-2|Phase 7.10.bug3 D-pre-3"
    - path: "<form/handler client-side identificado em Task 1>"
      provides: "Anti-zero submit guard + mensagem unificada"
      contains: "Phase 7.10.bug3 D-pre-3"
  key_links:
    - from: "src/services/contratos.js (excluirCusta)"
      to: "dbDelete dividas (junction devedores_dividas FK CASCADE)"
      via: "novasCustas.length === 0 && divida._so_custas === true"
      pattern: "novasCustas\\.length === 0 && divida\\._so_custas === true"
    - from: "src/services/contratos.js (criarCusta)"
      to: "throw 'Valor da custa deve ser maior que zero'"
      via: "valorNumerico > 0 (estrito)"
      pattern: "valorNumerico > 0"
    - from: "<form client-side>"
      to: "service criarCusta"
      via: "submit guard com mesma string de erro"
      pattern: "Valor da custa deve ser maior que zero"
---

<objective>
Limpar 10 dívidas-fantasma órfãs em prod (7 `_so_custas:true` vazias do MENDES E MENDES devedor_id=25 + 3 dívidas regulares legacy advair id=8), corrigir mecânica raiz Phase 7.9 Q2 conservative em `excluirCusta` (services/contratos.js:650-658) substituindo por cascade DELETE quando array vira vazio E `_so_custas=true`, e blindar criação futura via validação `valor > 0` server-side em `criarCusta` + client-side no form de cadastro de custa avulsa.

**Purpose:** sujeira visual constante no Módulo Dívidas (operador vê dados errados todo dia). Defesa-em-profundidade existente via Phase 7.10.bug2.sub1 helper retorna `{original:0, atualizado:0}` para array vazio (UI não quebra durante deploy).

**Output:**
- Submódulo: 1 commit atomic feat (services/contratos.js + arquivo client-side identificado)
- Pai: 1 commit chore bump
- 2 SQL DELETEs operacionais executados em prod (Sub-cleanup A + Sub-cleanup B) via Supabase SQL Editor
- 5 SCs UAT humano em prod
- ZERO push até `UAT 7.10.bug3 PASS` explícito

**D-01 motor INTOCADO trivialmente** (service layer + UI form + SQL ops; motor zero modificações).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07.10-bug3-limpeza-custas-orfas-anti-zero/07.10-bug3-CONTEXT.md
@src/mr-3/mr-cobrancas/src/services/contratos.js
@src/mr-3/mr-cobrancas/src/services/migrations/002_dividas_tabela.sql
@src/mr-3/mr-cobrancas/src/services/__tests__/custasCache.test.js
@src/mr-3/mr-cobrancas/package.json

<interfaces>
<!-- Trecho EXATO de services/contratos.js relevante para Tasks 2 + 3. Executor não precisa explorar codebase. -->

From src/mr-3/mr-cobrancas/src/services/contratos.js:64 (imports — dbDelete JÁ DISPONÍVEL):
```js
import { dbGet, dbInsert, dbUpdate, dbDelete, sb } from "../config/supabase.js";
```

From src/mr-3/mr-cobrancas/src/services/contratos.js:588-618 (criarCusta — alvo Task 3):
```js
export async function criarCusta(contratoId, payload) {
  // FK propagation: dividas.devedor_id é NOT NULL (schema 002_dividas_tabela.sql:18).
  // Reusa helper buscarContrato (L91-93) — TABLE = "contratos_dividas".
  const contratoRows = await buscarContrato(contratoId);
  const contrato = Array.isArray(contratoRows) ? contratoRows[0] : null;
  if (!contrato) throw new Error("contrato não encontrado");

  const dataCusta = payload?.data || "";
  const custaItem = {
    id:             payload?.id || gerarCustaId(),
    descricao:      String(payload?.descricao || "").trim(),
    valor:          Number(payload?.valor || 0),
    data:           dataCusta,
    pago:           !!payload?.pago,
    data_pagamento: payload?.pago ? (payload?.data_pagamento || hojeGoianiaDate()) : null,
  };
  // ... dbInsert dividas com _so_custas:true e custas:[custaItem]
}
```

From src/mr-3/mr-cobrancas/src/services/contratos.js:646-658 (excluirCusta — alvo Task 2):
```js
/**
 * Exclui uma custa do array JSONB. Se ficar vazio E divida._so_custas === true,
 * mantém a dívida vazia (Q2 conservative — evita cascade de effects não auditados).
 */
export async function excluirCusta(contratoId, dividaId, custaId) {
  const dividaRows = await dbGet("dividas", `id=eq.${encodeURIComponent(dividaId)}&limit=1`);
  const divida = Array.isArray(dividaRows) ? dividaRows[0] : null;
  if (!divida) throw new Error("divida não encontrada");
  const custasAtuais = Array.isArray(divida.custas) ? divida.custas : [];
  const novasCustas = custasAtuais.filter(c => String(c.id) !== String(custaId));
  await atualizarDivida(dividaId, { custas: novasCustas });
  // Q2 — dívida avulsa vazia é preservada conservativamente (comment locked).
}
```

From src/mr-3/mr-cobrancas/src/services/migrations/002_dividas_tabela.sql:158-184 (FK CASCADE confirmation):
```sql
CREATE TABLE devedores_dividas (
  id               UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  devedor_id       BIGINT NOT NULL REFERENCES devedores(id) ON DELETE CASCADE,
  divida_id        UUID NOT NULL REFERENCES dividas(id) ON DELETE CASCADE,  -- ← linha 161
  ...
);
```
**→ DELETE FROM dividas WHERE id=X propaga para devedores_dividas via FK CASCADE. Seguro.**

From src/mr-3/mr-cobrancas/package.json (scripts):
```json
"build": "vite build",
"test:regressao": "vitest run src/services/__tests__/calculos.test.js src/services/__tests__/saldoAtualizadoCache.test.js src/services/__tests__/custasCache.test.js src/services/__tests__/processosJudiciaisFilter.test.js src/services/__tests__/agruparPagamentosPorDevedor.test.js src/services/__tests__/devedoresDividasContrato.test.js src/services/__tests__/agruparPagamentosPorDevedorIncluindoSolidarios.test.js src/services/__tests__/dividasPorDevedorIncluindoSolidarios.test.js",
"prebuild": "npm run test:regressao"
```
**→ `npm run build` roda automaticamente test:regressao (34/34 motor regression) ANTES do vite build.**
</interfaces>

## Memory Feedbacks Aplicados

1. **`feedback_blindagens_integridade_apos_funcionamento`** — Phase 7.10.bug3 É a blindagem natural na cadência (Phase 7.9 + 7.10bcd + 7.10.bug2.sub1 = 3 features → 1 phase de blindagem). Cleanup + bugfix preventivo + validation cobre os 3 vetores típicos de gap pós-feature.
2. **`feedback_post_hoc_d_pre_must_be_validated_in_child_phase`** — TASK 0 com operador 2026-04-30 realizada via investigação SQL ampliada (queries 1-14). 6 D-pre lockeds derivam de evidência fresca, não de inferência herdada.
3. **`feedback_db_integration_gate_missing`** — Cleanup SQL precisa pre-execute audit ABORT GATE (D-pre-5: Query 7+14+AJUSTE 1 obrigatórias antes de DELETE). Lição Phase 7.9 SC1 (4 bugs em série em 30min UAT humano).
4. **`feedback_uat_humano_pega_bugs_que_gates_automatizados_nao_pegam`** — DELETE em prod precisa UAT humano explícito (Task 6 PAUSAs entre cada sub-passo).
5. **`feedback_grep_audit_pre_insert_shared_consumers`** — Task 1 grep audit obrigatório identificar form/handler client-side de criarCusta antes de Task 4 (Phase 7.14b removeu CustasAvulsasForm; novo callsite a determinar).
6. **`feedback_so_custas_ui_calculando_indefinido`** — origem tangencial (mesmo mecanismo `_so_custas`). Helper Phase 7.10.bug2.sub1 retorna `{original:0, atualizado:0}` para array vazio garante UI não quebra durante deploy mesmo se houver órfãs em flight.
7. **`feedback_custas_semantica_quitacao_dual`** — D-pre-3 anti-zero relacionado tangencialmente (semântica de custas). Custa zero no sistema ≠ isenção judicial; isenção é decisão registrada em decisão judicial específica.
8. **`feedback_defensive_grep_when_audit_undercounts`** — Investigação SQL ampliada (queries 7-14) pegou 3 órfãs advair que escaparam do diagnóstico inicial focado em MENDES. Pattern aplicado na ABORT GATE Task 6: re-confirmar count exato antes do DELETE em prod (10 = 7 MENDES + 3 advair).

## Invariantes Invioláveis

- **D-01 motor INTOCADO** trivialmente (`devedorCalc.js`, `services/pagamentos.js`, `services/dividas.js`, `utils/masks.js` zero modificações).
- **Schema INTOCADO** (sem ALTER TABLE — Migration 007 FK CASCADE em `dividas.contrato_id` DEFERRED Phase 7.10.bug4 BACKLOG).
- **Junction `devedores_dividas` CASCADE preservado** (Migration 002:161 — não tocar; é o que torna o cascade DELETE de excluirCusta seguro).
- **D-22/23/24/25 (custas legacy shape)** preservados — não alterar shape `{id, descricao, valor, data, pago, data_pagamento}`.
- **D-pre-5 da 7.10.bug2.sub1 (NÃO filtrar c.pago)** preservado (não tocamos motor nem helpers de leitura — apenas service writes).
- **autonomous: false** (UAT humano obrigatório, SQLs ops manualmente em Supabase Editor).
- **Build + 34/34 motor regression PASS** (via `npm run build` que dispara prebuild=test:regressao).
- **ABORT GATE pre-cleanup obrigatório** (count exato 10 antes do DELETE — `orfas_so_custas == 7 AND orfas_advair == 3`).
- **ZERO push** até execute completo + UAT humano PASS + tag `v1.4-phase7.10.bug3` no pai chore bump (orchestrator decide push após PAUSA #5 + PASS Task 6).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Investigation grep audit — identificar form/handler client-side de criarCusta (READ-ONLY)</name>

  <read_first>
    - src/mr-3/mr-cobrancas/src/services/contratos.js (region L575-720 — Phase 7.9 D-23 region completa: criarCusta L588 + editarCusta L629 + excluirCusta L650 + togglePagoCusta L664)
  </read_first>

  <action>
    Esta task é PURAMENTE INVESTIGATIVA. ZERO modificação de arquivo permitida. Output esperado: paths exatos + line ranges + decisão sobre dbDelete import.

    **Sub-passo 1.1 — grep callsites de `criarCusta`:**
    ```bash
    cd src/mr-3/mr-cobrancas
    grep -rn "criarCusta\|criar.*custa" src/components/ src/App.jsx 2>/dev/null
    ```
    Esperado: encontrar callsite client-side (provavelmente em `DetalheContrato.jsx → +Adicionar Documento → tipo "Custa Avulsa"` ou modal específico — Phase 7.14b removeu `CustasAvulsasForm` do drawer Pessoas, então o caminho remanescente é via DetalheContrato).

    **Sub-passo 1.2 — grep import statements de `criarCusta`:**
    ```bash
    grep -rn "import.*criarCusta\|from.*contratos.*criarCusta" src/ 2>/dev/null
    grep -rnE "^import .* from [\"'].*services/contratos" src/components/ src/App.jsx 2>/dev/null
    ```
    Esperado: confirmar consumer único (ou listar TODOS — feedback_grep_audit_pre_insert_shared_consumers manda audit defensivo amplo, não cirúrgico).

    **Sub-passo 1.3 — grep `dbDelete` em contratos.js (verificar se já está importado):**
    ```bash
    grep -nE "import.*dbDelete|dbDelete\s*\(" src/mr-3/mr-cobrancas/src/services/contratos.js
    ```
    **Resultado pré-checado pelo planner (referência do executor):**
    - Linha 64: `import { dbGet, dbInsert, dbUpdate, dbDelete, sb } from "../config/supabase.js";` → `dbDelete` JÁ DISPONÍVEL.
    - Linha 393: `await dbDelete(TABLE, contratoId);` (excluirContrato).
    - Linha 471: `await dbDelete("documentos_contrato", documentoId);` (excluirDocumento).
    Se grep confirmar esses 3 hits → Task 2 NÃO precisa adicionar import.
    Se diff: investigar antes de Task 2.

    **Sub-passo 1.4 — Read full do form/handler identificado:**
    Após Sub-passo 1.1+1.2 identificarem o(s) callsite(s), `Read` o arquivo (range que contém o handler/form) para entender estrutura: campo de valor (input name? state variable?), submit handler (async function? onClick? onSubmit?), local de validação atual (se houver).

    **Sub-passo 1.5 — Verbalizar decisão Task 4:**
    Compor briefing curto (3-5 linhas) com:
    - Path absoluto do arquivo identificado: `src/mr-3/mr-cobrancas/src/<...>`
    - Line range do handler/form
    - Nome do campo de valor (state variable)
    - Local exato onde inserir guard `valor > 0` (linha ou ponto sintático)
    - Mensagem UI exata: `"Valor da custa deve ser maior que zero"` (string match server-side)

    Output este briefing como mensagem assistente final da Task — orchestrator usa para resolver `<form/handler client-side a identificar em Task 1>` em files_modified do plan.
  </action>

  <acceptance_criteria>
    - [ ] Form/handler client-side identificado: path absoluto + line range + nome do state variable de valor
    - [ ] `dbDelete` status confirmado (3 hits esperados em contratos.js: L64 import + L393 excluirContrato + L471 excluirDocumento)
    - [ ] Decisão Task 2 sobre import `dbDelete`: SKIP add (já disponível) OU explicitar caso anômalo
    - [ ] ZERO modificação em código (read-only Task — `git status --porcelain src/` deve estar limpo no fim)
    - [ ] Briefing 3-5 linhas para Task 4 produzido como output da task
  </acceptance_criteria>

  <verify>
    <automated>cd src/mr-3/mr-cobrancas && test -z "$(git status --porcelain src/)"</automated>
  </verify>

  <done>
    Grep audit concluído. Form/handler client-side identificado e documentado. dbDelete status confirmado. ZERO modificação em árvore de trabalho. Briefing pronto para Task 4. PAUSA #1 — orchestrator confirma resolução do path antes de prosseguir.
  </done>

  <pause id="1">
    Orchestrator revisa briefing Task 1 e confirma path do form/handler client-side antes de iniciar Task 2/3/4. Se briefing identifica >1 callsite, orchestrator decide se Task 4 é única ou se split adicional é necessário (não esperado — única callsite remanescente pós-7.14b).
  </pause>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Bugfix excluirCusta cascade — substituir Q2 conservative por DELETE quando array vira vazio E _so_custas=true</name>

  <read_first>
    - src/mr-3/mr-cobrancas/src/services/contratos.js (range L640-680 — excluirCusta + funções vizinhas editarCusta/togglePagoCusta para garantir não-regressão visual)
  </read_first>

  <action>
    **Edit ÚNICO em `src/mr-3/mr-cobrancas/src/services/contratos.js` substituindo linhas 646-658 (excluirCusta + comment Q2 conservative locked).**

    **Diff exato esperado:**

    REMOVER (4 linhas docstring + 9 linhas função = 13 linhas L646-658):
    ```js
    /**
     * Exclui uma custa do array JSONB. Se ficar vazio E divida._so_custas === true,
     * mantém a dívida vazia (Q2 conservative — evita cascade de effects não auditados).
     */
    export async function excluirCusta(contratoId, dividaId, custaId) {
      const dividaRows = await dbGet("dividas", `id=eq.${encodeURIComponent(dividaId)}&limit=1`);
      const divida = Array.isArray(dividaRows) ? dividaRows[0] : null;
      if (!divida) throw new Error("divida não encontrada");
      const custasAtuais = Array.isArray(divida.custas) ? divida.custas : [];
      const novasCustas = custasAtuais.filter(c => String(c.id) !== String(custaId));
      await atualizarDivida(dividaId, { custas: novasCustas });
      // Q2 — dívida avulsa vazia é preservada conservativamente (comment locked).
    }
    ```

    INSERIR no lugar (docstring atualizado + função com cascade):
    ```js
    /**
     * Exclui uma custa do array JSONB.
     * Phase 7.10.bug3 D-pre-2 — substitui Q2 conservative Phase 7.9.
     * Quando array vira vazio E divida._so_custas === true, deleta a dívida-fantasma
     * (cascade automático via junction devedores_dividas FK CASCADE — Migration 002:161).
     */
    export async function excluirCusta(contratoId, dividaId, custaId) {
      const dividaRows = await dbGet("dividas", `id=eq.${encodeURIComponent(dividaId)}&limit=1`);
      const divida = Array.isArray(dividaRows) ? dividaRows[0] : null;
      if (!divida) throw new Error("divida não encontrada");
      const custasAtuais = Array.isArray(divida.custas) ? divida.custas : [];
      const novasCustas = custasAtuais.filter(c => String(c.id) !== String(custaId));
      // Phase 7.10.bug3 D-pre-2 — cascade DELETE substitui Q2 conservative Phase 7.9.
      // Junction devedores_dividas tem ON DELETE CASCADE (Migration 002:161),
      // portanto DELETE da dívida-fantasma propaga limpeza automática à junction.
      if (novasCustas.length === 0 && divida._so_custas === true) {
        await dbDelete("dividas", `id=eq.${encodeURIComponent(dividaId)}`);
      } else {
        await atualizarDivida(dividaId, { custas: novasCustas });
      }
    }
    ```

    **Justificativa do diff:**
    - Comment Q2 conservative locked REMOVIDO (já não é truth — cascade ativo).
    - Audit trail comment "Phase 7.10.bug3 D-pre-2" inserido em DOIS lugares: (1) docstring atualizado, (2) bloco inline antes do `if`. Ambos referenciam Migration 002:161 explicitamente para futuras auditorias.
    - Branch `else { atualizarDivida(...) }` preserva comportamento atual quando não é dívida-fantasma OU array ainda tem itens.
    - `dbDelete` JÁ está importado em L64 (confirmado em Task 1) — ZERO mudança no bloco de imports.
    - ZERO toque em editarCusta L629 ou togglePagoCusta L664.

    **Verificar pós-Edit:**
    1. `grep -c "Phase 7.10.bug3 D-pre-2" src/mr-3/mr-cobrancas/src/services/contratos.js` ≥ 2
    2. `grep -c "novasCustas\.length === 0 && divida\._so_custas === true" src/mr-3/mr-cobrancas/src/services/contratos.js` = 1
    3. `grep -c "dbDelete.*\"dividas\"" src/mr-3/mr-cobrancas/src/services/contratos.js` ≥ 1 (na excluirCusta)
    4. `grep -c "Q2 conservative" src/mr-3/mr-cobrancas/src/services/contratos.js` = 0 (Q2 conservative original 100% removido)
    5. `grep -c "Q2 — dívida avulsa vazia é preservada conservativamente" src/mr-3/mr-cobrancas/src/services/contratos.js` = 0 (comment locked original 100% removido)
    6. `cd src/mr-3/mr-cobrancas && npm run build` exit 0 (cobre prebuild=test:regressao 34/34 + vite build)
  </action>

  <acceptance_criteria>
    - [ ] `grep -c "Phase 7.10.bug3 D-pre-2" src/mr-3/mr-cobrancas/src/services/contratos.js` ≥ 2 (audit trail comment dual: docstring + inline)
    - [ ] `grep -c "novasCustas\.length === 0 && divida\._so_custas === true" src/mr-3/mr-cobrancas/src/services/contratos.js` = 1
    - [ ] `grep -c "dbDelete.*\"dividas\"" src/mr-3/mr-cobrancas/src/services/contratos.js` ≥ 1
    - [ ] `grep -c "Q2 conservative" src/mr-3/mr-cobrancas/src/services/contratos.js` = 0 (comment original 100% removido)
    - [ ] `grep -c "Q2 — dívida avulsa vazia é preservada conservativamente" src/mr-3/mr-cobrancas/src/services/contratos.js` = 0
    - [ ] Bloco de imports L64 INTOCADO (`dbDelete` já estava lá)
    - [ ] editarCusta L629 + togglePagoCusta L664 INTOCADOS
    - [ ] `cd src/mr-3/mr-cobrancas && npm run build` exit 0 (34/34 motor regression PASS + vite build PASS)
  </acceptance_criteria>

  <verify>
    <automated>cd src/mr-3/mr-cobrancas && \
      test "$(grep -c 'Phase 7.10.bug3 D-pre-2' src/services/contratos.js)" -ge 2 && \
      test "$(grep -c 'novasCustas\.length === 0 && divida\._so_custas === true' src/services/contratos.js)" = "1" && \
      test "$(grep -c 'Q2 conservative' src/services/contratos.js)" = "0" && \
      test "$(grep -c 'Q2 — dívida avulsa vazia é preservada conservativamente' src/services/contratos.js)" = "0" && \
      npm run build</automated>
  </verify>

  <done>
    excluirCusta cascade DELETE implementado. Audit trail comment dual presente. Q2 conservative comment original 100% removido. 34/34 motor regression PASS via prebuild hook. vite build PASS. PAUSA #2 — orchestrator revisa diff completo de excluirCusta antes de Task 3.
  </done>

  <pause id="2">
    Orchestrator revisa `git diff src/mr-3/mr-cobrancas/src/services/contratos.js` (espera ~13 linhas removidas + ~17 linhas inseridas, escopo limitado a L646-660). Confirma branch else preservado para casos não-fantasma.
  </pause>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Anti-zero server-side em criarCusta — throw quando valor <= 0</name>

  <read_first>
    - src/mr-3/mr-cobrancas/src/services/contratos.js (range L580-620 — criarCusta full + buscarContrato vizinha pra contexto)
  </read_first>

  <action>
    **Edit em `src/mr-3/mr-cobrancas/src/services/contratos.js` adicionando guard server-side LOGO APÓS validação de contrato (após `if (!contrato) throw new Error("contrato não encontrado");`, ANTES da composição de `dataCusta`/`custaItem`).**

    **Diff exato esperado (insert ~7 linhas após L593):**

    Localização: imediatamente após linha `if (!contrato) throw new Error("contrato não encontrado");` (atualmente L593) e ANTES de `const dataCusta = payload?.data || "";` (atualmente L595).

    INSERIR:
    ```js

      // Phase 7.10.bug3 D-pre-3 — bloquear valor <= 0 (estrito: NÃO valor >= 0).
      // Custa zero não é cadastro de escritório: isenção de custas é decisão
      // do juiz registrada em decisão judicial específica, não custa cadastrada
      // com valor 0 no sistema de cobrança.
      const valorNumerico = Number(payload?.valor || 0);
      if (!(valorNumerico > 0)) {
        throw new Error("Valor da custa deve ser maior que zero");
      }
    ```

    **Justificativa do diff:**
    - Posição: após validação de contrato existente (idempotente com pattern atual de validação no service) e ANTES da construção do `custaItem` (evita cálculo desnecessário se já vai abortar).
    - Padrão `!(valorNumerico > 0)` (estrito) — captura `valor === 0`, `valor < 0`, `valor === null/undefined → 0` via `payload?.valor || 0`, e `valor === NaN` via `Number()`. Equivale a "valor === 0 OR negativo OR não-numérico convertível".
    - 4 linhas de comentário audit trail D-pre-3 explicitam regra de negócio (semântica de isenção judicial vs custa zero) — futuras auditorias entendem o porquê.
    - Mensagem string EXATA `"Valor da custa deve ser maior que zero"` (sem ponto final, sem acento típico) — match literal com client-side em Task 4.
    - ZERO toque na composição de `custaItem`, no `dbInsert("dividas", ...)`, no return statement.

    **Verificar pós-Edit:**
    1. `grep -c "Phase 7.10.bug3 D-pre-3" src/mr-3/mr-cobrancas/src/services/contratos.js` ≥ 1
    2. `grep -c "Valor da custa deve ser maior que zero" src/mr-3/mr-cobrancas/src/services/contratos.js` = 1
    3. `grep -c "valorNumerico > 0" src/mr-3/mr-cobrancas/src/services/contratos.js` = 1
    4. `grep -c "throw new Error" src/mr-3/mr-cobrancas/src/services/contratos.js` aumenta exatamente em 1 vs baseline pré-Task 3 (não inadvertidamente removendo throws existentes)
    5. `cd src/mr-3/mr-cobrancas && npm run build` exit 0
  </action>

  <acceptance_criteria>
    - [ ] `grep -c "Phase 7.10.bug3 D-pre-3" src/mr-3/mr-cobrancas/src/services/contratos.js` ≥ 1
    - [ ] `grep -c "Valor da custa deve ser maior que zero" src/mr-3/mr-cobrancas/src/services/contratos.js` = 1
    - [ ] `grep -c "valorNumerico > 0" src/mr-3/mr-cobrancas/src/services/contratos.js` = 1
    - [ ] Throw inserido ANTES da composição de `custaItem` (não após dbInsert — falha antes do INSERT)
    - [ ] `cd src/mr-3/mr-cobrancas && npm run build` exit 0
    - [ ] excluirCusta D-pre-2 (Task 2) preservado: `grep -c "Phase 7.10.bug3 D-pre-2"` ≥ 2
  </acceptance_criteria>

  <verify>
    <automated>cd src/mr-3/mr-cobrancas && \
      test "$(grep -c 'Phase 7.10.bug3 D-pre-3' src/services/contratos.js)" -ge 1 && \
      test "$(grep -c 'Valor da custa deve ser maior que zero' src/services/contratos.js)" = "1" && \
      test "$(grep -c 'valorNumerico > 0' src/services/contratos.js)" = "1" && \
      test "$(grep -c 'Phase 7.10.bug3 D-pre-2' src/services/contratos.js)" -ge 2 && \
      npm run build</automated>
  </verify>

  <done>
    Anti-zero server-side em criarCusta inserido. Throw exato `"Valor da custa deve ser maior que zero"` blindando criação via REST direto, scripts admin, ou client com guard quebrado. 34/34 motor regression PASS. PAUSA #3 — orchestrator revisa diff criarCusta antes de Task 4.
  </done>

  <pause id="3">
    Orchestrator revisa `git diff src/mr-3/mr-cobrancas/src/services/contratos.js` cumulativo (Tasks 2 + 3). Confirma posição do guard ANTES da composição de custaItem.
  </pause>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Anti-zero client-side no form identificado em Task 1</name>

  <read_first>
    - Arquivo identificado em Task 1 (path absoluto resolvido na PAUSA #1; preencher aqui antes de iniciar)
  </read_first>

  <action>
    **Edit em `<arquivo identificado em Task 1>` adicionando validação client-side que bloqueia submit se valor <= 0, com mensagem unificada server-side.**

    **Pattern a aplicar (ajustar conforme estrutura real do form identificado em Task 1):**

    Localizar handler de submit/criação que invoca `criarCusta(...)` e inserir guard ANTES da chamada async:

    ```js
    // Phase 7.10.bug3 D-pre-3 — anti-zero client-side (espelha guard server-side).
    // Mensagem unificada com src/services/contratos.js criarCusta — falha aborta antes do REST.
    const valorNumericoCliente = Number(<state-variable-do-campo-valor>) || 0;
    if (!(valorNumericoCliente > 0)) {
      // Substituir por toast ou setError local conforme padrão do form (verificar Task 1):
      // Se form usa react-hot-toast (presente em package.json):
      //   import toast from 'react-hot-toast'; toast.error("Valor da custa deve ser maior que zero");
      // Se form usa state local de erro:
      //   setErroFormulario("Valor da custa deve ser maior que zero");
      alert("Valor da custa deve ser maior que zero"); // FALLBACK — orchestrator confirma padrão UI durante PAUSA #4
      return; // bloqueia submit
    }
    await criarCusta(contratoId, payload);
    ```

    **Decisões em aberto resolvidas durante Task 4:**
    1. **Padrão de exibição de erro no form:** `react-hot-toast` (ver `import toast from 'react-hot-toast'` no topo do arquivo em Task 1) vs setState local vs alert(). Orchestrator define em PAUSA #1 → decisão herdada.
    2. **State variable do valor:** nome resolvido em Task 1 (ex: `valorCusta`, `formData.valor`, etc.).
    3. **Posição do guard:** antes do `await criarCusta(...)` ou onSubmit handler. Não mudar lógica de submit existente.

    **Mensagem UI literal:** `"Valor da custa deve ser maior que zero"` — STRING IDÊNTICA ao server-side (Task 3) para que UAT possa diferenciar fonte da rejeição (client trips primeiro → mensagem UI imediata; server fallback se UI bypassada → mesmo texto).

    **Verificar pós-Edit:**
    1. `grep -c "Phase 7.10.bug3 D-pre-3" <arquivo identificado>` ≥ 1
    2. `grep -c "Valor da custa deve ser maior que zero" <arquivo identificado>` = 1 (string match server-side)
    3. `grep -c "valorNumericoCliente > 0" <arquivo identificado>` = 1 (ou nome equivalente — match com sub-passo 1.2 de Task 1)
    4. `cd src/mr-3/mr-cobrancas && npm run build` exit 0

    **Defesa em profundidade:** se este client-side guard for quebrado em refactor futuro (sem audit), o server-side em Task 3 ainda bloqueia.
  </action>

  <acceptance_criteria>
    - [ ] `grep -c "Phase 7.10.bug3 D-pre-3" <arquivo>` ≥ 1
    - [ ] `grep -c "Valor da custa deve ser maior que zero" <arquivo>` = 1 (mensagem unificada com server-side)
    - [ ] Guard inserido ANTES da chamada `await criarCusta(...)` (não após — bloqueio antes do REST)
    - [ ] Padrão de exibição de erro consistente com resto do arquivo (toast/setState/alert — orchestrator decide PAUSA #4)
    - [ ] ZERO mudança em src/services/contratos.js nesta task (Tasks 2+3 já tocaram)
    - [ ] `cd src/mr-3/mr-cobrancas && npm run build` exit 0 (34/34 motor regression PASS preservado)
  </acceptance_criteria>

  <verify>
    <automated>cd src/mr-3/mr-cobrancas && \
      ARQ="<resolvido na PAUSA #1>" && \
      test "$(grep -c 'Phase 7.10.bug3 D-pre-3' "$ARQ")" -ge 1 && \
      test "$(grep -c 'Valor da custa deve ser maior que zero' "$ARQ")" = "1" && \
      npm run build</automated>
  </verify>

  <done>
    Anti-zero client-side inserido no form identificado em Task 1, bloqueando submit antes do REST com mensagem unificada server-side. 34/34 motor regression PASS preservado. Defesa em profundidade ativa: client guard primeiro, server guard fallback. PAUSA #4 — orchestrator revisa diff form client-side cumulativo (Tasks 2+3+4) antes de Task 5.
  </done>

  <pause id="4">
    Orchestrator revisa `git diff` cumulativo nos 2 arquivos modificados (services/contratos.js + form identificado em Task 1). Confirma:
    - Mensagem string idêntica server↔client (`"Valor da custa deve ser maior que zero"`)
    - Padrão UI escolhido consistente com codebase
    - Guard antes do `await criarCusta(...)`
  </pause>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Build + Tests + Gate D-01 dual + Commit submódulo + Chore bump pai (ZERO push)</name>

  <read_first>
    - src/mr-3/mr-cobrancas/package.json (sanity scripts)
    - .gitignore (sanity — não acidentalmente staging arquivos ignorados)
  </read_first>

  <action>
    **Sub-passo 5.1 — Build full submódulo:**
    ```bash
    cd src/mr-3/mr-cobrancas
    npm run build
    ```
    Esperado:
    - `prebuild` hook dispara `npm run test:regressao` → 8 test files / 34 tests PASS (motor regression cumulative)
    - `vite build` exit 0

    **Sub-passo 5.2 — Gate 4a working tree D-01 motor INTOCADO (4 zeros):**
    ```bash
    cd src/mr-3/mr-cobrancas
    test "$(git diff -U0 src/utils/devedorCalc.js | wc -l)" = "0" && \
    test "$(git diff -U0 src/services/pagamentos.js | wc -l)" = "0" && \
    test "$(git diff -U0 src/services/dividas.js | wc -l)" = "0" && \
    test "$(git diff -U0 src/utils/masks.js | wc -l)" = "0" && \
    echo "Gate 4a PASS"
    ```
    Se qualquer teste falhar: ABORT — investigar drift acidental antes de commit.

    **Sub-passo 5.3 — Stage explícito (NÃO `git add .`):**
    Stage APENAS arquivos modificados pelas Tasks 2/3/4:
    ```bash
    cd src/mr-3/mr-cobrancas
    git add src/services/contratos.js
    git add <arquivo client-side identificado em Task 1>
    # Verificar pré-commit:
    git status --short
    # Esperado: 2 arquivos M staged, ZERO outros arquivos modificados
    ```

    **Sub-passo 5.4 — Commit submódulo atomic:**
    ```bash
    cd src/mr-3/mr-cobrancas
    git commit -m "feat(07.10.bug3): bugfix excluirCusta cascade + anti-zero validation

- excluirCusta: cascade DELETE quando array vira vazio E _so_custas=true
  (substitui Q2 conservative Phase 7.9; junction devedores_dividas
  FK CASCADE Migration 002:161 garante limpeza segura).
- criarCusta: throw \"Valor da custa deve ser maior que zero\" se valor <= 0
  (estrito; bloqueia REST direto + scripts admin).
- Form client-side <arquivo>: guard valor > 0 antes do submit
  (mensagem unificada server-side; defesa em profundidade).

D-01 motor INTOCADO: devedorCalc.js + services/pagamentos.js +
services/dividas.js + utils/masks.js zero modifications.

Refs: 7.10.bug3-D-pre-2, 7.10.bug3-D-pre-3, 7.10.bug3-D-pre-4

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
    ```

    **Sub-passo 5.5 — Gate 4b post-commit D-01 motor INTOCADO (--name-only):**
    ```bash
    cd src/mr-3/mr-cobrancas
    if git log -1 --format="" --name-only | grep -qE "(utils/devedorCalc\.js|services/pagamentos\.js|services/dividas\.js|utils/masks\.js)"; then
      echo "Gate 4b FAIL — D-01 motor file presente no commit submódulo"
      exit 1
    else
      echo "Gate 4b PASS — D-01 motor INTOCADO"
    fi
    ```
    Lição feedback_gate_motor_grep_use_name_only: usar `--name-only` (não `--stat`) — `--stat` casa em commit message text e gera false positive.

    **Sub-passo 5.6 — Voltar pro pai e bump submódulo:**
    ```bash
    cd ../../../
    # Verificar que estamos no diretório raiz mr-cobrancas (pai):
    pwd  # → C:/Users/advai/Downloads/mr-cobrancas-vercel_1/mr-cobrancas
    git status --short
    # Esperado: src/mr-3 modificado (M), nada mais
    git add src/mr-3
    git commit -m "chore(07.10.bug3): bump submódulo — bugfix excluirCusta cascade + anti-zero validation

Submódulo SHA: <SHA-do-commit-submódulo-Sub-passo-5.4>

Refs: 7.10.bug3-D-pre-2, 7.10.bug3-D-pre-3

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
    ```

    **Sub-passo 5.7 — Verificação final pré-PAUSA #5:**
    ```bash
    # No pai:
    git log -1 --format="%H %s"
    # Esperado: <SHA-pai> chore(07.10.bug3): bump submódulo ...
    # No submódulo:
    cd src/mr-3/mr-cobrancas
    git log -1 --format="%H %s"
    # Esperado: <SHA-submódulo> feat(07.10.bug3): bugfix excluirCusta cascade ...
    ```

    **ZERO `git push` neste task.** Push é decisão do orchestrator no Task 6 após autorização explícita.
  </action>

  <acceptance_criteria>
    - [ ] `npm run build` exit 0 (cobre prebuild=test:regressao 34/34 + vite build)
    - [ ] Gate 4a (4 zeros) PASS — devedorCalc.js + services/pagamentos.js + services/dividas.js + utils/masks.js intocados em working tree pré-commit
    - [ ] Stage explícito apenas 2 arquivos (services/contratos.js + arquivo client-side); `git status --short` limpo pós-commit
    - [ ] Commit submódulo presente: `git log -1 --format="%s"` no submódulo retorna `feat(07.10.bug3): bugfix excluirCusta cascade + anti-zero validation`
    - [ ] Gate 4b (`--name-only` filter) PASS — commit submódulo NÃO contém arquivos motor
    - [ ] Commit pai presente: `git log -1 --format="%s"` no pai retorna `chore(07.10.bug3): bump submódulo — bugfix excluirCusta cascade + anti-zero validation`
    - [ ] `git status` limpo no pai e no submódulo (zero arquivos modificados pendentes)
    - [ ] ZERO `git push` executado (verificável: `git log origin/master..HEAD --oneline` no pai retorna ≥1 linha não pushed)
  </acceptance_criteria>

  <verify>
    <automated>cd src/mr-3/mr-cobrancas && \
      npm run build && \
      test "$(git diff -U0 src/utils/devedorCalc.js | wc -l)" = "0" && \
      test "$(git diff -U0 src/services/pagamentos.js | wc -l)" = "0" && \
      test "$(git diff -U0 src/services/dividas.js | wc -l)" = "0" && \
      test "$(git diff -U0 src/utils/masks.js | wc -l)" = "0" && \
      ! git log -1 --format="" --name-only | grep -qE "(utils/devedorCalc\.js|services/pagamentos\.js|services/dividas\.js|utils/masks\.js)"</automated>
  </verify>

  <done>
    Build PASS + 34/34 motor regression PASS + Gate 4a (4 zeros working tree) + Gate 4b (post-commit --name-only filter). 1 commit submódulo atomic feat + 1 commit pai chore bump. Estado pré-push limpo (working tree clean ambos repos). PAUSA #5 — orchestrator revisa SHA submódulo + SHA pai + Gate 4b PASS confirmation antes de autorizar push em Task 6.
  </done>

  <pause id="5">
    Orchestrator revisa estado pré-push:
    - SHA submódulo (commit feat 07.10.bug3)
    - SHA pai (commit chore bump)
    - Gate 4b output literal (`Gate 4b PASS — D-01 motor INTOCADO`)
    - `git log origin/master..HEAD --oneline` no pai (deve listar ≥1 commit não pushed)

    Decide: autorizar push + Vercel deploy + SQL ops + UAT (Task 6) OU rollback se algo soou errado.
  </pause>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 6: SQL ops em prod + UAT humano CHECKPOINT (5 SCs)</name>

  <what-built>
    Tasks 2-5 produziram:
    - Submódulo `src/mr-3/mr-cobrancas`: 1 commit atomic feat (services/contratos.js + form client-side) — bugfix excluirCusta cascade + anti-zero server + anti-zero client
    - Pai `mr-cobrancas`: 1 commit chore bump submódulo
    - Estado: NÃO pushed, NÃO deployed, ZERO mudança em prod ainda
    - Gates 4a + 4b PASS (D-01 motor INTOCADO dual)

    Task 6 NÃO escreve código. Task 6 é checkpoint humano-driven com 4 sub-passos:
    (a) push + Vercel deploy
    (b) Pre-cleanup ABORT GATE
    (c) Sub-cleanup A + B SQL DELETE em prod
    (d) UAT humano 5 SCs
  </what-built>

  <how-to-verify>
    **Sub-passo 6.a — Push submódulo + chore bump pai (orchestrator autoriza explicitamente após PAUSA #5):**
    ```bash
    cd src/mr-3/mr-cobrancas
    git push origin master
    cd ../../../
    git push origin master
    ```
    Aguardar Vercel "Ready" (~1-3 min). Confirmar deploy via mrcobrancas.com.br responsiva sem 500.

    **Sub-passo 6.b — PRE-CLEANUP ABORT GATE (operador roda no Supabase SQL Editor):**

    Query 7 (count global `_so_custas` vazias):
    ```sql
    SELECT COUNT(*) AS orfas_so_custas
    FROM dividas
    WHERE _so_custas = true
      AND jsonb_array_length(COALESCE(custas, '[]'::jsonb)) = 0;
    ```

    Query 14 (resumo categorias):
    ```sql
    SELECT
      COUNT(*) FILTER (WHERE _so_custas = true AND jsonb_array_length(COALESCE(custas, '[]'::jsonb)) = 0) AS so_custas_vazias_orfas,
      COUNT(*) FILTER (WHERE _so_custas = true AND jsonb_array_length(COALESCE(custas, '[]'::jsonb)) > 0) AS so_custas_com_dados,
      COUNT(*) FILTER (WHERE _so_custas IS NOT TRUE AND contrato_id IS NULL) AS regulares_sem_contrato,
      COUNT(*) FILTER (WHERE _so_custas IS NOT TRUE AND contrato_id IS NOT NULL) AS regulares_normais,
      COUNT(*) AS total_dividas
    FROM dividas;
    ```

    Query AJUSTE 1 (count total `_so_custas:true`):
    ```sql
    SELECT COUNT(*) AS total_so_custas_true
    FROM dividas
    WHERE _so_custas = true;
    ```

    Query Sub-cleanup B preview (count órfãs advair via lista IDs):
    ```sql
    SELECT id, devedor_id, valor_total, contrato_id, created_at
    FROM dividas
    WHERE id IN (
      '85066e3d-289d-4cd2-9b00-09e8f219a5cd',
      'd82841e3-33d3-4c36-8a8d-93c7398530a1',
      '6cd6e321-62cd-4f68-b3aa-65d0f7a4f00f'
    );
    ```

    **ABORT GATE conditions (D-pre-5 + D-pre-6):**
    - Se `orfas_so_custas` > 7 → **ABORT**, re-investigar diferença antes de prosseguir (alguma custa criada/excluída entre 2026-04-30 e execução? UI cliente mudou comportamento?)
    - Se `orfas_so_custas` < 7 → **ABORT**, listar IDs presentes vs IDs documentados (quem deletou manualmente?)
    - Se Sub-cleanup B preview retornar < 3 rows → **ABORT**, listar quais IDs sumiram
    - Se Sub-cleanup B preview retornar > 3 rows → impossível (lista fechada), ABORT por sanity
    - Se `orfas_so_custas == 7` AND Sub-cleanup B preview retornar 3 rows com devedor_id=8 → **PROSSEGUE para 6.b.5**

    **Sub-passo 6.b.5 — BACKUP SNAPSHOT pré-DELETE (defesa em profundidade hardening, post-checker INFO #1):**

    Justificativa: ABORT GATE (6.b.4) cobre count divergente, mas não cobre "DELETE certo de dado errado" (bug Postgres `jsonb_array_length` edge case, race condition entre ABORT GATE e DELETE, erro humano). Backup permite rollback trivial via `INSERT INTO dividas SELECT * FROM dividas_backup_pre_710bug3` se SC1/SC2 FAIL pós-cleanup.

    Operador executa no Supabase SQL Editor:
    ```sql
    CREATE TABLE IF NOT EXISTS dividas_backup_pre_710bug3 AS
    SELECT * FROM dividas
    WHERE (_so_custas = true AND jsonb_array_length(COALESCE(custas, '[]'::jsonb)) = 0 AND devedor_id = 25)
       OR id IN (
         '85066e3d-289d-4cd2-9b00-09e8f219a5cd',
         'd82841e3-33d3-4c36-8a8d-93c7398530a1',
         '6cd6e321-62cd-4f68-b3aa-65d0f7a4f00f'
       );
    ```

    Verify pós-INSERT (operador executa):
    ```sql
    SELECT COUNT(*) FROM dividas_backup_pre_710bug3;
    -- Esperado: 10 rows (7 MENDES + 3 advair)
    ```

    **ABORT secundário:**
    - Se `COUNT(*) != 10` → **ABORT antes de Sub-cleanup A** (backup falhou, não prossegue com DELETE sem rede de proteção)
    - Se `COUNT(*) == 10` → **PROSSEGUE para 6.c**

    NOTA: tabela `dividas_backup_pre_710bug3` é temporária — drop ocorre em Sub-passo 6.e pós-UAT PASS confirmado.

    **Sub-passo 6.c — Sub-cleanup A + B SQL DELETE em prod (operador executa no Supabase SQL Editor um de cada vez, com pause entre):**

    Sub-cleanup A (7 órfãs MENDES):
    ```sql
    DELETE FROM dividas
    WHERE _so_custas = true
      AND jsonb_array_length(COALESCE(custas, '[]'::jsonb)) = 0
      AND devedor_id = 25;
    -- Esperado: DELETE 7
    ```

    PAUSA: re-rodar Query 7. Esperado: `orfas_so_custas = 0` (MENDES limpo).

    Sub-cleanup B (3 órfãs advair):
    ```sql
    DELETE FROM dividas
    WHERE id IN (
      '85066e3d-289d-4cd2-9b00-09e8f219a5cd',
      'd82841e3-33d3-4c36-8a8d-93c7398530a1',
      '6cd6e321-62cd-4f68-b3aa-65d0f7a4f00f'
    );
    -- Esperado: DELETE 3
    ```

    POST-CLEANUP — re-rodar Query 7 + Query 14:
    - Query 7: `orfas_so_custas = 0`
    - Query 14: `so_custas_vazias_orfas = 0` AND `regulares_sem_contrato = 0` (se eram só os 3 advair) AND `total_dividas = 140` (150 - 10)

    **Sub-passo 6.d — UAT humano 5 SCs em prod:**

    **SC1 — Módulo Dívidas filtro "mend":**
    1. Abrir mrcobrancas.com.br → Módulo Dívidas
    2. Filtro "mend"
    3. Esperado: **13 linhas** (não 20). ZERO linhas com Valor Original R$ 0,00 / Saldo Atualizado R$ 0,00. Soma `Σ Saldo` ≈ R$ 2.821,81.
    4. PASS se 13 linhas + sem R$ 0,00. FAIL caso contrário (FAIL → ABORT, rollback via INSERT manual a partir de backup ou continuar com sujeira reduzida — orchestrator decide).

    **SC2 — Módulo Dívidas filtro "advair":**
    1. Filtro "advair" (devedor id=8)
    2. Esperado: ZERO linhas R$ 150,00 órfãs (todas removidas). Outras dívidas legítimas advair (se houver) preservadas.
    3. PASS se órfãs sumiram E nenhuma dívida legítima foi deletada por engano. FAIL caso contrário.

    **SC3 — Bugfix excluirCusta cascade:**
    1. Em qualquer contrato: criar custa teste com valor R$ 100,00 (use form client-side com valor positivo para validar funcionamento)
    2. Verificar Módulo Dívidas: 1 nova linha aparece (dívida-fantasma `_so_custas:true`)
    3. Voltar ao DetalheContrato → deletar a custa criada
    4. Esperado: linha **desaparece imediatamente** do Módulo Dívidas (cascade DELETE ativo)
    5. Re-rodar Query 7: `orfas_so_custas = 0` (cascade removeu a dívida-fantasma, não deixou órfã)
    6. PASS se linha some + Query 7 = 0. FAIL caso contrário.

    **SC4 — Anti-zero validation:**
    1. Tentar criar custa com `valor=0` (digitar 0 ou deixar vazio que vira 0)
    2. Submeter
    3. Esperado: Submit bloqueado, mensagem "Valor da custa deve ser maior que zero"
    4. Confirmar via SQL: `SELECT COUNT(*) FROM dividas WHERE created_at > NOW() - INTERVAL '5 minutes' AND _so_custas = true;` → 0 (nada foi criado)
    5. PASS se UI bloqueia + DB sem inserção. FAIL caso contrário.

    **SC5 — Cleanup global validation:**
    1. Re-rodar Query 14 final
    2. Esperado: `so_custas_vazias_orfas = 0`, `regulares_sem_contrato = 0` (ou apenas dívidas históricas legítimas pré-Phase 5 que NÃO estão na lista B)
    3. PASS se Query 14 limpo. FAIL caso contrário.

    **Sub-passo 6.e — DROP backup snapshot pós-UAT PASS (cleanup tabela temporária):**

    Pré-condição: TODOS os 5 SCs (SC1-SC5) PASS confirmados. Operador autoriza explicitamente drop do backup (resume signal `UAT 7.10.bug3 PASS` recebido).

    Justificativa: tabela `dividas_backup_pre_710bug3` só existe enquanto rollback for possibilidade real. Pós-UAT PASS em prod, rollback não é mais necessário — drop libera espaço e mantém schema limpo (sem tabelas temporárias órfãs cumulative entre phases).

    Operador executa no Supabase SQL Editor:
    ```sql
    DROP TABLE IF EXISTS dividas_backup_pre_710bug3;
    ```

    Verify pós-DROP:
    ```sql
    SELECT EXISTS (
      SELECT 1 FROM information_schema.tables
      WHERE table_schema = 'public' AND table_name = 'dividas_backup_pre_710bug3'
    );
    -- Esperado: false
    ```

    **Resume signal exato:**
    - `UAT 7.10.bug3 PASS` — todos os 5 SCs PASS, autorizar tag v1.4-phase7.10.bug3 no commit pai chore bump (SHA da Task 5 sub-passo 5.6) e push da tag.
    - `UAT FAIL [SC#] [motivo]` — orchestrator decide rollback ou fix-forward conforme severidade.

    **NOTA: Tag push apenas no pai (chore bump)** — submódulo NÃO recebe tag (lição feedback_tag_push_apenas_no_pai_chore_bump; pai+submódulo compartilham mesmo remote URL → namespace global). Tag pattern: `git tag -a v1.4-phase7.10.bug3 <SHA-pai> -m "Phase 7.10.bug3 SHIPPED — limpeza custas órfãs + bugfix excluirCusta cascade + anti-zero validation" && git push origin v1.4-phase7.10.bug3`.
  </how-to-verify>

  <resume-signal>
    `UAT 7.10.bug3 PASS` (todos 5 SCs PASS) OU `UAT FAIL [SC#] [motivo]`
  </resume-signal>

  <acceptance_criteria>
    - [ ] Push submódulo + push pai executados após autorização explícita orchestrator (PAUSA #5)
    - [ ] Vercel deploy "Ready" confirmado pre-cleanup
    - [ ] ABORT GATE PASS: `orfas_so_custas == 7` AND Sub-cleanup B preview retorna 3 rows devedor_id=8
    - [ ] Sub-passo 6.b.5 backup snapshot criado pré-DELETE: `CREATE TABLE dividas_backup_pre_710bug3` + verify `COUNT(*) = 10`
    - [ ] Sub-cleanup A executado: DELETE 7 rows, sem erro
    - [ ] Sub-cleanup B executado: DELETE 3 rows, sem erro
    - [ ] Post-cleanup: Query 7 = 0, Query 14 `so_custas_vazias_orfas = 0`
    - [ ] SC1 PASS: 13 linhas filtro "mend", sem R$ 0,00 / R$ 0,00, soma ≈ R$ 2.821,81
    - [ ] SC2 PASS: zero órfãs filtro "advair", legítimas preservadas
    - [ ] SC3 PASS: criar+deletar custa teste → linha some imediatamente + Query 7 = 0
    - [ ] SC4 PASS: submit valor=0 bloqueado com mensagem exata "Valor da custa deve ser maior que zero", DB sem inserção
    - [ ] SC5 PASS: Query 14 final limpo
    - [ ] Sub-passo 6.e DROP backup pós-UAT PASS: `DROP TABLE IF EXISTS dividas_backup_pre_710bug3` + verify table não existe
    - [ ] Resume signal `UAT 7.10.bug3 PASS` recebido pelo orchestrator
    - [ ] Tag `v1.4-phase7.10.bug3` no commit pai chore bump + push da tag (apenas pai, submódulo NÃO tem tag)
  </acceptance_criteria>

  <verify>
    <automated>echo "Task 6 é checkpoint humano-driven — verificação via resume signal + Query 7+14 manual no Supabase + 5 SCs UAT visual. Acceptance criteria valida outputs textuais do operador."</automated>
  </verify>

  <done>
    SQL ops executados em prod (Sub-cleanup A + B totalizando 10 rows DELETE com cascade automático na junction). 5 SCs UAT humano PASS REAL. DB limpo (10 órfãs → 0). Bugfix excluirCusta cascade ativo em prod. Anti-zero validation ativo server+client. Tag v1.4-phase7.10.bug3 pushed no pai chore bump SHA. Phase SHIPPED.
  </done>
</task>

</tasks>

<verification>
**Phase-level verification (overall checks):**

1. **D-01 motor INTOCADO dual cumulative:**
   - Gate 4a working tree: 4 zeros pré-commit submódulo (Task 5)
   - Gate 4b post-commit `--name-only`: nenhum dos 4 arquivos motor presente no commit submódulo (Task 5)
   - Cumulative cross-phase: ≥60+ commits desde Phase 7.8.2a sem tocar motor

2. **Schema INTOCADO:**
   - ZERO ALTER TABLE/CREATE TABLE em qualquer arquivo modificado
   - Migration 007 (FK CASCADE em `dividas.contrato_id`) DEFERRED Phase 7.10.bug4 BACKLOG

3. **Junction CASCADE confirmation operacional:**
   - Sub-cleanup A DELETE em `dividas` propaga para `devedores_dividas` automaticamente (verificável via SELECT pós-DELETE: `SELECT COUNT(*) FROM devedores_dividas WHERE divida_id IN (<7 IDs MENDES>)` → 0)

4. **Audit trail comments presentes:**
   - `Phase 7.10.bug3 D-pre-2` ≥ 2 hits em `services/contratos.js` (docstring excluirCusta + inline)
   - `Phase 7.10.bug3 D-pre-3` ≥ 1 hit em `services/contratos.js` (criarCusta) + ≥ 1 hit no form client-side

5. **Mensagem unificada server↔client:**
   - String literal `"Valor da custa deve ser maior que zero"` aparece em EXATAMENTE 2 arquivos: `services/contratos.js` (Task 3) + form client-side (Task 4)

6. **34/34 motor regression PASS:**
   - `npm run build` no submódulo executa `prebuild=test:regressao` automaticamente

7. **ABORT GATE pre-cleanup:**
   - `orfas_so_custas == 7` AND Sub-cleanup B preview retorna 3 rows com devedor_id=8

8. **Post-cleanup global:**
   - Query 7 = 0
   - Query 14 `so_custas_vazias_orfas = 0`
   - 5 SCs UAT PASS REAL
</verification>

<success_criteria>
**Phase 7.10.bug3 SHIPPED quando todos abaixo TRUE:**

- [ ] Tasks 1-5 PASS automated verify
- [ ] Tasks 1-5 PAUSAs #1-5 confirmadas pelo orchestrator
- [ ] Task 6 ABORT GATE PASS (`orfas_so_custas == 7 AND advair preview == 3`)
- [ ] Task 6 Sub-cleanup A: DELETE 7 PASS sem erro
- [ ] Task 6 Sub-cleanup B: DELETE 3 PASS sem erro
- [ ] Task 6 post-cleanup Query 7 = 0
- [ ] Task 6 SC1 PASS: Módulo Dívidas filtro "mend" 13 linhas sem R$ 0,00, soma R$ 2.821,81
- [ ] Task 6 SC2 PASS: filtro "advair" zero órfãs, legítimas preservadas
- [ ] Task 6 SC3 PASS: criar+deletar custa teste → cascade DELETE ativo
- [ ] Task 6 SC4 PASS: submit valor=0 bloqueado com mensagem exata
- [ ] Task 6 SC5 PASS: Query 14 final limpo
- [ ] `UAT 7.10.bug3 PASS` recebido
- [ ] Tag `v1.4-phase7.10.bug3` em commit pai chore bump SHA + push da tag
- [ ] D-01 motor INTOCADO dual gate confirmed (cumulative)
- [ ] Schema INTOCADO confirmed (zero ALTER TABLE)
- [ ] 7 órfãs MENDES + 3 órfãs advair removidas (10 → 0)
- [ ] excluirCusta cascade DELETE ativo em prod (futura sujeira pelo mesmo mecanismo prevenida)
- [ ] Anti-zero validation ativo em prod (futura criação de custa zerada bloqueada)
- [ ] 18 → 19 tags v1.4 ativas
</success_criteria>

<output>
After completion, create `.planning/phases/07.10-bug3-limpeza-custas-orfas-anti-zero/07.10-bug3-01-SUMMARY.md`

Conteúdo do SUMMARY (template):
- Phase outcome (SHIPPED, FAIL, PARTIAL)
- Commits criados (SHAs submódulo + pai)
- Tag pushed
- 5 SCs UAT outcome detalhado
- ABORT GATE outcome
- D-01 motor INTOCADO dual gate confirmation
- Memory feedbacks reforçados ou novos candidatos pós-execução
- Eventuais deviations registradas (espera-se ZERO desvios — phase trivial)
- Estado pós-Phase: 10 → 0 órfãs, mecânica raiz fixada, validation defesa em profundidade ativa
</output>
