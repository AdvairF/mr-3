---
phase: 07.10a-processos-judiciais-pagamentos-divida
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: false
requirements_addressed: []
source: "ROADMAP.md line 342 + 07.10a-CONTEXT.md (D-26..D-29 + escopo §2 + UAT §6 + Shield greps §7 + Shield 23 fixture §7) — Phase has NO REQ-IDs (tech debt v1.0 legacy migration, atômica)."

files_modified:
  - src/mr-3/mr-cobrancas/src/App.jsx                                              # MOD (Fix A — L8478 prop swap, 1 linha)
  - src/mr-3/mr-cobrancas/src/components/ProcessosJudiciais.jsx                    # MOD (Fix B — L254 filter adapter, ~3 linhas)
  - src/mr-3/mr-cobrancas/src/services/__tests__/processosJudiciaisFilter.test.js  # NEW (Shield 23 — equivalência filter, 3 it() blocks)
  - src/mr-3/mr-cobrancas/package.json                                             # MOD (test:regressao estende com processosJudiciaisFilter.test.js)

must_haves:
  truths:
    - "App.jsx case 'processos' (L8478) passa prop `pagamentos={allPagamentosDivida}` (Phase 7.3 fonte correta) — D-26"
    - "ProcessosJudiciais.jsx (L254) filtra pagamentos via lookup `divida_id ∈ dividas-do-devedor` (pattern novo Set) — D-27"
    - "Pagamento sem `devedor_id` no shape (`pagamentos_divida` só tem `divida_id`) é mapeado deterministicamente para o devedor correto via fullDev.dividas"
    - "Devedor sem dívidas registradas (fullDev?.dividas vazio) gera filter vazio (zero pagamentos contados) — não crasha"
    - "Pagamentos com `divida_id` que não pertencem ao devedor são filtrados out (cross-entity isolation comportamental)"
    - "Shield 22 grep callsite — App.jsx case 'processos' contém `pagamentos={allPagamentosDivida}` (1 match) E NÃO contém `pagamentos={allPagamentos}` legacy no escopo regional (0 matches)"
    - "Shield 23 equivalência filter — 3 it() blocks PASS: (a) novo === legacy sintético com devedor_id injetado; (b) devedor com 0 dívidas filtra vazio; (c) pagamentos de outro devedor filtrados out"
    - "Regressão 23/23 PASS (20 base 7.9 + 3 Shield 23 sub-its em processosJudiciaisFilter.test.js — cada `it()` conta 1)"
    - "Motor Art.354 INTOCADO em todos 3 arquivos (devedorCalc.js, pagamentos.js, dividas.js — D-01 estrito); dual gate C1 (pré-commit) + C2 (pós-commit) ambos OK"
    - "Drift histórico aceito por design (D-28) — pagamentos novos via UI legacy `pagamentos_parciais` (App.jsx L2537+) NÃO aparecem em ProcessosJudiciais até unificação de writes em phase própria; documentar visivelmente em SUMMARY"
  artifacts:
    - path: "src/mr-3/mr-cobrancas/src/App.jsx"
      provides: "case 'processos' com prop `pagamentos={allPagamentosDivida}` (D-26 callsite locked)"
      contains: "pagamentos={allPagamentosDivida}"
    - path: "src/mr-3/mr-cobrancas/src/components/ProcessosJudiciais.jsx"
      provides: "Filter via Set lookup divida_id ∈ dividas-do-devedor (D-27 pattern reutilizável)"
      contains: "dividaIdsDoDevedor"
    - path: "src/mr-3/mr-cobrancas/src/services/__tests__/processosJudiciaisFilter.test.js"
      provides: "Shield 23 equivalência filter — defesa H3-class (lição 7.8.2a) contra shape mismatch silencioso"
      contains: "Shield 23|equivalência|dividaIdsDoDevedor"
    - path: "src/mr-3/mr-cobrancas/package.json"
      provides: "test:regressao estende com processosJudiciaisFilter.test.js"
      contains: "processosJudiciaisFilter.test.js"
  key_links:
    - from: "src/mr-3/mr-cobrancas/src/App.jsx (L8478)"
      to: "ProcessosJudiciais component (prop `pagamentos`)"
      via: "Prop swap allPagamentos → allPagamentosDivida no case 'processos'"
      pattern: "pagamentos=\\{allPagamentosDivida\\}"
    - from: "src/mr-3/mr-cobrancas/src/components/ProcessosJudiciais.jsx (L254)"
      to: "fullDev.dividas (lookup) → pagamentos.filter(p => has(p.divida_id))"
      via: "Set lookup divida_id ∈ dividas-do-devedor (D-27 pattern reutilizável)"
      pattern: "dividaIdsDoDevedor.*has.*p\\.divida_id"
    - from: "src/mr-3/mr-cobrancas/src/services/__tests__/processosJudiciaisFilter.test.js"
      to: "Pattern novo (filter via Set lookup) === Pattern legacy sintético (devedor_id injetado)"
      via: "expect(novo.map(id)).toEqual(legacy.map(id)) — H3-class equivalência (lição 7.8.2a Shield 5)"
      pattern: "expect.*novo.*toEqual.*legacy"
---

<objective>
Migrar 1º consumidor (mais simples) de `allPagamentos` legacy v1.0 → `allPagamentosDivida` v1.4 (fonte correta Phase 7.3). Escopo atômico: 2 arquivos modificados (App.jsx callsite + ProcessosJudiciais filter adapter) + 1 arquivo novo (Shield 23 equivalência filter) + package.json estende test:regressao. **Risco BAIXO-MÉDIO** — 1 cálculo isolado, motor 100% intocado, shape mismatch defendido por shield de equivalência (lição H3 da 7.8.2a).

Purpose:
- **D-26** — App.jsx L8478 prop swap (1 linha): `pagamentos={allPagamentos}` → `pagamentos={allPagamentosDivida}` no case "processos".
- **D-27** — ProcessosJudiciais.jsx L254 filter adapter (~3 linhas): novo pattern `Set` lookup divida_id ∈ dividas-do-devedor (porque `pagamentos_divida` schema não tem `devedor_id` direto — só FK `divida_id`).
- **Shield 22 (grep callsite)** — defesa contra refactor futuro acidentalmente trazer legacy de volta. Grep positivo (1 match `allPagamentosDivida`) + grep regional negativo (0 matches `allPagamentos` no escopo case "processos").
- **Shield 23 (equivalência filter)** — H3-class defesa contra shape mismatch silencioso (lição `memory/feedback_shields_wrapper_must_test_equivalence.md`). Test file dedicado (`processosJudiciaisFilter.test.js`, decisão pra escopo limpo) com 3 it() blocks: (a) novo === legacy sintético; (b) 0 dívidas → []; (c) cross-devedor isolation.
- **D-29** — Motor `calcularSaldoDevedorAtualizado` lê `p.data_pagamento + p.valor` apenas. Shape `pagamentos_divida` (que tem ambos) é compatível. Motor distribui FIFO pelas dividas do devedor — mesmo comportamento.
- **D-28** — Drift histórico aceito: pagamentos novos via UI legacy "Registrar pagamento parcial" (App.jsx L2537+) escrevem em `pagamentos_parciais` e NÃO aparecem em ProcessosJudiciais até unificação de writes em phase própria. Trade-off documentado.

Output:
- `src/mr-3/mr-cobrancas/src/App.jsx` (MOD — 1 linha L8478)
- `src/mr-3/mr-cobrancas/src/components/ProcessosJudiciais.jsx` (MOD — L254 filter)
- `src/mr-3/mr-cobrancas/src/services/__tests__/processosJudiciaisFilter.test.js` (NEW — Shield 23 com 3 it() blocks)
- `src/mr-3/mr-cobrancas/package.json` (MOD — test:regressao estende)
- Commit único no submódulo: `feat(07.10a): ProcessosJudiciais lê pagamentos_divida (filter via dividas-do-devedor)`
- `.planning/phases/07.10a-processos-judiciais-pagamentos-divida/07.10a-01-SUMMARY.md` (NEW — left uncommitted in parent until Plan 02 Task 5)

Fora de escopo:
- Migrar Dashboard / Devedores / ModuloDividas (phases 7.10b/c/d)
- Cortar writes em `pagamentos_parciais` (App.jsx L2537+) — phase própria pós todos consumers migrados
- Schema cleanup `pagamentos_parciais` table drop — phase final hipotética 7.10z
- Renomear `pagamentos_parciais` em calculos.test.js fixtures (variável local de test, não consumidor real)
- Tocar motor Art.354 (D-01 invariante)
- Adicionar prop nova ou hook novo
- Bump pai / Push / Tag (todos diferidos pra Plan 02 + ações humanas pós-smoke)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07.10a-processos-judiciais-pagamentos-divida/07.10a-CONTEXT.md
@.planning/phases/07.9-custas-judiciais-crud/07.9-CONTEXT.md

<!-- Arquivos modificados por este plano (source-of-truth atual) -->
@src/mr-3/mr-cobrancas/src/App.jsx
@src/mr-3/mr-cobrancas/src/components/ProcessosJudiciais.jsx
@src/mr-3/mr-cobrancas/package.json

<!-- Motor protegido (D-01 estrito — ZERO edição em qualquer um) -->
@src/mr-3/mr-cobrancas/src/utils/devedorCalc.js
@src/mr-3/mr-cobrancas/src/services/pagamentos.js
@src/mr-3/mr-cobrancas/src/services/dividas.js

<!-- Analogs pra padrões de teste (vitest + fixtures sintéticas) -->
@src/mr-3/mr-cobrancas/src/services/__tests__/saldoAtualizadoCache.test.js
@src/mr-3/mr-cobrancas/src/services/__tests__/custasCache.test.js

<interfaces>
Key contracts the executor needs (extracted from codebase) — NO codebase exploration needed:

App.jsx state allPagamentosDivida (JÁ EXISTE desde Phase 7.3):
- L8325: `const [allPagamentosDivida, setAllPagamentosDivida] = useState([]);`
- L8333-8344 (carregarTudo): fetch `dbGet("pagamentos_divida")` em parallel + `setAllPagamentosDivida(...)` em L8348
- Estado já consumido por: ModuloContratos (case "contratos") + DetalheContrato (via prop) — Phase 7.3+

App.jsx case "processos" callsite ATUAL (L8475-8481 — ANTES — legacy):
```jsx
case "processos": return <ProcessosJudiciais
  devedores={devedores}
  credores={credores}
  pagamentos={allPagamentos}        // ← LEGACY (pagamentos_parciais via Phase 7.3 fetch)
  hoje={hoje_app}
  onVerDevedor={(id) => { setTab("devedores"); setTimeout(() => { window.dispatchEvent(new CustomEvent("mr_abrir_devedor", { detail: id })); }, 100); }}
/>;
```

ProcessosJudiciais.jsx props (L65) e filter atual (L251-258):
```jsx
export default function ProcessosJudiciais({ devedores = [], credores = [], pagamentos = [], hoje, onVerDevedor }) {

// Tab Dívidas - calcular saldo por devedor do polo passivo
const dividasData = devedoresProcesso.map(row => {
  const fullDev = devedores.find(d => String(d.id) === String(row.devedor_id));
  const pgtosDev = pagamentos.filter(p => String(p.devedor_id) === String(row.devedor_id));   // ← QUEBRA: shape pagamentos_divida sem devedor_id
  const saldo = fullDev ? calcularSaldoDevedorAtualizado(fullDev, pgtosDev, hj) : 0;
  const nDividas = (fullDev?.dividas || []).length;
  return { row, fullDev, saldo, nDividas };
});
```

pagamentos_divida shape (Supabase schema — locked):
- id UUID PK
- divida_id UUID NOT NULL REFERENCES dividas(id)        ← FK indireto via divida (NO devedor_id direto)
- data_pagamento DATE NOT NULL                          ← motor lê (D-29)
- valor NUMERIC(15,2) NOT NULL                          ← motor lê (D-29)
- observacao TEXT

Motor calcularSaldoDevedorAtualizado (devedorCalc.js:59 — READ-ONLY D-01):
- Lê APENAS `p.data_pagamento` + `p.valor`. Não lê `p.devedor_id` nem `p.divida_id` nem `p.id`.
- Shape pagamentos_divida (que tem ambos data_pagamento+valor) é compatível (D-29).
- Motor distribui FIFO pelas dividas do devedor — mesmo comportamento.
</interfaces>

<open_questions>
**Q1 — Local do Shield 23: arquivo dedicado vs append em existente?**

CONTEXT.md §5 SC5: "planner decide entre arquivo novo `processosJudiciaisFilter.test.js` OU inline em `pagamentos.js.test` se existir OU appended em `saldoAtualizadoCache.test.js`."

→ **DECISÃO PLANNER**: **Arquivo dedicado `processosJudiciaisFilter.test.js`**. Razões:
  (a) Escopo limpo — Shield 23 é sobre ProcessosJudiciais filter pattern, não cache SWR nem motor;
  (b) Discoverability — futuras phases 7.10b/c/d que reusarem o pattern Set lookup encontram o test pelo nome;
  (c) Custo idêntico — package.json `test:regressao` estende ambas opções com mesmo overhead vitest;
  (d) Padrão `custasCache.test.js` (7.9) é precedente — phase nova → arquivo novo.

**Q2 — Inline comment no filter pattern: PT-BR ou EN?**

→ **DECISÃO PLANNER**: **PT-BR consistente** com resto do componente.

**Q3 — Test contagem final esperada?**

Phase 7.9 baseline: 20/20 (11 calculos + 5 saldoAtualizadoCache + 4 custas). Shield 23 tem 3 sub-its.

→ **EXPECTATIVA**: **23/23 PASS** (20 base + 3 sub-its Shield 23). Cada `it()` conta 1 em vitest.
</open_questions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: App.jsx L8478 — prop swap allPagamentos → allPagamentosDivida (Fix A, D-26)</name>
  <read_first>
    - `.planning/phases/07.10a-processos-judiciais-pagamentos-divida/07.10a-CONTEXT.md` (§2 Fix A + D-26)
    - `src/mr-3/mr-cobrancas/src/App.jsx` (L8470-8485 — região do switch case `tab`; verificar contexto antes/depois)
    - `src/mr-3/mr-cobrancas/src/App.jsx` (L8320-8350 — confirmar `allPagamentosDivida` state JÁ existe e é populado por `carregarTudo`)
  </read_first>
  <files>src/mr-3/mr-cobrancas/src/App.jsx (MOD — 1 linha)</files>
  <action>
    Modificar `src/mr-3/mr-cobrancas/src/App.jsx` com 1 edit cirúrgico (1 linha alterada).

    **Edit — L8478 (case "processos" — prop pagamentos):**

    Linha ATUAL (L8478):
    ```jsx
    pagamentos={allPagamentos}
    ```

    Trocar por:
    ```jsx
    pagamentos={allPagamentosDivida}
    ```

    **Contexto antes/depois (L8475-8481) DEVE ficar assim após o edit (verbatim — D-26):**

    ```jsx
    case "processos": return <ProcessosJudiciais
      devedores={devedores}
      credores={credores}
      pagamentos={allPagamentosDivida}
      hoje={hoje_app}
      onVerDevedor={(id) => { setTab("devedores"); setTimeout(() => { window.dispatchEvent(new CustomEvent("mr_abrir_devedor", { detail: id })); }, 100); }}
    />;
    ```

    **ZERO outras edits em App.jsx**. NÃO mexer em outros `pagamentos={allPagamentos}` (case "dividas" / outros — todos ainda usam allPagamentos legacy; backlog 7.10b/c/d). NÃO renomear state. NÃO adicionar prop nova. NÃO alterar carregarTudo.

    **D-01 estrito**: este task NÃO toca motor (devedorCalc.js, pagamentos.js, dividas.js).
  </action>
  <verify>
    <automated>grep -nE 'pagamentos=\{allPagamentosDivida\}' src/mr-3/mr-cobrancas/src/App.jsx | wc -l | awk '{ if ($1 < 1) { print "FAIL: zero matches"; exit 1 } else { print "OK: " $1 " match(es)"; exit 0 } }'</automated>
  </verify>
  <acceptance_criteria>
    - **Shield 22 positivo (callsite)**: `grep -nE 'pagamentos=\{allPagamentosDivida\}' src/mr-3/mr-cobrancas/src/App.jsx` returns `>= 1` match (deve ser exatamente 1 — o case "processos"; outros consumers ainda em legacy nesta phase)
    - **Shield 22 negativo regional (case "processos" + 10 linhas seguintes NÃO têm legacy)**: `awk '/case "processos":/{flag=1; n=0} flag{print; n++; if (n>10) flag=0}' src/mr-3/mr-cobrancas/src/App.jsx | grep -F 'pagamentos={allPagamentos}' | wc -l` returns `0` (zero matches no escopo regional)
    - **State preservado**: `grep -nE 'const \[allPagamentosDivida, setAllPagamentosDivida\]' src/mr-3/mr-cobrancas/src/App.jsx` returns `>= 1` match
    - **carregarTudo preservado**: `grep -nE 'setAllPagamentosDivida\(' src/mr-3/mr-cobrancas/src/App.jsx` returns `>= 1` match
    - **Outros consumers ainda em legacy** (sanity — confirma scope atomicity): `grep -nE 'pagamentos=\{allPagamentos\}' src/mr-3/mr-cobrancas/src/App.jsx | wc -l` returns `>= 1` (case "dividas" / Dashboard / Devedores — backlog 7.10b/c/d)
    - **D-01 estrito**: `cd src/mr-3 && git diff -- mr-cobrancas/src/utils/devedorCalc.js mr-cobrancas/src/services/pagamentos.js mr-cobrancas/src/services/dividas.js` outputs nothing
  </acceptance_criteria>
  <done>
    App.jsx L8478 case "processos" passa `pagamentos={allPagamentosDivida}` (D-26 locked). Outros callsites de `pagamentos={allPagamentos}` preservados (atomicidade — backlog 7.10b/c/d). State e carregarTudo intocados. Motor 100% intocado.
  </done>
</task>

<task type="auto">
  <name>Task 2: ProcessosJudiciais.jsx L254 — filter adapter via Set lookup divida_id ∈ dividas-do-devedor (Fix B, D-27)</name>
  <read_first>
    - `.planning/phases/07.10a-processos-judiciais-pagamentos-divida/07.10a-CONTEXT.md` (§2 Fix B + D-27 + D-29 motor compat)
    - `src/mr-3/mr-cobrancas/src/components/ProcessosJudiciais.jsx` (L240-260 — entender shape de `devedoresProcesso[i]` + uso de `fullDev.dividas` + assinatura `calcularSaldoDevedorAtualizado`)
    - `src/mr-3/mr-cobrancas/src/utils/devedorCalc.js:59` (motor — confirmar lê apenas p.data_pagamento + p.valor; D-29)
  </read_first>
  <files>src/mr-3/mr-cobrancas/src/components/ProcessosJudiciais.jsx (MOD — L254 region)</files>
  <action>
    Modificar `src/mr-3/mr-cobrancas/src/components/ProcessosJudiciais.jsx` com 1 edit cirúrgico (substitui 1 linha por 3 linhas + 1 comment).

    **Edit — L251-258 region (Tab Dívidas filter por devedor — D-27 pattern Set lookup):**

    Bloco ATUAL (L251-258 — verbatim):
    ```jsx
    // Tab Dívidas - calcular saldo por devedor do polo passivo
    const dividasData = devedoresProcesso.map(row => {
      const fullDev = devedores.find(d => String(d.id) === String(row.devedor_id));
      const pgtosDev = pagamentos.filter(p => String(p.devedor_id) === String(row.devedor_id));
      const saldo = fullDev ? calcularSaldoDevedorAtualizado(fullDev, pgtosDev, hj) : 0;
      const nDividas = (fullDev?.dividas || []).length;
      return { row, fullDev, saldo, nDividas };
    });
    ```

    Trocar por (verbatim):
    ```jsx
    // Tab Dívidas - calcular saldo por devedor do polo passivo
    const dividasData = devedoresProcesso.map(row => {
      const fullDev = devedores.find(d => String(d.id) === String(row.devedor_id));
      // Phase 7.10a — fonte trocada pra pagamentos_divida (sem devedor_id direto). Filter via lookup divida_id ∈ dividas-do-devedor.
      const dividaIdsDoDevedor = new Set((fullDev?.dividas || []).map(d => String(d.id)));
      const pgtosDev = pagamentos.filter(p => dividaIdsDoDevedor.has(String(p.divida_id)));
      const saldo = fullDev ? calcularSaldoDevedorAtualizado(fullDev, pgtosDev, hj) : 0;
      const nDividas = (fullDev?.dividas || []).length;
      return { row, fullDev, saldo, nDividas };
    });
    ```

    **Diferenças exatas:**
    - Linha removida: `const pgtosDev = pagamentos.filter(p => String(p.devedor_id) === String(row.devedor_id));`
    - Linhas adicionadas (3): comment PT-BR + `const dividaIdsDoDevedor = new Set(...)` + `const pgtosDev = pagamentos.filter(p => dividaIdsDoDevedor.has(String(p.divida_id)));`

    **Por que Set (D-27 pattern reutilizável):**
    - O(1) lookup amortizado por pagamento, vs O(N) array scan; perceptível com N grandes (advogado com 200+ dívidas).
    - Padrão limpo, deterministicamente correto: pagamento pertence ao devedor sse `p.divida_id ∈ dividas-do-devedor`.
    - Reutilizável em phases futuras 7.10b/c/d se houver consumer com lógica análoga.

    **Por que NÃO afeta motor (D-29):**
    - Motor `calcularSaldoDevedorAtualizado` lê apenas `p.data_pagamento + p.valor` — shape `pagamentos_divida` é compatível.
    - O filter não altera os campos do pagamento, só seleciona QUAIS pagamentos vão pro motor.
    - Motor distribui FIFO entre as dividas do devedor — mesmo comportamento.

    **ZERO outras edits em ProcessosJudiciais.jsx**. NÃO mexer em props da função (L65). NÃO mexer em outras seções (Tab info / polo / andamentos / documentos). NÃO importar nada novo.

    **D-01 estrito**: este task NÃO toca motor (devedorCalc.js, pagamentos.js, dividas.js).
  </action>
  <verify>
    <automated>grep -nE 'dividaIdsDoDevedor' src/mr-3/mr-cobrancas/src/components/ProcessosJudiciais.jsx | wc -l | awk '{ if ($1 < 2) { print "FAIL: expected >=2 occurrences (declaration + usage), got " $1; exit 1 } else { print "OK: " $1 " occurrences"; exit 0 } }'</automated>
  </verify>
  <acceptance_criteria>
    - **Filter pattern presente**: `grep -nE 'dividaIdsDoDevedor' src/mr-3/mr-cobrancas/src/components/ProcessosJudiciais.jsx` returns `>= 2` matches (declaration via `new Set` + usage via `.has(...)`)
    - **Set construction**: `grep -nE 'new Set\(\(fullDev\?\.dividas \|\| \[\]\)\.map\(d => String\(d\.id\)\)\)' src/mr-3/mr-cobrancas/src/components/ProcessosJudiciais.jsx` returns `>= 1` match
    - **Filter via has(divida_id)**: `grep -nE 'pagamentos\.filter\(p => dividaIdsDoDevedor\.has\(String\(p\.divida_id\)\)\)' src/mr-3/mr-cobrancas/src/components/ProcessosJudiciais.jsx` returns `>= 1` match
    - **Comment PT-BR**: `grep -nF 'Phase 7.10a' src/mr-3/mr-cobrancas/src/components/ProcessosJudiciais.jsx` returns `>= 1` match
    - **Padrão legacy removido (negativo)**: `grep -nE 'p\.devedor_id.*===.*row\.devedor_id' src/mr-3/mr-cobrancas/src/components/ProcessosJudiciais.jsx | wc -l` returns `0` matches
    - **Props inalterados (sanity)**: `grep -nE 'export default function ProcessosJudiciais' src/mr-3/mr-cobrancas/src/components/ProcessosJudiciais.jsx` returns `1` match (assinatura intocada)
    - **D-01 estrito**: `cd src/mr-3 && git diff -- mr-cobrancas/src/utils/devedorCalc.js mr-cobrancas/src/services/pagamentos.js mr-cobrancas/src/services/dividas.js` outputs nothing
  </acceptance_criteria>
  <done>
    ProcessosJudiciais.jsx L254 filter substituído por pattern Set lookup (D-27); comment PT-BR inline explica fonte trocada; props/outras seções intocadas; padrão legacy `p.devedor_id === row.devedor_id` removido; motor 100% intocado.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Criar processosJudiciaisFilter.test.js — Shield 23 equivalência filter (3 it() blocks) + estender package.json test:regressao</name>
  <read_first>
    - `.planning/phases/07.10a-processos-judiciais-pagamentos-divida/07.10a-CONTEXT.md` (§5 SC5 + §7 fixture Shield 23)
    - `src/mr-3/mr-cobrancas/src/services/__tests__/custasCache.test.js` (analog estilo vitest — Phase 7.9 precedente)
    - `src/mr-3/mr-cobrancas/src/services/__tests__/saldoAtualizadoCache.test.js` (analog estilo vitest — Phase 7.8.2a Shield 5 H3-class)
    - `src/mr-3/mr-cobrancas/package.json` (script `test:regressao` atual — após 7.9 inclui calculos.test.js + saldoAtualizadoCache.test.js + custasCache.test.js)
    - `memory/feedback_shields_wrapper_must_test_equivalence.md` (lição H3 7.8.2a — fundamentação Shield 23)
  </read_first>
  <behavior>
    - **Shield 23 (it #1) — Equivalência core**: Fixture devedor com 2 dívidas (`div-A`, `div-B`) + 3 pagamentos (1 em A + 1 em B + 1 spread em A). Filter NEW (Set lookup) === Filter LEGACY sintético (com `devedor_id` injetado a posteriori). `expect(novo.map(p => p.id)).toEqual(legacy.map(p => p.id))`. Asserção também `expect(novo).toHaveLength(3)` — todos pagamentos do devedor são contabilizados. Defesa H3-class contra shape mismatch silencioso (lição 7.8.2a Shield 5 — equivalência > propriedade observável).
    - **Shield 23b (it #2) — Boundary: devedor com 0 dívidas**: Fixture `dividas: []` + 1 pagamento qualquer. `expect(result).toHaveLength(0)`. Garante que filter não crasha e retorna [] determinísticamente quando devedor não tem dívidas registradas (caso edge real: novo devedor sem dívidas ainda criadas).
    - **Shield 23c (it #3) — Cross-entity isolation**: Fixture devedor com 1 dívida (`div-A`) + 2 pagamentos (um em `div-A`, outro em `div-OUTRO_DEVEDOR`). `expect(result.map(p => p.id)).toEqual(["p1"])`. Garante que pagamentos de outros devedores são filtrados out — defesa contra cross-entity leakage que poderia inflar saldo erroneamente.
  </behavior>
  <files>
    - src/mr-3/mr-cobrancas/src/services/__tests__/processosJudiciaisFilter.test.js (NOVO)
    - src/mr-3/mr-cobrancas/package.json (MOD — script test:regressao estende)
  </files>
  <action>
    **Parte A — Criar `src/mr-3/mr-cobrancas/src/services/__tests__/processosJudiciaisFilter.test.js`** (verbatim per CONTEXT.md §7 fixture):

    ```javascript
    /**
     * Phase 7.10a — Shield 23 equivalência filter (ProcessosJudiciais)
     *
     * Defesa H3-class (lição 7.8.2a Shield 5 — `feedback_shields_wrapper_must_test_equivalence.md`):
     * o filter pattern novo (Set lookup divida_id ∈ dividas-do-devedor — D-27) deve produzir
     * EXATAMENTE os mesmos resultados que o filter legacy sintético (com `devedor_id` injetado).
     * Equivalência > propriedade observável.
     *
     * 3 it() blocks:
     *   Shield 23  — Equivalência core: novo === legacy sintético; 3 pagamentos contabilizados
     *   Shield 23b — Boundary: devedor com 0 dívidas → filter retorna []
     *   Shield 23c — Cross-entity isolation: pagamentos de outro devedor filtrados out
     *
     * Roda junto da regressão TJGO + saldoAtualizadoCache + custasCache via `npm run test:regressao`
     * (esperado: 23/23 — 11 calculos + 5 saldoAtualizadoCache + 4 custas + 3 processosJudiciaisFilter).
     */

    import { describe, it, expect } from 'vitest';

    describe('ProcessosJudiciais filter — pagamentos_divida (Phase 7.10a)', () => {
      it('Shield 23 — equivalência: filter via Set lookup === filter legacy sintético', () => {
        const devedor = { id: "dev-1", dividas: [{ id: "div-A" }, { id: "div-B" }] };
        const pagamentos = [
          { id: "p1", divida_id: "div-A", data_pagamento: "2026-01-01", valor: 100 },
          { id: "p2", divida_id: "div-B", data_pagamento: "2026-02-01", valor: 200 },
          { id: "p3", divida_id: "div-A", data_pagamento: "2026-03-01", valor: 50 },
        ];

        // Filter NEW (pattern Phase 7.10a): Set lookup divida_id ∈ dividas-do-devedor
        const dividaIdsDoDevedor = new Set(devedor.dividas.map(d => String(d.id)));
        const novo = pagamentos.filter(p => dividaIdsDoDevedor.has(String(p.divida_id)));

        // Filter LEGACY equivalente sintético (simula shape antigo com devedor_id injetado)
        const sintetico = pagamentos.map(p => ({ ...p, devedor_id: devedor.id }));
        const legacy = sintetico.filter(p => p.devedor_id === devedor.id);

        // Equivalência: filters retornam exatamente os mesmos pagamentos
        expect(novo.map(p => p.id)).toEqual(legacy.map(p => p.id));
        expect(novo).toHaveLength(3);
      });

      it('Shield 23b — devedor com 0 dívidas: filter retorna []', () => {
        const devedor = { id: "dev-2", dividas: [] };
        const pagamentos = [
          { id: "p1", divida_id: "div-X", data_pagamento: "2026-01-01", valor: 100 },
        ];
        const dividaIdsDoDevedor = new Set(devedor.dividas.map(d => String(d.id)));
        const result = pagamentos.filter(p => dividaIdsDoDevedor.has(String(p.divida_id)));
        expect(result).toHaveLength(0);
      });

      it('Shield 23c — pagamentos com divida_id não pertencente: filtrados out', () => {
        const devedor = { id: "dev-3", dividas: [{ id: "div-A" }] };
        const pagamentos = [
          { id: "p1", divida_id: "div-A", data_pagamento: "2026-01-01", valor: 100 },
          { id: "p2", divida_id: "div-OUTRO_DEVEDOR", data_pagamento: "2026-02-01", valor: 200 },
        ];
        const dividaIdsDoDevedor = new Set(devedor.dividas.map(d => String(d.id)));
        const result = pagamentos.filter(p => dividaIdsDoDevedor.has(String(p.divida_id)));
        expect(result.map(p => p.id)).toEqual(["p1"]);
      });
    });
    ```

    **Parte B — Atualizar `src/mr-3/mr-cobrancas/package.json`** (script `test:regressao` estende com novo arquivo):

    Localizar script atual (post-Phase 7.9):
    ```json
    "test:regressao": "vitest run src/services/__tests__/calculos.test.js src/services/__tests__/saldoAtualizadoCache.test.js src/services/__tests__/custasCache.test.js"
    ```

    Substituir por (acrescenta `processosJudiciaisFilter.test.js` no FIM):
    ```json
    "test:regressao": "vitest run src/services/__tests__/calculos.test.js src/services/__tests__/saldoAtualizadoCache.test.js src/services/__tests__/custasCache.test.js src/services/__tests__/processosJudiciaisFilter.test.js"
    ```

    **NÃO mudar** `name`, `version`, `dependencies`, outros scripts (`dev`, `build`, `test`, `prebuild`).

    **Por que arquivo dedicado (Q1 decisão planner)**:
    - Escopo limpo (Shield 23 é sobre filter pattern ProcessosJudiciais, não cache nem motor)
    - Discoverability (futuras 7.10b/c/d encontram pelo nome)
    - Padrão `custasCache.test.js` (7.9) é precedente

    **Por que 3 it() blocks**:
    - it #1 (equivalência core) é o gold test H3-class — pega shape mismatch silencioso
    - it #2 (boundary 0 dívidas) defende contra crash em edge case real (novo devedor)
    - it #3 (cross-entity isolation) defende contra saldo inflado por pagamentos cross-devedor

    **Nota sobre contagem final**: esperado `23 passed` (20 base + 3 sub-its Shield 23). Cada `it()` conta 1. Use `npm run test:regressao` pós-implementação pra confirmar exato. Gate A aceita `>=21 passed` mas SUMMARY documenta número exato.
  </action>
  <verify>
    <automated>cd src/mr-3/mr-cobrancas &amp;&amp; npm run test:regressao 2>&amp;1 | tee /tmp/07.10a-01-t3-test.log | tail -30 &amp;&amp; grep -qE "Tests +(2[1-9]|[3-9][0-9]) passed|2[1-9] passed|[3-9][0-9] passed" /tmp/07.10a-01-t3-test.log</automated>
  </verify>
  <acceptance_criteria>
    - **Test file existe**: `ls src/mr-3/mr-cobrancas/src/services/__tests__/processosJudiciaisFilter.test.js` returns 0 (file present)
    - **3 it() blocks**: `grep -cE "^  it\(" src/mr-3/mr-cobrancas/src/services/__tests__/processosJudiciaisFilter.test.js` returns `3`
    - **Shield 23 marker**: `grep -cE "Shield 23" src/mr-3/mr-cobrancas/src/services/__tests__/processosJudiciaisFilter.test.js` returns `>= 3` (1 menção em cada it())
    - **dividaIdsDoDevedor presente**: `grep -cE "dividaIdsDoDevedor" src/mr-3/mr-cobrancas/src/services/__tests__/processosJudiciaisFilter.test.js` returns `>= 3` (declaração em cada it())
    - **equivalência core (it #1)**: `grep -cE "expect\(novo\.map\(p => p\.id\)\)\.toEqual\(legacy\.map\(p => p\.id\)\)" src/mr-3/mr-cobrancas/src/services/__tests__/processosJudiciaisFilter.test.js` returns `>= 1`
    - **package.json script atualizado**: `grep -c "processosJudiciaisFilter.test.js" src/mr-3/mr-cobrancas/package.json` returns `>= 1`
    - **Regressão**: `cd src/mr-3/mr-cobrancas && npm run test:regressao` exits 0 com pelo menos `21 passed` (esperado exato: `23 passed` — 20 base + 3 Shield 23 sub-its)
    - **D-01 estrito**: `cd src/mr-3 && git diff -- mr-cobrancas/src/utils/devedorCalc.js mr-cobrancas/src/services/pagamentos.js mr-cobrancas/src/services/dividas.js` outputs nothing
  </acceptance_criteria>
  <done>
    processosJudiciaisFilter.test.js criado com 3 it() blocks (Shield 23 / 23b / 23c); package.json `test:regressao` estendido; regressão `>=21/21 PASS` (esperado 23/23). H3-class equivalência defesa ativada.
  </done>
</task>

<task type="auto">
  <name>Task 4: Gates consolidados (Build, Regressão, D-01 dual gate, Shield 22, Shield 23) + commit ATÔMICO no submódulo</name>
  <read_first>
    - `.planning/phases/07.10a-processos-judiciais-pagamentos-divida/07.10a-CONTEXT.md` (§5 SC + §7 Shield greps)
    - `memory/feedback_gate_motor_grep_use_name_only.md` (Gate C2 usa `--name-only`, não `--stat`)
    - `memory/feedback_db_integration_gate_missing.md` (lições 7.9 — schema completeness, helper-first, single-source-of-truth)
    - Todos os arquivos modificados nas Tasks 1-3
  </read_first>
  <files>
    - src/mr-3 (commit dentro do submódulo — 4 files)
  </files>
  <action>
    **Executar gates consolidados (D-01 dual gate C1+C2 + Shield 22 + Shield 23 + Gate F status — pré e pós commit):**

    **Gate A — Build exit 0:**
    ```bash
    cd src/mr-3/mr-cobrancas && npm run build
    ```
    Esperado: exit 0.

    **Gate B — Regressão >=21/21 PASS (esperado exato 23/23):**
    ```bash
    cd src/mr-3/mr-cobrancas && npm run test:regressao
    ```
    Esperado: "Tests 23 passed" (20 base 7.9 + 3 sub-its Shield 23). Aceito `>=21 passed` se vitest reportar diferente por algum split.

    **Gate C1 (pré-commit) — D-01 estrito — motor diff VAZIO em todos 3 arquivos:**
    ```bash
    MOTOR_DIFF=$(cd src/mr-3 && git diff -- mr-cobrancas/src/utils/devedorCalc.js mr-cobrancas/src/services/pagamentos.js mr-cobrancas/src/services/dividas.js)
    [ -z "$MOTOR_DIFF" ] && echo "Gate C1: OK (motor 100% intocado)" || echo "Gate C1: FAIL (motor tocado — scope creep detectado)"
    ```
    Esperado: `OK`. Se `FAIL` → ABORT, NÃO COMMITAR. Identificar arquivo modificado e reverter.

    **Gate Shield22 — grep callsite App.jsx (positivo + negativo regional):**
    ```bash
    # Positivo — exatamente 1 match no callsite case "processos"
    POS=$(grep -nE 'pagamentos=\{allPagamentosDivida\}' src/mr-3/mr-cobrancas/src/App.jsx | wc -l)
    [ "$POS" -ge 1 ] && echo "Shield22 positivo: OK ($POS match)" || echo "Shield22 positivo: FAIL ($POS match, esperado >=1)"

    # Negativo regional — case "processos" + 10 linhas seguintes NÃO têm legacy
    NEG=$(awk '/case "processos":/{flag=1; n=0} flag{print; n++; if (n>10) flag=0}' src/mr-3/mr-cobrancas/src/App.jsx | grep -F 'pagamentos={allPagamentos}' | wc -l)
    [ "$NEG" -eq 0 ] && echo "Shield22 negativo: OK (0 matches no escopo regional)" || echo "Shield22 negativo: FAIL ($NEG matches — legacy ainda no case 'processos')"

    # Filter pattern em ProcessosJudiciais
    FILT=$(grep -nE 'dividaIdsDoDevedor' src/mr-3/mr-cobrancas/src/components/ProcessosJudiciais.jsx | wc -l)
    [ "$FILT" -ge 2 ] && echo "Shield22 filter: OK ($FILT occurrences)" || echo "Shield22 filter: FAIL ($FILT, esperado >=2)"
    ```
    Esperado: 3 OKs. Se qualquer FAIL → ABORT.

    **Gate Shield23 — test file existe + 3 it() blocks pass:**
    ```bash
    test -f src/mr-3/mr-cobrancas/src/services/__tests__/processosJudiciaisFilter.test.js && echo "Shield23 file: OK" || echo "Shield23 file: FAIL"
    ITS=$(grep -cE "^  it\(" src/mr-3/mr-cobrancas/src/services/__tests__/processosJudiciaisFilter.test.js)
    [ "$ITS" -eq 3 ] && echo "Shield23 it() count: OK (3)" || echo "Shield23 it() count: FAIL ($ITS, esperado 3)"
    # Validação de PASS — Gate B já rodou test:regressao incluindo este arquivo
    ```
    Esperado: 2 OKs. Test PASS coberto pelo Gate B.

    **Gate F — git status limpo (apenas 4 arquivos previstos em files_modified):**
    ```bash
    cd src/mr-3 && git status --short
    ```
    Esperado: apenas estes modificados/novos:
    - `M  mr-cobrancas/src/App.jsx`
    - `M  mr-cobrancas/src/components/ProcessosJudiciais.jsx`
    - `??  mr-cobrancas/src/services/__tests__/processosJudiciaisFilter.test.js` (novo)
    - `M  mr-cobrancas/package.json`

    **Se TODOS os gates pré-commit (A, B, C1, Shield22, Shield23, F) passarem** — criar commit ÚNICO no submódulo:

    ```bash
    cd src/mr-3 && git add \
      mr-cobrancas/src/App.jsx \
      mr-cobrancas/src/components/ProcessosJudiciais.jsx \
      mr-cobrancas/src/services/__tests__/processosJudiciaisFilter.test.js \
      mr-cobrancas/package.json

    cd src/mr-3 && git commit -m "$(cat <<'EOF'
feat(07.10a): ProcessosJudiciais lê pagamentos_divida (filter via dividas-do-devedor)

Migra 1º consumidor (mais simples) de allPagamentos legacy v1.0 →
allPagamentosDivida v1.4 (Phase 7.3 fonte correta). Escopo atômico:
- D-26: App.jsx L8478 prop swap (1 linha) no case "processos"
- D-27: ProcessosJudiciais.jsx L254 filter via Set lookup
        divida_id ∈ dividas-do-devedor (pattern novo — pagamentos_divida
        sem devedor_id direto)
- Shield 22: grep callsite (positivo + negativo regional)
- Shield 23: test file dedicado (3 it() blocks — equivalência core +
            boundary 0 dívidas + cross-entity isolation), defesa
            H3-class (lição 7.8.2a)

D-01 estrito: motor 100% intocado (devedorCalc.js + pagamentos.js +
              dividas.js). Dual gate C1 + C2 OK.
D-28: drift histórico aceito por design — pagamentos novos via UI
      legacy "Registrar pagamento parcial" ainda escrevem em
      pagamentos_parciais; phase própria de cleanup futura.
D-29: motor calcularSaldoDevedorAtualizado lê apenas data_pagamento +
      valor — shape pagamentos_divida compatível.

Regressão >=21/21 PASS (esperado exato: 23/23 — 20 base 7.9 + 3 sub-its
Shield 23). Backlog: 7.10b/c/d migram demais consumers (Devedores,
ModuloDividas, Dashboard).
EOF
)"
    ```

    **Gate C2 (pós-commit) — D-01 estrito — motor NÃO no commit (todos 3 arquivos; usa `--name-only`, NÃO `--stat`, lição 7.8.2a 608c124):**
    ```bash
    cd src/mr-3 && git log -1 --format="" --name-only | grep -qE "utils/devedorCalc\.js|services/pagamentos\.js|services/dividas\.js" && echo "Gate C2: FAIL (motor no commit)" || echo "Gate C2: OK"
    ```
    Esperado: `OK`. Se `FAIL` → scope creep → `git reset HEAD~1` + investigar + recommit limpo.

    **Reportar SHA do commit criado:**
    ```bash
    cd src/mr-3 && git log -1 --oneline
    cd src/mr-3 && git diff HEAD~1 HEAD --stat
    ```
    Esperado: 4 files changed (App.jsx + ProcessosJudiciais.jsx + processosJudiciaisFilter.test.js + package.json).

    **ZERO `git push` nesta task** (push é ação humana pós-PAUSA #2 em Plan 02).
    **ZERO bump de submódulo no pai nesta task** (Plan 02 Task 3).
    **ZERO criação de tag `v1.4-phase7.10a`** (explicitamente fora de escopo — ação separada pós-smoke prod).

    **Commit message convention (CONTEXT.md §8 task 7.10a-01):**
    Mensagem header DEVE começar exatamente com:
    `feat(07.10a): ProcessosJudiciais lê pagamentos_divida (filter via dividas-do-devedor)`

    **Se ANY Gate FAIL antes do commit**: HALT immediately. Return failure report. Do not commit. Do not fix-forward.
  </action>
  <verify>
    <automated>cd src/mr-3 &amp;&amp; git log -1 --oneline | grep -qE "feat\(07\.10a\)" &amp;&amp; cd src/mr-3 &amp;&amp; git log -1 --format="" --name-only | grep -qE "utils/devedorCalc\.js|services/pagamentos\.js|services/dividas\.js" &amp;&amp; { echo "Gate C2 FAIL — motor no commit"; exit 1; } || echo "Gate C2 OK"</automated>
  </verify>
  <acceptance_criteria>
    - **Gate A** — `cd src/mr-3/mr-cobrancas && npm run build` exits 0
    - **Gate B** — `cd src/mr-3/mr-cobrancas && npm run test:regressao` exits 0 com pelo menos `21 passed` (esperado exato `23 passed`)
    - **Gate C1 (pré-commit) — D-01 estrito** — `cd src/mr-3 && git diff -- mr-cobrancas/src/utils/devedorCalc.js mr-cobrancas/src/services/pagamentos.js mr-cobrancas/src/services/dividas.js` outputs VAZIO em todos 3 motor files. ANTES de `git add`.
    - **Gate C2 (pós-commit) — D-01 estrito** — `cd src/mr-3 && git log -1 --format="" --name-only | grep -qE "utils/devedorCalc\.js|services/pagamentos\.js|services/dividas\.js"` returns NO matches (`grep -q ... && exit FAIL || exit OK` outputs `OK`). Usa `--name-only` (NÃO `--stat`).
    - **Gate Shield22** — 3 sub-checks OK: positivo (`grep allPagamentosDivida` >= 1) + negativo regional (`awk + grep allPagamentos` no case "processos" + 10 linhas = 0) + filter (`dividaIdsDoDevedor` >= 2)
    - **Gate Shield23** — test file existe + `grep -cE "^  it\(" ... ` returns `3` + Gate B já validou PASS
    - **Gate F** — `cd src/mr-3 && git status --short` post-commit shows nothing (working tree clean)
    - **Commit message** — `cd src/mr-3 && git log -1 --oneline` shows commit começando com `feat(07.10a): ProcessosJudiciais lê pagamentos_divida (filter via dividas-do-devedor)`
    - **4 files no commit** — `cd src/mr-3 && git diff HEAD~1 HEAD --name-only | wc -l` returns `4` (App.jsx + ProcessosJudiciais.jsx + processosJudiciaisFilter.test.js + package.json)
    - ZERO `git push` executado neste task
    - ZERO bump de submódulo no pai executado (diferido Plan 02)
    - ZERO criação de tag `v1.4-phase7.10a` neste task
  </acceptance_criteria>
  <done>
    Todos gates (A, B, C1, C2, Shield22, Shield23, F) verdes; commit único atômico no submódulo com mensagem `feat(07.10a)`; 4 files (App.jsx + ProcessosJudiciais.jsx + new test + package.json); motor Art.354 intocado em ambos C1 (working tree) e C2 (commit contents) — todos 3 arquivos; nenhum push/bump/tag executado.
  </done>
</task>

<task type="auto">
  <name>Task 5: Criar 07.10a-01-SUMMARY.md (left uncommitted — Plan 02 Task 5 commits)</name>
  <read_first>
    - `.planning/phases/07.10a-processos-judiciais-pagamentos-divida/07.10a-CONTEXT.md` (D-28 documentation requirement)
    - Outputs de Tasks 1-4 (SHAs, gates, evidências)
  </read_first>
  <files>.planning/phases/07.10a-processos-judiciais-pagamentos-divida/07.10a-01-SUMMARY.md (NOVO — left uncommitted in parent)</files>
  <action>
    Criar `.planning/phases/07.10a-processos-judiciais-pagamentos-divida/07.10a-01-SUMMARY.md` com template:

    ```markdown
    # Phase 7.10a — Plan 01 SUMMARY — ProcessosJudiciais lê pagamentos_divida

    **Status**: Commited no submódulo, pai NÃO bumpado, push NÃO executado.
    **Data**: <YYYY-MM-DD>
    **SHA submódulo**: `<sha7>` — `feat(07.10a): ProcessosJudiciais lê pagamentos_divida (filter via dividas-do-devedor)`

    ## Arquivos no commit (4 files)

    Novos:
    - `src/mr-3/mr-cobrancas/src/services/__tests__/processosJudiciaisFilter.test.js` (~50 lines, 3 it() blocks Shield 23)

    Modificados:
    - `src/mr-3/mr-cobrancas/src/App.jsx` (+1/-1) — L8478 prop swap (D-26)
    - `src/mr-3/mr-cobrancas/src/components/ProcessosJudiciais.jsx` (+3/-1) — L254 filter adapter Set lookup (D-27)
    - `src/mr-3/mr-cobrancas/package.json` (+1/-1) — test:regressao estende com novo test

    ## D-01 ESTRITO — Phase 7.10a

    Motor Art.354 INTOCADO em todos os 3 arquivos: `devedorCalc.js`, `pagamentos.js`, `dividas.js`. ZERO mudança no motor.

    Phase 7.10a tocou apenas componentes consumidores (`App.jsx` callsite + `ProcessosJudiciais.jsx` filter pattern) + test novo + package.json. Motor 100% intocado por construção.

    Verificado por dual gate:
    - Gate C1 (pré-commit, working tree): `git diff` em devedorCalc.js + pagamentos.js + dividas.js → VAZIO
    - Gate C2 (pós-commit, commit contents): `git log -1 --format="" --name-only` em commit → NÃO casa em motor files (usa `--name-only`, NÃO `--stat` — lição 608c124 7.8.2a)

    ## Gates (Task 4)

    - Gate A — Build: **exit 0**
    - Gate B — Regressão: **<N>/<N> PASS** (esperado exato: 23/23 — 11 calculos + 5 saldoAtualizadoCache + 4 custas + 3 processosJudiciaisFilter Shield 23)
    - Gate C1 (pré-commit) — D-01 estrito: **OK** — diff vazio em todos 3 motor files
    - Gate C2 (pós-commit) — D-01 estrito: **OK** — todos 3 motor files NÃO aparecem em `git log -1 --name-only`
    - Gate Shield22 — grep callsite: **OK**
      - Positivo: `pagamentos={allPagamentosDivida}` em App.jsx case "processos" (1 match)
      - Negativo regional: `pagamentos={allPagamentos}` no case "processos" + 10 linhas seguintes (0 matches)
      - Filter pattern: `dividaIdsDoDevedor` em ProcessosJudiciais.jsx (>=2 occurrences)
    - Gate Shield23 — test file: **OK**
      - Arquivo `processosJudiciaisFilter.test.js` existe
      - 3 it() blocks (Shield 23 / 23b / 23c) — todos PASS via Gate B
    - Gate F — git status post-commit: **clean** (working tree limpo)

    ## Shields de teste novos (processosJudiciaisFilter.test.js)

    - Shield 23 — Equivalência core (filter via Set lookup === filter legacy sintético): **PASS**
    - Shield 23b — Boundary devedor com 0 dívidas: **PASS**
    - Shield 23c — Cross-entity isolation (pagamentos de outro devedor filtrados out): **PASS**

    Defesa H3-class (lição `memory/feedback_shields_wrapper_must_test_equivalence.md` da 7.8.2a — equivalência > propriedade observável).

    ## D-26..D-29 invariantes verificadas

    - **D-26** (App.jsx callsite locked): VERIFIED — Shield 22 positivo (1 match `allPagamentosDivida` no case "processos") + negativo regional (0 matches `allPagamentos` no escopo)
    - **D-27** (filter por divida_id, não devedor_id): VERIFIED — Shield 22 filter pattern (`dividaIdsDoDevedor` em ProcessosJudiciais.jsx >=2 occurrences) + Shield 23 equivalência (3 it() PASS)
    - **D-28** (drift histórico aceito): DOCUMENTED — ver seção "Drift histórico" abaixo
    - **D-29** (motor compatível): VERIFIED — D-01 estrito (Gate C1 + C2) + regressão Shield 23 demonstrando motor `calcularSaldoDevedorAtualizado` lê apenas `data_pagamento + valor` (lê via fixture sintética sem devedor_id)

    ## Drift histórico aceito por design (D-28)

    **Comportamento esperado pós-7.10a:**
    Pagamentos novos cadastrados via UI legacy "Registrar pagamento parcial" (`App.jsx` L2537/2558/2574) escrevem em `pagamentos_parciais` (tabela legacy v1.0), NÃO em `pagamentos_divida`. ProcessosJudiciais a partir desta phase lê apenas `pagamentos_divida` (Phase 7.3 fonte correta). Resultado: pagamentos novos via UI legacy NÃO aparecem em ProcessosJudiciais até unificação de writes em phase própria.

    **Trade-off aceito**: cortar writes em `pagamentos_parciais` é phase própria (sub-backlog 7.10e hipotético). Misturar com migração de leitura quebraria atomicidade. Risco baixo — UI legacy raramente usada em produção (advogado prefere fluxo Contratos via Phase 7 stored proc).

    **Comunicar ao usuário**: durante UAT (Plan 02 SC1-SC4), confirmar que processo de teste tem pagamentos via stored proc Phase 7 (visíveis em `pagamentos_divida`), NÃO via UI legacy.

    **Backlog cleanup** (phases futuras):
    - 7.10b/c/d — migrar Devedores / ModuloDividas / Dashboard pra `allPagamentosDivida`
    - 7.10e (hypothetical) — cortar writes em `pagamentos_parciais` (App.jsx UI legacy)
    - 7.10z (hypothetical) — drop tabela `pagamentos_parciais` (após período observação prod)

    ## Assumptions planner (aplicadas em Plan 01)

    - **Q1** — Local Shield 23: arquivo dedicado `processosJudiciaisFilter.test.js` (escopo limpo + discoverability + precedente custasCache.test.js da 7.9)
    - **Q2** — Inline comment no filter: PT-BR consistente com resto do componente
    - **Q3** — Test contagem: esperado 23/23 (20 base + 3 Shield 23 sub-its)

    ## Fora de escopo

    - Bump submódulo no pai → Plan 02 Task 3
    - Push humano (submódulo + pai) → ação pós-PAUSA #2 (Plan 02)
    - Tag `v1.4-phase7.10a` → pós-smoke prod (ação separada)
    - Cleanup writes legacy `pagamentos_parciais` → 7.10e hipotético
    - Migração outros consumers (Dashboard / Devedores / ModuloDividas) → 7.10b/c/d

    ## Próximos passos

    Executar Plan 02 (`/gsd-execute-phase 07.10a` retomando do Plan 02) para UAT SC1-SC4 + diff consolidado pré-bump + bump pai + push autorizado humano. 2 PAUSAs humanas (UAT cross-check + diff consolidado) — comparado a Phase 7.9 (3 PAUSAs), 7.10a omite a 3ª PAUSA (bump review pré-push pai) por risk reclassification: 4 files + 1 cálculo isolado + Shield 23 equivalência → 2 PAUSAs suficientes.
    ```

    **NÃO commitar este SUMMARY agora.** Plan 02 Task 5 commitará junto do SUMMARY 02 + bump no pai (D-15 — `docs(07.10a): PLAN, SUMMARY consolidados + STATE phase 7.10a SHIPPED`).

    **NÃO executar `git push`** (push é ação humana pós-PAUSA #2 em Plan 02).
  </action>
  <verify>
    <automated>test -f .planning/phases/07.10a-processos-judiciais-pagamentos-divida/07.10a-01-SUMMARY.md &amp;&amp; grep -qE "D-01 ESTRITO" .planning/phases/07.10a-processos-judiciais-pagamentos-divida/07.10a-01-SUMMARY.md &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/07.10a-processos-judiciais-pagamentos-divida/07.10a-01-SUMMARY.md` exists
    - SUMMARY contém SHA submódulo + 4 arquivos listados + seção "D-01 ESTRITO" + Gates (A, B, C1, C2, Shield22, Shield23, F) + Shields 23/23b/23c PASS + D-26..D-29 invariantes + Drift histórico (D-28) documentado + Q1/Q2/Q3 assumptions + Próximos passos
    - SUMMARY ainda NÃO commitado no pai (`cd . && git status --short` shows `?? .planning/phases/07.10a-processos-judiciais-pagamentos-divida/07.10a-01-SUMMARY.md` ou `M`)
    - ZERO `git push` executado neste task
    - ZERO criação de tag
  </acceptance_criteria>
  <done>
    SUMMARY 01 criado no pai (left uncommitted — Plan 02 Task 5 commitará junto). Phase 07.10a Plan 01 encerra em estado "submódulo commitado, pai pendente bump + docs commit, aguardando Plan 02 UAT + bump".
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App.jsx case "processos" → ProcessosJudiciais (prop pagamentos) | Single callsite, locked via Shield 22 grep (positivo + negativo regional) — único ponto de entrada da fonte de pagamentos pro componente |
| ProcessosJudiciais filter (Set lookup) → calcularSaldoDevedorAtualizado | Filter seleciona pagamentos do devedor; motor distribui FIFO entre dividas. D-29 garante shape compat. |
| Cross-devedor isolation | Pagamentos de OUTRO devedor (divida_id ∈ outro devedor) NÃO entram no filter. Shield 23c testa explicitamente. |
| Motor Art.354 ← edits não autorizados | D-01 estrito dual gate C1 (pré-commit working tree, todos 3 motor files vazios) + C2 (pós-commit commit contents, regex inclui todos 3) — usa `--name-only` (lição 608c124) |
| Hook cache (useSaldoAtualizadoCache) ← shape pagamentos_divida | Não toca nesta phase — ProcessosJudiciais não consome o hook (consume direto via prop). Cache SWR continua isolado. |

## Threat Register (STRIDE adapted, lições 7.9 aplicadas)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.10a-01 | Tampering (silent shape mismatch novo vs legacy filter) | ProcessosJudiciais.jsx L254 filter pattern | mitigate | **Shield 23 equivalência** (3 it() blocks H3-class — lição 7.8.2a Shield 5): `expect(novo.map(id)).toEqual(legacy.map(id))` com fixture sintética. Defesa contra wrapper que filtra diferente do esperado. Gate B regressão >=21/21 PASS. |
| T-07.10a-02 | Elevation of Privilege (motor tocado) | Qualquer edit em devedorCalc.js, pagamentos.js, dividas.js | mitigate | D-01 dual gate C1 (pré-commit `git diff` vazio nos 3) + C2 (pós-commit `git log --name-only` regex inclui todos 3) — lição 608c124 usa `--name-only` (não `--stat` que casa em commit message PT-BR rica). |
| T-07.10a-03 | Repudiation (callsite refactor acidental traz legacy de volta) | App.jsx case "processos" | mitigate | **Shield 22 positivo + negativo regional**: grep `pagamentos={allPagamentosDivida}` no callsite (1 match) + awk regional `case "processos":` + 10 linhas seguintes NÃO contém `pagamentos={allPagamentos}` (0 matches). Gate Shield22 bloqueia commit se qualquer regredir. |
| T-07.10a-04 | Information Disclosure (cross-devedor leakage — saldo inflado por pagamento de outro devedor) | Filter pattern Set lookup | mitigate | **Shield 23c (cross-entity isolation)**: fixture com pagamento `divida_id: "div-OUTRO_DEVEDOR"` deve ser filtrado out. `expect(result.map(p => p.id)).toEqual(["p1"])` — só pagamentos do devedor correto. |
| T-07.10a-05 | DoS (filter performance com N grande) | ProcessosJudiciais Set lookup | accept | Set lookup é O(1) amortizado vs O(N) array scan legacy. Performance MELHORA com N grande. UAT SC1-SC2 valida em ambiente real (advogado com 200+ dívidas). |
| T-07.10a-06 | Tampering (devedor sem dívidas crasha) | fullDev?.dividas opcional chaining | mitigate | **Shield 23b (boundary 0 dívidas)**: fixture `devedor.dividas = []` → `new Set([]).has(...)` = false → filter retorna `[]`. Não crasha. Optional chaining `fullDev?.dividas || []` defende contra `fullDev` ausente também. |
| T-07.10a-07 | Information Disclosure (drift histórico — UI legacy escreve em pagamentos_parciais, não aparece em ProcessosJudiciais) | App.jsx L2537+ UI legacy "Registrar pagamento parcial" | accept | **D-28** trade-off aceito por design. Cortar writes é phase própria (sub-backlog 7.10e). Documentado visivelmente em SUMMARY. UAT comunica ao usuário (cenário SC1 garante pagamentos via stored proc Phase 7). |
| T-07.10a-08 | Spoofing (test fixture sintético engana shield) | Shield 23 fixture | mitigate | Fixture explícito com 3 pagamentos (1 em A + 1 em B + 1 spread) cobre cenário real (multi-divida + duplicatas em mesma divida). Equivalence === legacy sintético com `devedor_id` injetado garante semântica preservada. |
</threat_model>

<verification>
- [ ] Task 1 — App.jsx L8478 prop swap aplicado; Shield 22 positivo (1 match) + negativo regional (0 matches); state e carregarTudo intocados; outros consumers ainda em legacy (atomicidade)
- [ ] Task 2 — ProcessosJudiciais.jsx L254 filter adapter aplicado (Set lookup divida_id); padrão legacy `p.devedor_id === row.devedor_id` removido; comment PT-BR inline
- [ ] Task 3 — processosJudiciaisFilter.test.js criado (3 it() blocks Shield 23/23b/23c); package.json `test:regressao` estendido; regressão >=21/21 PASS (esperado 23/23)
- [ ] Task 4 — Gates A, B, C1, C2, Shield22, Shield23, F todos verdes; commit único atômico no submódulo `feat(07.10a)`; 4 files no commit; ZERO push/bump/tag
- [ ] Task 5 — SUMMARY.md criado no pai (left uncommitted — Plan 02 Task 5 commit); D-26..D-29 verificadas; D-28 drift histórico documentado
- [ ] Motor Art.354 INTOCADO em todos 3 arquivos (D-01 estrito) — verificado via Gate C1 (working tree pré-commit) + Gate C2 (commit contents pós-commit)
- [ ] ZERO `git push` executado em qualquer task
- [ ] ZERO bump de submódulo no pai (diferido Plan 02 Task 3)
- [ ] ZERO criação de tag `v1.4-phase7.10a` (diferido pós-smoke prod)
</verification>

<success_criteria>
1. `src/mr-3/mr-cobrancas/src/App.jsx` (mod) — L8478 case "processos" passa `pagamentos={allPagamentosDivida}` (D-26)
2. `src/mr-3/mr-cobrancas/src/components/ProcessosJudiciais.jsx` (mod) — L254 filter via Set lookup `dividaIdsDoDevedor.has(String(p.divida_id))` (D-27); comment PT-BR inline; padrão legacy removido
3. `src/mr-3/mr-cobrancas/src/services/__tests__/processosJudiciaisFilter.test.js` (novo) — Shield 23 com 3 it() blocks (equivalência core + boundary 0 dívidas + cross-entity isolation)
4. `src/mr-3/mr-cobrancas/package.json` (mod) — `test:regressao` inclui processosJudiciaisFilter.test.js
5. Gates A, B, C1, C2, Shield22, Shield23, F todos verdes
6. Regressão >=21/21 PASS (esperado exato 23/23 — 20 base + 3 Shield 23 sub-its)
7. Build exit 0
8. Motor Art.354: D-01 ESTRITO em todos 3 arquivos (devedorCalc.js + pagamentos.js + dividas.js). ZERO mudança no motor. Verificado via dual gate C1 + C2 (`--name-only`)
9. D-26..D-29 invariantes verificadas (Shield 22 grep + Shield 23 equivalência + D-01 dual gate + drift histórico documentado)
10. Commit único atômico no submódulo: `feat(07.10a): ProcessosJudiciais lê pagamentos_divida (filter via dividas-do-devedor)` — 4 files
11. SUMMARY 01 criado no pai (left uncommitted — Plan 02 Task 5 commitará junto)
12. ZERO `git push` autorizado neste plan
13. ZERO bump de submódulo no pai (diferido Plan 02 Task 3)
14. ZERO criação de tag `v1.4-phase7.10a` (diferido pós-smoke prod)
</success_criteria>

<output>
Após completion: `.planning/phases/07.10a-processos-judiciais-pagamentos-divida/07.10a-01-SUMMARY.md` contendo:
- SHA do commit no submódulo (`feat(07.10a)`)
- Lista 4 arquivos no commit (App.jsx + ProcessosJudiciais.jsx + processosJudiciaisFilter.test.js + package.json)
- D-01 ESTRITO confirmation (motor untouched — dual gate C1 + C2)
- Shield 22 grep evidence (positivo + negativo regional + filter pattern)
- Shield 23 3/3 it() PASS evidence
- Regressão N/N PASS (esperado 23/23)
- D-26..D-29 invariantes verificadas
- Drift histórico (D-28) documentado visivelmente — comportamento esperado + comunicação ao usuário no UAT + backlog cleanup
- Assumptions planner Q1/Q2/Q3
- Nota "Plan 02 pendente — UAT SC1-SC4 + diff consolidado pré-bump + bump pai + autorização push humana (2 PAUSAs)"
- SUMMARY left uncommitted — Plan 02 Task 5 commit + bump pai juntos
</output>
