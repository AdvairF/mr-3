# Phase 7.10a — Plan 01 SUMMARY — ProcessosJudiciais lê pagamentos_divida

**Status**: Commited no submódulo, pai NÃO bumpado, push NÃO executado.
**Data**: 2026-04-25
**SHA submódulo**: `2757f74` — `feat(07.10a): ProcessosJudiciais lê pagamentos_divida (filter via dividas-do-devedor)`

## Arquivos no commit (4 files)

Novos:
- `src/mr-3/mr-cobrancas/src/services/__tests__/processosJudiciaisFilter.test.js` (62 linhas, 3 it() blocks Shield 23)

Modificados:
- `src/mr-3/mr-cobrancas/src/App.jsx` (+1/-1) — L8478 prop swap (D-26)
- `src/mr-3/mr-cobrancas/src/components/ProcessosJudiciais.jsx` (+3/-1) — L254 filter adapter Set lookup (D-27)
- `src/mr-3/mr-cobrancas/package.json` (+1/-1) — test:regressao estende com novo test

Diff stat consolidado:
```
mr-cobrancas/package.json                          |  2 +-
mr-cobrancas/src/App.jsx                           |  2 +-
mr-cobrancas/src/components/ProcessosJudiciais.jsx |  4 +-
.../__tests__/processosJudiciaisFilter.test.js     | 62 ++++++++++++++++++++++
4 files changed, 67 insertions(+), 3 deletions(-)
```

## D-01 ESTRITO — Phase 7.10a

Motor Art.354 INTOCADO em todos os 3 arquivos: `devedorCalc.js`, `pagamentos.js`, `dividas.js`. ZERO mudança no motor.

Phase 7.10a tocou apenas componentes consumidores (`App.jsx` callsite + `ProcessosJudiciais.jsx` filter pattern) + test novo + package.json. Motor 100% intocado por construção.

Verificado por dual gate:
- **Gate C1 (pré-commit, working tree)**: `git diff` em devedorCalc.js + pagamentos.js + dividas.js → VAZIO (nenhuma saída)
- **Gate C2 (pós-commit, commit contents)**: `git log -1 --format="" --name-only` em commit `2757f74` → NÃO casa em motor files (usa `--name-only`, NÃO `--stat` — lição 608c124 7.8.2a)

Evidência Gate C2 — `git log -1 --format="" --name-only` em `2757f74`:
```
mr-cobrancas/package.json
mr-cobrancas/src/App.jsx
mr-cobrancas/src/components/ProcessosJudiciais.jsx
mr-cobrancas/src/services/__tests__/processosJudiciaisFilter.test.js
```
Nenhum motor file aparece.

## Gates (Task 4)

- **Gate A — Build**: exit 0 (vite v8.0.8, 132 modules transformed, ✓ built in 819ms)
- **Gate B — Regressão**: **23/23 PASS** (4 test files — calculos 11 + saldoAtualizadoCache 5 + custasCache 4 + processosJudiciaisFilter 3)
- **Gate C1 (pré-commit) — D-01 estrito**: OK — diff vazio em todos 3 motor files
- **Gate C2 (pós-commit) — D-01 estrito**: OK — motor files NÃO aparecem em `git log -1 --name-only`
- **Gate Shield22 — grep callsite**: OK
  - Positivo: `pagamentos={allPagamentosDivida}` em App.jsx case "processos" (1 match — L8478)
  - Negativo regional: `pagamentos={allPagamentos}` no case "processos" + 10 linhas seguintes (0 matches)
  - Filter pattern: `dividaIdsDoDevedor` em ProcessosJudiciais.jsx (2 occurrences — L255 declaração + L256 uso)
- **Gate Shield23 — test file**: OK
  - Arquivo `processosJudiciaisFilter.test.js` existe (62 linhas)
  - 3 it() blocks (Shield 23 / 23b / 23c) — todos PASS via Gate B
- **Gate F — git status post-commit**: OK — working tree limpo (apenas pre-existing untracked: `.planning/*` parent-managed + `node_modules/` gitignored)

## Shields de teste novos (processosJudiciaisFilter.test.js)

- **Shield 23 — Equivalência core** (filter via Set lookup === filter legacy sintético): **PASS**
  - Fixture: devedor com 2 dívidas (`div-A`, `div-B`) + 3 pagamentos (1A + 1B + 1A spread)
  - Asserção: `expect(novo.map(p => p.id)).toEqual(legacy.map(p => p.id))` + `expect(novo).toHaveLength(3)`
- **Shield 23b — Boundary devedor com 0 dívidas**: **PASS**
  - Fixture: `dividas: []` + 1 pagamento qualquer
  - Asserção: `expect(result).toHaveLength(0)` — não crasha, retorna [] determinísticamente
- **Shield 23c — Cross-entity isolation** (pagamentos de outro devedor filtrados out): **PASS**
  - Fixture: devedor com 1 dívida (`div-A`) + 2 pagamentos (`div-A` + `div-OUTRO_DEVEDOR`)
  - Asserção: `expect(result.map(p => p.id)).toEqual(["p1"])` — só pagamento do devedor correto

Defesa H3-class (lição `memory/feedback_shields_wrapper_must_test_equivalence.md` da 7.8.2a — equivalência > propriedade observável). Pega shape mismatch silencioso entre filter NEW vs filter LEGACY sintético com `devedor_id` injetado.

## D-26..D-29 invariantes verificadas

- **D-26** (App.jsx callsite locked): VERIFIED — Shield 22 positivo (1 match `pagamentos={allPagamentosDivida}` no case "processos") + negativo regional (0 matches `pagamentos={allPagamentos}` no escopo regional)
- **D-27** (filter por divida_id, não devedor_id): VERIFIED — Shield 22 filter pattern (`dividaIdsDoDevedor` em ProcessosJudiciais.jsx 2 occurrences) + Shield 23 equivalência (3 it() PASS)
- **D-28** (drift histórico aceito): DOCUMENTED — ver seção "Drift histórico" abaixo
- **D-29** (motor compatível): VERIFIED — D-01 estrito (Gate C1 + C2 ambos OK) + regressão Shield 23 demonstrando que filter pattern não altera shape de pagamento (motor `calcularSaldoDevedorAtualizado` lê apenas `data_pagamento + valor`, não consulta `devedor_id` nem `divida_id`)

## Drift histórico aceito por design (D-28)

**Comportamento esperado pós-7.10a:**

Pagamentos novos cadastrados via UI legacy "Registrar pagamento parcial" (`App.jsx` L2537/2558/2574) escrevem em `pagamentos_parciais` (tabela legacy v1.0), NÃO em `pagamentos_divida`. ProcessosJudiciais a partir desta phase lê apenas `pagamentos_divida` (Phase 7.3 fonte correta).

**Resultado**: pagamentos novos via UI legacy NÃO aparecem em ProcessosJudiciais até unificação de writes em phase própria.

**Trade-off aceito**: cortar writes em `pagamentos_parciais` é phase própria (sub-backlog 7.10e hipotético). Misturar com migração de leitura quebraria atomicidade. Risco baixo — UI legacy raramente usada em produção (advogado prefere fluxo Contratos via Phase 7 stored proc).

**Comunicar ao usuário**: durante UAT (Plan 02 SC1-SC4), confirmar que processo de teste tem pagamentos via stored proc Phase 7 (visíveis em `pagamentos_divida`), NÃO via UI legacy. Se SC2 vs SC3 mostrar drift de centavo, investigar antes de aprovar (drift pode indicar `pagamentos_parciais` tinha registros que `pagamentos_divida` não tem ou vice-versa — neste caso, drift = FAIL e bloquear bump).

**Backlog cleanup** (phases futuras):
- **7.10b/c/d** — migrar Devedores / ModuloDividas / Dashboard pra `allPagamentosDivida` (consumers ainda em legacy nesta phase, atomicidade preservada)
- **7.10e (hypothetical)** — cortar writes em `pagamentos_parciais` (App.jsx UI legacy L2537+)
- **7.10z (hypothetical)** — drop tabela `pagamentos_parciais` (após período de observação prod 1-2 semanas sem regressão)

## Assumptions planner (aplicadas em Plan 01)

- **Q1** — Local Shield 23: arquivo dedicado `processosJudiciaisFilter.test.js` (escopo limpo + discoverability + precedente custasCache.test.js da 7.9)
- **Q2** — Inline comment no filter: PT-BR consistente com resto do componente (`// Phase 7.10a — fonte trocada pra pagamentos_divida (sem devedor_id direto). Filter via lookup divida_id ∈ dividas-do-devedor.`)
- **Q3** — Test contagem: esperado 23/23 — CONFIRMADO (20 base + 3 Shield 23 sub-its, cada `it()` conta 1)

## Outros consumers ainda em legacy (atomicidade preservada — backlog 7.10b/c/d)

Sanity check pós-Plan 01 — `grep -nE 'allPagamentos=\{allPagamentos\}' src/App.jsx` returns 4 matches:
- L8467 — `case "dashboard"` → Dashboard (backlog 7.10d, alto drift)
- L8468 — `case "devedores"` → Devedores (backlog 7.10c, Map agrupador)
- L8488 — `case "dividas"` → ModuloDividas (backlog 7.10b/c)
- L8500 — `case "contratos"` → ModuloContratos prop legacy (cross-check pós-migração)

Apenas case "processos" (L8478) migrado nesta phase — atomicidade explícita.

## UAT cross-check empírico — confiança em shields automatizados (cross-check valor SKIPPED por bug pré-existente)

PAUSA #1 do Plan 02 originalmente exigia cross-check empírico de valor saldo (prod 7.9 vs local 7.10a) em 1 processo com ≥2 pagamentos via stored proc Phase 7. Drift de centavo = FAIL.

**Pivot UAT (2026-04-25)**: durante PAUSA #1, descobri bug pré-existente em prod que IMPEDE cadastro de processo de teste:

```
Erro ao salvar: Could not find the table 'public.processos_judiciais[object Object]' in the schema cache
```

Typo JS clássico (concat objeto com string em algum `dbInsert("processos_judiciais" + algumObjeto, ...)`). Tela ProcessosJudiciais em prod mostra Total=0, Ativos=0, Arquivados=0 — feature de criação NUNCA funcionou em prod. Bug é INDEPENDENTE da migração 7.10a.

**Validação fortuita da estratégia atômica**: ProcessosJudiciais foi escolhido como "menos arriscado" pra 7.10a precisamente porque ninguém usa em prod. Coincidência: era também o único consumidor com feature alvo broken.

**UAT executado** (matriz):

```
SC1 — Pré-check pagamentos_divida disponíveis:    [✅] PASS — 3 registros confirmados via SQL
SC2 — Screenshot prod saldo (cross-check):        [⏭️] SKIP — bug pré-existente impede cadastro
SC3 — Cross-check local saldo:                    [⏭️] SKIP — mesmo motivo
SC4 — Cross-check defesa em profundidade:         [⏭️] SKIP — coberto por Shields 22+23 automatizados
SC-empírico — /processos local sem console error: [✅] PASS — render OK, console limpo, "Nenhum processo cadastrado" igual prod
```

**Defesa**: confiança em shields automatizados:
- **Shield 22 grep estrutural** — App.jsx callsite usa `allPagamentosDivida` (positivo 1 match) + zero `allPagamentos` legacy regional (regex awk 10 linhas após case "processos") — zero retrocesso possível.
- **Shield 23 equivalência semântica** — fixture sintética: filter via Set lookup (`dividaIdsDoDevedor.has(p.divida_id)`) === filter legacy sintético com `devedor_id` injetado. 3 sub-its (equivalência básica + devedor 0 dívidas + cross-divida cross-devedor) — todos PASS. Cobre o pattern semantic correctness sem depender de dados reais de prod.

**Trade-off aceito**: ship phase 7.10a com risco residual conhecido (sem cross-check empírico de valor saldo em dados reais), monitorar smoke prod com cuidado redobrado pós-bump. Bug pré-existente registrado como Phase 7.10.bug em ROADMAP.md backlog. Lição registrada em `memory/feedback_proc_judiciais_create_bug_preexisting.md` (14ª memory entry indexada).

## Fora de escopo (deferido)

- Bump submódulo no pai → Plan 02 Task 3
- Push humano (submódulo + pai) → ação pós-PAUSA #2 (Plan 02)
- Tag `v1.4-phase7.10a` → pós-smoke prod (ação separada, NÃO criada nesta phase)
- Cleanup writes legacy `pagamentos_parciais` → 7.10e hipotético
- Migração outros consumers (Dashboard / Devedores / ModuloDividas) → 7.10b/c/d
- Fix bug pré-existente cadastro processo (`[object Object]` typo) → Phase 7.10.bug (registrada em ROADMAP.md backlog)

## Próximos passos

Executar Plan 02 (`/gsd-execute-phase 07.10a` retomando do Plan 02) para UAT SC1-SC4 + diff consolidado pré-bump + bump pai + push autorizado humano. 2 PAUSAs humanas (UAT cross-check + diff consolidado) — comparado a Phase 7.9 (3 PAUSAs), 7.10a omite a 3ª PAUSA (bump review pré-push pai) por risk reclassification: 4 files + 1 cálculo isolado + Shield 23 equivalência → 2 PAUSAs suficientes.

**Nota**: Este SUMMARY 01 fica left uncommitted no repositório pai. Plan 02 Task 5 commitará junto do SUMMARY 02 + bump pai (D-15 — `docs(07.10a): PLAN, SUMMARY consolidados + STATE phase 7.10a SHIPPED`).
