---
phase: 7.13d
plan: 01
title: "Encargos Defaults Vazios — NovoContrato + AdicionarDocumento + Selecione"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/NovoContrato.jsx
  - src/components/AdicionarDocumento.jsx
  # Task 3 toca UM dos dois (executor decide CASO A ou B):
  - src/utils/correcao.js          # CASO A
  - src/components/DiretrizesContrato.jsx  # CASO B (alternativo)
  # CASO C (caso A precise modificar Inp.jsx para honrar option.disabled):
  - src/components/ui/Inp.jsx      # CASO C (apenas se Task 3 escolher A com disabled)
autonomous: false
estimated_lines: 25-30
estimated_files: 2-3
requirements:
  - D-pre-1
  - D-pre-2
  - D-pre-3
  - D-pre-4
  - D-pre-5
must_haves:
  truths:
    - "Wizard NovoContrato abre com 5 campos críticos vazios (multa, juros %, honor %, índice, juros tipo)"
    - "Operador NÃO consegue submeter NovoContrato em branco — botão disabled OU toast bloqueador"
    - "Dropdowns Índice + Taxa de Juros mostram '— Selecione —' como primeira opção"
    - "AdicionarDocumento herda valor do contrato pós-Path E (sem fallback para defaults velhos)"
    - "Contratos existentes pré-Path E continuam editáveis sem regressão (back-compat)"
  artifacts:
    - path: "src/components/NovoContrato.jsx"
      provides: "ENCARGOS_PADRAO com defaults vazios em 5 campos críticos + camposCriticosOk + handleSalvar early-return"
      contains: 'indexador: ""'
    - path: "src/components/AdicionarDocumento.jsx"
      provides: "state encargos com fallback ?? \"\" alinhado + camposCriticosOk + handleSalvar early-return"
      contains: '?? ""'
    - path: "src/utils/correcao.js OR src/components/DiretrizesContrato.jsx"
      provides: "'— Selecione —' disabled como primeira opção em INDICE + JUROS dropdowns"
      contains: "— Selecione —"
  key_links:
    - from: "NovoContrato.handleSalvar"
      to: "criarContrato (services/contratos.js)"
      via: "early-return bloqueia se camposCriticosOk false"
      pattern: 'if \(!camposCriticosOk\)'
    - from: "AdicionarDocumento state encargos"
      to: "contrato.indice_correcao / contrato.multa_percentual / etc"
      via: 'fallback ?? ""'
      pattern: '\?\? ""'
    - from: "DiretrizesContrato <Inp options>"
      to: "INDICE_OPTIONS / JUROS_OPTIONS in utils/correcao.js"
      via: "primeiro elemento value='' label='— Selecione —' disabled"
      pattern: "— Selecione —"
---

<objective>
Eliminar fricção UX de cadastro NovoContrato: defaults arbitrários
hardcoded (multa "2", juros "1", honor "10", "igpm", "fixo_1") forçam
operador a apagar e redigitar o valor real do termo de acordo a CADA
novo cadastro. Cada termo varia (multa 10/20/30%, honor 0/10%, índice
INPC/IPCA/IGPM, juros taxa_legal/fixo/outros) — não existe padrão.

**Path E locked** (CONTEXT.md §2):
- 5 campos críticos vazios em ENCARGOS_PADRAO
- Validação client-side bloqueia submit em branco (botão disabled +
  handleSalvar early-return defesa em profundidade)
- Dropdowns ganham "— Selecione —" disabled primeira opção
- AdicionarDocumento alinha cascata (`?? ""` em vez de `?? "default_velho"`)
- 2 campos com default semântico mantidos: `despesas: "0"` (zero =
  sem despesas) + `art523_opcao: "nao_aplicar"` (default jurídico
  Art.523 CPC, opt-in explícito)
- `juros_am` validação CONDICIONAL: crítico apenas se
  `juros_tipo === "outros"` (outros tipos têm fórmula implícita ou fixa)

**Origem:** Phase 7.13c REJECT (2026-04-30 ABORT GATE D-pre-7.3).
TASK 0 com dono revelou DOR REAL do operador (defaults atrapalham —
"apaga 2% e digita 30% toda vez"), redirecionando phase de Path C
(UI Diretrizes confusa) → Path E sem custo de implementação errada.

**Severidade:** BAIXA (UX cleanup, não bug financeiro).

Purpose: zero fricção wizard novos cadastros + zero risco submit
silencioso com valor default errado.
Output: 2-3 arquivos editados (~25-30 linhas), 1 commit atomic,
D-01 motor INTOCADO trivialmente.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07.13d-encargos-defaults-vazios/07.13d-CONTEXT.md
@.planning/phases/07.13c-encargos-fonte-unica/07.13c-LEARNINGS.md
@src/mr-3/mr-cobrancas/src/components/NovoContrato.jsx
@src/mr-3/mr-cobrancas/src/components/AdicionarDocumento.jsx
@src/mr-3/mr-cobrancas/src/components/DiretrizesContrato.jsx
@src/mr-3/mr-cobrancas/src/utils/correcao.js
@src/mr-3/mr-cobrancas/src/components/ui/Inp.jsx

<interfaces>
<!-- Key contracts the executor needs — extracted from codebase -->

**CRITICAL: state usa SHORT-FORM keys, payload Supabase usa LONG-FORM keys.**

NovoContrato.jsx ENCARGOS_PADRAO (state, L7-16) — SHORT-FORM:
```js
const ENCARGOS_PADRAO = {
  indexador: "igpm",                    // ← state key (NOT indice_correcao)
  data_inicio_atualizacao: "",
  multa_pct: "2",                       // ← state key (NOT multa_percentual)
  juros_tipo: "fixo_1",
  juros_am: "1",                        // ← state key (NOT juros_am_percentual)
  honorarios_pct: "10",                 // ← state key (NOT honorarios_percentual)
  despesas: "0",
  art523_opcao: "nao_aplicar",
};
```

NovoContrato.jsx payload Supabase (handleSalvar L49-61) — LONG-FORM:
```js
const payload = {
  ...
  indice_correcao:         encargos.indexador || null,
  juros_tipo:              encargos.juros_tipo || null,
  juros_am_percentual:     parseFloat(encargos.juros_am) || 0,
  multa_percentual:        parseFloat(encargos.multa_pct) || 0,
  honorarios_percentual:   parseFloat(encargos.honorarios_pct) || 0,
  ...
};
```

AdicionarDocumento.jsx state encargos (L28-37) — SHORT-FORM com fallback:
```js
const [encargos, setEncargos] = useState({
  indexador:               contrato.indice_correcao         ?? "igpm",       // ← muda para ?? ""
  data_inicio_atualizacao: contrato.data_inicio_atualizacao ?? "",
  multa_pct:               String(contrato.multa_percentual    ?? "2"),     // ← muda para ?? ""
  juros_tipo:              contrato.juros_tipo               ?? "fixo_1",   // ← muda para ?? ""
  juros_am:                String(contrato.juros_am_percentual ?? "1"),    // ← muda para ?? ""
  honorarios_pct:          String(contrato.honorarios_percentual ?? "10"), // ← muda para ?? ""
  despesas:                String(contrato.despesas           ?? "0"),     // ← MANTÉM "0" (semântico)
  art523_opcao:            contrato.art523_opcao             ?? "nao_aplicar", // ← MANTÉM (semântico)
});
```

correcao.js INDICE_OPTIONS / JUROS_OPTIONS — shape `{v, l}`:
```js
export const INDICE_OPTIONS = [
  { v:"igpm", l:"IGP-M" },
  { v:"ipca", l:"IPCA" },
  { v:"selic", l:"SELIC" },
  { v:"inpc", l:"INPC" },
  { v:"inpc_ipca", l:"IPCA a partir de 30/08/24; antes INPC" },
  { v:"nenhum", l:"Sem correção" },
];

export const JUROS_OPTIONS = [
  { v:"taxa_legal_406", l:"Taxa Legal (Art. 406 CC) — STJ Tema 1368" },
  { v:"taxa_legal_406_12", l:"Taxa Legal/art.406 CC: a partir de ago/2024; antes 1% a.m." },
  { v:"legal_classico", l:"0,5% a.m. até 01/2003 e 1% a.m. após" },
  { v:"fixo_05", l:"0,5% a.m. (6% a.a.)" },
  { v:"fixo_1", l:"1% a.m. (12% a.a.)" },
  { v:"selic", l:"Juros da Selic" },
  { v:"sem_juros", l:"Sem juros" },
  { v:"outros", l:"Outros (informar %)" },
];
```

Inp.jsx (ui/Inp.jsx) — RENDER MAP atual (L18-25):
```jsx
{options ? (
  <select value={value} onChange={e => onChange(e.target.value)} style={st} {...rest}>
    {options.map(o => (
      <option key={typeof o === "string" ? o : o.v} value={typeof o === "string" ? o : o.v}>
        {typeof o === "string" ? o : o.l}
      </option>
    ))}
  </select>
) : ...}
```
**Importante:** `<Inp>` HOJE NÃO honra `disabled` em options. Para CASO A
puro (apenas adicionar `{v:"", l:"— Selecione —", disabled:true}` em
correcao.js), Inp.jsx precisa ser ESTENDIDO para `<option ... disabled={o.disabled}>`.
Alternativa: CASO B (DiretrizesContrato.jsx renderiza condicionalmente
sem usar `<Inp options>` para o sentinel).

DiretrizesContrato.jsx (consumidor único de INDICE_OPTIONS+JUROS_OPTIONS, L12+L15):
```jsx
<Inp label="Índice" value={value.indexador}    onChange={v => onChange("indexador", v)}    options={INDICE_OPTIONS} />
<Inp label="Taxa de Juros" value={value.juros_tipo} onChange={v => onChange("juros_tipo", v)} options={JUROS_OPTIONS} />
<Inp label="Juros (% a.m.)" value={value.juros_am} onChange={v => onChange("juros_am", v)} type="number" disabled={value.juros_tipo !== "outros"} />
```
Note: campo "Juros (% a.m.)" JÁ tem `disabled={value.juros_tipo !== "outros"}` — confirma D-pre-1
condicional `juros_am` crítico apenas quando `juros_tipo === "outros"`.
</interfaces>
</context>

## Memory Feedbacks Aplicados

1. **uat_humano_pega_bugs_que_gates_automatizados_nao_pegam**
   — Task 5 UAT humano em prod (5 SCs) é detector primário de regressão
     que gates automatizados (build + lint + test:regressao) não pegam.
     Lição central da 7.13b cumulative.

2. **post_hoc_d_pre_must_be_validated_in_child_phase**
   — D-pre-1..5 da 7.13d são validação fresca da premissa via TASK 0
     com dono na 7.13c REJECT (operador clarificou DOR REAL "apaga 2%
     digita 30% toda vez"), não herança cega de POST-HOC. Memory novo
     da 7.13c REJECT.

3. **discuss_walkthrough_fluxo_real**
   — Origem da phase: caso de uso CONCRETO do operador ("apaga e digita
     a cada cadastro"), não inferência técnica abstrata ("a UI é confusa?").
     Lição 7.9 Q1.

4. **gate_motor_grep_use_name_only**
   — Task 4 dual gate D-01 motor usa `git log -1 --format="" --name-only`
     em vez de `--stat` (lição 7.8.2a commit 608c124 falso positivo
     Gate C2 — `--stat` casa em commit message PT-BR).

5. **grep_global_antes_de_fix_pontual**
   — Task 1+2+3 verificam callsites alinhados (NovoContrato +
     AdicionarDocumento + DiretrizesContrato + correcao.js) antes de
     declarar Path E completo. Lição 7.9 rounds 2-4.

<tasks>

<task type="auto">
  <name>Task 1: NovoContrato.jsx defaults vazios + validação condicional + early-return</name>

  <files>src/mr-3/mr-cobrancas/src/components/NovoContrato.jsx</files>

  <read_first>
- src/mr-3/mr-cobrancas/src/components/NovoContrato.jsx (full — 173 lines)
- .planning/phases/07.13d-encargos-defaults-vazios/07.13d-CONTEXT.md §3 (D-pre-1, D-pre-2, D-pre-3)
  </read_first>

  <action>
**A. Replace ENCARGOS_PADRAO (L7-16) — SHORT-FORM state keys:**

Substituir o objeto literal exato por:
```js
const ENCARGOS_PADRAO = {
  indexador: "",                  // D-pre-1 crítico (era "igpm")
  data_inicio_atualizacao: "",
  multa_pct: "",                  // D-pre-1 crítico (era "2")
  juros_tipo: "",                 // D-pre-1 crítico (era "fixo_1")
  juros_am: "",                   // D-pre-1 crítico condicional (era "1")
  honorarios_pct: "",             // D-pre-1 crítico (era "10")
  despesas: "0",                  // D-pre-2 semântico (zero = sem despesas)
  art523_opcao: "nao_aplicar",    // D-pre-2 semântico (Art.523 CPC opt-in)
};
```

**B. Replace podesSalvar (L38) — validação client-side com condicional juros_am:**

Substituir a linha:
```js
const podesSalvar = !!devedor_id;
```
por:
```js
// D-pre-3 — 5 campos críticos + condicional juros_am quando juros_tipo === "outros"
const camposCriticosOk =
  !!encargos.indexador &&
  !!encargos.multa_pct &&
  !!encargos.juros_tipo &&
  !!encargos.honorarios_pct &&
  (encargos.juros_tipo !== "outros" || !!encargos.juros_am);
const podesSalvar = !!devedor_id && camposCriticosOk;
```

**C. handleSalvar (L44-48) — adicionar early-return defesa em profundidade:**

Inserir IMEDIATAMENTE APÓS a linha `if (!credor_id) ...` e ANTES de `setSalvando(true)`:
```js
if (!camposCriticosOk) {
  toast.error("Preencha todos os encargos antes de salvar.");
  return;
}
```

**D. Mantém TUDO MAIS intacto:**
- payload Supabase L49-61 INTOCADO (long-form keys preservadas)
- handleEncargos L40-42 INTOCADO
- toast `react-hot-toast` import L2 INTOCADO

**E. Atualizar tooltip do botão (L163) para refletir nova validação:**

Substituir:
```jsx
<span title={!devedor_id ? "Selecione o devedor" : undefined}>
```
por:
```jsx
<span title={!devedor_id ? "Selecione o devedor" : !camposCriticosOk ? "Preencha todos os encargos" : undefined}>
```
  </action>

  <verify>
    <automated>
Comandos verificados em sequência (cwd = src/mr-3/mr-cobrancas):

```bash
cd src/mr-3/mr-cobrancas

# Defaults vazios (5 campos críticos, SHORT-FORM):
[ "$(grep -c 'indexador: ""' src/components/NovoContrato.jsx)" -ge 1 ]
[ "$(grep -c 'multa_pct: ""' src/components/NovoContrato.jsx)" -ge 1 ]
[ "$(grep -c 'juros_tipo: ""' src/components/NovoContrato.jsx)" -ge 1 ]
[ "$(grep -c 'juros_am: ""' src/components/NovoContrato.jsx)" -ge 1 ]
[ "$(grep -c 'honorarios_pct: ""' src/components/NovoContrato.jsx)" -ge 1 ]

# Defaults velhos REMOVIDOS:
[ "$(grep -cE 'indexador: "igpm"' src/components/NovoContrato.jsx)" -eq 0 ]
[ "$(grep -cE 'multa_pct: "2"' src/components/NovoContrato.jsx)" -eq 0 ]
[ "$(grep -cE 'juros_tipo: "fixo_1"' src/components/NovoContrato.jsx)" -eq 0 ]
[ "$(grep -cE 'juros_am: "1"' src/components/NovoContrato.jsx)" -eq 0 ]
[ "$(grep -cE 'honorarios_pct: "10"' src/components/NovoContrato.jsx)" -eq 0 ]

# Defaults semânticos PRESERVADOS:
[ "$(grep -c 'despesas: "0"' src/components/NovoContrato.jsx)" -ge 1 ]
[ "$(grep -c 'art523_opcao: "nao_aplicar"' src/components/NovoContrato.jsx)" -ge 1 ]

# Validação:
[ "$(grep -c 'camposCriticosOk' src/components/NovoContrato.jsx)" -ge 4 ]   # decl + podesSalvar + handleSalvar + tooltip (≥4 — threshold ≥3 deixaria drift no tooltip passar silencioso)
[ "$(grep -c 'juros_tipo !== "outros"' src/components/NovoContrato.jsx)" -ge 1 ]
[ "$(grep -c 'Preencha todos os encargos' src/components/NovoContrato.jsx)" -ge 1 ]

# Build OK:
npm run build  # exit 0
```
    </automated>
  </verify>

  <done>
- ENCARGOS_PADRAO com 5 strings vazias críticas + 2 semânticas mantidas
- camposCriticosOk declarado + usado em podesSalvar + usado em handleSalvar + usado em tooltip
- Validação condicional juros_am respeita `juros_tipo === "outros"`
- Tooltip atualizado refletindo nova validação
- npm run build exit 0
  </done>
</task>

**PAUSA INTERNA pós-Task 1** — orchestrator revisa diff isolado:
```bash
cd src/mr-3/mr-cobrancas && git diff src/components/NovoContrato.jsx
```
Antes de prosseguir Task 2, confirmar:
- 5 critical defaults vazios
- 2 semantic defaults intactos
- camposCriticosOk + early-return + tooltip presentes
- payload Supabase L49-61 INTOCADO

---

<task type="auto">
  <name>Task 2: AdicionarDocumento.jsx fallbacks alinhados + validação condicional + early-return</name>

  <files>src/mr-3/mr-cobrancas/src/components/AdicionarDocumento.jsx</files>

  <read_first>
- src/mr-3/mr-cobrancas/src/components/AdicionarDocumento.jsx (full — 227 lines)
- diff aplicado em Task 1 (referência de pattern):
  `cd src/mr-3/mr-cobrancas && git diff src/components/NovoContrato.jsx`
- .planning/phases/07.13d-encargos-defaults-vazios/07.13d-CONTEXT.md §3 (D-pre-5)
  </read_first>

  <action>
**A. Replace state encargos initial value (L28-37) — fallback `?? ""` em vez de `?? "default_velho"`:**

Substituir o bloco useState exato:
```js
const [encargos, setEncargos] = useState({
  indexador:               contrato.indice_correcao         ?? "igpm",
  data_inicio_atualizacao: contrato.data_inicio_atualizacao ?? "",
  multa_pct:               String(contrato.multa_percentual    ?? "2"),
  juros_tipo:              contrato.juros_tipo               ?? "fixo_1",
  juros_am:                String(contrato.juros_am_percentual ?? "1"),
  honorarios_pct:          String(contrato.honorarios_percentual ?? "10"),
  despesas:                String(contrato.despesas           ?? "0"),
  art523_opcao:            contrato.art523_opcao             ?? "nao_aplicar",
});
```
por:
```js
// D-pre-5 — fallbacks alinhados com Path E (5 críticos vazios, 2 semânticos mantidos)
const [encargos, setEncargos] = useState({
  indexador:               contrato.indice_correcao         ?? "",
  data_inicio_atualizacao: contrato.data_inicio_atualizacao ?? "",
  multa_pct:               String(contrato.multa_percentual    ?? ""),
  juros_tipo:              contrato.juros_tipo               ?? "",
  juros_am:                String(contrato.juros_am_percentual ?? ""),
  honorarios_pct:          String(contrato.honorarios_percentual ?? ""),
  despesas:                String(contrato.despesas           ?? "0"),         // semântico mantido
  art523_opcao:            contrato.art523_opcao             ?? "nao_aplicar", // semântico mantido
});
```

**B. Replace podesSalvar (L39-44) — incluir validação encargos críticos com condicional juros_am:**

Substituir o bloco exato:
```js
const podesSalvar =
  !!tipo &&
  !!valor && parseFloat(valor) > 0 &&
  !!dataEmissao &&
  !!numParcelas && parseInt(numParcelas) >= 1 && parseInt(numParcelas) <= 999 &&
  Array.isArray(parcelasCustom) && parcelasCustom.length === parseInt(numParcelas);
```
por:
```js
// D-pre-3 — 5 campos críticos + condicional juros_am quando juros_tipo === "outros"
const camposCriticosOk =
  !!encargos.indexador &&
  !!encargos.multa_pct &&
  !!encargos.juros_tipo &&
  !!encargos.honorarios_pct &&
  (encargos.juros_tipo !== "outros" || !!encargos.juros_am);

const podesSalvar =
  !!tipo &&
  !!valor && parseFloat(valor) > 0 &&
  !!dataEmissao &&
  !!numParcelas && parseInt(numParcelas) >= 1 && parseInt(numParcelas) <= 999 &&
  Array.isArray(parcelasCustom) && parcelasCustom.length === parseInt(numParcelas) &&
  camposCriticosOk;
```

**C. handleSalvar (L65) — adicionar early-return defesa em profundidade:**

Inserir IMEDIATAMENTE APÓS a linha `if (parseInt(numParcelas) > 999) ...` (L69) e ANTES da validação de parcelas:
```js
if (!camposCriticosOk) {
  toast.error("Preencha todos os encargos antes de salvar.");
  return;
}
```

**D. Mantém TUDO MAIS intacto:**
- documentoPayload L90-105 INTOCADO (long-form keys preservadas)
- TabelaParcelasEditaveis L186-198 INTOCADO
- handleEncargos L46-48 INTOCADO
- invalidateContrato call L107 INTOCADO
  </action>

  <verify>
    <automated>
```bash
cd src/mr-3/mr-cobrancas

# Defaults velhos REMOVIDOS dos fallbacks:
[ "$(grep -c '?? "igpm"' src/components/AdicionarDocumento.jsx)" -eq 0 ]
[ "$(grep -c '?? "fixo_1"' src/components/AdicionarDocumento.jsx)" -eq 0 ]
[ "$(grep -cE '\?\? "(2|1|10)"' src/components/AdicionarDocumento.jsx)" -eq 0 ]

# Fallbacks vazios PRESENTES (5 críticos + 1 data_inicio_atualizacao):
[ "$(grep -cE '\?\? ""' src/components/AdicionarDocumento.jsx)" -ge 6 ]

# Defaults semânticos PRESERVADOS:
[ "$(grep -c '?? "0"' src/components/AdicionarDocumento.jsx)" -ge 1 ]
[ "$(grep -c '?? "nao_aplicar"' src/components/AdicionarDocumento.jsx)" -ge 1 ]

# Validação:
[ "$(grep -c 'camposCriticosOk' src/components/AdicionarDocumento.jsx)" -ge 3 ]  # decl + podesSalvar + handleSalvar
[ "$(grep -c 'juros_tipo !== "outros"' src/components/AdicionarDocumento.jsx)" -ge 1 ]
[ "$(grep -c 'Preencha todos os encargos' src/components/AdicionarDocumento.jsx)" -ge 1 ]

# Build OK:
npm run build  # exit 0
```
    </automated>
  </verify>

  <done>
- 5 fallbacks críticos viraram `?? ""` (indexador, multa_pct, juros_tipo, juros_am, honorarios_pct)
- 2 fallbacks semânticos preservados (`?? "0"` e `?? "nao_aplicar"`)
- camposCriticosOk declarado + usado em podesSalvar + usado em handleSalvar
- npm run build exit 0
  </done>
</task>

**PAUSA INTERNA pós-Task 2** — orchestrator revisa diff cascata:
```bash
cd src/mr-3/mr-cobrancas && git diff src/components/AdicionarDocumento.jsx
```
Confirmar paridade de pattern com NovoContrato.jsx (camposCriticosOk + early-return + condicional juros_am).

---

<task type="auto">
  <name>Task 3: "— Selecione —" disabled em INDICE_OPTIONS + JUROS_OPTIONS dropdowns</name>

  <files>
src/mr-3/mr-cobrancas/src/utils/correcao.js (CASO A)
src/mr-3/mr-cobrancas/src/components/DiretrizesContrato.jsx (CASO B)
src/mr-3/mr-cobrancas/src/components/ui/Inp.jsx (CASO A complementar — apenas se necessário)
  </files>

  <read_first>
- src/mr-3/mr-cobrancas/src/utils/correcao.js (foco L143-162: INDICE_OPTIONS + JUROS_OPTIONS)
- src/mr-3/mr-cobrancas/src/components/DiretrizesContrato.jsx (full — 52 lines)
- src/mr-3/mr-cobrancas/src/components/ui/Inp.jsx (full — verificar se honra `o.disabled`)
- .planning/phases/07.13d-encargos-defaults-vazios/07.13d-CONTEXT.md §3 (D-pre-4)
  </read_first>

  <action>
**DECISÃO PENDENTE PRO EXECUTOR — 3 caminhos disponíveis. Documentar
escolha em SUMMARY.md seção "DEVIATIONS" se diverge do CASO A
(default esperado).**

**CASO A — modificar correcao.js + Inp.jsx (preferido se Inp.jsx for trivialmente extensível):**

A.1. Adicionar como PRIMEIRO elemento de INDICE_OPTIONS (utils/correcao.js L143):
```js
export const INDICE_OPTIONS = [
  { v:"", l:"— Selecione —", disabled:true },
  { v:"igpm", l:"IGP-M" },
  // ... resto inalterado
];
```

A.2. Adicionar como PRIMEIRO elemento de JUROS_OPTIONS (utils/correcao.js L153):
```js
export const JUROS_OPTIONS = [
  { v:"", l:"— Selecione —", disabled:true },
  { v:"taxa_legal_406", l:"Taxa Legal (Art. 406 CC) — STJ Tema 1368" },
  // ... resto inalterado
];
```

A.3. Estender Inp.jsx (componente Inp + INP) para honrar `o.disabled` em options.
Localizar em ui/Inp.jsx os 2 blocos `options.map(o => (...)` (L19-25 em Inp + L57-63 em INP)
e substituir cada `<option key=... value=...>` por:
```jsx
<option
  key={typeof o === "string" ? o : o.v}
  value={typeof o === "string" ? o : o.v}
  disabled={typeof o === "object" && o.disabled === true}
>
  {typeof o === "string" ? o : o.l}
</option>
```

**CASO B — modificar APENAS DiretrizesContrato.jsx (alternativo, se mexer em
Inp.jsx for considerado risco — Inp.jsx é shared component em outros lugares):**

B.1. Em DiretrizesContrato.jsx L12 e L15, NÃO usar `<Inp options>` — substituir
por `<select>` inline com `<option>` sentinel + map:

Para o campo Índice (L12):
```jsx
<div>
  <label style={{ fontSize: 11, fontWeight: 700, color: "#64748b", display: "block", marginBottom: 4, textTransform: "uppercase" }}>
    Índice
  </label>
  <select
    value={value.indexador}
    onChange={e => onChange("indexador", e.target.value)}
    style={{ width: "100%", padding: "8px 10px", border: "1.5px solid #e2e8f0", borderRadius: 9, fontSize: 13, outline: "none", fontFamily: "'Plus Jakarta Sans',sans-serif", boxSizing: "border-box" }}
  >
    <option value="" disabled>— Selecione —</option>
    {INDICE_OPTIONS.map(o => (
      <option key={o.v} value={o.v}>{o.l}</option>
    ))}
  </select>
</div>
```

Mesmo pattern para Taxa de Juros (L15) usando `JUROS_OPTIONS`.

**CASO C — fallback: render condicional inline em correcao.js (NÃO mexer em Inp.jsx):**

Adicionar `{v:"", l:"— Selecione —"}` (sem disabled) como primeiro elemento.
Aceita que browser permita re-selecionar `""` mas validação client-side de
Task 1+2 bloqueia submit. **Menos preferido** — `"— Selecione —"` deveria
ser visualmente disabled. Documentar como tradeoff em SUMMARY.md.

**Critério de decisão:**

A mudança proposta no CASO A (adicionar `disabled` prop em
options) é TRIVIALMENTE BACKWARD-COMPATIBLE: options
existentes sem `disabled` key resolvem para `disabled={false}`
via JS short-circuit (`typeof o === 'object' && o.disabled === true`
retorna `false` quando `o.disabled` é `undefined`). Logo CASO A é
SAFE independente do count de consumers.

CASO A é a opção PREFERIDA. Verificação opcional:
```bash
cd src/mr-3/mr-cobrancas && grep -rn "from.*['\"].*ui/Inp" src/
```

CASO B só se Inp.jsx tem testes unit que falhariam em
modificação (verificar):
```bash
grep -rn 'Inp\.jsx' src/__tests__ src/components/__tests__
```
Se ≥1 test file existir → considerar CASO B.

CASO C apenas se ambos A e B forem rejeitados pelo orchestrator na PAUSA pós-Task 3.

**Documentação obrigatória em DEVIATIONS:**
Se executor escolher CASO B ou C (em vez de CASO A), incluir em SUMMARY.md
seção `## Deviations`:
```
- Task 3: CASO {B|C} aplicado em vez de CASO A.
  Motivo: {razão concreta — e.g. "Inp.jsx tem N consumers além de DiretrizesContrato; mudança em shared component fora de escopo BAIXA"}
  Impacto: {e.g. "Sentinel sem disabled prop visual; validação client-side Task 1+2 cobre"}
```
  </action>

  <verify>
    <automated>
```bash
cd src/mr-3/mr-cobrancas

# CASO A — sentinel em correcao.js + Inp.jsx atualizado:
if grep -qE '\{ v:"", l:"— Selecione —"' src/utils/correcao.js; then
  # CASO A detectado
  [ "$(grep -cE 'l:"— Selecione —"' src/utils/correcao.js)" -ge 2 ]   # 1 INDICE + 1 JUROS
  [ "$(grep -c 'disabled:true' src/utils/correcao.js)" -ge 2 ]        # ambos sentinels disabled
  # Inp.jsx deve honrar o.disabled:
  [ "$(grep -c 'o.disabled' src/components/ui/Inp.jsx)" -ge 1 ]
elif grep -qE '— Selecione —' src/components/DiretrizesContrato.jsx; then
  # CASO B detectado (sentinel inline em DiretrizesContrato)
  [ "$(grep -c '— Selecione —' src/components/DiretrizesContrato.jsx)" -ge 2 ]   # 1 INDICE + 1 JUROS
  [ "$(grep -c 'disabled' src/components/DiretrizesContrato.jsx)" -ge 2 ]        # 2 disabled (sentinels)
  # correcao.js INTOCADO:
  [ "$(git diff src/utils/correcao.js | wc -l)" -eq 0 ]
else
  echo "ERRO: nenhum dos casos A/B detectado"
  exit 1
fi

# Build OK:
npm run build  # exit 0
```
    </automated>
  </verify>

  <done>
- "— Selecione —" presente em ≥ 2 lugares (1 INDICE + 1 JUROS)
- Sentinel é `disabled` (CASO A com Inp.jsx update OU CASO B com option inline)
- Decisão CASO A vs B vs C documentada (CASO A default; B ou C como deviation)
- npm run build exit 0
  </done>
</task>

**PAUSA INTERNA pós-Task 3** — orchestrator confirma decisão de implementação
revendo diff:
```bash
cd src/mr-3/mr-cobrancas && git diff
```
Validar caso escolhido (A, B, ou C) está coerente; se CASO B ou C, confirmar
DEVIATION está documentado conforme spec.

---

<task type="auto">
  <name>Task 4: Build + lint + tests + D-01 motor dual gate</name>

  <files>none (verification only)</files>

  <read_first>
- src/mr-3/mr-cobrancas/package.json (confirmar scripts: build, lint, test)
- .planning/phases/07.13d-encargos-defaults-vazios/07.13d-CONTEXT.md §5 (Invariantes — D-01 motor INTOCADO trivialmente, 34/34 motor regression PASS esperado)
- MEMORY.md `feedback_gate_motor_grep_use_name_only.md` (lição 7.8.2a)
  </read_first>

  <action>
**Sequência atomica (cwd = src/mr-3/mr-cobrancas):**

```bash
cd src/mr-3/mr-cobrancas

# 1. Build:
npm run build
# Esperado: exit 0

# 2. Lint:
npm run lint
# Esperado: exit 0

# 3. Tests (motor regression):
npm test
# Esperado: exit 0, 34/34 PASS na suite motor regression
# (D-01 INTOCADO trivialmente → suite NÃO mudou)

# 4. D-01 motor dual gate:

# 4a. Diff -U0 motor files = ZERO hunks (nenhuma mudança de conteúdo):
[ "$(git diff -U0 src/utils/devedorCalc.js | wc -l)" -eq 0 ]
[ "$(git diff -U0 src/services/pagamentos.js | wc -l)" -eq 0 ]
[ "$(git diff -U0 src/services/dividas.js | wc -l)" -eq 0 ]
[ "$(git diff -U0 src/utils/masks.js | wc -l)" -eq 0 ]

# 4b. git log HEAD --name-only NÃO contém arquivos motor
# (NOTA: este gate é executado APÓS commit final — pré-commit verifica
# via 4a; pós-commit verifica via 4b para confirmar histórico limpo):
git log -1 --format="" --name-only HEAD | grep -E '(devedorCalc|masks|services/(pagamentos|dividas))' | wc -l
# Esperado: 0
# (Lição feedback_gate_motor_grep_use_name_only — usar --name-only,
# NÃO --stat. --stat casa em commit message PT-BR causando falso positivo.)
```

**Captura output em SUMMARY.md** seção `## D-01 Motor Dual Gate`:
- Output do gate 4a (4 zeros)
- Output do gate 4b (0)
- Lista de arquivos motor verificados
  </action>

  <verify>
    <automated>
```bash
cd src/mr-3/mr-cobrancas

# IMPORTANTE: Gates 4a (pre-commit) e 4b (post-commit) NÃO
# devem ser rodados sequencialmente sem o git commit entre
# eles. Executor deve fazer commit do submódulo APÓS gates
# pre-commit GREEN, e SOMENTE então rodar Gate 4b.

# === PRE-COMMIT GATES (rodar ANTES de git commit) ===

# Gate 1+2+3 — build/lint/test exit 0:
npm run build && npm run lint && npm test

# Gate 4a — diff -U0 motor files = ZERO hunks (working tree
# antes do commit confirma motor não tocado):
[ "$(git diff -U0 src/utils/devedorCalc.js | wc -l)" -eq 0 ]
[ "$(git diff -U0 src/services/pagamentos.js | wc -l)" -eq 0 ]
[ "$(git diff -U0 src/services/dividas.js | wc -l)" -eq 0 ]
[ "$(git diff -U0 src/utils/masks.js | wc -l)" -eq 0 ]

# === COMMIT FINAL DO SUBMÓDULO (após Tasks 1+2+3 + gates pre-commit GREEN) ===
# git commit -m "feat(07.13d): defaults vazios + validação + Selecione"

# === POST-COMMIT GATE (rodar APÓS git commit) ===

# Gate 4b — git log HEAD --name-only NÃO contém arquivos motor.
# CRÍTICO: rodar APENAS APÓS o commit final (HEAD aponta pro
# commit dessa task; antes do commit, HEAD aponta pro estado
# anterior e gate retornaria false positive/negative).
[ "$(git log -1 --format='' --name-only HEAD | grep -cE '(devedorCalc|masks|services/(pagamentos|dividas))')" -eq 0 ]
```
    </automated>
  </verify>

  <done>
- npm run build exit 0
- npm run lint exit 0
- npm test exit 0 com 34/34 motor regression PASS
- Gate 4a: 4 zeros (devedorCalc.js + pagamentos.js + dividas.js + masks.js diff = 0)
- Gate 4b: log --name-only do commit final NÃO inclui arquivos motor
- Output capturado em SUMMARY.md seção D-01 Motor Dual Gate
  </done>
</task>

**PAUSA INTERNA pós-Task 4** — orchestrator re-confirma D-01 INTOCADO via dual
gate output capturado. Se qualquer gate falhar → STOP, investigar, propor
fix-forward antes de Task 5.

---

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: UAT humano em prod (5 SCs) — CHECKPOINT BLOQUEANTE</name>

  <what-built>
- NovoContrato wizard com 5 campos críticos vazios + validação client-side bloqueante + tooltip explicativo
- AdicionarDocumento alinhado: fallbacks `?? ""` em vez de `?? "default_velho"` + mesma validação
- Dropdowns Índice + Taxa de Juros com "— Selecione —" como primeira opção (disabled)
- 2 defaults semânticos preservados: `despesas: "0"` + `art523_opcao: "nao_aplicar"`
- D-01 motor INTOCADO (devedorCalc.js + services/pagamentos.js + services/dividas.js + masks.js zero diff)
- 34/34 motor regression PASS, npm run build/lint/test exit 0
  </what-built>

  <how-to-verify>
**Pré-requisito:** orchestrator faz commit + push submódulo + bump pai +
deploy Vercel ANTES de iniciar UAT (commit final). Aguardar deploy
completo em prod.

**Acessar mrcobrancas.com.br** (prod). Console F12 aberto durante toda UAT
(verificar ZERO erro novo).

---

**SC1 — NovoContrato wizard vazios visíveis:**
1. Acessar tela "Contratos" → clicar "+ Novo Contrato"
2. Selecionar devedor + credor (mas NÃO mexer em encargos)
3. Observar seção "Encargos Padrão do Contrato" / "Diretrizes do Contrato"
4. Verificar VISUALMENTE:
   - Campo "Índice" → dropdown mostra "— Selecione —" (cinza/disabled visual)
   - Campo "Multa (%)" → input vazio (placeholder ou nada digitado)
   - Campo "Taxa de Juros" → dropdown mostra "— Selecione —"
   - Campo "Juros (% a.m.)" → DESABILITADO (já era — disabled quando juros_tipo !== "outros")
   - Campo "Honorários (%)" → input vazio
   - Campo "Despesas (R$)" → "0" (semântico mantido — PASS esperado)
   - Campo "Art. 523" → "Não aplicar" (semântico mantido — PASS esperado)
   - Campo "Data Início Atualização" → vazio (já era)
5. Botão "Criar Contrato" → DESABILITADO (estado disabled visual)
6. Hover no botão → tooltip mostra "Preencha todos os encargos"

**Resultado esperado SC1:** ✅ TODOS visíveis vazios + dropdowns "— Selecione —" + botão disabled + tooltip correto

---

**SC2 — Submit em branco bloqueado:**
1. Cenário a partir de SC1 (encargos em branco)
2. Tentar clicar no botão "Criar Contrato" disabled
3. Botão NÃO responde (disabled visual + lógico)
4. Alternativa: usar DevTools Console para forçar `disabled=false` no botão
5. Clicar — handleSalvar early-return dispara toast vermelho "Preencha todos os encargos antes de salvar"
6. Contrato NÃO criado em prod (verificar listagem Contratos não aumentou)

**Resultado esperado SC2:** ✅ Botão disabled OU toast bloqueador + contrato NÃO persiste

---

**SC3 — Submit válido com valores reais:**
1. Cenário a partir de SC1
2. Preencher TODOS os encargos com valores reais de um termo de acordo
   exemplo do operador (não os defaults velhos):
   - Índice: "INPC" (NÃO "IGP-M" — testar valor diferente do default antigo)
   - Multa: "30" (NÃO "2")
   - Taxa de Juros: "Outros (informar %)"
   - Juros (% a.m.): "1.5" (campo agora habilita por causa de "outros")
   - Honorários: "0" (NÃO "10" — operador real reportou honor 0% como caso comum)
   - Despesas: "0" (deixa como está)
   - Data Início Atualização: data do termo
   - Art. 523: "Não aplicar"
3. Botão "Criar Contrato" → HABILITA (camposCriticosOk = true)
4. Clicar → toast.success "Contrato criado. Adicione o primeiro documento."
5. Redireciona para tela de adicionar documento
6. Verificar VIA DB ou via UI que contrato persistiu com:
   - `indice_correcao = "inpc"`
   - `multa_percentual = 30`
   - `juros_tipo = "outros"`
   - `juros_am_percentual = 1.5`
   - `honorarios_percentual = 0`

**Resultado esperado SC3:** ✅ Contrato criado em prod com valores reais; toast success; redirect funcional

---

**SC4 — Edit contrato existente (back-compat pré-Path E):**
1. Acessar lista Contratos → escolher contrato CRIADO ANTES da phase 7.13d
   (qualquer contrato existente em prod com encargos preenchidos)
2. Verificar que abre em DetalheContrato com encargos preservados
   (multa, juros, honor, índice todos com valores antigos)
3. Editar 1 encargo (ex: multa 2% → 5%) → salvar
4. Verificar que mudança persiste sem regressão

**Resultado esperado SC4:** ✅ Edit normal sem regressão; encargos antigos preservados; back-compat total

---

**SC5 — AdicionarDocumento herda contrato pós-Path E:**
1. Cenário a partir de SC3 (contrato pós-Path E criado com valores reais)
2. Tela de adicionar documento já abre (redirect de SC3)
3. Verificar que campos de Encargos do Documento herdam do contrato:
   - Índice: "INPC" (do contrato)
   - Multa: "30" (do contrato)
   - Juros: "Outros" (do contrato)
   - Juros %: "1.5" (do contrato)
   - Honor: "0" (do contrato)
4. Confirmar que NÃO mostra defaults velhos ("IGP-M", "2", "fixo_1", "1", "10")
5. Editar 1 encargo no documento (ex: honor 0% → 10% só para esse documento)
6. Preencher tipo + valor + data + parcelas + tabela parcelas
7. Submit → documento adicionado com sucesso

**Resultado esperado SC5:** ✅ Carrega valores do contrato (não defaults velhos); permite editar por documento; submit OK

---

**Console F12 cumulative:** ZERO erro novo durante SC1-SC5 (warnings React aceitáveis se já existiam pré-phase).
  </how-to-verify>

  <resume-signal>
Orchestrator retorna UMA das duas strings exatas:

**`UAT 7.13d PASS`** → autoriza commit final + bump submódulo no pai +
push pai + tag `v1.4-phase7.13d` no pai (chore bump). Procede para
SUMMARY.md + LEARNINGS.md.

**`UAT FAIL [motivo]`** → executor diagnostica problema, propõe
fix-forward concreto (qual SC falhou, qual arquivo, qual mudança).
NÃO push tag. Aguarda novo ciclo.
  </resume-signal>

  <verify>
    <automated>
N/A — checkpoint humano. Verificação automática prévia coberta por Tasks 1-4.
Após resume-signal PASS, gate de auditoria final:
```bash
cd src/mr-3/mr-cobrancas

# Final gate cumulative:
[ "$(git log -1 --format="" --name-only HEAD | grep -cE '(devedorCalc|masks|services/(pagamentos|dividas))')" -eq 0 ]
[ "$(git log -1 --format="" --name-only HEAD | grep -cE 'src/components/(NovoContrato|AdicionarDocumento)\.jsx$')" -ge 2 ]
```
    </automated>
  </verify>

  <done>
- 5/5 SCs PASS REAL em prod mrcobrancas.com.br
- Console F12 sem erro novo
- Resume-signal `UAT 7.13d PASS` retornado pelo orchestrator
- Memory feedback `uat_humano_pega_bugs_que_gates_automatizados_nao_pegam` aplicado
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → form NovoContrato/AdicionarDocumento | input do operador; valores numéricos (multa, juros, honor) viram parseFloat() no payload Supabase |
| form → Supabase (criarContrato / adicionarDocumento) | payload long-form keys (`indice_correcao`, `multa_percentual`, etc) crossando para DB |
| contrato (DB) → AdicionarDocumento state encargos | leitura back-compat de contratos existentes; fallback `?? ""` permite contrato pós-Path E não corromper UI doc |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7.13d-01 | Tampering | NovoContrato submit em branco com defaults vazios | mitigate | Validação client-side `camposCriticosOk` (Task 1) bloqueia botão; handleSalvar early-return defesa em profundidade caso DOM seja manipulado via DevTools |
| T-7.13d-02 | Repudiation | Operador alega ter cadastrado com valor X mas DB tem default arbitrário | mitigate | Defaults vazios eliminam ambiguidade — operador DIGITA explicitamente o valor real; submit silencioso com default errado fica impossível |
| T-7.13d-03 | Information Disclosure | n/a | accept | Phase é UI-only client-side; sem novo endpoint, sem novo data flow |
| T-7.13d-04 | Denial of Service | n/a | accept | Phase reduz fluxo (submit bloqueado), não amplifica; sem loop, sem fetch novo |
| T-7.13d-05 | Elevation of Privilege | n/a | accept | Sem mudança em RLS, sem mudança em service layer, sem mudança em authz |
| T-7.13d-06 | Spoofing | AdicionarDocumento com contrato.X null + fallback velho herda default arbitrário | mitigate | Task 2 substitui `?? "default_velho"` por `?? ""` — se contrato não tem encargo, doc também não terá (consistente, sem spoofing de origem) |
</threat_model>

<verification>
**Phase-level checks (cumulative):**

1. **Path E completo aplicado** — 5 campos críticos vazios em ambos NovoContrato + AdicionarDocumento
2. **Validação client-side em ambos** — camposCriticosOk + condicional juros_am
3. **Defesa em profundidade** — handleSalvar early-return em ambos
4. **Sentinel "— Selecione —"** — presente em ≥ 2 lugares
5. **D-01 motor INTOCADO trivialmente** — dual gate diff -U0 + log --name-only
6. **Build/lint/test** — exit 0 nas 3 fases
7. **34/34 motor regression PASS** — suite não regrediu
8. **UAT humano** — 5/5 SCs PASS REAL em prod
9. **Memory feedbacks aplicados** — 5 feedbacks citados explicitamente

**Invariantes invioláveis verificados:**

- D-01 motor INTOCADO trivialmente (UI-only)
- Schema INTOCADO (zero migration)
- Service layer INTOCADO (`services/contratos.js` inalterado)
- Contratos existentes não afetados (back-compat total via SC4)
- Edit DetalheContrato não afetado (não tocado nesta phase)
- masks.js INTOCADO cumulative (D-pre-3 RESTRITO da 7.14 preservado)
- App.jsx INTOCADO (drawer Pessoas legacy preservado da 7.14 D-pre-12)
- DetalheContrato.jsx INTOCADO cumulative
</verification>

<success_criteria>
- [ ] Task 1 PASS: NovoContrato.jsx com ENCARGOS_PADRAO 5 vazios + 2 semânticos, camposCriticosOk + handleSalvar early-return + tooltip
- [ ] Task 2 PASS: AdicionarDocumento.jsx com fallback `?? ""` em 5 críticos + 2 semânticos preservados, camposCriticosOk + handleSalvar early-return
- [ ] Task 3 PASS: "— Selecione —" disabled em INDICE + JUROS dropdowns (CASO A, B ou C documentado)
- [ ] Task 4 PASS: build + lint + test exit 0 + dual gate D-01 motor INTOCADO
- [ ] Task 5 PASS: 5 SCs UAT humano PASS REAL em prod mrcobrancas.com.br
- [ ] Resume-signal `UAT 7.13d PASS` retornado pelo orchestrator
- [ ] Tag `v1.4-phase7.13d` aplicada no pai (chore bump apenas — feedback `tag_push_apenas_no_pai_chore_bump`)
- [ ] SUMMARY.md gerado com seção D-01 Motor Dual Gate + DEVIATIONS (se aplicável)
- [ ] LEARNINGS.md gerado com 5 D-pre validados + lições da phase + memory feedback NOVO `feedback_validate_phase_premise_with_user_use_case_first`
</success_criteria>

<output>
Após completar todas as 5 tasks com UAT PASS:
1. Criar `.planning/phases/07.13d-encargos-defaults-vazios/07.13d-01-SUMMARY.md`
2. Criar `.planning/phases/07.13d-encargos-defaults-vazios/07.13d-LEARNINGS.md`
3. Atualizar `.planning/STATE.md` (orchestrator) com phase shipped + tag aplicada
4. Atualizar `MEMORY.md` (orchestrator) com feedback NOVO planejado
   `feedback_validate_phase_premise_with_user_use_case_first`
</output>
