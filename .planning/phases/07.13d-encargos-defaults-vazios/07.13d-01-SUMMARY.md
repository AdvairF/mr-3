---
phase: 7.13d
plan: 01
title: "Encargos Defaults Vazios — NovoContrato + AdicionarDocumento + Selecione + DetalheContrato fix-forward"
status: SHIPPED_PARTIAL
shipped_date: 2026-04-30
tag: v1.4-phase7.13d
tag_target: f80a8e3 (pai chore bump)
---

# Phase 7.13d Plan 01 — SUMMARY

## Status: SHIPPED parcial 2026-04-30

Tag `v1.4-phase7.13d` em `f80a8e3` pai (chore bump). Path E entregue; bug SC4b independente registrado como Phase 7.13d.bug2 BACKLOG.

## Commits

### Submódulo `src/mr-3/mr-cobrancas` (origin/main)
- `58cc6fa` — feat(07.13d): defaults vazios + validação obrigatória + Selecione (4 files: NovoContrato + AdicionarDocumento + Inp + correcao.js)
- `2071c46` — fix(07.13d.bug): DetalheContrato edit — defaults vazios + validação (1 file: DetalheContrato.jsx)

### Pai `mr-cobrancas` (origin/master)
- `3fae579` — docs(07.13d): insert phase Encargos Defaults Vazios — Path E locked
- `1734568` — docs(07.13d): plan 01
- `9aead9b` — chore(07.13d): bump submódulo mr-3 → 58cc6fa
- `f80a8e3` — chore(07.13d.bug): bump submódulo mr-3 → 2071c46 (TAG TARGET)

## Path E entregue (4 EDITs originais + 4 EDITs fix-forward)

### NovoContrato.jsx (Task 1 plan original)
- ENCARGOS_PADRAO 5 críticos vazios (`indexador`, `multa_pct`, `juros_tipo`, `juros_am`, `honorarios_pct`)
- 2 semânticos preservados (`despesas: "0"`, `art523_opcao: "nao_aplicar"`)
- `camposCriticosOk` validação client-side com condicional `juros_am` quando `juros_tipo === "outros"`
- `handleSalvar` early-return defesa em profundidade
- Tooltip `<span title>` atualizado

### AdicionarDocumento.jsx (Task 2 plan original)
- Fallbacks `?? ""` em 5 críticos (alinha cascata Path E)
- 2 semânticos preservados (`despesas`, `art523_opcao`)
- `camposCriticosOk` + `handleSalvar` early-return idem

### Inp.jsx (Task 3 CASE A)
- `<option>` honra `o.disabled` via `typeof === 'object' && o.disabled === true`
- Backward-compatible: 5 outros consumers (App, DetalheContrato, DividaForm, NovaCustaModal, DiretrizesContrato) sem `disabled` key resolvem `disabled={false}` via short-circuit

### correcao.js (Task 3 CASE A)
- INDICE_OPTIONS L143: `{ v:"", l:"— Selecione —", disabled:true }` primeiro elemento
- JUROS_OPTIONS L153: idem

### DetalheContrato.jsx (fix-forward pós-UAT FAIL — smoking gun PRIMARY)
- `initEditForm` L78-91: 5 críticos `?? ""` (era `?? "igpm"/"2"/"fixo_1"/"1"/"10"`)
- `useMemo camposCriticosOk` após `handleEncargos` (pattern coerente com 3 outros `useMemo` no arquivo)
- `handleSalvar` early-return defesa em profundidade
- Save button wrap `<span title>` + `disabled={salvando || !camposCriticosOk}`

## UAT validation em prod (mrcobrancas.com.br)

| SC | Cenário | Resultado |
|---|---|---|
| **SC1** | Wizard NovoContrato vazios visíveis | ✅ PASS — 5 críticos vazios + dropdowns "— Selecione —" disabled + botão disabled + tooltip |
| **SC2** | Submit em branco bloqueado | ✅ PASS implícito (botão disabled visível em SC1) |
| **SC3** | Submit válido com valores reais | ✅ PASS REAL — contrato `f4417baa` criado: multa=30, juros_am=1.5, honor=0, juros_tipo="outros", indexador=inpc, art523=nao_aplicar |
| **SC4a** | Edit DetalheContrato sem mexer (back-compat) | ✅ PASS — valores preservados |
| **SC4b** | Edit DetalheContrato modificando 1 encargo | ❌ FAIL — anomalia "input N → DB N-x" não-determinística (15→12, 25→24); INDEPENDENTE de Path E |
| SC4c | Edit com campo vazio (validação) | ⏸ NÃO TESTADO — UAT abortado por fadiga; mesma classe SC2 que PASS |
| SC5 | AdicionarDocumento herda contrato pós-Path E | ⏸ NÃO TESTADO — pipeline paralelo SC3 sugere OK |

**Console F12 cumulative durante SC1+SC3+SC4a:** ZERO erro novo.

## Bug SC4b — investigação read-only completa

### Pipeline auditado (lossless verificado)

```
User typing in <Inp type="number"> (DiretrizesContrato.jsx)
  → Inp.jsx delegates to InputBR (paste-friendly BR)
  → InputBR(type="value") renders native <input type="number">
  → onChange = e => onChange(e.target.value)  // RAW string passthrough
  → DetalheContrato.handleEncargos(field, val)
  → setEditForm({ ...f, encargos: { ...f.encargos, [field]: val } })
  → handleSalvar payload: parseFloat(state.X) || 0
  → editarContrato(contratoId, payload)
  → dbUpdate(TABLE, id, body) → sb(t, "PATCH", b, ...)  // REST PATCH puro
  → Supabase contratos_dividas
```

**ZERO transformações encontradas em todo o pipeline.** `parseFloat("15") = 15`, `parseFloat("25") = 25`. `dbUpdate` é REST PATCH puro sem transformação.

### Hipóteses (priorizadas pós-investigação)

1. **Bug pré-existente latente** em DetalheContrato edit flow específico (operador raramente edita contratos existentes — geralmente cria novos via wizard ou adiciona docs)
2. **Browser quirk** (autocomplete/autofill/locale interferindo)
3. **Browser extension** mutando DOM antes do submit
4. Não-decisivo: orchestrator decidiu não investigar profundamente sob fadiga UAT real

### Não testado por fadiga

- Network tab capture do payload PATCH (campo UI ↔ payload JSON ↔ DB triangulação)
- pg_trigger audit Supabase
- Reprodução em browser anônimo / outro browser

### NovoContrato com mesmo pipeline FUNCIONA (SC3 PASS)

→ Bug SC4b é específico ao DetalheContrato edit flow (NOT regressão de Path E).

## Restauração SQL prod (orchestrator-side)

ACORDO `ce7b8d47-e93a-42ed-a909-e5038baf3411` corrompido durante UAT 7.13d:

```sql
-- Pré-fix DetalheContrato (commit 2071c46)
UPDATE contratos_dividas SET multa_percentual = 10
WHERE id = 'ce7b8d47-e93a-42ed-a909-e5038baf3411';

-- Pós-UAT (testes corromperam honor)
UPDATE contratos_dividas SET honorarios_percentual = 10
WHERE id = 'ce7b8d47-e93a-42ed-a909-e5038baf3411';
```

**Nota mitigante:** motor lê encargos das **DÍVIDAS** (contratos_dividas é metadata header), não do contrato. Cálculos jurídicos (saldo atualizado, juros, multa, honor) usam dividas. ACORDO dividas (12 rows) preservaram multa=10/juros=1/honor=10 corretos. Corrupção do header foi visual/declarativa apenas, sem impacto financeiro real.

## D-01 motor INTOCADO cumulative

Dual gate confirmed em ambos commits (58cc6fa + 2071c46):

| File | Gate 4a (working tree) | Gate 4b (post-commit) |
|---|---|---|
| `src/utils/devedorCalc.js` | 0 hunks | 0 matches |
| `src/services/pagamentos.js` | 0 hunks | 0 matches |
| `src/services/dividas.js` | 0 hunks | 0 matches |
| `src/utils/masks.js` | 0 hunks | 0 matches |

Memory feedback aplicado: `gate_motor_grep_use_name_only` (lição 7.8.2a) — `git log -1 --format="" --name-only` em vez de `--stat`.

## Reconciliações Path E

| Item | Status | Justificativa |
|---|---|---|
| `npm run lint` script | SKIP | Nunca configurado no projeto — não regressão 7.13d |
| `npm test` (full suite) `filaDevedor.test.js` | SKIP | Pre-existente Phase 7.13b — integration test pattern Node CLI, não vitest suite (backlog futuro) |
| `npm run test:regressao` 34/34 motor | ✅ GATE EFETIVO | Auto-roda via `prebuild` hook |
| `npm run build` exit 0 | ✅ GATE COMPOSTO | Build = test:regressao + vite build |

## Memory feedbacks aplicados (5 originais + 2 novos descobertos pós-UAT)

### Originais (citados no plan):
1. `uat_humano_pega_bugs_que_gates_automatizados_nao_pegam` — UAT pegou anomalia SC4b que gates automatizados não detectaram
2. `post_hoc_d_pre_must_be_validated_in_child_phase` — D-pre-1..5 validados via TASK 0 7.13c REJECT com dono
3. `discuss_walkthrough_fluxo_real` — Origem da phase: dor concreta operador ("apaga 2 digita 30")
4. `gate_motor_grep_use_name_only` — Task 4 dual gate (b) usou `--name-only`
5. `grep_global_antes_de_fix_pontual` — Task 1+2+3 verificaram callsites alinhados (NovoContrato + AdicionarDocumento + Inp)

### Novos descobertos pós-UAT (criar em LEARNINGS):
6. `grep_state_pattern_paralelo_em_componentes_relacionados` — Plan original buscou IMPORT consumers; deveria ter buscado tb state-pattern paralelo (`useState({indexador: ...})` ou helper standalone como `initEditForm`). DetalheContrato.jsx escapou da varredura inicial.
7. `independent_bug_during_uat_does_not_block_phase` — Aceitar shipped parcial + flag bug independente é pattern saudável quando UAT custou múltiplas horas e bug é não-determinístico fora-escopo.

## Phase 7.13d.bug2 BACKLOG (deferred)

**Severidade:** BAIXA-MÉDIA
**Sintoma:** "input N → DB N-x" em DetalheContrato edit (15→12, 25→24, deltas inconsistentes)
**Causa:** indeterminada após investigação read-only do pipeline cliente
**Mitigação prática:** operador raramente edita contratos existentes; motor preserva cálculos via dividas
**Workaround:** "salvar 2x preserva valor" reportado pelo orchestrator
**Próxima sessão:** Network tab capture + pg_trigger audit + testar browser anônimo

## Lições principais

1. **Plan grep audit incompleto** — buscar consumers via IMPORT não captura state-pattern paralelo via helper standalone (`initEditForm`). DetalheContrato escapou.
2. **Pipeline lossless ≠ bug ausente** — código local pode ser lossless mas anomalia ainda existe. Causas browser-side/DB-side requerem capture empírico (Network tab + Supabase audit).
3. **Pragmatismo sob fadiga UAT** — pattern saudável aceitar shipped parcial + flag independent bug em vez de over-investigar não-determinístico.
4. **Motor lê das dividas** — corrupção header contratos_dividas tem impacto declarativo apenas; cálculos jurídicos preservados via dividas. Reduz urgência fix do bug2.
