# Phase 7.13d — Encargos Defaults Vazios (CONTEXT)

**Severidade:** BAIXA — UX cleanup, não bug financeiro
**Milestone:** v1.4 — Pagamentos por Contrato + PDF Demonstrativo
**Origem:** descoberta na Phase 7.13c REJECT (2026-04-30 ABORT GATE D-pre-7.3) durante investigação pré-discuss da 7.13d UI cleanup. TASK 0 com dono revelou que dor real era OUTRA: fricção de cadastro no NovoContrato wizard.
**autonomous:** false (UAT humano obrigatório antes de tag)
**Discuss formal:** SKIPPED — briefing v2 (sessão 2026-04-30) substantivo, Q1-Q5 respondidas pelo orchestrator.

---

## 1. Resumo do problema

Wizard `NovoContrato` (`src/components/NovoContrato.jsx` L7-16) vem com defaults arbitrários hardcoded:

```js
const ENCARGOS_PADRAO = {
  indexador: "igpm",                  // ⚠ default arbitrário
  multa_pct: "2",                     // ⚠ DOR REAL — operador apaga sempre
  juros_tipo: "fixo_1",               // ⚠ default arbitrário
  juros_am: "1",                      // ⚠ default arbitrário
  honorarios_pct: "10",               // ⚠ default arbitrário
  despesas: "0",                      // ✅ default semântico (zero = sem despesas)
  art523_opcao: "nao_aplicar",        // ✅ default jurídico (Art.523 opt-in)
  data_inicio_atualizacao: "",        // ✓ já vazio
};
```

**Comportamento atual reportado pelo dono em UAT 2026-04-30:**

- Operador APAGA "2" e digita valor real do termo a cada novo cadastro
- Cada termo de acordo varia (multa 10/20/30%, honor 0/10%, juros taxa_legal/fixo_1/outros) — sem padrão repetível
- Defaults atrapalham mais que ajudam — fricção UX direta

**Validação atual** (`NovoContrato.jsx` L38):

```js
const podesSalvar = !!devedor_id;  // ⚠ só checa devedor — submit em branco passa hoje
```

**Cascata em AdicionarDocumento** (`src/components/AdicionarDocumento.jsx` L28-37):

```js
// state encargos com fallback DUPLO contrato.X ?? "default_velho"
indexador:    contrato.indice_correcao         ?? "igpm",        // ⚠ inconsistente com Path E
multa_pct:    String(contrato.multa_percentual ?? "2"),          // ⚠ idem
juros_tipo:   contrato.juros_tipo              ?? "fixo_1",      // ⚠ idem
juros_am:     String(contrato.juros_am_percentual ?? "1"),       // ⚠ idem
honorarios_pct: String(contrato.honorarios_percentual ?? "10"),  // ⚠ idem
```

Se contrato pós-Path E vier com encargos null, fallback resgata defaults velhos → defeats Path E spirit.

---

## 2. Decisão arquitetural locked: Path E

**Path E (locked PAUSA #1 2026-04-30):**

- Defaults vazios em campos críticos (5 campos)
- Validação client-side obrigatória bloqueia submit
- Dropdowns ganham "— Selecione —" disabled como primeira opção
- art523_opcao mantém "nao_aplicar" (default jurídico semântico)
- data_inicio_atualizacao vazio (operador digita data do termo)
- despesas mantém "0" (default semântico zero)
- AdicionarDocumento alinha (fallback `?? ""` em vez de `?? "default_velho"`)

**Comportamento esperado pós-fix:**

| Campo | Default ATUAL | Default PATH E | Tipo |
|---|---|---|---|
| `indice_correcao` (indexador) | `"igpm"` | `""` (UI: "— Selecione —") | crítico |
| `multa_percentual` (multa_pct) | `"2"` | `""` | crítico |
| `juros_tipo` | `"fixo_1"` | `""` (UI: "— Selecione —") | crítico |
| `juros_am_percentual` (juros_am) | `"1"` | `""` | crítico (se juros_tipo === "outros") |
| `honorarios_percentual` (honorarios_pct) | `"10"` | `""` | crítico |
| `despesas` | `"0"` | `"0"` | semântico (mantém) |
| `art523_opcao` | `"nao_aplicar"` | `"nao_aplicar"` | semântico (mantém) |
| `data_inicio_atualizacao` | `""` | `""` | já vazio |

---

## 3. D-pre lockeds (5)

### D-pre-1 — 5 campos críticos com defaults vazios

`indice_correcao`, `multa_percentual`, `juros_tipo`, `juros_am_percentual`, `honorarios_percentual` começam vazios em `NovoContrato.ENCARGOS_PADRAO` e em `AdicionarDocumento` initial state quando contrato.X é null.

**Condicional `juros_am_percentual`:** crítico apenas se `juros_tipo === "outros"` (outros tipos como `"taxa_legal_406_12"` ou `"fixo_1"` têm fórmula implícita ou fixa, não exigem `juros_am` explícito).

### D-pre-2 — 2 campos com default semântico mantido

- `despesas`: mantém `"0"` (zero é estado semântico legítimo "sem despesas adicionais")
- `art523_opcao`: mantém `"nao_aplicar"` (default jurídico explícito do Art.523 CPC, não arbitrário — Art.523 só aplica se opt-in)

### D-pre-3 — Validação client-side bloqueia submit em branco

`NovoContrato.podesSalvar` retorna `false` se qualquer dos 5 campos críticos estiver vazio. Botão "Criar Contrato" disabled. Mesmo pattern em `AdicionarDocumento.podesSalvar`. `handleSalvar` adiciona early-return com toast em campos vazios (defesa em profundidade).

**Validação condicional `juros_am`:** `podesSalvar` checa `juros_am` vazio APENAS quando `juros_tipo === "outros"`. Plan Task 1 implementa lógica condicional. Mesma lógica em `handleSalvar` early-return e em `AdicionarDocumento.podesSalvar`.

### D-pre-4 — Dropdowns ganham "— Selecione —" disabled primeira opção

`INDICE_OPTIONS` + `JUROS_OPTIONS` (em `utils/correcao.js`) recebem `<option value="" disabled>— Selecione —</option>` como primeira opção, OU é renderizado condicionalmente em `DiretrizesContrato.jsx` quando `value === ""`. Decisão de implementação no Plan Task 3 (verificar arquivo `correcao.js` antes).

### D-pre-5 — AdicionarDocumento alinha (fallback `?? ""`)

`AdicionarDocumento` state encargos remove fallbacks hardcoded `?? "default_velho"`, substitui por `?? ""`. Validação obrigatória nos mesmos 5 campos críticos no submit do documento. Defensive coding limpo: se contrato vazio, doc vazio também.

---

## 4. Plan structure (1 plan único)

**Por que 1 plan e não 3 (pattern 7.13b/7.14):**

- Severidade BAIXA
- Escopo trivial: ~25-30 linhas em 2-3 arquivos
- Sem mudança de schema, sem mudança de motor, sem mudança de service layer
- D-01 INTOCADO trivialmente
- 1 commit atomic é suficiente

**Tasks estimadas:**

| Task | Descrição | Arquivo | Linhas estimadas |
|---|---|---|---|
| 1 | NovoContrato defaults vazios + validação | `NovoContrato.jsx` | ~15 |
| 2 | AdicionarDocumento fallbacks alinhados + validação | `AdicionarDocumento.jsx` | ~10 |
| 3 | INDICE_OPTIONS + JUROS_OPTIONS "— Selecione —" disabled | `utils/correcao.js` ou `DiretrizesContrato.jsx` | 0-5 |
| 4 | Build + lint + npm test (motor regression INTOCADO, esperado 34/34 PASS) | — | — |
| 5 | UAT humano (5 SCs abaixo) | — | — |

---

## 5. Invariantes

- **D-01 motor INTOCADO** trivialmente (UI-only — `devedorCalc.js` não tocado)
- **Schema INTOCADO** (zero migration)
- **Service layer INTOCADO** (`services/contratos.js` inalterado)
- **Contratos existentes** não afetados (Path E só muda defaults de novos cadastros — back-compat total)
- **Edit de contrato em DetalheContrato** não afetado (continua editando valores existentes)
- **34/34 motor regression** PASS esperado sem mudanças

---

## 6. UAT scenarios

| # | Cenário | Resultado esperado |
|---|---|---|
| **SC1** | Acessar wizard NovoContrato → observar campos críticos | Vazios visíveis (multa, juros %, honor %); dropdowns mostram "— Selecione —"; despesas "0"; art523 "nao_aplicar" |
| **SC2** | Tentar submeter em branco (só preencher devedor + credor) | Botão "Criar Contrato" disabled OU toast bloqueador "preencha todos os encargos" |
| **SC3** | Digitar multa 30% + juros_tipo "outros" + juros_am 1.5% + honor 0% + INPC + submit | Contrato persiste com valores reais; toast success; redireciona pra DetalheContrato |
| **SC4** | Edit contrato existente (criado pré-Path E) | Funciona normal; encargos antigos preservados; sem regressão |
| **SC5** | AdicionarDocumento em contrato pós-Path E (com encargos preenchidos pelo operador) | Carrega valores do contrato; permite editar por documento; submit OK |

---

## 7. Memory feedbacks aplicados

- `uat_humano_pega_bugs_que_gates_automatizados_nao_pegam` (TASK 0 redirecionou phase de Path C errado pra Path E real)
- `post_hoc_d_pre_must_be_validated_in_child_phase` (lição NOVA da 7.13c REJECT — TASK 0 com dono ANTES de discuss formal)
- `discuss_walkthrough_fluxo_real` (caso de uso real do operador "apaga 2% e digita 30%" detectou friction concreto)

**Memory feedback NOVO planejado** (criar AO FINAL da phase, não agora):

```
feedback_validate_phase_premise_with_user_use_case_first
```

Why: Phase 7.13d ORIGINAL (UI Diretrizes confusa em DetalheContrato) era inferência técnica do orchestrator pós-7.13c REJECT. Operador real tinha DOR DIFERENTE (defaults wizard). TASK 0 com caso de uso real do operador ("apaga 2% e digita 30% toda vez") redirecionou phase pra problema real, economizando ~70%+ de escopo errado.

How to apply: phases nascidas como POST-HOC de outra phase devem ter TASK 0 com dono usando CASO DE USO REAL ("descreve o passo-a-passo que te incomoda") em vez de pergunta abstrata sobre tecnologia ("a UI X é confusa?"). Pattern complementar a `post_hoc_d_pre_must_be_validated_in_child_phase` — esse exige validação fresca da premissa; este especifica O QUE perguntar (caso de uso, não dedução técnica).

---

## 8. Estado físico no momento do CONTEXT.md commit

- HEAD pai: `f860c8a` (Phase 7.13c REJECT pushed)
- HEAD submódulo `src/mr-3`: `6f69989` (Phase 7.13b SHIPPED, INALTERADO)
- Working tree: 33 D + 11 M + 23 ?? + 1 ? (pre-existing fora-escopo intacto)
- D-01 motor INTOCADO cumulative
- Cadeia origin/master sincronizada (0 ahead/behind)
