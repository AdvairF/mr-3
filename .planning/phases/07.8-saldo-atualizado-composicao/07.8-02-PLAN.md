---
phase: 07.8-saldo-atualizado-composicao
plan: 02
type: execute
wave: 2
depends_on:
  - 07.8-01
files_modified:
  - src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx
autonomous: false   # D-15: modo cansado — pausa #2 (diff review pré-commit) antes do feat(07.8-02) no submodule main. SEM PUSH.
requirements: []    # Gap closure — sem REQ-IDs formais. Contrato = SC-1, SC-3, SC-7 em 07.8-CONTEXT.md §6 + D-04, D-05, D-18.

must_haves:
  truths:
    - "Grid Resumo Financeiro passa de `repeat(3, 1fr)` para `repeat(4, 1fr)` QUANDO `contrato.num_documentos > 0` (D-04)"
    - "Grid VOLTA a `repeat(3, 1fr)` QUANDO `contrato.num_documentos === 0` — coluna Saldo Atualizado não renderiza (D-05, SC-3)"
    - "Nova coluna label EXATO `Saldo Atualizado` + valor formatado via `fmtBRL` (D-04)"
    - "Valor da coluna vem de `useMemo` chamando `calcularDetalheEncargosContrato(dividas, allPagamentosDivida, hoje)` — deps `[dividas, allPagamentosDivida, hoje]` (D-18)"
    - "Coluna é clickable: `cursor: 'pointer'` + hover visual sutil (border mudança ou boxShadow), mas onClick é no-op NESTA plan (plan 07.8-03 wire modal)"
    - "Mesma estética das outras 3 colunas: fontSize 22, fontFamily Space Grotesk, padding, borderRadius, background branco (D-04 'mesma card')"
    - "Import de `calcularDetalheEncargosContrato` adicionado no import block de services/pagamentos.js (ou utils/devedorCalc.js — confirmar arquivo origem)"
    - "Motor Art.354 intocado: git diff HEAD -- src/utils/devedorCalc.js src/services/pagamentos.js src/services/dividas.js retorna vazio (D-01)"
    - "`calcularTotaisContratoNominal` e `total_pago`/`saldo_restante` (Phase 7.3) preservados — 'Em Aberto' continua nominal (CONTEXT §4 'Fora do escopo' último item)"
    - "npm run test:regressao 10/10 verde pós-commit (motor intocado + shield da 07.8-01)"
    - "npm run build exit 0 pós-commit"
    - "1 commit local submodule mr-3 branch main subject EXATO `feat(07.8-02): 4ª coluna Saldo Atualizado em DetalheContrato` — SEM PUSH"
  artifacts:
    - path: "src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx"
      provides: "Resumo Financeiro com 4 colunas (Valor Total / Total Pago / Em Aberto / Saldo Atualizado) quando num_documentos>0; 3 colunas quando num_documentos===0"
      contains: "repeat(4, 1fr)"
  key_links:
    - from: "DetalheContrato.jsx (import block)"
      to: "calcularDetalheEncargosContrato (utils/devedorCalc.js — export da plan 07.8-01)"
      via: "import nomeado no topo do arquivo"
      pattern: "import\\s*\\{[^}]*calcularDetalheEncargosContrato[^}]*\\}\\s*from"
    - from: "DetalheContrato.jsx (cálculo do saldo atualizado)"
      to: "useMemo sobre `calcularDetalheEncargosContrato(dividas, allPagamentosDivida, hoje)`"
      via: "hook React com deps `[dividas, allPagamentosDivida, hoje]`"
      pattern: "useMemo\\(\\s*\\(\\)\\s*=>\\s*calcularDetalheEncargosContrato"
    - from: "Resumo Financeiro (render condicional)"
      to: "coluna 'Saldo Atualizado' visível se `contrato.num_documentos > 0` (D-05)"
      via: "condicional no array de colunas + gridTemplateColumns calculado"
      pattern: "contrato\\.num_documentos\\s*>\\s*0|num_documentos\\s*===\\s*0|repeat\\((3|4), 1fr\\)"
---

<objective>
Modificar o bloco "Resumo Financeiro" de `src/components/DetalheContrato.jsx` (linhas 582-598 atuais) para adicionar uma 4ª coluna "Saldo Atualizado" que renderiza o saldo devedor atualizado pelo Art. 354 CC (correção + multa + juros + honorários sobre saldo nominal). A coluna é clickable (cursor:pointer + hover sutil) mas o `onClick` é no-op nesta plan — plan 07.8-03 conecta o modal de decomposição.

Comportamento condicional (D-05):
- Quando `contrato.num_documentos > 0`: grid `repeat(4, 1fr)` com as 4 colunas (Valor Total / Total Pago / Em Aberto / Saldo Atualizado).
- Quando `contrato.num_documentos === 0`: grid volta a `repeat(3, 1fr)` — coluna "Saldo Atualizado" não aparece (contrato vazio pós-Phase 7.7).

Purpose: Advogado vê no painel do contrato o saldo atualizado Art.354 ao lado do saldo nominal ("Em Aberto" da Phase 7.3), sem precisar abrir DetalheDivida parcela a parcela. Prepara visualmente a UX para o modal de composição (plan 07.8-03).

Output:
- 1 arquivo modificado: `src/components/DetalheContrato.jsx`:
  - Import de `calcularDetalheEncargosContrato` adicionado ao import block existente (linha ~9-12).
  - `useMemo` novo para `detalheEncargosContrato` chamando o adapter.
  - Bloco "Resumo Financeiro" (linhas 582-598) refatorado com grid dinâmico + 4ª coluna condicional.
- 1 commit local no submodule `src/mr-3/mr-cobrancas` branch `main` com subject EXATO `feat(07.8-02): 4ª coluna Saldo Atualizado em DetalheContrato` (D-15).
- **Zero commits no repo pai nesta plan** — bump é 07.8-04 Task 2.
- **Zero push** em qualquer momento. Zero tag criada.

Contexto de conformidade:
- D-01 Motor Art.354 intocado — só DetalheContrato.jsx muda.
- D-04 Grid repeat(3)→repeat(4), nova coluna clickable cursor:pointer + hover sutil, mesma estética (fontSize 22, Space Grotesk).
- D-05 Hide coluna quando num_documentos===0.
- D-14 Decomposição AGREGADA contrato-level (não per-parcela) — adapter já entrega isso.
- D-15 Modo cansado — plan autonomous:false, Task 3 é pausa #2.
- D-17 Submodule main, zero push sem autorização.
- D-18 useMemo cacheia — deps [dividas, allPagamentosDivida, hoje].
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07.8-saldo-atualizado-composicao/07.8-CONTEXT.md

<!-- Plan predecessora (adapter consumido neste plan): -->
@.planning/phases/07.8-saldo-atualizado-composicao/07.8-01-PLAN.md

<!-- Precedente do padrão "edit UI limpo, SEM PUSH, pausa pré-commit" (seguir estrutura): -->
@.planning/phases/07.7-excluir-documento-individual/07.7-02-PLAN.md

<interfaces>
<!-- Interfaces-chave do codebase atual. Executor usa DIRETO sem explorar. -->

From src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx linhas 1-12 (imports EXISTENTES — estender, NÃO reescrever):
```javascript
import { useState, useEffect, useMemo } from "react";
import toast from "react-hot-toast";
import Btn from "./ui/Btn.jsx";
import AtrasoCell from "./AtrasoCell.jsx";
import AdicionarDocumento from "./AdicionarDocumento.jsx";
import DiretrizesContrato from "./DiretrizesContrato.jsx";
import TabelaParcelasEditaveis from "./TabelaParcelasEditaveis.jsx";
import { Inp } from "./ui/Inp.jsx";
import { listarDocumentosPorContrato, editarContrato, cascatearCredorDevedor, registrarEvento, listarHistorico,
         registrarPagamentoContrato, excluirPagamentoContrato, listarPagamentosContrato, excluirContrato,
         calcularTotaisContratoNominal, atualizarParcelasCustom, excluirDocumento } from "../services/contratos.js";
import { listarPagamentos, calcularSaldoPorDividaIndividual } from "../services/pagamentos.js";
```

**Ação na Task 1:** ADICIONAR 1 NOVO import (linha nova) após o import de `pagamentos.js` (linha 12):
```javascript
import { calcularDetalheEncargosContrato } from "../utils/devedorCalc.js";
```

**Por que import separado:** `calcularDetalheEncargosContrato` está em `utils/devedorCalc.js` (plan 07.8-01), não em `services/pagamentos.js` — import direto do utils é mais limpo que re-exportar pelo service. `useMemo` já está disponível no import de React linha 1.

From src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx props do componente (linha 109-120):
```javascript
export default function DetalheContrato({
  contrato,
  dividas,              // array de parcelas do contrato
  devedores,
  credores,
  allPagamentos,
  allPagamentosDivida,  // pagamentos em pagamentos_divida para todas as parcelas
  hoje,                 // "YYYY-MM-DD"
  onVoltar,
  onVerDetalhe,
  onCarregarTudo,
}) {
```

**Estas 3 props já existem e são as deps do useMemo: `dividas`, `allPagamentosDivida`, `hoje`.**

From src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx:491 (helper pré-existente Phase 7.3 — PRESERVAR):
```javascript
const { total_pago, saldo_restante } = calcularTotaisContratoNominal(dividas || [], allPagamentosDivida);
```

**Essa linha fica INTOCADA.** O "Em Aberto" continua nominal (CONTEXT §4 "Fora do escopo" último item). A 4ª coluna é um ADD, não um replace.

From src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx:582-598 (bloco ATUAL do Resumo Financeiro — TEMPLATE do refactor):
```jsx
{/* 3. Financial summary card */}
<div style={{ background: "linear-gradient(135deg,#f0fdf4 0%,#fff 100%)", borderRadius: 16, padding: "16px 24px", border: "1px solid #bbf7d0", marginBottom: 16 }}>
  <p style={{ fontWeight: 700, fontSize: 13, color: "#0f172a", marginBottom: 12 }}>Resumo Financeiro</p>
  <div style={{ display: "grid", gridTemplateColumns: "repeat(3, 1fr)", gap: 12 }}>
    {[
      { label: "Valor Total",    value: fmtBRL(contrato.valor_total) },
      { label: "Total Pago",     value: fmtBRL(total_pago) },
      { label: "Em Aberto",      value: fmtBRL(saldo_restante) },
    ].map(({ label, value }) => (
      <div key={label} style={{ background: "#fff", borderRadius: 12, padding: "12px 16px", border: "1px solid #e2e8f0", textAlign: "center" }}>
        <p style={{ fontSize: 11, fontWeight: 700, color: "#64748b", textTransform: "uppercase", letterSpacing: ".5px", marginBottom: 4 }}>{label}</p>
        <p style={{ fontSize: 22, fontWeight: 700, color: "#0f172a", fontFamily: "'Space Grotesk',sans-serif", margin: 0 }}>{value}</p>
      </div>
    ))}
  </div>
  <p style={{ fontSize: 11, color: "#94a3b8", fontStyle: "italic", marginTop: 8, marginBottom: 0 }}>(amortização sequencial conforme Art. 354 CC)</p>
</div>
```

From src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx fmtBRL (linha 14 — existente):
```javascript
function fmtBRL(v) { if (v == null || v === "") return "—"; return Number(v).toLocaleString("pt-BR", { style: "currency", currency: "BRL" }); }
```

**Reutilizar `fmtBRL` para formatar `detalheEncargosContrato.saldoAtualizado`.**
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Import + useMemo + refactor do bloco Resumo Financeiro com 4ª coluna condicional</name>
  <files>src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx</files>

  <read_first>
    - `src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx` linhas 1-15 (import block — posição exata para adicionar `calcularDetalheEncargosContrato` import)
    - `src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx` linhas 109-125 (assinatura do componente — confirmar props `dividas`, `allPagamentosDivida`, `hoje` disponíveis)
    - `src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx` linhas 485-495 (onde `total_pago`/`saldo_restante` são calculados via helper nominal Phase 7.3 — NÃO tocar; adicionar useMemo LOGO DEPOIS nessa região OU antes do return)
    - `src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx` linhas 580-600 (bloco Resumo Financeiro — região do refactor)
    - `.planning/phases/07.8-saldo-atualizado-composicao/07.8-CONTEXT.md` §5 D-04 (grid 3→4), D-05 (hide quando num_documentos===0), D-18 (useMemo deps)
  </read_first>

  <action>
    **Passo A — Adicionar import (após linha 12, antes da linha 13 do fmtBRL):**

    Linha a inserir:
    ```javascript
    import { calcularDetalheEncargosContrato } from "../utils/devedorCalc.js";
    ```

    `useMemo` já está importado de React (linha 1); nenhum outro import necessário.

    **Passo B — Adicionar useMemo após o cálculo nominal (linha ~492, após `const { total_pago, saldo_restante } = calcularTotaisContratoNominal(...)`):**

    Inserir linha em branco e depois:
    ```javascript
    // Phase 7.8 — saldo atualizado Art.354 (correção + multa + juros + honorários).
    // Adapter thin calcularDetalheEncargosContrato delega ao motor existente (plan 07.8-01).
    // useMemo cacheia — recalcula só quando dividas/pagamentos/hoje mudam (D-18).
    const detalheEncargosContrato = useMemo(
      () => calcularDetalheEncargosContrato(dividas || [], allPagamentosDivida || [], hoje),
      [dividas, allPagamentosDivida, hoje]
    );
    const saldoAtualizadoContrato = detalheEncargosContrato?.saldoAtualizado ?? 0;
    ```

    **Detalhes obrigatórios do useMemo:**
    - Deps EXATAS: `[dividas, allPagamentosDivida, hoje]` — 3 entradas.
    - `dividas || []` e `allPagamentosDivida || []` — normalização defensiva caso props venham undefined em primeiro render.
    - Nome da variável `detalheEncargosContrato` — usado no body (saldoAtualizado) e também passado pro modal na plan 07.8-03. Nome exato importa para grep.
    - Nome da variável `saldoAtualizadoContrato` — atalho derivado. Opcional esse derivado: se executor preferir, pode fazer inline `detalheEncargosContrato?.saldoAtualizado ?? 0` no render. Ambas formas válidas; grep no AC cobre ambas (`detalheEncargosContrato`).

    **Passo C — Refatorar bloco Resumo Financeiro (linhas 582-598 atuais):**

    Substituir o bloco INTEIRO por:
    ```jsx
    {/* 3. Financial summary card — Phase 7.8: +4ª coluna Saldo Atualizado Art.354 quando num_documentos>0 (D-04, D-05) */}
    <div style={{ background: "linear-gradient(135deg,#f0fdf4 0%,#fff 100%)", borderRadius: 16, padding: "16px 24px", border: "1px solid #bbf7d0", marginBottom: 16 }}>
      <p style={{ fontWeight: 700, fontSize: 13, color: "#0f172a", marginBottom: 12 }}>Resumo Financeiro</p>
      {(() => {
        const temDocumentos = (contrato.num_documentos || 0) > 0;
        const colunas = [
          { label: "Valor Total",      value: fmtBRL(contrato.valor_total), clickable: false },
          { label: "Total Pago",       value: fmtBRL(total_pago),           clickable: false },
          { label: "Em Aberto",        value: fmtBRL(saldo_restante),       clickable: false },
        ];
        if (temDocumentos) {
          colunas.push({
            label: "Saldo Atualizado",
            value: fmtBRL(saldoAtualizadoContrato),
            clickable: true,
          });
        }
        const nCols = colunas.length;
        return (
          <div style={{ display: "grid", gridTemplateColumns: `repeat(${nCols}, 1fr)`, gap: 12 }}>
            {colunas.map(({ label, value, clickable }) => (
              <div
                key={label}
                onClick={clickable ? () => { /* Phase 7.8-03: abrir DecomposicaoSaldoModal */ } : undefined}
                style={{
                  background: "#fff",
                  borderRadius: 12,
                  padding: "12px 16px",
                  border: clickable ? "1px solid #86efac" : "1px solid #e2e8f0",
                  textAlign: "center",
                  cursor: clickable ? "pointer" : "default",
                  transition: clickable ? "box-shadow .15s, border-color .15s" : undefined,
                }}
                onMouseEnter={clickable ? (e) => {
                  e.currentTarget.style.boxShadow = "0 4px 12px rgba(34,197,94,.2)";
                  e.currentTarget.style.borderColor = "#22c55e";
                } : undefined}
                onMouseLeave={clickable ? (e) => {
                  e.currentTarget.style.boxShadow = "none";
                  e.currentTarget.style.borderColor = "#86efac";
                } : undefined}
                title={clickable ? "Clique para ver composição (Art. 354 CC)" : undefined}
              >
                <p style={{ fontSize: 11, fontWeight: 700, color: "#64748b", textTransform: "uppercase", letterSpacing: ".5px", marginBottom: 4 }}>{label}</p>
                <p style={{ fontSize: 22, fontWeight: 700, color: "#0f172a", fontFamily: "'Space Grotesk',sans-serif", margin: 0 }}>{value}</p>
              </div>
            ))}
          </div>
        );
      })()}
      <p style={{ fontSize: 11, color: "#94a3b8", fontStyle: "italic", marginTop: 8, marginBottom: 0 }}>(amortização sequencial conforme Art. 354 CC)</p>
    </div>
    ```

    **Detalhes obrigatórios do refactor:**
    - IIFE (`(() => { ... })()`) dentro do card para permitir lógica condicional limpa no JSX. Alternativa válida: extrair pra função helper no topo do componente. Prefiro IIFE pois mantém a mudança localizada.
    - `gridTemplateColumns: \`repeat(${nCols}, 1fr)\`` — interpolação dinâmica. 3 colunas quando `num_documentos===0`, 4 quando `>0`. Usa template string, não CSS `--var`.
    - `clickable: false` para as 3 colunas antigas (Valor Total, Total Pago, Em Aberto); `clickable: true` SÓ para "Saldo Atualizado".
    - `onClick` da coluna clickable é no-op agora (comentário `// Phase 7.8-03`) — plan 07.8-03 wire a abertura do modal.
    - Cursor: `pointer` se clickable, `default` caso contrário.
    - Hover visual sutil: border muda para verde + boxShadow verde suave em mouseenter, reverte em mouseleave. Só no clickable.
    - `title` HTML attribute para tooltip "Clique para ver composição (Art. 354 CC)" — ajuda UX sem depender de tooltip custom.
    - Preservar o `<p>` de rodapé "(amortização sequencial conforme Art. 354 CC)" — fica igual, FORA da IIFE (é nota do card, não da tabela).

    **O que NÃO fazer nesta plan:**
    - NÃO implementar o modal `DecomposicaoSaldoModal.jsx` (plan 07.8-03).
    - NÃO tocar `calcularTotaisContratoNominal` (Phase 7.3 helper) nem `total_pago` / `saldo_restante` — "Em Aberto" continua nominal.
    - NÃO tocar outras regiões do componente (historico, documentos, pagamentos, edição, etc).
    - NÃO tocar `src/utils/devedorCalc.js`, `src/services/pagamentos.js`, `src/services/dividas.js`, `src/services/contratos.js` (invariante motor + escopo cirúrgico).
    - NÃO adicionar `useState` novo — useMemo é suficiente (cacheia por render).
  </action>

  <verify>
    <automated>cd src/mr-3/mr-cobrancas &amp;&amp; npm run build</automated>
  </verify>

  <acceptance_criteria>
    <!-- Cada item é grep-verificável. Rodar da raiz do submodule src/mr-3/mr-cobrancas. -->
    - **Import adicionado:** `grep -c "import { calcularDetalheEncargosContrato } from \"../utils/devedorCalc.js\"" src/components/DetalheContrato.jsx` retorna **1** (aceita também variação com aspas simples)
    - **Alternativa grep (mais tolerante a aspas):** `grep -cE "import[^;]*calcularDetalheEncargosContrato[^;]*from[^;]*devedorCalc" src/components/DetalheContrato.jsx` retorna **≥1**
    - **useMemo com adapter:** `grep -cE "useMemo\\(\\s*\\(\\)\\s*=>\\s*calcularDetalheEncargosContrato" src/components/DetalheContrato.jsx` retorna **1**
    - **Deps corretas do useMemo (D-18):** `grep -cE "\\[dividas,\\s*allPagamentosDivida,\\s*hoje\\]" src/components/DetalheContrato.jsx` retorna **≥1** (o useMemo da phase 7.8)
    - **Variável detalheEncargosContrato presente:** `grep -c "detalheEncargosContrato" src/components/DetalheContrato.jsx` retorna **≥2** (declaração + uso)
    - **Grid dinâmico 3/4 (D-04 + D-05):** `grep -cE 'gridTemplateColumns.*repeat\\(\\$\\{.*nCols' src/components/DetalheContrato.jsx` retorna **1** (template string com nCols). Aceita alternativa explícita `repeat(${colunas.length}, 1fr)`.
    - **Label literal "Saldo Atualizado":** `grep -c "\"Saldo Atualizado\"" src/components/DetalheContrato.jsx` retorna **1**
    - **Condicional num_documentos (D-05):** `grep -cE "contrato\\.num_documentos.*>\\s*0|\\(contrato\\.num_documentos\\s*\\|\\|\\s*0\\)\\s*>\\s*0" src/components/DetalheContrato.jsx` retorna **≥1** (guard que controla presença da 4ª coluna)
    - **Hide path verificável:** `grep -c "temDocumentos" src/components/DetalheContrato.jsx` retorna **≥1** (nome da flag calculada)
    - **cursor:pointer no clickable:** `grep -cE 'cursor:\\s*clickable\\s*\\?\\s*"pointer"' src/components/DetalheContrato.jsx` retorna **1**
    - **Hover visual (box-shadow ou border transition):** `grep -cE 'onMouseEnter.*boxShadow' src/components/DetalheContrato.jsx` retorna **≥1** (pode estar no objeto style inline ou handler)
    - **Phase 7.3 helper preservado:** `grep -c "calcularTotaisContratoNominal" src/components/DetalheContrato.jsx` retorna **≥1** (helper nominal Phase 7.3 NÃO removido)
    - **Label "Em Aberto" preservado:** `grep -c "\"Em Aberto\"" src/components/DetalheContrato.jsx` retorna **1** (coluna nominal Phase 7.3 continua)
    - **Comentário Phase 7.8 no card:** `grep -cE "Phase 7\\.8" src/components/DetalheContrato.jsx` retorna **≥1** (comentário de referência histórica)
    - **Motor Art.354 invariante (D-01) — arquivos irmãos vazios:** `git diff HEAD -- src/utils/devedorCalc.js` retorna **vazio**; `git diff HEAD -- src/services/pagamentos.js` retorna **vazio**; `git diff HEAD -- src/services/dividas.js` retorna **vazio**; `git diff HEAD -- src/services/__tests__/calculos.test.js` retorna **vazio** (teste shield da 07.8-01 preservado sem alteração)
    - **Escopo do diff desta plan:** `git diff --name-only HEAD` retorna EXATAMENTE **1 linha**: `src/components/DetalheContrato.jsx`
    - **Build exit 0:** `npm run build` exit 0 — qualquer erro JSX (IIFE mal fechada, template string mal escapada) aparece aqui.
    - **Regressão 10/10:** `npm run test:regressao` exit 0 (9/9 TJGO + 1 shield da 07.8-01 — motor intocado nesta plan).
  </acceptance_criteria>

  <done>
    `DetalheContrato.jsx` importa `calcularDetalheEncargosContrato`, calcula `detalheEncargosContrato` via useMemo com deps `[dividas, allPagamentosDivida, hoje]`, e o bloco Resumo Financeiro renderiza 4 colunas quando `contrato.num_documentos > 0` (a 4ª clickable/cursor:pointer/hover verde) e volta a 3 colunas quando `=== 0`. `calcularTotaisContratoNominal` + "Em Aberto" preservados. Build exit 0, regressão 10/10 pass. Motor Art.354 intocado (arquivos irmãos diff vazio). Ainda não commitado — Task 2 tem gates determinísticos, Task 3 tem pausa.
  </done>
</task>

<task type="auto">
  <name>Task 2: Gates determinísticos — regressão 10/10 + build exit 0 + invariante motor (antes da pausa)</name>
  <files>
    (nenhum arquivo modificado — só gates)
  </files>

  <read_first>
    - `.planning/phases/07.8-saldo-atualizado-composicao/07.8-CONTEXT.md` §5 D-01 (INV motor), D-15 (pausas — Task 3 desta plan é a pausa #2)
    - `.planning/phases/07.7-excluir-documento-individual/07.7-01-PLAN.md` Task 2 — **template literal** do padrão de gates determinísticos pré-pausa
  </read_first>

  <action>
    Rodar os 5 gates determinísticos na raiz do submodule. Se QUALQUER gate falhar, NÃO avançar para Task 3 e reportar ao usuário com o diff/output completo do gate ofensor.

    ```bash
    cd src/mr-3/mr-cobrancas

    # Gate 1 — Regressão 10/10 (invariante motor Art.354, D-01):
    npm run test:regressao
    # Esperado: exit 0. 9 TJGO + 1 shield da 07.8-01.
    # Se falhar: reportar qual teste quebrou. Causa única possível: edição indevida em
    # utils/devedorCalc.js ou services/pagamentos.js/dividas.js — executor NÃO devia tocar
    # esses. Rodar `git diff HEAD` pra auditar.

    # Gate 2 — Build limpo:
    npm run build
    # Esperado: exit 0. Falha provável: JSX mal formado em DetalheContrato.jsx
    # (IIFE sem fechamento correto, template string sem backtick, key faltando).

    # Gate 3 — Invariante motor Art.354 + arquivos adjacentes (D-01):
    git diff HEAD -- src/utils/devedorCalc.js
    git diff HEAD -- src/services/pagamentos.js
    git diff HEAD -- src/services/dividas.js
    git diff HEAD -- src/services/__tests__/calculos.test.js
    # Esperado: TODOS vazios. Qualquer byte → reportar e ABORTAR.

    # Gate 4 — Escopo do diff (garantir só DetalheContrato.jsx mexido no submodule):
    git diff --name-only HEAD
    # Esperado: EXATAMENTE "src/components/DetalheContrato.jsx".
    # Qualquer outro arquivo → reportar e aguardar confirmação.

    # Gate 5 — Sanidade semântica (confirma D-04 + D-05 + D-18 textualmente):
    grep -c "calcularDetalheEncargosContrato" src/components/DetalheContrato.jsx       # esperado: ≥2 (import + useMemo)
    grep -c "repeat(\${nCols}" src/components/DetalheContrato.jsx                      # esperado: 1 (grid dinâmico) OU
    grep -cE "repeat\\(\\$\\{(nCols|colunas\\.length)" src/components/DetalheContrato.jsx  # esperado: 1
    grep -c "\"Saldo Atualizado\"" src/components/DetalheContrato.jsx                  # esperado: 1
    grep -cE "num_documentos.*>\\s*0" src/components/DetalheContrato.jsx               # esperado: ≥1
    grep -c "useMemo" src/components/DetalheContrato.jsx                               # esperado: ≥2 (1 pré-existente em linhas ~474 + 1 novo Phase 7.8)
    ```
  </action>

  <verify>
    <automated>cd src/mr-3/mr-cobrancas &amp;&amp; npm run test:regressao &amp;&amp; npm run build &amp;&amp; [ -z "$(git diff HEAD -- src/utils/devedorCalc.js src/services/pagamentos.js src/services/dividas.js src/services/__tests__/calculos.test.js)" ] &amp;&amp; [ "$(git diff --name-only HEAD | wc -l)" = "1" ] &amp;&amp; echo "ALL GATES PASS"</automated>
  </verify>

  <acceptance_criteria>
    - `cd src/mr-3/mr-cobrancas && npm run test:regressao` exit 0 (10/10 PASS)
    - `cd src/mr-3/mr-cobrancas && npm run build` exit 0
    - `cd src/mr-3/mr-cobrancas && git diff HEAD -- src/utils/devedorCalc.js` retorna **vazio**
    - `cd src/mr-3/mr-cobrancas && git diff HEAD -- src/services/pagamentos.js` retorna **vazio**
    - `cd src/mr-3/mr-cobrancas && git diff HEAD -- src/services/dividas.js` retorna **vazio**
    - `cd src/mr-3/mr-cobrancas && git diff HEAD -- src/services/__tests__/calculos.test.js` retorna **vazio** (teste shield da 07.8-01 preservado)
    - `cd src/mr-3/mr-cobrancas && git diff --name-only HEAD` retorna EXATAMENTE `src/components/DetalheContrato.jsx` (1 linha)
    - Todos os greps semânticos do Gate 5 passam com contadores esperados
  </acceptance_criteria>

  <done>
    Todos os gates determinísticos passaram. Safe para apresentar diff review ao usuário (Task 3 pausa #2).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: PAUSA #2 — diff review antes do commit no submodule (D-15 modo cansado)</name>
  <files>
    src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx (commit local no submodule main — SEM PUSH)
  </files>

  <what-built>
    Task 1 adicionou a 4ª coluna "Saldo Atualizado" no Resumo Financeiro de `DetalheContrato.jsx`. Task 2 confirmou gates determinísticos verdes (regressão 10/10, build exit 0, motor intocado, diff only DetalheContrato.jsx).

    Esta Task 3 é a **pausa #2 das 5 pausas totais do modo cansado (D-15)**:

    1. Agente mostra ao usuário: `git status --short` + `git diff --stat HEAD` + `git diff HEAD -- src/components/DetalheContrato.jsx` no submodule.
    2. Agente confirma verbalmente: motor Art.354 intocado (diff vazio em devedorCalc/pagamentos/dividas/calculos.test.js), regressão 10/10, build exit 0, diff escopo = 1 arquivo.
    3. Aguarda autorização explícita do usuário (`approved` / `commit it` / equivalente).
    4. Após autorização: `git add src/components/DetalheContrato.jsx` + `git commit` local no submodule `main` com mensagem EXATA `feat(07.8-02): 4ª coluna Saldo Atualizado em DetalheContrato`.
    5. **Zero `git push`** em qualquer ponto. Zero commits no pai (bump é 07.8-04 Task 2).
    6. Reportar SHA do commit no final e encerrar a plan.
  </what-built>

  <read_first>
    - `.planning/phases/07.8-saldo-atualizado-composicao/07.8-CONTEXT.md` §5 D-15 (5 pausas; esta é a #2), D-17 (convenção commit local, zero push)
    - `.planning/phases/07.7-excluir-documento-individual/07.7-02-PLAN.md` Task 3 — template literal do padrão commit-no-submodule-SEM-PUSH
    - `.planning/phases/07.8-saldo-atualizado-composicao/07.8-01-PLAN.md` Task 3 — pausa #1 da mesma phase (formato já estabelecido)
  </read_first>

  <how-to-verify>

**Passo 1 — Mostrar diff completo ao usuário (PAUSA #2):**

```bash
cd src/mr-3/mr-cobrancas
git status --short
# esperado: "M src/components/DetalheContrato.jsx"

git diff --stat HEAD
# esperado: 1 file changed, +N insertions (≈45-60), N_del deletions (≈15-17 do bloco antigo substituído pelo novo)

git diff HEAD -- src/components/DetalheContrato.jsx
# diff completo — import novo + useMemo novo + bloco Resumo Financeiro refatorado.

git diff HEAD -- src/utils/devedorCalc.js src/services/pagamentos.js src/services/dividas.js src/services/__tests__/calculos.test.js
# DEVE SER VAZIO (gate invariante motor D-01 + preservação do shield 07.8-01).

git rev-parse --abbrev-ref HEAD
# esperado: main

git log origin/main..main --oneline
# esperado: ≥1 linha (commit da 07.8-01 pendente; possíveis commits de phases anteriores).
```

**Passo 2 — Reportar ao usuário (texto EXATO):**

> ```
> ## PAUSA #2 (07.8-02) — diff review antes do commit no submodule
>
> Gates determinísticos:
> - Regressão: 10/10 PASS (9 TJGO + 1 shield 07.8-01)
> - Build: exit 0
> - Motor Art.354 (devedorCalc.js + pagamentos.js + dividas.js) diff HEAD: VAZIO ✓ (invariante D-01)
> - Teste shield 07.8-01 preservado: `git diff HEAD -- src/services/__tests__/calculos.test.js` VAZIO ✓
> - Escopo do diff: APENAS src/components/DetalheContrato.jsx ✓
>
> Adicionado em DetalheContrato.jsx:
> - Import: `calcularDetalheEncargosContrato` de utils/devedorCalc.js (plan 07.8-01).
> - useMemo `detalheEncargosContrato` com deps [dividas, allPagamentosDivida, hoje] (D-18).
> - Bloco Resumo Financeiro refatorado:
>   * Grid dinâmico `repeat(${nCols}, 1fr)` — 4 colunas quando contrato.num_documentos>0,
>     3 colunas quando ===0 (D-04/D-05 — SC-3 do UAT).
>   * Nova coluna "Saldo Atualizado" clickable (cursor:pointer + hover verde + tooltip
>     "Clique para ver composição (Art. 354 CC)").
>   * onClick é no-op nesta plan — plan 07.8-03 wire DecomposicaoSaldoModal.
> - Phase 7.3 helper (calcularTotaisContratoNominal) + coluna "Em Aberto" preservados.
>
> Diff completo acima.
>
> Autoriza commit no submodule src/mr-3/mr-cobrancas branch main com subject EXATO:
> `feat(07.8-02): 4ª coluna Saldo Atualizado em DetalheContrato` ?
>
> Responda **'approved'** ou **'commit it'** para prosseguir.
> Qualquer outra coisa cancela — DetalheContrato.jsx continua modificado localmente sem commit.
> ```

**AGUARDAR resposta do usuário.** NÃO prosseguir sem autorização explícita.

Se o usuário apontar correção: aplicar, re-rodar gates Task 2, voltar a PAUSA #2. Se pedir abort: deixar DetalheContrato.jsx modificado localmente sem commit e reportar.

**Passo 3 — Após autorização: commit no submodule:**

```bash
cd src/mr-3/mr-cobrancas
git checkout main                                       # garantir que está em main
git add src/components/DetalheContrato.jsx              # APENAS este arquivo (não usar git add -A / .)
git status --short                                      # confirmar SOMENTE "M src/components/DetalheContrato.jsx" stageado

git commit -m "$(cat <<'EOF'
feat(07.8-02): 4ª coluna Saldo Atualizado em DetalheContrato

Phase 7.8 — adiciona coluna clickable "Saldo Atualizado" ao Resumo Financeiro
do contrato, ao lado das 3 colunas nominais existentes (Valor Total, Total
Pago, Em Aberto — Phase 7.3 preservada).

Grid dinâmico (D-04/D-05):
- contrato.num_documentos > 0  → repeat(4, 1fr) com 4 colunas.
- contrato.num_documentos === 0 → repeat(3, 1fr); coluna nova não aparece
  (estado válido pós-Phase 7.7 quando todo o contrato foi esvaziado).

Cálculo (D-18):
- useMemo sobre calcularDetalheEncargosContrato(dividas, allPagamentosDivida, hoje).
- Deps [dividas, allPagamentosDivida, hoje] — recalcula só quando mudam.
- Adapter delega ao motor calcularDetalheEncargos existente (plan 07.8-01).

UX (D-04):
- Mesma estética das outras 3 colunas (fontSize 22, Space Grotesk).
- Clickable: cursor:pointer + hover verde (border-color + box-shadow).
- tooltip HTML title "Clique para ver composição (Art. 354 CC)".
- onClick no-op nesta plan — plan 07.8-03 wire DecomposicaoSaldoModal.

Invariante D-01: motor Art.354 literalmente intocado.
- src/utils/devedorCalc.js + src/services/pagamentos.js + src/services/dividas.js:
  git diff HEAD vazio nesta plan.
- src/services/__tests__/calculos.test.js: git diff HEAD vazio (shield 07.8-01
  preservado sem alteração).

Regressão 10/10 PASS (9 TJGO + 1 shield 07.8-01). Build exit 0.

Próximas plans:
- 07.8-03: DecomposicaoSaldoModal + impressão A4 isolada + wire onClick.
- 07.8-04: UAT SC-1..SC-7 + bump submodule no pai (SEM PUSH).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"

git log -1 --format=%H                                   # capturar SHA full
git log -1 --format=%h                                   # SHA short
```

**Passo 4 — Reportar SHA final + estado "zero push":**

Formato no chat:
```
## Plan 07.8-02 commitada LOCALMENTE (SEM PUSH)

Submodule src/mr-3/mr-cobrancas @ main:
- <SHA_short> feat(07.8-02): 4ª coluna Saldo Atualizado em DetalheContrato

Arquivo: src/components/DetalheContrato.jsx (+~60/-17 linhas no bloco Resumo Financeiro + import + useMemo)

Invariante motor Art.354: PRESERVADO (devedorCalc.js + pagamentos.js + dividas.js + calculos.test.js diff vazio).
Regressão: 10/10 PASS.
Build: exit 0.

Commits locais pendentes no submodule main: ≥2 (07.8-01 + 07.8-02 desta phase; possíveis anteriores).
Push ainda NÃO autorizado — aguardando 07.8-03 + UAT 07.8-04.
Próxima plan: 07.8-03 (DecomposicaoSaldoModal + impressão A4).
```

  </how-to-verify>

  <acceptance_criteria>
    **PAUSA #2 autorizada:**
    - Agente executou `git status --short` + `git diff --stat HEAD` + `git diff HEAD -- src/components/DetalheContrato.jsx` e mostrou ao usuário ANTES do commit
    - Agente reportou gates Task 2 todos verdes (regressão 10/10, build 0, motor + shield preservados, escopo = 1 arquivo)
    - Usuário respondeu `approved`, `commit it`, ou equivalente explícito ANTES do commit
    - Texto do chat contém marcador `PAUSA #2` ou equivalente referenciando 07.8-02

    **Commit correto:**
    - `cd src/mr-3/mr-cobrancas && git rev-parse --abbrev-ref HEAD` retorna `main`
    - `cd src/mr-3/mr-cobrancas && git log -1 --format=%s` retorna EXATAMENTE `feat(07.8-02): 4ª coluna Saldo Atualizado em DetalheContrato`
    - `cd src/mr-3/mr-cobrancas && git log -1 --name-only --format=` lista EXATAMENTE 1 arquivo: `src/components/DetalheContrato.jsx`
    - `cd src/mr-3/mr-cobrancas && git log -1 --format=%b` contém as strings: `Phase 7.8`, `Saldo Atualizado`, `D-01`, `D-04`, `D-05`, `D-18`, `useMemo`, `repeat(4, 1fr)` OU `repeat(3, 1fr)`, `num_documentos`, `DecomposicaoSaldoModal`, `10/10`, `Co-Authored-By: Claude Opus 4.7`

    **Zero push / zero tag:**
    - `cd src/mr-3/mr-cobrancas && git log origin/main..main --oneline` retorna **≥2 linhas** (commits 07.8-01 + 07.8-02 + possíveis anteriores)
    - **NENHUM** comando `git push` executado nesta task
    - **NENHUMA** tag criada: `git tag -l "v1.4-phase7.8*"` retorna vazio

    **Invariante motor preservada pós-commit:**
    - `cd src/mr-3/mr-cobrancas && git diff HEAD~1 -- src/utils/devedorCalc.js` retorna vazio
    - `cd src/mr-3/mr-cobrancas && git diff HEAD~1 -- src/services/pagamentos.js` retorna vazio
    - `cd src/mr-3/mr-cobrancas && git diff HEAD~1 -- src/services/dividas.js` retorna vazio
    - `cd src/mr-3/mr-cobrancas && git diff HEAD~1 -- src/services/__tests__/calculos.test.js` retorna vazio (shield 07.8-01 preservado)
    - `cd src/mr-3/mr-cobrancas && git show HEAD --stat` mostra EXATAMENTE 1 arquivo: `src/components/DetalheContrato.jsx`

    **Hygiene (nenhum arquivo fora de escopo stageado):**
    - `cd src/mr-3/mr-cobrancas && git show HEAD --stat` lista SOMENTE `src/components/DetalheContrato.jsx`
  </acceptance_criteria>

  <resume-signal>
    Para prosseguir:
    - **`approved`** / **`commit it`** / **`next`** → commit criado; plan 07.8-02 completa; próxima = 07.8-03.
    - **`reject <razão>`** / **`cancel`** → DetalheContrato.jsx continua modificado localmente sem commit; agente reporta e encerra plan.
    - **`fix <detalhe>`** → agente aplica fix, re-roda gates Task 2, volta a PAUSA #2.
  </resume-signal>

  <done>
    Commit `feat(07.8-02): 4ª coluna Saldo Atualizado em DetalheContrato` existe no submodule `src/mr-3/mr-cobrancas` branch `main` com APENAS 1 arquivo stageado (DetalheContrato.jsx). 2 autorizações humanas acumuladas na phase (PAUSA #1 + PAUSA #2). Zero push, zero tag. Próxima plan: 07.8-03 (DecomposicaoSaldoModal + impressão A4 + wire onClick).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| Agente → Motor Art.354 (arquivos motor) | Invariante D-01: DetalheContrato.jsx edit NÃO pode cascata em devedorCalc.js, pagamentos.js, dividas.js. |
| Agente → Plan predecessora (teste shield 07.8-01) | `calculos.test.js` deve permanecer intocado nesta plan — diff HEAD vazio. |
| Agente → Git CLI do submodule | Comandos locais (add/commit, npm). Zero push. |
| Agente → Usuário (checkpoint PAUSA #2) | 1 PAUSA bloqueante. Autorização explícita obrigatória. |

## STRIDE Threat Register

| Threat ID | Categoria | Componente | Disposição | Mitigação |
|-----------|-----------|------------|------------|-----------|
| T-07.8-02-01 | Tampering (motor tocado indevidamente) | Edit em DetalheContrato.jsx cascata em devedorCalc.js/pagamentos.js/dividas.js | mitigate | AC explícita: `git diff HEAD -- src/utils/devedorCalc.js src/services/pagamentos.js src/services/dividas.js` VAZIO. Gate 3 da Task 2. |
| T-07.8-02-02 | Tampering (shield 07.8-01 alterado) | Edit acidental em `calculos.test.js` | mitigate | AC `git diff HEAD -- src/services/__tests__/calculos.test.js` VAZIO. Gate 3 da Task 2. |
| T-07.8-02-03 | Tampering (`calcularTotaisContratoNominal` removido) | Refactor acidental remove Phase 7.3 helper | mitigate | AC grep `calcularTotaisContratoNominal` ≥1 + grep `"Em Aberto"` = 1. D-04 "4ª coluna ADICIONAL, não replace". |
| T-07.8-02-04 | DoS (grid quebra visualmente quando num_documentos===0) | repeat(4, 1fr) com 3 itens cria espaço vazio | mitigate | D-05 explícito: quando num_documentos===0, temDocumentos=false, nCols=3, gridTemplateColumns=repeat(3, 1fr). UAT SC-3 valida. |
| T-07.8-02-05 | Elevation of Privilege (usuário fatigado aprova cegamente) | Modo cansado D-15 | accept | D-15 adiciona pausas para mitigar fadiga. Se usuário aprova sem revisar, risco aceito. |
| T-07.8-02-06 | Tampering (push acidental) | `git push` inadvertido | mitigate | AC negativa explícita "NENHUM git push". Commits locais até 07.8-04. |
| T-07.8-02-07 | Tampering (`git add -A`) | Commit inclui `.planning/STATE.md` etc | mitigate | `git add src/components/DetalheContrato.jsx` EXPLICITAMENTE. Passo 3 confirma status stageado = 1 arquivo. |
| T-07.8-02-08 | Tag criada prematuramente | `git tag v1.4-phase7.8` | mitigate | AC `git tag -l "v1.4-phase7.8*"` = vazio. Tag só em ação separada pós-UAT prod. |
| T-07.8-02-09 | Repudiation (commit body inconsistente) | Mensagem diverge do padrão | mitigate | HEREDOC com mensagem literal; AC grep de strings-chave (`D-01`, `D-04`, `D-05`, `D-18`, `useMemo`, `num_documentos`). |
| T-07.8-02-10 | Information Disclosure | Commit body revela dados internos | accept | Todos os dados expostos são intencionais. Sem secrets. |
</threat_model>

<verification>
Verificação final da plan 07.8-02 (após Task 3 completa):

1. **Submodule main (estado final):**
   - `cd src/mr-3/mr-cobrancas && git log -1 --format=%s` → `feat(07.8-02): 4ª coluna Saldo Atualizado em DetalheContrato`
   - `cd src/mr-3/mr-cobrancas && git log -1 --name-only --format=` → 1 linha: `src/components/DetalheContrato.jsx`
   - `cd src/mr-3/mr-cobrancas && git log origin/main..main --oneline` → ≥2 linhas (07.8-02 tip + 07.8-01)

2. **Invariante motor Art.354 preservada (D-01):**
   - `git diff HEAD~1 -- src/utils/devedorCalc.js src/services/pagamentos.js src/services/dividas.js src/services/__tests__/calculos.test.js` → todos vazios
   - `git show HEAD --stat` → APENAS `src/components/DetalheContrato.jsx`

3. **Testes + build:**
   - `npm run test:regressao` → exit 0 (10/10)
   - `npm run build` → exit 0

4. **UX verifiable via grep:**
   - Grid dinâmico presente (`repeat(${nCols}` ou `repeat(${colunas.length}`)
   - Condicional num_documentos>0 presente
   - Label "Saldo Atualizado" presente (1 ocorrência)
   - useMemo com adapter presente
   - Phase 7.3 helper preservado (calcularTotaisContratoNominal + "Em Aberto")

5. **Zero push + zero tag:**
   - Nenhum `git push` executado
   - `git tag -l "v1.4-phase7.8*"` vazio

6. **Pai intocado:**
   - Pai: nenhum commit novo nesta plan. Bump é 07.8-04 Task 2.
</verification>

<success_criteria>
Plan 07.8-02 completa quando:

1. ✅ Import `calcularDetalheEncargosContrato` em DetalheContrato.jsx
2. ✅ useMemo `detalheEncargosContrato` com deps `[dividas, allPagamentosDivida, hoje]` (D-18)
3. ✅ Grid dinâmico: `repeat(4, 1fr)` se num_documentos>0, `repeat(3, 1fr)` se ===0 (D-04/D-05)
4. ✅ Nova coluna "Saldo Atualizado" clickable (cursor:pointer + hover) com onClick no-op (plan 07.8-03 wire)
5. ✅ Phase 7.3 (`calcularTotaisContratoNominal`, "Em Aberto") preservada
6. ✅ Build exit 0
7. ✅ Regressão 10/10 PASS
8. ✅ Motor Art.354 invariante (devedorCalc.js + pagamentos.js + dividas.js + calculos.test.js diff vazio)
9. ✅ PAUSA #2 autorizada explicitamente pelo usuário
10. ✅ 1 commit local no submodule `main` com subject EXATO e APENAS 1 arquivo stageado
11. ✅ Zero push, zero tag
12. ✅ Pai intocado — bump é 07.8-04 Task 2
</success_criteria>

<output>
Após Task 3 completa, criar `.planning/phases/07.8-saldo-atualizado-composicao/07.8-02-SUMMARY.md` contendo:

- **SHA:** commit do submodule (full + short)
- **Arquivo modificado:** `src/components/DetalheContrato.jsx` com contagem de linhas adicionadas/removidas
- **Gates determinísticos:** output de regressão (10/10), build (exit 0), motor diff + shield diff (vazios), escopo (1 arquivo)
- **Autorização humana:** texto literal do usuário em PAUSA #2
- **Invariante motor Art.354:** confirmação que devedorCalc.js, pagamentos.js, dividas.js, calculos.test.js não mudaram nesta plan
- **UX changes:** grid dinâmico 3/4 + coluna clickable sem modal (onClick no-op nesta plan)
- **Phase 7.3 preservada:** `calcularTotaisContratoNominal` + "Em Aberto" intocadas
- **Zero push + zero tag:** confirmação explícita
- **Próximos passos:**
  1. Plan 07.8-03: DecomposicaoSaldoModal.jsx + impressão A4 + wire onClick
  2. Plan 07.8-04: UAT SC-1..SC-7 + bump submodule no pai (SEM PUSH)
  3. Tag `v1.4-phase7.8` APENAS após UAT prod OK

O SUMMARY é o artefato de fechamento desta plan.
</output>
</content>
