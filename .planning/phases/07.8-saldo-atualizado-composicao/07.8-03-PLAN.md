---
phase: 07.8-saldo-atualizado-composicao
plan: 03
type: execute
wave: 3
depends_on:
  - 07.8-02
files_modified:
  - src/mr-3/mr-cobrancas/src/components/DecomposicaoSaldoModal.jsx
  - src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx
autonomous: false   # D-15: modo cansado — pausa #3 (diff review pré-commit) antes do feat(07.8-03) no submodule main. SEM PUSH.
requirements: []    # Gap closure — sem REQ-IDs formais. Contrato = SC-1, SC-2, SC-4, SC-5, SC-6, SC-7 em 07.8-CONTEXT.md §6 + D-06..D-14.

must_haves:
  truths:
    - "Novo componente src/components/DecomposicaoSaldoModal.jsx criado, importando Modal base de ./ui/Modal.jsx (D-06)"
    - "Modal base de Modal.jsx intocado — git diff HEAD -- src/components/ui/Modal.jsx retorna vazio (D-06)"
    - "Body do modal renderiza linha-a-linha: Valor Original / + Correção / + Multa / + Juros / + Honorários / [+ Art.523 Multa] / [+ Art.523 Honorários] / [+ Custas] / = Saldo Devedor Atualizado (CONTEXT §3 Fix C)"
    - "Valor Original e Saldo Devedor Atualizado SEMPRE aparecem no modal (D-07)"
    - "Linhas com valor=0 são escondidas (exceto Valor Original e Total final) — D-07 + UAT SC-2"
    - "Correção negativa renderiza com sinal '-' e cor vermelha `#dc2626` (D-08 + UAT)"
    - "Art.523 adiciona 2 linhas SÓ quando `detalhe.art523.total > 0`: 'Art. 523 §1º — Multa (10%)' + 'Art. 523 §1º — Honorários (10%)' (D-09)"
    - "Custas adiciona 1 linha SÓ quando `detalhe.custas.atualizado > 0`: 'Custas judiciais (atualizadas)' (D-10)"
    - "Nota rodapé itálico pequeno cinza: 'Pode haver discrepância de R$ 0,01 por arredondamento.' (D-11)"
    - "3 botões no footer: Copiar (clipboard.writeText + toast), Imprimir (window.open A4), Fechar (D-06, D-12, D-13)"
    - "Botão Copiar chama `navigator.clipboard.writeText(textoFormatado)` + toast success/error (D-12)"
    - "Botão Imprimir chama `window.open('', '_blank', 'width=900,height=700')` + HTML com `@page size:A4 margin:2.5cm 3cm` + `Times New Roman` + `@media print{.bar{display:none}}` (D-13)"
    - "ZERO CSS print no app principal — `@media print` existe APENAS dentro do HTML string do window.open (D-13)"
    - "DetalheContrato.jsx importa DecomposicaoSaldoModal e wire onClick da 4ª coluna abre o modal via setState"
    - "Motor Art.354 intocado: git diff HEAD -- src/utils/devedorCalc.js src/services/pagamentos.js src/services/dividas.js src/services/__tests__/calculos.test.js retorna vazio (D-01)"
    - "npm run test:regressao 10/10 verde; npm run build exit 0"
    - "1 commit local submodule mr-3 branch main subject EXATO `feat(07.8-03): DecomposicaoSaldoModal + impressão isolada A4` — SEM PUSH"
  artifacts:
    - path: "src/mr-3/mr-cobrancas/src/components/DecomposicaoSaldoModal.jsx"
      provides: "Modal wrapper sobre Modal.jsx base que renderiza decomposição linha-a-linha + 3 botões (Copiar/Imprimir/Fechar)"
      contains: "import Modal from \"./ui/Modal.jsx\""
    - path: "src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx"
      provides: "Import + state `showDecomposicaoModal` + wire onClick da 4ª coluna abre o modal"
      contains: "DecomposicaoSaldoModal"
  key_links:
    - from: "DecomposicaoSaldoModal.jsx (wrapper)"
      to: "Modal base em ./ui/Modal.jsx"
      via: "import Modal from './ui/Modal.jsx' + uso como <Modal title onClose>"
      pattern: "import\\s+Modal\\s+from\\s+['\"]\\./ui/Modal"
    - from: "Botão Imprimir"
      to: "window.open('', '_blank', 'width=900,height=700') com HTML auto-contido"
      via: "document.write do HTML isolado + @page A4 + Times New Roman + @media print"
      pattern: "window\\.open\\s*\\(\\s*['\"]{2}\\s*,\\s*['\"]_blank"
    - from: "Botão Copiar"
      to: "navigator.clipboard.writeText(textoFormatado) + toast"
      via: "async handler que forma texto decomposto + grava em clipboard + toast de feedback"
      pattern: "navigator\\.clipboard\\.writeText"
    - from: "DetalheContrato.jsx 4ª coluna (plan 07.8-02)"
      to: "setShowDecomposicaoModal(true) via onClick da coluna clickable"
      via: "state `const [showDecomposicaoModal, setShowDecomposicaoModal] = useState(false)` + onClick wire"
      pattern: "setShowDecomposicaoModal"
    - from: "DetalheContrato.jsx render condicional do modal"
      to: "<DecomposicaoSaldoModal detalhe={...} contrato={...} credor={...} devedor={...} onClose={...} />"
      via: "render dentro do mesmo componente, sibling do return principal ou dentro do card"
      pattern: "<DecomposicaoSaldoModal"
---

<objective>
Criar novo componente `src/components/DecomposicaoSaldoModal.jsx` que usa `Modal.jsx` base (intocado, D-06) como wrapper e renderiza a decomposição do saldo atualizado Art. 354 CC linha-a-linha, com 3 botões (Copiar / Imprimir / Fechar). E wire o onClick da 4ª coluna "Saldo Atualizado" (plan 07.8-02) para abrir esse modal.

Comportamento do modal:
- Body exibe Valor Original + componentes (correção, multa, juros, honorários) + [Art.523 se aplicável] + [Custas se aplicável] + Saldo Devedor Atualizado total.
- Linhas com valor=0 escondidas, EXCETO Valor Original e Total (D-07).
- Correção negativa renderiza com sinal `-` + cor `#dc2626` (D-08).
- Art.523 adiciona 2 linhas extras SÓ quando `detalhe.art523.total > 0` (D-09).
- Custas adiciona 1 linha SÓ quando `detalhe.custas.atualizado > 0` (D-10).
- Nota rodapé: itálico pequeno cinza "Pode haver discrepância de R$ 0,01 por arredondamento." (D-11).
- Botão Copiar: `navigator.clipboard.writeText` texto formatado + toast (D-12).
- Botão Imprimir: `window.open` com HTML A4 auto-contido, Times New Roman, `@media print{.bar{display:none}}` isolado (D-13).
- Botão Fechar: chama onClose prop.

Purpose: Advogado consegue apresentar ao cliente/devedor a decomposição formal do saldo (base legal Art. 354 CC) com cópia pra WhatsApp e impressão A4 em Times New Roman para ofício/acordo. Motor Art.354 permanece intocado; modal consome shape do adapter (plan 07.8-01) via prop.

Output:
- **Novo arquivo:** `src/components/DecomposicaoSaldoModal.jsx` (~180-220 linhas incluindo JSX + print HTML).
- **Arquivo modificado:** `src/components/DetalheContrato.jsx` — import do modal + state + wire do onClick da 4ª coluna (plan 07.8-02) + render condicional do modal.
- 1 commit local no submodule `src/mr-3/mr-cobrancas` branch `main` com subject EXATO `feat(07.8-03): DecomposicaoSaldoModal + impressão isolada A4` (D-15).
- **Zero commits no pai** — bump é 07.8-04 Task 2.
- **Zero push** em qualquer momento. Zero tag criada.

Contexto de conformidade:
- D-01 Motor Art.354 intocado.
- D-06 Modal.jsx base intocado; novo componente é WRAPPER.
- D-07 Linhas zero-value hidden (exceto Valor Original e Total).
- D-08 Correção negativa red `#dc2626`.
- D-09 Art.523 como linhas extras condicionais.
- D-10 Custas como linha extra condicional.
- D-11 Nota rodapé de arredondamento.
- D-12 Copiar via clipboard API + toast.
- D-13 Imprimir via window.open com HTML A4 isolado.
- D-14 Decomposição AGREGADA (shape do adapter, não per-parcela).
- D-15 Modo cansado — Task 3 é pausa #3.
- D-17 Submodule main, zero push.
- D-18 Edge cases: correção negativa, indexador=nenhum (linha correção hidden quando valor=0), rounding footer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07.8-saldo-atualizado-composicao/07.8-CONTEXT.md

<!-- Plans predecessoras (consumo do adapter + coluna clickable): -->
@.planning/phases/07.8-saldo-atualizado-composicao/07.8-01-PLAN.md
@.planning/phases/07.8-saldo-atualizado-composicao/07.8-02-PLAN.md

<!-- Template literal do pattern de impressão isolada (GerarPeticao.jsx:1006-1014 é precedente canônico D-13): -->
<!-- Ler arquivo diretamente: src/mr-3/mr-cobrancas/src/components/GerarPeticao.jsx linhas 1006-1014 -->

<interfaces>
<!-- Interfaces-chave do codebase atual. Executor usa DIRETO sem explorar. -->

From src/mr-3/mr-cobrancas/src/components/ui/Modal.jsx (55 linhas — INTOCADO D-06):
```jsx
export default function Modal({ title, onClose, children, width = 560 }) {
  return (
    <div style={{ position: "fixed", inset: 0, background: "rgba(15,23,42,.45)", ... }}>
      <style>{`@keyframes fadeInModal{from{opacity:0;transform:scale(.97)}to{opacity:1;transform:scale(1)}}`}</style>
      <div style={{ background: "#fff", borderRadius: 20, width: "100%", maxWidth: width, maxHeight: "90vh", overflowY: "auto", ... }}>
        {/* Header with title + close button ✕ */}
        <div style={{ display: "flex", justifyContent: "space-between", ... }}>
          <p style={{ fontFamily: "'Space Grotesk',sans-serif", fontWeight: 800, fontSize: 16, ... }}>{title}</p>
          <button onClick={onClose} ...>✕</button>
        </div>
        {/* Body */}
        <div style={{ padding: "20px 24px 24px" }}>{children}</div>
      </div>
    </div>
  );
}
```

**Consumo no wrapper novo:**
```jsx
<Modal title="Composição do Saldo Atualizado" onClose={onClose} width={600}>
  { body + footer com 3 botões }
</Modal>
```

From src/mr-3/mr-cobrancas/src/components/GerarPeticao.jsx:1006-1014 (pattern canônico D-13 — copiar estrutura, adaptar conteúdo):
```javascript
function imprimir() {
  if (!peticaoTexto) return;
  const w = window.open("", "_blank", "width=900,height=700");
  w.document.write(`<!DOCTYPE html><html lang="pt-BR"><head><meta charset="UTF-8"/><title>Petição</title>
<style>@page{size:A4;margin:2.5cm 3cm 2.5cm 3.5cm}body{font-family:"Times New Roman",serif;font-size:12pt;line-height:1.8;color:#000;background:#fff}pre{font-family:"Times New Roman",serif;font-size:12pt;line-height:1.8;white-space:pre-wrap;word-wrap:break-word;margin:0}.bar{background:#f1f5f9;padding:12px 20px;margin-bottom:20px;border-radius:8px;display:flex;gap:12px;align-items:center}@media print{.bar{display:none}}</style></head><body>
<div class="bar"><button onclick="window.print()" style="background:#1e40af;color:#fff;border:none;border-radius:8px;padding:8px 20px;font-size:13px;cursor:pointer;font-weight:700;">🖨️ Imprimir / PDF</button><button onclick="window.close()" style="background:#e2e8f0;color:#475569;border:none;border-radius:8px;padding:8px 16px;font-size:13px;cursor:pointer;">✕ Fechar</button><span style="font-size:11px;color:#64748b">Ctrl+P → Salvar como PDF</span></div>
<pre>${peticaoTexto.replace(/</g,"&lt;").replace(/>/g,"&gt;")}</pre>
<div style="margin-top:40px;border-top:1px solid #999;padding-top:10px;font-size:9pt;color:#888;text-align:center">MR Cobranças — ${new Date().toLocaleString("pt-BR")}</div></body></html>`);
  w.document.close(); w.focus();
}
```

**Adaptação no modal novo:** mesmo `window.open` + mesmo `@page` + mesmo `Times New Roman` + mesmo `.bar` + `@media print{.bar{display:none}}`. Conteúdo muda: em vez de `<pre>peticaoTexto</pre>`, uma tabela HTML simples com as linhas da decomposição.

From shape do adapter (plan 07.8-01 — `calcularDetalheEncargosContrato` retorna):
```javascript
{
  valorOriginal: number,
  multa:        { valor: number, ... },
  juros:        { valor: number, ... },
  correcao:     { valor: number, ... },   // pode ser NEGATIVO (D-08)
  honorarios:   { valor: number, ... },
  custas:       { original: number, atualizado: number },
  art523:       { multa: number, honorarios: number, total: number },
  totalEncargos: number,
  totalPago:     number,
  saldoAtualizado: number,
  diasEmAtraso:  number | null,
  detalhePorDivida: Array<...>
}
```

From react-hot-toast (já importado no projeto — `import toast from "react-hot-toast"`):
- `toast.success("Copiado!")` — sucesso.
- `toast.error("Falha ao copiar")` — erro.

From DetalheContrato.jsx variáveis disponíveis no escopo onde o modal será renderizado (linhas 489-491):
```javascript
const devedor = devedores.find(d => String(d.id) === String(contrato.devedor_id));
const credor  = credores?.find(c => String(c.id) === String(contrato.credor_id));
const { total_pago, saldo_restante } = calcularTotaisContratoNominal(dividas || [], allPagamentosDivida);
// Phase 7.8-02 adicionou:
// const detalheEncargosContrato = useMemo(...);
```

Indexador label — derivar do primeiro doc do contrato OU do contrato (CONTEXT.md menciona `indice_correcao`):
```javascript
// Derive indexador label para o texto "Correção monetária (INDEXADOR)"
const indexadorLabel = (contrato?.indice_correcao || "nenhum").toUpperCase();
// Ou null/generico se indice_correcao for falsy
```

From Phase 7.7 commits PENDING NO SUBMODULE (visíveis em STATE.md last_activity) — contexto para o UAT mais tarde:
- 07.8-01 (adapter + shield) já commitado local antes desta plan.
- 07.8-02 (4ª coluna) já commitado local antes desta plan.
- Nenhum push foi executado.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar src/components/DecomposicaoSaldoModal.jsx com body + 3 botões (Copiar/Imprimir/Fechar)</name>
  <files>src/mr-3/mr-cobrancas/src/components/DecomposicaoSaldoModal.jsx</files>

  <read_first>
    - `src/mr-3/mr-cobrancas/src/components/ui/Modal.jsx` linhas 1-55 (Modal base — confirmar props `{ title, onClose, children, width = 560 }` e NÃO tocar)
    - `src/mr-3/mr-cobrancas/src/components/GerarPeticao.jsx` linhas 1006-1014 (pattern literal D-13 de window.open — copiar estrutura HTML/CSS)
    - `src/mr-3/mr-cobrancas/src/utils/devedorCalc.js` linha 365 (`calcularDetalheEncargos` — confirmar shape exato do retorno, especialmente `multa.valor`, `juros.valor`, `correcao.valor`, `honorarios.valor`, `custas.original`, `custas.atualizado`, `art523.multa`, `art523.honorarios`, `art523.total`, `saldoAtualizado`)
    - `.planning/phases/07.8-saldo-atualizado-composicao/07.8-CONTEXT.md` §5 D-06..D-14 (todas as decisões de UX do modal), §6 SC-1, SC-2, SC-4, SC-5, SC-6, SC-7
  </read_first>

  <action>
    **Criar arquivo NOVO `src/components/DecomposicaoSaldoModal.jsx`** com o conteúdo a seguir. O componente é default export, recebe props `{ detalhe, contrato, credor, devedor, indexadorLabel, dataCalculo, onClose }`, e renderiza body + footer com 3 botões.

    **Código COMPLETO a escrever:**

    ```jsx
    import { useMemo } from "react";
    import toast from "react-hot-toast";
    import Modal from "./ui/Modal.jsx";

    // Phase 7.8 — Wrapper sobre Modal.jsx base (D-06). Mostra decomposição Art.354 CC
    // linha-a-linha, com botões Copiar (clipboard) e Imprimir (window.open A4 isolado).

    function fmtBRL(v) {
      if (v == null || v === "") return "—";
      return Number(v).toLocaleString("pt-BR", { style: "currency", currency: "BRL" });
    }

    // Formata número com sinal `-` + cor vermelha para correção negativa (D-08).
    function fmtBRLSigned(v) {
      if (v == null || v === "" || Number.isNaN(Number(v))) return "—";
      const n = Number(v);
      if (n < 0) {
        return "- " + fmtBRL(Math.abs(n));
      }
      return fmtBRL(n);
    }

    // Aplica cor vermelha ao valor se for negativo (D-08).
    function valueColor(v) {
      return Number(v) < 0 ? "#dc2626" : "#0f172a";
    }

    export default function DecomposicaoSaldoModal({
      detalhe,          // shape de calcularDetalheEncargosContrato (plan 07.8-01)
      contrato,
      credor,
      devedor,
      indexadorLabel,   // string tipo "IGP-M", "INPC_IPCA", etc
      dataCalculo,      // "YYYY-MM-DD" ou Date
      onClose,
    }) {
      const d = detalhe || {};
      const valorOriginal   = Number(d.valorOriginal || 0);
      const correcaoValor   = Number(d?.correcao?.valor || 0);
      const multaValor      = Number(d?.multa?.valor || 0);
      const jurosValor      = Number(d?.juros?.valor || 0);
      const honorariosValor = Number(d?.honorarios?.valor || 0);
      const art523Multa     = Number(d?.art523?.multa || 0);
      const art523Hon       = Number(d?.art523?.honorarios || 0);
      const art523Total     = Number(d?.art523?.total || 0);
      const custasAtualizado = Number(d?.custas?.atualizado || 0);
      const saldoAtualizado = Number(d.saldoAtualizado || 0);

      const indexador = (indexadorLabel && String(indexadorLabel).trim()) || "—";

      const referencia = contrato?.referencia || contrato?.id || "—";
      const credorNome = credor?.nome || "—";
      const devedorNome = devedor?.nome || "—";

      const dataCalculoFmt = useMemo(() => {
        if (!dataCalculo) return new Date().toLocaleDateString("pt-BR");
        try {
          const d = typeof dataCalculo === "string" ? new Date(dataCalculo + "T00:00:00") : new Date(dataCalculo);
          if (isNaN(d.getTime())) return String(dataCalculo);
          return d.toLocaleDateString("pt-BR", { timeZone: "America/Sao_Paulo" });
        } catch {
          return String(dataCalculo);
        }
      }, [dataCalculo]);

      // Linhas da decomposição — D-07 zero-value hidden (exceto Valor Original e Total final).
      // Ordem: Valor Original (sempre), Correção (se !=0), Multa (se >0), Juros (se >0),
      // Honorários (se >0), Art.523 Multa + Honorários (se art523.total > 0 — D-09),
      // Custas (se custasAtualizado > 0 — D-10), Total (sempre).
      const linhas = [];
      linhas.push({ label: "Valor Original", value: valorOriginal, signed: false, bold: false, sempre: true });

      if (correcaoValor !== 0) {
        linhas.push({
          label: `Correção monetária (${indexador})`,
          value: correcaoValor,
          signed: true,         // usa fmtBRLSigned + cor vermelha se <0 (D-08)
          bold: false,
        });
      }

      if (multaValor > 0) {
        linhas.push({ label: "Multa contratual", value: multaValor, signed: false, bold: false });
      }

      if (jurosValor > 0) {
        linhas.push({ label: "Juros de mora", value: jurosValor, signed: false, bold: false });
      }

      if (honorariosValor > 0) {
        linhas.push({ label: "Honorários", value: honorariosValor, signed: false, bold: false });
      }

      if (art523Total > 0) {
        if (art523Multa > 0) linhas.push({ label: "Art. 523 §1º — Multa (10%)", value: art523Multa, signed: false, bold: false });
        if (art523Hon > 0)   linhas.push({ label: "Art. 523 §1º — Honorários (10%)", value: art523Hon, signed: false, bold: false });
      }

      if (custasAtualizado > 0) {
        linhas.push({ label: "Custas judiciais (atualizadas)", value: custasAtualizado, signed: false, bold: false });
      }

      linhas.push({ label: "Saldo Devedor Atualizado", value: saldoAtualizado, signed: false, bold: true, sempre: true });

      // Texto formatado para clipboard (D-12).
      const textoFormatado = useMemo(() => {
        const lines = [];
        lines.push(`Composição do Saldo Atualizado — ${credorNome} vs ${devedorNome}`);
        lines.push(`Contrato: ${referencia} — Data: ${dataCalculoFmt}`);
        lines.push("");
        for (const l of linhas) {
          const valStr = l.signed ? fmtBRLSigned(l.value) : fmtBRL(l.value);
          const prefix = l.bold ? "= " : (l === linhas[0] ? "  " : "+ ");
          lines.push(`${prefix}${l.label}: ${valStr}`);
        }
        lines.push("");
        lines.push("(Pode haver discrepância de R$ 0,01 por arredondamento.)");
        return lines.join("\n");
      }, [linhas, credorNome, devedorNome, referencia, dataCalculoFmt]);

      // Handler Copiar (D-12).
      async function handleCopiar() {
        try {
          if (!navigator?.clipboard?.writeText) throw new Error("clipboard indisponível");
          await navigator.clipboard.writeText(textoFormatado);
          toast.success("Copiado!");
        } catch (e) {
          toast.error("Falha ao copiar. Seu navegador pode não suportar clipboard API.");
        }
      }

      // Handler Imprimir (D-13).
      function handleImprimir() {
        const w = window.open("", "_blank", "width=900,height=700");
        if (!w) {
          toast.error("Pop-up bloqueado. Permita pop-ups e tente novamente.");
          return;
        }
        // Montar corpo HTML da tabela (sem CSS print no app principal — tudo isolado aqui).
        const bodyRowsHtml = linhas.map((l) => {
          const valStr = l.signed ? fmtBRLSigned(l.value) : fmtBRL(l.value);
          const color = l.signed && Number(l.value) < 0 ? "#dc2626" : "#000";
          const fontWeight = l.bold ? 700 : 400;
          const borderTop = l.bold ? "1px solid #000" : "none";
          return `<tr><td style="padding:6px 4px;border-top:${borderTop};font-weight:${fontWeight};">${escapeHtml(l.label)}</td><td style="padding:6px 4px;text-align:right;color:${color};border-top:${borderTop};font-weight:${fontWeight};font-variant-numeric:tabular-nums;">${valStr}</td></tr>`;
        }).join("");

        const titulo = `Composição do Saldo Atualizado — ${escapeHtml(credorNome)} vs ${escapeHtml(devedorNome)}`;
        const subtitulo = `Contrato: ${escapeHtml(String(referencia))} — Data do cálculo: ${dataCalculoFmt}`;
        const timestamp = new Date().toLocaleString("pt-BR");

        w.document.write(`<!DOCTYPE html><html lang="pt-BR"><head><meta charset="UTF-8"/><title>Composição do Saldo</title>
<style>
@page{size:A4;margin:2.5cm 3cm}
body{font-family:"Times New Roman",serif;font-size:12pt;line-height:1.8;color:#000;background:#fff;margin:0}
h1{font-size:14pt;margin:0 0 6px 0}
.sub{font-size:11pt;color:#333;margin-bottom:20px}
table{width:100%;border-collapse:collapse;margin-top:12px}
.footer-note{margin-top:18px;font-style:italic;color:#666;font-size:10pt}
.sig{margin-top:60px;text-align:center;font-size:11pt}
.sig-line{display:inline-block;border-top:1px solid #000;width:60%;padding-top:4px}
.stamp{margin-top:30px;border-top:1px solid #999;padding-top:8px;font-size:9pt;color:#888;text-align:center}
.bar{background:#f1f5f9;padding:12px 20px;margin-bottom:20px;border-radius:8px;display:flex;gap:12px;align-items:center}
@media print{.bar{display:none}}
</style></head><body>
<div class="bar">
  <button onclick="window.print()" style="background:#1e40af;color:#fff;border:none;border-radius:8px;padding:8px 20px;font-size:13px;cursor:pointer;font-weight:700;">🖨️ Imprimir / PDF</button>
  <button onclick="window.close()" style="background:#e2e8f0;color:#475569;border:none;border-radius:8px;padding:8px 16px;font-size:13px;cursor:pointer;">✕ Fechar</button>
  <span style="font-size:11px;color:#64748b">Ctrl+P → Salvar como PDF</span>
</div>
<h1>${titulo}</h1>
<div class="sub">${subtitulo}</div>
<table>
  <thead>
    <tr><th style="text-align:left;padding:6px 4px;border-bottom:2px solid #000;">Componente</th><th style="text-align:right;padding:6px 4px;border-bottom:2px solid #000;">Valor (R$)</th></tr>
  </thead>
  <tbody>${bodyRowsHtml}</tbody>
</table>
<p class="footer-note">Pode haver discrepância de R$ 0,01 por arredondamento. Amortização sequencial conforme Art. 354 do Código Civil.</p>
<div class="sig"><span class="sig-line">${escapeHtml(credorNome)} — Credor(a)</span></div>
<div class="stamp">MR Cobranças — ${timestamp}</div>
</body></html>`);
        w.document.close();
        w.focus();
      }

      // Escape HTML seguro (evita XSS em nomes de credor/devedor com caracteres especiais).
      function escapeHtml(s) {
        return String(s ?? "")
          .replace(/&/g, "&amp;")
          .replace(/</g, "&lt;")
          .replace(/>/g, "&gt;")
          .replace(/"/g, "&quot;")
          .replace(/'/g, "&#39;");
      }

      return (
        <Modal title="Composição do Saldo Atualizado" onClose={onClose} width={600}>
          {/* Header interno — credor / devedor / contrato / data */}
          <div style={{ fontSize: 12, color: "#475569", marginBottom: 12 }}>
            <div><strong>Credor:</strong> {credorNome}</div>
            <div><strong>Devedor:</strong> {devedorNome}</div>
            <div><strong>Contrato:</strong> {String(referencia)} — <strong>Data:</strong> {dataCalculoFmt}</div>
          </div>

          {/* Tabela de decomposição */}
          <div style={{ border: "1px solid #e2e8f0", borderRadius: 10, overflow: "hidden", marginBottom: 12 }}>
            <table style={{ width: "100%", borderCollapse: "collapse", fontSize: 13 }}>
              <tbody>
                {linhas.map((l, idx) => {
                  const isTotal = !!l.bold;
                  const valStr = l.signed ? fmtBRLSigned(l.value) : fmtBRL(l.value);
                  const color = l.signed ? valueColor(l.value) : "#0f172a";
                  return (
                    <tr key={idx} style={{
                      borderTop: isTotal ? "2px solid #0f172a" : (idx === 0 ? "none" : "1px solid #f1f5f9"),
                      background: isTotal ? "#f0fdf4" : "transparent",
                    }}>
                      <td style={{ padding: "10px 14px", fontWeight: isTotal ? 700 : 500, color: "#0f172a" }}>
                        {l.label}
                      </td>
                      <td style={{
                        padding: "10px 14px",
                        textAlign: "right",
                        fontFamily: "'Space Grotesk',sans-serif",
                        fontWeight: isTotal ? 800 : 600,
                        fontSize: isTotal ? 16 : 14,
                        color,
                        fontVariantNumeric: "tabular-nums",
                      }}>
                        {valStr}
                      </td>
                    </tr>
                  );
                })}
              </tbody>
            </table>
          </div>

          {/* Nota rodapé D-11 */}
          <p style={{ fontSize: 11, color: "#94a3b8", fontStyle: "italic", margin: "4px 0 16px 0" }}>
            Pode haver discrepância de R$ 0,01 por arredondamento.
          </p>

          {/* Footer — 3 botões */}
          <div style={{ display: "flex", gap: 8, justifyContent: "flex-end", flexWrap: "wrap" }}>
            <button
              onClick={handleCopiar}
              style={{ background: "#0d9488", color: "#fff", border: "none", borderRadius: 10, padding: "10px 16px", fontSize: 13, fontWeight: 700, cursor: "pointer" }}
            >
              📋 Copiar
            </button>
            <button
              onClick={handleImprimir}
              style={{ background: "#1e40af", color: "#fff", border: "none", borderRadius: 10, padding: "10px 16px", fontSize: 13, fontWeight: 700, cursor: "pointer" }}
            >
              🖨️ Imprimir
            </button>
            <button
              onClick={onClose}
              style={{ background: "#e2e8f0", color: "#475569", border: "none", borderRadius: 10, padding: "10px 16px", fontSize: 13, fontWeight: 700, cursor: "pointer" }}
            >
              ✕ Fechar
            </button>
          </div>
        </Modal>
      );
    }
    ```

    **Detalhes obrigatórios:**
    - Arquivo novo — criar via Write tool.
    - Default export nomeado `DecomposicaoSaldoModal`.
    - Import Modal EXATAMENTE: `import Modal from "./ui/Modal.jsx";` (caminho relativo, extensão explícita — padrão do projeto).
    - Import toast EXATAMENTE: `import toast from "react-hot-toast";`.
    - NÃO modificar `src/components/ui/Modal.jsx` — wrapper puro, Modal base fica intocado (D-06).
    - NÃO adicionar dependências novas no package.json — `react-hot-toast` já é dep do projeto.
    - Pattern de impressão EXATAMENTE conforme `GerarPeticao.jsx:1006-1014`: `window.open("", "_blank", "width=900,height=700")` + `@page{size:A4;margin:2.5cm 3cm}` + `Times New Roman` + `.bar{@media print{display:none}}` (D-13).
    - ZERO `@media print` em CSS do app principal — só dentro do HTML string do window.open.
    - `escapeHtml` helper para evitar XSS em nomes com caracteres especiais quando injetados no HTML do print.
    - `fmtBRLSigned` para correção negativa (D-08) — sinal `-` + cor vermelha no render React.
    - Hide de linhas zero-value EXCETO Valor Original e Saldo Devedor Atualizado (D-07).
    - Art.523 linhas condicionais (D-09): adiciona SÓ se `art523.total > 0`.
    - Custas linha condicional (D-10): adiciona SÓ se `custas.atualizado > 0`.
    - Nota rodapé literal: "Pode haver discrepância de R$ 0,01 por arredondamento." (D-11).
    - 3 botões: Copiar (teal), Imprimir (azul), Fechar (cinza).
    - Texto do clipboard (D-12): formato especificado no CONTEXT §5 D-12 ("Composição do Saldo Atualizado — [credor] vs [devedor] ...").
  </action>

  <verify>
    <automated>cd src/mr-3/mr-cobrancas &amp;&amp; node -e "import('./src/components/DecomposicaoSaldoModal.jsx').then(m =&gt; { if (typeof m.default !== 'function') { console.error('FAIL: default export não é componente'); process.exit(1); } console.log('OK: DecomposicaoSaldoModal default exported'); }).catch(e =&gt; { console.error('FAIL import:', e.message); process.exit(1); })"</automated>
  </verify>

  <acceptance_criteria>
    <!-- Rodar da raiz do submodule src/mr-3/mr-cobrancas. -->
    - **Arquivo criado:** `test -f src/components/DecomposicaoSaldoModal.jsx` retorna 0 (arquivo existe)
    - **Default export presente:** `grep -c "export default function DecomposicaoSaldoModal" src/components/DecomposicaoSaldoModal.jsx` retorna **1**
    - **Import Modal base (D-06):** `grep -cE 'import\\s+Modal\\s+from\\s+["'"'"']\\.\\/ui\\/Modal\\.jsx["'"'"']' src/components/DecomposicaoSaldoModal.jsx` retorna **1**
    - **Modal base intocado (D-06):** `git diff HEAD -- src/components/ui/Modal.jsx` retorna **vazio**
    - **Import toast:** `grep -cE 'import\\s+toast\\s+from\\s+["'"'"']react-hot-toast["'"'"']' src/components/DecomposicaoSaldoModal.jsx` retorna **1**
    - **3 botões (D-06, D-12, D-13):** `grep -c "📋 Copiar" src/components/DecomposicaoSaldoModal.jsx` retorna **1**; `grep -c "🖨️ Imprimir" src/components/DecomposicaoSaldoModal.jsx` retorna **1**; `grep -c "✕ Fechar" src/components/DecomposicaoSaldoModal.jsx` retorna **1**
    - **Copiar → clipboard API (D-12):** `grep -c "navigator.clipboard.writeText" src/components/DecomposicaoSaldoModal.jsx` retorna **1**
    - **Copiar → toast feedback (D-12):** `grep -c "toast.success" src/components/DecomposicaoSaldoModal.jsx` retorna **≥1**; `grep -c "toast.error" src/components/DecomposicaoSaldoModal.jsx` retorna **≥1**
    - **Imprimir → window.open (D-13):** `grep -cE 'window\\.open\\(\\s*["'"'"'][^"'"'"']*["'"'"']\\s*,\\s*["'"'"']_blank["'"'"']' src/components/DecomposicaoSaldoModal.jsx` retorna **1**
    - **Print HTML: @page A4 (D-13):** `grep -c "@page{size:A4" src/components/DecomposicaoSaldoModal.jsx` retorna **1** (ou com espaços aceitáveis — usar grep tolerante: `grep -cE "@page\\s*\\{\\s*size:\\s*A4"` retorna **≥1**)
    - **Print HTML: Times New Roman (D-13):** `grep -c "Times New Roman" src/components/DecomposicaoSaldoModal.jsx` retorna **≥1**
    - **Print HTML: @media print com .bar hidden (D-13):** `grep -cE "@media\\s+print\\s*\\{\\s*\\.bar\\s*\\{\\s*display:\\s*none" src/components/DecomposicaoSaldoModal.jsx` retorna **1**
    - **Zero CSS print no app principal (D-13):** `grep -rE "@media\\s+print" src/ --include="*.jsx" --include="*.js" --include="*.css" | grep -v "DecomposicaoSaldoModal.jsx" | grep -v "GerarPeticao.jsx"` retorna **vazio** (só os 2 lugares conhecidos que usam o pattern window.open)
    - **Correção negativa red #dc2626 (D-08):** `grep -c "#dc2626" src/components/DecomposicaoSaldoModal.jsx` retorna **≥1**
    - **Correção negativa com sinal `-` (D-08):** `grep -cE '"- "|"- \\+ "|fmtBRLSigned' src/components/DecomposicaoSaldoModal.jsx` retorna **≥1** (fmtBRLSigned helper presente)
    - **Hide zero-value (D-07):** `grep -cE "multaValor\\s*>\\s*0|jurosValor\\s*>\\s*0|honorariosValor\\s*>\\s*0|correcaoValor\\s*!==\\s*0|correcaoValor\\s*!=\\s*0" src/components/DecomposicaoSaldoModal.jsx` retorna **≥3** (condicionais para esconder linhas zero)
    - **Valor Original sempre aparece (D-07):** `grep -c "\"Valor Original\"" src/components/DecomposicaoSaldoModal.jsx` retorna **1**
    - **Saldo Devedor Atualizado sempre aparece (D-07):** `grep -c "\"Saldo Devedor Atualizado\"" src/components/DecomposicaoSaldoModal.jsx` retorna **1**
    - **Art.523 condicional (D-09):** `grep -cE "art523.*total\\s*>\\s*0|art523Total\\s*>\\s*0" src/components/DecomposicaoSaldoModal.jsx` retorna **≥1**; `grep -c "Art. 523 §1º — Multa" src/components/DecomposicaoSaldoModal.jsx` retorna **1**; `grep -c "Art. 523 §1º — Honorários" src/components/DecomposicaoSaldoModal.jsx` retorna **1**
    - **Custas condicional (D-10):** `grep -cE "custas.*atualizado\\s*>\\s*0|custasAtualizado\\s*>\\s*0" src/components/DecomposicaoSaldoModal.jsx` retorna **≥1**; `grep -c "Custas judiciais (atualizadas)" src/components/DecomposicaoSaldoModal.jsx` retorna **1**
    - **Nota rodapé rounding (D-11):** `grep -c "Pode haver discrepância de R\\$ 0,01" src/components/DecomposicaoSaldoModal.jsx` retorna **≥1** (pode aparecer 2 vezes: 1 no JSX + 1 no HTML do print; aceita ≥1)
    - **escapeHtml presente (defesa XSS):** `grep -c "function escapeHtml" src/components/DecomposicaoSaldoModal.jsx` retorna **1**
    - **Motor Art.354 invariante (D-01):** `git diff HEAD -- src/utils/devedorCalc.js src/services/pagamentos.js src/services/dividas.js src/services/__tests__/calculos.test.js` retorna **vazio**
    - **Escopo desta task específica:** `git diff --name-only HEAD` + `git ls-files --others --exclude-standard` combined deve listar EXATAMENTE `src/components/DecomposicaoSaldoModal.jsx` (arquivo novo). Comando: `{ git diff --name-only HEAD; git ls-files --others --exclude-standard src/components/; } | sort -u` retorna a linha do arquivo novo.
    - Comando do `<verify>` automated retorna "OK: DecomposicaoSaldoModal default exported"
  </acceptance_criteria>

  <done>
    `src/components/DecomposicaoSaldoModal.jsx` criado com default export, wrapping `Modal` base (intocado), renderizando decomposição linha-a-linha com: Valor Original sempre visível; componentes condicionais (D-07); correção negativa red `#dc2626` (D-08); Art.523 condicional (D-09); Custas condicional (D-10); nota rodapé (D-11); 3 botões (Copiar clipboard, Imprimir window.open A4, Fechar); `@media print{.bar{display:none}}` isolado dentro do window.open (D-13). Motor Art.354 intocado. Task 2 wire no DetalheContrato; Task 3 gates determinísticos; Task 4 pausa.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire DecomposicaoSaldoModal em DetalheContrato.jsx — import + state + onClick da 4ª coluna + render condicional</name>
  <files>src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx</files>

  <read_first>
    - `src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx` linhas 1-15 (import block — mesma região da plan 07.8-02; adicionar 1 import novo)
    - `src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx` linhas 120-145 (state declarations — região para adicionar `showDecomposicaoModal`)
    - `src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx` bloco Resumo Financeiro (refatorado em plan 07.8-02) — o onClick no-op `/* Phase 7.8-03: abrir DecomposicaoSaldoModal */` agora recebe `setShowDecomposicaoModal(true)`
    - `src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx` final do return JSX do componente (para inserir `{showDecomposicaoModal && <DecomposicaoSaldoModal ... />}`)
    - `.planning/phases/07.8-saldo-atualizado-composicao/07.8-02-PLAN.md` Task 1 action (confirmar nome exato da variável `detalheEncargosContrato` e onClick placeholder)
  </read_first>

  <action>
    **Passo A — Adicionar import do novo modal:**

    Após o import de `calcularDetalheEncargosContrato` (adicionado em plan 07.8-02), adicionar linha:
    ```javascript
    import DecomposicaoSaldoModal from "./DecomposicaoSaldoModal.jsx";
    ```

    **Passo B — Adicionar state (próximo a outros states, p.ex. após `const [registrandoPagamento, setRegistrandoPagamento] = ...` ou próximo a outros modais; região linha ~120-145):**

    ```javascript
    // Phase 7.8 — estado de abertura do modal de composição do saldo atualizado.
    const [showDecomposicaoModal, setShowDecomposicaoModal] = useState(false);
    ```

    **Passo C — Wire onClick da 4ª coluna (substituir o no-op da plan 07.8-02):**

    No bloco Resumo Financeiro (a IIFE adicionada na plan 07.8-02), localizar a linha:
    ```javascript
    onClick={clickable ? () => { /* Phase 7.8-03: abrir DecomposicaoSaldoModal */ } : undefined}
    ```

    Substituir por:
    ```javascript
    onClick={clickable ? () => setShowDecomposicaoModal(true) : undefined}
    ```

    **Passo D — Render condicional do modal. Inserir JSX antes do fechamento do return principal do componente (tipicamente próximo ao `</div>` final que fecha o container raiz):**

    ```jsx
    {showDecomposicaoModal && (
      <DecomposicaoSaldoModal
        detalhe={detalheEncargosContrato}
        contrato={contrato}
        credor={credor}
        devedor={devedor}
        indexadorLabel={contrato?.indice_correcao ? String(contrato.indice_correcao).toUpperCase() : "—"}
        dataCalculo={hoje}
        onClose={() => setShowDecomposicaoModal(false)}
      />
    )}
    ```

    **Localização sugerida:** logo antes do `</div>` que fecha o card/container raiz do componente, OU como sibling direto desse container. Se houver render condicional `{isPagamento && <SomeOtherModal ... />}` no final, inserir próximo a ele. Se executor não achar lugar óbvio, inserir como ÚLTIMO filho do fragmento/container antes do fechamento do return.

    **Detalhes obrigatórios:**
    - Import novo APÓS import de `calcularDetalheEncargosContrato` (plan 07.8-02).
    - State `const [showDecomposicaoModal, setShowDecomposicaoModal] = useState(false);` — `useState` já importado de React linha 1.
    - onClick wire SÓ para a coluna clickable (label "Saldo Atualizado"). Outras 3 colunas continuam `clickable: false` → onClick undefined.
    - Render condicional com `&& (` — curta-circuito, modal só monta quando `showDecomposicaoModal === true`.
    - Props passadas ao modal: `detalhe` (do useMemo plan 07.8-02), `contrato`, `credor`, `devedor` (já calculados no componente), `indexadorLabel` (derivado de contrato.indice_correcao), `dataCalculo={hoje}`, `onClose={() => setShowDecomposicaoModal(false)}`.
    - NÃO tocar: `calcularTotaisContratoNominal`, helpers da Phase 7.3, useMemo `detalheEncargosContrato` da plan 07.8-02, nem qualquer outro bloco.
    - NÃO tocar `src/utils/devedorCalc.js`, `src/services/*.js`, `src/services/__tests__/*`.
  </action>

  <verify>
    <automated>cd src/mr-3/mr-cobrancas &amp;&amp; npm run build</automated>
  </verify>

  <acceptance_criteria>
    - **Import do modal:** `grep -cE 'import\\s+DecomposicaoSaldoModal\\s+from\\s+["'"'"']\\.\\/DecomposicaoSaldoModal(\\.jsx)?["'"'"']' src/components/DetalheContrato.jsx` retorna **1**
    - **State presente:** `grep -c "const \\[showDecomposicaoModal, setShowDecomposicaoModal\\] = useState(false)" src/components/DetalheContrato.jsx` retorna **1**
    - **onClick wire (substitui no-op da 07.8-02):** `grep -c "setShowDecomposicaoModal(true)" src/components/DetalheContrato.jsx` retorna **1**
    - **No-op original removido:** `grep -c "Phase 7.8-03: abrir DecomposicaoSaldoModal" src/components/DetalheContrato.jsx` retorna **0** (o comentário placeholder do no-op foi substituído; se executor preferir manter comentário explicativo diferente, aceita ≤1)
    - **Render condicional:** `grep -c "showDecomposicaoModal &&" src/components/DetalheContrato.jsx` retorna **1**
    - **<DecomposicaoSaldoModal ... />:** `grep -c "<DecomposicaoSaldoModal" src/components/DetalheContrato.jsx` retorna **1**
    - **Props passadas:** `grep -cE "detalhe=\\{detalheEncargosContrato\\}" src/components/DetalheContrato.jsx` retorna **1**; `grep -cE "onClose=\\{.*setShowDecomposicaoModal\\(false\\)" src/components/DetalheContrato.jsx` retorna **1**; `grep -cE "dataCalculo=\\{hoje\\}" src/components/DetalheContrato.jsx` retorna **1**
    - **indexadorLabel derivado:** `grep -cE "indexadorLabel=" src/components/DetalheContrato.jsx` retorna **1**
    - **Escopo combinado das 2 tasks:** `{ git diff --name-only HEAD; git ls-files --others --exclude-standard src/components/; } | sort -u` retorna EXATAMENTE 2 linhas:
      - `src/components/DecomposicaoSaldoModal.jsx` (arquivo novo criado na Task 1)
      - `src/components/DetalheContrato.jsx` (arquivo modificado na Task 2)
    - **Motor Art.354 invariante (D-01):** `git diff HEAD -- src/utils/devedorCalc.js src/services/pagamentos.js src/services/dividas.js src/services/__tests__/calculos.test.js` retorna **vazio**
    - **Modal.jsx base intocado (D-06):** `git diff HEAD -- src/components/ui/Modal.jsx` retorna **vazio**
    - **Build exit 0:** `npm run build` exit 0
    - **Regressão 10/10:** `npm run test:regressao` exit 0
  </acceptance_criteria>

  <done>
    DetalheContrato.jsx importa `DecomposicaoSaldoModal`, declara state `showDecomposicaoModal`, onClick da 4ª coluna abre o modal via `setShowDecomposicaoModal(true)`, e render condicional `{showDecomposicaoModal && <DecomposicaoSaldoModal ... />}` passa `detalhe={detalheEncargosContrato}` + outras props. Build + regressão verdes. Motor + Modal base intocados. Ainda não commitado.
  </done>
</task>

<task type="auto">
  <name>Task 3: Gates determinísticos — regressão 10/10 + build + invariante motor + Modal base intocado (antes da pausa)</name>
  <files>
    (nenhum arquivo modificado — só gates)
  </files>

  <read_first>
    - `.planning/phases/07.8-saldo-atualizado-composicao/07.8-CONTEXT.md` §5 D-01 (INV motor), D-06 (Modal base intocado), D-15 (pausas — Task 4 desta plan é a pausa #3)
    - `.planning/phases/07.8-saldo-atualizado-composicao/07.8-02-PLAN.md` Task 2 — template dos gates da plan anterior (re-usar estrutura)
  </read_first>

  <action>
    Rodar os gates determinísticos na raiz do submodule. Se QUALQUER gate falhar, NÃO avançar para Task 4 e reportar ao usuário com output completo do gate ofensor.

    ```bash
    cd src/mr-3/mr-cobrancas

    # Gate 1 — Regressão 10/10 (D-01 motor intocado):
    npm run test:regressao
    # Esperado: exit 0. 9 TJGO + 1 shield 07.8-01.

    # Gate 2 — Build:
    npm run build
    # Esperado: exit 0. Falhas prováveis:
    #   - JSX mal formado em DecomposicaoSaldoModal.jsx (template string sem backtick correto,
    #     escape HTML dentro de ${} sem interpolação correta).
    #   - Import path errado de Modal.jsx (caminho relativo quebrado).
    #   - Prop sintax error no <DecomposicaoSaldoModal ...> em DetalheContrato.jsx.

    # Gate 3 — Invariante motor + shield + Modal base (D-01 + D-06):
    git diff HEAD -- src/utils/devedorCalc.js
    git diff HEAD -- src/services/pagamentos.js
    git diff HEAD -- src/services/dividas.js
    git diff HEAD -- src/services/__tests__/calculos.test.js
    git diff HEAD -- src/components/ui/Modal.jsx
    # Esperado: TODOS vazios. Qualquer byte → reportar e ABORTAR.

    # Gate 4 — Escopo combinado dos arquivos da plan:
    { git diff --name-only HEAD; git ls-files --others --exclude-standard src/components/; } | sort -u
    # Esperado: EXATAMENTE 2 linhas:
    #   src/components/DecomposicaoSaldoModal.jsx  (arquivo novo)
    #   src/components/DetalheContrato.jsx         (modificado)

    # Gate 5 — Zero @media print fora dos 2 pontos canônicos (D-13):
    grep -rnE "@media[[:space:]]+print" src --include="*.jsx" --include="*.js" --include="*.css" | \
      grep -v "DecomposicaoSaldoModal.jsx" | grep -v "GerarPeticao.jsx"
    # Esperado: vazio. Se aparecer, significa que alguém adicionou @media print em CSS global
    # ou em outro componente — violação D-13.

    # Gate 6 — Sanidade semântica:
    grep -c "navigator.clipboard.writeText" src/components/DecomposicaoSaldoModal.jsx  # esperado: 1
    grep -cE 'window\\.open\\(\\s*["'"'"'][^"'"'"']*["'"'"']\\s*,\\s*["'"'"']_blank' src/components/DecomposicaoSaldoModal.jsx   # esperado: 1
    grep -cE "@page\\s*\\{\\s*size:\\s*A4" src/components/DecomposicaoSaldoModal.jsx   # esperado: ≥1
    grep -c "Times New Roman" src/components/DecomposicaoSaldoModal.jsx                 # esperado: ≥1
    grep -c "#dc2626" src/components/DecomposicaoSaldoModal.jsx                         # esperado: ≥1
    grep -c "Art. 523 §1º" src/components/DecomposicaoSaldoModal.jsx                    # esperado: 2
    grep -c "Custas judiciais (atualizadas)" src/components/DecomposicaoSaldoModal.jsx  # esperado: 1
    grep -c "<DecomposicaoSaldoModal" src/components/DetalheContrato.jsx                 # esperado: 1
    grep -c "setShowDecomposicaoModal(true)" src/components/DetalheContrato.jsx          # esperado: 1
    ```
  </action>

  <verify>
    <automated>cd src/mr-3/mr-cobrancas &amp;&amp; npm run test:regressao &amp;&amp; npm run build &amp;&amp; [ -z "$(git diff HEAD -- src/utils/devedorCalc.js src/services/pagamentos.js src/services/dividas.js src/services/__tests__/calculos.test.js src/components/ui/Modal.jsx)" ] &amp;&amp; echo "ALL GATES PASS"</automated>
  </verify>

  <acceptance_criteria>
    - `cd src/mr-3/mr-cobrancas && npm run test:regressao` exit 0 (10/10)
    - `cd src/mr-3/mr-cobrancas && npm run build` exit 0
    - `cd src/mr-3/mr-cobrancas && git diff HEAD -- src/utils/devedorCalc.js src/services/pagamentos.js src/services/dividas.js src/services/__tests__/calculos.test.js src/components/ui/Modal.jsx` retorna **vazio** (5 arquivos sem mudança)
    - Escopo combinado (Gate 4) retorna EXATAMENTE 2 linhas: `src/components/DecomposicaoSaldoModal.jsx` (novo) + `src/components/DetalheContrato.jsx` (modificado)
    - Gate 5 (zero @media print fora dos 2 pontos canônicos) retorna vazio
    - Todos os greps semânticos do Gate 6 retornam contadores esperados
  </acceptance_criteria>

  <done>
    Todos os gates determinísticos passaram. Safe para apresentar diff review ao usuário (Task 4 pausa #3).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: PAUSA #3 — diff review antes do commit no submodule (D-15 modo cansado)</name>
  <files>
    src/mr-3/mr-cobrancas/src/components/DecomposicaoSaldoModal.jsx (novo arquivo)
    src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx (modificado)
  </files>

  <what-built>
    Task 1 criou `src/components/DecomposicaoSaldoModal.jsx` (~220 linhas) — wrapper sobre Modal.jsx base (D-06 intocado) com decomposição linha-a-linha, 3 botões, correção negativa red, print A4 isolado via window.open.
    Task 2 integrou o modal em `DetalheContrato.jsx`: import + state + wire onClick da 4ª coluna + render condicional.
    Task 3 confirmou gates determinísticos verdes (regressão 10/10, build exit 0, motor + shield + Modal base intocados, @media print só nos 2 pontos canônicos, escopo 2 arquivos).

    Esta Task 4 é a **pausa #3 das 5 pausas totais do modo cansado (D-15)**:

    1. Agente mostra ao usuário: `git status --short` + `git diff --stat HEAD` + diff detalhado dos 2 arquivos (novo + modificado).
    2. Agente confirma verbalmente: motor intocado + Modal base intocado + shield preservado + @media print isolado + regressão 10/10 + build 0.
    3. Aguarda autorização explícita (`approved` / `commit it`).
    4. Após autorização: `git add` dos 2 arquivos + `git commit` local no submodule `main` com mensagem EXATA `feat(07.8-03): DecomposicaoSaldoModal + impressão isolada A4`.
    5. **Zero `git push`**. Zero commits no pai.
    6. Reportar SHA final + estado "zero push" + próximos passos.
  </what-built>

  <read_first>
    - `.planning/phases/07.8-saldo-atualizado-composicao/07.8-CONTEXT.md` §5 D-15 (5 pausas; esta é a #3), D-17
    - `.planning/phases/07.8-saldo-atualizado-composicao/07.8-02-PLAN.md` Task 3 — template da pausa #2 (mesmo padrão)
    - `.planning/phases/07.8-saldo-atualizado-composicao/07.8-01-PLAN.md` Task 3 — template da pausa #1 (mesmo padrão)
  </read_first>

  <how-to-verify>

**Passo 1 — Mostrar diff completo ao usuário (PAUSA #3):**

```bash
cd src/mr-3/mr-cobrancas

git status --short
# esperado (ordem pode variar):
#   ?? src/components/DecomposicaoSaldoModal.jsx
#   M  src/components/DetalheContrato.jsx

git diff --stat HEAD
# esperado: 1 file changed (DetalheContrato) com +N/-N_del linhas
# (DecomposicaoSaldoModal é untracked, não aparece em diff HEAD stat ainda — mostrar separado)

# Arquivo novo — mostrar conteúdo inline (ou diff vs /dev/null):
echo "=== NEW FILE: src/components/DecomposicaoSaldoModal.jsx ==="
cat src/components/DecomposicaoSaldoModal.jsx
echo "=== END NEW FILE (~$(wc -l < src/components/DecomposicaoSaldoModal.jsx) linhas) ==="

# Arquivo modificado:
git diff HEAD -- src/components/DetalheContrato.jsx

# Invariantes:
git diff HEAD -- src/utils/devedorCalc.js src/services/pagamentos.js src/services/dividas.js src/services/__tests__/calculos.test.js src/components/ui/Modal.jsx
# DEVE SER VAZIO.

git rev-parse --abbrev-ref HEAD
# esperado: main

git log origin/main..main --oneline
# esperado: ≥2 linhas (07.8-02 e 07.8-01 commits pendentes).
```

**Passo 2 — Reportar ao usuário (texto EXATO):**

> ```
> ## PAUSA #3 (07.8-03) — diff review antes do commit no submodule
>
> Gates determinísticos:
> - Regressão: 10/10 PASS (9 TJGO + 1 shield 07.8-01)
> - Build: exit 0
> - Motor Art.354 (devedorCalc.js + pagamentos.js + dividas.js) diff HEAD: VAZIO ✓ (invariante D-01)
> - Teste shield 07.8-01 preservado: `git diff HEAD -- src/services/__tests__/calculos.test.js` VAZIO ✓
> - Modal base (D-06): `git diff HEAD -- src/components/ui/Modal.jsx` VAZIO ✓
> - Zero `@media print` fora dos 2 pontos canônicos (DecomposicaoSaldoModal.jsx + GerarPeticao.jsx) ✓ (D-13)
> - Escopo do diff: APENAS 2 arquivos ✓
>   * NEW: src/components/DecomposicaoSaldoModal.jsx (~220 linhas)
>   * MOD: src/components/DetalheContrato.jsx (+~10 linhas: import + state + wire + render condicional)
>
> DecomposicaoSaldoModal.jsx implementa:
> - Wrapper sobre Modal.jsx base (D-06 intocado).
> - Decomposição linha-a-linha: Valor Original / [+ Correção (indexador)] / [+ Multa] /
>   [+ Juros] / [+ Honorários] / [+ Art.523 Multa+Honorários se total>0] / [+ Custas
>   se atualizado>0] / = Saldo Devedor Atualizado.
> - Linhas zero-value escondidas (D-07); Valor Original e Total sempre visíveis.
> - Correção negativa: sinal `-` + cor vermelha #dc2626 (D-08).
> - Nota rodapé itálico: "Pode haver discrepância de R$ 0,01 por arredondamento." (D-11).
> - 3 botões: Copiar (clipboard + toast, D-12), Imprimir (window.open A4 Times New
>   Roman isolado, D-13), Fechar.
> - escapeHtml defensivo nos nomes injetados no HTML de impressão.
>
> DetalheContrato.jsx alterações:
> - import DecomposicaoSaldoModal from "./DecomposicaoSaldoModal.jsx"
> - const [showDecomposicaoModal, setShowDecomposicaoModal] = useState(false)
> - onClick da 4ª coluna agora chama setShowDecomposicaoModal(true)
> - Render condicional {showDecomposicaoModal && <DecomposicaoSaldoModal ... />}
>   com props detalhe={detalheEncargosContrato} + contrato/credor/devedor/indexadorLabel/dataCalculo/onClose
>
> Diffs completos acima.
>
> Autoriza commit no submodule src/mr-3/mr-cobrancas branch main com subject EXATO:
> `feat(07.8-03): DecomposicaoSaldoModal + impressão isolada A4` ?
>
> Responda **'approved'** ou **'commit it'** para prosseguir.
> Qualquer outra coisa cancela — os 2 arquivos continuam no working tree sem commit.
> ```

**AGUARDAR resposta do usuário.** NÃO prosseguir sem autorização explícita.

Se usuário pedir correção: aplicar, re-rodar Task 3 gates, voltar a PAUSA #3. Se abort: manter arquivos no working tree sem commit e reportar.

**Passo 3 — Após autorização: commit no submodule:**

```bash
cd src/mr-3/mr-cobrancas
git checkout main                                               # garantir main

git add src/components/DecomposicaoSaldoModal.jsx src/components/DetalheContrato.jsx  # APENAS estes 2
git status --short                                              # confirmar SÓ 2 arquivos stageados

git commit -m "$(cat <<'EOF'
feat(07.8-03): DecomposicaoSaldoModal + impressão isolada A4

Phase 7.8 — novo componente DecomposicaoSaldoModal.jsx (wrapper sobre
Modal.jsx base D-06 intocado) que renderiza a composição do saldo atualizado
Art.354 CC linha-a-linha, com 3 botões: Copiar (clipboard + toast),
Imprimir (window.open A4), Fechar.

Body (D-07, D-08, D-09, D-10, D-11):
- Valor Original + Correção (indexador) + Multa + Juros + Honorários.
- Art. 523 §1º (Multa + Honorários 10%) condicionais em art523.total > 0 (D-09).
- Custas judiciais (atualizadas) condicional em custas.atualizado > 0 (D-10).
- Linhas zero-value escondidas EXCETO Valor Original e Total final (D-07).
- Correção negativa: sinal "-" + cor vermelha #dc2626 (D-08).
- Nota rodapé: "Pode haver discrepância de R$ 0,01 por arredondamento." (D-11).

Botão Copiar (D-12):
- navigator.clipboard.writeText(textoFormatado) + toast.success/error.
- Texto pré-formatado multi-linha compatível com WhatsApp/Notes.

Botão Imprimir (D-13):
- Pattern canônico de GerarPeticao.jsx:1006-1014 — window.open("", "_blank",
  "width=900,height=700") + HTML auto-contido.
- @page{size:A4;margin:2.5cm 3cm} + Times New Roman 12pt + line-height 1.8.
- @media print{.bar{display:none}} isolado DENTRO do HTML da janela nova.
- ZERO @media print no app principal.
- escapeHtml defensivo nos nomes injetados.

DetalheContrato.jsx alterações mínimas:
- import DecomposicaoSaldoModal from "./DecomposicaoSaldoModal.jsx"
- state showDecomposicaoModal/setShowDecomposicaoModal (useState false).
- onClick da 4ª coluna "Saldo Atualizado" (plan 07.8-02) agora abre o modal.
- Render condicional {showDecomposicaoModal && <DecomposicaoSaldoModal
  detalhe={detalheEncargosContrato} ... />}.

Invariante D-01: motor Art.354 literalmente intocado.
- src/utils/devedorCalc.js + src/services/pagamentos.js + src/services/dividas.js
  + src/services/__tests__/calculos.test.js: git diff HEAD vazio.
- src/components/ui/Modal.jsx: git diff HEAD vazio (D-06).

Regressão 10/10 PASS (9 TJGO + 1 shield 07.8-01). Build exit 0.

Próximas plans:
- 07.8-04: UAT SC-1..SC-7 + bump submodule no pai (SEM PUSH).
- Tag v1.4-phase7.8: APENAS após UAT prod, ação separada.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"

git log -1 --format=%H                                          # capturar SHA full
git log -1 --format=%h                                          # SHA short
```

**Passo 4 — Reportar SHA final + estado "zero push":**

Formato no chat:
```
## Plan 07.8-03 commitada LOCALMENTE (SEM PUSH)

Submodule src/mr-3/mr-cobrancas @ main:
- <SHA_short> feat(07.8-03): DecomposicaoSaldoModal + impressão isolada A4

Arquivos:
- NEW src/components/DecomposicaoSaldoModal.jsx (~220 linhas)
- MOD src/components/DetalheContrato.jsx (+~10 linhas: import + state + wire + render)

Invariante motor Art.354: PRESERVADO.
Modal base: PRESERVADO (D-06).
Regressão: 10/10 PASS.
Build: exit 0.
Zero @media print fora dos 2 pontos canônicos (DecomposicaoSaldoModal + GerarPeticao).

Commits locais pendentes no submodule main: ≥3 (07.8-01 + 07.8-02 + 07.8-03 desta phase; possíveis anteriores).
Push ainda NÃO autorizado — aguardando 07.8-04 UAT + bump.
Próxima plan: 07.8-04 (UAT SC-1..SC-7 + bump submodule no pai).
```

  </how-to-verify>

  <acceptance_criteria>
    **PAUSA #3 autorizada:**
    - Agente executou `git status --short` + diff detalhado (novo arquivo via cat + modificado via git diff) antes do commit
    - Agente reportou gates Task 3 verdes (regressão 10/10, build 0, motor + shield + Modal base intocados, @media print só canônico, escopo 2 arquivos)
    - Usuário respondeu `approved` / `commit it` / equivalente ANTES do commit
    - Texto do chat contém marcador `PAUSA #3` referenciando 07.8-03

    **Commit correto:**
    - `cd src/mr-3/mr-cobrancas && git rev-parse --abbrev-ref HEAD` retorna `main`
    - `cd src/mr-3/mr-cobrancas && git log -1 --format=%s` retorna EXATAMENTE `feat(07.8-03): DecomposicaoSaldoModal + impressão isolada A4`
    - `cd src/mr-3/mr-cobrancas && git log -1 --name-only --format=` lista EXATAMENTE 2 arquivos (ordem irrelevante):
      - `src/components/DecomposicaoSaldoModal.jsx`
      - `src/components/DetalheContrato.jsx`
    - `cd src/mr-3/mr-cobrancas && git log -1 --format=%b` contém: `Phase 7.8`, `DecomposicaoSaldoModal`, `Modal base`, `D-01`, `D-06`, `D-07`, `D-08`, `D-09`, `D-10`, `D-11`, `D-12`, `D-13`, `window.open`, `@page`, `Times New Roman`, `clipboard`, `10/10`, `Co-Authored-By: Claude Opus 4.7`

    **Zero push / zero tag:**
    - `cd src/mr-3/mr-cobrancas && git log origin/main..main --oneline` retorna **≥3 linhas** (07.8-01 + 07.8-02 + 07.8-03 + possíveis anteriores)
    - **NENHUM** comando `git push` executado
    - **NENHUMA** tag criada: `git tag -l "v1.4-phase7.8*"` vazio

    **Invariante preservada pós-commit:**
    - `cd src/mr-3/mr-cobrancas && git diff HEAD~1 -- src/utils/devedorCalc.js src/services/pagamentos.js src/services/dividas.js src/services/__tests__/calculos.test.js src/components/ui/Modal.jsx` retorna vazio
    - `cd src/mr-3/mr-cobrancas && git show HEAD --stat` mostra EXATAMENTE 2 arquivos desta plan

    **Hygiene:**
    - Nenhum arquivo fora de escopo stageado (confirmar `git show HEAD --stat` = 2 arquivos; `.planning/*` e outros NÃO aparecem)
  </acceptance_criteria>

  <resume-signal>
    - **`approved`** / **`commit it`** / **`next`** → commit criado; plan 07.8-03 completa; próxima = 07.8-04.
    - **`reject <razão>`** / **`cancel`** → arquivos continuam no working tree sem commit.
    - **`fix <detalhe>`** → agente aplica fix, re-roda Task 3, volta a PAUSA #3.
  </resume-signal>

  <done>
    Commit `feat(07.8-03): DecomposicaoSaldoModal + impressão isolada A4` existe no submodule `src/mr-3/mr-cobrancas` branch `main` com APENAS 2 arquivos (DecomposicaoSaldoModal.jsx novo + DetalheContrato.jsx modificado). 3 autorizações acumuladas na phase (PAUSA #1 + #2 + #3). Zero push, zero tag. Próxima plan: 07.8-04 (UAT + bump).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| Agente → Motor Art.354 | D-01 invariante: DecomposicaoSaldoModal consome shape via prop; zero edit em devedorCalc/pagamentos/dividas. |
| Agente → Modal base (ui/Modal.jsx) | D-06: wrapper consome; Modal base intocado. |
| Agente → window.open isolated context | HTML renderizado em janela nova, CSS isolado, input dos nomes (credor/devedor/referência) passa por escapeHtml. |
| Agente → navigator.clipboard (browser API) | Pode falhar em navegadores antigos / contexts não-secure (http em produção) — toast.error cobre o fallback. |
| Agente → Usuário (checkpoint PAUSA #3) | 1 PAUSA bloqueante. Autorização explícita obrigatória. |

## STRIDE Threat Register

| Threat ID | Categoria | Componente | Disposição | Mitigação |
|-----------|-----------|------------|------------|-----------|
| T-07.8-03-01 | Tampering (motor tocado) | Edit acidental em devedorCalc.js/pagamentos.js/dividas.js | mitigate | AC explícita `git diff HEAD` vazio nos arquivos motor. Gate 3 da Task 3. |
| T-07.8-03-02 | Tampering (Modal base tocado — viola D-06) | Edit acidental em ui/Modal.jsx | mitigate | AC `git diff HEAD -- src/components/ui/Modal.jsx` vazio. Gate 3. |
| T-07.8-03-03 | Tampering (shield 07.8-01 alterado) | Edit em calculos.test.js | mitigate | AC diff vazio. Gate 3. |
| T-07.8-03-04 | Injection XSS via HTML string do window.open | Nomes de credor/devedor/referência contêm `<script>` ou aspas | mitigate | Helper `escapeHtml` aplicado a todos os campos dinâmicos antes de concatenar no `document.write`. AC grep `function escapeHtml` presente. |
| T-07.8-03-05 | Pop-up blocker impede impressão | window.open retorna null | accept+mitigate | Check `if (!w) toast.error(...)` no handler. UAT SC-6 cobre cenário feliz; cenário bloqueado reporta toast. |
| T-07.8-03-06 | clipboard API indisponível (http, navegadores antigos) | navigator.clipboard undefined | mitigate | try/catch no handleCopiar + `if (!navigator?.clipboard?.writeText) throw` + toast.error fallback. |
| T-07.8-03-07 | @media print vaza pro app principal | CSS global contaminado | mitigate | Gate 5 da Task 3: `grep -rnE "@media[[:space:]]+print"` fora dos 2 pontos canônicos = vazio. D-13 explícito. |
| T-07.8-03-08 | CSS conflita com card do DetalheContrato | Estilos inline no modal vazam/sobrepõem | accept | Modal já é portal visual (position fixed, z-index 1000 via Modal base). Estilos são inline scoped no componente. |
| T-07.8-03-09 | Tampering (push acidental) | git push inadvertido | mitigate | AC negativa "NENHUM git push". |
| T-07.8-03-10 | Tampering (`git add -A`) | Commit inclui arquivos fora de escopo | mitigate | `git add <files-explicit>`. Passo 3 confirma status = 2 arquivos. |
| T-07.8-03-11 | Elevation of Privilege (usuário fatigado aprova cegamente) | Modo cansado D-15 | accept | D-15 adiciona pausas para mitigar. Risco aceito se user aprova sem revisar. |
| T-07.8-03-12 | Tag criada prematuramente | git tag v1.4-phase7.8 | mitigate | AC `git tag -l` vazio. Tag em ação separada. |
| T-07.8-03-13 | Information Disclosure (commit body) | SHAs, decisões internas no body | accept | Dados intencionalmente expostos para auditoria. Sem secrets. |
| T-07.8-03-14 | Repudiation (commit body inconsistente) | Mensagem diverge | mitigate | HEREDOC literal; AC grep de strings-chave (D-01, D-06, window.open, @page, etc). |
</threat_model>

<verification>
Verificação final da plan 07.8-03 (após Task 4 completa):

1. **Submodule main (estado final):**
   - `git log -1 --format=%s` → `feat(07.8-03): DecomposicaoSaldoModal + impressão isolada A4`
   - `git log -1 --name-only --format=` → 2 linhas (DecomposicaoSaldoModal.jsx + DetalheContrato.jsx)
   - `git log origin/main..main --oneline` → ≥3 linhas (07.8-01 + 07.8-02 + 07.8-03 + possíveis anteriores)

2. **Invariante motor Art.354 preservada (D-01):**
   - `git diff HEAD~1 -- src/utils/devedorCalc.js src/services/pagamentos.js src/services/dividas.js src/services/__tests__/calculos.test.js` → vazios
   - `git show HEAD --stat` → 2 arquivos

3. **Modal base intocado (D-06):**
   - `git diff HEAD~1 -- src/components/ui/Modal.jsx` → vazio

4. **Isolamento do print (D-13):**
   - Gate 5 "zero @media print fora dos 2 pontos canônicos" vazio
   - `grep @page` em DecomposicaoSaldoModal retorna 1; em GerarPeticao 1; zero em outros arquivos

5. **Testes + build:**
   - Regressão 10/10 PASS
   - Build exit 0

6. **Semântica UI verificável via grep:**
   - clipboard.writeText presente
   - window.open presente
   - @page size:A4 presente
   - Times New Roman presente
   - #dc2626 (correção negativa red) presente
   - Art. 523 §1º strings presentes
   - Custas judiciais presente
   - escapeHtml helper presente

7. **Integração em DetalheContrato:**
   - Import DecomposicaoSaldoModal presente
   - State showDecomposicaoModal presente
   - onClick wire setShowDecomposicaoModal(true) presente
   - Render condicional + props corretas

8. **Zero push + zero tag:**
   - Nenhum push executado
   - git tag -l vazio

9. **Pai intocado:**
   - Bump é 07.8-04 Task 2.
</verification>

<success_criteria>
Plan 07.8-03 completa quando:

1. ✅ `src/components/DecomposicaoSaldoModal.jsx` criado com default export
2. ✅ Modal base `ui/Modal.jsx` intocado (D-06)
3. ✅ Body com linhas condicionais (Valor Original sempre; zero-value hidden EXCETO Total; Art.523 se total>0; Custas se atualizado>0)
4. ✅ Correção negativa com sinal `-` + cor `#dc2626` (D-08)
5. ✅ Nota rodapé literal "Pode haver discrepância de R$ 0,01 por arredondamento." (D-11)
6. ✅ Botão Copiar com `navigator.clipboard.writeText` + toast.success/error (D-12)
7. ✅ Botão Imprimir com `window.open("", "_blank", ...)` + `@page size:A4` + `Times New Roman` + `@media print{.bar{display:none}}` isolado (D-13)
8. ✅ `escapeHtml` defensivo nos campos injetados no HTML de impressão
9. ✅ DetalheContrato.jsx: import + state + wire onClick + render condicional
10. ✅ Build exit 0; Regressão 10/10 PASS
11. ✅ Motor Art.354 + shield 07.8-01 intocados; Modal base intocado
12. ✅ Zero `@media print` fora dos 2 pontos canônicos
13. ✅ PAUSA #3 autorizada explicitamente
14. ✅ 1 commit local no submodule `main` com subject EXATO e APENAS 2 arquivos stageados
15. ✅ Zero push, zero tag
16. ✅ Pai intocado
</success_criteria>

<output>
Após Task 4, criar `.planning/phases/07.8-saldo-atualizado-composicao/07.8-03-SUMMARY.md` contendo:

- **SHA:** commit do submodule (full + short)
- **Arquivos:** 2 (DecomposicaoSaldoModal.jsx novo + DetalheContrato.jsx modificado) com contagens de linhas
- **Gates determinísticos:** output de regressão (10/10), build (0), motor + shield + Modal base + @media print isolation (todos VAZIOS ou SÓ canônicos), escopo (2 arquivos)
- **Autorização humana:** texto literal do usuário em PAUSA #3
- **Invariantes preservadas:** D-01 (motor) + D-06 (Modal base) + shield 07.8-01
- **UX entregue:** decomposição linha-a-linha + zero-value hidden + correção negativa red + Art.523/Custas condicionais + rounding footer + 3 botões funcionais + print A4 isolado + clipboard
- **Segurança:** escapeHtml defensivo no print HTML; try/catch no clipboard; pop-up block fallback
- **Zero push + zero tag:** confirmação
- **Próximos passos:**
  1. Plan 07.8-04: UAT SC-1..SC-7 + bump submodule no pai (SEM PUSH)
  2. Tag `v1.4-phase7.8` APENAS pós-UAT prod, ação separada

O SUMMARY é o artefato de fechamento desta plan.
</output>
</content>
