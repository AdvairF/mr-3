---
phase: 07.8-saldo-atualizado-composicao
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mr-3/mr-cobrancas/src/utils/devedorCalc.js
  - src/mr-3/mr-cobrancas/src/services/__tests__/calculos.test.js
autonomous: false   # D-15: modo cansado — pausa #1 (diff review pré-commit) antes do feat(07.8-01) no submodule main. SEM PUSH.
requirements: []    # Gap closure — sem REQ-IDs formais. Contrato de verificação = SC-1..SC-7 em 07.8-CONTEXT.md §6 + D-01..D-18.

must_haves:
  truths:
    - "calcularDetalheEncargosContrato(dividasDoContrato, pagamentos_divida, hoje) existe em src/utils/devedorCalc.js, exportada nomeada, 8-12 linhas (D-02)"
    - "Adapter INTERNAMENTE chama calcularDetalheEncargos existente — ZERO re-implementação do loop Art.354 (D-01)"
    - "Adapter retorna shape IDÊNTICO a calcularDetalheEncargos (valorOriginal, multa, juros, correcao, honorarios, custas, art523, totalEncargos, totalPago, saldoAtualizado, diasEmAtraso, detalhePorDivida) (D-02)"
    - "Motor Art.354 LITERALMENTE intocado: calcularSaldoPorDividaIndividual, calcularSaldoDevedorAtualizado, calcularSaldosPorDivida, calcularTotalPagoPorDivida, calcularDetalheEncargos, calcularPlanilhaCompleta, calcularResumoFinanceiro SEM alteração de corpo (D-01)"
    - "Teste shield NOVO em calculos.test.js testa APENAS o adapter (5-10 linhas) — `calcularDetalheEncargosContrato([], [], 'YYYY-MM-DD')` retorna shape válido com zeros (D-03)"
    - "9 testes originais de calculos.test.js intocados — grep por `calcularPlanilhaCompleta` e `calcularSaldoDevedorAtualizado` continua ≥1 no arquivo (D-03)"
    - "git diff HEAD -- src/utils/devedorCalc.js mostra APENAS `+` lines na seção adicionada ao final — ZERO `-` lines em funções existentes (D-01)"
    - "git diff HEAD -- src/services/pagamentos.js retorna vazio (D-01)"
    - "git diff HEAD -- src/services/dividas.js retorna vazio (D-01)"
    - "npm run test:regressao verde após commit (9/9 originais + 1 shield = 10/10 OU 9/9 preservados + shield passando em describe separado)"
    - "npm run build exit 0 após commit"
    - "Zero UI modificada nesta plan — plans 07.8-02 e 07.8-03 tratam UI"
    - "1 commit local no submodule mr-3 branch main subject EXATO `feat(07.8-01): adapter calcularDetalheEncargosContrato + teste shield` — SEM PUSH"
  artifacts:
    - path: "src/mr-3/mr-cobrancas/src/utils/devedorCalc.js"
      provides: "calcularDetalheEncargosContrato(dividasDoContrato, pagamentos_divida, hoje) — wrapper thin sobre calcularDetalheEncargos(pseudoDevedor, pagamentos, hoje)"
      contains: "export function calcularDetalheEncargosContrato"
    - path: "src/mr-3/mr-cobrancas/src/services/__tests__/calculos.test.js"
      provides: "Teste shield do adapter: shape válido + zeros para devedor vazio"
      contains: "calcularDetalheEncargosContrato"
  key_links:
    - from: "calcularDetalheEncargosContrato (adapter novo)"
      to: "calcularDetalheEncargos (motor existente intocado)"
      via: "construção de pseudoDevedor = { dividas: dividasDoContrato } + delegação total"
      pattern: "calcularDetalheEncargos\\(\\s*\\{\\s*dividas"
    - from: "teste shield novo"
      to: "calcularDetalheEncargosContrato"
      via: "import nomeado + assertions sobre shape (valorOriginal: 0, saldoAtualizado: 0, etc)"
      pattern: "calcularDetalheEncargosContrato\\(\\s*\\[\\s*\\]"
    - from: "motor Art.354 (invariante D-01)"
      to: "git diff HEAD -- src/utils/devedorCalc.js src/services/pagamentos.js src/services/dividas.js"
      via: "só `+` lines na seção adicionada ao final de devedorCalc.js; pagamentos.js e dividas.js vazios"
      pattern: "D-01"
---

<objective>
Adicionar ao motor de cálculo `src/mr-3/mr-cobrancas/src/utils/devedorCalc.js` um adapter thin `calcularDetalheEncargosContrato(dividasDoContrato, pagamentos_divida, hoje)` que delega 100% a `calcularDetalheEncargos` existente (linha 365 do arquivo), passando `{ dividas: dividasDoContrato }` como pseudoDevedor. Tamanho total: 8-12 linhas incluindo JSDoc.

Adicionar também em `src/services/__tests__/calculos.test.js` (mesmo arquivo dos 9 testes de regressão TJGO existentes) um teste shield NOVO de 5-10 linhas que exercita APENAS o adapter com devedor vazio — garante que o wrapping não introduziu regressão trivial (shape válido + zeros para `{ dividas: [] }`). Motor original e seus 9 testes permanecem literalmente intocados.

Purpose: DetalheContrato.jsx (plans 07.8-02 e 07.8-03) precisa chamar a decomposição de encargos do contrato por `dividas` (lista de parcelas do contrato) — mas `calcularDetalheEncargos` expõe assinatura `(devedor, pagamentos, hoje)` orientada ao devedor agregado. Adapter preserva motor intocado (D-01 invariante crítica — regressão 9/9 TJGO por construção) e dá à UI uma assinatura contrato-level idiomática.

Output:
- Adapter `calcularDetalheEncargosContrato` em `utils/devedorCalc.js` (exportada nomeada), localizada no final do arquivo após `calcularPlanilhaCompleta` (linha 823), dentro de uma seção JSDoc clara.
- Teste shield em `services/__tests__/calculos.test.js` (describe isolado ou `it` adicional, 5-10 linhas).
- 1 commit local no submodule `src/mr-3/mr-cobrancas` branch `main` com subject EXATO `feat(07.8-01): adapter calcularDetalheEncargosContrato + teste shield` (D-15).
- **Zero commits no repo pai nesta plan** — bump acontece em 07.8-04 Task 2 após UAT.
- **Zero push** em qualquer momento. Zero tag criada.

Contexto de conformidade:
- D-01 Motor Art.354 intocado — função nova é additiva; `calcularSaldoPorDividaIndividual`, `calcularSaldoDevedorAtualizado`, `calcularSaldosPorDivida`, `calcularTotalPagoPorDivida`, `calcularDetalheEncargos`, `calcularPlanilhaCompleta`, `calcularResumoFinanceiro` NÃO alteradas.
- D-02 Nome EXATO `calcularDetalheEncargosContrato`, assinatura `(dividasDoContrato, pagamentos_divida, hoje)`, retorno shape idêntico a `calcularDetalheEncargos`.
- D-03 Teste shield só do adapter, em mesmo arquivo dos 9 regressão, pseudo-devedor vazio → zeros.
- D-15 Modo cansado — plan `autonomous: false`, Task 3 é pausa #1 de diff review pré-commit.
- D-17 Convenções: submodule mr-3 branch main, zero push sem autorização.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07.8-saldo-atualizado-composicao/07.8-CONTEXT.md

<!-- Precedentes do padrão "additive-only, SEM PUSH, pausa pré-commit" (seguir estrutura): -->
@.planning/phases/07.7-excluir-documento-individual/07.7-01-PLAN.md

<interfaces>
<!-- Interfaces-chave do codebase atual. Executor usa DIRETO sem explorar. -->

From src/mr-3/mr-cobrancas/src/utils/devedorCalc.js:365 (função EXISTENTE — motor Art.354, INTOCADA):
```javascript
/**
 * @param {Object} devedor  devedor com { dividas: [...] }
 * @param {Array}  pagamentos
 * @param {string} hoje "YYYY-MM-DD"
 * @returns {{ valorOriginal, multa, juros, correcao, honorarios, custas,
 *             totalEncargos, totalPago, saldoAtualizado, diasEmAtraso, detalhePorDivida }}
 */
export function calcularDetalheEncargos(devedor, pagamentos, hoje) {
  const dividasCalc = parseDividas(devedor?.dividas).filter((d) => !d._nominal && !d._so_custas);
  // ... loop Art.354 completo, 200+ linhas
  // retorna { valorOriginal, multa: {valor}, juros: {valor}, correcao: {valor},
  //          honorarios: {valor}, custas: {original, atualizado},
  //          art523: {multa, honorarios, total}, totalEncargos, totalPago,
  //          saldoAtualizado, diasEmAtraso, detalhePorDivida: [...] }
}
```

**OBSERVAÇÃO-CHAVE (D-02):** `calcularDetalheEncargos` acessa `devedor?.dividas` com optional chaining — tolera `devedor` nulo ou sem `dividas`. Adapter constroi pseudoDevedor explícito pra garantir invariante.

From src/mr-3/mr-cobrancas/src/utils/devedorCalc.js:570 (função EXISTENTE — INTOCADA):
```javascript
export function calcularPlanilhaCompleta(devedor, pagamentos, hoje) { /* ... */ }
// Linha 823: fim do arquivo com `}`. Adapter vai DEPOIS dessa chave.
```

From src/mr-3/mr-cobrancas/src/services/__tests__/calculos.test.js linhas 13-22 (imports EXISTENTES — REUTILIZAR, NÃO adicionar import do arquivo inteiro):
```javascript
import { describe, it, expect } from "vitest";
import {
  calcularFatorCorrecao,
  calcularJurosArt406,
  calcularJurosArt406_12aa,
  calcularJurosAcumulados,
  calcularArt523,
} from "../../utils/correcao.js";
import { calcularSaldoDevedorAtualizado, calcularPlanilhaCompleta } from "../../utils/devedorCalc.js";
import casos from "./casos-tjgo.json";
```

**Ação do Task 2 (teste shield):** estender o import existente da linha 21 pra incluir `calcularDetalheEncargosContrato`:
```javascript
import { calcularSaldoDevedorAtualizado, calcularPlanilhaCompleta, calcularDetalheEncargosContrato } from "../../utils/devedorCalc.js";
```

Zero novos imports além desse; `describe`, `it`, `expect` já disponíveis.

Shape esperado para `calcularDetalheEncargosContrato([], [], "2026-04-24")` (CONTEXT §6 SC-2):
```javascript
{
  valorOriginal: 0,
  multa: { valor: 0, ... },
  juros: { valor: 0, ... },
  correcao: { valor: 0, ... },
  honorarios: { valor: 0, ... },
  custas: { original: 0, atualizado: 0 },
  art523: { multa: 0, honorarios: 0, total: 0 },
  totalEncargos: 0,
  totalPago: 0,
  saldoAtualizado: 0,
  diasEmAtraso: 0,    // ou null — verificar o que o motor retorna; teste não assume valor específico
  detalhePorDivida: []
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implementar adapter calcularDetalheEncargosContrato em src/utils/devedorCalc.js</name>
  <files>src/mr-3/mr-cobrancas/src/utils/devedorCalc.js</files>

  <read_first>
    - `src/mr-3/mr-cobrancas/src/utils/devedorCalc.js` linhas 1-30 (imports do motor — adapter NÃO precisa de nada novo; `calcularDetalheEncargos` já está no mesmo arquivo)
    - `src/mr-3/mr-cobrancas/src/utils/devedorCalc.js` linhas 360-410 (assinatura de `calcularDetalheEncargos` — confirmar `(devedor, pagamentos, hoje)` com optional chaining em `devedor?.dividas`)
    - `src/mr-3/mr-cobrancas/src/utils/devedorCalc.js` linhas 815-823 (fim do arquivo — a chave `}` fecha `calcularPlanilhaCompleta`; adapter vai DEPOIS dessa linha)
    - `.planning/phases/07.8-saldo-atualizado-composicao/07.8-CONTEXT.md` §4 (Fix A) + §5 D-01, D-02 (invariante + assinatura), §6 SC-1, §8 ("Motor Art.354 já expõe decomposição")
  </read_first>

  <action>
    **Localização da nova função:** Inserir APÓS a linha 823 (chave `}` que fecha `calcularPlanilhaCompleta`) e ANTES de qualquer trailing newline/EOF. Adicionar um header de seção novo antes do JSDoc.

    **Código a inserir (LITERAL — 10 linhas de código + JSDoc; zero re-implementação):**

    ```javascript

    // ─── ADAPTER CONTRATO-LEVEL (Phase 7.8) ──────────────────────────────────────

    /**
     * Adapter thin para consumo em DetalheContrato.jsx.
     *
     * Phase 7.8 (D-02) — envolve `calcularDetalheEncargos` existente com uma
     * assinatura contrato-level que recebe a lista de parcelas (`dividas`) do
     * contrato diretamente, em vez do objeto devedor agregado. Motor Art.354
     * INTOCADO (D-01) — zero duplicação de lógica; delegação pura.
     *
     * @param {Array}  dividasDoContrato  parcelas do contrato (mesmo shape de devedor.dividas)
     * @param {Array}  pagamentos_divida  pagamentos em pagamentos_divida para as parcelas
     * @param {string} hoje               "YYYY-MM-DD"
     * @returns {ReturnType<typeof calcularDetalheEncargos>} shape idêntico ao original
     */
    export function calcularDetalheEncargosContrato(dividasDoContrato, pagamentos_divida, hoje) {
      const pseudoDevedor = { dividas: dividasDoContrato || [] };
      return calcularDetalheEncargos(pseudoDevedor, pagamentos_divida || [], hoje);
    }
    ```

    **Detalhes obrigatórios:**
    - Nome literal: `calcularDetalheEncargosContrato` (export nomeado, NÃO default).
    - Assinatura EXATA: `(dividasDoContrato, pagamentos_divida, hoje)` — 3 argumentos posicionais, nomes exatos no parameter list.
    - Corpo: APENAS 2 linhas de lógica (construção do pseudoDevedor + return delegado). Zero loops, zero condicionais, zero re-implementação.
    - Normalização defensiva: `dividasDoContrato || []` e `pagamentos_divida || []` — se caller passar `undefined/null`, adapter entrega array vazio ao motor (garante shape válido com zeros — SC-2 CONTEXT).
    - NÃO adicionar novos imports (calcularDetalheEncargos está no mesmo arquivo).
    - NÃO tocar `calcularDetalheEncargos`, `calcularPlanilhaCompleta`, `calcularSaldoPorDividaIndividual`, `calcularSaldoDevedorAtualizado`, `calcularSaldosPorDivida`, `calcularTotalPagoPorDivida`, `calcularResumoFinanceiro`, nem qualquer helper interno existente.
    - NÃO tocar `src/services/pagamentos.js` nem `src/services/dividas.js` (invariante motor D-01).
  </action>

  <verify>
    <automated>cd src/mr-3/mr-cobrancas &amp;&amp; node -e "import('./src/utils/devedorCalc.js').then(m =&gt; { if (typeof m.calcularDetalheEncargosContrato !== 'function') { console.error('FAIL: calcularDetalheEncargosContrato não exportada'); process.exit(1); } const r = m.calcularDetalheEncargosContrato([], [], '2026-04-24'); if (!r || typeof r !== 'object' || r.valorOriginal !== 0 || r.saldoAtualizado !== 0) { console.error('FAIL: shape inválido', r); process.exit(1); } console.log('OK: adapter exportado + shape válido com zeros'); })"</automated>
  </verify>

  <acceptance_criteria>
    <!-- Cada item é grep-verificável. Rodar da raiz do submodule src/mr-3/mr-cobrancas. -->
    - `grep -c "export function calcularDetalheEncargosContrato" src/utils/devedorCalc.js` retorna **1**
    - `grep -c "export function calcularDetalheEncargos" src/utils/devedorCalc.js` retorna ≥**2** (1 original em linha 365 + 1 do adapter novo que começa com "calcularDetalheEncargosContrato" — o grep pega por prefixo; ajustar regex se necessário)
    - `grep -nE "^export function calcularDetalheEncargos\\b" src/utils/devedorCalc.js` retorna **1 linha** (a função ORIGINAL em linha 365, com boundary `\b` — não pega o adapter)
    - `grep -nE "^export function calcularDetalheEncargosContrato\\b" src/utils/devedorCalc.js` retorna **1 linha** (o adapter novo no final do arquivo)
    - `grep -c "return calcularDetalheEncargos(" src/utils/devedorCalc.js` retorna **≥1** (o adapter delega ao motor; também pode ter calls internos pré-existentes — esperado ≥1)
    - `grep -c "const pseudoDevedor" src/utils/devedorCalc.js` retorna **1** (só o adapter usa essa variável)
    - **Motor Art.354 invariante (D-01) — APENAS ADIÇÕES:** `git diff HEAD -- src/utils/devedorCalc.js | grep -cE "^-[^-]"` retorna **0** (zero linhas removidas; só `+` lines; o `[^-]` exclui o header `---` do diff)
    - **Motor Art.354 invariante (D-01) — arquivos irmãos NÃO tocados:** `git diff HEAD -- src/services/pagamentos.js` retorna **vazio** (0 bytes)
    - **Motor Art.354 invariante (D-01) — arquivos irmãos NÃO tocados:** `git diff HEAD -- src/services/dividas.js` retorna **vazio** (0 bytes)
    - **Funções existentes preservadas — grep de controle:** `grep -c "export function calcularSaldoPorDividaIndividual" src/utils/devedorCalc.js` retorna **1**; `grep -c "export function calcularSaldoDevedorAtualizado" src/utils/devedorCalc.js` retorna **1**; `grep -c "export function calcularPlanilhaCompleta" src/utils/devedorCalc.js` retorna **1**; `grep -c "export function calcularResumoFinanceiro" src/utils/devedorCalc.js` retorna **1** (todas preservadas)
    - **Escopo do diff (esta task específica):** `git diff --name-only HEAD` retorna EXATAMENTE `src/utils/devedorCalc.js` (só este arquivo modificado nesta task; `calculos.test.js` entra em Task 2)
    - Comando do `<verify>` automated retorna "OK: adapter exportado + shape válido com zeros"
  </acceptance_criteria>

  <done>
    `calcularDetalheEncargosContrato(dividasDoContrato, pagamentos_divida, hoje)` existe em `src/utils/devedorCalc.js`, exportada nomeada, 10-15 linhas incluindo JSDoc, delega 100% a `calcularDetalheEncargos` via pseudoDevedor. Motor Art.354 literalmente intocado (zero `-` lines no diff). `services/pagamentos.js` e `services/dividas.js` diff vazio. Ainda não commitado.
  </done>
</task>

<task type="auto">
  <name>Task 2: Teste shield do adapter em src/services/__tests__/calculos.test.js (5-10 linhas)</name>
  <files>src/mr-3/mr-cobrancas/src/services/__tests__/calculos.test.js</files>

  <read_first>
    - `src/mr-3/mr-cobrancas/src/services/__tests__/calculos.test.js` linhas 1-30 (imports existentes — especialmente linha 21 onde está `calcularSaldoDevedorAtualizado, calcularPlanilhaCompleta`; é ali que `calcularDetalheEncargosContrato` será adicionado à destructuring)
    - `src/mr-3/mr-cobrancas/src/services/__tests__/calculos.test.js` linhas 180-199 (final do arquivo — onde o novo `describe` shield será inserido, DEPOIS dos 9 testes originais, ANTES do fechamento do arquivo)
    - `.planning/phases/07.8-saldo-atualizado-composicao/07.8-CONTEXT.md` §5 D-03 (teste shield SÓ do adapter, 5-10 linhas)
  </read_first>

  <action>
    **Passo A — Estender import existente (linha 21 do arquivo):**

    Linha atual:
    ```javascript
    import { calcularSaldoDevedorAtualizado, calcularPlanilhaCompleta } from "../../utils/devedorCalc.js";
    ```

    Alterar para:
    ```javascript
    import { calcularSaldoDevedorAtualizado, calcularPlanilhaCompleta, calcularDetalheEncargosContrato } from "../../utils/devedorCalc.js";
    ```

    **APENAS** adicionar o novo nome à lista; NÃO remover `calcularSaldoDevedorAtualizado` nem `calcularPlanilhaCompleta` (esses são os 2 imports usados pelos 9 testes de regressão TJGO — remover quebra TODOS).

    **Passo B — Adicionar describe novo NO FINAL do arquivo**, após a última linha executável dos 9 testes existentes e antes de qualquer trailing newline. Bloco a inserir (LITERAL, 10-12 linhas):

    ```javascript

    // ─── SHIELD: adapter contrato-level (Phase 7.8, D-03) ────────────────────────
    // Valida APENAS o adapter (wrapping thin). Motor Art.354 já validado pelos 9
    // casos TJGO acima. Contrato mínimo: devedor vazio → shape válido com zeros.

    describe("calcularDetalheEncargosContrato (adapter contrato-level, Phase 7.8)", () => {
      it("retorna shape válido com zeros para contrato vazio", () => {
        const r = calcularDetalheEncargosContrato([], [], "2026-04-24");
        expect(r).toBeTypeOf("object");
        expect(r.valorOriginal).toBe(0);
        expect(r.saldoAtualizado).toBe(0);
        expect(r.totalEncargos).toBe(0);
        expect(r.totalPago).toBe(0);
        expect(Array.isArray(r.detalhePorDivida)).toBe(true);
        expect(r.detalhePorDivida).toHaveLength(0);
      });
    });
    ```

    **Detalhes obrigatórios:**
    - Import da linha 21: estender apenas (não substituir linha inteira para um import diferente).
    - `describe` com label EXATO `"calcularDetalheEncargosContrato (adapter contrato-level, Phase 7.8)"`.
    - Um único `it` com 1 caso de teste de shape — pseudo-devedor vazio.
    - Assertions APENAS sobre campos estruturais com valores determinísticos (zeros + array vazio). NÃO asserting sobre `multa.valor`, `juros.valor`, etc — o motor retorna shape aninhado e podemos mudar nesta plan o subset mínimo que o teste verifica. Se o executor quiser mais cobertura, adicionar `expect(r.multa).toBeTypeOf("object")` OU parar em `totalEncargos` + `totalPago` (suficiente pra pegar regressão trivial).
    - NÃO tocar NENHUM dos 9 casos TJGO existentes (funções `calcularCasoCompleto`, blocos `describe`/`it` anteriores).
    - NÃO adicionar `import casos from` novo (ajá importado linha 22).
    - Opcionalmente adicionar `expect(typeof r.multa).toBe("object")` se quiser 1 linha extra de robustez — máximo 10 linhas dentro do `describe`.
  </action>

  <verify>
    <automated>cd src/mr-3/mr-cobrancas &amp;&amp; npm run test:regressao</automated>
  </verify>

  <acceptance_criteria>
    <!-- Rodar da raiz do submodule src/mr-3/mr-cobrancas. -->
    - `grep -c "calcularDetalheEncargosContrato" src/services/__tests__/calculos.test.js` retorna **≥2** (1 no import + ≥1 na chamada do teste)
    - `grep -n "import.*calcularSaldoDevedorAtualizado.*calcularPlanilhaCompleta.*calcularDetalheEncargosContrato.*from.*devedorCalc" src/services/__tests__/calculos.test.js` retorna **1 linha** (import estendido na linha 21; pode aceitar ordem alternativa dos nomes desde que os 3 estejam presentes)
    - `grep -c 'calcularDetalheEncargosContrato(\[\]' src/services/__tests__/calculos.test.js` retorna **1** (chamada do teste shield com array vazio)
    - **9 testes originais preservados (grep de controle D-03):** `grep -c "calcularSaldoDevedorAtualizado" src/services/__tests__/calculos.test.js` retorna ≥**1** (import + uso nos testes originais); `grep -c "calcularPlanilhaCompleta" src/services/__tests__/calculos.test.js` retorna ≥**1**; `grep -c "casos-tjgo.json" src/services/__tests__/calculos.test.js` retorna ≥**1** (import dos 9 casos TJGO original)
    - **describe shield presente:** `grep -c 'describe("calcularDetalheEncargosContrato' src/services/__tests__/calculos.test.js` retorna **1**
    - **Regressão 9/9 + shield (bloqueante):** `npm run test:regressao` exit 0. Esperado em output: `10 passed` OU `9 passed` + shield em describe separado passando. Qualquer falha → ABORTAR.
    - **Build (bloqueante):** `npm run build` exit 0.
    - **Motor Art.354 invariante (D-01) — ainda vazio após Task 2:** `git diff HEAD -- src/services/pagamentos.js` retorna **vazio**; `git diff HEAD -- src/services/dividas.js` retorna **vazio**.
    - **Escopo cumulativo da plan:** `git diff --name-only HEAD` retorna EXATAMENTE 2 arquivos (order-independent):
      - `src/utils/devedorCalc.js`
      - `src/services/__tests__/calculos.test.js`
    - **devedorCalc.js continua só adições (D-01):** `git diff HEAD -- src/utils/devedorCalc.js | grep -cE "^-[^-]"` retorna **0**
  </acceptance_criteria>

  <done>
    Teste shield adicionado em `services/__tests__/calculos.test.js` exercitando `calcularDetalheEncargosContrato([], [], "YYYY-MM-DD")` → shape válido com zeros. Import linha 21 estendido (3 nomes em vez de 2). 9 testes TJGO originais intocados. `npm run test:regressao` retorna 10/10 (ou 9/9 + shield describe passando). `npm run build` exit 0. `services/pagamentos.js` e `services/dividas.js` diff vazio. Safe para Task 3 (pausa #1).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: PAUSA #1 — diff review antes do commit no submodule (D-15 modo cansado)</name>
  <files>
    src/mr-3/mr-cobrancas/src/utils/devedorCalc.js (commit local no submodule main — SEM PUSH)
    src/mr-3/mr-cobrancas/src/services/__tests__/calculos.test.js (commit local no submodule main — SEM PUSH)
  </files>

  <what-built>
    Task 1 adicionou `calcularDetalheEncargosContrato` ao final de `src/utils/devedorCalc.js` (10-15 linhas, adapter puro). Task 2 adicionou teste shield em `calculos.test.js` (describe + 1 it, 10-12 linhas) e estendeu o import da linha 21.

    Esta Task 3 é a **pausa #1 das 5 pausas totais do modo cansado (D-15)**:

    1. Agente mostra ao usuário:
       - `git status --short` no submodule (esperado 2 arquivos modified).
       - `git diff --stat HEAD` (esperado 2 files changed, ~30 insertions, 0 deletions).
       - `git diff HEAD -- src/utils/devedorCalc.js` (só `+` lines ao final).
       - `git diff HEAD -- src/services/__tests__/calculos.test.js` (só `+` lines + 1 `-`/`+` no import se o import foi substituído por re-write; ideal é só `+` no describe novo e `-/+` na linha 21 do import — aceitável).
       - `git diff HEAD -- src/services/pagamentos.js src/services/dividas.js` (vazio — invariante motor D-01).
    2. Agente confirma verbalmente: motor Art.354 intocado por invariante construtivo (só adições em devedorCalc.js na seção nova; pagamentos.js e dividas.js vazios), regressão 9/9 + shield = 10/10 passando, build exit 0, diff escopo = 2 arquivos.
    3. Aguarda autorização explícita do usuário (`approved` / `commit it` / equivalente).
    4. Após autorização: `git add` dos 2 arquivos + `git commit` local no submodule `main` com mensagem EXATA `feat(07.8-01): adapter calcularDetalheEncargosContrato + teste shield`.
    5. **Zero `git push`** em qualquer ponto. Zero commits no pai (bump é 07.8-04 Task 2).
    6. Reportar SHA do commit no final e encerrar a plan.
  </what-built>

  <read_first>
    - `.planning/phases/07.8-saldo-atualizado-composicao/07.8-CONTEXT.md` §5 D-15 (5 pausas; esta é a pausa #1), D-17 (convenção commit local, zero push)
    - `.planning/phases/07.7-excluir-documento-individual/07.7-01-PLAN.md` Task 3 Passos 1-4 — **template literal** do padrão commit-no-submodule-SEM-PUSH com pausa
    - `.planning/STATE.md` `last_activity` — confirma padrão histórico "commits locais, aguarda autorização"
  </read_first>

  <how-to-verify>

**Passo 1 — Gates determinísticos finais (antes de chamar PAUSA):**

```bash
cd src/mr-3/mr-cobrancas

# Gate 1 — Regressão 9/9 + shield:
npm run test:regressao
# Esperado: exit 0. Output contém "passed" — total ≥10 OU 9 TJGO + shield (aceitáveis).

# Gate 2 — Build:
npm run build
# Esperado: exit 0.

# Gate 3 — Invariante motor (D-01):
git diff HEAD -- src/services/pagamentos.js
git diff HEAD -- src/services/dividas.js
# Esperado: ambos vazios (0 bytes).

# Gate 4 — devedorCalc.js SÓ adições (D-01):
git diff HEAD -- src/utils/devedorCalc.js | grep -cE "^-[^-]"
# Esperado: 0 (zero linhas `-` fora do header `---` do diff).

# Gate 5 — Escopo:
git diff --name-only HEAD
# Esperado: EXATAMENTE 2 linhas (order-independent):
#   src/utils/devedorCalc.js
#   src/services/__tests__/calculos.test.js
```

Se QUALQUER gate falhar, **ABORTAR** e reportar ao usuário. NÃO avançar para PAUSA.

**Passo 2 — Mostrar diff completo ao usuário (PAUSA #1):**

```bash
cd src/mr-3/mr-cobrancas
git status --short
# esperado: 2 linhas "M src/utils/devedorCalc.js" e "M src/services/__tests__/calculos.test.js"

git diff --stat HEAD
# esperado: 2 files changed, +N insertions (≈25-30), N_del deletions (0 ou 1 se o import linha 21 foi substituído inline)

git diff HEAD -- src/utils/devedorCalc.js
# diff completo do adapter novo no final do arquivo.

git diff HEAD -- src/services/__tests__/calculos.test.js
# diff do import estendido (linha 21) + describe shield novo no final.

git diff HEAD -- src/services/pagamentos.js src/services/dividas.js
# DEVE SER VAZIO (gate invariante motor D-01).

git rev-parse --abbrev-ref HEAD
# esperado: main

git log origin/main..main --oneline
# pode estar vazio (primeira plan da phase 7.8) OU conter commits pendentes de phases anteriores
# (7.5.1 etc — nunca pushed; ver STATE.md `last_activity`). OK.
```

**Passo 3 — Reportar ao usuário (texto EXATO):**

> ```
> ## PAUSA #1 (07.8-01) — diff review antes do commit no submodule
>
> Gates determinísticos:
> - Regressão: 9/9 + shield passando (total 10/10 ou 9 TJGO + 1 shield em describe separado)
> - Build: exit 0
> - Motor Art.354 (pagamentos.js + dividas.js) diff HEAD: VAZIO ✓ (invariante D-01)
> - devedorCalc.js: APENAS adições (0 `-` lines fora do header do diff) ✓ (invariante D-01 construtivo)
> - Escopo do diff: APENAS src/utils/devedorCalc.js + src/services/__tests__/calculos.test.js ✓
>
> Adicionado: calcularDetalheEncargosContrato(dividasDoContrato, pagamentos_divida, hoje)
> — adapter thin (10 linhas + JSDoc) que delega 100% a calcularDetalheEncargos existente
> via pseudoDevedor = { dividas: dividasDoContrato || [] }. Motor Art.354 intocado por
> construção (D-01/D-02). Teste shield adicionado em calculos.test.js (describe isolado,
> valida shape + zeros para contrato vazio, 10-12 linhas) sem alterar nenhum dos 9 casos
> TJGO existentes (D-03).
>
> Diff completo acima.
>
> Autoriza commit no submodule src/mr-3/mr-cobrancas branch main com subject EXATO:
> `feat(07.8-01): adapter calcularDetalheEncargosContrato + teste shield` ?
>
> Responda **'approved'** ou **'commit it'** para prosseguir.
> Qualquer outra coisa cancela — os 2 arquivos continuam modificados localmente sem commit.
> ```

**AGUARDAR resposta do usuário.** NÃO prosseguir sem autorização explícita.

Se o usuário apontar correção: aplicar, re-rodar gates Passo 1, voltar a PAUSA. Se pedir abort: deixar os 2 arquivos modificados localmente sem commit e reportar.

**Passo 4 — Após autorização: commit no submodule:**

```bash
cd src/mr-3/mr-cobrancas
git checkout main                                       # garantir que está em main (não detached HEAD)
git add src/utils/devedorCalc.js src/services/__tests__/calculos.test.js   # APENAS estes 2 arquivos
git status --short                                      # confirmar SOMENTE os 2 arquivos stageados, nada mais

git commit -m "$(cat <<'EOF'
feat(07.8-01): adapter calcularDetalheEncargosContrato + teste shield

Phase 7.8 — adiciona wrapper thin contrato-level sobre calcularDetalheEncargos
existente (devedorCalc.js:365). Assinatura (dividasDoContrato, pagamentos_divida,
hoje) — preserva shape de retorno do motor (valorOriginal, multa, juros, correcao,
honorarios, custas, art523, totalEncargos, totalPago, saldoAtualizado,
diasEmAtraso, detalhePorDivida).

Pseudo-devedor { dividas: dividasDoContrato || [] } passado ao motor garante
shape válido mesmo com contrato vazio (SC-2/SC-7 do CONTEXT.md). Zero
re-implementação da lógica Art.354 — delegação pura.

Invariante D-01: motor Art.354 literalmente intocado.
- calcularSaldoPorDividaIndividual, calcularSaldoDevedorAtualizado,
  calcularSaldosPorDivida, calcularTotalPagoPorDivida, calcularDetalheEncargos,
  calcularPlanilhaCompleta, calcularResumoFinanceiro: SEM alteração de corpo.
- src/services/pagamentos.js + src/services/dividas.js: git diff HEAD vazio.
- devedorCalc.js: apenas adições no final (zero `-` lines fora do header do diff).

Teste shield NOVO em calculos.test.js (describe isolado, 1 it) valida shape
+ zeros para calcularDetalheEncargosContrato([], [], "YYYY-MM-DD"). 9 casos
TJGO existentes permanecem intocados. Regressão total: 10/10 PASS.

Próximas plans:
- 07.8-02: 4ª coluna "Saldo Atualizado" em DetalheContrato.jsx
  (com useMemo e hide quando num_documentos===0).
- 07.8-03: DecomposicaoSaldoModal + impressão A4 isolada.
- 07.8-04: UAT SC-1..SC-7 + bump submodule no pai (SEM PUSH).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"

git log -1 --format=%H                                   # capturar SHA full
git log -1 --format=%h                                   # SHA short
```

**Passo 5 — Reportar SHA final + estado "zero push":**

Formato no chat:
```
## Plan 07.8-01 commitada LOCALMENTE (SEM PUSH)

Submodule src/mr-3/mr-cobrancas @ main:
- <SHA_short> feat(07.8-01): adapter calcularDetalheEncargosContrato + teste shield

Arquivos: src/utils/devedorCalc.js (+~30 linhas) + src/services/__tests__/calculos.test.js (+~12 linhas)

Invariante motor Art.354: PRESERVADO (pagamentos.js + dividas.js diff vazio; devedorCalc.js só adições).
Regressão: 10/10 PASS (9 TJGO + 1 shield).
Build: exit 0.

Push ainda NÃO autorizado — aguardando 07.8-02 + 07.8-03 + UAT 07.8-04.
Próxima plan: 07.8-02 (4ª coluna Saldo Atualizado em DetalheContrato.jsx).
```

  </how-to-verify>

  <acceptance_criteria>
    **PAUSA #1 autorizada:**
    - Agente executou `git status --short` + `git diff --stat HEAD` + `git diff HEAD` (pelos 4 arquivos relevantes) e mostrou ao usuário ANTES do commit
    - Agente reportou gates Passo 1 todos verdes (regressão 10/10, build 0, pagamentos.js+dividas.js vazios, devedorCalc.js só adições, escopo = 2 arquivos)
    - Usuário respondeu `approved`, `commit it`, ou equivalente explícito ANTES do commit
    - Texto do chat contém marcador `PAUSA #1` ou equivalente referenciando 07.8-01

    **Commit correto:**
    - `cd src/mr-3/mr-cobrancas && git rev-parse --abbrev-ref HEAD` retorna `main`
    - `cd src/mr-3/mr-cobrancas && git log -1 --format=%s` retorna EXATAMENTE `feat(07.8-01): adapter calcularDetalheEncargosContrato + teste shield`
    - `cd src/mr-3/mr-cobrancas && git log -1 --name-only --format=` lista EXATAMENTE 2 arquivos (em qualquer ordem):
      - `src/utils/devedorCalc.js`
      - `src/services/__tests__/calculos.test.js`
    - `cd src/mr-3/mr-cobrancas && git log -1 --format=%b` contém as strings: `Phase 7.8`, `calcularDetalheEncargosContrato`, `D-01`, `Motor Art.354`, `pagamentos.js`, `dividas.js`, `shield`, `10/10`, `Co-Authored-By: Claude Opus 4.7`

    **Zero push / zero tag:**
    - `cd src/mr-3/mr-cobrancas && git log origin/main..main --oneline` retorna **≥1 linha** (o novo commit local; pode ter outros de phases anteriores)
    - **NENHUM** comando `git push` executado nesta task (histórico do shell deve confirmar)
    - **NENHUMA** tag criada: `git tag -l "v1.4-phase7.8*"` retorna vazio

    **Invariante motor preservada pós-commit:**
    - `cd src/mr-3/mr-cobrancas && git diff HEAD~1 -- src/services/pagamentos.js` retorna vazio
    - `cd src/mr-3/mr-cobrancas && git diff HEAD~1 -- src/services/dividas.js` retorna vazio
    - `cd src/mr-3/mr-cobrancas && git show HEAD --stat` mostra EXATAMENTE 2 arquivos (devedorCalc.js + calculos.test.js)
    - `cd src/mr-3/mr-cobrancas && git show HEAD -- src/utils/devedorCalc.js | grep -cE "^-[^-]"` retorna **0** (só adições no commit)
  </acceptance_criteria>

  <resume-signal>
    Para prosseguir:
    - **`approved`** / **`commit it`** / **`next`** → commit criado localmente no submodule; plan 07.8-01 completa; próxima = 07.8-02.
    - **`reject <razão>`** / **`cancel`** → os 2 arquivos continuam modificados localmente sem commit; agente reporta e encerra plan sem commit.
    - **`fix <detalhe>`** → agente aplica fix, re-roda gates Passo 1, volta a PAUSA #1.
  </resume-signal>

  <done>
    Commit `feat(07.8-01): adapter calcularDetalheEncargosContrato + teste shield` existe no submodule `src/mr-3/mr-cobrancas` branch `main` com APENAS 2 arquivos stageados (devedorCalc.js + calculos.test.js). 1 autorização humana registrada (PAUSA #1). Zero push, zero tag. Próxima plan: 07.8-02 (4ª coluna no Resumo Financeiro).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| Agente → Motor Art.354 (devedorCalc.js funções existentes) | Invariante crítica D-01: adapter é ADDITIVO; qualquer byte removido de função existente = regressão bloqueante. |
| Agente → Git CLI do submodule | Comandos locais (git add/commit, npm test/build). Sem boundary de rede — zero push. |
| Agente → Usuário (checkpoint PAUSA #1) | 1 PAUSA bloqueante. Sem fallback automatizado — autorização explícita obrigatória. |

## STRIDE Threat Register

| Threat ID | Categoria | Componente | Disposição | Mitigação |
|-----------|-----------|------------|------------|-----------|
| T-07.8-01-01 | Tampering (motor tocado indevidamente) | Adapter edit inadvertidamente altera `calcularDetalheEncargos` ou helpers internos | mitigate | Acceptance criteria `git diff HEAD -- src/utils/devedorCalc.js \| grep -cE "^-[^-]"` retorna 0. Se qualquer `-` aparecer fora do header do diff, ABORTAR. |
| T-07.8-01-02 | Tampering (arquivos irmãos tocados) | Adapter edit cascata em `services/pagamentos.js` ou `services/dividas.js` | mitigate | AC explícita `git diff HEAD -- src/services/pagamentos.js src/services/dividas.js` vazio. Gate 3 da Task 3 Passo 1. |
| T-07.8-01-03 | Tampering (9 testes TJGO quebrados) | Import linha 21 substituído incorretamente, perdendo `calcularSaldoDevedorAtualizado` ou `calcularPlanilhaCompleta` | mitigate | `npm run test:regressao` 10/10 como gate bloqueante antes da PAUSA. Grep por nomes originais ≥1. |
| T-07.8-01-04 | Tampering (push acidental) | Agente invoca `git push` inadvertidamente | mitigate | AC explicitamente negativa: "NENHUM comando git push". Neste plan, commits ficam locais aguardando 07.8-04 bump. |
| T-07.8-01-05 | Tampering (`git add -A` inclui arquivos fora de escopo) | Commit final inclui `.planning/STATE.md` etc | mitigate | `git add src/utils/devedorCalc.js src/services/__tests__/calculos.test.js` EXPLICITAMENTE por path. Passo 4 confirma `git status --short` mostra SÓ 2 stageados. AC: `git show HEAD --stat` = 2 arquivos. |
| T-07.8-01-06 | Elevation of Privilege (usuário fatigado autoriza sem revisar) | Modo cansado D-15, 5 pausas totais | accept | D-15 EXPLICITAMENTE adiciona pausas para mitigar fadiga. Autorização explícita exigida. Se usuário auto-aprova cegamente, risco aceito. |
| T-07.8-01-07 | Repudiation (commit message inconsistente) | Mensagem do commit diverge do padrão | mitigate | Texto literal no commit message (HEREDOC). AC inclui grep de strings-chave no corpo. |
| T-07.8-01-08 | Tag criada prematuramente | `git tag v1.4-phase7.8` disparado nesta plan | mitigate | AC explícita `git tag -l "v1.4-phase7.8*"` = vazio. D-17: tag só em ação separada pós-UAT prod. |
| T-07.8-01-09 | Information Disclosure (commit body vaza estado) | Mensagem reporta SHAs, decisões internas | accept | Todos os dados expostos são intencionais e úteis para auditoria futura. Sem secrets. |
</threat_model>

<verification>
Verificação final da plan 07.8-01 (após Task 3 completa):

1. **Submodule main (estado final):**
   - `cd src/mr-3/mr-cobrancas && git log -1 --format=%s` → `feat(07.8-01): adapter calcularDetalheEncargosContrato + teste shield`
   - `cd src/mr-3/mr-cobrancas && git log -1 --name-only --format=` → 2 linhas: `src/utils/devedorCalc.js` + `src/services/__tests__/calculos.test.js`
   - `cd src/mr-3/mr-cobrancas && git log origin/main..main --oneline` → inclui o novo commit (≥1 linha)

2. **Invariante motor Art.354 preservada:**
   - `cd src/mr-3/mr-cobrancas && git show HEAD -- src/services/pagamentos.js` → vazio
   - `cd src/mr-3/mr-cobrancas && git show HEAD -- src/services/dividas.js` → vazio
   - `cd src/mr-3/mr-cobrancas && git show HEAD -- src/utils/devedorCalc.js | grep -cE "^-[^-]"` → 0

3. **Testes + build:**
   - `cd src/mr-3/mr-cobrancas && npm run test:regressao` → exit 0 (9/9 TJGO + 1 shield)
   - `cd src/mr-3/mr-cobrancas && npm run build` → exit 0

4. **Sanidade grep:**
   - `grep -c "export function calcularDetalheEncargosContrato" src/utils/devedorCalc.js` = 1
   - `grep -c "calcularDetalheEncargosContrato" src/services/__tests__/calculos.test.js` ≥ 2
   - 9 testes TJGO marcadores presentes (`calcularSaldoDevedorAtualizado`, `calcularPlanilhaCompleta`, `casos-tjgo.json`)

5. **Zero push + zero tag:**
   - `git log origin/main..main --oneline` retorna ≥1 linha
   - `git tag -l "v1.4-phase7.8*"` vazio
   - Nenhum comando `git push` no histórico da sessão

6. **Pai intocado:**
   - Repo pai: nenhum commit novo criado nesta plan. Bump será 07.8-04 Task 2.
</verification>

<success_criteria>
Plan 07.8-01 completa quando:

1. ✅ Adapter `calcularDetalheEncargosContrato` existe em `src/utils/devedorCalc.js` (exportada nomeada, 10-15 linhas com JSDoc, delega 100% ao motor existente)
2. ✅ Teste shield adicionado em `src/services/__tests__/calculos.test.js` (describe isolado + 1 it, 10-12 linhas)
3. ✅ 9 testes TJGO originais literalmente intocados (grep confirmatório)
4. ✅ Regressão 10/10 PASS (`npm run test:regressao` exit 0)
5. ✅ Build exit 0 (`npm run build`)
6. ✅ Invariante motor Art.354: `pagamentos.js` + `dividas.js` diff vazio; `devedorCalc.js` só adições (0 `-` lines fora do diff header)
7. ✅ PAUSA #1 autorizada explicitamente pelo usuário antes do commit
8. ✅ 1 commit local no submodule `main` com subject EXATO e APENAS 2 arquivos stageados
9. ✅ Zero push em toda a plan
10. ✅ Zero tag criada (`git tag -l "v1.4-phase7.8*"` vazio)
11. ✅ Pai (repo parent) intocado — bump é ação de 07.8-04 Task 2
</success_criteria>

<output>
Após conclusão da Task 3, criar `.planning/phases/07.8-saldo-atualizado-composicao/07.8-01-SUMMARY.md` contendo:

- **SHA:** commit do submodule (full + short)
- **Arquivos modificados:** 2 (devedorCalc.js + calculos.test.js) com contagem de linhas adicionadas
- **Gates determinísticos:** output de regressão (10/10), build (exit 0), motor diff (pagamentos.js+dividas.js vazios; devedorCalc.js só adições), escopo (2 arquivos)
- **Autorização humana:** texto literal do usuário em PAUSA #1
- **Invariante motor Art.354:** confirmação explícita que as 7 funções existentes (calcularSaldoPorDividaIndividual, calcularSaldoDevedorAtualizado, calcularSaldosPorDivida, calcularTotalPagoPorDivida, calcularDetalheEncargos, calcularPlanilhaCompleta, calcularResumoFinanceiro) não mudaram
- **Teste shield:** 1 it em describe isolado; 9 casos TJGO preservados
- **Zero push + zero tag:** confirmação explícita
- **Próximos passos:**
  1. Plan 07.8-02: 4ª coluna "Saldo Atualizado" em DetalheContrato.jsx (depende deste adapter)
  2. Plan 07.8-03: DecomposicaoSaldoModal + print A4 isolada
  3. Plan 07.8-04: UAT SC-1..SC-7 + bump submodule no pai (SEM PUSH)
  4. Tag `v1.4-phase7.8` APENAS após UAT prod OK, em ação separada

O SUMMARY é o artefato de fechamento desta plan.
</output>
</content>
