---
phase: 07.8-saldo-atualizado-composicao
plan: 04
type: execute
wave: 4
depends_on:
  - 07.8-01
  - 07.8-02
  - 07.8-03
files_modified:
  - src/mr-3 (bump do submodule pointer no repo pai branch master)
autonomous: false   # D-15: modo cansado — pausa #4 (UAT SC-1..SC-7) + pausa #5 (bump review) nesta plan. SEM PUSH. TAG NÃO criada.
requirements: []    # Gap closure — sem REQ-IDs formais. Contrato = SC-1..SC-7 em 07.8-CONTEXT.md §6 + D-15, D-16, D-17.

must_haves:
  truths:
    - "SC-1: Contrato com 1 doc, 1 parcela vencida, encargos padrão — clicar 'Saldo Atualizado' abre modal com linhas: Valor Original + Correção + Multa + Juros + Honorários + Total (D-04, D-07)"
    - "SC-2: Contrato SEM encargos (multa=0, juros=0, honorários=0, indexador=nenhum) — modal mostra só 'Valor Original = Saldo Atualizado'; linhas zeradas escondidas (D-07)"
    - "SC-3: Contrato com num_documentos=0 (pós-7.7) — Resumo Financeiro mostra 3 colunas normal; coluna 'Saldo Atualizado' NÃO aparece (D-05)"
    - "SC-4: Contrato com Art.523 aplicável — modal mostra linhas extras 'Art. 523 §1º — Multa (10%)' + 'Art. 523 §1º — Honorários (10%)' (D-09)"
    - "SC-5: Clicar 'Copiar' — clipboard recebe texto formatado; colar em WhatsApp/Notes confirma formato (D-12)"
    - "SC-6: Clicar 'Imprimir' — nova janela abre com layout A4 + Times New Roman + decomposição formatada; dialog de impressão via botão 🖨️; 'Salvar como PDF' no dialog funciona (D-13)"
    - "SC-7: Parcelas todas quitadas — motor retorna saldoAtualizado=0, componentes=0; modal mostra só 'Valor Original' e 'Saldo Devedor Atualizado: R$ 0,00'"
    - "PAUSA #4 (UAT): usuário reporta PASS/FAIL de cada SC-1..SC-7 no chat antes de avançar para bump"
    - "PAUSA #5 (bump commit review): agente mostra 3 commits do submodule (git log origin/main..main --stat) + confirma motor Art.354 diff vazio + Modal base diff vazio antes de bumpar o pai"
    - "SC-bump: 1 commit no pai branch master com subject EXATO `chore(07.8): bump submodule mr-3 — saldo atualizado + composição com impressão (07.8-01 + 02 + 03)` (D-15)"
    - "SC-no-push: zero git push em toda a phase. git log origin/main..main ≥3 no submodule. git log origin/master..master ≥1 no pai."
    - "SC-no-tag: tag v1.4-phase7.8 NÃO é criada nesta phase — ação separada pós-UAT prod (padrão 7.5.1/7.6/7.7)"
  artifacts:
    - path: "src/mr-3 (submodule pointer no pai)"
      provides: "Bump do ponteiro para o tip do submodule main (SHA do commit 07.8-03). Encapsula os 3 commits da phase 7.8 em 1 commit atômico no pai."
      contains: "bump submodule mr-3"
    - path: ".planning/phases/07.8-saldo-atualizado-composicao/07.8-04-SUMMARY.md"
      provides: "Evidência: SHAs (3+1), UAT SC-1..SC-7, 2 autorizações (PAUSA #4 + #5), invariante motor + Modal base, zero push, próximos passos"
      contains: "SC-1|SC-2|SC-3|SC-4|SC-5|SC-6|SC-7"
  key_links:
    - from: "Task 1 (checkpoint UAT = PAUSA #4)"
      to: "localhost npm run dev + 7 cenários manuais SC-1..SC-7 reportados pelo usuário"
      via: "gate humano antes de qualquer bump do pai"
      pattern: "SC-1|SC-2|SC-3|SC-4|SC-5|SC-6|SC-7"
    - from: "Task 2 (checkpoint = PAUSA #5)"
      to: "commit bump submodule mr-3 no pai branch master"
      via: "gate humano diff consolidado + mensagem — SEM PUSH"
      pattern: "PAUSA #5"
    - from: "Tag v1.4-phase7.8"
      to: "Ação separada do usuário pós-UAT prod — NÃO criar nesta plan"
      via: "padrão 7.5.1/7.6/7.7 — tag só após smoke test em produção"
      pattern: "NÃO criar tag"
---

<objective>
Fechamento da Phase 7.8: UAT manual em localhost dos 7 cenários SC-1..SC-7 (D-16) + checkpoint humano no bump do submodule no repo pai. **SEM PUSH em qualquer momento**. **Tag `v1.4-phase7.8` NÃO é criada nesta phase** — ação separada pós-UAT produção (padrão 7.5.1/7.6/7.7).

Estado esperado no início desta plan:
- Submódulo `src/mr-3/mr-cobrancas` branch `main` com **3 commits locais** pendentes de push (desta phase):
  - **Commit A (07.8-01):** `feat(07.8-01): adapter calcularDetalheEncargosContrato + teste shield` — 2 arquivos (devedorCalc.js + calculos.test.js).
  - **Commit B (07.8-02):** `feat(07.8-02): 4ª coluna Saldo Atualizado em DetalheContrato` — 1 arquivo (DetalheContrato.jsx).
  - **Commit C (07.8-03):** `feat(07.8-03): DecomposicaoSaldoModal + impressão isolada A4` — 2 arquivos (DecomposicaoSaldoModal.jsx novo + DetalheContrato.jsx modificado).
- Podem haver commits de phases anteriores ainda pendentes de push (7.5.1 etc conforme STATE.md). OK — esta plan verifica o DELTA da 7.8.
- Repo pai branch `master`: `src/mr-3` marcado como modified (bump pendente). Pode haver outros arquivos modificados de sessões anteriores (`.planning/STATE.md`, etc) — **NÃO** fazem parte desta phase, ficam unstaged.

Esta plan executa:

1. **Task 1 (checkpoint:human-verify = PAUSA #4):** Gates determinísticos consolidados + UAT manual dos 7 cenários SC-1..SC-7 em localhost. Usuário executa e reporta PASS/FAIL de cada. Agente aguarda autorização explícita antes de avançar.

2. **Task 2 (checkpoint:human-verify = PAUSA #5):** Mostrar os 3 commits do submodule consolidados + confirmar motor Art.354 + Modal base diff vazio. Aguardar autorização. Commit único no pai com mensagem EXATA. Reportar SHAs finais + zero push + próximos passos.

Padrão idêntico ao seguido nas Phases 7.2/7.3/7.4/7.5/7.5.1/7.6/7.7 (D-17).

Os commits das Plans 07.8-01/02/03 NÃO são undone, amendados ou rebase-ados. Esta plan só **verifica + aprova (2x) + bumpa o pai**.

Purpose: Advogado faz smoke test em localhost dos 7 cenários críticos (incluindo edge cases D-05 contrato vazio, D-07 zero-value hidden, D-08 correção negativa, D-09 Art.523, D-12 clipboard, D-13 print A4) e confirma que motor Art.354 continua 10/10 verde. Commit do bump fica local no pai até o usuário decidir push em sessão separada (padrão 7.5.1 / 7.6 / 7.7).

Output:
- Log de execução dos 7 cenários UAT (reportado inline no chat, resumido no SUMMARY).
- 1 commit local no pai branch `master` com mensagem EXATA `chore(07.8): bump submodule mr-3 — saldo atualizado + composição com impressão (07.8-01 + 02 + 03)` e apenas `src/mr-3` stageado.
- SUMMARY registrando: SHAs do submodule (x3) + SHA do pai (x1) + UAT result + 2 autorizações humanas (pausas #1/#2/#3 já registradas nas plans anteriores) + invariantes + confirmação sem push + sem tag.
- **NENHUMA TAG criada.** Tag `v1.4-phase7.8` em ação separada pós-UAT prod.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07.8-saldo-atualizado-composicao/07.8-CONTEXT.md

<!-- 3 plans predecessoras (Tasks dependem de todas completas): -->
@.planning/phases/07.8-saldo-atualizado-composicao/07.8-01-PLAN.md
@.planning/phases/07.8-saldo-atualizado-composicao/07.8-02-PLAN.md
@.planning/phases/07.8-saldo-atualizado-composicao/07.8-03-PLAN.md

<!-- TEMPLATE literal do padrão duplo-checkpoint UAT+bump+SEM PUSH (seguir estrutura passo-a-passo): -->
@.planning/phases/07.7-excluir-documento-individual/07.7-03-PLAN.md

<!-- Precedentes adicionais do mesmo padrão commit+bump+SEM PUSH: -->
<!-- .planning/phases/07.6-parcelas-n-grande/07.6-03-PLAN.md Task 2 (template 10-passo com UAT + bump) -->
<!-- .planning/phases/07.5.1-sincronizacao-saldosmap-ux-input-valor/07.5.1-01-PLAN.md Task 3 -->

<interfaces>
<!-- Pré-condições verificáveis desta plan. Executor checa ANTES de abrir Task 1. -->

**Pré-condições (se FALHAREM, abortar plan e reportar ao usuário):**

```bash
cd src/mr-3/mr-cobrancas
git rev-parse --abbrev-ref HEAD                                       # esperado: main

git log -3 --format="%H %s" origin/main..main 2>/dev/null | head -3
# esperado: no MÍNIMO 3 linhas MAIS RECENTES dessa phase, em ordem new-to-old:
#   <SHA3>  feat(07.8-03): DecomposicaoSaldoModal + impressão isolada A4
#   <SHA2>  feat(07.8-02): 4ª coluna Saldo Atualizado em DetalheContrato
#   <SHA1>  feat(07.8-01): adapter calcularDetalheEncargosContrato + teste shield
# Podem existir mais commits atrás (de phases anteriores não pushed) — OK.

# Confirmar que as 3 MAIS RECENTES são as esperadas (robusto a pre-existentes):
git log origin/main..main --format="%s" | head -3
# 1ª linha (tip): feat(07.8-03): ...
# 2ª linha:        feat(07.8-02): ...
# 3ª linha:        feat(07.8-01): ...

# Invariante motor Art.354 — soma dos 3 commits:
git diff HEAD~3 HEAD -- src/utils/devedorCalc.js
# Esperado: APENAS adição do adapter (10-15 linhas) no final; zero `-` lines fora do header.
# Comando determinístico: verificar que a chunk adicionada NÃO altera funções existentes.
git diff HEAD~3 HEAD -- src/utils/devedorCalc.js | grep -cE "^-[^-]"
# Esperado: 0

git diff HEAD~3 HEAD -- src/services/pagamentos.js                    # vazio
git diff HEAD~3 HEAD -- src/services/dividas.js                        # vazio
git diff HEAD~3 HEAD -- src/components/ui/Modal.jsx                    # vazio (D-06)
```

Se o output não bater → PARAR e reportar `git log -5 --format="%H %s" origin/main..main` completo pro usuário diagnosticar.

**Estado do pai (repo master) ANTES desta plan:**
```bash
cd <raiz do pai>
git rev-parse --abbrev-ref HEAD                                       # esperado: master
git status --short
# esperado: `src/mr-3` aparece como modified (bump pendente).
# Podem existir entries pré-existentes (.planning/STATE.md, .planning/ROADMAP.md
# modificados ou deletados em sessões anteriores — vide gitStatus inicial do planning_context).
# Essas entries NÃO fazem parte desta phase. O commit final adiciona APENAS `src/mr-3`.
```

**Textos EXATOS das PAUSAs (replicar literal no chat quando cada gate for atingido):**

---

**PAUSA #4 (UAT — Task 1, Passo 8):**
> ```
> ## UAT Phase 7.8 — 7 cenários SC-1..SC-7 em localhost
> Dev server: <URL capturada do npm run dev>
>
> Por favor execute os 7 cenários abaixo no browser e reporte PASS / FAIL de cada.
> Todos devem passar antes de eu prosseguir para o bump do pai.
>
> ### SC-1 — Happy path (contrato com encargos padrão)
> 1. Abrir contrato EXISTENTE com 1 doc, 1+ parcelas vencidas, encargos padrão
>    (ex: multa 2%, juros 1% a.m., honorários 10%, IGP-M).
> 2. Observar "Resumo Financeiro" no DetalheContrato.
> 3. Esperado:
>    - Grid com 4 colunas: Valor Total / Total Pago / Em Aberto / Saldo Atualizado.
>    - "Saldo Atualizado" tem cursor:pointer + hover verde ao passar o mouse.
>    - Valor de "Saldo Atualizado" > "Em Aberto" (encargos aplicados).
> 4. Clicar "Saldo Atualizado".
> 5. Esperado:
>    - Modal abre com header "Composição do Saldo Atualizado".
>    - Body mostra linhas: Valor Original / + Correção monetária (IGP-M) / + Multa
>      contratual / + Juros de mora / + Honorários / = Saldo Devedor Atualizado.
>    - Nota rodapé: "Pode haver discrepância de R$ 0,01 por arredondamento."
>    - 3 botões: Copiar / Imprimir / Fechar.
> Pass/Fail:
>
> ### SC-2 — Contrato SEM encargos (linhas zero-value escondidas, D-07)
> 1. Criar ou abrir contrato com multa=0, juros=0, honorários=0, indexador=nenhum.
> 2. Clicar "Saldo Atualizado".
> 3. Esperado:
>    - Modal mostra APENAS "Valor Original" e "Saldo Devedor Atualizado" iguais.
>    - Linhas Correção/Multa/Juros/Honorários NÃO aparecem (D-07).
>    - Nota rodapé presente.
> Pass/Fail:
>
> ### SC-3 — Contrato com num_documentos=0 (pós-Phase 7.7 delete last doc, D-05)
> 1. Abrir contrato e excluir todos os documentos (ou criar contrato vazio).
> 2. Confirmar contrato.num_documentos = 0 via UI ou SQL Editor.
> 3. Observar Resumo Financeiro.
> 4. Esperado:
>    - Grid com 3 colunas APENAS: Valor Total / Total Pago / Em Aberto.
>    - Coluna "Saldo Atualizado" NÃO aparece (D-05).
>    - Layout sem espaço vazio (grid repeat(3, 1fr)).
> Pass/Fail:
>
> ### SC-4 — Contrato com Art.523 aplicável (D-09)
> 1. Criar/abrir contrato com art523_opcao != "nao_aplicar".
> 2. Clicar "Saldo Atualizado".
> 3. Esperado:
>    - Modal mostra linhas extras: "Art. 523 §1º — Multa (10%)" e
>      "Art. 523 §1º — Honorários (10%)" (D-09).
>    - Total Saldo Devedor Atualizado contém a soma do Art.523.
> Pass/Fail:
>
> ### SC-5 — Botão Copiar funciona (clipboard, D-12)
> 1. Em qualquer contrato com encargos, abrir modal de composição.
> 2. Clicar "📋 Copiar".
> 3. Esperado:
>    - Toast verde "Copiado!" aparece.
>    - Colar em WhatsApp/Notes/qualquer text field confirma texto formatado:
>      "Composição do Saldo Atualizado — [credor] vs [devedor]"
>      "Contrato: [ref] — Data: DD/MM/YYYY"
>      "  Valor Original: R$ X"
>      "+ Correção monetária (INDEX): R$ X"
>      "..."
>      "= Saldo Devedor Atualizado: R$ X"
>      "(Pode haver discrepância de R$ 0,01 por arredondamento.)"
> Pass/Fail:
>
> ### SC-6 — Botão Imprimir funciona (window.open A4, D-13)
> 1. Em qualquer contrato com encargos, abrir modal de composição.
> 2. Clicar "🖨️ Imprimir".
> 3. Esperado:
>    - Nova janela/aba abre.
>    - Layout: cabeçalho com credor/devedor/contrato/data + tabela da decomposição
>      + rodapé de assinatura + timestamp.
>    - Font aparenta ser Times New Roman (serifado, 12pt).
>    - Barra superior cinza com botões "🖨️ Imprimir / PDF" + "✕ Fechar" + hint
>      "Ctrl+P → Salvar como PDF".
> 4. Clicar "🖨️ Imprimir / PDF" da barra superior.
> 5. Esperado:
>    - Dialog de impressão do navegador abre.
>    - Layout visualizado no dialog é A4 (margens grandes, serifado).
>    - Barra cinza NÃO aparece no preview de impressão (@media print{.bar{display:none}}).
>    - Destino "Salvar como PDF" funciona — baixa PDF com composição.
> Pass/Fail:
>
> ### SC-7 — Parcelas todas quitadas (edge case, saldo zero)
> 1. Abrir contrato onde TODAS as parcelas estão 100% quitadas
>    (pagamentos_divida cobre integral).
> 2. Observar Resumo Financeiro.
> 3. Esperado:
>    - "Em Aberto" = R$ 0,00 (nominal Phase 7.3).
>    - "Saldo Atualizado" = R$ 0,00 (motor Art.354).
>    - Clicar "Saldo Atualizado" abre modal mostrando só "Valor Original"
>      e "Saldo Devedor Atualizado: R$ 0,00" (demais linhas zeradas escondidas).
> Pass/Fail:
>
> ---
> Quando responder, confirme também:
> - Nenhum erro no console do browser (DevTools > Console).
> - Nenhum warning React novo.
> - Regressão: `cd src/mr-3/mr-cobrancas && npm run test:regressao` exit 0 (10/10).
>
> **Nota sobre D-18 edge cases:** se você observar correção NEGATIVA em algum
> contrato (deflação INPC rara), confirme que a linha aparece com sinal `-` e
> cor vermelha. Se encontrar, reporte; isso é feature, não bug.
> ```

---

**PAUSA #5 (Task 2, Passo 3):**
> ```
> ## PAUSA #5 (07.8-04) — bump commit review antes do commit final no pai
>
> UAT localhost Phase 7.8: 7 cenários SC-1..SC-7 reportados pelo usuário na Task 1.
>
> Gates determinísticos:
> - Regressão: 10/10 PASS (9 TJGO + 1 shield 07.8-01)
> - Build: exit 0
> - Motor Art.354 (devedorCalc.js — só adições 07.8-01; pagamentos.js + dividas.js vazios): VAZIO ✓ (D-01)
> - Modal base (src/components/ui/Modal.jsx) diff HEAD~3..HEAD: VAZIO ✓ (D-06)
> - Zero @media print fora dos 2 pontos canônicos (DecomposicaoSaldoModal + GerarPeticao): VAZIO ✓ (D-13)
>
> Submodule src/mr-3/mr-cobrancas @ main tem 3 commits locais desta phase pendentes de push:
> - <SHA3_short> feat(07.8-03): DecomposicaoSaldoModal + impressão isolada A4 (2 arquivos: DecomposicaoSaldoModal.jsx + DetalheContrato.jsx)
> - <SHA2_short> feat(07.8-02): 4ª coluna Saldo Atualizado em DetalheContrato (1 arquivo: DetalheContrato.jsx)
> - <SHA1_short> feat(07.8-01): adapter calcularDetalheEncargosContrato + teste shield (2 arquivos: devedorCalc.js + calculos.test.js)
>
> Pai (raiz do repo) em branch `master`. Status:
> - `src/mr-3` modified (bump do ponteiro pendente) ✓
> - (Outros arquivos pré-existentes podem aparecer como modified — NÃO serão stageados.)
>
> SHAs a apontar:
> - <SHA1_short> feat(07.8-01) + <SHA2_short> feat(07.8-02) + <SHA3_short> feat(07.8-03) (tip: <SHA3_full>)
>
> Autoriza commit no pai com mensagem EXATA:
> `chore(07.8): bump submodule mr-3 — saldo atualizado + composição com impressão (07.8-01 + 02 + 03)`
> ?
>
> Responda **'bump it'** ou **'approved'** para prosseguir.
> Esta autorização é ÚNICA para o bump commit — qualquer outra coisa cancela
> (submodule continua com 3 commits locais, só o bump no pai fica pendente).
> ```

---

**Mensagem EXATA do commit do pai (D-15):**
```
chore(07.8): bump submodule mr-3 — saldo atualizado + composição com impressão (07.8-01 + 02 + 03)

Submodule src/mr-3 @ branch main: 3 commits cirúrgicos da Phase 7.8 (descoberto no discuss 2026-04-24 que motor Art.354 já expõe decomposição — phase vira "conectar peças existentes" sem tocar o motor).

- feat(07.8-01): adapter calcularDetalheEncargosContrato + teste shield — wrapper thin (10 linhas) sobre calcularDetalheEncargos existente; pseudoDevedor={dividas: dividasDoContrato||[]} delega ao motor; zero re-implementação. Teste shield novo em calculos.test.js (describe isolado) valida shape+zeros para contrato vazio. 9 testes TJGO originais intocados.
- feat(07.8-02): 4ª coluna Saldo Atualizado em DetalheContrato — grid dinâmico repeat(${nCols}, 1fr): 4 colunas quando contrato.num_documentos>0, 3 colunas quando ===0 (D-05 pós-7.7). Coluna clickable cursor:pointer + hover verde; onClick wire via setState (plan 07.8-03). useMemo com deps [dividas, allPagamentosDivida, hoje] cacheia adapter (D-18). Phase 7.3 (calcularTotaisContratoNominal + "Em Aberto") preservada.
- feat(07.8-03): DecomposicaoSaldoModal + impressão isolada A4 — novo componente wrapper sobre Modal.jsx base (D-06 intocado). Body linha-a-linha com zero-value hidden exceto Valor Original e Total (D-07); correção negativa red #dc2626 (D-08); Art.523 linhas extras condicionais (D-09); Custas condicional (D-10); rodapé rounding (D-11); 3 botões: Copiar (navigator.clipboard.writeText + toast, D-12), Imprimir (window.open A4 Times New Roman isolado com @media print{.bar{display:none}}, D-13), Fechar. escapeHtml defensivo no HTML do print.

Motor Art.354 (devedorCalc.js + pagamentos.js + dividas.js) LITERALMENTE intocado — invariante estrutural D-01 preservada nos 3 commits. devedorCalc.js diff HEAD~3..HEAD só adições no final (zero `-` lines fora do header do diff). Modal base (ui/Modal.jsx) diff vazio (D-06). Regressão 10/10 verde (9 TJGO + 1 shield 07.8-01). Build exit 0. UAT localhost dos 7 cenários SC-1..SC-7 (CONTEXT D-16) reportado pelo usuário.

Phase absorve backlog futuro (Saldo Devedor Atualizado + Composição da Dívida — DEPRECATED em backlog pós-7.8). Flag 7.5.2 (encargos stale pós-delete) permanece pendência pré-existente independente.

Tag v1.4-phase7.8 pendente — só após UAT de produção em ação separada (padrão 7.5.1/7.6/7.7).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

</interfaces>
</context>

<tasks>

<!--
  2 tasks em 1 wave SEQUENCIAL:
  - Task 1: Gates determinísticos consolidados + UAT manual dos 7 cenários SC-1..SC-7. Bloqueia até usuário confirmar.
  - Task 2: PAUSA #5 (bump commit review) + commit final no pai.
  Task 2 SÓ executa após Task 1 autorizada. Ambas são checkpoints humanos.
  NENHUMA das 2 tasks faz push. Tag é ação separada do usuário, fora desta plan.
-->

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: PAUSA #4 — Gates determinísticos + UAT localhost dos 7 cenários SC-1..SC-7</name>
  <files>
    (nenhum arquivo modificado nesta task — é UAT manual + gates determinísticos)
  </files>

  <what-built>
    Plans 07.8-01, 07.8-02, 07.8-03 já commitaram localmente no submodule `src/mr-3/mr-cobrancas` branch `main`:
    - **Commit A (07.8-01):** `feat(07.8-01): adapter calcularDetalheEncargosContrato + teste shield` — 2 arquivos.
    - **Commit B (07.8-02):** `feat(07.8-02): 4ª coluna Saldo Atualizado em DetalheContrato` — 1 arquivo.
    - **Commit C (07.8-03):** `feat(07.8-03): DecomposicaoSaldoModal + impressão isolada A4` — 2 arquivos.

    Esta Task 1 executa os 6 gates determinísticos consolidados + UAT manual:
    1. **Gate regressão:** `npm run test:regressao` 10/10 no submodule.
    2. **Gate build:** `npm run build` exit 0.
    3. **Gate invariante motor (D-01):** `git diff HEAD~3 HEAD -- src/utils/devedorCalc.js src/services/pagamentos.js src/services/dividas.js` — devedorCalc só adições, pagamentos+dividas vazios.
    4. **Gate Modal base (D-06):** `git diff HEAD~3 HEAD -- src/components/ui/Modal.jsx` vazio.
    5. **Gate @media print (D-13):** zero `@media print` fora dos 2 pontos canônicos.
    6. **Gate escopo phase 7.8:** `git diff --name-only HEAD~3 HEAD` retorna EXATAMENTE 4 arquivos:
       - `src/utils/devedorCalc.js`
       - `src/services/__tests__/calculos.test.js`
       - `src/components/DetalheContrato.jsx`
       - `src/components/DecomposicaoSaldoModal.jsx`
    7. **UAT manual (7 cenários D-16):** SC-1 happy path, SC-2 zero-value hidden, SC-3 contrato vazio, SC-4 Art.523, SC-5 Copiar, SC-6 Imprimir, SC-7 saldo zero.

    Agente reporta resultado dos gates + apresenta UAT e **aguarda confirmação explícita do usuário (PAUSA #4)** antes de abrir Task 2.
  </what-built>

  <read_first>
    - `.planning/phases/07.8-saldo-atualizado-composicao/07.8-CONTEXT.md` §5 D-01 (INV motor), D-05 (hide coluna), D-06 (Modal base intocado), D-07 (zero-value hidden), D-08 (correção negativa), D-09 (Art.523), D-10 (Custas), D-11 (rodapé), D-12 (Copiar), D-13 (Imprimir), D-15 (pausas — esta é a #4), D-16 (SC-1..SC-7 EXATOS)
    - `.planning/phases/07.8-saldo-atualizado-composicao/07.8-01-PLAN.md` + `07.8-02-PLAN.md` + `07.8-03-PLAN.md` — confirmar SHAs esperados dos 3 commits no submodule
    - `.planning/phases/07.7-excluir-documento-individual/07.7-03-PLAN.md` Task 1 — template literal do padrão de gates determinísticos pré-UAT + formato do prompt de UAT (reusar estrutura)
  </read_first>

  <how-to-verify>

### PARTE A — Gates determinísticos (agente executa ANTES de abrir UAT manual)

**Pré-requisito:** cwd = raiz do repo pai.

**Passo 1 — Confirmar branch e 3 commits locais recentes no submodule main:**
```bash
cd src/mr-3/mr-cobrancas
git rev-parse --abbrev-ref HEAD
# esperado: main

git log --format="%H %s" origin/main..main | head -3
# esperado: 3 linhas MAIS RECENTES (tip primeiro):
# <SHA3> feat(07.8-03): DecomposicaoSaldoModal + impressão isolada A4
# <SHA2> feat(07.8-02): 4ª coluna Saldo Atualizado em DetalheContrato
# <SHA1> feat(07.8-01): adapter calcularDetalheEncargosContrato + teste shield
# Pode haver commits adicionais atrás (phases anteriores não pushed) — OK.

# Capturar SHAs:
SHA3=$(git log origin/main..main --format=%H | head -1)
SHA2=$(git log origin/main..main --format=%H | head -2 | tail -1)
SHA1=$(git log origin/main..main --format=%H | head -3 | tail -1)
SHA3_short=$(echo $SHA3 | cut -c1-7)
SHA2_short=$(echo $SHA2 | cut -c1-7)
SHA1_short=$(echo $SHA1 | cut -c1-7)
```

Se 3 subjects MAIS RECENTES não baterem com 07.8-03/02/01 → **PARAR** e reportar `git log -6 --format="%H %s" origin/main..main` completo.

**Passo 2 — Gate regressão (bloqueante, D-01):**
```bash
cd src/mr-3/mr-cobrancas
npm run test:regressao
```
Esperado: `10/10 PASS` (9 TJGO + 1 shield 07.8-01). Exit 0.

Se falhar: **PARAR** — diagnóstico:
```bash
git diff HEAD~3 HEAD -- src/utils/devedorCalc.js src/services/pagamentos.js src/services/dividas.js
```
Qualquer diff em funções existentes do motor → regressão 07.8-01 não preservou invariante D-01; reverter o commit ofensor.

**Passo 3 — Gate build (bloqueante):**
```bash
cd src/mr-3/mr-cobrancas
npm run build
```
Exit 0 obrigatório. Falhas prováveis em ordem:
- JSX mal formado em DecomposicaoSaldoModal.jsx (template string do print com backtick/escape errado).
- JSX mal formado em DetalheContrato.jsx (IIFE sem fechamento, prop sintax).
- Import path errado (Modal.jsx ou DecomposicaoSaldoModal.jsx).

**Passo 4 — Gate invariante motor Art.354 (bloqueante, D-01):**
```bash
cd src/mr-3/mr-cobrancas

# devedorCalc.js — só adições (07.8-01 adicionou adapter; 02/03 não tocaram):
git diff HEAD~3 HEAD -- src/utils/devedorCalc.js | grep -cE "^-[^-]"
# Esperado: 0 (zero `-` lines; só `+` lines na adição do adapter).

# pagamentos.js + dividas.js — vazios (não tocados em nenhum dos 3 commits):
git diff HEAD~3 HEAD -- src/services/pagamentos.js
git diff HEAD~3 HEAD -- src/services/dividas.js
# Esperado: ambos vazios (0 bytes).
```

Qualquer byte fora do padrão → reportar EXATAMENTE o diff e **ABORTAR**.

**Passo 5 — Gate Modal base intocado (D-06):**
```bash
cd src/mr-3/mr-cobrancas
git diff HEAD~3 HEAD -- src/components/ui/Modal.jsx
# Esperado: vazio.
```

Se não vazio → 07.8-03 violou D-06 (Modal base deveria ser wrapper-only). Reportar e ABORTAR.

**Passo 6 — Gate @media print isolation (D-13):**
```bash
cd src/mr-3/mr-cobrancas
grep -rnE "@media[[:space:]]+print" src --include="*.jsx" --include="*.js" --include="*.css" | \
  grep -v "DecomposicaoSaldoModal.jsx" | grep -v "GerarPeticao.jsx"
```
Esperado: vazio. Qualquer match → CSS print vazando pro app principal; reportar e ABORTAR.

**Passo 7 — Gate escopo da phase (só 4 arquivos):**
```bash
cd src/mr-3/mr-cobrancas
git diff --name-only HEAD~3 HEAD
```
Esperado: EXATAMENTE 4 linhas (ordem pode variar):
- `src/utils/devedorCalc.js`
- `src/services/__tests__/calculos.test.js`
- `src/components/DetalheContrato.jsx`
- `src/components/DecomposicaoSaldoModal.jsx`

Qualquer outro arquivo → reportar e aguardar confirmação do usuário antes de avançar.

**Passo 8 — Sanidade grep pós-commit (verificação do trabalho das 3 plans):**
```bash
cd src/mr-3/mr-cobrancas

# 07.8-01 artefatos:
grep -c "export function calcularDetalheEncargosContrato" src/utils/devedorCalc.js       # esperado: 1
grep -c "calcularDetalheEncargosContrato" src/services/__tests__/calculos.test.js         # esperado: ≥2
grep -c "describe(\"calcularDetalheEncargosContrato" src/services/__tests__/calculos.test.js  # esperado: 1

# 07.8-02 artefatos:
grep -cE "useMemo\\(\\s*\\(\\)\\s*=>\\s*calcularDetalheEncargosContrato" src/components/DetalheContrato.jsx  # esperado: 1
grep -c "\"Saldo Atualizado\"" src/components/DetalheContrato.jsx                          # esperado: 1
grep -cE "num_documentos.*>\\s*0|temDocumentos" src/components/DetalheContrato.jsx         # esperado: ≥1

# 07.8-03 artefatos:
test -f src/components/DecomposicaoSaldoModal.jsx && echo "Modal file exists" || echo "MISSING"  # esperado: "Modal file exists"
grep -c "export default function DecomposicaoSaldoModal" src/components/DecomposicaoSaldoModal.jsx  # esperado: 1
grep -c "navigator.clipboard.writeText" src/components/DecomposicaoSaldoModal.jsx          # esperado: 1
grep -cE 'window\\.open\\(\\s*["'"'"'][^"'"'"']*["'"'"']\\s*,\\s*["'"'"']_blank' src/components/DecomposicaoSaldoModal.jsx  # esperado: 1
grep -cE "@page\\s*\\{\\s*size:\\s*A4" src/components/DecomposicaoSaldoModal.jsx           # esperado: ≥1
grep -c "Times New Roman" src/components/DecomposicaoSaldoModal.jsx                        # esperado: ≥1
grep -c "#dc2626" src/components/DecomposicaoSaldoModal.jsx                                # esperado: ≥1
grep -c "<DecomposicaoSaldoModal" src/components/DetalheContrato.jsx                       # esperado: 1
grep -c "setShowDecomposicaoModal(true)" src/components/DetalheContrato.jsx                # esperado: 1
```

Se QUALQUER gate falhar → reportar ao usuário e **NÃO** prosseguir para Parte B.

---

### PARTE B — UAT manual dos 7 cenários SC-1..SC-7 (usuário executa no browser localhost)

**Passo 9 — Iniciar dev server:**
```bash
cd src/mr-3/mr-cobrancas
npm run dev
```
(Rodar em background — Bash tool com `run_in_background: true`. Capturar URL do output — tipicamente `http://localhost:5173/`.)

**Passo 10 — Reportar os 7 cenários ao usuário com formato EXATO (texto integral da PAUSA #4, ver <interfaces> acima — replicar literal substituindo `<URL capturada>` pelo URL real do dev server).**

**AGUARDAR resposta do usuário.** NÃO prosseguir para Task 2 sem confirmação explícita de que os 7 cenários passaram (ou que falhas são conhecidas/aceitáveis).

Diagnóstico de falhas no UAT:
- **SC-1 FAIL (modal não abre):** verificar `grep -n "setShowDecomposicaoModal(true)" src/components/DetalheContrato.jsx`. Se ausente, bug no 07.8-03 wire.
- **SC-1 FAIL (valor errado):** verificar que `calcularDetalheEncargosContrato` retorna shape correto; `console.log(detalheEncargosContrato)` via DevTools.
- **SC-2 FAIL (linhas zero aparecem):** verificar condicionais no `linhas.push(...)` em DecomposicaoSaldoModal.jsx. Deve usar `> 0` (ou `!= 0` para correção).
- **SC-3 FAIL (coluna aparece com num_documentos=0):** verificar `temDocumentos` logic em DetalheContrato.jsx (plan 07.8-02).
- **SC-4 FAIL (Art.523 não aparece):** verificar `if (art523Total > 0)` em DecomposicaoSaldoModal. Também conferir que o contrato de teste tem `art523_opcao != "nao_aplicar"` configurado corretamente.
- **SC-5 FAIL (clipboard não copia):** verificar DevTools console — `navigator.clipboard` pode estar indisponível em http (precisa https ou localhost). Localhost funciona em navegadores modernos.
- **SC-6 FAIL (print layout errado):** verificar HTML do window.open; confirmar `@page size:A4` + `Times New Roman` no style tag. Pop-up bloqueado → toast.error deve aparecer.
- **SC-7 FAIL (saldo > 0 com tudo quitado):** verificar `pagamentos_divida` está capturando amortização corretamente. Pode ser bug pré-existente do motor, não da phase 7.8.

Em qualquer FAIL bloqueante: reverter o commit ofensor via `git revert` no submodule e NÃO prosseguir; abrir novo ciclo.

  </how-to-verify>

  <acceptance_criteria>
    - `cd src/mr-3/mr-cobrancas && git log --format="%s" origin/main..main | head -3` retorna EXATAMENTE 3 linhas (tip primeiro):
      1. `feat(07.8-03): DecomposicaoSaldoModal + impressão isolada A4`
      2. `feat(07.8-02): 4ª coluna Saldo Atualizado em DetalheContrato`
      3. `feat(07.8-01): adapter calcularDetalheEncargosContrato + teste shield`
    - `cd src/mr-3/mr-cobrancas && npm run test:regressao` exit 0 (10/10)
    - `cd src/mr-3/mr-cobrancas && npm run build` exit 0
    - `cd src/mr-3/mr-cobrancas && git diff HEAD~3 HEAD -- src/utils/devedorCalc.js | grep -cE "^-[^-]"` retorna **0** (D-01 só adições)
    - `cd src/mr-3/mr-cobrancas && git diff HEAD~3 HEAD -- src/services/pagamentos.js` retorna **vazio** (D-01)
    - `cd src/mr-3/mr-cobrancas && git diff HEAD~3 HEAD -- src/services/dividas.js` retorna **vazio** (D-01)
    - `cd src/mr-3/mr-cobrancas && git diff HEAD~3 HEAD -- src/components/ui/Modal.jsx` retorna **vazio** (D-06)
    - Gate 6 (`@media print` fora dos 2 pontos canônicos) retorna **vazio** (D-13)
    - `cd src/mr-3/mr-cobrancas && git diff --name-only HEAD~3 HEAD` retorna EXATAMENTE 4 arquivos
    - Todos os greps do Passo 8 retornam os contadores esperados
    - Usuário reportou resultado dos 7 cenários SC-1..SC-7 no chat (texto contendo "SC-1", "SC-2", ..., "SC-7" ou equivalente) e indicou aprovação para prosseguir
    - **NENHUM** comando `git push` executado em Parte A nem durante Parte B
    - Texto do chat contém marcador `PAUSA #4` ou equivalente referenciando UAT Phase 7.8
  </acceptance_criteria>

  <resume-signal>
    Para prosseguir:
    - **`approved`** / **`next`** / **`ship it`** → UAT passou, agente abre Task 2 (PAUSA #5).
    - **`FAIL SC-N: descrição`** → agente investiga, aplica fix local se possível (re-inicia gate de regressão+build+motor+modal+@media print), ou solicita novo ciclo de plan se fix não-trivial.
  </resume-signal>

  <done>
    Gates determinísticos PASS (regressão 10/10 + build 0 + motor invariante + Modal base intocado + @media print isolado + escopo 4 arquivos). UAT dos 7 cenários SC-1..SC-7 reportado pelo usuário via PAUSA #4 e aprovado explicitamente. Safe para avançar para Task 2 (PAUSA #5 + commit bump no pai).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: PAUSA #5 — bump commit review + commit bump submodule no pai branch master (SEM PUSH)</name>
  <files>
    src/mr-3 (submodule pointer no repo pai — bump)
  </files>

  <what-built>
    Após UAT da Task 1 autorizado (PAUSA #4), executa o protocolo de bump do pai (padrão 7.2/7.3/7.4/7.5/7.5.1/7.6/7.7):

    - **PAUSA #5:** mostrar os 3 commits do submodule (`git log origin/main..main --stat` + `git log -3 origin/main..main --oneline`) + invariantes de motor/Modal/print confirmadas + SHAs consolidados. Aguardar autorização explícita.
    - **Passo intermediário:** cd pra raiz do pai, confirmar `master` + `src/mr-3` modified.
    - **Commit único no pai:** `git add src/mr-3 && git commit` com mensagem EXATA da D-15 via HEREDOC.
    - Reportar SHA final + confirmação zero push + próximos passos (push + tag em ação separada).

    NENHUM dos commits do submodule é undone, amendado ou rebase-ado nesta task.
  </what-built>

  <read_first>
    - `.planning/phases/07.8-saldo-atualizado-composicao/07.8-CONTEXT.md` §5 D-15 (pausa #5), D-17 (mensagem EXATA + push separado + tag separada)
    - `.planning/phases/07.7-excluir-documento-individual/07.7-03-PLAN.md` Task 2 — **template literal** PAUSA #4 (equivale nossa PAUSA #5 semanticamente) + commit bump
    - `.planning/phases/07.6-parcelas-n-grande/07.6-03-PLAN.md` Task 2 Passos 1-7 — template do 10-passo
    - `.planning/STATE.md` `last_activity` — confirma padrão "commits locais, sem push" desde 7.1
  </read_first>

  <how-to-verify>

**Passo 1 — Capturar SHAs e mostrar evidências ao usuário (PAUSA #5):**

```bash
cd src/mr-3/mr-cobrancas
git status --short                                       # esperado: árvore limpa pós-commits

# Capturar 3 SHAs desta phase (os 3 mais recentes do origin/main..main):
SHA3=$(git log origin/main..main --format=%H | head -1)
SHA2=$(git log origin/main..main --format=%H | head -2 | tail -1)
SHA1=$(git log origin/main..main --format=%H | head -3 | tail -1)
SHA3_short=$(echo $SHA3 | cut -c1-7)
SHA2_short=$(echo $SHA2 | cut -c1-7)
SHA1_short=$(echo $SHA1 | cut -c1-7)
SHA3_full=$SHA3

# Log consolidado:
git log -3 origin/main..main --stat
git log -3 origin/main..main --format="=== %h %s ==="

# Invariantes (já verificados na Task 1, replicar pra auditoria visual):
git diff HEAD~3 HEAD -- src/utils/devedorCalc.js | head -20        # mostrar as primeiras 20 linhas do adapter adicionado
git diff HEAD~3 HEAD -- src/services/pagamentos.js                 # DEVE SER VAZIO
git diff HEAD~3 HEAD -- src/services/dividas.js                    # DEVE SER VAZIO
git diff HEAD~3 HEAD -- src/components/ui/Modal.jsx                # DEVE SER VAZIO (D-06)
```

Reportar (replicar texto EXATO da PAUSA #5 do <interfaces> acima, substituindo `<SHA1_short>`, `<SHA2_short>`, `<SHA3_short>`, `<SHA3_full>`).

**AGUARDAR resposta do usuário.** NÃO prosseguir sem `approved` / `bump it` explícito.

Se usuário apontar correção nos commits:
- Correção no 07.8-03 (tip): preferível 4º commit de correção (amend quebra SHA). Re-rodar Task 1 gates + PAUSA #5.
- Correção no 07.8-02 ou 07.8-01: 4º commit de correção (rebase interativo destrutivo, requer autorização separada).

---

**Passo 2 — Após PAUSA #5 autorizada: cd pra raiz do repo pai e verificar estado:**

```bash
cd ../../..                                                # volta pra raiz do pai
pwd                                                        # confirmar path do pai
git rev-parse --abbrev-ref HEAD                           # esperado: master
git status --short
```

Esperado:
- `pwd` imprime path do pai (raiz do repo parent).
- Branch = `master`.
- `git status` mostra `src/mr-3` como modified (bump pendente). **Pode** listar entries pré-existentes (`.planning/STATE.md`, `.planning/ROADMAP.md` etc). Essas NÃO fazem parte desta phase.

**IMPORTANTE (hygiene do commit):** O próximo `git add src/mr-3` adiciona APENAS o bump. **NÃO** usar `git add -A` / `git add .` / `git add -u`. Outros arquivos modificados ficam unstaged.

---

**Passo 3 — Commit do bump no pai:**

```bash
# Continuamos na raiz do pai.
git add src/mr-3                                          # APENAS o bump, nada mais
git status --short                                        # confirmar SOMENTE `M src/mr-3` stageado

git commit -m "$(cat <<'EOF'
chore(07.8): bump submodule mr-3 — saldo atualizado + composição com impressão (07.8-01 + 02 + 03)

Submodule src/mr-3 @ branch main: 3 commits cirúrgicos da Phase 7.8 (descoberto no discuss 2026-04-24 que motor Art.354 já expõe decomposição — phase vira "conectar peças existentes" sem tocar o motor).

- feat(07.8-01): adapter calcularDetalheEncargosContrato + teste shield — wrapper thin (10 linhas) sobre calcularDetalheEncargos existente; pseudoDevedor={dividas: dividasDoContrato||[]} delega ao motor; zero re-implementação. Teste shield novo em calculos.test.js (describe isolado) valida shape+zeros para contrato vazio. 9 testes TJGO originais intocados.
- feat(07.8-02): 4ª coluna Saldo Atualizado em DetalheContrato — grid dinâmico repeat(${nCols}, 1fr): 4 colunas quando contrato.num_documentos>0, 3 colunas quando ===0 (D-05 pós-7.7). Coluna clickable cursor:pointer + hover verde; onClick wire via setState (plan 07.8-03). useMemo com deps [dividas, allPagamentosDivida, hoje] cacheia adapter (D-18). Phase 7.3 (calcularTotaisContratoNominal + "Em Aberto") preservada.
- feat(07.8-03): DecomposicaoSaldoModal + impressão isolada A4 — novo componente wrapper sobre Modal.jsx base (D-06 intocado). Body linha-a-linha com zero-value hidden exceto Valor Original e Total (D-07); correção negativa red #dc2626 (D-08); Art.523 linhas extras condicionais (D-09); Custas condicional (D-10); rodapé rounding (D-11); 3 botões: Copiar (navigator.clipboard.writeText + toast, D-12), Imprimir (window.open A4 Times New Roman isolado com @media print{.bar{display:none}}, D-13), Fechar. escapeHtml defensivo no HTML do print.

Motor Art.354 (devedorCalc.js + pagamentos.js + dividas.js) LITERALMENTE intocado — invariante estrutural D-01 preservada nos 3 commits. devedorCalc.js diff HEAD~3..HEAD só adições no final (zero `-` lines fora do header do diff). Modal base (ui/Modal.jsx) diff vazio (D-06). Regressão 10/10 verde (9 TJGO + 1 shield 07.8-01). Build exit 0. UAT localhost dos 7 cenários SC-1..SC-7 (CONTEXT D-16) reportado pelo usuário.

Phase absorve backlog futuro (Saldo Devedor Atualizado + Composição da Dívida — DEPRECATED em backlog pós-7.8). Flag 7.5.2 (encargos stale pós-delete) permanece pendência pré-existente independente.

Tag v1.4-phase7.8 pendente — só após UAT de produção em ação separada (padrão 7.5.1/7.6/7.7).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"

git log -1 --format=%H                                    # capturar SHA pai full
git log -1 --format=%h                                    # SHA pai short
```

---

**Passo 4 — Reportar SHAs finais + estado "zero push" + próximos passos:**

Formato EXATO no chat:
```
## Phase 7.8 shipped LOCALLY (SEM PUSH)

Submodule src/mr-3/mr-cobrancas @ main:
- <SHA1_short> feat(07.8-01): adapter calcularDetalheEncargosContrato + teste shield
- <SHA2_short> feat(07.8-02): 4ª coluna Saldo Atualizado em DetalheContrato
- <SHA3_short> feat(07.8-03): DecomposicaoSaldoModal + impressão isolada A4

Parent repo master:
- <SHA_pai_short> chore(07.8): bump submodule mr-3 — saldo atualizado + composição com impressão (07.8-01 + 02 + 03)

Invariante motor Art.354: PRESERVADO (devedorCalc.js só adições; pagamentos.js + dividas.js diff vazio; Modal base ui/Modal.jsx diff vazio — D-06).
Zero @media print fora dos 2 pontos canônicos (DecomposicaoSaldoModal + GerarPeticao). Regressão 10/10 PASS. Build exit 0.

UAT localhost 7 cenários SC-1..SC-7: reportados pelo usuário na Task 1.

5 autorizações humanas registradas na phase:
- PAUSA #1 (07.8-01 Task 3): diff review adapter + shield ✓
- PAUSA #2 (07.8-02 Task 3): diff review 4ª coluna ✓
- PAUSA #3 (07.8-03 Task 4): diff review modal + print ✓
- PAUSA #4 (07.8-04 Task 1): UAT SC-1..SC-7 ✓
- PAUSA #5 (07.8-04 Task 2): bump commit review ✓

Próximos passos (ação separada do usuário, NÃO executar agora):
1. **Push quando autorizar:** `cd src/mr-3/mr-cobrancas && git push origin main` + `cd <raiz_pai> && git push origin master` (NESSA ORDEM — submodule primeiro, pai depois).
2. **Deploy Vercel:** auto-deploy após push do pai (cache headers da 7.4 garantem F5 pós-deploy).
3. **UAT produção:** testar Saldo Atualizado + modal + impressão em mrcobrancas.com.br com contrato real.
4. **Tag v1.4-phase7.8:** criar SOMENTE após UAT prod OK (`git tag v1.4-phase7.8 <SHA_pai>` + push da tag).

Status: Phase 7.8 pronta para push — zero push executado nesta sessão. Zero tag criada.
```

---

**Passo 5 — Criar SUMMARY da phase:**

Criar `.planning/phases/07.8-saldo-atualizado-composicao/07.8-04-SUMMARY.md` com:
- SHAs: 3 do submodule + 1 do pai (full + short)
- Output literal do UAT 7 cenários (copiar report do usuário da Task 1)
- Output dos gates determinísticos (regressão + build + motor diff + Modal base + @media print + escopo)
- Texto literal das 5 autorizações do usuário (PAUSA #1/#2/#3 já registradas nas plans 07.8-01/02/03 SUMMARYs; #4 + #5 nesta plan)
- Evidência invariante motor Art.354 (confirmação que pagamentos.js + dividas.js não mudaram; devedorCalc.js só adições)
- Evidência Modal base intocado (D-06)
- Evidência @media print isolado (D-13)
- Próximos passos EXATOS (push + deploy + UAT prod + tag)
- Confirmação final: "zero push executado, zero tag criada"

**NÃO** atualizar `.planning/STATE.md` ou `.planning/ROADMAP.md` nesta task — é ação de `/gsd-close-phase` ou `/gsd-complete-phase`.

---

**Passo 6 — Confirmar SEM PUSH:**

```bash
cd src/mr-3/mr-cobrancas
git log origin/main..main --oneline
# esperado: ≥3 linhas (07.8-03 + 07.8-02 + 07.8-01)

cd ../../..
git log origin/master..master --oneline
# esperado: ≥1 linha (o commit de bump desta phase + possivelmente commits de phases anteriores ainda não pushed)
```

Se alguma linha 0 → push foi executado indevidamente; investigar crítico. (Não deveria acontecer — nenhum `git push` foi chamado.)

  </how-to-verify>

  <acceptance_criteria>
    **PAUSA #5 autorizada:**
    - Agente executou `git log -3 origin/main..main --stat` + exibiu 3 SHAs e os subjects dos 3 commits ao usuário
    - Agente reportou invariantes confirmadas: motor Art.354 (devedorCalc só adições; pagamentos+dividas vazios), Modal base vazio (D-06), @media print isolado (D-13)
    - Usuário respondeu `approved`, `bump it`, ou equivalente explícito ANTES do commit no pai
    - Texto do chat contém marcador `PAUSA #5` ou equivalente

    **Commit do pai correto:**
    - `git rev-parse --abbrev-ref HEAD` na raiz do pai retorna `master`
    - `git log -1 --format=%s` retorna EXATAMENTE `chore(07.8): bump submodule mr-3 — saldo atualizado + composição com impressão (07.8-01 + 02 + 03)`
    - `git log -1 --name-only --format=` lista EXATAMENTE 1 entrada: `src/mr-3`
    - `git show --stat HEAD` mostra 1 submodule pointer atualizado (`Subproject commit <SHA_antigo> → <SHA_novo>`)
    - `git log -1 --format=%b` contém: `Phase 7.8`, `calcularDetalheEncargosContrato`, `D-01`, `D-06`, `Motor Art.354 (...) LITERALMENTE intocado`, `Modal base`, `window.open`, `Times New Roman`, `SC-1..SC-7`, `Tag v1.4-phase7.8 pendente`, `Co-Authored-By: Claude Opus 4.7`

    **Zero push:**
    - `cd src/mr-3/mr-cobrancas && git log origin/main..main --oneline` retorna **≥3 linhas** (07.8-01/02/03 locais, possíveis de phases anteriores)
    - `cd <raiz do pai> && git log origin/master..master --oneline` retorna **≥1 linha** (novo commit de bump)
    - **NENHUM** comando `git push` executado em toda a Task 2 (histórico do shell deve confirmar)

    **Zero tag:**
    - `git tag -l "v1.4-phase7.8*"` retorna vazio

    **SUMMARY criado:**
    - `.planning/phases/07.8-saldo-atualizado-composicao/07.8-04-SUMMARY.md` existe e contém:
      * SHAs (submodule x3 + pai x1)
      * Report literal do UAT SC-1..SC-7
      * Output dos gates determinísticos
      * Referência das 5 autorizações (PAUSAs #1..#5)
      * Evidência invariante motor + Modal base + @media print isolation
      * Próximos passos (push + deploy + UAT prod + tag)
      * Confirmação explícita: "zero push, zero tag"

    **Nenhum arquivo fora de escopo foi commitado:**
    - `git show HEAD --stat` no pai mostra EXATAMENTE 1 entrada: `src/mr-3`
    - Arquivos `.planning/STATE.md`, `.planning/ROADMAP.md` etc pré-existentes como modified NÃO aparecem no commit de bump (permanecem unstaged)
  </acceptance_criteria>

  <resume-signal>
    Esta task tem 1 gate humano (PAUSA #5) — autorização explícita (`approved` / `bump it`). Agente NÃO prossegue sem.

    Após Passo 4 reportado: task completa. Usuário pode:
    - Encerrar sessão e autorizar push em sessão separada (padrão 7.5.1 / 7.6 / 7.7).
    - Pedir `/gsd-close-phase 07.8` pra atualizar STATE.md + ROADMAP.md (fora do escopo desta task).
    - Autorizar push imediato: agente executa `cd src/mr-3/mr-cobrancas && git push origin main && cd ../../.. && git push origin master` APENAS se user disser explicitamente `push agora` / `autoriza push`.
  </resume-signal>

  <done>
    Commit `chore(07.8): bump submodule mr-3 — saldo atualizado + composição com impressão (07.8-01 + 02 + 03)` existe no repo pai branch `master` com APENAS `src/mr-3` stageado. 4 SHAs finais reportados (3 submodule + 1 pai). 5 autorizações humanas acumuladas na phase (PAUSA #1 + #2 + #3 + #4 + #5). SUMMARY criado. Zero push, zero tag criada. Próximos passos (push + deploy + UAT prod + tag) documentados para ação separada do usuário.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| Agente → Git CLI do submodule e do pai | Comandos locais (git add/commit, npm). Sem boundary de rede — zero push. |
| Agente → Usuário (checkpoints humanos) | 2 PAUSAs bloqueantes nesta plan (#4 UAT + #5 bump). Autorização explícita obrigatória. |

## STRIDE Threat Register

| Threat ID | Categoria | Componente | Disposição | Mitigação |
|-----------|-----------|------------|------------|-----------|
| T-07.8-04-01 | Tampering (push acidental) | Agente invoca `git push` inadvertidamente | mitigate | AC negativa explícita "NENHUM comando git push". Passo 6 conta commits pendentes; se count = 0, push ocorreu e é crítico. |
| T-07.8-04-02 | Tampering (`git add -A`) | Commit do pai inclui `.planning/STATE.md` etc | mitigate | `git add src/mr-3` EXPLICITAMENTE (nunca `-A`/`.`). Passo 3 confirma `git status --short` = SÓ `M src/mr-3` stageado. AC: `git show HEAD --stat` = 1 arquivo. |
| T-07.8-04-03 | Elevation of Privilege (usuário fatigado autoriza sem revisar) | Modo cansado D-15, 5 pausas totais | accept | D-15 EXPLICITAMENTE adiciona pausas para mitigar fadiga. Autorização explícita exigida em cada gate. Se usuário auto-aprova cegamente, risco aceito. |
| T-07.8-04-04 | Repudiation (commit message inconsistente) | Mensagem do bump diverge do padrão | mitigate | Texto literal via HEREDOC. AC inclui grep de strings-chave (`Phase 7.8`, `D-01`, `D-06`, `Motor Art.354 (...) LITERALMENTE intocado`, `Tag v1.4-phase7.8 pendente`). |
| T-07.8-04-05 | DoS (dev server não sobe, bloqueia UAT) | npm run dev falha | accept | Unlikely (build já passou); se acontecer, usuário reporta, agente diagnostica sem urgência. |
| T-07.8-04-06 | Tag criada prematuramente | `git tag v1.4-phase7.8` disparado | mitigate | AC explícita `git tag -l "v1.4-phase7.8*"` = vazio. D-17 + CONTEXT §6 SC-17 "Tag v1.4-phase7.8 NÃO é criada nesta phase". |
| T-07.8-04-07 | Information Disclosure (commit body) | SHAs, paths internos | accept | Dados expostos são intencionais para auditoria. Sem secrets. |
| T-07.8-04-08 | Tampering (revert de commit do submodule durante bump) | Usuário pede abort de um commit no meio | accept+mitigate | Se usuário pedir correção em 07.8-03 tip: 4º commit de correção preferível (amend quebra SHA). Agente documenta e re-inicia gates Task 1. |
| T-07.8-04-09 | UAT reporta bug crítico (SC fail bloqueante) | Bug no modal, print, ou wire | mitigate | `<resume-signal>` trata FAIL: agente investiga, aplica fix local (novo commit de correção), re-roda gates + UAT. |
| T-07.8-04-10 | Navegador bloqueia pop-up de print (SC-6) | window.open retorna null | accept+mitigate | Handler no DecomposicaoSaldoModal.jsx (plan 07.8-03) já trata `if (!w) toast.error`. UAT SC-6 documenta expectativa; usuário permite pop-up pro teste. |
</threat_model>

<verification>
Verificação final da plan 07.8-04 (após Task 2 completa):

1. **Submodule main (estado final):**
   - `cd src/mr-3/mr-cobrancas && git log origin/main..main --oneline | head -3` → 3 linhas (07.8-03 + 07.8-02 + 07.8-01)
   - `cd src/mr-3/mr-cobrancas && git diff HEAD~3 HEAD -- src/utils/devedorCalc.js | grep -cE "^-[^-]"` → 0 (D-01 só adições)
   - `cd src/mr-3/mr-cobrancas && git diff HEAD~3 HEAD -- src/services/pagamentos.js src/services/dividas.js` → vazio (D-01)
   - `cd src/mr-3/mr-cobrancas && git diff HEAD~3 HEAD -- src/components/ui/Modal.jsx` → vazio (D-06)
   - `cd src/mr-3/mr-cobrancas && npm run test:regressao` → 10/10 PASS
   - `cd src/mr-3/mr-cobrancas && npm run build` → exit 0

2. **Pai master (estado final):**
   - `cd <raiz> && git log -1 --format=%s` → `chore(07.8): bump submodule mr-3 — saldo atualizado + composição com impressão (07.8-01 + 02 + 03)`
   - `cd <raiz> && git log -1 --name-only --format=` → `src/mr-3` (1 linha apenas)
   - `cd <raiz> && git log origin/master..master --oneline` → inclui o commit novo

3. **Zero push:**
   - Ambos `git log origin/*..` retornam ≥1 commit local. Nenhum `git push` executado.

4. **Sem tag:**
   - `cd <raiz> && git tag -l "v1.4-phase7.8*"` → vazio.

5. **UAT:**
   - 7 cenários SC-1..SC-7 reportados pelo usuário (Task 1 PAUSA #4), referenciados no SUMMARY.

6. **5 autorizações humanas registradas na phase:**
   - PAUSA #1 (07.8-01 Task 3), #2 (07.8-02 Task 3), #3 (07.8-03 Task 4), #4 (07.8-04 Task 1), #5 (07.8-04 Task 2) — todas com texto do usuário no chat.

7. **SUMMARY criado** com: SHAs (3+1), UAT report, gates, 5 autorizações referenciadas, invariante motor + Modal base + @media print, zero push/tag, próximos passos.

8. **Isolamento de impressão (D-13):**
   - `grep @media print` fora de `DecomposicaoSaldoModal.jsx` e `GerarPeticao.jsx` → vazio.
</verification>

<success_criteria>
Plan 07.8-04 completa quando:

1. ✅ Task 1: gates determinísticos PASS (regressão 10/10, build exit 0, motor diff correto, Modal base vazio, @media print isolado, escopo 4 arquivos)
2. ✅ Task 1: UAT 7 cenários SC-1..SC-7 reportados pelo usuário (PAUSA #4) e aprovados
3. ✅ Task 2 PAUSA #5: bump commit autorizado explicitamente (`approved` / `bump it`)
4. ✅ Task 2: commit no pai branch master com subject EXATO e APENAS `src/mr-3` stageado
5. ✅ Zero push em toda a phase (ambos `git log origin/*..` ≥1 commit local)
6. ✅ Zero tag criada (`git tag -l "v1.4-phase7.8*"` vazio)
7. ✅ SUMMARY criado em `.planning/phases/07.8-saldo-atualizado-composicao/07.8-04-SUMMARY.md`
8. ✅ Invariante motor Art.354 preservada de ponta a ponta (verificada em todas as 4 plans da phase)
9. ✅ Modal base `ui/Modal.jsx` intocado de ponta a ponta (D-06)
10. ✅ Zero `@media print` fora dos 2 pontos canônicos (D-13)
11. ✅ 5 pausas totais registradas (D-15): #1 (07.8-01), #2 (07.8-02), #3 (07.8-03), #4 (07.8-04 UAT), #5 (07.8-04 bump) — todas com autorização explícita do usuário.
</success_criteria>

<output>
Após conclusão da Task 2, criar `.planning/phases/07.8-saldo-atualizado-composicao/07.8-04-SUMMARY.md` contendo:

- **SHAs:** 3 do submodule (07.8-01 + 07.8-02 + 07.8-03) + 1 do pai (bump), full + short
- **UAT:** resultado literal dos 7 cenários SC-1..SC-7 (copiar report do usuário da Task 1)
- **Gates determinísticos:** output de regressão (10/10), build (exit 0), motor diff (devedorCalc só adições; pagamentos+dividas vazios), Modal base (vazio, D-06), @media print (só pontos canônicos, D-13), escopo (4 arquivos)
- **Autorizações humanas (5):** referência textual a PAUSA #1/#2/#3/#4/#5 com snippets do usuário
- **Invariante motor Art.354:** confirmação explícita que `devedorCalc.js` + `pagamentos.js` + `dividas.js` não tiveram mudança de corpo (só adição do adapter no final de devedorCalc.js)
- **Modal base intocado (D-06):** `git diff HEAD~3 HEAD -- src/components/ui/Modal.jsx` vazio
- **Isolamento de impressão (D-13):** zero `@media print` fora dos 2 arquivos canônicos
- **Zero push + zero tag:** confirmação explícita
- **Próximos passos:**
  1. Push quando autorizar: `cd src/mr-3/mr-cobrancas && git push origin main && cd <raiz> && git push origin master`
  2. Deploy Vercel automático pós-push (cache 7.4 OK)
  3. UAT produção em mrcobrancas.com.br com contrato real
  4. Tag `v1.4-phase7.8` APENAS após UAT prod OK, em ação separada autorizada

O SUMMARY é o artefato de fechamento da phase — `/gsd-close-phase` usa para atualizar STATE.md e ROADMAP.md.
</output>
</content>
