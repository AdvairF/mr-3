# Phase 7.10bcd — Migração 3 consumidores (Devedores + ModuloDividas + Dashboard) para pagamentos_divida

**Status**: CONTEXT em discussão — aguarda OK do usuário antes de `/gsd-insert-phase 7.10bcd`.
**Inserção**: manual pós-7.10a ship (precedente phases 7.x via Edit em ROADMAP.md).
**Discussão**: sessão 2026-04-25 pós-ship 7.10a (tag v1.4-phase7.10a em 7edb8cb pai + 2757f74 submódulo).
**Modo**: standard reforçado — **5 humanos checkpoints** (3 sub-PAUSAs intra-Plan-01 + 2 PAUSAs Plan 02). `autonomous: false` em todos os plans.
**Risco**: MÉDIO-ALTO (3 consumidores = 3x mais surface em 1 phase; mitigations M1-M6 reduzem mas não eliminam).
**Estratégia**: fast-track aceito pelo usuário com mitigations não-negociáveis (commits atômicos + UAT cross-check obrigatório por consumidor + atomicidade no ship).

---

## 1. Problema

3 consumidores legacy lendo `allPagamentos` (alimentado por `pagamentos_parciais` v1.0) ainda em prod pós-7.10a:
- **Dashboard** (App.jsx L562-720) — 7 refs efetivas, múltiplos cálculos visíveis em cards (Carteira, Recuperado, Em Aberto, Taxa, Recebido Período)
- **Devedores** (App.jsx L2957+) — 3 refs, 1 Map agrupador `pgtosPorDevedor` consumido em 5 callsites internos (PDF, prop pra PessoasVinculadas, saldo per linha, imprimirFicha)
- **ModuloDividas** (.jsx) — 3 refs (1 prop + 2 pass-through), 2 sub-componentes downstream (TabelaDividas + DetalheDivida) com filter `p.devedor_id === divida.devedor_id` quebrado por design no shape novo

Migrar TODOS de uma vez (fast-track) reduz tempo total mas concentra risco. Estratégia atômica recomendada (1 consumidor por phase) foi REJEITADA pelo usuário; aceito fast-track com mitigations M1-M6 não-negociáveis.

ProcessosJudiciais (7.10a) já migrado e shipado — ZERO referência a `allPagamentos` legacy nas 4 telas atuais já alinhadas pós-7.10a + 7.10bcd.

## 2. Escopo aprovado — 3 consumidores em 1 phase, 3 commits atômicos

### Fix A — Helper compartilhado (NEW)
**`src/mr-3/mr-cobrancas/src/utils/agruparPagamentosPorDevedor.js`** (NOVO):
```js
export function agruparPagamentosPorDevedor(devedores, pagamentosDivida) {
  const dividaIdToDevedorId = new Map();
  (devedores || []).forEach(d =>
    (d.dividas || []).forEach(div =>
      dividaIdToDevedorId.set(String(div.id), String(d.id))
    )
  );
  const map = new Map();
  (pagamentosDivida || []).forEach(p => {
    const devId = dividaIdToDevedorId.get(String(p.divida_id));
    if (!devId) return;
    if (!map.has(devId)) map.set(devId, []);
    map.get(devId).push(p);
  });
  return map;
}
```

Test trivial: `src/services/__tests__/agruparPagamentosPorDevedor.test.js` (NEW) com 2 it() (devedor com pagamentos múltiplos + devedor sem dívidas).

### Fix B — Commit 07.10b: Devedores

**App.jsx callsite (L8468)**: `allPagamentos={allPagamentos}` → `allPagamentos={allPagamentosDivida}` no case "devedores".

**App.jsx Devedores function (L2984-2992)**: `pgtosPorDevedor` Map agrupador. Trocar `forEach(p => map[p.devedor_id])` por `agruparPagamentosPorDevedor(devedores, allPagamentos)` (a prop ainda chama allPagamentos mas o conteúdo é pagamentos_divida pós-callsite swap).

**Shield 24a** (positivo + negativo) — grep callsite case "devedores" + grep negativo regional.

**Files no commit 07.10b**: 1 (App.jsx) + 1 helper (NEW) + 1 test (NEW) + package.json update test:regressao = **4 files**.

### Fix C — Commit 07.10c: ModuloDividas + sub-componentes

**App.jsx callsite (L8488)**: `allPagamentos={allPagamentos}` → `allPagamentos={allPagamentosDivida}` no case "dividas".

**ModuloDividas.jsx**: 0 cálculos diretos — pass-through pra TabelaDividas + DetalheDivida sem mudança.

**TabelaDividas.jsx L41-44**: refactor `buildDevedorObjParaSaldo`. Antes:
```js
const pagamentosDoDevedor = allPagamentos.filter(p => String(p.devedor_id) === String(divida.devedor_id));
```
Depois (Set lookup pattern 7.10a — D-27):
```js
const dividaIdsDoDevedor = new Set((devedor.dividas || []).map(d => String(d.id)));
const pagamentosDoDevedor = allPagamentos.filter(p => dividaIdsDoDevedor.has(String(p.divida_id)));
```

**DetalheDivida.jsx L74**: refactor mesmo pattern.

**Shield 24b** — 3 callsites (App.jsx case "dividas" + TabelaDividas filter + DetalheDivida filter).

**Files no commit 07.10c**: 3 (App.jsx + TabelaDividas + DetalheDivida) — ModuloDividas pode ficar untouched (pass-through inalterado, prop name mantida).

### Fix D — Commit 07.10d: Dashboard

**App.jsx callsite (L8467)**: `allPagamentos={allPagamentos}` → `allPagamentos={allPagamentosDivida}` no case "dashboard".

**App.jsx Dashboard function (L562-720)**:
- L568 `forEach`: trocar pra usar `agruparPagamentosPorDevedor` helper (mesma estratégia de Devedores)
- L613 `reduce`: zero mudança (lê só `p.valor`)
- L667 `filter por data_pagamento`: zero mudança (lê só `p.data_pagamento`)
- L696 `filter por devedor_id`: refactor inline Set lookup (D-27 pattern), OU usar helper se simétrico ao Devedores

**Shield 24c** (positivo + negativo) — grep callsite case "dashboard" + grep negativo regional.

**Fix D-extra (parent App L8425-8433)**: O Map `pgtosPorDevedorCarteira` em escopo do **App parent** (FORA da function Dashboard interna) alimenta `totalCarteira` em L8434-8440 → exibido em L8735 como card 'Carteira' do header GLOBAL (visível em TODAS as tabs, não apenas Dashboard). Este Map é distinto do `pgtosPorDevedorCarteira` interno do Dashboard L566. Migrar TAMBÉM no commit 07.10d via mesmo helper `agruparPagamentosPorDevedor`. Trocar `forEach(p => map[p.devedor_id])` por `agruparPagamentosPorDevedor(devedores, allPagamentosDivida)`. Sem mudança no consumer downstream — apenas troca de fonte do Map.

**Files no commit 07.10d**: 1 (App.jsx Dashboard mudanças + parent App L8425 Map) — package.json talvez se adicionar shield separado, mas helper já registrado em test:regressao.

### Total escopo phase 7.10bcd

- **3 commits atômicos** no submódulo (07.10b + 07.10c + 07.10d)
- **2 arquivos novos** (helper + test do helper)
- **5 arquivos modificados** (App.jsx em 3 commits diferentes — Devedores em 07.10b, callsite case "dividas" em 07.10c, callsite case "dashboard" + Dashboard internal body + parent App pgtosPorDevedorCarteira + ultimosPagamentos + recuperadoPeriodo em 07.10d — note App.jsx aparece em 3 commits mas é 1 arquivo; + TabelaDividas + DetalheDivida + package.json). Total 6 files cumulativos: helper NEW + helper test NEW + App.jsx + TabelaDividas + DetalheDivida + package.json. ModuloDividas zero diff confirmado.
- **3 shields novos** (24a + 24b + 24c — 1 por consumidor) + **Shield 24d** (parent App L8425 pgtosPorDevedorCarteira — granular adicional)
- **5 humanos checkpoints** (3 intra-Plan-01 + 2 Plan 02)

## 3. Fora do escopo

- Migrar ModuloContratos / DetalheContrato / ProcessosJudiciais (já migrados em 7.3 / 7.8.2a / 7.10a)
- Cortar writes em `pagamentos_parciais` (UI legacy ainda ativa — phase 7.10e hipotético)
- Drop schema `pagamentos_parciais` (phase final pós-cleanup writes)
- Tocar motor Art.354 (D-01 invariante mantido)
- Adicionar mutation handlers (D-05 não muda — esta phase é leitura only)
- Renomear `pagamentos_parciais` em fixtures de test (calculos.test.js + casos-tjgo.json — fixture local, não consumidor real)
- Refactorar PessoasVinculadas pra ler de outra fonte (recebe `pagamentos` prop pré-filtrada, sem mudança)
- Remover prop `allPagamentos` legacy (continua existindo no caller App.jsx — Vercel build ainda precisa do state, só callsites trocam pra `allPagamentosDivida`)

## 4. Decisões locked (discuss 2026-04-25)

### Invariantes herdadas — MANTIDAS sem alteração
- **D-01 a D-29** (de phases 7.x anteriores) — motor 100% intocado, isolation, fingerprint deep 14 campos, Set lookup pattern 7.10a etc. Esta phase NÃO toca nenhum desses escopos.
- **D-05 callers completude** — esta phase NÃO adiciona mutation handlers (10 handlers continua). Esta phase é puro consumer-side leitura.
- **D-27** Set lookup `divida_id ∈ dividas-do-devedor` — pattern reutilizado inline em TabelaDividas + DetalheDivida (Fix C).

### Novas invariantes 7.10bcd

- **D-30** — **3 commits atômicos no submódulo** (M1). NÃO 1 commit feat(7.10bcd) gigante. Sim sequência: `feat(07.10b): Devedores...` → `feat(07.10c): ModuloDividas...` → `feat(07.10d): Dashboard...`. Bisect funciona; isolamento de regressão garantido. Cada commit independente git-bisect-able.

- **D-31** — **Helper compartilhado `agruparPagamentosPorDevedor`** (M2 / Q2). Criado no commit 07.10b (primeiro consumidor que precisa). Reutilizado em 07.10d (Dashboard L568). Test unit trivial (2 it()). Custo: 1 file new + 1 test new. DRY > inline duplication (lição "helper-first" da 7.9).

- **D-32** — **5 humanos checkpoints proporcionais ao risco** (M3 + M6 + Q6). 3 sub-PAUSAs intra-Plan-01 (após cada commit: regressão + grep + reporte) + 2 PAUSAs Plan 02 (UAT cross-check + diff consolidado pré-bump). Defesa contra empilhamento de regressão silenciosa.

- **D-33** — **UAT cross-check 7 pontos visuais OBRIGATÓRIOS** (M2 / Q4):
  - Devedores: saldo MENDES per linha (esperado R$ 36.871,64 já validado em 7.9)
  - ModuloDividas: TabelaDividas → coluna "Saldo Atualizado" + DetalheDivida → linha "Saldo Atualizado" (mesma dívida)
  - Dashboard 5 cards: **Carteira**, **Recuperado**, **Em Aberto**, **Taxa Recuperação**, **Recebido Período** (este último MANDATÓRIO porque exercita filter por dataInicio = caminho mais complexo)
  - Total: 7 pontos visuais cross-check prod vs local. Drift de centavo em qualquer um = FAIL phase inteira.

- **D-34** — **Atomicidade no ship** (M4). Se UAT cross-check de qualquer 1 dos 7 pontos visuais FAIL → rollback total dos 3 commits via `git reset --hard 2757f74` no submódulo (volta ao state 7.10a-pre-bcd — rollback APENAS dos 3 commits 7.10bcd, preserva 7.10a shipped). Baseline 2757f74 = post-7.10a HEAD pré-7.10bcd (NÃO d354daa que é pre-7.10a — phase 7.10a já está SHIPPED em prod com tag v1.4-phase7.10a). Phase aborta inteira, não ship parcial 07.10b+c sem 07.10d.
  - **Exceção única**: bug trivial fixável em <5min vira fix-forward dentro da mesma phase (flow normal de fix-forward — não é exceção, é caminho regular).
  - Decisão registrada por usuário 2026-04-25: M4 não-negociável. Fast-track 3 juntos é decisão atômica; FAIL parcial = rollback total.

### Pattern reutilizável (Fix C — Set lookup inline)

```js
const dividaIdsDoDevedor = new Set((devedor.dividas || []).map(d => String(d.id)));
const pagamentosDoDevedor = pagamentosDivida.filter(p => dividaIdsDoDevedor.has(String(p.divida_id)));
```

Aplicado em:
- TabelaDividas.jsx L41-44 (commit 07.10c)
- DetalheDivida.jsx L74 (commit 07.10c)
- Dashboard L696 (commit 07.10d)

3 callsites = ainda inline OK (≤5 = inline; ≥5 = vira helper). Não justifica helper compartilhado pra esse pattern menor.

### Pattern reutilizável (Fix A — Helper agruparPagamentosPorDevedor)

Aplicado em:
- Devedores L2986 (commit 07.10b)
- Dashboard L568 (commit 07.10d)

2 callsites + complexidade da lookup map = vira helper. Custo de 1 file novo se justifica vs duplicação inline + manutenção dupla.

## 5. Success Criteria

1. Helper `src/utils/agruparPagamentosPorDevedor.js` criado + test unit (commit 07.10b)
2. Devedores Map (App.jsx L2984-2992) usa helper; callsite L8468 swap allPagamentos→allPagamentosDivida (commit 07.10b)
3. ModuloDividas + TabelaDividas + DetalheDivida usam Set lookup pattern; callsite L8488 swap (commit 07.10c)
4. Dashboard cálculos usam helper agrupador + Set lookup inline; callsite L8467 swap (commit 07.10d)
5. **Shield 24a** positivo (callsite L8468 contém `allPagamentos={allPagamentosDivida}`) + negativo regional (case "devedores" + 10 linhas sem `allPagamentos={allPagamentos}` legacy)
6. **Shield 24b** positivo + negativo regional pra case "dividas" + greps em TabelaDividas + DetalheDivida (filter pattern Set lookup presente, filter legacy `p.devedor_id` ausente)
7. **Shield 24c** positivo + negativo regional pra case "dashboard"
8. **Regressão por commit (M5)**: passa após CADA UM dos 3 commits (não apenas pós-execute completo). Esperado 25/25 (23 da 7.10a + 2 do helper test) ou superior.
9. Build exit 0 após cada commit
10. **D-01 estrito** em todos 3 motor files (devedorCalc.js + pagamentos.js + dividas.js — diff vazio em cada commit)
11. **D-30 commits atômicos**: `git log 2757f74..HEAD --oneline` no submódulo mostra exatamente 3 commits sequenciais com mensagens `feat(07.10b)`, `feat(07.10c)`, `feat(07.10d)` (baseline 2757f74 = post-7.10a HEAD pré-7.10bcd; NÃO d354daa que é pre-7.10a já shipped)
12. **D-33 UAT cross-check 7 pontos visuais** todos PASS (Devedores 1 + ModuloDividas 2 + Dashboard 5 cards mandatórios)
13. ZERO push sem autorização explícita por step
14. Tag `v1.4-phase7.10bcd` NÃO criada nesta phase (pós-smoke prod)
15. **D-34 atomicidade**: se qualquer UAT FAIL → rollback total `git reset --hard 2757f74` (baseline post-7.10a HEAD pré-7.10bcd; NÃO d354daa que é pre-7.10a já shipped), nunca ship parcial

## 6. UAT cenários (7 cenários)

### SC1 — Pré-check pagamentos_divida disponíveis [MANDATÓRIO]
Confirmar que `SELECT COUNT(*) FROM pagamentos_divida` retorna ≥1 (pagamentos via stored proc Phase 7).

### SC2 — Devedores: saldo MENDES R$ 36.871,64 [MANDATÓRIO]
PROD vs LOCAL: lista de Devedores → MENDES → coluna "Saldo Atualizado". Esperado IDÊNTICO (R$ 36.871,64 já validado 7.9). Drift de centavo = FAIL.

### SC3 — ModuloDividas TabelaDividas: linha mesma dívida [MANDATÓRIO]
PROD vs LOCAL: ModuloDividas → lista de dívidas → identificar 1 dívida específica → coluna "Saldo Atualizado". Esperado IDÊNTICO.

### SC4 — DetalheDivida: mesma dívida do SC3 [MANDATÓRIO]
PROD vs LOCAL: clicar na dívida do SC3 → DetalheDivida → linha "Saldo Atualizado". Esperado IDÊNTICO ao SC3 + IDÊNTICO entre prod/local.

### SC5 — Dashboard card "Carteira" [MANDATÓRIO]
PROD vs LOCAL: Dashboard → card "Total Carteira" (totalCarteira agregado). Drift = FAIL.

### SC6 — Dashboard card "Recuperado" + "Em Aberto" + "Taxa" [MANDATÓRIO]
PROD vs LOCAL: Dashboard → 3 cards. Drift em qualquer = FAIL. (Pode ser 1 SC consolidado ou 3 separados; planner decide.)

### SC7 — Dashboard card "Recebido Período" — caminho mais complexo (filter por dataInicio) [MANDATÓRIO]
PROD vs LOCAL: Dashboard → selecionar período (ex: "30 dias") → card "Recebido no Período". Filter exercitado: `allPagamentos.filter(p.data_pagamento >= dataInicio)`. Drift = FAIL. **Não pode SKIP** — caminho mais arriscado de toda phase.

Mandatórios: TODOS os 7 (SC1-SC7). Sem opção de SKIP nesta phase — risco aceito de fast-track exige cobertura cross-check completa.

## 7. Shield greps — assinaturas exatas

```bash
# Shield 24a positivo (App.jsx case "devedores") — 1 match esperado
grep -nE 'allPagamentos=\{allPagamentosDivida\}' src/mr-3/mr-cobrancas/src/App.jsx | grep -A0 "devedores"

# Shield 24a negativo regional (case "devedores" + 10 linhas)
awk '/case "devedores":/{flag=1; n=0} flag{print; n++; if (n>10) flag=0}' src/mr-3/mr-cobrancas/src/App.jsx | grep -F 'allPagamentos={allPagamentos}'
# Esperado: zero matches

# Shield 24b — case "dividas" + TabelaDividas filter + DetalheDivida filter
awk '/case "dividas":/{flag=1; n=0} flag{print; n++; if (n>10) flag=0}' src/mr-3/mr-cobrancas/src/App.jsx | grep -F 'allPagamentos={allPagamentos}'
grep -nE 'dividaIdsDoDevedor' src/mr-3/mr-cobrancas/src/components/TabelaDividas.jsx
grep -nE 'dividaIdsDoDevedor' src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx
# Filter legacy ausente em ambos:
grep -nE 'p\.devedor_id\s*===\s*.*\.devedor_id' src/mr-3/mr-cobrancas/src/components/TabelaDividas.jsx src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx
# Esperado: zero matches

# Shield 24c — case "dashboard" + 10 linhas
awk '/case "dashboard":/{flag=1; n=0} flag{print; n++; if (n>10) flag=0}' src/mr-3/mr-cobrancas/src/App.jsx | grep -F 'allPagamentos={allPagamentos}'
# Esperado: zero matches

# Helper agruparPagamentosPorDevedor presente
grep -nE 'export function agruparPagamentosPorDevedor' src/mr-3/mr-cobrancas/src/utils/agruparPagamentosPorDevedor.js
```

## 8. Task structure preliminar (planner refina)

### Plan 01 — 3 commits atômicos com PAUSAs intra-execute

- **Task 1** — Criar helper agruparPagamentosPorDevedor.js + test (NEW files). Run regressão.
- **Task 2** — Commit 07.10b: Devedores migra fonte. App.jsx callsite + Devedores Map usa helper. Run regressão. **PAUSA #1a — usuário valida regressão + grep + reporte. Aguarda OK.**
- **Task 3** — Commit 07.10c: ModuloDividas + sub-componentes. App.jsx callsite + TabelaDividas filter + DetalheDivida filter. Run regressão. **PAUSA #1b — usuário valida. Aguarda OK.**
- **Task 4** — Commit 07.10d: Dashboard agregados. App.jsx callsite + Dashboard L568 helper + L696 Set lookup. Run regressão. **PAUSA #1c — usuário valida + Plan 01 gates finais. Aguarda OK.**
- **Task 5** — Plan 01 gates finais (já validados intra-PAUSAs): D-01 dual gate C1+C2 final + Shield 24a+b+c todos OK + commits chain `feat(07.10b)→feat(07.10c)→feat(07.10d)` + 25/25 regressão.
- **Task 6** — SUMMARY 01: chain de 3 commits + ESTRATÉGIA fast-track + mitigations M1-M6 aplicadas. Left uncommitted no parent.

### Plan 02 — UAT + bump

- **Task 1 PAUSA #2** — UAT cross-check 7 SCs (SC1-SC7). Drift de centavo em qualquer = FAIL phase inteira (D-34 atomicidade).
- **Task 2 PAUSA #3** — Diff consolidado pré-bump (3 commits, 5 files modificados, 2 NEW). Motor diff vazio em todos 3.
- **Task 3** auto — Push submódulo (3 commits). Bump pai. Push pai (Vercel deploy).
- **Task 4** auto — SUMMARY 02 + STATE.md SHIPPED + docs commit consolidado.

**Total**: 5 humanos checkpoints (3 intra-Plan-01 + 2 Plan 02).

**Splitting**: 1 plan impl (3 commits) + 1 plan UAT/bump. Mesmo pattern 7.9/7.10a mas com PAUSAs intra-Plan-01 mais granulares.

## 9. Investigação pré-discuss — achados

### 4 callsites Devedores Map auditados (Q5 grep)

Consumidores downstream do `pgtosPorDevedor.get(devId)` array:
- L3545 → imprimirFicha (PDF) — usa `p.valor`, `p.data_pagamento`, `p.observacao`, `length`
- L3679 → PessoasVinculadas (sub-component) — recebe prop, ZERO acesso ao item individual (`pagamentos\.` retornou ZERO matches)
- L4056 → calcularSaldoDevedorAtualizado — motor lê só data_pagamento+valor (já confirmado 7.10a)
- L4056 → reduce(parseFloat(p.valor)) — só valor
- L4123 → imprimirFicha — mesma de L3545

**ZERO callsite usa `p.devedor_id` no item individual.** Helper enriched (Q5 opção a) NÃO necessário. Pattern simples sem enrichment funciona.

### Helper-first audit (lição 7.9)

Sem helper compatível existente. Helper novo `agruparPagamentosPorDevedor` é a forma certa pra Map agrupador (Devedores L2986 + Dashboard L568). Set lookup inline (D-27) é a forma certa pra filter individual (TabelaDividas L44 + DetalheDivida L74 + Dashboard L696).

### Schema check (lição 7.9)

`pagamentos_divida` shape já confirmado em 7.10a CONTEXT (de pagamentos.js L7-14 JSDoc):
- `id UUID`, `divida_id UUID NOT NULL`, `data_pagamento DATE NOT NULL`, `valor NUMERIC(15,2) NOT NULL`, `observacao TEXT`
- **Sem `devedor_id` direto**. Lookup intermediário via dividas-do-devedor é OBRIGATÓRIO em qualquer pattern de filtro/agrupamento por devedor.

### Single-source-of-truth check (lição 7.9)

Cards do Dashboard (Carteira, Recuperado, Em Aberto, Taxa, Recebido Período) compartilham fórmula com Devedores (saldo per linha) + ModuloDividas/DetalheDivida (saldo per dívida) — todos derivam de `calcularSaldoDevedorAtualizado` ou similar via Map agrupador. Helper `agruparPagamentosPorDevedor` é o single source of truth de agrupamento. Pós-migração, drift entre callsites é controlado.

### State App.jsx already populated

`allPagamentosDivida` em App.jsx L8325 (state), L8344 (fetch via `dbGet("pagamentos_divida")`), L8501 (já passado pra ModuloContratos) — Phase 7.3 setup já existe. ZERO migration nova; 3 props swap.

### Risk reclassification — fast-track aceito

3 consumidores migrados em 1 phase (vs 3 phases atômicas) é **maior risco** mas aceito pelo usuário com mitigations não-negociáveis. Risco residual: bug serial pego em UAT cross-check (D-34 rollback total mitiga); Shield 24a+b+c grep não pega bugs de cálculo (motor compatível verificado mas drift histórico pode aparecer); 3 commits atômicos permitem bisect se regressão pós-smoke prod.

## 10. Notas de implementação

- **Helper-first applied**: `agruparPagamentosPorDevedor` nasce em commit 07.10b, reutilizado em 07.10d. Test trivial blinda regressão silenciosa do helper.
- **Set lookup inline replicado** em 4 lugares: TabelaDividas L44 + DetalheDivida L74 + Dashboard L696 (ultimosPagamentos) + Dashboard L682 (recuperadoPeriodo). Padrão 7.10a já estabelecido. Se 5+ replicarem em phases futuras, extrair pra helper.
- **Drift histórico continua aceito** (D-28 da 7.10a). Pagamentos via UI legacy "Registrar pagamento parcial" (App.jsx L2537+) ainda escrevem em `pagamentos_parciais`. Pós-7.10bcd, **TODOS** os consumidores em prod usarão pagamentos_divida — pagamentos legacy ficam órfãos (apenas no banco, sem nenhum consumer mostrando). Phase 7.10e hipotético cortará writes.
- **PessoasVinculadas** continua recebendo prop `pagamentos` filtrada (do Map). Componente não acessa item individual — sem necessidade de mudança.
- **imprimirFicha** consume só `data_pagamento + valor + observacao` — shape novo compatível direto.
- **calcularDetalheEncargos** (motor L365) consome só data_pagamento+valor — verificado em grep.

## 11. Backlog pós-7.10bcd

- **Phase 7.10.bug** (já registrada em ROADMAP backlog) — Fix typo `[object Object]` em dbInsert "processos_judiciais". Independente. Sem prioridade fixa.
- **Phase 7.10e** (hipotético) — Cortar writes em `pagamentos_parciais` (App.jsx L2537/2558/2574 UI legacy). Quando: pós-smoke 7.10bcd OK + período de observação prod (1-2 semanas) sem regressão.
- **Phase 7.10f** (hipotético) — Schema cleanup: drop tabela `pagamentos_parciais`. Quando: pós-7.10e + período de observação adicional.
- **Phase 7.11+** — outras phases backlog v1.4 (React Router, Múltiplos devedores, Phase 8 PDF Demonstrativo).

**Sub-backlog 7.10bcd (se descoberto durante execução)**:
- 7.10bcd.1: Adapter ProcessosJudiciais pra usar helper agruparPagamentosPorDevedor (refactor DRY pós-fact, baixa prioridade)
- 7.10bcd.2: Auditoria de outros services (filaDevedor.js, etc) que possam ainda escrever em pagamentos_parciais — preparação 7.10e
