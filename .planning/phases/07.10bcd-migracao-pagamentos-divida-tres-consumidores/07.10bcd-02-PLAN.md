---
phase: 07.10bcd-migracao-pagamentos-divida-tres-consumidores
plan: 02
type: execute
wave: 2
depends_on: ["07.10bcd-01"]
autonomous: false
requirements_addressed: []
source: "ROADMAP.md line 361-368 + 07.10bcd-CONTEXT.md §6 (SC1-SC7 UAT MANDATÓRIOS — D-33) + §4 (D-34 atomicidade no ship) + §8 (task 7.10bcd-02) — 2 PAUSAs humanas (UAT 7 SCs + diff consolidado pré-bump). Phase has NO REQ-IDs."

files_modified: []  # Plan 02 não edita código-fonte — só PAUSAs humanas + bump no pai (submódulo já commitado em 3 commits no Plan 01) + commit docs SUMMARY 01 + 02 + STATE.md
files_touched:
  - src/mr-3                                                                                                # bump no pai (git add src/mr-3 no repo pai)
  - .planning/phases/07.10bcd-migracao-pagamentos-divida-tres-consumidores/07.10bcd-01-SUMMARY.md           # commitado neste plan (criado em Plan 01 Task 6, left uncommitted)
  - .planning/phases/07.10bcd-migracao-pagamentos-divida-tres-consumidores/07.10bcd-02-SUMMARY.md           # novo — SUMMARY após UAT + bump
  - .planning/STATE.md                                                                                       # update — phase 7.10bcd SHIPPED

must_haves:
  truths:
    - "UAT SC1-SC7 executado pelo usuário em prod (recomendado — drift de centavo é mais detectável com volumes reais; ou localhost se ambiente prod indisponível) com PASS explícito em TODOS os 7 cenários (D-33 — TODOS MANDATÓRIOS, sem opção de SKIP)"
    - "Drift de centavo em qualquer 1 dos 7 SCs = FAIL phase inteira (D-34 atomicidade — rollback total `git -C src/mr-3 reset --hard 2757f74` no submódulo, exceto bug trivial fixável <5min vira fix-forward dentro da phase)"
    - "Diff consolidado do submódulo aprovado pelo usuário (PAUSA #2) antes do bump pai — esperado 6 files cumulativos no chain (helper NEW + helper test NEW + App.jsx + TabelaDividas + DetalheDivida + package.json) + ModuloDividas zero diff (pass-through preservado) + D-01 motor diff vazio em todos 3 motor files cumulativo desde 2757f74 + D-30 EXATAMENTE 3 commits sequenciais com headers verbatim"
    - "Bump de submódulo no pai criado com mensagem `chore(07.10bcd): bump submodule mr-3 — Devedores + ModuloDividas + Dashboard leem pagamentos_divida`"
    - "SHA-bump no pai === SHA do último commit submódulo (07.10d) aprovado em PAUSA #2 (cross-check verificado em Task 3 `<verify>` automatizado — substitui PAUSA #3 dedicada — pattern 7.10a)"
    - "Push humano executado pelo planner em Task 3 — `git -C src/mr-3 push origin main` (3 commits) seguido de `git push origin master` (bump pai). Vercel auto-build dispara"
    - "Tag `v1.4-phase7.10bcd` NÃO criada nesta phase (out of scope — ação separada pós-smoke prod)"
    - "5 humanos checkpoints total na phase 7.10bcd (D-32): 3 sub-PAUSAs intra-Plan-01 (após cada commit atômico) + 2 PAUSAs Plan 02 (UAT 7 SCs + diff consolidado pré-bump). Plan 02 omite 3ª PAUSA dedicada de bump-review por risk reclassification — cross-check SHA submódulo↔pai automatizado em Task 3 `<verify>` substitui (pattern 7.10a herdado)"
  artifacts:
    - path: ".planning/phases/07.10bcd-migracao-pagamentos-divida-tres-consumidores/07.10bcd-02-SUMMARY.md"
      provides: "Registro UAT SC1-SC7 + SHA-bump submódulo↔pai + resultados das 2 PAUSAs + matriz consolidada de 7 SCs + drift D-28 observação"
      contains: "SC1.*PASS"
    - path: ".planning/phases/07.10bcd-migracao-pagamentos-divida-tres-consumidores/07.10bcd-01-SUMMARY.md"
      provides: "SUMMARY 01 (criado em Plan 01 Task 6, commitado em Plan 02 Task 4 junto com SUMMARY 02 + STATE.md)"
      contains: "D-01 ESTRITO cumulativo"
    - path: ".planning/STATE.md"
      provides: "STATE.md atualizado last_activity + status referenciando Phase 7.10bcd SHIPPED"
      contains: "Phase 7.10bcd SHIPPED"
  key_links:
    - from: "Plan 02 PAUSA #1 (UAT SC1-SC7)"
      to: "Plan 02 Task 2 (diff consolidado)"
      via: "Sem APROVADO em TODOS os 7 SCs, não autoriza PAUSA #2 (D-33 mandatórios sem SKIP) — FAIL = rollback total D-34"
      pattern: "PAUSA #1.*APROVADO"
    - from: "Plan 02 PAUSA #2 (diff consolidado pré-bump)"
      to: "Plan 02 Task 3 (push submódulo + bump pai + push pai)"
      via: "Sem OK diff consolidado submódulo + 3 commits + 6 files + motor vazio cumulativo, não bumpa nem autoriza push"
      pattern: "PAUSA #2.*APROVADO"
    - from: "Plan 02 Task 3 (bump pai + push)"
      to: "Plan 02 Task 4 (SUMMARY 02 + STATE.md SHIPPED + commit docs consolidado)"
      via: "Bump + push precedem commit docs consolidados (SUMMARY 01 + SUMMARY 02 + STATE.md num único commit `docs(07.10bcd)`)"
      pattern: "SHA-novo.*Subproject commit"
    - from: "Plan 02 D-34 atomicidade rollback"
      to: "git -C src/mr-3 reset --hard 2757f74"
      via: "FAIL UAT em qualquer 1 dos 7 SCs (não-explicável por D-28) → rollback total dos 3 commits → phase aborta inteira (não ship parcial)"
      pattern: "git reset --hard 2757f74"
---

<objective>
Executar UAT cross-check dos 7 success criteria (SC1-SC7 — CONTEXT.md §6 — TODOS MANDATÓRIOS, D-33) em prod ou localhost, bumpar submódulo no pai com 3 commits, push humano + Vercel deploy, e documentar SUMMARY 02 + STATE SHIPPED. Os 3 commits do submódulo já foram criados em Plan 01 Tasks 2-4 (`feat(07.10b)` → `feat(07.10c)` → `feat(07.10d)`). Este plan executa **2 PAUSAs humanas sequenciais** antes de qualquer mudança irreversível no pai — comparado a Phase 7.9 (3 PAUSAs), 7.10bcd omite a 3ª PAUSA específica de bump-review por risk reclassification (mesma decisão da 7.10a), mas mantém atomicidade no ship via D-34.

Purpose:
- **PAUSA #1 (UAT SC1-SC7)** impede bug comportamental indo pra prod. **TODOS os 7 SCs MANDATÓRIOS** (D-33 — sem opção de SKIP):
  - SC1 Pré-check pagamentos_divida disponíveis
  - SC2 Devedores MENDES R$ 36.871,64 (drift centavo = FAIL)
  - SC3 ModuloDividas TabelaDividas saldo dívida
  - SC4 DetalheDivida saldo mesma dívida (cross-check com SC3)
  - SC5 Dashboard "Carteira"
  - SC6 Dashboard "Recuperado" + "Em Aberto" + "Taxa"
  - SC7 Dashboard "Recebido Período" — caminho mais complexo (filter dataInicio)
  - **D-34 atomicidade**: FAIL em qualquer 1 dos 7 = rollback total `git -C src/mr-3 reset --hard 2757f74`. Exceção única: bug trivial fixável <5min vira fix-forward.
- **PAUSA #2 (diff consolidado pós-3-commits submódulo)** impede push de submódulo errado. Verifica 6 files cumulativos + ModuloDividas zero diff + D-01 motor vazio cumulativo + D-30 EXATAMENTE 3 commits sequenciais com headers verbatim + Shields 24a/b/c grep. Cross-check de SHA submódulo↔pai consolidado nesta PAUSA via checklist 10 pontos (não em PAUSA dedicada).

**Por que 2 PAUSAs Plan 02 (não 3 como Phase 7.9):** risk reclassification documentada (mesma decisão da 7.10a) — cross-check de SHA bump pai↔submódulo é integrado em Task 3 `<verify>` automatizado ao invés de PAUSA dedicada — checklist 10 pontos cobre todas verificações. Trade-off justificado: cerimônia média ao invés de pesada para escopo cirúrgico (3 commits atômicos com gates já validados em Plan 01 sub-PAUSAs intra-execute). **Total phase 7.10bcd = 5 humanos checkpoints** (3 sub-PAUSAs Plan 01 + 2 PAUSAs Plan 02 — D-32).

Output:
- UAT SC1-SC7 PASS registrado pelo usuário (matriz 7 SCs com PROD/LOCAL valores cross-check)
- Bump de submódulo no pai com commit `chore(07.10bcd): bump submodule mr-3 — Devedores + ModuloDividas + Dashboard leem pagamentos_divida`
- Push do submódulo (3 commits) executado em Task 3 + push do pai
- `07.10bcd-02-SUMMARY.md` documentando UAT 7 SCs + SHAs + 2 PAUSAs + drift D-28 observação
- `07.10bcd-01-SUMMARY.md` (criado em Plan 01 Task 6) commitado junto neste plan
- `.planning/STATE.md` atualizado — Phase 7.10bcd SHIPPED
- Commit docs consolidado: `docs(07.10bcd): PLAN, SUMMARY consolidados + STATE phase 7.10bcd SHIPPED` (após bump + push)

Fora de escopo:
- Tag `v1.4-phase7.10bcd` (ação separada pós-smoke prod — NÃO criada nesta phase)
- Qualquer edit de código-fonte (Plan 01 já executou tudo)
- Smoke test em prod (ação humana pós-push — re-rodar 7 SCs com dados de prod)
- Sub-phase 7.10e — cortar writes em pagamentos_parciais (UI legacy): backlog após período de observação prod (1-2 semanas) sem regressão
- Sub-phase 7.10f — drop tabela pagamentos_parciais: backlog após writes cortados E período observação adicional
- Phase 7.10.bug — fix typo `[object Object]` em dbInsert processos_judiciais (independente)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07.10bcd-migracao-pagamentos-divida-tres-consumidores/07.10bcd-CONTEXT.md
@.planning/phases/07.10bcd-migracao-pagamentos-divida-tres-consumidores/07.10bcd-01-PLAN.md
@.planning/phases/07.10bcd-migracao-pagamentos-divida-tres-consumidores/07.10bcd-01-SUMMARY.md

<!-- Plan 01 criou os 3 commits no submódulo + SUMMARY 01 (left uncommitted no pai); este plan UAT + bumpa no pai após APROVADO + push humano + commita SUMMARY 01 + 02 + STATE juntos. -->
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1 — PAUSA #1: UAT SC1-SC7 cross-check em prod ou localhost (TODOS MANDATÓRIOS — D-33)</name>
  <read_first>
    - `.planning/phases/07.10bcd-migracao-pagamentos-divida-tres-consumidores/07.10bcd-CONTEXT.md` (§6 SC1-SC7 — **verbatim** + §4 D-33 mandatórios + D-34 atomicidade)
    - `.planning/phases/07.10bcd-migracao-pagamentos-divida-tres-consumidores/07.10bcd-01-SUMMARY.md` (confirmar 3 commits criados + Plan 01 gates finais cumulativos verdes + drift histórico D-28 documentado)
  </read_first>
  <what-built>
    Plan 01 Tasks 1-6 criou 3 commits atômicos sequenciais no submódulo (cadeia D-30 bisect-able):
    - **commit 07.10b**: helper agruparPagamentosPorDevedor.js (NEW) + test 2 it() (NEW) + App.jsx L8468 callsite swap (case "devedores") + L2984-2992 Devedores Map usa helper + package.json estende test:regressao. 4 files.
    - **commit 07.10c**: App.jsx L8488 callsite swap (case "dividas") + TabelaDividas.jsx L41-44 buildDevedorObjParaSaldo Set lookup divida_id (D-27) + DetalheDivida.jsx L74 mesmo pattern. 3 files. ModuloDividas pass-through preservado (zero diff).
    - **commit 07.10d**: App.jsx L8467 callsite swap (case "dashboard") + Dashboard L568 helper compartilhado D-31 + L682 lookup intermediário + L696 Set lookup inline. 1 file.

    Passou todos gates Plan 01 cumulativos (Build exit 0 + Regressão 25/25 PASS por commit + cumulativo + D-01 dual gate C1+C2 cumulativo desde 2757f74 + D-30 EXATAMENTE 3 commits + Shields 24a+24b+24c todos OK + ModuloDividas zero diff cumulativo + 6 files cumulativos no chain + 3 sub-PAUSAs #1a/b/c todas APROVADAS pelo usuário).

    O pai AINDA NÃO foi bumpado. O localhost (ou prod via deploy preview se habilitado) deve rodar com o submódulo no SHA do último commit (`feat(07.10d)`).

    **Drift histórico (D-28) — IMPORTANTE pra UAT:** pagamentos novos via UI legacy "Registrar pagamento parcial" (App.jsx L2537+) escrevem em `pagamentos_parciais` e NÃO aparecem em NENHUM consumer pós-7.10bcd (Devedores, ModuloDividas, Dashboard, ProcessosJudiciais já desde 7.10a). Garantir que dados de teste tenham pagamentos via stored proc Phase 7 (visíveis em `pagamentos_divida` — fluxo Contratos), NÃO via UI legacy. Cenário SC1 deixa isto explícito.

    **D-34 atomicidade no ship:** se UAT FAIL em qualquer 1 dos 7 SCs (não-explicável por D-28) → rollback total. Phase aborta inteira; nunca ship parcial 07.10b+c sem 07.10d. Exceção: bug trivial fixável <5min vira fix-forward (caminho regular).
  </what-built>
  <how-to-verify>
    **Preparação:**
    ```bash
    cd src/mr-3/mr-cobrancas
    npm install   # se necessário
    npm run dev   # inicia Vite
    ```
    Abrir `http://localhost:5173` (ou porta exibida), logar.

    **Alternativa (RECOMENDADA pra esta phase)**: testar diretamente em prod (`mrcobrancas.com.br`) usando dados existentes — drift de centavo é MAIS detectável com volumes reais (advogado com 200+ dívidas via fluxo Phase 7 Contratos). Para fast-track 3 consumidores em 1 phase, prod é o ambiente correto.

    **Executar os 7 SCs (copiados VERBATIM da CONTEXT.md §6). Marcar PASS/FAIL em cada um. TODOS MANDATÓRIOS (D-33 — sem opção de SKIP):**

    ---

    **SC1 — Pré-check pagamentos_divida disponíveis** [MANDATÓRIO]
    - Passos: Confirmar que ambiente de teste (prod ou localhost) tem `pagamentos_divida` populado. Via console SQL Supabase OU via UI ModuloContratos → DetalheContrato → seção "Pagamentos Recebidos" deve mostrar ≥1 pagamento.
    - Expectativa: ≥1 pagamento em `pagamentos_divida` registrado via stored proc Phase 7. Se zero → ambiente não qualificado (cadastrar fixture via fluxo Contratos OU testar em prod).
    - Resultado: PASS [ ] / FAIL [ ]   Count pagamentos_divida: ________

    ---

    **SC2 — Devedores: saldo MENDES R$ 36.871,64** [MANDATÓRIO — drift centavo = FAIL]
    - Passos: Lista de Devedores → buscar/encontrar MENDES → coluna "Saldo Atualizado" da linha. PROD vs LOCAL cross-check.
    - Expectativa: **R$ 36.871,64** (já validado em 7.9 com pagamentos_parciais — mesmo valor esperado pós-migração porque motor + dados são equivalentes). Drift de centavo = FAIL.
    - Hipóteses se FAIL (ordem de probabilidade):
      (a) Drift histórico D-28 — devedor MENDES tem pagamento via UI legacy (improvável; MENDES foi validado em 7.9 com fluxo Phase 7 Contratos)
      (b) Bug no helper agruparPagamentosPorDevedor (cobrir com test trivial — pegaria em Plan 01 Task 1 Gate B se assim fosse)
      (c) Bug no Devedores Map L2986 (cobrir Shield 24a — pegaria em Plan 01 Task 2)
    - Resultado: PASS [ ] / FAIL [ ]   Saldo MENDES PROD: R$ ________   LOCAL: R$ ________   Drift: R$ ________ (esperado 0,00)

    ---

    **SC3 — ModuloDividas TabelaDividas: saldo de 1 dívida específica** [MANDATÓRIO]
    - Passos: ModuloDividas → lista de dívidas → identificar 1 dívida específica (preferência: dívida do MENDES pra cross-check com SC2) → coluna "Saldo Atualizado" da linha.
    - Expectativa: PROD vs LOCAL cross-check IDÊNTICO. Drift de centavo = FAIL. Se cross-check com SC2: saldo da dívida deve ser componente lógico do total do devedor.
    - Resultado: PASS [ ] / FAIL [ ]   Dívida selecionada: ________   Saldo PROD: R$ ________   LOCAL: R$ ________   Drift: R$ ________

    ---

    **SC4 — DetalheDivida: saldo mesma dívida do SC3** [MANDATÓRIO — cross-check com SC3]
    - Passos: Clicar na dívida selecionada em SC3 → DetalheDivida → linha "Saldo Atualizado" no Resumo Financeiro card.
    - Expectativa: IDÊNTICO ao SC3 (mesma dívida, mesma fonte de cálculo) E IDÊNTICO entre PROD/LOCAL. Drift de centavo = FAIL. Defesa em profundidade — DetalheDivida tem `devedor?.dividas` optional chaining (vs TabelaDividas direto), bug aqui passaria em SC3 mas falharia em SC4 com edge cases (devedor undefined).
    - Resultado: PASS [ ] / FAIL [ ]   Saldo DetalheDivida PROD: R$ ________   LOCAL: R$ ________   Cross-check SC3: ________

    ---

    **SC5 — Dashboard card "Carteira" (PROD vs LOCAL)** [MANDATÓRIO]
    - **SC5a (Dashboard interno)**: tab Dashboard → card "Total Carteira" (totalCarteira agregado dentro do componente Dashboard L591). Drift = FAIL.
    - **SC5b (Header global)**: card "Carteira" no HEADER GLOBAL (presente em TODAS as tabs — visível mesmo navegando para Devedores/Processos/Contratos). Drift = FAIL crítico (bug visual grave). Origem: parent App L8425-8433 pgtosPorDevedorCarteira → L8434-8440 totalCarteira → L8735 render header card.
    - Ambos devem ser IDÊNTICOS entre prod e local. Se SC5a passa mas SC5b falha = inconsistência de migração; se ambos drift = bug semântico fundamental.
    - Passos: Dashboard (após login) → card "Total Carteira" (totalCarteira agregado L591-610). Anotar valor (SC5a). Depois navegar para outra tab (ex: Devedores) e anotar card "Carteira" do header global (SC5b).
    - Expectativa: PROD vs LOCAL IDÊNTICO em ambos SC5a + SC5b. Drift de centavo = FAIL. Este card usa o helper compartilhado `agruparPagamentosPorDevedor` (commit 07.10d L568 interno + L8425 parent App). Se SC2 PASS mas SC5 FAIL → bug específico do Dashboard agregação L591-610 (anti-dupla-contagem `principaisDividaIds`) ou do parent App L8425 (Fix D-extra).
    - Resultado:
      - SC5a (Dashboard interno): PASS [ ] / FAIL [ ]   Total Carteira PROD: R$ ________   LOCAL: R$ ________   Drift: R$ ________
      - SC5b (Header global): PASS [ ] / FAIL [ ]   Carteira PROD: R$ ________   LOCAL: R$ ________   Drift: R$ ________

    ---

    **SC6 — Dashboard cards "Recuperado" + "Em Aberto" + "Taxa Recuperação"** [MANDATÓRIO]
    - Passos: Dashboard → 3 cards:
      - **Recuperado** (totalRecuperadoGlobal L612-615 — `allPagamentos.reduce((s,p) => s + p.valor)`. ZERO mudança em commit 07.10d — D-29 motor compat. Bug aqui = bug na fonte trocada — se SC1 PASS mas SC6 Recuperado FAIL → fonte allPagamentosDivida vazia ou shape incompatível com p.valor)
      - **Em Aberto** (`totalCarteira - totalRecuperadoGlobal` L618 — composição de SC5 + Recuperado)
      - **Taxa Recuperação** (`totalRecuperadoGlobal / totalCarteira * 100` L617 — composição)
    - Expectativa: PROD vs LOCAL IDÊNTICO em todos 3. Drift em qualquer = FAIL. Pode ser 1 SC consolidado ou 3 separados (planner consolidou).
    - Resultado:
      - Recuperado: PASS [ ] / FAIL [ ]   PROD: R$ ________   LOCAL: R$ ________
      - Em Aberto: PASS [ ] / FAIL [ ]    PROD: R$ ________   LOCAL: R$ ________
      - Taxa: PASS [ ] / FAIL [ ]          PROD: ________%    LOCAL: ________%

    ---

    **SC7 — Dashboard card "Recebido Período" — caminho mais complexo (filter dataInicio)** [MANDATÓRIO — não pode SKIP — D-33]
    - Passos: Dashboard → selecionar período (RECOMENDADO: "30 dias" — dataInicio default; também testar "Tudo" como sanity adicional) → card "Recebido no Período" (recebidoPeriodo L672-675).
    - Filter exercitado (L665-670):
      ```js
      const pagamentosNoPeriodo = useMemo(() =>
        dataInicio
          ? allPagamentos.filter(p => (p.data_pagamento || "") >= dataInicio)
          : allPagamentos,
        [allPagamentos, dataInicio]
      );
      ```
      Este filter é **ZERO mudança** em commit 07.10d (D-29 — só lê `p.data_pagamento` que existe em pagamentos_divida). Bug aqui = shape mismatch crítico (improvável; pagamentos_divida tem data_pagamento DATE NOT NULL).
    - Expectativa: PROD vs LOCAL IDÊNTICO em "30 dias" + "Tudo". Drift de centavo = FAIL. **Não pode SKIP** — caminho mais arriscado de toda phase (envolve filter por data + reduce). Caso edge especial: período "Hoje" (dataInicio == hoje) — opcional como sanity adicional.
    - Resultado:
      - Recebido Período "30 dias": PASS [ ] / FAIL [ ]   PROD: R$ ________   LOCAL: R$ ________
      - Recebido Período "Tudo": PASS [ ] / FAIL [ ]      PROD: R$ ________   LOCAL: R$ ________

    ---

    **Matriz consolidada — preencher TODOS antes de resumir:**
    ```
    SC1  — Pré-check pagamentos_divida disponíveis:   [ ] PASS   [ ] FAIL   Count: ______
    SC2  — Devedores MENDES R$ 36.871,64:             [ ] PASS   [ ] FAIL   Drift: R$ ______ (esperado 0,00)
    SC3  — ModuloDividas TabelaDividas saldo dívida:  [ ] PASS   [ ] FAIL   Drift: R$ ______
    SC4  — DetalheDivida saldo mesma dívida do SC3:   [ ] PASS   [ ] FAIL   Cross-check SC3: ______
    SC5  — Dashboard "Carteira":                       [ ] PASS   [ ] FAIL   Drift: R$ ______
    SC6  — Dashboard "Recuperado" + "Em Aberto" + "Taxa":
            Recuperado:                                [ ] PASS   [ ] FAIL   Drift: R$ ______
            Em Aberto:                                 [ ] PASS   [ ] FAIL   Drift: R$ ______
            Taxa:                                      [ ] PASS   [ ] FAIL   Drift: ______%
    SC7  — Dashboard "Recebido Período" 30 dias:      [ ] PASS   [ ] FAIL   Drift: R$ ______
            Recebido Período "Tudo" (sanity):          [ ] PASS   [ ] FAIL   Drift: R$ ______
    ```

    **Regra de aprovação (D-33):** PASS em **TODOS os 7 SCs** é MANDATÓRIO. Sem opção de SKIP. Drift de centavo = FAIL.

    **Se algum SC = FAIL com drift histórico esperado (D-28)**: re-testar com dados cujos pagamentos sejam 100% via Phase 7 (não via UI legacy). Se confirmado drift apenas em devedores com UI legacy mix → ACEITO por D-28, registrar em SUMMARY como "drift histórico observado — esperado por design D-28, não-bug".

    **D-34 atomicidade — Se ALGUM SC mandatório = FAIL não-explicável por D-28:**
    - Registrar sintoma observado.
    - Decidir entre 2 caminhos:
      **(A) Bug trivial fixável <5min**: vira fix-forward dentro da phase. NÃO é exceção, é caminho regular. Voltar pro Plan 01 (executor pode revisitar Tasks 2-4 em modo gap-closure pra aplicar fix; novo commit 07.10b/c/d.fix); re-rodar SC FAIL pra confirmar.
      **(B) Bug não-trivial OU múltiplos SCs FAIL**: ROLLBACK TOTAL D-34:
      ```bash
      cd src/mr-3 && git reset --hard 2757f74
      ```
      (descarta os 3 commits 07.10b/c/d). Phase aborta inteira. Documentar em SUMMARY 02 + STATE.md como "Phase 7.10bcd ABORTED — D-34 atomicidade — UAT FAIL em SC<x>: <sintoma>". NUNCA ship parcial 07.10b+c sem 07.10d.
    - **NÃO APROVAR este task** em caso de FAIL — responder `FAIL: <SCx>: <sintoma>` ou `FAIL: ROLLBACK D-34: <SCs falhos>`.
    - Voltar pro Plan 01 (`/gsd-execute-phase 07.10bcd --gaps-only`) ou abortar a phase inteira.
  </how-to-verify>
  <resume-signal>
    Digite `APROVADO` (ou `APROVADO com ressalva: <nota>`) SE TODOS os 7 SCs (SC1-SC7) passaram (drift de centavo = FAIL — D-33 mandatórios sem SKIP). Caso contrário, responda:
    - `FAIL: <SCx>: <sintoma>` (1 SC FAIL não-explicável por D-28; usuário decide fix-forward <5min ou rollback)
    - `FAIL: ROLLBACK D-34: <SCs falhos>` (rollback total `git -C src/mr-3 reset --hard 2757f74`)
    - `FAIL: drift D-28: <devedor>` (drift causado por UI legacy — re-testar com dados Phase 7)
    Em caso de FAIL não-D-28, executor aborta SEM bumpar o pai e SEM autorizar push.
  </resume-signal>
  <acceptance_criteria>
    - Usuário respondeu explicitamente `APROVADO` (ou `APROVADO com ressalva`)
    - Matriz SC1-SC7 preenchida com PASS/FAIL em cada item (TODOS MANDATÓRIOS — sem SKIP)
    - SC1 = PASS (pré-check pagamentos_divida disponíveis)
    - SC2 = PASS (Devedores MENDES drift 0,00 esperado)
    - SC3 = PASS (TabelaDividas saldo dívida cross-check)
    - SC4 = PASS (DetalheDivida saldo mesma dívida — cross-check com SC3)
    - SC5 = PASS (Dashboard Carteira)
    - SC6 = PASS em 3 sub-cards (Recuperado + Em Aberto + Taxa)
    - SC7 = PASS em "30 dias" (mandatório); "Tudo" sanity adicional opcional
    - Drift histórico (D-28) registrado se observado: nota explícita "drift observado — esperado por D-28, dados com pagamento via UI legacy"
    - **D-34 atomicidade documentada** se rollback executado: SUMMARY 02 + STATE como "Phase 7.10bcd ABORTED" + comando `git -C src/mr-3 reset --hard 2757f74` registrado
    - Se houver FAIL mandatório não-explicável por D-28, task aborta e o executor NÃO prossegue pro Task 2
  </acceptance_criteria>
  <done>
    Matriz SC1-SC7 preenchida + resposta humana `APROVADO` registrada no output do task. Drift histórico (D-28) observado documentado com nota explícita se aplicável. D-33 mandatórios todos PASS confirmados.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 — PAUSA #2: Diff consolidado submódulo pré-bump (3 commits cumulativos — inclui cross-check SHA submódulo↔pai checklist 10 pontos)</name>
  <read_first>
    - `.planning/phases/07.10bcd-migracao-pagamentos-divida-tres-consumidores/07.10bcd-CONTEXT.md` (§4 D-30..D-34 + §7 Shield greps + §8 commit message convention)
    - `.planning/phases/07.10bcd-migracao-pagamentos-divida-tres-consumidores/07.10bcd-01-SUMMARY.md` (gates evidence + 6 files cumulativos + ModuloDividas zero diff)
  </read_first>
  <what-built>
    Task 1 (PAUSA #1) APROVADA pelo usuário com TODOS os 7 SCs PASS. Ainda não foi feito bump no pai. Os 3 commits do submódulo (Plan 01 Tasks 2-4) existem e podem ser revisados agora cumulativamente.

    **Por que esta PAUSA consolida cross-check de SHA**: Phase 7.9 tinha 3 PAUSAs separadas (UAT + diff submódulo + bump review pai). Phase 7.10bcd consolida em 2 (mesma decisão da 7.10a): a verificação de SHA submódulo↔pai (que em 7.9 era PAUSA #3 dedicada) é incluída no checklist desta PAUSA #2 (item 10) E re-validada por automação em Task 3 `<verify>`. Justificativa: Plan 01 já validou 3 commits em 3 sub-PAUSAs intra-execute (D-32) — escopo cirúrgico permite cerimônia média no Plan 02.
  </what-built>
  <how-to-verify>
    **Executar dentro de `src/mr-3` (submódulo) e no root do pai:**

    **1. Confirmar 3 commits sequenciais no submódulo:**
    ```bash
    cd src/mr-3 && git log 2757f74..HEAD --oneline
    ```
    Expectativa: **EXATAMENTE 3 commits sequenciais** (mais recente primeiro):
    ```
    <SHA-07.10d> feat(07.10d): Dashboard agregados leem pagamentos_divida (helper + Set lookup inline)
    <SHA-07.10c> feat(07.10c): ModuloDividas + sub-componentes leem pagamentos_divida (Set lookup divida_id)
    <SHA-07.10b> feat(07.10b): Devedores lê pagamentos_divida (Map agrupador via helper compartilhado)
    ```
    Anotar `<SHA-07.10d>` (último commit, será o SHA do bump pai). Cross-check com mensagens verbatim CONTEXT.md §2 (Fix B/C/D).

    **2. Stat consolidado (count de arquivos cumulativo desde 2757f74):**
    ```bash
    cd src/mr-3 && git diff 2757f74..HEAD --stat
    ```
    Expectativa: **6 arquivos cumulativos**:
    ```
    mr-cobrancas/package.json                                                       | 2 +-
    mr-cobrancas/src/App.jsx                                                        | <N> +++++/---
    mr-cobrancas/src/components/DetalheDivida.jsx                                   | 4 +++-
    mr-cobrancas/src/components/TabelaDividas.jsx                                   | 4 +++-
    mr-cobrancas/src/services/__tests__/agruparPagamentosPorDevedor.test.js         | <N> +++++ (NEW)
    mr-cobrancas/src/utils/agruparPagamentosPorDevedor.js                           | <N> +++++ (NEW)
    6 files changed, ...
    ```
    `mr-cobrancas/src/components/ModuloDividas.jsx` NÃO deve aparecer (pass-through preservado — commit 07.10c sanity).

    **3. ModuloDividas pass-through preservado (zero diff cumulativo):**
    ```bash
    cd src/mr-3 && git diff 2757f74..HEAD -- mr-cobrancas/src/components/ModuloDividas.jsx | wc -l
    ```
    Expectativa: `0`. Qualquer ≥1 = FAIL — abortar.

    **4. D-01 estrito cumulativo — TODOS 3 motor files VAZIOS desde 2757f74:**
    ```bash
    cd src/mr-3 && git diff 2757f74..HEAD -- \
      mr-cobrancas/src/utils/devedorCalc.js \
      mr-cobrancas/src/services/pagamentos.js \
      mr-cobrancas/src/services/dividas.js
    ```
    Expectativa: ZERO linhas em todos os 3 motor files (D-01 estrito cumulativo mantido). Qualquer linha = FAIL — abortar.

    **5. D-30 EXATAMENTE 3 commits sequenciais com headers verbatim:**
    ```bash
    cd src/mr-3 && git log 2757f74..HEAD --format="%s"
    ```
    Expectativa (3 linhas — ordem cronológica reverse — mais recente primeiro):
    ```
    feat(07.10d): Dashboard agregados leem pagamentos_divida (helper + Set lookup inline)
    feat(07.10c): ModuloDividas + sub-componentes leem pagamentos_divida (Set lookup divida_id)
    feat(07.10b): Devedores lê pagamentos_divida (Map agrupador via helper compartilhado)
    ```
    Headers DEVEM ser verbatim CONTEXT.md §2 (Fix B/C/D).

    **6. Revisar diff completo de cada commit individualmente** (recomendado pra escopo cirúrgico):
    ```bash
    cd src/mr-3 && git show <SHA-07.10b>
    cd src/mr-3 && git show <SHA-07.10c>
    cd src/mr-3 && git show <SHA-07.10d>
    ```
    Verificar que cada commit toca files isolados conforme planning + sem scope creep.

    **7. Shields 24a + 24b + 24c grep verification (re-roda agora pré-bump):**
    ```bash
    # Shield 24a — case "devedores"
    awk '/case "devedores":/{flag=1; n=0} flag{print; n++; if (n>10) flag=0}' src/mr-3/mr-cobrancas/src/App.jsx | grep -F 'allPagamentos='
    # Esperado: 1 match `allPagamentos={allPagamentosDivida}` + 0 matches `allPagamentos={allPagamentos}` legacy

    # Shield 24b — case "dividas" + sub-componentes
    awk '/case "dividas":/{flag=1; n=0} flag{print; n++; if (n>10) flag=0}' src/mr-3/mr-cobrancas/src/App.jsx | grep -F 'allPagamentos='
    grep -nE 'dividaIdsDoDevedor' src/mr-3/mr-cobrancas/src/components/TabelaDividas.jsx src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx
    grep -nE 'p\.devedor_id\s*===\s*.*\.devedor_id' src/mr-3/mr-cobrancas/src/components/TabelaDividas.jsx src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx
    # Esperados: 1 match positivo + 0 negativo / ≥2 dividaIdsDoDevedor em cada arquivo / 0 legacy filter

    # Shield 24c — case "dashboard"
    awk '/case "dashboard":/{flag=1; n=0} flag{print; n++; if (n>10) flag=0}' src/mr-3/mr-cobrancas/src/App.jsx | grep -F 'allPagamentos='
    # Esperado: 1 match `allPagamentos={allPagamentosDivida}` + 0 matches `allPagamentos={allPagamentos}` legacy

    # Helper presente em ≥2 callsites em App.jsx (Devedores Map + Dashboard pgtosPorDevedorCarteira)
    grep -nE 'agruparPagamentosPorDevedor\(devedores' src/mr-3/mr-cobrancas/src/App.jsx
    # Esperado: ≥2 matches
    ```
    Esperado: TODOS os Shields 24a/b/c todos OK final cumulativo + helper ≥2 callsites.

    **8. Helper test file presence + 2 it() blocks PASS:**
    ```bash
    test -f src/mr-3/mr-cobrancas/src/utils/agruparPagamentosPorDevedor.js && echo OK
    test -f src/mr-3/mr-cobrancas/src/services/__tests__/agruparPagamentosPorDevedor.test.js && echo OK
    grep -cE "^  it\(" src/mr-3/mr-cobrancas/src/services/__tests__/agruparPagamentosPorDevedor.test.js
    # Esperado: helper file + test file present, exatamente 2 it() blocks
    ```

    **9. Regressão final 25/25 PASS cumulativo:**
    ```bash
    cd src/mr-3/mr-cobrancas && npm run test:regressao 2>&1 | tail -5
    ```
    Esperado: `Tests 25 passed` (23 base 7.10a + 2 helper test).

    **10. Cross-check SHA submódulo (preparação pra bump pai — Task 3 vai usar este SHA):**
    ```bash
    cd src/mr-3 && git log --format="%H" -1
    ```
    Anotar `<SHA-submódulo>` completo (este é o SHA de `feat(07.10d)` — último commit, ponteiro do submódulo no momento do bump). Em Task 3 (bump pai), o `+Subproject commit` no diff do pai deve bater com este SHA.

    **Pontos de revisão humana (checklist 10 pontos):**
    1. [ ] **EXATAMENTE 3 commits sequenciais no submódulo** com headers verbatim CONTEXT.md §2 (Fix B/C/D): feat(07.10b) → feat(07.10c) → feat(07.10d)
    2. [ ] **6 arquivos cumulativos modificados** (sem surpresas — nada fora de escopo): App.jsx + TabelaDividas + DetalheDivida + package.json + helper NEW + helper test NEW
    3. [ ] **ModuloDividas pass-through preservado** (zero diff cumulativo)
    4. [ ] **D-01 estrito ZERO touch cumulativo** em `pagamentos.js`, `dividas.js`, `devedorCalc.js` desde 2757f74 (todos 3 motor files intocados — git diff vazio)
    5. [ ] **App.jsx 3 callsites swap aplicados** corretamente: case "devedores" L8468 (em commit 07.10b) + case "dividas" L8488 (em commit 07.10c) + case "dashboard" L8467 (em commit 07.10d). Outros consumers (case "contratos" / case "processos") intactos
    6. [ ] **Helper agruparPagamentosPorDevedor.js (NEW)** + **test 2 it() (NEW)**: signature verbatim CONTEXT §2 Fix A; 2 it() blocks (devedor com pagamentos múltiplos + boundary 0 dívidas)
    7. [ ] **Devedores Map L2986 + Dashboard L568 usam helper** (≥2 callsites de `agruparPagamentosPorDevedor(devedores` em App.jsx)
    8. [ ] **Set lookup pattern D-27 replicado em 4 ocorrências** (expandido de 3 para 4 callsites incluindo Dashboard L682 recuperadoPeriodo — não muda gates, apenas docs): TabelaDividas L41-44 + DetalheDivida L74 + Dashboard L696 (ultimosPagamentos) + Dashboard L682 (recuperadoPeriodo). Pattern legacy `p.devedor_id === ...devedor_id` ausente em ambos sub-componentes
    8b. [ ] **Map agrupador 4 callsites cobertos** (lição feedback_grep_global_before_pointwise_fix.md aplicada pre-execute): L568 Dashboard interno (helper) + L2986 Devedores (helper) + L8425 parent App pgtosPorDevedorCarteira (helper Fix D-extra) + L8427 forEach interno do Map L8425 (eliminado pelo helper). Shield 24d granular cobre o 4º callsite parent App.
    9. [ ] **Shields 24a + 24b + 24c todos OK** + **package.json estende test:regressao** com agruparPagamentosPorDevedor.test.js no FIM
    10. [ ] **SHA submódulo `<SHA-07.10d>` anotado** pra cross-check com bump pai em Task 3 (validação automatizada substitui PAUSA #3 dedicada)

    **Regressão 25/25 PASS** confirmada (item 9 deste checklist + item 8 cobrem evidência).
  </how-to-verify>
  <resume-signal>
    Digite `APROVADO` SE checklist 10 pontos todos OK + SHA submódulo `<SHA-07.10d>` anotado + regressão 25/25 final cumulativo. Caso contrário, responda `FAIL: <motivo>` (ex: `FAIL: arquivo X fora de escopo`, `FAIL: devedorCalc.js modificado em commit 07.10c`, `FAIL: Shield 24b legacy ainda presente em DetalheDivida`, `FAIL: ModuloDividas tem diff inesperado`). Em caso de FAIL, executor aborta SEM bumpar o pai; usuário pode fazer `git -C src/mr-3 reset --hard 2757f74` (rollback total D-34 hard — descarta os 3 commits 07.10bcd, preserva 7.10a shipped) no submódulo pra corrigir.

    **NOTA**: Use `--hard` NÃO `--soft` em D-34 atomicidade — descarta working tree completamente. Se quiser preservar mudanças pra inspeção, fazer `git stash` antes do reset.
  </resume-signal>
  <acceptance_criteria>
    - Usuário respondeu `APROVADO` explicitamente
    - SHA do último commit submódulo `<SHA-07.10d>` anotado pra uso em Task 3 e SUMMARY 02
    - Contagem de arquivos cumulativos = **6** (App.jsx + TabelaDividas + DetalheDivida + package.json + helper NEW + helper test NEW); ModuloDividas zero diff
    - `git diff 2757f74..HEAD` em arquivos do motor (devedorCalc.js / pagamentos.js / dividas.js) = saída vazia em todos 3 (D-01 estrito cumulativo)
    - **EXATAMENTE 3 commits sequenciais** com headers verbatim `feat(07.10b)`, `feat(07.10c)`, `feat(07.10d)` (D-30 atomicidade)
    - Shields 24a + 24b + 24c todos OK (positivo + negativo regional + filter pattern + helper grep)
    - Helper file + helper test file existem; 2 it() blocks
    - Regressão 25/25 PASS cumulativo final
    - Checklist 10 pontos todos OK
    - Se houver FAIL, executor NÃO prossegue pro Task 3
  </acceptance_criteria>
  <done>
    Diff consolidado dos 3 commits aprovado pelo usuário + SHA do último commit submódulo (`<SHA-07.10d>`) registrado + checklist 10 pontos verificado. Cross-check de SHA submódulo já validado nesta PAUSA (consolidado vs Phase 7.9 PAUSA #3 dedicada — pattern 7.10a herdado).
  </done>
</task>

<task type="auto">
  <name>Task 3 — Push submódulo (3 commits) + bump submódulo no pai (chore(07.10bcd)) + push pai (Vercel deploy) + cross-check SHA</name>
  <read_first>
    - `.planning/phases/07.10bcd-migracao-pagamentos-divida-tres-consumidores/07.10bcd-CONTEXT.md` (§4 + §8 commit convention pai)
    - SHA submódulo anotado em Task 2 (`<SHA-07.10d>`)
  </read_first>
  <files>src/mr-3 (ponteiro de submódulo — staged no repo pai)</files>
  <action>
    Após APROVADO em Task 1 (UAT 7 SCs) + Task 2 (diff consolidado + SHA anotado), executar push do submódulo + bump no pai + push pai (Vercel deploy auto-dispara).

    **NOTA importante:** Diferente da Phase 7.10a (que deixa push como ação humana pós Plan 02), Phase 7.10bcd executa push em Task 3 (auto) — pattern de cerimônia média confirmado pelo usuário no spec. Cross-check SHA submódulo↔pai automatizado substitui PAUSA #3 dedicada.

    **1. Verificar estado do pai PRÉ-bump:**
    ```bash
    git status
    ```
    Expectativa: apenas `modified: src/mr-3 (new commits)` listado (mais possivelmente `?? .planning/phases/07.10bcd-.../07.10bcd-01-SUMMARY.md` se Plan 01 Task 6 deixou uncommitted — esperado, será tratado em Task 4). Nenhum outro arquivo do pai deve estar modificado.

    **2. Anotar SHA submódulo PRÉ-push (cross-check 1/2):**
    ```bash
    git -C src/mr-3 log --format="%H" -1
    ```
    Anotar `<SHA-submódulo-pré>` completo (esperado === SHA `<SHA-07.10d>` aprovado em PAUSA #2). Se diferente → ABORT (alguém commitou algo no submódulo entre Task 2 e Task 3 — investigar).

    **3. Push submódulo (3 commits sequenciais):**
    ```bash
    cd src/mr-3 && git push origin main
    ```
    Expectativa: 3 commits empurrados (07.10b → 07.10c → 07.10d). Se push rejeitado (auth ou conflito remote): ABORT, reportar erro pra usuário decidir.

    **4. Adicionar o submódulo ao index do pai:**
    ```bash
    git add src/mr-3
    ```
    **IMPORTANTE**: `git add src/mr-3` (SEM trailing slash). Isso registra o novo SHA do submódulo no pai — NÃO mexe no working tree do submódulo.

    **5. Commit no pai com mensagem EXATA:**
    ```bash
    git commit -m "chore(07.10bcd): bump submodule mr-3 — Devedores + ModuloDividas + Dashboard leem pagamentos_divida"
    ```

    **6. Confirmar bump + SHA cross-check (2/2):**
    ```bash
    git log --oneline -1
    git diff HEAD~1 HEAD -- src/mr-3
    ```
    Esperado:
    - Último commit do pai = `<SHA-pai-bump> chore(07.10bcd): bump submodule mr-3 — Devedores + ModuloDividas + Dashboard leem pagamentos_divida`
    - Diff do src/mr-3 mostra `-Subproject commit <SHA-velho>` (`2757f74` pre-bump) e `+Subproject commit <SHA-novo>` onde `<SHA-novo>` === `<SHA-submódulo-pré>` anotado em passo 2 (=== `<SHA-07.10d>`).

    **7. Cross-check explícito automatizado (substitui PAUSA #3 dedicada — pattern 7.10a):**
    ```bash
    SHA_SUB=$(git -C src/mr-3 log --format="%H" -1)
    SHA_PAI_SUBPROJECT=$(git diff HEAD~1 HEAD -- src/mr-3 | grep "^+Subproject commit " | awk '{print $3}')
    [ "$SHA_SUB" = "$SHA_PAI_SUBPROJECT" ] && echo "Cross-check SHA: OK ($SHA_SUB === $SHA_PAI_SUBPROJECT)" || echo "Cross-check SHA: FAIL ($SHA_SUB !== $SHA_PAI_SUBPROJECT — bump apontou pra commit errado)"
    ```
    Esperado: `OK`. Se `FAIL`: ABORT, `git -C src/mr-3 reset --hard 2757f74` (rollback total D-34 — descarta 3 commits 7.10bcd, preserva 7.10a shipped). Use `--hard` NÃO `--soft` em D-34 atomicidade — descarta working tree completamente; se quiser preservar mudanças pra inspeção, fazer `git stash` antes. Investigar causa (raro — possível causa: novo commit no submódulo entre Task 2 e Task 3, mas push do passo 3 deveria ter capturado o SHA anotado).

    **8. Push pai (Vercel deploy auto-dispara):**
    ```bash
    git push origin master
    ```
    Expectativa: bump pai empurrado. Vercel auto-build dispara em ~1-2min. Smoke test em prod é ação humana pós-task (re-rodar 7 SCs com dados de prod).

    **NÃO criar tag `v1.4-phase7.10bcd` aqui.** Tag é separada pós-smoke prod (ação humana).
  </action>
  <verify>
    <automated>git log -1 --oneline | grep -qE "chore\(07\.10bcd\): bump submodule mr-3" &amp;&amp; SHA_SUB=$(git -C src/mr-3 log --format="%H" -1) &amp;&amp; SHA_PAI=$(git diff HEAD~1 HEAD -- src/mr-3 | grep "^+Subproject commit " | awk '{print $3}') &amp;&amp; [ "$SHA_SUB" = "$SHA_PAI" ] &amp;&amp; echo "OK: bump + push + cross-check SHA"</automated>
  </verify>
  <acceptance_criteria>
    - **Push submódulo executado**: `cd src/mr-3 && git push origin main` exit 0 (3 commits empurrados pra origin/main)
    - **Bump pai criado**: `git log -1 --oneline` output matches `chore(07.10bcd): bump submodule mr-3 — Devedores + ModuloDividas + Dashboard leem pagamentos_divida`
    - **Subproject commit cross-check**: `git diff HEAD~1 HEAD -- src/mr-3` shows `-Subproject commit <2757f74>` and `+Subproject commit <SHA-07.10d>` only
    - **Cross-check SHA automatizado PASS**: `<SHA-submódulo>` (de `git -C src/mr-3 log --format="%H" -1`) === `<SHA-novo>` (do `+Subproject commit` no diff do pai). Validação automatizada no `<verify>` cobre.
    - **Push pai executado**: `git push origin master` exit 0 (bump pai empurrado pra origin/master). Vercel deploy auto-dispara
    - `git status` após push está clean (exceto SUMMARYs pendentes — serão tratados em Task 4)
    - ZERO criação de tag `v1.4-phase7.10bcd`
  </acceptance_criteria>
  <done>
    3 commits do submódulo empurrados pra origin/main; bump pai criado com mensagem `chore(07.10bcd)`; um único commit de bump no pai; cross-check SHA submódulo↔pai automatizado PASS; push pai executado (Vercel auto-build disparado); working tree clean (exceto SUMMARYs pendentes); tag NÃO criada (out of scope).
  </done>
</task>

<task type="auto">
  <name>Task 4 — Criar 07.10bcd-02-SUMMARY.md + atualizar STATE.md (Phase 7.10bcd SHIPPED) + commit docs consolidado (SUMMARY 01 + 02 + STATE)</name>
  <read_first>
    - `.planning/phases/07.10bcd-migracao-pagamentos-divida-tres-consumidores/07.10bcd-CONTEXT.md` (§5 SC + §8 commit conventions + §11 backlog)
    - `.planning/phases/07.10bcd-migracao-pagamentos-divida-tres-consumidores/07.10bcd-01-SUMMARY.md` (já existe — left uncommitted no pai pelo Plan 01 Task 6)
    - Outputs das Tasks 1-3 (matriz SC1-SC7, SHAs submódulo + pai, cross-check)
  </read_first>
  <files>
    - .planning/phases/07.10bcd-migracao-pagamentos-divida-tres-consumidores/07.10bcd-02-SUMMARY.md (NOVO)
    - .planning/phases/07.10bcd-migracao-pagamentos-divida-tres-consumidores/07.10bcd-01-SUMMARY.md (commit junto — criado em Plan 01 Task 6)
    - .planning/STATE.md (update — Phase 7.10bcd SHIPPED)
  </files>
  <action>
    **Parte A — Criar `.planning/phases/07.10bcd-migracao-pagamentos-divida-tres-consumidores/07.10bcd-02-SUMMARY.md`:**

    ```markdown
    # Phase 7.10bcd — Plan 02 SUMMARY — UAT 7 SCs + push + bump pai (Devedores + ModuloDividas + Dashboard leem pagamentos_divida)

    **Status**: Pushed + bumped + smoke prod pendente
    **Data**: <YYYY-MM-DD>

    ## Tasks executados

    ### Task 1 — PAUSA #1 UAT SC1-SC7 (TODOS MANDATÓRIOS — D-33)

    <matriz SC1-SC7 PASS/FAIL preenchida pelo usuário — TODOS mandatórios sem SKIP>

    ```
    SC1  — Pré-check pagamentos_divida disponíveis:   [ ] PASS   [ ] FAIL   Count: ______
    SC2  — Devedores MENDES R$ 36.871,64:             [ ] PASS   [ ] FAIL   PROD: R$ ______   LOCAL: R$ ______   Drift: R$ ______
    SC3  — ModuloDividas TabelaDividas saldo dívida:  [ ] PASS   [ ] FAIL   Dívida: ______   PROD: R$ ______   LOCAL: R$ ______
    SC4  — DetalheDivida saldo mesma dívida do SC3:   [ ] PASS   [ ] FAIL   PROD: R$ ______   LOCAL: R$ ______   Cross-check SC3: ______
    SC5  — Dashboard "Carteira":                       [ ] PASS   [ ] FAIL   PROD: R$ ______   LOCAL: R$ ______
    SC6  — Dashboard 3 cards:
            Recuperado:                                [ ] PASS   [ ] FAIL   PROD: R$ ______   LOCAL: R$ ______
            Em Aberto:                                 [ ] PASS   [ ] FAIL   PROD: R$ ______   LOCAL: R$ ______
            Taxa:                                      [ ] PASS   [ ] FAIL   PROD: ______%    LOCAL: ______%
    SC7  — Dashboard "Recebido Período":
            "30 dias":                                 [ ] PASS   [ ] FAIL   PROD: R$ ______   LOCAL: R$ ______
            "Tudo" (sanity):                           [ ] PASS   [ ] FAIL   PROD: R$ ______   LOCAL: R$ ______
    ```

    Drift histórico (D-28) observado: <SIM/NÃO + descrição>

    Resultado: APROVADO | APROVADO com ressalva | FAIL fix-forward <SCx> | FAIL ROLLBACK D-34 <SCs>

    ### Task 2 — PAUSA #2 Diff consolidado submódulo pré-bump (3 commits cumulativos)

    - **3 commits sequenciais** (D-30 atomicidade — bisect-able):
      | # | SHA | Mensagem header |
      |---|-----|-----------------|
      | 1 | `<sha7-07.10b>` | feat(07.10b): Devedores lê pagamentos_divida (Map agrupador via helper compartilhado) |
      | 2 | `<sha7-07.10c>` | feat(07.10c): ModuloDividas + sub-componentes leem pagamentos_divida (Set lookup divida_id) |
      | 3 | `<sha7-07.10d>` | feat(07.10d): Dashboard agregados leem pagamentos_divida (helper + Set lookup inline) |
    - **6 arquivos cumulativos modificados**: helper NEW + helper test NEW + App.jsx + TabelaDividas + DetalheDivida + package.json
    - **ModuloDividas pass-through preservado** (zero diff cumulativo desde 2757f74)
    - **D-01 estrito cumulativo**: motor diff VAZIO em todos 3 motor files (devedorCalc.js + pagamentos.js + dividas.js) cumulativo desde 2757f74
    - **Shields 24a + 24b + 24c** todos OK (positivo + negativo regional + filter pattern + helper grep)
    - **Helper agruparPagamentosPorDevedor.js + test 2 it()** existem; 2 it() blocks PASS
    - **Regressão 25/25 PASS final cumulativo** (23 base 7.10a + 2 helper test)
    - **Checklist 10 pontos**: TODOS OK

    Resultado: APROVADO

    ### Task 3 — Push submódulo + bump no pai + push pai + cross-check SHA

    - **Push submódulo**: 3 commits empurrados pra origin/main (07.10b → 07.10c → 07.10d)
    - **SHA commit pai (bump)**: `<SHA-pai-bump>`
    - **Mensagem bump**: `chore(07.10bcd): bump submodule mr-3 — Devedores + ModuloDividas + Dashboard leem pagamentos_divida`
    - **Cross-check automatizado**: SHA submódulo (`<sha7-07.10d>`) === `+Subproject commit` no pai → **OK**
    - **Push pai**: bump empurrado pra origin/master. Vercel auto-build disparado.

    ## SHAs registrados

    | Escopo | SHA | Mensagem |
    |--------|-----|----------|
    | Submódulo mr-3 (commit 1/3) | `<sha7-07.10b>` | feat(07.10b): Devedores lê pagamentos_divida (Map agrupador via helper compartilhado) |
    | Submódulo mr-3 (commit 2/3) | `<sha7-07.10c>` | feat(07.10c): ModuloDividas + sub-componentes leem pagamentos_divida (Set lookup divida_id) |
    | Submódulo mr-3 (commit 3/3) | `<sha7-07.10d>` | feat(07.10d): Dashboard agregados leem pagamentos_divida (helper + Set lookup inline) |
    | Pai (root, bump) | `<sha7-pai-bump>` | chore(07.10bcd): bump submodule mr-3 — Devedores + ModuloDividas + Dashboard leem pagamentos_divida |
    | Pai (root, docs) | `<sha7-pai-docs>` | docs(07.10bcd): PLAN, SUMMARY consolidados + STATE phase 7.10bcd SHIPPED |

    Baseline pré-7.10bcd: `2757f74` (post-7.10a HEAD).

    ## Push

    - Push do submódulo: **executado em Task 3** (3 commits empurrados pra origin/main)
    - Push do pai: **executado em Task 3** (bump empurrado pra origin/master)
    - Vercel auto-build: disparado pelo push do pai

    ## 5 humanos checkpoints (D-32 — proporcional ao risco fast-track)

    Total na phase 7.10bcd:
    - **Plan 01** (intra-execute): 3 sub-PAUSAs após cada commit atômico — #1a (pós 07.10b) + #1b (pós 07.10c) + #1c (pós 07.10d)
    - **Plan 02**: 2 PAUSAs sequenciais — #1 (UAT 7 SCs cross-check prod vs local) + #2 (diff consolidado pré-bump checklist 10 pontos)

    ## Por que 2 PAUSAs em Plan 02 (não 3 como Phase 7.9)

    Phase 7.9 tinha 3 PAUSAs separadas (UAT + diff submódulo + bump review pai). Phase 7.10bcd Plan 02 consolida em 2 PAUSAs (mesma decisão da 7.10a):
    - PAUSA #1 — UAT SC1-SC7 cross-check (TODOS mandatórios — D-33)
    - PAUSA #2 — Diff consolidado submódulo (3 commits) + cross-check SHA submódulo↔pai (consolidado em checklist 10 pontos)

    Risk reclassification documentada (mesma decisão da 7.10a — herdada): Plan 01 já validou 3 commits em 3 sub-PAUSAs intra-execute (D-32 — defesa contra empilhamento de regressão silenciosa); Plan 02 tem cerimônia média (2 PAUSAs) ao invés de pesada. Cross-check SHA submódulo↔pai automatizado em Task 3 `<verify>` substitui PAUSA dedicada de bump-review. Trade-off justificado: 3 sub-PAUSAs Plan 01 + 2 PAUSAs Plan 02 = 5 humanos checkpoints proporcional ao risco fast-track 3 consumidores em 1 phase.

    ## Drift histórico (D-28 herdado)

    Observado durante UAT 7 SCs: <SIM/NÃO + descrição>.

    Esperado por design — pagamentos novos via UI legacy "Registrar pagamento parcial" (App.jsx L2537+) escrevem em `pagamentos_parciais` e NÃO aparecem em NENHUM consumer pós-7.10bcd (Devedores via 07.10b + ModuloDividas via 07.10c + Dashboard via 07.10d + ProcessosJudiciais já desde 7.10a + ModuloContratos desde 7.3 + DetalheContrato desde 7.8.2a).

    **Pós-7.10bcd:** TODOS os consumidores em prod usam pagamentos_divida; pagamentos via UI legacy ficam órfãos no banco sem nenhum consumer mostrando. Cleanup é phase própria (sub-backlog 7.10e hipotético — pós-período observação prod 1-2 semanas).

    ## D-30..D-34 invariantes verificadas (verify final)

    - **D-30** (3 commits atômicos bisect-able): VERIFIED — cadeia 3 commits sequenciais com headers verbatim CONTEXT.md §2; bisect funcional confirmado
    - **D-31** (helper compartilhado): VERIFIED — `agruparPagamentosPorDevedor.js` em ≥2 callsites em App.jsx (Devedores Map L2986 + Dashboard L568); test trivial 2 it() PASS
    - **D-32** (5 humanos checkpoints): VERIFIED — total 5 (3 sub-PAUSAs Plan 01 + 2 PAUSAs Plan 02)
    - **D-33** (UAT 7 SCs MANDATÓRIOS): VERIFIED via Task 1 — TODOS PASS confirmado pelo usuário (sem SKIP)
    - **D-34** (atomicidade no ship): VERIFIED — UAT PASS → ship completo; rollback path documentado mas não exercitado nesta phase (FAIL não ocorreu)

    ## Fora de escopo (diferido — backlog)

    - Tag `v1.4-phase7.10bcd`: NÃO criada — ação separada pós-smoke prod
    - Smoke test prod (mrcobrancas.com.br): ação humana pós-push — re-rodar 7 SCs com dados de prod
    - Sub-phase 7.10e (hipotético) — cortar writes em pagamentos_parciais (UI legacy): backlog após período de observação prod sem regressão (1-2 semanas)
    - Sub-phase 7.10f (hipotético) — drop tabela pagamentos_parciais: backlog após writes cortados E período observação adicional
    - Sub-phase 7.10.bug — fix typo `[object Object]` em dbInsert processos_judiciais (independente, sem prioridade fixa)
    - Sub-phase 7.11+ — outras phases backlog v1.4 (React Router, Múltiplos devedores, Phase 8 PDF Demonstrativo)
    - Sub-backlog 7.10bcd.1 (se descoberto): Adapter ProcessosJudiciais pra usar helper agruparPagamentosPorDevedor (refactor DRY pós-fact, baixa prioridade)
    - Sub-backlog 7.10bcd.2 (se descoberto): Auditoria de outros services (filaDevedor.js etc) que possam ainda escrever em pagamentos_parciais — preparação 7.10e

    ## Próximos passos

    ### 1. Smoke prod (mrcobrancas.com.br) pós-Vercel build (~1-2min)

    Re-validar 7 SCs em prod (mesmo roteiro de PAUSA #1 mas em mrcobrancas.com.br). Foco em:

    - **SC2 prod**: Devedores → MENDES saldo R$ 36.871,64 (mesmo valor de 7.9 ship)
    - **SC3+SC4 prod**: ModuloDividas TabelaDividas + DetalheDivida (1 dívida — mesma do UAT local)
    - **SC5a prod**: Dashboard tab → card "Total Carteira" interno
    - **SC5b prod**: Header GLOBAL → card "Carteira" (re-validar em 2-3 tabs distintas pra confirmar consistência cross-tab)
    - **SC6 prod**: Dashboard cards "Recuperado" + "Em Aberto" + "Taxa Recuperação"
    - **SC7 prod**: Dashboard "Recebido Período" (selecionar período "30 dias")

    Drift de centavo em qualquer = bug em prod. Investigar via DevTools console + git log + comparar com state local.

    ### 2. Tag v1.4-phase7.10bcd (após smoke prod OK)

    Ação humana separada: `git tag v1.4-phase7.10bcd <SHA pai novo>` + `git push origin v1.4-phase7.10bcd`. NÃO criada automaticamente — depende de smoke prod PASS explícito.

    ### 3-5. Continuação backlog

    3. Se regressão descoberta pós-smoke: abrir sub-phase 7.10bcd.x apropriada (preferência: usar bisect cumulativo dos 3 commits — cada um é bisect-able por D-30)
    4. **Período de observação prod (1-2 semanas)** — monitorar pra garantir que TODOS consumidores estão alinhados sem regressão
    5. Pós-observação OK: avaliar inserção Phase 7.10e (cortar writes pagamentos_parciais UI legacy)
    6. Pós-7.10e: avaliar inserção Phase 7.10f (drop tabela pagamentos_parciais)
    ```

    **Parte B — Atualizar `.planning/STATE.md`** (incrementar progress + status):

    Editar `last_activity` + `status` no frontmatter (substituir conteúdo atual referente a 7.10a/7.10bcd planning):
    ```yaml
    last_activity: <YYYY-MM-DD> — Phase 7.10bcd SHIPPED — Devedores + ModuloDividas + Dashboard leem pagamentos_divida (3 commits atômicos sequenciais bisect-able no submódulo: feat(07.10b) → feat(07.10c) → feat(07.10d); helper compartilhado agruparPagamentosPorDevedor.js + test 2 it() criados; Set lookup pattern D-27 replicado em 4 ocorrências; ModuloDividas pass-through preservado zero diff). Submódulo 3 commits empurrados (origin/main). Pai bump <SHA-pai-bump> empurrado (origin/master) — Vercel auto-build disparado. Regressão 25/25 PASS cumulativo (23 base 7.10a + 2 helper). Motor Art.354 100% intocado cumulativo (D-01 estrito desde 2757f74 — gates C1+C2 verbose). UAT 7 SCs MANDATÓRIOS PASS humano (D-33 — Devedores MENDES + ModuloDividas TabelaDividas + DetalheDivida + Dashboard Carteira/Recuperado/Em Aberto/Taxa/Recebido Período). 5 humanos checkpoints (3 sub-PAUSAs Plan 01 intra-execute + 2 PAUSAs Plan 02 — D-32). Estratégia fast-track aceita pelo usuário com 6 mitigations não-negociáveis (M1-M6). Drift histórico D-28 CONTINUA aceito por design — pós-7.10bcd TODOS consumidores em prod usam pagamentos_divida; pagamentos via UI legacy ficam órfãos. Smoke prod + tag v1.4-phase7.10bcd separada (ação humana). Backlog: 7.10e hipotético (cortar writes legacy) + 7.10f hipotético (drop tabela) + 7.10.bug (independente).
    status: Phase 7.10bcd SHIPPED — Devedores + ModuloDividas + Dashboard leem pagamentos_divida (3 commits atômicos no submódulo + bump pai pushed; Vercel auto-build disparado; smoke prod humano pendente). Tag v1.4-phase7.10bcd NÃO criada (pós-smoke prod).
    ```

    **Parte C — Commit docs consolidado no pai (SUMMARY 01 + 02 + STATE):**

    ```bash
    git add \
      .planning/phases/07.10bcd-migracao-pagamentos-divida-tres-consumidores/07.10bcd-01-SUMMARY.md \
      .planning/phases/07.10bcd-migracao-pagamentos-divida-tres-consumidores/07.10bcd-02-SUMMARY.md \
      .planning/STATE.md

    git commit -m "$(cat <<'EOF'
docs(07.10bcd): PLAN, SUMMARY consolidados + STATE phase 7.10bcd SHIPPED

- 07.10bcd-01-SUMMARY.md (criado em Plan 01 Task 6, commitado agora junto)
  - Cadeia 3 commits atômicos sequenciais (07.10b → 07.10c → 07.10d) D-30
  - Helper agruparPagamentosPorDevedor + test 2 it() D-31
  - Set lookup D-27 replicado em 4 ocorrências
  - ModuloDividas pass-through preservado (zero diff cumulativo)
  - D-01 estrito cumulativo (motor 100% intocado desde 2757f74)
- 07.10bcd-02-SUMMARY.md (UAT 7 SCs MANDATÓRIOS APROVADOS + bump pai +
  push + 2 PAUSAs APROVADAS — D-33 + D-34 atomicidade verified)
- STATE.md atualizado — Phase 7.10bcd SHIPPED

Estratégia fast-track 3 consumidores em 1 phase aceita pelo usuário com
6 mitigations não-negociáveis (M1-M6). 5 humanos checkpoints
proporcionais ao risco (D-32). Drift histórico D-28 herdado CONTINUA
aceito por design — pós-7.10bcd, TODOS consumidores em prod usam
pagamentos_divida; UI legacy escreve órfãos no banco sem consumer.

Smoke prod + tag v1.4-phase7.10bcd separada (ação humana). Backlog:
7.10e hipotético (cortar writes legacy) + 7.10f hipotético (drop
tabela pagamentos_parciais) + 7.10.bug (independente).
EOF
)"
    ```

    *Nota*: o commit de docs é separado do commit de bump (que veio em Task 3) — mantém a history limpa. Convenção D-15 7.9 herdada (pattern 7.10a também usado).

    **NÃO incluir `git push` aqui.** Push humano é ação separada (usuário pode `git push origin master` quando quiser empurrar os docs também — separar push de bump (Task 3) e push de docs (esta task) permite o usuário fazer um push só dos docs).

    **NÃO criar tag `v1.4-phase7.10bcd`.** Tag é ação separada pós-smoke prod.

    **NOTA importante:** Esta task NÃO empurra o commit de docs (apenas commit local). O usuário decide quando fazer `git push origin master` pra empurrar os docs (geralmente junto de um próximo commit, ou separado pós-smoke). Isso difere da Task 3 (bump + push imediato pra Vercel disparar build) — docs commit não precisa de Vercel build.
  </action>
  <verify>
    <automated>test -f .planning/phases/07.10bcd-migracao-pagamentos-divida-tres-consumidores/07.10bcd-02-SUMMARY.md &amp;&amp; git log -1 --oneline | grep -qE "docs\(07\.10bcd\)" &amp;&amp; grep -q "Phase 7.10bcd SHIPPED" .planning/STATE.md &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/07.10bcd-migracao-pagamentos-divida-tres-consumidores/07.10bcd-02-SUMMARY.md` exists
    - SUMMARY 02 contém matriz SC1-SC7 + SHAs (3 submódulo + 2 pai bump+docs) + registro das 2 PAUSAs + lista 6 arquivos cumulativos + ModuloDividas pass-through + drift histórico D-28 + D-30..D-34 invariantes verified + próximos passos (smoke prod + tag separada + backlog 7.10e/f/bug)
    - SUMMARY 01 (criado em Plan 01 Task 6) commitado junto neste task
    - `.planning/STATE.md` atualizado (`last_activity` + `status` referenciando Phase 7.10bcd SHIPPED com detalhes completos: 3 commits + helper + Set lookup + Motor intocado + 5 checkpoints + drift D-28 + smoke pendente)
    - `git log -1 --oneline` shows `docs(07.10bcd): PLAN, SUMMARY consolidados + STATE phase 7.10bcd SHIPPED`
    - 3 files no commit docs: SUMMARY 01 + SUMMARY 02 + STATE.md
    - ZERO criação de tag `v1.4-phase7.10bcd`
    - Push do commit docs é deixado pra ação humana subsequente (não executado nesta task)
  </acceptance_criteria>
  <done>
    SUMMARY 02 criado + SUMMARY 01 commitado junto + STATE.md atualizado (Phase 7.10bcd SHIPPED) + commit docs consolidado aplicado no pai. Phase 07.10bcd encerra em estado "3 commits no submódulo pushed + bump pai pushed (Vercel build disparado) + docs commitados localmente (push humano subsequente) + smoke prod pendente + tag v1.4-phase7.10bcd separada".
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (Plan 02 — workflow correctness + ship atomicity)

| Boundary | Description |
|----------|-------------|
| Plan 01 → Plan 02 | 3 commits no submódulo já existem; Plan 02 herda estado verde validado em 3 sub-PAUSAs intra-Plan-01. Se Plan 01 falhou gates finais cumulativos (Task 5), Plan 02 não deveria ter começado. |
| PAUSA #1 (UAT 7 SCs) → Plan 02 Task 2 | UAT FAIL em qualquer 1 dos 7 SCs (D-33 mandatórios) impede diff review; evita bump + push com bug comportamental. D-34 atomicidade: rollback total se FAIL não-fixável <5min. |
| PAUSA #2 (diff consolidado) → Plan 02 Task 3 | Diff review FAIL impede push + bump; evita apontar pai pra commit errado ou empurrar 3 commits com scope creep. PAUSA #2 consolida cross-check de SHA (vs Phase 7.9 PAUSA #3 dedicada). |
| Task 3 cross-check automatizado → Push humano em Task 3 | Cross-check SHA submódulo↔pai em `<verify>` substitui PAUSA dedicada de bump-review (risk reclassified — pattern 7.10a herdado). FAIL automatizado bloqueia continuação (e.g., bump aponta pra commit errado). |
| Push pai (Task 3) → Vercel auto-build | Push pai dispara Vercel deploy. Smoke test em prod (re-rodar 7 SCs com dados reais) é ação humana pós-Task 3, NÃO automatizado. |
| Drift histórico D-28 → UAT SC1-SC7 | Drift de centavo causado por UI legacy é ESPERADO (D-28 aceito por design); drift causado por bug semântico é FAIL. Distinção UAT FAIL real vs drift D-28 documentada em SC2-SC7 instructions. |
| D-34 atomicidade → rollback `git reset --hard 2757f74` | UAT FAIL não-fixável <5min ⇒ rollback total dos 3 commits. Phase aborta inteira; nunca ship parcial 07.10b+c sem 07.10d. Path documentado em PAUSA #1 + SUMMARY 02. |
| Planner → tag creation | Proibido. Tag `v1.4-phase7.10bcd` é ação humana pós-smoke prod. |

## Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.10bcd-P02-01 | Elevation of Privilege (push pré-aprovação) | Task 3 push submódulo + push pai | mitigate | Push só executa APÓS APROVADO em Task 1 (UAT) + Task 2 (diff consolidado). Acceptance criteria de Tasks 1-2 explicitamente bloqueiam progressão. |
| T-07.10bcd-P02-02 | Tampering (bump com SHA errado — aponta pra commit que não foi UAT-aprovado) | Task 3 (bump no pai) | mitigate | Task 3 inclui cross-check automatizado em `<verify>`: `[ "$SHA_SUB" = "$SHA_PAI_SUBPROJECT" ] && OK`. FAIL → `git -C src/mr-3 reset --hard 2757f74` (rollback total D-34 — descarta 3 commits 7.10bcd, preserva 7.10a shipped) + retry. Use `--hard` NÃO `--soft` em D-34 atomicidade. PAUSA #2 já validou SHA submódulo manualmente (item 10 checklist). |
| T-07.10bcd-P02-03 | Repudiation (UAT 7 SCs sem registro completo) | Task 1 matriz SC1-SC7 | mitigate | Task 4 exige matriz preenchida no SUMMARY.md com PROD/LOCAL valores em cada SC. Sem matriz, task fails acceptance. SC2 (Devedores MENDES R$ 36.871,64) e SC7 (Recebido Período) são proxies de regressão crítica. |
| T-07.10bcd-P02-04 | Information Disclosure (tag prematura) | Criação de tag antes de smoke prod | mitigate | Explicitly forbidden em múltiplos acceptance criteria (Tasks 3 e 4). Tag `v1.4-phase7.10bcd` = ação humana separada pós-smoke prod. |
| T-07.10bcd-P02-05 | DoS (UAT 7 SCs skipped — D-33 mandatórios contornados) | PAUSA #1 contornada por usuário aprovando sem rodar 7 SCs | mitigate | Task 1 é `checkpoint:human-verify gate="blocking"`. Resume signal exige `APROVADO` literal. Matriz preenchida com PROD/LOCAL valores serve como evidência forense em SUMMARY 02 — sem valores, fica claro que UAT foi superficial. |
| T-07.10bcd-P02-06 | Tampering (docs commit mistura com bump) | Task 3 + Task 4 | mitigate | Task 3 commita só bump (`chore`); Task 4 commita só docs (`docs`). Commits separados mantêm history limpa. Convenção D-15 7.9 herdada. |
| T-07.10bcd-P02-07 | Tampering (bump sem UAT) | Sequência PAUSA #1 → #2 → bump+push contornada | mitigate | 2 checkpoints blocking em ordem estrita; cada task depende do APROVADO anterior explicitamente. |
| T-07.10bcd-P02-08 | Information Disclosure (drift D-28 mal-interpretado como bug) | UAT 7 SCs com devedor que tem UI legacy | mitigate | SC2-SC7 instruções explicitam: "drift de centavo causado por D-28 é ESPERADO; re-testar com devedor 100% Phase 7 antes de FAIL final". SUMMARY 02 documenta drift observado se houver. |
| T-07.10bcd-P02-09 | **Tampering (UAT FAIL parcial — 1 SC FAIL → ship parcial sem rollback)** | Decisão D-34 atomicidade contornada | mitigate | **D-34 atomicidade not negociável** — FAIL não-fixável <5min ⇒ ROLLBACK TOTAL `git -C src/mr-3 reset --hard 2757f74` (descarta 3 commits). Phase aborta inteira; NUNCA ship parcial 07.10b+c sem 07.10d. Resume signal de Task 1 distingue `FAIL: <SCx>: <sintoma>` (fix-forward) de `FAIL: ROLLBACK D-34: <SCs>` (rollback total). Documentado em PAUSA #1 + SUMMARY 02. |
| T-07.10bcd-P02-10 | **Tampering (rollback D-34 contornado em FAVOR de ship parcial)** | UAT FAIL → usuário decide ship parcial em vez de rollback | mitigate | M4 não-negociável (decisão usuário 2026-04-25). Documentado em CONTEXT.md §4 D-34 + PAUSA #1 instruções. Se usuário decidir ship parcial pós-FAIL, isso é deviation explícita do D-34 e DEVE ser documentada em SUMMARY 02 com justificativa específica (e.g., "SC7 FAIL aceito como tech debt 7.10bcd-fix"). Path default = rollback. |
| T-07.10bcd-P02-11 | Tampering (push pai antes de push submódulo) | Task 3 ordem dos pushes | mitigate | Task 3 ordem estrita: (3) push submódulo → (4-6) bump pai + cross-check → (8) push pai. Push pai antes do push submódulo causa erro Vercel (submódulo no SHA novo não está em origin/main; build falha tentando fazer fetch). Validation automatizada no `<verify>` confirma cross-check SHA pós-bump. |
| T-07.10bcd-P02-12 | Information Disclosure (drift D-28 escondido — usuário aprova SC PASS sem registrar drift) | UAT SCs onde drift aparece mas usuário marca PASS | accept | UI legacy raramente usada em prod (advogado prefere fluxo Contratos via Phase 7 stored proc). Drift escondido aceito como ruído baixo; SUMMARY 02 ainda permite documentação retroativa se observado em smoke prod. |
| T-07.10bcd-P02-13 | DoS (Vercel build falha pós-push pai — submódulo SHA inacessível) | Task 3 sequência push submódulo + push pai | mitigate | Task 3 push submódulo PRIMEIRO (passo 3) ANTES de push pai (passo 8). Garante origin/main do submódulo tem SHA referenciado pelo bump pai. Vercel build sucesso esperado pós-push pai. |
| T-07.10bcd-P02-14 | **Information Disclosure (cross-tab visual drift — header card global Carteira lendo fonte legacy seria visível em todas tabs)** | parent App L8425 pgtosPorDevedorCarteira → L8434 totalCarteira → L8735 header card "Carteira" | mitigate | **Fix D-extra (4º callsite Map agrupador)**: parent App L8425 também migrado em commit 07.10d via mesmo helper. **Shield 24d granular** valida (2 Maps useMemo + ambos usam helper + zero forEach legacy). **SC5b mandatório** em PAUSA #1 + smoke prod (re-validar em 2-3 tabs distintas pra confirmar consistência cross-tab). Drift = FAIL crítico (bug visual grave visível em TODAS as tabs). Lição feedback_grep_global_before_pointwise_fix.md aplicada pre-execute identificou esta callsite via grep global de `allPagamentos.forEach`. |
</threat_model>

<verification>
- [ ] Task 1 — PAUSA #1 — Matriz SC1-SC7 preenchida + APROVADO do usuário (TODOS MANDATÓRIOS — D-33; sem SKIP); drift D-28 documentado se observado; **D-34 atomicidade exercitada se FAIL ocorrer** (rollback total ou fix-forward documentado)
- [ ] Task 2 — PAUSA #2 — Diff consolidado submódulo aprovado + SHA `<SHA-07.10d>` registrado; 6 arquivos cumulativos + ModuloDividas zero diff; D-01 motor diff vazio cumulativo em todos 3 motor files; D-30 EXATAMENTE 3 commits sequenciais com headers verbatim; checklist 10 pontos OK; Shields 24a + 24b + 24c PASS; Helper test 2 it() PASS; Regressão 25/25 PASS final
- [ ] Task 3 — Push submódulo (3 commits) executado; Bump no pai commitado com mensagem `chore(07.10bcd): bump submodule mr-3 — Devedores + ModuloDividas + Dashboard leem pagamentos_divida`; cross-check SHA submódulo↔pai automatizado PASS; Push pai executado (Vercel auto-build disparado)
- [ ] Task 4 — SUMMARY 02 criado + SUMMARY 01 (criado em Plan 01) commitado junto + STATE.md atualizado (Phase 7.10bcd SHIPPED) + commit docs `docs(07.10bcd): PLAN, SUMMARY consolidados + STATE phase 7.10bcd SHIPPED`
- [ ] ZERO `git push` executado em Task 4 (apenas commit local; usuário decide quando empurrar docs)
- [ ] ZERO tag `v1.4-phase7.10bcd` criada
- [ ] Phase state final: "3 commits submódulo pushed + bump pai pushed + Vercel build disparado + docs commitados localmente + smoke prod humano pendente + tag separada"
- [ ] Risk reclassification (2 PAUSAs vs 3 da Phase 7.9; mesma decisão da 7.10a) justificada e documentada em SUMMARY 02
- [ ] D-32 (5 humanos checkpoints total na phase 7.10bcd: 3 sub-PAUSAs Plan 01 + 2 PAUSAs Plan 02) verificado
- [ ] D-33 (UAT 7 SCs MANDATÓRIOS sem SKIP) verificado em Task 1
- [ ] D-34 (atomicidade no ship) documentada em SUMMARY 02 — comando rollback registrado (mesmo se não exercitado)
</verification>

<success_criteria>
1. UAT SC1-SC7 executado em prod ou localhost com PASS em TODOS os 7 SCs (D-33 mandatórios sem SKIP); drift de centavo em qualquer = FAIL; drift D-28 distinguido de bug semântico
2. Drift histórico D-28 documentado em SUMMARY 02 se observado durante UAT
3. **D-34 atomicidade**: se UAT FAIL não-fixável <5min ocorrer → rollback total `git -C src/mr-3 reset --hard 2757f74` documentado em SUMMARY + STATE como "Phase 7.10bcd ABORTED"
4. Diff consolidado do submódulo aprovado pelo usuário com 6 arquivos cumulativos (helper NEW + helper test NEW + App.jsx + TabelaDividas + DetalheDivida + package.json) + ModuloDividas zero diff cumulativo + D-01 estrito cumulativo motor diff vazio em todos 3 + D-30 EXATAMENTE 3 commits sequenciais com headers verbatim + Shields 24a + 24b + 24c todos OK + Helper test 2 it() existe e PASS + Regressão 25/25 PASS final cumulativo
5. Checklist 10 pontos da PAUSA #2 todos OK (3 commits + 6 files + ModuloDividas zero diff + D-01 cumulativo + commit msgs + 3 callsites swap + helper + Devedores Map + Dashboard helper + Set lookup pattern D-27 replicado + Shields + helper test + SHA cross-check anotado)
6. **Push do submódulo executado em Task 3** (3 commits empurrados pra origin/main) ANTES do bump pai
7. Bump no pai criado com commit `chore(07.10bcd): bump submodule mr-3 — Devedores + ModuloDividas + Dashboard leem pagamentos_divida`
8. Cross-check SHA submódulo (`<SHA-07.10d>`) === `+Subproject commit` no pai PASS (validação automatizada em Task 3 `<verify>`)
9. **Push do pai executado em Task 3** (bump empurrado pra origin/master; Vercel auto-build disparado)
10. `07.10bcd-02-SUMMARY.md` criado com matriz SC1-SC7, SHAs (3 submódulo + 2 pai bump+docs), registro das 2 PAUSAs, drift D-28 observação, D-30..D-34 invariantes verified, próximos passos (smoke prod + tag separada + backlog 7.10e/f/bug)
11. `07.10bcd-01-SUMMARY.md` (criado em Plan 01 Task 6, left uncommitted) commitado junto em Task 4
12. `.planning/STATE.md` atualizado (last_activity + status referenciando Phase 7.10bcd SHIPPED com todos os detalhes completos)
13. Commit docs consolidado `docs(07.10bcd): PLAN, SUMMARY consolidados + STATE phase 7.10bcd SHIPPED` aplicado no pai (3 files: SUMMARY 01 + SUMMARY 02 + STATE.md)
14. ZERO criação da tag `v1.4-phase7.10bcd` (out of scope — pós-smoke prod)
15. Estado final: phase 7.10bcd "3 commits submódulo pushed + bump pai pushed + Vercel build disparado + docs commitados localmente (push humano subsequente) + smoke prod humano pendente + tag v1.4-phase7.10bcd separada"
</success_criteria>

<output>
Após completion: `.planning/phases/07.10bcd-migracao-pagamentos-divida-tres-consumidores/07.10bcd-02-SUMMARY.md` contendo:
- Matriz SC1-SC7 preenchida com PASS/FAIL em todos 7 (TODOS MANDATÓRIOS — D-33)
- SHAs: 3 commits submódulo (07.10b/c/d) + bump pai + commit docs consolidado
- Resultado de cada uma das 2 PAUSAs (UAT 7 SCs + diff consolidado pré-bump)
- Cross-check SHA submódulo↔pai PASS (Task 3 verify automatizado)
- Drift histórico D-28 observação (sim/não + descrição) — distinção bug semântico vs UI legacy
- Justificativa de risk reclassification (2 PAUSAs Plan 02 vs 3 da Phase 7.9; mesma decisão da 7.10a; cerimônia média justificada por 3 sub-PAUSAs Plan 01 prévias)
- D-30..D-34 invariantes verified (especialmente D-34 atomicidade — comando rollback registrado mesmo se não exercitado)
- Lista explícita de ações pendentes: (a) push humano commit docs subsequente; (b) smoke prod (re-rodar 7 SCs com dados reais); (c) tag v1.4-phase7.10bcd separada pós-smoke; (d) período observação prod 1-2 semanas; (e) backlog 7.10e (cortar writes legacy) + 7.10f (drop tabela) + 7.10.bug

Fim da phase 7.10bcd. Próxima ação: usuário aguarda Vercel build (~2min) → smoke prod cross-check 7 SCs com dados reais → (se OK) cria tag `v1.4-phase7.10bcd` em ação separada → período observação prod → avaliar inserção 7.10e/7.10f/7.10.bug. Backlog imediato: 7.10e (cortar writes pagamentos_parciais UI legacy) após período observação OK.
</output>
