---
phase: 07.10bcd-migracao-pagamentos-divida-tres-consumidores
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: false
requirements_addressed: []
source: "ROADMAP.md line 361-368 + 07.10bcd-CONTEXT.md (D-30..D-34 + escopo §2 Fix A/B/C/D + UAT §6 SC1-SC7 + Shield 24a/b/c §7 + commits atômicos M1) — Phase has NO REQ-IDs (tech debt fast-track v1.0 legacy migration de 3 consumidores)."

files_modified:
  - src/mr-3/mr-cobrancas/src/utils/agruparPagamentosPorDevedor.js                      # NEW (Fix A — helper compartilhado, Map agrupador)
  - src/mr-3/mr-cobrancas/src/services/__tests__/agruparPagamentosPorDevedor.test.js    # NEW (test trivial helper, 2 it() blocks)
  - src/mr-3/mr-cobrancas/package.json                                                  # MOD (test:regressao estende com agruparPagamentosPorDevedor.test.js)
  - src/mr-3/mr-cobrancas/src/App.jsx                                                   # MOD (3 commits diferentes — case "devedores" L8468 em 07.10b; case "dividas" L8488 em 07.10c; case "dashboard" L8467 + Dashboard L568+L696 em 07.10d; Devedores Map L2984-2992 em 07.10b)
  - src/mr-3/mr-cobrancas/src/components/TabelaDividas.jsx                              # MOD (commit 07.10c — L41-44 buildDevedorObjParaSaldo Set lookup divida_id)
  - src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx                              # MOD (commit 07.10c — L74 filter Set lookup divida_id)
  # ModuloDividas.jsx NÃO listado — pass-through inalterado (prop name `allPagamentos` mantida; conteúdo = pagamentos_divida pós-callsite swap em App.jsx L8488)

must_haves:
  truths:
    - "Helper `agruparPagamentosPorDevedor` (NEW src/utils/) cria Map devedor_id → pagamentos[] via lookup intermediário divida_id (D-31 helper-first; pagamentos_divida não tem devedor_id direto)"
    - "Test trivial 2 it() blocks PASS: (a) devedor com pagamentos múltiplos em 2 dívidas; (b) devedor sem dívidas → Map vazio sem crash"
    - "package.json `test:regressao` estendido: 25/25 esperado (23 base 7.10a + 2 helper test)"
    - "Commit 07.10b — Devedores migra fonte: App.jsx L8468 callsite swap `allPagamentos`→`allPagamentosDivida` no case 'devedores'; App.jsx L2984-2992 Devedores Map troca forEach manual por chamada `agruparPagamentosPorDevedor(devedores, allPagamentos)` (a prop ainda chama allPagamentos mas o conteúdo é pagamentos_divida pós-callsite swap — D-30 commit atômico bisect-able)"
    - "Commit 07.10c — ModuloDividas + sub-componentes: App.jsx L8488 callsite swap no case 'dividas'; ModuloDividas.jsx pass-through inalterado; TabelaDividas.jsx L41-44 refactor `buildDevedorObjParaSaldo` Set lookup pattern D-27 (filter via dividaIdsDoDevedor); DetalheDivida.jsx L74 mesmo pattern (D-30 commit atômico bisect-able)"
    - "Commit 07.10d — Dashboard agregados: App.jsx L8467 callsite swap no case 'dashboard'; Dashboard L568 forEach trocado por helper compartilhado; L613 reduce + L667 filter dataInicio inalterados (lêem só p.valor + p.data_pagamento — D-29 motor compat); L696 pgtosDev refactor inline Set lookup pattern D-27 (D-30 commit atômico bisect-able)"
    - "Shield 24a (commit 07.10b) — App.jsx case 'devedores' positivo (1 match `allPagamentos={allPagamentosDivida}`) + negativo regional (10 linhas seguintes: 0 matches `allPagamentos={allPagamentos}` legacy)"
    - "Shield 24b (commit 07.10c) — case 'dividas' positivo + negativo regional + greps em TabelaDividas + DetalheDivida (filter pattern `dividaIdsDoDevedor` presente; legacy `p.devedor_id === ...devedor_id` ausente em ambos)"
    - "Shield 24c (commit 07.10d) — case 'dashboard' positivo + negativo regional"
    - "Shield 24d (commit 07.10d Fix D-extra) — parent App L8425 pgtosPorDevedorCarteira: 2 Maps useMemo (Dashboard L566 interno + parent App L8425) + ambos usam helper agruparPagamentosPorDevedor + ZERO forEach manual (verificação granular adicional). 4 callsites Map agrupador no total identificadas via grep global lição feedback_grep_global_before_pointwise_fix.md"
    - "5 humanos checkpoints (D-32): 3 sub-PAUSAs intra-Plan-01 (após cada commit atômico — usuário valida regressão + grep + reporte) + 2 PAUSAs em Plan 02 (UAT 7 SCs + diff consolidado pré-bump)"
    - "Regressão por commit (M5): passa após CADA UM dos 3 commits (não apenas pós-execute final). Esperado 25/25 cumulativo (23 base + 2 helper)"
    - "Motor Art.354 INTOCADO em todos 3 arquivos (devedorCalc.js + pagamentos.js + dividas.js — D-01 estrito) — dual gate C1 (working tree pré cada commit) + C2 (commit contents cumulativo dos 3 commits) ambos OK"
    - "D-30 commits atômicos cumulativos: `git -C src/mr-3 log 2757f74..HEAD --oneline` retorna EXATAMENTE 3 commits sequenciais com headers `feat(07.10b)`, `feat(07.10c)`, `feat(07.10d)` (baseline 2757f74 = post-7.10a HEAD pré-7.10bcd)"
    - "D-28 drift histórico CONTINUA aceito por design: writes UI legacy em pagamentos_parciais não cortados (phase 7.10e hipotética). Pós-7.10bcd TODOS os consumidores em prod usam pagamentos_divida — pagamentos via UI legacy ficam órfãos no banco sem nenhum consumer mostrando"
    - "D-34 atomicidade no ship: se UAT FAIL em qualquer 1 dos 7 SCs (Plan 02) → rollback total `git -C src/mr-3 reset --hard 2757f74` no submódulo. Phase aborta inteira; nunca ship parcial 07.10b+c sem 07.10d. Exceção: bug trivial fixável <5min vira fix-forward"
  artifacts:
    - path: "src/mr-3/mr-cobrancas/src/utils/agruparPagamentosPorDevedor.js"
      provides: "Helper compartilhado D-31 — Map agrupador devedor_id → pagamentos[] via lookup intermediário divida_id (pagamentos_divida sem devedor_id direto)"
      contains: "export function agruparPagamentosPorDevedor"
    - path: "src/mr-3/mr-cobrancas/src/services/__tests__/agruparPagamentosPorDevedor.test.js"
      provides: "Test trivial helper — 2 it() blocks (devedor com pagamentos múltiplos; devedor sem dívidas → Map vazio)"
      contains: "agruparPagamentosPorDevedor"
    - path: "src/mr-3/mr-cobrancas/package.json"
      provides: "test:regressao estende com agruparPagamentosPorDevedor.test.js"
      contains: "agruparPagamentosPorDevedor.test.js"
    - path: "src/mr-3/mr-cobrancas/src/App.jsx"
      provides: "3 callsites swap (L8467 dashboard + L8468 devedores + L8488 dividas) + Devedores Map L2986 usa helper + Dashboard L568 usa helper + Dashboard L696 Set lookup inline (3 commits diferentes — D-30 atomicidade)"
      contains: "agruparPagamentosPorDevedor"
    - path: "src/mr-3/mr-cobrancas/src/components/TabelaDividas.jsx"
      provides: "buildDevedorObjParaSaldo refactor — Set lookup divida_id ∈ dividas-do-devedor (D-27 pattern reutilizável)"
      contains: "dividaIdsDoDevedor"
    - path: "src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx"
      provides: "filter refactor — Set lookup divida_id ∈ dividas-do-devedor (D-27 pattern reutilizável)"
      contains: "dividaIdsDoDevedor"
  key_links:
    - from: "src/mr-3/mr-cobrancas/src/utils/agruparPagamentosPorDevedor.js (helper)"
      to: "App.jsx L2986 Devedores Map (commit 07.10b) + App.jsx L568 Dashboard pgtosPorDevedorCarteira (commit 07.10d)"
      via: "import + chamada `agruparPagamentosPorDevedor(devedores, allPagamentos)` em 2 callsites"
      pattern: "agruparPagamentosPorDevedor\\(devedores"
    - from: "App.jsx case 'devedores' (L8468)"
      to: "Devedores function L2984-2992 (Map agrupador)"
      via: "Prop `allPagamentos` ainda nominalmente — conteúdo é pagamentos_divida pós-callsite swap"
      pattern: "case \"devedores\".*allPagamentos=\\{allPagamentosDivida\\}"
    - from: "App.jsx case 'dividas' (L8488)"
      to: "ModuloDividas (pass-through) → TabelaDividas L41-44 + DetalheDivida L74 (Set lookup divida_id)"
      via: "Prop pass-through; sub-componentes refatoram filter para D-27 pattern"
      pattern: "case \"dividas\".*allPagamentos=\\{allPagamentosDivida\\}"
    - from: "App.jsx case 'dashboard' (L8467)"
      to: "Dashboard function L562-720 (helper L568 + reduce L613 inalterado + filter L667 inalterado + Set lookup inline L696)"
      via: "Prop swap + helper agrupador + Set lookup inline replicado"
      pattern: "case \"dashboard\".*allPagamentos=\\{allPagamentosDivida\\}"
    - from: "Helper agruparPagamentosPorDevedor"
      to: "Test trivial 2 it() (Shield helper)"
      via: "expect(map.get('dev-1')).toHaveLength(2) + expect(map).toBeInstanceOf(Map) — defesa contra regressão silenciosa do helper (lição feedback_shields_wrapper_must_test_equivalence.md 7.8.2a)"
      pattern: "agruparPagamentosPorDevedor\\("
    - from: "Chain de 3 commits sequenciais (07.10b → 07.10c → 07.10d)"
      to: "git bisect funcional + isolamento de regressão por consumer"
      via: "D-30 commits atômicos bisect-able (M1) — cada um deixa estado verde (build + regressão 25/25)"
      pattern: "feat\\(07\\.10[bcd]\\)"
---

<objective>
Migrar 3 consumidores legacy (Devedores + ModuloDividas/sub-componentes + Dashboard) de `allPagamentos` v1.0 → `allPagamentosDivida` v1.4 (Phase 7.3 fonte correta) em 1 phase com **3 commits atômicos sequenciais bisect-able** no submódulo (07.10b → 07.10c → 07.10d). Risco MÉDIO-ALTO (3x mais surface vs 7.10a) mitigado por 6 controles não-negociáveis (M1-M6 do CONTEXT.md §1).

Purpose:
- **D-30 commits atômicos** (M1) — NÃO 1 commit feat(7.10bcd) gigante. Sim sequência: `feat(07.10b): Devedores...` → `feat(07.10c): ModuloDividas...` → `feat(07.10d): Dashboard...`. Bisect funciona; isolamento de regressão por consumer garantido.
- **D-31 helper compartilhado** (M2/Q2) — `agruparPagamentosPorDevedor.js` criado no commit 07.10b (1º consumidor que precisa). Reutilizado em 07.10d (Dashboard L568). Test trivial blinda regressão silenciosa do helper. DRY > inline duplication (lição "helper-first" da 7.9 `feedback_db_integration_gate_missing.md`).
- **D-32 5 humanos checkpoints proporcionais ao risco** (M3 + M6) — 3 sub-PAUSAs intra-Plan-01 (após cada commit atômico: regressão + grep + reporte) + 2 PAUSAs Plan 02 (UAT 7 SCs + diff consolidado pré-bump). Defesa contra empilhamento de regressão silenciosa.
- **D-27 Set lookup inline replicado** (lição 7.10a) — TabelaDividas L41-44 + DetalheDivida L74 + Dashboard L696. 3 callsites = ainda inline OK (≤5 = inline; ≥5 = vira helper). Não justifica helper compartilhado pra esse pattern menor.
- **D-29 motor compat** — `calcularSaldoDevedorAtualizado` lê apenas `p.data_pagamento + p.valor`. Shape `pagamentos_divida` (que tem ambos) é compatível. Motor distribui FIFO pelas dividas do devedor — mesmo comportamento.
- **D-28 drift histórico CONTINUA aceito** — pagamentos novos via UI legacy "Registrar pagamento parcial" (App.jsx L2537+) escrevem em `pagamentos_parciais` e NÃO aparecem em nenhum consumer pós-7.10bcd. Phase 7.10e hipotética cortará writes. Pós-7.10bcd: TODOS os consumidores em prod usam pagamentos_divida; pagamentos via UI legacy ficam órfãos no banco sem nenhum consumer mostrando.
- **D-34 atomicidade no ship** (M4) — se UAT FAIL em qualquer 1 dos 7 SCs Plan 02 → rollback total `git -C src/mr-3 reset --hard 2757f74` (volta ao state 7.10a-pre-bcd). Phase aborta inteira; nunca ship parcial 07.10b+c sem 07.10d. Exceção: bug trivial fixável <5min vira fix-forward (caminho regular).

Output:
- `src/mr-3/mr-cobrancas/src/utils/agruparPagamentosPorDevedor.js` (NEW — Fix A helper)
- `src/mr-3/mr-cobrancas/src/services/__tests__/agruparPagamentosPorDevedor.test.js` (NEW — 2 it() helper test)
- `src/mr-3/mr-cobrancas/package.json` (MOD — test:regressao estende)
- `src/mr-3/mr-cobrancas/src/App.jsx` (MOD em 3 commits diferentes — Devedores em 07.10b, callsite case "dividas" em 07.10c, callsite case "dashboard" + Dashboard body em 07.10d)
- `src/mr-3/mr-cobrancas/src/components/TabelaDividas.jsx` (MOD — commit 07.10c — Set lookup)
- `src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx` (MOD — commit 07.10c — Set lookup)
- 3 commits atômicos sequenciais no submódulo: `feat(07.10b)` → `feat(07.10c)` → `feat(07.10d)`
- `.planning/phases/07.10bcd-migracao-pagamentos-divida-tres-consumidores/07.10bcd-01-SUMMARY.md` (NEW — left uncommitted in parent até Plan 02 Task 4)

Fora de escopo:
- Migrar ModuloContratos / DetalheContrato / ProcessosJudiciais (já migrados em 7.3 / 7.8.2a / 7.10a)
- Cortar writes em `pagamentos_parciais` (App.jsx L2537+ UI legacy ainda ativa — phase 7.10e hipotético)
- Drop schema `pagamentos_parciais` (phase final pós-cleanup writes)
- Tocar motor Art.354 (D-01 invariante mantido em todos 3 commits)
- Adicionar mutation handlers (D-05 não muda — esta phase é leitura only)
- Renomear `pagamentos_parciais` em fixtures de test (calculos.test.js + casos-tjgo.json — fixture local, não consumidor real)
- Refactorar PessoasVinculadas pra ler de outra fonte (recebe `pagamentos` prop pré-filtrada, sem mudança)
- Remover prop `allPagamentos` legacy (continua existindo no caller App.jsx — Vercel build ainda precisa do state, só callsites trocam pra `allPagamentosDivida`)
- ModuloDividas.jsx pass-through (prop name `allPagamentos` mantida — conteúdo é pagamentos_divida pós-callsite swap, ZERO edição no componente)
- Bump pai / Push / Tag (todos diferidos pra Plan 02 + ações humanas pós-smoke)
- UAT 7 SCs (Plan 02 PAUSA #2)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07.10bcd-migracao-pagamentos-divida-tres-consumidores/07.10bcd-CONTEXT.md
@.planning/phases/07.10a-processos-judiciais-pagamentos-divida/07.10a-CONTEXT.md

<!-- Arquivos modificados por este plano (source-of-truth atual) -->
@src/mr-3/mr-cobrancas/src/App.jsx
@src/mr-3/mr-cobrancas/src/components/ModuloDividas.jsx
@src/mr-3/mr-cobrancas/src/components/TabelaDividas.jsx
@src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx
@src/mr-3/mr-cobrancas/package.json

<!-- Motor protegido (D-01 estrito — ZERO edição em qualquer um) -->
@src/mr-3/mr-cobrancas/src/utils/devedorCalc.js
@src/mr-3/mr-cobrancas/src/services/pagamentos.js
@src/mr-3/mr-cobrancas/src/services/dividas.js

<!-- Analogs: 7.10a Shield 23 fixture (pattern de test reutilizável) + saldoAtualizadoCache test (vitest style) -->
@src/mr-3/mr-cobrancas/src/services/__tests__/processosJudiciaisFilter.test.js
@src/mr-3/mr-cobrancas/src/services/__tests__/saldoAtualizadoCache.test.js

<interfaces>
Key contracts the executor needs (extracted from codebase) — NO codebase exploration needed:

App.jsx state allPagamentosDivida (JÁ EXISTE desde Phase 7.3):
- L8325: `const [allPagamentosDivida, setAllPagamentosDivida] = useState([]);`
- L8344 (carregarTudo): fetch `dbGet("pagamentos_divida")` em parallel + `setAllPagamentosDivida(...)` em L8348
- Estado já consumido por: ModuloContratos (case "contratos") + DetalheContrato (via prop) + ProcessosJudiciais (case "processos") — Phase 7.3+ + 7.10a

App.jsx 3 callsites legacy ATUAIS (verbatim) — alvo desta phase:

```jsx
// L8467 (case "dashboard") — alvo commit 07.10d
case "dashboard": return <Dashboard devedores={devedores} processos={processos} andamentos={andamentos} user={user} lembretes={lembretesList} allPagamentos={allPagamentos} />;

// L8468 (case "devedores") — alvo commit 07.10b
case "devedores": return <Devedores devedores={devedores} setDevedores={setDevedores} credores={credores} onModalChange={setModalAberto} user={user} processos={processos} setTab={setTab} allPagamentos={allPagamentos} />;

// L8488 (case "dividas") — alvo commit 07.10c
case "dividas": return (
  <ModuloDividas
    allDividas={allDividas}
    devedores={devedores}
    credores={credores}
    allPagamentos={allPagamentos}
    hoje={hoje_app}
    onCarregarTudo={carregarTudo}
    setTab={setTab}
  />
);
```

App.jsx Devedores function L2984-2992 (Map agrupador legacy — alvo commit 07.10b):
```jsx
const pgtosPorDevedor = useMemo(() => {
  const m = new Map();
  allPagamentos.forEach(p => {
    const k = String(p.devedor_id);          // ← QUEBRA: pagamentos_divida sem devedor_id
    if (!m.has(k)) m.set(k, []);
    m.get(k).push(p);
  });
  return m;
}, [allPagamentos]);
```

App.jsx Dashboard function L562-720 (5 refs efetivas — alvo commit 07.10d):
```jsx
function Dashboard({ devedores, processos, andamentos, user, lembretes = [], allPagamentos = [] }) {
  // ...
  // L566-574 — Map agrupador (mesmo problema do Devedores) — usa helper
  const pgtosPorDevedorCarteira = useMemo(() => {
    const m = new Map();
    allPagamentos.forEach(p => {
      const k = String(p.devedor_id);          // ← QUEBRA
      if (!m.has(k)) m.set(k, []);
      m.get(k).push(p);
    });
    return m;
  }, [allPagamentos]);

  // L612-615 — reduce só p.valor — ZERO mudança (D-29 motor compat)
  const totalRecuperadoGlobal = useMemo(() =>
    allPagamentos.reduce((s, p) => s + (parseFloat(p.valor) || 0), 0),
    [allPagamentos]
  );

  // L665-670 — filter por p.data_pagamento — ZERO mudança (D-29)
  const pagamentosNoPeriodo = useMemo(() =>
    dataInicio
      ? allPagamentos.filter(p => (p.data_pagamento || "") >= dataInicio)
      : allPagamentos,
    [allPagamentos, dataInicio]
  );

  // L690-701 — pgtosDev filter por p.devedor_id (último ref) — Set lookup inline pattern D-27
  const ultimosPagamentos = useMemo(() => {
    return [...pagamentosNoPeriodo]
      .sort(...).slice(0, 5).map(p => {
        const dev = devedores.find(d => String(d.id) === String(p.devedor_id));
        const pgtosDev = allPagamentos.filter(pp => String(pp.devedor_id) === String(p.devedor_id));   // ← QUEBRA
        // ...
      });
  }, [pagamentosNoPeriodo, devedores, allPagamentos, hoje]);
}
```

ModuloDividas.jsx (pass-through INALTERADO):
```jsx
export default function ModuloDividas({ allDividas, devedores, credores, allPagamentos, hoje, onCarregarTudo, setTab }) {
  // ... ZERO cálculos diretos com allPagamentos. Apenas pass-through:
  <TabelaDividas allPagamentos={allPagamentos} ... />
  <DetalheDivida allPagamentos={allPagamentos} ... />
}
```
NOTA: prop name `allPagamentos` é MANTIDA aqui (conteúdo passa a ser pagamentos_divida pós-callsite swap em App.jsx L8488). ZERO edição neste arquivo no commit 07.10c.

TabelaDividas.jsx L41-46 (buildDevedorObjParaSaldo legacy — alvo commit 07.10c):
```jsx
function buildDevedorObjParaSaldo(divida, devedores, allPagamentos) {
  const devedor = devedores.find(d => String(d.id) === String(divida.devedor_id));
  if (!devedor) return null;
  const pagamentosDoDevedor = allPagamentos.filter(p => String(p.devedor_id) === String(divida.devedor_id));   // ← QUEBRA
  return { devedor, pagamentos: pagamentosDoDevedor };
}
```

DetalheDivida.jsx L73-74 (filter legacy — alvo commit 07.10c):
```jsx
const devedor = devedores.find(d => String(d.id) === String(divida.devedor_id));
const pagamentosDoDevedor = allPagamentos.filter(p => String(p.devedor_id) === String(divida.devedor_id));   // ← QUEBRA
```

pagamentos_divida shape (Supabase schema — locked, herdado 7.10a):
- id UUID PK
- divida_id UUID NOT NULL REFERENCES dividas(id)        ← FK indireto via divida (NO devedor_id direto)
- data_pagamento DATE NOT NULL                          ← motor lê (D-29)
- valor NUMERIC(15,2) NOT NULL                          ← motor lê (D-29)
- observacao TEXT

Motor calcularSaldoDevedorAtualizado (devedorCalc.js:59 — READ-ONLY D-01 estrito):
- Lê APENAS `p.data_pagamento` + `p.valor`. Não lê `p.devedor_id` nem `p.divida_id` nem `p.id`.
- Shape pagamentos_divida (que tem ambos data_pagamento+valor) é compatível (D-29).
- Motor distribui FIFO pelas dividas do devedor — mesmo comportamento.

Submódulo baseline pré-7.10bcd: SHA `2757f74` (post-7.10a HEAD pré-7.10bcd).
- Cumulative motor diff `git -C src/mr-3 diff 2757f74..HEAD -- <3 motor files>` deve ser VAZIO ao final do plan (D-01 cumulativo).
- Cumulative commit count `git -C src/mr-3 log 2757f74..HEAD --oneline` deve ser EXATAMENTE 3 ao final (D-30).
</interfaces>

<open_questions>
**Q1 — Prop name em ModuloDividas / Devedores: renomear ou manter?**

CONTEXT.md §2 Fix B/C: "ZERO edição no componente ModuloDividas (pass-through inalterado, prop name `allPagamentos` mantida — o conteúdo é pagamentos_divida pós-callsite swap)."

→ **DECISÃO PLANNER**: **MANTER prop name `allPagamentos` em ModuloDividas + Devedores + sub-componentes**. Razões:
  (a) Reduz superfície de mudança (props rename cascateia em todos sub-componentes);
  (b) Comentário inline explícito em cada commit body explica o "naming débito técnico aceito" — o conteúdo é pagamentos_divida apesar do nome legacy;
  (c) Phase 7.10e hipotética (cleanup writes) pode renomear globalmente quando legacy for removido.

**Q2 — Helper signature: enriched Map<devedor_id, pagamentos[]> ou plain Map?**

CONTEXT.md §2 Fix A: signature retorna `Map<string, pagamentos[]>` plain (pagamentos têm shape original sem `devedor_id` injetado). Q5 audit (CONTEXT §9) confirmou: ZERO callsite usa `p.devedor_id` no item individual pós-Map.

→ **DECISÃO PLANNER**: **Plain Map sem enrichment** (signature exata do CONTEXT §2 Fix A — copiado verbatim em Task 1). Helper enrichment NÃO necessário; pattern simples funciona pra todos 5 callsites downstream do Map (PDF, PessoasVinculadas, motor, reduce).

**Q3 — Test count final esperado pós-helper?**

Phase 7.10a baseline: 23/23 (11 calculos + 5 saldoAtualizadoCache + 4 custas + 3 processosJudiciaisFilter Shield 23).
Helper test: 2 it() blocks.

→ **EXPECTATIVA**: **25/25 PASS cumulativo** (23 base + 2 helper). Cada `it()` conta 1 em vitest.

**Q4 — Onde adicionar Shield 24 grep enforcement: pre-commit hook ou Task verify?**

CONTEXT.md §7 lista os 3 sub-Shields (24a + 24b + 24c) como assinaturas exatas grep + awk.

→ **DECISÃO PLANNER**: **Inline em cada Task como Gate específico (pre-commit)** + cumulative re-run em Task 5 final gates. Não pre-commit hook (overkill pra phase atômica). Cada commit valida seu próprio shield ANTES do git commit.

**Q5 — Baseline cumulative SHA: 2757f74 ou d354daa?**

CONTEXT.md menciona "2757f74" implicitamente (post-7.10a HEAD = pré-7.10bcd baseline). User prompt mencionou "d354daa" mas isso seria pre-7.10a (incluiria commits da 7.10a no count cumulativo, quebrando D-30 esperado de "EXATAMENTE 3 commits").

→ **DECISÃO PLANNER**: **Usar `2757f74` como baseline pré-7.10bcd** em todos os gates cumulativos (C1 motor diff, C2 motor name-only, D-30 commit count). Justificativa: 7.10a já foi shipped (commit 2757f74 + bump pai 7edb8cb + tag v1.4-phase7.10a); baseline de 7.10bcd começa após. Usuário pode reconciliar se for ambiguidade — mas semântica de "EXATAMENTE 3 commits" só é satisfeita com baseline 2757f74.

**RECONCILIADO 2026-04-25 com user**: divergência prompt original mencionava "d354daa" mas decisão final locked é **2757f74** em TODOS os pontos do plan (CONTEXT.md §4 D-34 + §5 SC #11 + §5 SC #15 + Plan 01 frontmatter truths + Plan 01 Tasks 2-5 gates cumulativos + Plan 02 Task 2 PAUSA #2 + Plan 02 Task 1 PAUSA #1 D-34 rollback comando). d354daa = pre-7.10a (já shipped, NÃO usar como rollback target — destruiria phase 7.10a também). 2757f74 = post-7.10a HEAD pré-7.10bcd (rollback correto preservando 7.10a).
</open_questions>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar helper agruparPagamentosPorDevedor.js + test trivial 2 it() + estender package.json (NEW files, NO COMMIT)</name>
  <read_first>
    - `.planning/phases/07.10bcd-migracao-pagamentos-divida-tres-consumidores/07.10bcd-CONTEXT.md` (§2 Fix A — helper signature verbatim + §7 Shield helper grep)
    - `src/mr-3/mr-cobrancas/src/services/__tests__/processosJudiciaisFilter.test.js` (analog vitest style — 7.10a precedente)
    - `src/mr-3/mr-cobrancas/package.json` (script `test:regressao` atual — após 7.10a inclui 4 test files)
    - `memory/feedback_shields_wrapper_must_test_equivalence.md` (helper test trivial pattern — defesa H3 7.8.2a)
    - `memory/feedback_db_integration_gate_missing.md` (helper-first lição 7.9 — DRY > inline duplication)
  </read_first>
  <behavior>
    - **it #1 — Devedor com pagamentos múltiplos em 2 dívidas**: Fixture devedores=[{id:"dev-1",dividas:[{id:"div-A"},{id:"div-B"}]}] + pagamentos=[{divida_id:"div-A",...},{divida_id:"div-B",...},{divida_id:"div-A",...}]. `expect(map.get("dev-1")).toHaveLength(3)` + `expect(map).toBeInstanceOf(Map)`.
    - **it #2 — Devedor sem dívidas (boundary edge)**: Fixture devedores=[{id:"dev-2",dividas:[]}] + 1 pagamento qualquer com divida_id arbitrário. `expect(map.has("dev-2")).toBe(false)` (devedor sem dívidas registradas não vira chave do Map; lookup divida_id retorna undefined; pagamento descartado). Não crasha.
  </behavior>
  <files>
    - src/mr-3/mr-cobrancas/src/utils/agruparPagamentosPorDevedor.js (NOVO)
    - src/mr-3/mr-cobrancas/src/services/__tests__/agruparPagamentosPorDevedor.test.js (NOVO)
    - src/mr-3/mr-cobrancas/package.json (MOD — script test:regressao estende)
  </files>
  <action>
    **Parte A — Criar `src/mr-3/mr-cobrancas/src/utils/agruparPagamentosPorDevedor.js`** (verbatim per CONTEXT.md §2 Fix A):

    ```javascript
    /**
     * Phase 7.10bcd — Helper compartilhado D-31
     *
     * Agrupa pagamentos por devedor via lookup intermediário divida_id (pagamentos_divida shape
     * sem devedor_id direto — só FK divida_id; mapeamento via fullDev.dividas).
     *
     * Reutilizado em:
     *   - App.jsx Devedores Map L2986 (commit 07.10b)
     *   - App.jsx Dashboard pgtosPorDevedorCarteira L568 (commit 07.10d)
     *
     * 2 callsites + complexidade da lookup map = vira helper. DRY > inline duplication
     * (lição "helper-first" da 7.9 — feedback_db_integration_gate_missing.md).
     *
     * @param {Array} devedores - Lista de devedores com .dividas[]
     * @param {Array} pagamentosDivida - Pagamentos no shape pagamentos_divida (com divida_id)
     * @returns {Map<string, Array>} Map devedor_id → pagamentos[]; devedor sem dividas registradas
     *                                 OU pagamento com divida_id desconhecido = descartado.
     */
    export function agruparPagamentosPorDevedor(devedores, pagamentosDivida) {
      const dividaIdToDevedorId = new Map();
      (devedores || []).forEach(d =>
        (d.dividas || []).forEach(div =>
          dividaIdToDevedorId.set(String(div.id), String(d.id))
        )
      );
      const map = new Map();
      (pagamentosDivida || []).forEach(p => {
        const devId = dividaIdToDevedorId.get(String(p.divida_id));
        if (!devId) return;
        if (!map.has(devId)) map.set(devId, []);
        map.get(devId).push(p);
      });
      return map;
    }
    ```

    **Parte B — Criar `src/mr-3/mr-cobrancas/src/services/__tests__/agruparPagamentosPorDevedor.test.js`** (2 it() blocks):

    ```javascript
    /**
     * Phase 7.10bcd — Test trivial helper agruparPagamentosPorDevedor (D-31)
     *
     * Defesa contra regressão silenciosa do helper compartilhado (lição
     * memory/feedback_shields_wrapper_must_test_equivalence.md da 7.8.2a):
     * cache/wrapper compartilhado SEMPRE precisa shield próprio, não só propriedades
     * indiretas. Helper agruparPagamentosPorDevedor é compartilhado entre Devedores
     * (commit 07.10b) e Dashboard (commit 07.10d) — bug aqui propaga pra 2 consumers.
     *
     * 2 it() blocks:
     *   - Devedor com pagamentos múltiplos em 2 dívidas (caminho normal)
     *   - Devedor sem dívidas registradas (boundary — não crasha, não vira chave do Map)
     *
     * Roda junto da regressão via `npm run test:regressao` (esperado: 25/25 — 23 base 7.10a + 2 helper).
     */

    import { describe, it, expect } from 'vitest';
    import { agruparPagamentosPorDevedor } from '../../utils/agruparPagamentosPorDevedor.js';

    describe('agruparPagamentosPorDevedor — helper compartilhado (Phase 7.10bcd D-31)', () => {
      it('devedor com pagamentos múltiplos em 2 dívidas — Map agrupa todos pelo devedor_id', () => {
        const devedores = [
          { id: "dev-1", dividas: [{ id: "div-A" }, { id: "div-B" }] }
        ];
        const pagamentos = [
          { id: "p1", divida_id: "div-A", data_pagamento: "2026-01-01", valor: 100 },
          { id: "p2", divida_id: "div-B", data_pagamento: "2026-02-01", valor: 200 },
          { id: "p3", divida_id: "div-A", data_pagamento: "2026-03-01", valor: 50 },
        ];
        const map = agruparPagamentosPorDevedor(devedores, pagamentos);
        expect(map).toBeInstanceOf(Map);
        expect(map.get("dev-1")).toHaveLength(3);
        expect(map.get("dev-1").map(p => p.id)).toEqual(["p1", "p2", "p3"]);
      });

      it('devedor sem dívidas registradas — Map vazio (não vira chave, pagamento descartado)', () => {
        const devedores = [{ id: "dev-2", dividas: [] }];
        const pagamentos = [
          { id: "p1", divida_id: "div-X", data_pagamento: "2026-01-01", valor: 100 },
        ];
        const map = agruparPagamentosPorDevedor(devedores, pagamentos);
        expect(map).toBeInstanceOf(Map);
        expect(map.has("dev-2")).toBe(false);
        expect(map.size).toBe(0);
      });
    });
    ```

    **Parte C — Atualizar `src/mr-3/mr-cobrancas/package.json`** (script `test:regressao` estende com novo arquivo):

    Localizar script atual (post-Phase 7.10a):
    ```json
    "test:regressao": "vitest run src/services/__tests__/calculos.test.js src/services/__tests__/saldoAtualizadoCache.test.js src/services/__tests__/custasCache.test.js src/services/__tests__/processosJudiciaisFilter.test.js"
    ```

    Substituir por (acrescenta `agruparPagamentosPorDevedor.test.js` no FIM):
    ```json
    "test:regressao": "vitest run src/services/__tests__/calculos.test.js src/services/__tests__/saldoAtualizadoCache.test.js src/services/__tests__/custasCache.test.js src/services/__tests__/processosJudiciaisFilter.test.js src/services/__tests__/agruparPagamentosPorDevedor.test.js"
    ```

    **NÃO mudar** `name`, `version`, `dependencies`, outros scripts (`dev`, `build`, `test`, `prebuild`).

    **Parte D — Run regressão (validar PASS antes de commit em Tasks 2-4):**
    ```bash
    cd src/mr-3/mr-cobrancas && npm run test:regressao
    ```
    Esperado: `25 passed` (23 base 7.10a + 2 helper).

    **NO COMMIT YET.** Esta task NÃO commita — os 3 arquivos ficam staged via `git status` e serão incluídos no commit 07.10b (Task 2). Razão: helper + test são CRIADOS pra serem usados em 07.10b (Devedores) — pertencem ao mesmo commit atômico. package.json estende test:regressao pra cobrir o helper também.

    **D-01 estrito**: este task NÃO toca motor (devedorCalc.js, pagamentos.js, dividas.js).

    **ZERO `git commit`** nesta task. **ZERO `git push`**. **ZERO bump pai**.
  </action>
  <verify>
    <automated>test -f src/mr-3/mr-cobrancas/src/utils/agruparPagamentosPorDevedor.js &amp;&amp; test -f src/mr-3/mr-cobrancas/src/services/__tests__/agruparPagamentosPorDevedor.test.js &amp;&amp; grep -q "agruparPagamentosPorDevedor.test.js" src/mr-3/mr-cobrancas/package.json &amp;&amp; cd src/mr-3/mr-cobrancas &amp;&amp; npm run test:regressao 2>&amp;1 | tee /tmp/07.10bcd-01-t1-test.log | tail -20 &amp;&amp; grep -qE "Tests +25 passed" /tmp/07.10bcd-01-t1-test.log</automated>
  </verify>
  <acceptance_criteria>
    - **Helper file existe**: `test -f src/mr-3/mr-cobrancas/src/utils/agruparPagamentosPorDevedor.js` returns 0
    - **Helper export presente**: `grep -nE 'export function agruparPagamentosPorDevedor' src/mr-3/mr-cobrancas/src/utils/agruparPagamentosPorDevedor.js` returns `>= 1` match
    - **Helper signature verbatim CONTEXT §2 Fix A**: helper builds dividaIdToDevedorId Map first, then iterates pagamentosDivida looking up via divida_id (`grep -cE "dividaIdToDevedorId" src/mr-3/mr-cobrancas/src/utils/agruparPagamentosPorDevedor.js` returns `>= 2` — declaração + uso)
    - **Test file existe**: `test -f src/mr-3/mr-cobrancas/src/services/__tests__/agruparPagamentosPorDevedor.test.js` returns 0
    - **2 it() blocks**: `grep -cE "^  it\(" src/mr-3/mr-cobrancas/src/services/__tests__/agruparPagamentosPorDevedor.test.js` returns `2`
    - **Test importa helper corretamente**: `grep -nE "import.*agruparPagamentosPorDevedor.*from.*utils/agruparPagamentosPorDevedor" src/mr-3/mr-cobrancas/src/services/__tests__/agruparPagamentosPorDevedor.test.js` returns `>= 1`
    - **package.json script atualizado**: `grep -c "agruparPagamentosPorDevedor.test.js" src/mr-3/mr-cobrancas/package.json` returns `>= 1`
    - **Regressão**: `cd src/mr-3/mr-cobrancas && npm run test:regressao` exits 0 com EXATO `25 passed` — 23 base + 2 helper (verify automatizado agora enforça `Tests +25 passed` exato; mínimo aceitável NÃO é 23, é 25 exato)
    - **NO COMMIT executed**: `cd src/mr-3 && git log --oneline -1` ainda shows `2757f74` (HEAD inalterado)
    - **D-01 estrito**: `cd src/mr-3 && git diff -- mr-cobrancas/src/utils/devedorCalc.js mr-cobrancas/src/services/pagamentos.js mr-cobrancas/src/services/dividas.js` outputs nothing
    - **3 files staged-ready (not committed yet)**: `cd src/mr-3 && git status --short` shows the 3 new/modified files
  </acceptance_criteria>
  <done>
    Helper `agruparPagamentosPorDevedor.js` criado (NEW); test trivial 2 it() criado (NEW); package.json estende test:regressao; regressão 25/25 PASS (23 base + 2 helper); ZERO commit nesta task — arquivos ficam preparados pra Task 2 commitar junto com Fix B em commit atômico 07.10b. D-01 motor 100% intocado.
  </done>
</task>

<task type="auto">
  <name>Task 2: Commit 07.10b — Devedores migra fonte (App.jsx callsite L8468 + Map L2984-2992 usa helper) + sub-PAUSA #1a</name>
  <read_first>
    - `.planning/phases/07.10bcd-migracao-pagamentos-divida-tres-consumidores/07.10bcd-CONTEXT.md` (§2 Fix B + D-30 + D-31 + Shield 24a §7)
    - `src/mr-3/mr-cobrancas/src/App.jsx` (L8460-8500 — switch case `tab` + L2980-3000 — Devedores Map)
    - Output da Task 1 (helper + test + package.json staged-ready)
  </read_first>
  <files>src/mr-3/mr-cobrancas/src/App.jsx (MOD — 2 regions: L8468 callsite + L2984-2992 Map)</files>
  <action>
    Modificar `src/mr-3/mr-cobrancas/src/App.jsx` em 2 regions cirúrgicas, depois COMMITAR commit 07.10b com TODOS os 4 arquivos (App.jsx + helper NEW + helper test NEW + package.json MOD).

    **Edit 1 — L8468 (case "devedores" — prop swap):**

    Linha ATUAL (L8468):
    ```jsx
    case "devedores": return <Devedores devedores={devedores} setDevedores={setDevedores} credores={credores} onModalChange={setModalAberto} user={user} processos={processos} setTab={setTab} allPagamentos={allPagamentos} />;
    ```

    Trocar por (apenas o último prop muda — `allPagamentos={allPagamentos}` → `allPagamentos={allPagamentosDivida}`):
    ```jsx
    case "devedores": return <Devedores devedores={devedores} setDevedores={setDevedores} credores={credores} onModalChange={setModalAberto} user={user} processos={processos} setTab={setTab} allPagamentos={allPagamentosDivida} />;
    ```

    **NOTA naming débito técnico aceito (Q1 decisão planner):** A prop name continua `allPagamentos` (não renomeada para `allPagamentosDivida` no callsite-side dentro do componente Devedores), mas o conteúdo passado é `allPagamentosDivida` state. Phase 7.10e hipotética pode renomear quando legacy for removido.

    **Edit 2 — L2984-2992 (Devedores Map agrupador — usa helper):**

    Bloco ATUAL (L2984-2992 — verbatim):
    ```jsx
    const pgtosPorDevedor = useMemo(() => {
      const m = new Map();
      allPagamentos.forEach(p => {
        const k = String(p.devedor_id);
        if (!m.has(k)) m.set(k, []);
        m.get(k).push(p);
      });
      return m;
    }, [allPagamentos]);
    ```

    Trocar por (verbatim):
    ```jsx
    // Phase 7.10bcd — fonte trocada pra pagamentos_divida (sem devedor_id direto). Helper agrupa via lookup divida_id ∈ dividas-do-devedor.
    const pgtosPorDevedor = useMemo(
      () => agruparPagamentosPorDevedor(devedores, allPagamentos),
      [devedores, allPagamentos]
    );
    ```

    **Edit 3 — Adicionar import do helper no topo do App.jsx** (sanity — verificar primeiro se já existe; se sim, skip; se não, adicionar):

    Procurar `agruparPagamentosPorDevedor` no top-of-file imports. Se ausente, adicionar logo após o último import existente em src/utils/:
    ```jsx
    import { agruparPagamentosPorDevedor } from "./utils/agruparPagamentosPorDevedor.js";
    ```
    (localizar via `grep -n "from \"./utils/" src/mr-3/mr-cobrancas/src/App.jsx | head -5` para encontrar bloco de imports utils existente; adicionar logo após linha relevante)

    **ZERO outras edits em App.jsx**. NÃO mexer em outros `allPagamentos={allPagamentos}` (case "dashboard" L8467 + case "dividas" L8488 — backlog Tasks 3-4). NÃO renomear state. NÃO alterar carregarTudo. NÃO mexer em Dashboard function ou outros componentes.

    **D-01 estrito**: este task NÃO toca motor (devedorCalc.js, pagamentos.js, dividas.js).

    **GATES PRÉ-COMMIT (validar antes de commit 07.10b):**

    **Gate A — Build exit 0:**
    ```bash
    cd src/mr-3/mr-cobrancas && npm run build
    ```

    **Gate B — Regressão 25/25 PASS (M5 regressão por commit):**
    ```bash
    cd src/mr-3/mr-cobrancas && npm run test:regressao
    ```
    Esperado: `25 passed` (23 base + 2 helper).

    **Gate C1 (pré-commit) — D-01 estrito — motor diff VAZIO em todos 3 arquivos (cumulativo desde 2757f74):**
    ```bash
    MOTOR_DIFF=$(cd src/mr-3 && git diff 2757f74..HEAD -- mr-cobrancas/src/utils/devedorCalc.js mr-cobrancas/src/services/pagamentos.js mr-cobrancas/src/services/dividas.js; cd src/mr-3 && git diff -- mr-cobrancas/src/utils/devedorCalc.js mr-cobrancas/src/services/pagamentos.js mr-cobrancas/src/services/dividas.js)
    [ -z "$MOTOR_DIFF" ] && echo "Gate C1: OK (motor 100% intocado pré + cumulativo)" || echo "Gate C1: FAIL (motor tocado — scope creep)"
    ```
    Esperado: `OK`. Se `FAIL` → ABORT, NÃO COMMITAR.

    **Gate Shield 24a — grep callsite + filter pattern App.jsx case "devedores":**
    ```bash
    # Positivo — `allPagamentos={allPagamentosDivida}` ≥ 1 match no case "devedores"
    POS=$(awk '/case "devedores":/{flag=1; n=0} flag{print; n++; if (n>3) flag=0}' src/mr-3/mr-cobrancas/src/App.jsx | grep -F 'allPagamentos={allPagamentosDivida}' | wc -l)
    [ "$POS" -ge 1 ] && echo "Shield24a positivo: OK ($POS match)" || echo "Shield24a positivo: FAIL ($POS, esperado >=1)"

    # Negativo regional — case "devedores" + 10 linhas seguintes NÃO contém allPagamentos={allPagamentos} legacy
    NEG=$(awk '/case "devedores":/{flag=1; n=0} flag{print; n++; if (n>10) flag=0}' src/mr-3/mr-cobrancas/src/App.jsx | grep -F 'allPagamentos={allPagamentos}' | wc -l)
    [ "$NEG" -eq 0 ] && echo "Shield24a negativo: OK (0 matches)" || echo "Shield24a negativo: FAIL ($NEG matches — legacy ainda no case 'devedores')"

    # Helper presente em Devedores Map
    HELPER=$(grep -nE 'agruparPagamentosPorDevedor\(devedores' src/mr-3/mr-cobrancas/src/App.jsx | wc -l)
    [ "$HELPER" -ge 1 ] && echo "Shield24a helper: OK ($HELPER match)" || echo "Shield24a helper: FAIL ($HELPER, esperado >=1)"
    ```
    Esperado: 3 OKs.

    **Gate F — git status check:**
    ```bash
    cd src/mr-3 && git status --short
    ```
    Esperado: 4 arquivos:
    - `M  mr-cobrancas/src/App.jsx`
    - `??  mr-cobrancas/src/utils/agruparPagamentosPorDevedor.js`
    - `??  mr-cobrancas/src/services/__tests__/agruparPagamentosPorDevedor.test.js`
    - `M  mr-cobrancas/package.json`

    **Se TODOS os gates pré-commit passarem — criar commit ATÔMICO 07.10b:**

    ```bash
    cd src/mr-3 && git add \
      mr-cobrancas/src/App.jsx \
      mr-cobrancas/src/utils/agruparPagamentosPorDevedor.js \
      mr-cobrancas/src/services/__tests__/agruparPagamentosPorDevedor.test.js \
      mr-cobrancas/package.json

    cd src/mr-3 && git commit -m "$(cat <<'EOF'
feat(07.10b): Devedores lê pagamentos_divida (Map agrupador via helper compartilhado)

Migra 1º dos 3 consumidores fast-track (D-30 commit atômico bisect-able):
- Fix A (D-31): helper novo agruparPagamentosPorDevedor.js (compartilhado
  com Dashboard em commit 07.10d) + test trivial 2 it() (devedor com
  pagamentos múltiplos + boundary devedor sem dívidas)
- Fix B: App.jsx L8468 callsite swap (case "devedores":
  allPagamentos={allPagamentos} → allPagamentos={allPagamentosDivida})
- App.jsx L2984-2992 Devedores Map: forEach manual (p.devedor_id direto)
  → chamada agruparPagamentosPorDevedor(devedores, allPagamentos) via
  lookup intermediário divida_id (pagamentos_divida sem devedor_id direto)
- package.json test:regressao estende com novo helper test
- Naming débito técnico aceito (Q1): prop name `allPagamentos` mantida
  no callsite-side (conteúdo é pagamentos_divida pós-swap); rename
  global em phase 7.10e hipotética

Shield 24a: positivo callsite + negativo regional (zero legacy no case
"devedores" + 10 linhas) + helper grep em Devedores Map.

D-01 estrito: motor 100% intocado (devedorCalc.js + pagamentos.js +
dividas.js — cumulativo desde 2757f74).
D-29: motor calcularSaldoDevedorAtualizado lê apenas data_pagamento +
valor — shape pagamentos_divida compatível.
D-28: drift histórico CONTINUA aceito por design — pagamentos novos via
UI legacy "Registrar pagamento parcial" ainda escrevem em
pagamentos_parciais; phase 7.10e hipotética cortará writes.

Regressão 25/25 PASS (23 base 7.10a + 2 helper test). Próximos commits:
07.10c (ModuloDividas + sub-componentes) e 07.10d (Dashboard) — fast-track
3 consumidores em 1 phase com mitigations M1-M6 não-negociáveis.
EOF
)"
    ```

    **Gate C2 (pós-commit) — D-01 estrito — motor NÃO no commit (usa --name-only, lição 7.8.2a 608c124):**
    ```bash
    cd src/mr-3 && git log -1 --format="" --name-only | grep -qE "utils/devedorCalc\.js|services/pagamentos\.js|services/dividas\.js" && echo "Gate C2: FAIL (motor no commit)" || echo "Gate C2: OK"
    ```
    Esperado: `OK`. Se `FAIL` → scope creep → `git reset HEAD~1` + investigar + recommit limpo.

    **Reportar SHA do commit 07.10b:**
    ```bash
    cd src/mr-3 && git log -1 --oneline
    cd src/mr-3 && git diff HEAD~1 HEAD --stat
    ```
    Esperado: 4 files (App.jsx + helper NEW + helper test NEW + package.json).

    **Commit message convention (CONTEXT.md §2 Fix B):**
    Mensagem header DEVE começar exatamente com:
    `feat(07.10b): Devedores lê pagamentos_divida (Map agrupador via helper compartilhado)`

    **Se ANY Gate FAIL antes do commit**: HALT immediately. Report failure. Do not commit. Do not fix-forward (próximas tasks dependem deste estado verde).

    **ZERO `git push`**. **ZERO bump pai**. **ZERO criar tag**.

    ---

    **SUB-PAUSA #1a (checkpoint:human-verify gate="blocking")** — após commit 07.10b criado:

    **<what-built>**
    Commit 07.10b atômico criado no submódulo:
    - Helper `agruparPagamentosPorDevedor.js` (NEW) + test 2 it() (NEW)
    - App.jsx L8468 callsite swap (case "devedores")
    - App.jsx L2984-2992 Devedores Map usa helper
    - package.json test:regressao estende com helper test
    - 4 files no commit. Motor 100% intocado (D-01 cumulativo C1+C2).

    **<how-to-verify>**
    Usuário valida em ordem:
    1. **Regressão 25/25 PASS:**
       ```bash
       cd src/mr-3/mr-cobrancas && npm run test:regressao 2>&1 | tail -5
       ```
       Esperado: `Tests 25 passed`.
    2. **Shield 24a grep evidence:**
       ```bash
       awk '/case "devedores":/{flag=1; n=0} flag{print; n++; if (n>10) flag=0}' src/mr-3/mr-cobrancas/src/App.jsx | grep -F 'allPagamentos='
       ```
       Esperado: 1 match `allPagamentos={allPagamentosDivida}` + 0 matches `allPagamentos={allPagamentos}`.
    3. **Helper grep:**
       ```bash
       grep -nE 'agruparPagamentosPorDevedor\(devedores' src/mr-3/mr-cobrancas/src/App.jsx
       ```
       Esperado: ≥1 match no Devedores Map.
    4. **Commit message + SHA:**
       ```bash
       cd src/mr-3 && git log -1 --oneline
       ```
       Esperado: SHA novo + `feat(07.10b): Devedores lê pagamentos_divida (Map agrupador via helper compartilhado)`.
    5. **Reporte:** confirmar regressão verde + Shield 24a OK + commit 07.10b correto.

    **<resume-signal>**
    Digite `APROVADO` SE regressão 25/25 + Shield 24a OK + commit message correto. Caso contrário, responda `FAIL: <sintoma>` (e.g., `FAIL: Shield24a negativo casa em legacy ainda`, `FAIL: regressão 24/25 — agruparPagamentosPorDevedor.test.js falhou`). Em caso de FAIL, executor aborta sem prosseguir pra Task 3 (não pode acumular regressão silenciosa).
  </action>
  <verify>
    <automated>cd src/mr-3 &amp;&amp; git log -1 --oneline | grep -qE "feat\(07\.10b\): Devedores lê pagamentos_divida" &amp;&amp; cd src/mr-3 &amp;&amp; git log -1 --format="" --name-only | grep -qE "utils/devedorCalc\.js|services/pagamentos\.js|services/dividas\.js" &amp;&amp; { echo "Gate C2 FAIL"; exit 1; } || echo "Gate C2 OK + commit 07.10b created"</automated>
  </verify>
  <acceptance_criteria>
    - **Gate A** — Build exits 0
    - **Gate B** — Regressão 25/25 PASS (M5 — esperado exato 25 passed; mínimo aceitável 23 passed)
    - **Gate C1 (pré-commit)** — `git diff` em motor files VAZIO (cumulativo + working tree)
    - **Gate C2 (pós-commit)** — `git log -1 --format="" --name-only` NÃO casa em motor files (`--name-only`, não `--stat`)
    - **Gate Shield 24a** — 3 sub-checks OK: positivo callsite (≥1 match) + negativo regional (0 matches) + helper grep (≥1 match)
    - **Gate F** — git status pre-commit shows 4 files (App.jsx + helper + helper test + package.json)
    - **Commit message** — `cd src/mr-3 && git log -1 --oneline` shows `feat(07.10b): Devedores lê pagamentos_divida (Map agrupador via helper compartilhado)`
    - **4 files no commit** — `cd src/mr-3 && git diff HEAD~1 HEAD --name-only | wc -l` returns `4`
    - **Sub-PAUSA #1a aprovada** — usuário respondeu `APROVADO` explicitamente; regressão + Shield + commit todos verdes
    - **Helper import em App.jsx** — `grep -nE "import.*agruparPagamentosPorDevedor.*from.*utils/agruparPagamentosPorDevedor" src/mr-3/mr-cobrancas/src/App.jsx` returns `>= 1`
    - **Devedores Map usa helper** — `grep -nE 'pgtosPorDevedor.*useMemo.*agruparPagamentosPorDevedor\(devedores' src/mr-3/mr-cobrancas/src/App.jsx` (multiline) returns evidence (or equivalent multi-grep)
    - **Outros consumers AINDA em legacy (sanity — atomicidade)** — `awk '/case "dashboard":/{flag=1; n=0} flag{print; n++; if (n>3) flag=0}' src/mr-3/mr-cobrancas/src/App.jsx | grep -F 'allPagamentos={allPagamentos}' | wc -l` returns `>= 1` (case "dashboard" ainda legacy — Task 4 vai corrigir)
    - **D-01 estrito** — Gate C1 + C2 ambos OK
    - ZERO `git push` executado
    - ZERO bump de submódulo no pai
    - ZERO criação de tag
  </acceptance_criteria>
  <done>
    Commit 07.10b atômico criado no submódulo com 4 files (App.jsx + helper NEW + helper test NEW + package.json); message exato; gates A/B/C1/C2/Shield24a/F todos verdes; sub-PAUSA #1a APROVADA pelo usuário; regressão 25/25 PASS; motor 100% intocado cumulativo. Estado verde pra Task 3 prosseguir com commit 07.10c.
  </done>
</task>

<task type="auto">
  <name>Task 3: Commit 07.10c — ModuloDividas + sub-componentes (App.jsx callsite L8488 + TabelaDividas L41-44 + DetalheDivida L74) + sub-PAUSA #1b</name>
  <read_first>
    - `.planning/phases/07.10bcd-migracao-pagamentos-divida-tres-consumidores/07.10bcd-CONTEXT.md` (§2 Fix C + D-30 + D-27 Set lookup pattern + Shield 24b §7)
    - `src/mr-3/mr-cobrancas/src/App.jsx` (L8483-8493 — case "dividas")
    - `src/mr-3/mr-cobrancas/src/components/ModuloDividas.jsx` (todo — confirmar pass-through ZERO edição)
    - `src/mr-3/mr-cobrancas/src/components/TabelaDividas.jsx` (L38-46 buildDevedorObjParaSaldo)
    - `src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx` (L70-78 filter)
    - Output da Task 2 (commit 07.10b já criado, sub-PAUSA #1a APROVADA)
  </read_first>
  <files>
    - src/mr-3/mr-cobrancas/src/App.jsx (MOD — L8488 callsite)
    - src/mr-3/mr-cobrancas/src/components/TabelaDividas.jsx (MOD — L41-44)
    - src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx (MOD — L74)
    - ModuloDividas.jsx — ZERO edição (pass-through inalterado, prop name mantida)
  </files>
  <action>
    Modificar 3 arquivos em paralelo (callsite + 2 sub-componentes filter), depois COMMITAR commit 07.10c atômico com 3 files.

    **Edit 1 — App.jsx L8488 (case "dividas" — prop swap):**

    Linha ATUAL (L8488):
    ```jsx
    allPagamentos={allPagamentos}
    ```

    Trocar por:
    ```jsx
    allPagamentos={allPagamentosDivida}
    ```

    Contexto antes/depois (L8483-8493) DEVE ficar assim após o edit:
    ```jsx
    case "dividas": return (
      <ModuloDividas
        allDividas={allDividas}
        devedores={devedores}
        credores={credores}
        allPagamentos={allPagamentosDivida}
        hoje={hoje_app}
        onCarregarTudo={carregarTudo}
        setTab={setTab}
      />
    );
    ```

    **NOTA:** prop name `allPagamentos` no callsite continua nominal — conteúdo é `allPagamentosDivida` state. ModuloDividas pass-through propaga para TabelaDividas/DetalheDivida com mesmo nome (D-debt aceito Q1).

    **Edit 2 — ModuloDividas.jsx — ZERO EDIÇÃO** (sanity verification):

    Antes de prosseguir, confirmar que ModuloDividas.jsx é pass-through inalterado:
    ```bash
    grep -cE "allPagamentos\.(filter|forEach|reduce|map)" src/mr-3/mr-cobrancas/src/components/ModuloDividas.jsx
    ```
    Esperado: `0` matches (zero cálculos diretos). Se ≥1 → ABORT, scope review necessário (CONTEXT.md afirma pass-through; deviation requer reabrir).

    **Edit 3 — TabelaDividas.jsx L41-46 (buildDevedorObjParaSaldo refactor — D-27 Set lookup pattern):**

    Bloco ATUAL (L41-46 — verbatim):
    ```jsx
    function buildDevedorObjParaSaldo(divida, devedores, allPagamentos) {
      const devedor = devedores.find(d => String(d.id) === String(divida.devedor_id));
      if (!devedor) return null;
      const pagamentosDoDevedor = allPagamentos.filter(p => String(p.devedor_id) === String(divida.devedor_id));
      return { devedor, pagamentos: pagamentosDoDevedor };
    }
    ```

    Trocar por (verbatim — D-27 pattern):
    ```jsx
    function buildDevedorObjParaSaldo(divida, devedores, allPagamentos) {
      const devedor = devedores.find(d => String(d.id) === String(divida.devedor_id));
      if (!devedor) return null;
      // Phase 7.10bcd — fonte trocada pra pagamentos_divida (sem devedor_id direto). Filter via Set lookup divida_id ∈ dividas-do-devedor (D-27 pattern reutilizável).
      const dividaIdsDoDevedor = new Set((devedor.dividas || []).map(d => String(d.id)));
      const pagamentosDoDevedor = allPagamentos.filter(p => dividaIdsDoDevedor.has(String(p.divida_id)));
      return { devedor, pagamentos: pagamentosDoDevedor };
    }
    ```

    Diferenças exatas:
    - Linha removida: `const pagamentosDoDevedor = allPagamentos.filter(p => String(p.devedor_id) === String(divida.devedor_id));`
    - Linhas adicionadas (3): comment PT-BR + `const dividaIdsDoDevedor = new Set(...)` + `const pagamentosDoDevedor = allPagamentos.filter(p => dividaIdsDoDevedor.has(String(p.divida_id)));`

    **Edit 4 — DetalheDivida.jsx L73-74 (filter refactor — D-27 Set lookup pattern):**

    Bloco ATUAL (L73-74 — verbatim):
    ```jsx
    const devedor = devedores.find(d => String(d.id) === String(divida.devedor_id));
    const pagamentosDoDevedor = allPagamentos.filter(p => String(p.devedor_id) === String(divida.devedor_id));
    ```

    Trocar por (verbatim — D-27 pattern):
    ```jsx
    const devedor = devedores.find(d => String(d.id) === String(divida.devedor_id));
    // Phase 7.10bcd — fonte trocada pra pagamentos_divida (sem devedor_id direto). Filter via Set lookup divida_id ∈ dividas-do-devedor (D-27 pattern reutilizável).
    const dividaIdsDoDevedor = new Set((devedor?.dividas || []).map(d => String(d.id)));
    const pagamentosDoDevedor = allPagamentos.filter(p => dividaIdsDoDevedor.has(String(p.divida_id)));
    ```

    Diferenças exatas:
    - Linha removida: `const pagamentosDoDevedor = allPagamentos.filter(p => String(p.devedor_id) === String(divida.devedor_id));`
    - Linhas adicionadas (3): comment PT-BR + `const dividaIdsDoDevedor = new Set(...)` (com optional chaining `devedor?.dividas` porque devedor pode ser undefined em DetalheDivida) + filter via has(divida_id)

    **NOTA:** DetalheDivida usa `devedor?.dividas` (optional chaining) porque `devedor = devedores.find(...)` pode retornar undefined; TabelaDividas usa `devedor.dividas` direto porque já passou no early-return `if (!devedor) return null;` na linha 43.

    **ZERO outras edits**. NÃO mexer em outros callsites App.jsx (case "dashboard" L8467 — Task 4). NÃO mexer em ModuloDividas.jsx. NÃO importar nada novo nos sub-componentes (Set é builtin).

    **D-01 estrito**: este task NÃO toca motor (devedorCalc.js, pagamentos.js, dividas.js).

    **GATES PRÉ-COMMIT (validar antes de commit 07.10c):**

    **Gate A — Build exit 0:**
    ```bash
    cd src/mr-3/mr-cobrancas && npm run build
    ```

    **Gate B — Regressão 25/25 PASS (M5 regressão por commit):**
    ```bash
    cd src/mr-3/mr-cobrancas && npm run test:regressao
    ```
    Esperado: `25 passed` cumulativo (23 base + 2 helper).

    **Gate C1 (pré-commit) — D-01 estrito — motor diff cumulativo VAZIO desde 2757f74:**
    ```bash
    MOTOR_DIFF=$(cd src/mr-3 && git diff 2757f74..HEAD -- mr-cobrancas/src/utils/devedorCalc.js mr-cobrancas/src/services/pagamentos.js mr-cobrancas/src/services/dividas.js; cd src/mr-3 && git diff -- mr-cobrancas/src/utils/devedorCalc.js mr-cobrancas/src/services/pagamentos.js mr-cobrancas/src/services/dividas.js)
    [ -z "$MOTOR_DIFF" ] && echo "Gate C1: OK" || echo "Gate C1: FAIL"
    ```
    Esperado: `OK`.

    **Gate Shield 24b — 3 sub-greps:**
    ```bash
    # Sub-grep 1: case "dividas" callsite swap (positivo + negativo regional)
    POS=$(awk '/case "dividas":/{flag=1; n=0} flag{print; n++; if (n>10) flag=0}' src/mr-3/mr-cobrancas/src/App.jsx | grep -F 'allPagamentos={allPagamentosDivida}' | wc -l)
    NEG=$(awk '/case "dividas":/{flag=1; n=0} flag{print; n++; if (n>10) flag=0}' src/mr-3/mr-cobrancas/src/App.jsx | grep -F 'allPagamentos={allPagamentos}' | wc -l)
    [ "$POS" -ge 1 ] && [ "$NEG" -eq 0 ] && echo "Shield24b case dividas: OK" || echo "Shield24b case dividas: FAIL (POS=$POS NEG=$NEG)"

    # Sub-grep 2: TabelaDividas filter via Set lookup
    TAB=$(grep -nE 'dividaIdsDoDevedor' src/mr-3/mr-cobrancas/src/components/TabelaDividas.jsx | wc -l)
    [ "$TAB" -ge 2 ] && echo "Shield24b TabelaDividas: OK ($TAB)" || echo "Shield24b TabelaDividas: FAIL ($TAB, esperado >=2)"

    # Sub-grep 3: DetalheDivida filter via Set lookup
    DET=$(grep -nE 'dividaIdsDoDevedor' src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx | wc -l)
    [ "$DET" -ge 2 ] && echo "Shield24b DetalheDivida: OK ($DET)" || echo "Shield24b DetalheDivida: FAIL ($DET, esperado >=2)"

    # Sub-grep 4: padrão legacy AUSENTE em ambos sub-componentes
    LEG=$(grep -nE 'p\.devedor_id\s*===\s*.*\.devedor_id' src/mr-3/mr-cobrancas/src/components/TabelaDividas.jsx src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx | wc -l)
    [ "$LEG" -eq 0 ] && echo "Shield24b legacy ausente: OK" || echo "Shield24b legacy ausente: FAIL ($LEG matches — pattern legacy ainda presente)"

    # Sub-grep 5: ModuloDividas ZERO mudança (pass-through preservado)
    MOD=$(cd src/mr-3 && git diff HEAD -- mr-cobrancas/src/components/ModuloDividas.jsx | wc -l)
    [ "$MOD" -eq 0 ] && echo "Shield24b ModuloDividas pass-through: OK (zero diff)" || echo "Shield24b ModuloDividas: FAIL (diff $MOD lines — esperado pass-through inalterado)"
    ```
    Esperado: 5 OKs.

    **Gate F — git status check:**
    ```bash
    cd src/mr-3 && git status --short
    ```
    Esperado: 3 arquivos modificados:
    - `M  mr-cobrancas/src/App.jsx`
    - `M  mr-cobrancas/src/components/TabelaDividas.jsx`
    - `M  mr-cobrancas/src/components/DetalheDivida.jsx`

    **Se TODOS os gates pré-commit passarem — criar commit ATÔMICO 07.10c:**

    ```bash
    cd src/mr-3 && git add \
      mr-cobrancas/src/App.jsx \
      mr-cobrancas/src/components/TabelaDividas.jsx \
      mr-cobrancas/src/components/DetalheDivida.jsx

    cd src/mr-3 && git commit -m "$(cat <<'EOF'
feat(07.10c): ModuloDividas + sub-componentes leem pagamentos_divida (Set lookup divida_id)

Migra 2º dos 3 consumidores fast-track (D-30 commit atômico bisect-able):
- Fix C: App.jsx L8488 callsite swap (case "dividas":
  allPagamentos={allPagamentos} → allPagamentos={allPagamentosDivida})
- ModuloDividas.jsx: ZERO edição (pass-through inalterado, prop name
  `allPagamentos` mantida — conteúdo é pagamentos_divida pós-callsite swap;
  débito técnico naming aceito Q1)
- TabelaDividas.jsx L41-44 buildDevedorObjParaSaldo: refactor filter
  legacy (p.devedor_id === divida.devedor_id) → Set lookup divida_id ∈
  dividas-do-devedor (D-27 pattern reutilizável 7.10a)
- DetalheDivida.jsx L74: refactor mesmo pattern (com optional chaining
  devedor?.dividas porque devedor pode ser undefined neste contexto)

Shield 24b: case "dividas" positivo + negativo regional + greps em
TabelaDividas + DetalheDivida (filter pattern dividaIdsDoDevedor presente;
legacy p.devedor_id === ...devedor_id ausente em ambos) + ModuloDividas
zero diff (pass-through preservado).

D-01 estrito: motor 100% intocado (devedorCalc.js + pagamentos.js +
dividas.js — cumulativo desde 2757f74).
D-27: Set lookup inline replicado (3 callsites com 07.10d ainda — vira
helper se ≥5 phases futuras).
D-29: motor calcularSaldoDevedorAtualizado lê apenas data_pagamento +
valor — shape pagamentos_divida compatível.
D-28: drift histórico CONTINUA aceito por design.

Regressão 25/25 PASS cumulativo. Próximo commit: 07.10d (Dashboard
agregados — helper agruparPagamentosPorDevedor + Set lookup inline em
ultimosPagamentos).
EOF
)"
    ```

    **Gate C2 (pós-commit) — D-01 estrito — motor NÃO no commit:**
    ```bash
    cd src/mr-3 && git log -1 --format="" --name-only | grep -qE "utils/devedorCalc\.js|services/pagamentos\.js|services/dividas\.js" && echo "Gate C2: FAIL" || echo "Gate C2: OK"
    ```

    **Reportar SHA do commit 07.10c:**
    ```bash
    cd src/mr-3 && git log -1 --oneline
    cd src/mr-3 && git diff HEAD~1 HEAD --stat
    ```
    Esperado: 3 files (App.jsx + TabelaDividas + DetalheDivida).

    **Commit message convention (CONTEXT.md §2 Fix C):**
    Mensagem header DEVE começar exatamente com:
    `feat(07.10c): ModuloDividas + sub-componentes leem pagamentos_divida (Set lookup divida_id)`

    **Se ANY Gate FAIL antes do commit**: HALT immediately. Report failure. Do not commit.

    **ZERO `git push`**. **ZERO bump pai**. **ZERO criar tag**.

    ---

    **SUB-PAUSA #1b (checkpoint:human-verify gate="blocking")** — após commit 07.10c criado:

    **<what-built>**
    Commit 07.10c atômico criado no submódulo:
    - App.jsx L8488 callsite swap (case "dividas")
    - ModuloDividas.jsx ZERO edição (pass-through preservado)
    - TabelaDividas.jsx L41-44 Set lookup pattern D-27
    - DetalheDivida.jsx L74 Set lookup pattern D-27 (com optional chaining)
    - 3 files no commit. Motor 100% intocado (D-01 cumulativo C1+C2).
    - Cadeia atual: 07.10b → 07.10c (2 commits sequenciais sobre baseline 2757f74).

    **<how-to-verify>**
    Usuário valida em ordem:
    1. **Regressão 25/25 PASS:**
       ```bash
       cd src/mr-3/mr-cobrancas && npm run test:regressao 2>&1 | tail -5
       ```
       Esperado: `Tests 25 passed`.
    2. **Shield 24b grep evidence (5 sub-greps):**
       ```bash
       awk '/case "dividas":/{flag=1; n=0} flag{print; n++; if (n>10) flag=0}' src/mr-3/mr-cobrancas/src/App.jsx | grep -F 'allPagamentos='
       grep -nE 'dividaIdsDoDevedor' src/mr-3/mr-cobrancas/src/components/TabelaDividas.jsx
       grep -nE 'dividaIdsDoDevedor' src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx
       grep -nE 'p\.devedor_id\s*===\s*.*\.devedor_id' src/mr-3/mr-cobrancas/src/components/TabelaDividas.jsx src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx
       ```
       Esperados: 1 match `allPagamentos={allPagamentosDivida}` + 0 legacy / ≥2 dividaIdsDoDevedor em cada arquivo / 0 legacy filter pattern.
    3. **ModuloDividas pass-through preservado:**
       ```bash
       cd src/mr-3 && git diff HEAD~2 HEAD -- mr-cobrancas/src/components/ModuloDividas.jsx | wc -l
       ```
       Esperado: 0 (zero diff cumulativo).
    4. **Cadeia commits:**
       ```bash
       cd src/mr-3 && git log 2757f74..HEAD --oneline
       ```
       Esperado: 2 commits sequenciais (07.10b primeiro, 07.10c segundo).
    5. **Reporte:** confirmar regressão verde + Shield 24b OK + commit 07.10c correto + cadeia 07.10b→07.10c.

    **<resume-signal>**
    Digite `APROVADO` SE regressão 25/25 + Shield 24b 5/5 OK + commit message correto + cadeia 2 commits. Caso contrário, responda `FAIL: <sintoma>`. Em caso de FAIL, executor aborta sem prosseguir pra Task 4.
  </action>
  <verify>
    <automated>cd src/mr-3 &amp;&amp; git log -1 --oneline | grep -qE "feat(07.10c): ModuloDividas + sub-componentes" &amp;&amp; cd src/mr-3 &amp;&amp; { git log -1 --format="" --name-only | grep -qE "utils/devedorCalc.js|services/pagamentos.js|services/dividas.js" &amp;&amp; { echo "Gate C2 FAIL"; exit 1; } || echo "Gate C2 OK + commit 07.10c created + cadeia 2 commits"; }</automated>
  </verify>
  <acceptance_criteria>
    - **Gate A** — Build exits 0
    - **Gate B** — Regressão 25/25 PASS (M5)
    - **Gate C1 (pré-commit)** — `git diff 2757f74..HEAD` em motor files VAZIO + working tree também vazio
    - **Gate C2 (pós-commit)** — `git log -1 --format="" --name-only` NÃO casa em motor files
    - **Gate Shield 24b** — 5 sub-checks OK: case "dividas" positivo + negativo + TabelaDividas dividaIdsDoDevedor ≥2 + DetalheDivida dividaIdsDoDevedor ≥2 + legacy pattern ausente em ambos + ModuloDividas zero diff cumulativo
    - **Gate F** — git status pre-commit shows 3 files (App.jsx + TabelaDividas + DetalheDivida)
    - **Commit message** — `cd src/mr-3 && git log -1 --oneline` shows `feat(07.10c): ModuloDividas + sub-componentes leem pagamentos_divida (Set lookup divida_id)`
    - **3 files no commit** — `cd src/mr-3 && git diff HEAD~1 HEAD --name-only | wc -l` returns `3`
    - **Cadeia commits 2 (07.10b + 07.10c)** — `cd src/mr-3 && git log 2757f74..HEAD --oneline | wc -l` returns `2`
    - **ModuloDividas pass-through** — `cd src/mr-3 && git diff 2757f74..HEAD -- mr-cobrancas/src/components/ModuloDividas.jsx | wc -l` returns `0`
    - **Sub-PAUSA #1b aprovada** — usuário respondeu `APROVADO` explicitamente
    - **Outros consumers AINDA em legacy (sanity)** — `awk '/case "dashboard":/{flag=1; n=0} flag{print; n++; if (n>3) flag=0}' src/mr-3/mr-cobrancas/src/App.jsx | grep -F 'allPagamentos={allPagamentos}' | wc -l` returns `>= 1` (case "dashboard" ainda legacy — Task 4 vai corrigir)
    - **D-01 estrito cumulativo** — Gate C1 + C2 ambos OK
    - ZERO `git push` executado
    - ZERO bump de submódulo no pai
    - ZERO criação de tag
  </acceptance_criteria>
  <done>
    Commit 07.10c atômico criado no submódulo com 3 files (App.jsx + TabelaDividas + DetalheDivida); message exato; ModuloDividas pass-through preservado (zero diff); gates A/B/C1/C2/Shield24b/F todos verdes; sub-PAUSA #1b APROVADA pelo usuário; regressão 25/25 PASS cumulativo; motor 100% intocado cumulativo. Cadeia 2 commits (07.10b→07.10c). Estado verde pra Task 4 prosseguir com commit 07.10d.
  </done>
</task>

<task type="auto">
  <name>Task 4: Commit 07.10d — Dashboard agregados (App.jsx callsite L8467 + Dashboard L568 helper + L696 Set lookup) + sub-PAUSA #1c</name>
  <read_first>
    - `.planning/phases/07.10bcd-migracao-pagamentos-divida-tres-consumidores/07.10bcd-CONTEXT.md` (§2 Fix D + D-30 + D-31 helper reuse + D-27 Set lookup + Shield 24c §7)
    - `src/mr-3/mr-cobrancas/src/App.jsx` (L562-720 — Dashboard function + L8467 callsite)
    - Output da Task 3 (commit 07.10c já criado, sub-PAUSA #1b APROVADA, cadeia 2 commits)
  </read_first>
  <files>src/mr-3/mr-cobrancas/src/App.jsx (MOD — L8467 callsite + L566-574 Dashboard Map helper + L696 Set lookup inline)</files>
  <action>
    Modificar `src/mr-3/mr-cobrancas/src/App.jsx` em 3 regions cirúrgicas (callsite + Dashboard Map helper + ultimosPagamentos Set lookup), depois COMMITAR commit 07.10d atômico com 1 file.

    **Edit 1 — L8467 (case "dashboard" — prop swap):**

    Linha ATUAL (L8467):
    ```jsx
    case "dashboard": return <Dashboard devedores={devedores} processos={processos} andamentos={andamentos} user={user} lembretes={lembretesList} allPagamentos={allPagamentos} />;
    ```

    Trocar por (apenas o último prop muda):
    ```jsx
    case "dashboard": return <Dashboard devedores={devedores} processos={processos} andamentos={andamentos} user={user} lembretes={lembretesList} allPagamentos={allPagamentosDivida} />;
    ```

    **Edit 2 — L566-574 (Dashboard pgtosPorDevedorCarteira Map — usa helper):**

    Bloco ATUAL (L566-574 — verbatim):
    ```jsx
    const pgtosPorDevedorCarteira = useMemo(() => {
      const m = new Map();
      allPagamentos.forEach(p => {
        const k = String(p.devedor_id);
        if (!m.has(k)) m.set(k, []);
        m.get(k).push(p);
      });
      return m;
    }, [allPagamentos]);
    ```

    Trocar por (verbatim — usa helper compartilhado D-31):
    ```jsx
    // Phase 7.10bcd — fonte trocada pra pagamentos_divida (sem devedor_id direto). Helper compartilhado agrupa via lookup divida_id ∈ dividas-do-devedor (mesmo de Devedores Map L2986).
    const pgtosPorDevedorCarteira = useMemo(
      () => agruparPagamentosPorDevedor(devedores, allPagamentos),
      [devedores, allPagamentos]
    );
    ```

    **NOTA D-29 — L612-615 reduce + L665-670 filter dataInicio: ZERO mudança.**
    
    Verificar que NÃO foi alterado (só lê `p.valor` e `p.data_pagamento` que existem em pagamentos_divida — D-29 motor compat):
    - L613: `allPagamentos.reduce((s, p) => s + (parseFloat(p.valor) || 0), 0)` — INALTERADO
    - L667: `allPagamentos.filter(p => (p.data_pagamento || "") >= dataInicio)` — INALTERADO

    **Edit 3 — L690-701 (ultimosPagamentos pgtosDev Set lookup inline — D-27 pattern):**

    Bloco ATUAL (L690-701 — verbatim):
    ```jsx
    const ultimosPagamentos = useMemo(() => {
      return [...pagamentosNoPeriodo]
        .sort((a, b) => (b.data_pagamento || "").localeCompare(a.data_pagamento || ""))
        .slice(0, 5)
        .map(p => {
          const dev = devedores.find(d => String(d.id) === String(p.devedor_id));
          const pgtosDev = allPagamentos.filter(pp => String(pp.devedor_id) === String(p.devedor_id));
          const totalPago = pgtosDev.reduce((s, pp) => s + (parseFloat(pp.valor) || 0), 0);
          const saldoDev = calcularSaldoDevedorAtualizado(dev || {}, pgtosDev, hoje);
          return { ...p, nomeDevedor: dev?.nome || "—", saldoRestante: saldoDev };
        });
    }, [pagamentosNoPeriodo, devedores, allPagamentos, hoje]);
    ```

    **NOTA semantic shift importante:** No legacy, `p.devedor_id` (do pagamento que está sendo iterado em `.map`) é usado para encontrar `dev` E para filtrar `pgtosDev`. No shape novo, `p.divida_id` (do pagamento iterado) NÃO dá direto o `devedor_id` — precisa lookup intermediário. Mantemos a semântica usando o helper `dividaIdToDevedorId` localmente OU usando o `dev` encontrado depois — opção mais limpa: usar o `dev` (já encontrado) + Set lookup inline pra filtrar pgtosDev.

    Trocar por (verbatim — D-27 Set lookup inline + lookup intermediário pra `dev` resolvido via helper map ou inline):
    ```jsx
    const ultimosPagamentos = useMemo(() => {
      // Phase 7.10bcd — pagamentos_divida sem devedor_id direto. Lookup intermediário divida_id → devedor_id via helper map; pgtosDev filter via Set lookup divida_id ∈ dividas-do-devedor (D-27).
      const dividaIdToDevedorId = new Map();
      devedores.forEach(d => (d.dividas || []).forEach(div => dividaIdToDevedorId.set(String(div.id), String(d.id))));
      return [...pagamentosNoPeriodo]
        .sort((a, b) => (b.data_pagamento || "").localeCompare(a.data_pagamento || ""))
        .slice(0, 5)
        .map(p => {
          const devId = dividaIdToDevedorId.get(String(p.divida_id));
          const dev = devId ? devedores.find(d => String(d.id) === devId) : null;
          const dividaIdsDoDevedor = new Set((dev?.dividas || []).map(d => String(d.id)));
          const pgtosDev = allPagamentos.filter(pp => dividaIdsDoDevedor.has(String(pp.divida_id)));
          const totalPago = pgtosDev.reduce((s, pp) => s + (parseFloat(pp.valor) || 0), 0);
          const saldoDev = calcularSaldoDevedorAtualizado(dev || {}, pgtosDev, hoje);
          return { ...p, nomeDevedor: dev?.nome || "—", saldoRestante: saldoDev };
        });
    }, [pagamentosNoPeriodo, devedores, allPagamentos, hoje]);
    ```

    Diferenças exatas:
    - Linha removida: `const dev = devedores.find(d => String(d.id) === String(p.devedor_id));`
    - Linha removida: `const pgtosDev = allPagamentos.filter(pp => String(pp.devedor_id) === String(p.devedor_id));`
    - Linhas adicionadas: comment PT-BR + lookup map intermediário (no escopo do useMemo, não dentro do .map — performance) + dev lookup via devId resolved + Set lookup inline + filter via has(divida_id)

    **NOTA Q3 (recuperadoPeriodo L677-686):** Esta sub-region tem outro `dev = devedores.find(d => String(d.id) === String(p.devedor_id))` (L682). Mesmo problema — `p.devedor_id` não existe em pagamentos_divida. Aplicar mesmo pattern de lookup intermediário:

    **Edit 4 — L677-686 (recuperadoPeriodo lookup intermediário — D-27 pattern):**

    Bloco ATUAL (L677-686 — verbatim):
    ```jsx
    const recuperadoPeriodo = useMemo(() => {
      // Soma pagamentos do período para devedores com status de pagamento total
      const statusPago = new Set(["pago_integral", "recuperado"]);
      return pagamentosNoPeriodo
        .filter(p => {
          const dev = devedores.find(d => String(d.id) === String(p.devedor_id));
          return dev && statusPago.has(dev.status);
        })
        .reduce((s, p) => s + (parseFloat(p.valor) || 0), 0);
    }, [pagamentosNoPeriodo, devedores]);
    ```

    Trocar por (verbatim — lookup intermediário via Map):
    ```jsx
    const recuperadoPeriodo = useMemo(() => {
      // Soma pagamentos do período para devedores com status de pagamento total
      // Phase 7.10bcd — pagamentos_divida sem devedor_id direto. Lookup intermediário divida_id → devedor_id (D-27).
      const statusPago = new Set(["pago_integral", "recuperado"]);
      const dividaIdToDevedorId = new Map();
      devedores.forEach(d => (d.dividas || []).forEach(div => dividaIdToDevedorId.set(String(div.id), String(d.id))));
      return pagamentosNoPeriodo
        .filter(p => {
          const devId = dividaIdToDevedorId.get(String(p.divida_id));
          const dev = devId ? devedores.find(d => String(d.id) === devId) : null;
          return dev && statusPago.has(dev.status);
        })
        .reduce((s, p) => s + (parseFloat(p.valor) || 0), 0);
    }, [pagamentosNoPeriodo, devedores]);
    ```

    **Edit 5 — L8425-8433 (parent App pgtosPorDevedorCarteira — usa helper, Fix D-extra):**

    Bloco ATUAL (L8425-8433 — verbatim — parent App scope, FORA da function Dashboard interna):
    ```jsx
    const pgtosPorDevedorCarteira = useMemo(() => {
      const m = new Map();
      allPagamentos.forEach(p => {
        const k = String(p.devedor_id);
        if (!m.has(k)) m.set(k, []);
        m.get(k).push(p);
      });
      return m;
    }, [allPagamentos]);
    ```

    Trocar por (Phase 7.10bcd Fix D-extra — usa helper compartilhado, fonte allPagamentosDivida):
    ```jsx
    const pgtosPorDevedorCarteira = useMemo(
      () => agruparPagamentosPorDevedor(devedores, allPagamentosDivida),
      [devedores, allPagamentosDivida]
    );
    ```

    **Importante**: este Map é DISTINTO do interno Dashboard L566 (que recebe `allPagamentos` como prop). Aqui é parent App scope, lê state `allPagamentosDivida` direto (já existe state desde Phase 7.3, L8325). Esta é a 4ª callsite identificada via grep global de `allPagamentos.forEach` (lição feedback_grep_global_before_pointwise_fix.md aplicada — 4 callsites Map agrupador no total: L568 Dashboard interno, L2986 Devedores, L8425 parent App pgtosPorDevedorCarteira, e o L8427 que é DENTRO do mesmo Map L8425).

    O Map alimenta `totalCarteira` em L8434-8440 → exibido em L8735 como card 'Carteira' do header GLOBAL (visível em TODAS as tabs, não apenas Dashboard). Sem mudança no consumer downstream — apenas troca de fonte do Map.

    Import do helper já adicionado em commit 07.10b (Task 2 Edit 3) — não precisa re-adicionar.

    **ZERO outras edits em App.jsx**. NÃO mexer em outros consumers (já migrados em 07.10b/c). NÃO mexer em totalRecuperadoGlobal L612-615 (reduce só p.valor — D-29 OK). NÃO mexer em pagamentosNoPeriodo L665-670 (filter por p.data_pagamento — D-29 OK).

    **D-01 estrito**: este task NÃO toca motor (devedorCalc.js, pagamentos.js, dividas.js).

    **GATES PRÉ-COMMIT (validar antes de commit 07.10d):**

    **Gate A — Build exit 0:**
    ```bash
    cd src/mr-3/mr-cobrancas && npm run build
    ```

    **Gate B — Regressão 25/25 PASS (M5):**
    ```bash
    cd src/mr-3/mr-cobrancas && npm run test:regressao
    ```

    **Gate C1 (pré-commit) — D-01 estrito — motor diff cumulativo VAZIO desde 2757f74:**
    ```bash
    MOTOR_DIFF=$(cd src/mr-3 && git diff 2757f74..HEAD -- mr-cobrancas/src/utils/devedorCalc.js mr-cobrancas/src/services/pagamentos.js mr-cobrancas/src/services/dividas.js; cd src/mr-3 && git diff -- mr-cobrancas/src/utils/devedorCalc.js mr-cobrancas/src/services/pagamentos.js mr-cobrancas/src/services/dividas.js)
    [ -z "$MOTOR_DIFF" ] && echo "Gate C1: OK" || echo "Gate C1: FAIL"
    ```

    **Gate Shield 24c — case "dashboard" callsite + helper grep + Set lookup inline:**
    ```bash
    # Sub-grep 1: case "dashboard" callsite swap (positivo + negativo regional)
    POS=$(awk '/case "dashboard":/{flag=1; n=0} flag{print; n++; if (n>3) flag=0}' src/mr-3/mr-cobrancas/src/App.jsx | grep -F 'allPagamentos={allPagamentosDivida}' | wc -l)
    NEG=$(awk '/case "dashboard":/{flag=1; n=0} flag{print; n++; if (n>10) flag=0}' src/mr-3/mr-cobrancas/src/App.jsx | grep -F 'allPagamentos={allPagamentos}' | wc -l)
    [ "$POS" -ge 1 ] && [ "$NEG" -eq 0 ] && echo "Shield24c case dashboard: OK" || echo "Shield24c case dashboard: FAIL (POS=$POS NEG=$NEG)"

    # Sub-grep 2: Helper presente em Dashboard pgtosPorDevedorCarteira (≥2 callsites do helper em App.jsx — Devedores L2986 + Dashboard L568)
    HELP=$(grep -nE 'agruparPagamentosPorDevedor\(devedores' src/mr-3/mr-cobrancas/src/App.jsx | wc -l)
    [ "$HELP" -ge 2 ] && echo "Shield24c helper: OK ($HELP callsites)" || echo "Shield24c helper: FAIL ($HELP, esperado >=2)"

    # Sub-grep 3: Set lookup inline em Dashboard ultimosPagamentos + recuperadoPeriodo (≥2 ocorrências em App.jsx esperadas)
    SET=$(grep -nE 'dividaIdsDoDevedor' src/mr-3/mr-cobrancas/src/App.jsx | wc -l)
    [ "$SET" -ge 1 ] && echo "Shield24c Set inline: OK ($SET)" || echo "Shield24c Set inline: FAIL ($SET, esperado >=1)"

    # Sub-grep 4: Padrão legacy AUSENTE em Dashboard region (L562-720)
    LEG_DASH=$(awk 'NR>=562 && NR<=720' src/mr-3/mr-cobrancas/src/App.jsx | grep -E 'p\.devedor_id|pp\.devedor_id' | wc -l)
    [ "$LEG_DASH" -eq 0 ] && echo "Shield24c legacy ausente Dashboard: OK" || echo "Shield24c legacy: FAIL ($LEG_DASH matches em Dashboard region)"
    ```
    Esperado: 4 OKs.

    **Gate Shield 24d — parent App L8425 pgtosPorDevedorCarteira usa helper (verificação granular adicional Fix D-extra):**
    ```bash
    # Shield 24d positivo: 2 Maps pgtosPorDevedorCarteira useMemo (Dashboard L566 + parent App L8425)
    POS_24D=$(grep -nE 'pgtosPorDevedorCarteira.*=.*useMemo' src/mr-3/mr-cobrancas/src/App.jsx | wc -l)
    [ "$POS_24D" -eq 2 ] && echo "Shield24d positivo (2 Maps): OK" || echo "Shield24d positivo: FAIL ($POS_24D, esperado 2)"

    # Shield 24d helper: ambos Maps usam agruparPagamentosPorDevedor (≥2 matches do helper dentro de blocos pgtosPorDevedorCarteira)
    HELP_24D=$(awk '/^  const pgtosPorDevedorCarteira = useMemo/{flag=1; n=0} flag{print; n++; if (n>15) flag=0}' src/mr-3/mr-cobrancas/src/App.jsx | grep -F 'agruparPagamentosPorDevedor' | wc -l)
    [ "$HELP_24D" -ge 2 ] && echo "Shield24d helper em ambos Maps: OK ($HELP_24D)" || echo "Shield24d helper: FAIL ($HELP_24D, esperado >=2)"

    # Shield 24d negativo: ZERO forEach manual em qualquer pgtosPorDevedorCarteira (legacy pattern eliminado)
    LEG_24D=$(awk '/^  const pgtosPorDevedorCarteira = useMemo/{flag=1; n=0} flag{print; n++; if (n>15) flag=0}' src/mr-3/mr-cobrancas/src/App.jsx | grep -F 'forEach' | wc -l)
    [ "$LEG_24D" -eq 0 ] && echo "Shield24d negativo (zero forEach legacy): OK" || echo "Shield24d negativo: FAIL ($LEG_24D matches forEach manual)"
    ```
    Esperado: 3 OKs (2 Maps + helper em ambos + zero forEach legacy).

    **Gate F — git status check:**
    ```bash
    cd src/mr-3 && git status --short
    ```
    Esperado: 1 arquivo modificado:
    - `M  mr-cobrancas/src/App.jsx`

    **Se TODOS os gates pré-commit passarem — criar commit ATÔMICO 07.10d:**

    ```bash
    cd src/mr-3 && git add mr-cobrancas/src/App.jsx

    cd src/mr-3 && git commit -m "$(cat <<'EOF'
feat(07.10d): Dashboard agregados leem pagamentos_divida (helper + Set lookup inline)

Migra 3º (último) dos consumidores fast-track (D-30 commit atômico bisect-able):
- Fix D: App.jsx L8467 callsite swap (case "dashboard":
  allPagamentos={allPagamentos} → allPagamentos={allPagamentosDivida})
- App.jsx Dashboard L566-574 pgtosPorDevedorCarteira: forEach manual
  (p.devedor_id direto) → chamada agruparPagamentosPorDevedor helper
  compartilhado (mesmo de Devedores Map em commit 07.10b — D-31 reuse)
- App.jsx Dashboard L612-615 totalRecuperadoGlobal: ZERO mudança (lê
  só p.valor — D-29 motor compat)
- App.jsx Dashboard L665-670 pagamentosNoPeriodo: ZERO mudança (filter
  só p.data_pagamento — D-29)
- App.jsx Dashboard L677-686 recuperadoPeriodo: refactor lookup
  intermediário divida_id → devedor_id via Map local (D-27 pattern;
  inline porque escopo limitado ao useMemo)
- App.jsx Dashboard L690-701 ultimosPagamentos: refactor lookup
  intermediário + Set lookup inline pra pgtosDev (D-27 pattern)

Shield 24c: case "dashboard" positivo + negativo regional + helper
agruparPagamentosPorDevedor presente em ≥2 callsites (Devedores +
Dashboard) + Set lookup inline em Dashboard region + padrão legacy
p.devedor_id ausente na Dashboard region (L562-720).

D-01 estrito: motor 100% intocado (devedorCalc.js + pagamentos.js +
dividas.js — cumulativo desde 2757f74).
D-31: helper agruparPagamentosPorDevedor reusado (DRY > inline; 2
callsites + complexidade da lookup map justificam helper compartilhado;
lição "helper-first" 7.9).
D-27: Set lookup inline replicado em 3 callsites (TabelaDividas L44 +
DetalheDivida L74 + Dashboard L696). Total: ainda inline OK (3 ≤ 5);
≥5 viraria helper.
D-29: motor calcularSaldoDevedorAtualizado lê apenas data_pagamento +
valor — shape pagamentos_divida compatível.
D-28: drift histórico CONTINUA aceito por design — pagamentos novos via
UI legacy não aparecem em NENHUM consumer pós-7.10bcd. Phase 7.10e
hipotética cortará writes.

Regressão 25/25 PASS cumulativo. Cadeia 3 commits sequenciais
(07.10b → 07.10c → 07.10d) — D-30 atomicidade + bisect-able.
Plan 02 pendente: UAT 7 SCs cross-check + bump pai.
EOF
)"
    ```

    **Gate C2 (pós-commit) — D-01 estrito — motor NÃO no commit:**
    ```bash
    cd src/mr-3 && git log -1 --format="" --name-only | grep -qE "utils/devedorCalc\.js|services/pagamentos\.js|services/dividas\.js" && echo "Gate C2: FAIL" || echo "Gate C2: OK"
    ```

    **Gate D-30 (commits atômicos cumulativo) — EXATAMENTE 3 commits sequenciais com headers feat(07.10b/c/d):**
    ```bash
    COMMITS=$(cd src/mr-3 && git log 2757f74..HEAD --format="%s" | wc -l)
    [ "$COMMITS" -eq 3 ] && echo "D-30 count: OK (3 commits)" || echo "D-30 count: FAIL ($COMMITS, esperado 3)"

    cd src/mr-3 && git log 2757f74..HEAD --format="%s" | grep -cE "^feat\(07\.10[bcd]\):" | grep -q "^3$" && echo "D-30 headers: OK" || echo "D-30 headers: FAIL"
    ```

    **Reportar SHA do commit 07.10d:**
    ```bash
    cd src/mr-3 && git log -1 --oneline
    cd src/mr-3 && git log 2757f74..HEAD --oneline
    cd src/mr-3 && git diff HEAD~1 HEAD --stat
    ```
    Esperado: 1 file (App.jsx) + cadeia 3 commits.

    **Commit message convention (CONTEXT.md §2 Fix D):**
    Mensagem header DEVE começar exatamente com:
    `feat(07.10d): Dashboard agregados leem pagamentos_divida (helper + Set lookup inline)`

    **Se ANY Gate FAIL antes do commit**: HALT immediately. Report failure. Do not commit.

    **ZERO `git push`**. **ZERO bump pai**. **ZERO criar tag**.

    ---

    **SUB-PAUSA #1c (checkpoint:human-verify gate="blocking")** — após commit 07.10d criado + Plan 01 gates finais cumulativos:

    **<what-built>**
    Commit 07.10d atômico criado no submódulo:
    - App.jsx L8467 callsite swap (case "dashboard")
    - App.jsx Dashboard L568 pgtosPorDevedorCarteira usa helper compartilhado D-31
    - App.jsx Dashboard L613/L667 inalterados (D-29 motor compat)
    - App.jsx Dashboard L696 pgtosDev refactor inline Set lookup D-27
    - App.jsx Dashboard L682 recuperadoPeriodo refactor lookup intermediário D-27
    - 1 file no commit. Motor 100% intocado (D-01 cumulativo C1+C2).
    - **Cadeia FINAL 3 commits: 07.10b → 07.10c → 07.10d (D-30 atomicidade bisect-able).**

    **<how-to-verify>**
    Usuário valida em ordem (Plan 01 gates finais cumulativos):
    1. **Build exit 0:**
       ```bash
       cd src/mr-3/mr-cobrancas && npm run build
       ```
    2. **Regressão 25/25 PASS final:**
       ```bash
       cd src/mr-3/mr-cobrancas && npm run test:regressao 2>&1 | tail -5
       ```
       Esperado: `Tests 25 passed` cumulativo.
    3. **Gate C1 cumulativo D-01 estrito (motor diff vazio desde baseline):**
       ```bash
       cd src/mr-3 && git diff 2757f74..HEAD -- mr-cobrancas/src/utils/devedorCalc.js mr-cobrancas/src/services/pagamentos.js mr-cobrancas/src/services/dividas.js
       ```
       Esperado: VAZIO (zero linhas).
    4. **Gate C2 cumulativo D-01 estrito (motor NÃO em nenhum dos 3 commits):**
       ```bash
       cd src/mr-3 && git log 2757f74..HEAD --format="" --name-only | sort -u | grep -E "utils/devedorCalc\.js|services/pagamentos\.js|services/dividas\.js"
       ```
       Esperado: VAZIO (zero matches).
    5. **Gate D-30 cumulativo (EXATAMENTE 3 commits sequenciais):**
       ```bash
       cd src/mr-3 && git log 2757f74..HEAD --oneline
       ```
       Esperado: 3 linhas, mais recente = `feat(07.10d)`, depois `feat(07.10c)`, depois `feat(07.10b)`.
    6. **Shields 24a + 24b + 24c todos OK final cumulativo:**
       ```bash
       # 24a — case "devedores"
       awk '/case "devedores":/{flag=1; n=0} flag{print; n++; if (n>10) flag=0}' src/mr-3/mr-cobrancas/src/App.jsx | grep -F 'allPagamentos='
       # 24b — case "dividas" + sub-componentes
       awk '/case "dividas":/{flag=1; n=0} flag{print; n++; if (n>10) flag=0}' src/mr-3/mr-cobrancas/src/App.jsx | grep -F 'allPagamentos='
       grep -nE 'dividaIdsDoDevedor' src/mr-3/mr-cobrancas/src/components/TabelaDividas.jsx src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx
       # 24c — case "dashboard"
       awk '/case "dashboard":/{flag=1; n=0} flag{print; n++; if (n>10) flag=0}' src/mr-3/mr-cobrancas/src/App.jsx | grep -F 'allPagamentos='
       # Helper em ≥2 callsites em App.jsx
       grep -nE 'agruparPagamentosPorDevedor\(devedores' src/mr-3/mr-cobrancas/src/App.jsx
       ```
       Esperados: 1 match `allPagamentosDivida` + 0 matches `allPagamentos` legacy em CADA case (devedores, dividas, dashboard); ≥2 dividaIdsDoDevedor em cada sub-componente; ≥2 callsites helper em App.jsx.
    7. **Files cumulativos no chain (7 arquivos esperados, contando ModuloDividas zero diff):**
       ```bash
       cd src/mr-3 && git log 2757f74..HEAD --name-only --format="" | sort -u | grep -v "^$"
       ```
       Esperado: 6 paths (helper NEW + helper test NEW + App.jsx + TabelaDividas + DetalheDivida + package.json). ModuloDividas NÃO aparece (pass-through preservado).
    8. **Reporte final:** confirmar build + regressão 25/25 + Gates C1/C2 cumulativos + D-30 3 commits + Shields 24a/b/c todos OK + files cumulativos.

    **<resume-signal>**
    Digite `APROVADO` SE:
    - Build exit 0
    - Regressão 25/25 PASS cumulativo
    - Gate C1 cumulativo motor diff VAZIO desde 2757f74
    - Gate C2 cumulativo motor NÃO em nenhum dos 3 commits
    - Gate D-30 EXATAMENTE 3 commits sequenciais (feat(07.10b), feat(07.10c), feat(07.10d))
    - Shields 24a + 24b + 24c todos OK
    - 6 files cumulativos no chain (App.jsx + TabelaDividas + DetalheDivida + package.json + helper NEW + helper test NEW; ModuloDividas zero diff preservado)

    Caso contrário, responda `FAIL: <sintoma>`. Em caso de FAIL Plan 01 incompleto, executor aborta sem prosseguir pra Tasks 5-6 (Plan 01 gates finais não satisfeitos = phase não pode entrar em Plan 02 UAT).
  </action>
  <verify>
    <automated>cd src/mr-3 &amp;&amp; git log -1 --oneline | grep -qE "feat\(07\.10d\): Dashboard agregados" &amp;&amp; { COMMITS=$(git log 2757f74..HEAD --oneline | wc -l); [ "$COMMITS" -eq 3 ]; } &amp;&amp; { MOTOR=$(git log 2757f74..HEAD --format="" --name-only | sort -u | grep -E "utils/devedorCalc\.js|services/pagamentos\.js|services/dividas\.js" | wc -l); [ "$MOTOR" -eq 0 ]; } &amp;&amp; echo "Plan 01 final gates OK — 3 commits + D-01 cumulativo"</automated>
  </verify>
  <acceptance_criteria>
    - **Gate A** — Build exits 0
    - **Gate B** — Regressão 25/25 PASS (M5 cumulativo)
    - **Gate C1 (pré-commit) cumulativo** — `git diff 2757f74..HEAD` em motor files VAZIO
    - **Gate C2 (pós-commit) cumulativo** — `git log 2757f74..HEAD --format="" --name-only` NÃO casa em motor files (lição 7.8.2a 608c124)
    - **Gate Shield 24c** — 4 sub-checks OK: case "dashboard" positivo + negativo regional + helper ≥2 callsites + Set lookup inline ≥1 + legacy ausente em Dashboard region
    - **Gate Shield 24d** — 3 sub-checks OK (Fix D-extra parent App L8425): 2 Maps pgtosPorDevedorCarteira useMemo (Dashboard L566 + parent App L8425) + ambos usam helper agruparPagamentosPorDevedor + ZERO forEach manual em qualquer pgtosPorDevedorCarteira
    - **Gate D-30 cumulativo** — `cd src/mr-3 && git log 2757f74..HEAD --format="%s" | grep -cE "^feat\(07\.10[bcd]\):"` returns `3`; commits sequenciais
    - **Gate F** — git status pre-commit shows 1 file (App.jsx)
    - **Commit message** — `cd src/mr-3 && git log -1 --oneline` shows `feat(07.10d): Dashboard agregados leem pagamentos_divida (helper + Set lookup inline)`
    - **1 file no commit** — `cd src/mr-3 && git diff HEAD~1 HEAD --name-only | wc -l` returns `1`
    - **Cadeia FINAL 3 commits** — `cd src/mr-3 && git log 2757f74..HEAD --oneline | wc -l` returns `3`
    - **Files cumulativos no chain** — `cd src/mr-3 && git log 2757f74..HEAD --name-only --format="" | sort -u | grep -v "^$" | wc -l` returns `6` (App.jsx + TabelaDividas + DetalheDivida + package.json + helper NEW + helper test NEW)
    - **ModuloDividas pass-through** — `cd src/mr-3 && git diff 2757f74..HEAD -- mr-cobrancas/src/components/ModuloDividas.jsx | wc -l` returns `0`
    - **Sub-PAUSA #1c aprovada** — usuário respondeu `APROVADO` explicitamente após Plan 01 gates finais cumulativos
    - **Helper presente em ≥2 callsites em App.jsx** — `grep -cE 'agruparPagamentosPorDevedor\(devedores' src/mr-3/mr-cobrancas/src/App.jsx` returns `>= 2` (Devedores + Dashboard)
    - **D-01 estrito cumulativo** — Gate C1 + C2 ambos OK desde baseline 2757f74
    - ZERO `git push` executado
    - ZERO bump de submódulo no pai
    - ZERO criação de tag
  </acceptance_criteria>
  <done>
    Commit 07.10d atômico criado no submódulo com 1 file (App.jsx); message exato; gates A/B/C1/C2/Shield24c/D-30/F todos verdes cumulativos; sub-PAUSA #1c APROVADA pelo usuário; cadeia FINAL 3 commits sequenciais (07.10b → 07.10c → 07.10d) D-30 atomicidade; regressão 25/25 PASS cumulativo; motor 100% intocado cumulativo desde 2757f74; 6 files no chain (helper NEW + helper test NEW + App.jsx + TabelaDividas + DetalheDivida + package.json) + ModuloDividas pass-through preservado.
  </done>
</task>

<task type="auto">
  <name>Task 5: Gates finais cumulativos consolidados (Plan 01 ship readiness)</name>
  <read_first>
    - Outputs das Tasks 1-4 (3 commits SHAs + sub-PAUSAs aprovadas)
    - `.planning/phases/07.10bcd-migracao-pagamentos-divida-tres-consumidores/07.10bcd-CONTEXT.md` (§5 SC + §7 Shield greps + §11 D-34 atomicidade)
    - `memory/feedback_gate_motor_grep_use_name_only.md` (Gate C2 usa --name-only)
  </read_first>
  <files>(none — só validação cumulativa)</files>
  <action>
    Executar todos os gates finais consolidados (cumulativos pós-3-commits) como sanity final pré-Plan 02. Reportar matriz consolidada de gates pra usuário.

    **Gate Final 1 — Build exit 0:**
    ```bash
    cd src/mr-3/mr-cobrancas && npm run build
    ```
    Esperado: exit 0.

    **Gate Final 2 — Regressão 25/25 PASS final cumulativo:**
    ```bash
    cd src/mr-3/mr-cobrancas && npm run test:regressao 2>&1 | tee /tmp/07.10bcd-01-final-test.log | tail -5
    grep -qE "Tests +(25|2[6-9]|[3-9][0-9]) passed|25 passed|2[6-9] passed|[3-9][0-9] passed" /tmp/07.10bcd-01-final-test.log && echo "Gate Final 2: OK" || echo "Gate Final 2: FAIL"
    ```
    Esperado: `Tests 25 passed` cumulativo.

    **Gate Final C1 (D-01 strict cumulative) — motor diff VAZIO desde 2757f74:**
    ```bash
    cd src/mr-3 && git diff 2757f74..HEAD -- \
      mr-cobrancas/src/utils/devedorCalc.js \
      mr-cobrancas/src/services/pagamentos.js \
      mr-cobrancas/src/services/dividas.js | wc -l
    # Esperado: 0
    ```

    **Gate Final C2 (D-01 strict cumulative) — motor NÃO no chain dos 3 commits:**
    ```bash
    cd src/mr-3 && git log 2757f74..HEAD --format="" --name-only | sort -u | grep -E "utils/devedorCalc\.js|services/pagamentos\.js|services/dividas\.js" | wc -l
    # Esperado: 0
    ```

    **Gate Final D-30 (commits atômicos cumulativo) — EXATAMENTE 3 commits sequenciais:**
    ```bash
    COUNT=$(cd src/mr-3 && git log 2757f74..HEAD --oneline | wc -l)
    [ "$COUNT" -eq 3 ] && echo "D-30 count: OK (3 commits)" || echo "D-30 count: FAIL ($COUNT)"

    HEADERS=$(cd src/mr-3 && git log 2757f74..HEAD --format="%s" | grep -cE "^feat\(07\.10[bcd]\):")
    [ "$HEADERS" -eq 3 ] && echo "D-30 headers: OK (3 feat headers)" || echo "D-30 headers: FAIL ($HEADERS)"

    # Verbatim verify per Task spec
    cd src/mr-3 && git log 2757f74..HEAD --format="%s" | sort
    # Esperado (sorted):
    # feat(07.10b): Devedores lê pagamentos_divida (Map agrupador via helper compartilhado)
    # feat(07.10c): ModuloDividas + sub-componentes leem pagamentos_divida (Set lookup divida_id)
    # feat(07.10d): Dashboard agregados leem pagamentos_divida (helper + Set lookup inline)
    ```

    **Gate Final Shield 24a/b/c todos OK final cumulativo:**
    ```bash
    # Shield 24a — case "devedores"
    POS_A=$(awk '/case "devedores":/{flag=1; n=0} flag{print; n++; if (n>3) flag=0}' src/mr-3/mr-cobrancas/src/App.jsx | grep -F 'allPagamentos={allPagamentosDivida}' | wc -l)
    NEG_A=$(awk '/case "devedores":/{flag=1; n=0} flag{print; n++; if (n>10) flag=0}' src/mr-3/mr-cobrancas/src/App.jsx | grep -F 'allPagamentos={allPagamentos}' | wc -l)
    [ "$POS_A" -ge 1 ] && [ "$NEG_A" -eq 0 ] && echo "Shield24a: OK" || echo "Shield24a: FAIL"

    # Shield 24b — case "dividas" + sub-componentes
    POS_B=$(awk '/case "dividas":/{flag=1; n=0} flag{print; n++; if (n>10) flag=0}' src/mr-3/mr-cobrancas/src/App.jsx | grep -F 'allPagamentos={allPagamentosDivida}' | wc -l)
    NEG_B=$(awk '/case "dividas":/{flag=1; n=0} flag{print; n++; if (n>10) flag=0}' src/mr-3/mr-cobrancas/src/App.jsx | grep -F 'allPagamentos={allPagamentos}' | wc -l)
    SUB=$(grep -cE 'dividaIdsDoDevedor' src/mr-3/mr-cobrancas/src/components/TabelaDividas.jsx src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx | awk -F: '{ s+=$2 } END { print s }')
    LEG_SUB=$(grep -cE 'p\.devedor_id\s*===\s*.*\.devedor_id' src/mr-3/mr-cobrancas/src/components/TabelaDividas.jsx src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx | awk -F: '{ s+=$2 } END { print s }')
    [ "$POS_B" -ge 1 ] && [ "$NEG_B" -eq 0 ] && [ "$SUB" -ge 4 ] && [ "$LEG_SUB" -eq 0 ] && echo "Shield24b: OK" || echo "Shield24b: FAIL (POS=$POS_B NEG=$NEG_B SUB=$SUB LEG=$LEG_SUB)"

    # Shield 24c — case "dashboard"
    POS_C=$(awk '/case "dashboard":/{flag=1; n=0} flag{print; n++; if (n>3) flag=0}' src/mr-3/mr-cobrancas/src/App.jsx | grep -F 'allPagamentos={allPagamentosDivida}' | wc -l)
    NEG_C=$(awk '/case "dashboard":/{flag=1; n=0} flag{print; n++; if (n>10) flag=0}' src/mr-3/mr-cobrancas/src/App.jsx | grep -F 'allPagamentos={allPagamentos}' | wc -l)
    HELP=$(grep -cE 'agruparPagamentosPorDevedor\(devedores' src/mr-3/mr-cobrancas/src/App.jsx)
    [ "$POS_C" -ge 1 ] && [ "$NEG_C" -eq 0 ] && [ "$HELP" -ge 2 ] && echo "Shield24c: OK" || echo "Shield24c: FAIL (POS=$POS_C NEG=$NEG_C HELP=$HELP)"

    # Shield 24d — parent App L8425 pgtosPorDevedorCarteira (Fix D-extra granular)
    POS_24D=$(grep -nE 'pgtosPorDevedorCarteira.*=.*useMemo' src/mr-3/mr-cobrancas/src/App.jsx | wc -l)
    HELP_24D=$(awk '/^  const pgtosPorDevedorCarteira = useMemo/{flag=1; n=0} flag{print; n++; if (n>15) flag=0}' src/mr-3/mr-cobrancas/src/App.jsx | grep -F 'agruparPagamentosPorDevedor' | wc -l)
    LEG_24D=$(awk '/^  const pgtosPorDevedorCarteira = useMemo/{flag=1; n=0} flag{print; n++; if (n>15) flag=0}' src/mr-3/mr-cobrancas/src/App.jsx | grep -F 'forEach' | wc -l)
    [ "$POS_24D" -eq 2 ] && [ "$HELP_24D" -ge 2 ] && [ "$LEG_24D" -eq 0 ] && echo "Shield24d: OK" || echo "Shield24d: FAIL (POS=$POS_24D HELP=$HELP_24D LEG=$LEG_24D)"
    ```

    **NOTA grep global pre-execute (lição feedback_grep_global_before_pointwise_fix.md aplicada):** Phase 7.10bcd cobre 4 callsites de Map agrupador identificadas via grep global de `allPagamentos.forEach` (L568 Dashboard interno, L2986 Devedores, L8425 parent App pgtosPorDevedorCarteira, e o L8427 que é DENTRO do mesmo Map L8425).

    **Gate Final Files cumulativos (6 paths esperados):**
    ```bash
    cd src/mr-3 && git log 2757f74..HEAD --name-only --format="" | sort -u | grep -v "^$"
    ```
    Esperado (sorted):
    ```
    mr-cobrancas/package.json
    mr-cobrancas/src/App.jsx
    mr-cobrancas/src/components/DetalheDivida.jsx
    mr-cobrancas/src/components/TabelaDividas.jsx
    mr-cobrancas/src/services/__tests__/agruparPagamentosPorDevedor.test.js
    mr-cobrancas/src/utils/agruparPagamentosPorDevedor.js
    ```
    6 paths. ModuloDividas.jsx NÃO listado (pass-through preservado).

    **Gate Final Working tree clean:**
    ```bash
    cd src/mr-3 && git status --short
    ```
    Esperado: VAZIO (working tree limpo pós 3 commits).

    **Reportar matriz consolidada Plan 01 final:**
    ```
    ┌────────────────────────────────────────────┬──────────┐
    │ Gate                                        │ Status   │
    ├────────────────────────────────────────────┼──────────┤
    │ Build exit 0                                │ <OK/FAIL>│
    │ Regressão 25/25 PASS cumulativo             │ <OK/FAIL>│
    │ C1 cumulativo D-01 (motor diff vazio)       │ <OK/FAIL>│
    │ C2 cumulativo D-01 (motor não no chain)     │ <OK/FAIL>│
    │ D-30 EXATAMENTE 3 commits sequenciais       │ <OK/FAIL>│
    │ Shield 24a (case "devedores")               │ <OK/FAIL>│
    │ Shield 24b (case "dividas" + sub-comp.)     │ <OK/FAIL>│
    │ Shield 24c (case "dashboard")               │ <OK/FAIL>│
    │ Helper em ≥2 callsites em App.jsx           │ <OK/FAIL>│
    │ ModuloDividas pass-through (zero diff)      │ <OK/FAIL>│
    │ Files cumulativos = 6                       │ <OK/FAIL>│
    │ Working tree clean                          │ <OK/FAIL>│
    └────────────────────────────────────────────┴──────────┘
    ```

    **Se TODOS verdes** → Plan 01 ship-ready, prosseguir Task 6 (SUMMARY).

    **Se ANY FAIL** → HALT. Reportar matriz com sintomas. Usuário decide se rollback total (D-34 atomicidade — `git -C src/mr-3 reset --hard 2757f74`) ou fix-forward (se trivial <5min). Plan 02 NÃO pode iniciar com gates falhos.

    **ZERO `git push`**. **ZERO bump pai**. **ZERO criar tag** nesta task.
  </action>
  <verify>
    <automated>cd src/mr-3 &amp;&amp; { COUNT=$(git log 2757f74..HEAD --oneline | wc -l); [ "$COUNT" -eq 3 ]; } &amp;&amp; { MOTOR_DIFF=$(git diff 2757f74..HEAD -- mr-cobrancas/src/utils/devedorCalc.js mr-cobrancas/src/services/pagamentos.js mr-cobrancas/src/services/dividas.js | wc -l); [ "$MOTOR_DIFF" -eq 0 ]; } &amp;&amp; { MOTOR_NAMES=$(git log 2757f74..HEAD --format="" --name-only | sort -u | grep -E "utils/devedorCalc\.js|services/pagamentos\.js|services/dividas\.js" | wc -l); [ "$MOTOR_NAMES" -eq 0 ]; } &amp;&amp; { FILES=$(git log 2757f74..HEAD --name-only --format="" | sort -u | grep -v "^$" | wc -l); [ "$FILES" -eq 6 ]; } &amp;&amp; cd src/mr-3/mr-cobrancas &amp;&amp; npm run test:regressao 2>&amp;1 | tail -5 | grep -qE "25 passed|2[6-9] passed|[3-9][0-9] passed" &amp;&amp; echo "Plan 01 ship-ready"</automated>
  </verify>
  <acceptance_criteria>
    - **Build** — exit 0 final cumulativo
    - **Regressão** — 25/25 PASS final cumulativo (M5 — passou após CADA um dos 3 commits + final)
    - **Gate C1 cumulativo** — `git diff 2757f74..HEAD` em 3 motor files VAZIO
    - **Gate C2 cumulativo** — `git log 2757f74..HEAD --format="" --name-only | sort -u` NÃO casa em motor files
    - **Gate D-30 cumulativo** — EXATAMENTE 3 commits sequenciais com headers exatos `feat(07.10b)`, `feat(07.10c)`, `feat(07.10d)`
    - **Shields 24a/b/c/d todos OK** final cumulativo (15 sub-checks no total — 3 cases × {positivo + negativo} + 4 sub-componente checks + helper grep + Shield 24d 3 sub-checks parent App pgtosPorDevedorCarteira)
    - **Helper em ≥2 callsites** — grep `agruparPagamentosPorDevedor\(devedores` em App.jsx returns ≥2
    - **ModuloDividas pass-through** — `git diff 2757f74..HEAD -- ModuloDividas.jsx` returns 0 lines
    - **Files cumulativos** — 6 paths (helper NEW + helper test NEW + App.jsx + TabelaDividas + DetalheDivida + package.json)
    - **Working tree clean** — `git status --short` vazio
    - **Matriz consolidada reportada** ao usuário (12 gates listados)
    - ZERO `git push` executado
    - ZERO bump de submódulo no pai
    - ZERO criação de tag
  </acceptance_criteria>
  <done>
    Plan 01 ship-ready: matriz consolidada de gates finais cumulativos toda verde (12 gates); 3 commits atômicos sequenciais (07.10b → 07.10c → 07.10d) confirmados; D-01 estrito cumulativo; D-30 atomicidade cumulativa; D-31 helper reutilizado em 2 callsites; D-27 Set lookup replicado em 3 callsites + 1 inline em Dashboard; D-29 motor compat preservado; D-28 drift histórico documentado. Estado verde pra Task 6 (SUMMARY) prosseguir.
  </done>
</task>

<task type="auto">
  <name>Task 6: Criar 07.10bcd-01-SUMMARY.md (left uncommitted — Plan 02 Task 4 commits)</name>
  <read_first>
    - `.planning/phases/07.10bcd-migracao-pagamentos-divida-tres-consumidores/07.10bcd-CONTEXT.md` (§4 D-30..D-34 + §5 SC + §10 notas implementação + §11 backlog)
    - Outputs de Tasks 1-5 (SHAs dos 3 commits, gates evidence, files cumulativos)
  </read_first>
  <files>.planning/phases/07.10bcd-migracao-pagamentos-divida-tres-consumidores/07.10bcd-01-SUMMARY.md (NOVO — left uncommitted in parent)</files>
  <action>
    Criar `.planning/phases/07.10bcd-migracao-pagamentos-divida-tres-consumidores/07.10bcd-01-SUMMARY.md` com template:

    ```markdown
    # Phase 7.10bcd — Plan 01 SUMMARY — 3 consumidores migrados (Devedores + ModuloDividas + Dashboard) leem pagamentos_divida

    **Status**: 3 commits atômicos sequenciais no submódulo, pai NÃO bumpado, push NÃO executado.
    **Data**: <YYYY-MM-DD>
    **Estratégia**: fast-track aceito pelo usuário com 6 mitigations não-negociáveis (M1-M6 do CONTEXT.md §1).

    ## Cadeia de 3 commits atômicos (D-30 bisect-able)

    | # | SHA | Mensagem header |
    |---|-----|-----------------|
    | 1 | `<sha7-07.10b>` | `feat(07.10b): Devedores lê pagamentos_divida (Map agrupador via helper compartilhado)` |
    | 2 | `<sha7-07.10c>` | `feat(07.10c): ModuloDividas + sub-componentes leem pagamentos_divida (Set lookup divida_id)` |
    | 3 | `<sha7-07.10d>` | `feat(07.10d): Dashboard agregados leem pagamentos_divida (helper + Set lookup inline)` |

    Baseline pré-7.10bcd: `2757f74` (post-7.10a HEAD). Todos os gates cumulativos referenciam esta baseline.

    ## Files cumulativos no chain (6 paths)

    Novos:
    - `src/mr-3/mr-cobrancas/src/utils/agruparPagamentosPorDevedor.js` (~25 lines, helper compartilhado D-31)
    - `src/mr-3/mr-cobrancas/src/services/__tests__/agruparPagamentosPorDevedor.test.js` (~40 lines, 2 it() blocks)

    Modificados:
    - `src/mr-3/mr-cobrancas/src/App.jsx` — em 3 commits diferentes (Devedores em 07.10b L8468 callsite + L2986 Map; case "dividas" em 07.10c L8488; case "dashboard" + Dashboard body em 07.10d L8467 + L568 + L682 + L696)
    - `src/mr-3/mr-cobrancas/src/components/TabelaDividas.jsx` (commit 07.10c — L41-44 buildDevedorObjParaSaldo Set lookup divida_id)
    - `src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx` (commit 07.10c — L74 filter Set lookup divida_id)
    - `src/mr-3/mr-cobrancas/package.json` (commit 07.10b — test:regressao estende com agruparPagamentosPorDevedor.test.js)

    Pass-through preservado (ZERO diff cumulativo):
    - `src/mr-3/mr-cobrancas/src/components/ModuloDividas.jsx` — confirmado via `git diff 2757f74..HEAD` returns zero lines (D-30 escopo cirúrgico — débito técnico naming aceito Q1)

    ## D-01 ESTRITO cumulativo — Phase 7.10bcd

    Motor Art.354 INTOCADO em todos 3 motor files (`devedorCalc.js`, `pagamentos.js`, `dividas.js`) ao longo dos 3 commits sequenciais.

    Phase 7.10bcd tocou apenas componentes consumidores (App.jsx callsites + Devedores Map + Dashboard body + TabelaDividas filter + DetalheDivida filter) + helper novo + helper test + package.json. Motor 100% intocado por construção.

    Verificado por dual gate cumulativo:
    - **Gate C1 cumulativo** (working tree + 3 commits): `git diff 2757f74..HEAD -- <3 motor files>` → VAZIO
    - **Gate C2 cumulativo** (commit contents 3 commits): `git log 2757f74..HEAD --format="" --name-only | sort -u | grep -E "<3 motor files>"` → VAZIO (usa `--name-only`, NÃO `--stat` — lição 608c124 7.8.2a)

    ## Estratégia fast-track + M1-M6 mitigations aplicadas

    Decisão usuário 2026-04-25: migrar 3 consumidores em 1 phase (vs 3 phases atômicas) — risco MÉDIO-ALTO mitigado por 6 controles não-negociáveis:

    | M | Mitigation | Aplicação no Plan 01 |
    |---|------------|----------------------|
    | M1 | 3 commits atômicos no submódulo (NÃO 1 commit gigante) — bisect funciona | D-30: Tasks 2/3/4 commitam separadamente; Gate D-30 cumulativo verifica EXATAMENTE 3 commits sequenciais com headers exatos |
    | M2 | Helper compartilhado (NÃO inline duplication entre Devedores + Dashboard) | D-31: Task 1 cria `agruparPagamentosPorDevedor.js` + test 2 it(); commit 07.10b primeiro consumer; commit 07.10d reusa |
    | M3 | 5 humanos checkpoints proporcionais ao risco | D-32: 3 sub-PAUSAs intra-Plan-01 (após cada commit) + 2 PAUSAs Plan 02 |
    | M4 | Atomicidade no ship — UAT FAIL = rollback total (não ship parcial) | D-34: documentado em Plan 02 PAUSA #2 com comando `git -C src/mr-3 reset --hard 2757f74`; Plan 01 sub-PAUSAs validam cada commit antes de continuar (impede acumular bug serial) |
    | M5 | Regressão por commit (PASS após CADA um, não só pós-execute final) | Tasks 2/3/4 incluem Gate B (regressão 25/25) ANTES do `git commit`; Task 5 valida cumulativo final |
    | M6 | UAT cross-check 7 SCs MANDATÓRIOS (sem SKIP) | D-33: documentado em Plan 02 PAUSA #1 — SC1-SC7 todos mandatórios com drift centavo = FAIL |

    ## Gates Plan 01 (Tasks 2-5)

    Matriz consolidada de Task 5 (gates finais cumulativos pós-3-commits):

    - **Build** — exit 0 final cumulativo: **OK**
    - **Regressão** — **25/25 PASS** cumulativo (23 base 7.10a + 2 helper test): passou após CADA um dos 3 commits (M5) + final cumulativo (Task 5)
    - **Gate C1 cumulativo** — D-01 estrito: **OK** (motor diff `git diff 2757f74..HEAD -- <3 files>` VAZIO)
    - **Gate C2 cumulativo** — D-01 estrito: **OK** (motor NÃO em `git log 2757f74..HEAD --format="" --name-only | sort -u`)
    - **Gate D-30 cumulativo** — commits atômicos: **OK** — EXATAMENTE 3 commits sequenciais com headers `feat(07.10b)`, `feat(07.10c)`, `feat(07.10d)` (verbatim CONTEXT.md §2)
    - **Shield 24a** (case "devedores"): **OK** — positivo callsite (1 match `allPagamentos={allPagamentosDivida}`) + negativo regional (0 matches `allPagamentos={allPagamentos}` legacy nas 10 linhas seguintes) + helper grep (≥1 callsite em Devedores Map L2986)
    - **Shield 24b** (case "dividas" + TabelaDividas + DetalheDivida): **OK** — case "dividas" positivo + negativo + dividaIdsDoDevedor ≥2 em cada sub-componente + legacy `p.devedor_id === ...devedor_id` ausente em ambos + ModuloDividas zero diff cumulativo
    - **Shield 24c** (case "dashboard"): **OK** — positivo + negativo + helper ≥2 callsites em App.jsx (Devedores L2986 + Dashboard L568) + Set lookup inline ≥1 em Dashboard region + legacy ausente em L562-720
    - **Shield 24d** (parent App L8425 pgtosPorDevedorCarteira — Fix D-extra granular): **OK** — 2 Maps useMemo (Dashboard L566 + parent App L8425) + ambos usam helper agruparPagamentosPorDevedor + ZERO forEach manual em qualquer pgtosPorDevedorCarteira

    ## 4 callsites Map agrupador cobertos (não apenas 3 — grep global pre-execute aplicado)

    Phase 7.10bcd cobre **4 callsites** de Map agrupador identificadas via grep global de `allPagamentos.forEach` (lição feedback_grep_global_before_pointwise_fix.md aplicada pre-execute):
    1. **L568 Dashboard interno** (commit 07.10d) — usa helper compartilhado
    2. **L2986 Devedores** (commit 07.10b) — usa helper compartilhado
    3. **L8425 parent App pgtosPorDevedorCarteira** (commit 07.10d Fix D-extra) — usa helper, alimenta `totalCarteira` exibido em L8735 como card 'Carteira' do header GLOBAL (visível em TODAS as tabs)
    4. **L8427 forEach interno do Map L8425** (eliminado pelo helper junto com #3)

    ## Drift histórico D-28 — card "Carteira" header global

    Pós-7.10bcd, card "Carteira" do header global (renderizado em L8735, alimentado por totalCarteira L8434 via parent App pgtosPorDevedorCarteira L8425) também migra para fonte pagamentos_divida. SEM drift no header card pós-7.10bcd, ao contrário do esperado se phase original não cobrisse o 4º callsite (lição grep global aplicada antecipadamente).
    - **Files cumulativos** — 6 paths (helper NEW + helper test NEW + App.jsx + TabelaDividas + DetalheDivida + package.json); ModuloDividas zero diff
    - **Working tree clean** — pós 3 commits

    ## D-30..D-34 invariantes verificadas

    - **D-30** (3 commits atômicos bisect-able): VERIFIED — Gate D-30 cumulativo (3 commits sequenciais com headers exatos)
    - **D-31** (helper compartilhado): VERIFIED — `agruparPagamentosPorDevedor.js` criado em commit 07.10b, reusado em 07.10d (Dashboard L568); test trivial 2 it() defesa contra regressão silenciosa do helper (lição 7.8.2a feedback_shields_wrapper_must_test_equivalence.md)
    - **D-32** (5 humanos checkpoints): VERIFIED — Plan 01 executou 3 sub-PAUSAs intra-tasks (PAUSA #1a pós 07.10b; #1b pós 07.10c; #1c pós 07.10d). Plan 02 executará 2 PAUSAs (UAT 7 SCs + diff consolidado pré-bump). Total: 5.
    - **D-33** (UAT 7 SCs MANDATÓRIOS): DEFERRED to Plan 02 PAUSA #1 — Plan 01 só implementa; UAT cross-check é responsabilidade de Plan 02
    - **D-34** (atomicidade no ship): DOCUMENTED — comando rollback `git -C src/mr-3 reset --hard 2757f74` registrado em Plan 02 PAUSA #2 (se UAT FAIL em qualquer 1 dos 7 SCs)

    ## D-27 Set lookup pattern reutilizável (replicado em 3 callsites + 1 inline)

    Pattern:
    ```js
    const dividaIdsDoDevedor = new Set((devedor.dividas || []).map(d => String(d.id)));
    const pagamentosDoDevedor = pagamentosDivida.filter(p => dividaIdsDoDevedor.has(String(p.divida_id)));
    ```

    Aplicado em:
    1. TabelaDividas.jsx L41-44 (commit 07.10c)
    2. DetalheDivida.jsx L74 (commit 07.10c) — com optional chaining `devedor?.dividas`
    3. Dashboard ultimosPagamentos L696 (commit 07.10d) — inline com lookup intermediário pra `dev`
    4. Dashboard recuperadoPeriodo L682 (commit 07.10d) — lookup intermediário equivalente

    Total: 4 ocorrências (3 callsites principais + 1 inline secundário). Ainda inline OK (≤5 = inline; ≥5 = vira helper). Phases futuras hipotéticas que ultrapassem 5 callsites devem extrair helper.

    ## D-31 helper compartilhado (reusado em 2 callsites)

    `agruparPagamentosPorDevedor(devedores, pagamentosDivida)` aplicado em:
    1. App.jsx L2986 Devedores Map agrupador (commit 07.10b)
    2. App.jsx L568 Dashboard pgtosPorDevedorCarteira (commit 07.10d)

    Custo: 1 file novo (~25 lines) + 1 test novo (~40 lines, 2 it()). DRY > inline duplication. Lição "helper-first" 7.9 (`memory/feedback_db_integration_gate_missing.md`) aplicada antecipadamente em phase 7.10bcd.

    Test trivial:
    - it #1 — devedor com pagamentos múltiplos em 2 dívidas: `Map.get("dev-1").toHaveLength(3)`
    - it #2 — devedor sem dívidas: `map.has("dev-2") === false` + `map.size === 0`

    Defesa contra regressão silenciosa: bug no helper propaga pra 2 consumers (Devedores + Dashboard); test direto pega antes de UAT (lição 7.8.2a Shield 5).

    ## D-28 drift histórico CONTINUA aceito por design

    Pós-7.10bcd: **TODOS** os consumidores em prod (ModuloContratos via 7.3 + DetalheContrato via 7.8.2a + ProcessosJudiciais via 7.10a + Devedores via 07.10b + ModuloDividas/sub-componentes via 07.10c + Dashboard via 07.10d) usam `pagamentos_divida` como fonte.

    Pagamentos via UI legacy "Registrar pagamento parcial" (App.jsx L2537/2558/2574) ainda escrevem em `pagamentos_parciais` (tabela legacy v1.0). Esses pagamentos ficam **órfãos** no banco — sem nenhum consumer mostrando. Phase 7.10e hipotética cortará writes pra unificar fonte.

    Trade-off aceito por design (D-28 herdado da 7.10a). Cleanup é phase própria (sub-backlog 7.10e).

    Comunicação ao usuário durante UAT 7.10bcd Plan 02 PAUSA #1: garantir que dados de teste em prod tenham pagamentos via stored proc Phase 7 (`pagamentos_divida`), NÃO via UI legacy. Drift de centavo causado por D-28 é ESPERADO; drift causado por bug semântico é FAIL.

    ## Assumptions planner (aplicadas em Plan 01)

    - **Q1** — Prop name em ModuloDividas / Devedores: MANTIDA `allPagamentos` (débito técnico naming aceito; conteúdo é pagamentos_divida pós-callsite swap; rename global em phase 7.10e hipotética)
    - **Q2** — Helper signature: plain Map sem enrichment (CONTEXT §2 Fix A verbatim; ZERO callsite usa `p.devedor_id` no item individual pós-Map)
    - **Q3** — Test count final: 25/25 (23 base 7.10a + 2 helper test)
    - **Q4** — Shield 24 enforcement: inline em cada Task como Gate específico pre-commit (não pre-commit hook)
    - **Q5** — Baseline cumulativo: `2757f74` (post-7.10a HEAD pré-7.10bcd) — semântica correta de "EXATAMENTE 3 commits 07.10b/c/d"

    ## Fora de escopo (diferido)

    - Bump submódulo no pai → Plan 02 Task 3
    - Push humano (submódulo + pai) → ação pós-PAUSA #2 (Plan 02)
    - Tag `v1.4-phase7.10bcd` → pós-smoke prod (ação separada humana)
    - Cleanup writes legacy `pagamentos_parciais` → 7.10e hipotético
    - Drop tabela `pagamentos_parciais` → 7.10f hipotético (pós-7.10e + observação prod)
    - Migrar PessoasVinculadas (recebe prop `pagamentos` filtrada — ZERO acesso ao item individual; intocado nesta phase)
    - Renomear `pagamentos_parciais` em fixtures `calculos.test.js` + `casos-tjgo.json` (fixture local, não consumidor real)
    - Adicionar mutation handlers em pagamentos_divida (D-05 não muda — esta phase é leitura only)
    - Cleanup Phase 7.10.bug ([object Object] typo dbInsert processos_judiciais) — independente

    ## Próximos passos

    1. Plan 02 Task 1 (PAUSA #1 — UAT 7 SCs cross-check):
       - SC1 pré-check pagamentos_divida disponíveis
       - SC2 Devedores MENDES R$ 36.871,64
       - SC3 ModuloDividas TabelaDividas saldo dívida
       - SC4 DetalheDivida saldo mesma dívida (cross-check com SC3)
       - SC5 Dashboard "Carteira"
       - SC6 Dashboard "Recuperado" + "Em Aberto" + "Taxa"
       - SC7 Dashboard "Recebido Período" (filter dataInicio — caminho mais complexo)
       - **D-33**: TODOS os 7 mandatórios. Drift de centavo = FAIL phase inteira.
       - **D-34**: FAIL = rollback total `git -C src/mr-3 reset --hard 2757f74` (exceto bug trivial fixável <5min)
    2. Plan 02 Task 2 (PAUSA #2 — diff consolidado pré-bump pai)
    3. Plan 02 Task 3 — push submódulo (3 commits) + bump pai + push pai
    4. Plan 02 Task 4 — SUMMARY 02 + STATE.md SHIPPED + docs commit consolidado
    5. Pós Plan 02: smoke prod (mrcobrancas.com.br) — re-rodar 7 SCs com dados de prod
    6. Pós smoke OK: criar tag `v1.4-phase7.10bcd` em ação humana separada
    ```

    **NÃO commitar este SUMMARY agora.** Plan 02 Task 4 commitará junto do SUMMARY 02 + bump no pai (D-15 — `docs(07.10bcd): PLAN, SUMMARY consolidados + STATE phase 7.10bcd SHIPPED`).

    **NÃO executar `git push`** (push é ação humana pós-PAUSA #2 em Plan 02).
    **NÃO bumpar pai** (Plan 02 Task 3).
    **NÃO criar tag**.
  </action>
  <verify>
    <automated>test -f .planning/phases/07.10bcd-migracao-pagamentos-divida-tres-consumidores/07.10bcd-01-SUMMARY.md &amp;&amp; grep -qE "D-01 ESTRITO cumulativo" .planning/phases/07.10bcd-migracao-pagamentos-divida-tres-consumidores/07.10bcd-01-SUMMARY.md &amp;&amp; grep -qE "feat\(07\.10b\)" .planning/phases/07.10bcd-migracao-pagamentos-divida-tres-consumidores/07.10bcd-01-SUMMARY.md &amp;&amp; grep -qE "feat\(07\.10c\)" .planning/phases/07.10bcd-migracao-pagamentos-divida-tres-consumidores/07.10bcd-01-SUMMARY.md &amp;&amp; grep -qE "feat\(07\.10d\)" .planning/phases/07.10bcd-migracao-pagamentos-divida-tres-consumidores/07.10bcd-01-SUMMARY.md &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/07.10bcd-migracao-pagamentos-divida-tres-consumidores/07.10bcd-01-SUMMARY.md` exists
    - SUMMARY contém: cadeia 3 commits SHA + estratégia fast-track + M1-M6 aplicadas + D-30..D-34 invariantes + helper agruparPagamentosPorDevedor + test 2/2 + 6 files cumulativos + ModuloDividas zero diff + Shields 24a/b/c grep evidence + Regressão 25/25 + D-28 drift histórico + Assumptions Q1-Q5 + Próximos passos
    - SUMMARY ainda NÃO commitado no pai (`git status --short` mostra `??` ou `M` na linha do SUMMARY)
    - Footer de Próximos passos lista 7 SCs UAT explicitamente
    - D-34 atomicidade (`git reset --hard 2757f74`) documentada
    - ZERO `git push` executado neste task
    - ZERO bump submódulo no pai
    - ZERO criação de tag
  </acceptance_criteria>
  <done>
    SUMMARY 01 criado no pai (left uncommitted — Plan 02 Task 4 commitará junto). Phase 07.10bcd Plan 01 encerra em estado "3 commits atômicos no submódulo, pai pendente bump + docs commit, aguardando Plan 02 UAT 7 SCs + bump".
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App.jsx 3 callsites (case "devedores" L8468 + case "dividas" L8488 + case "dashboard" L8467) → 3 consumers | Single callsite cada, locked via Shield 24a/b/c grep (positivo + negativo regional). Único ponto de entrada da fonte de pagamentos pra cada componente. |
| Helper agruparPagamentosPorDevedor → 2 consumers (Devedores Map + Dashboard pgtosPorDevedorCarteira) | Helper compartilhado D-31; bug aqui propaga pra 2 callsites simultaneamente. Test trivial 2 it() é defesa H3-class (lição 7.8.2a). |
| Set lookup inline pattern → 4 ocorrências (TabelaDividas + DetalheDivida + Dashboard ultimosPagamentos + Dashboard recuperadoPeriodo) | Pattern D-27 replicado; cada ocorrência isolada. ≤5 = inline OK; ≥5 = vira helper em phase futura. |
| Cross-devedor isolation | Pagamentos de OUTRO devedor (divida_id ∈ outro devedor) NÃO entram em nenhum filter (Set lookup garante). Lição 7.10a Shield 23c herdada. |
| Motor Art.354 ← edits não autorizados | D-01 estrito dual gate cumulativo C1 (working tree pré cada commit) + C2 (chain de 3 commits) — usa `--name-only` lição 608c124. |
| Cadeia de 3 commits sequenciais → bisect funcional | D-30 atomicidade. Cada commit deixa estado verde (build + regressão 25/25). Permite isolamento de regressão por consumer pós-smoke prod. |
| Plan 01 → Plan 02 (UAT 7 SCs) | Sub-PAUSAs #1a/b/c em Plan 01 NÃO substituem UAT cross-check de Plan 02. UAT é cross-check empírico em prod com dados reais; sub-PAUSAs são gates automatizados intermediários. |
| Plan 02 PAUSA #1 (UAT FAIL) → D-34 atomicidade | UAT FAIL em qualquer 1 dos 7 SCs → rollback total `git -C src/mr-3 reset --hard 2757f74` (3 commits descartados). Phase aborta inteira. |
| Drift histórico D-28 → UAT SC1-SC7 | Drift de centavo causado por UI legacy é ESPERADO (D-28 aceito); drift causado por bug semântico é FAIL. UAT cross-check garante distinção. |

## Threat Register (STRIDE adapted, lições 7.9 + 7.10a aplicadas + ameaças específicas de fast-track)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.10bcd-01 | Tampering (silent shape mismatch helper agruparPagamentosPorDevedor) | helper file + 2 callsites Devedores + Dashboard | mitigate | **Test trivial 2 it()** (it #1 devedor com pagamentos múltiplos / it #2 boundary devedor sem dívidas). Defesa H3-class (lição 7.8.2a Shield 5 — `feedback_shields_wrapper_must_test_equivalence.md`). Bug no helper propaga pra 2 consumers — test direto pega antes do UAT. |
| T-07.10bcd-02 | Elevation of Privilege (motor tocado em qualquer dos 3 commits) | devedorCalc.js, pagamentos.js, dividas.js | mitigate | **D-01 dual gate cumulativo**: C1 (`git diff 2757f74..HEAD` em 3 motor files vazio antes de cada commit) + C2 (`git log 2757f74..HEAD --format="" --name-only | sort -u` regex inclui todos 3 — usa `--name-only`, NÃO `--stat`, lição 608c124 7.8.2a). Gates pré E pós cada commit. |
| T-07.10bcd-03 | Repudiation (callsite refactor acidental traz legacy de volta em qualquer 1 dos 3 cases) | App.jsx case "devedores" L8468 + case "dividas" L8488 + case "dashboard" L8467 | mitigate | **Shields 24a + 24b + 24c**: grep `allPagamentos={allPagamentosDivida}` no callsite (1 match cada) + awk regional `case "X":` + 10 linhas seguintes NÃO contém `allPagamentos={allPagamentos}` legacy (0 matches cada). Gate por commit + Gate Final cumulativo. |
| T-07.10bcd-04 | Information Disclosure (cross-devedor leakage — saldo inflado por pagamento de outro devedor em qualquer 1 dos 4 callsites Set lookup) | TabelaDividas + DetalheDivida + Dashboard L696 + Dashboard L682 | mitigate | **Pattern D-27 Set lookup uniforme** + lição 7.10a Shield 23c herdada (cross-entity isolation). Test trivial helper it #2 (boundary 0 dívidas → Map vazio sem crash) cobre caso edge real. UAT 7 SCs cross-check em prod confirma comportamento. |
| T-07.10bcd-05 | DoS (filter performance com N grande em 4 callsites Set lookup + 2 callsites helper) | Dashboard ultimosPagamentos + Dashboard recuperadoPeriodo + TabelaDividas + DetalheDivida + 2 helper Map | accept | Set lookup é O(1) amortizado vs O(N) array scan legacy; helper agruparPagamentosPorDevedor é O(devedores × dividas + pagamentos). Performance MELHORA com N grande. UAT SC2-SC7 valida em ambiente real (advogado com 200+ dívidas). |
| T-07.10bcd-06 | **Tampering (bug serial em fast-track — bug introduzido em commit 07.10b passa nos gates dele mas quebra commit 07.10c ou 07.10d cumulativamente)** | Sequência 07.10b → 07.10c → 07.10d | mitigate | **M5 regressão por commit** (passa após CADA um, não só pós-execute final). **3 sub-PAUSAs** (D-32) onde usuário valida regressão + Shield + reporte ANTES de prosseguir. Sub-PAUSA FAIL → executor aborta sem acumular regressão silenciosa. **D-34 atomicidade** garante rollback total se UAT em Plan 02 detectar bug serial. |
| T-07.10bcd-07 | **Tampering (drift entre commits — App.jsx editado 3x em commits diferentes pode acumular contradições)** | App.jsx em commits 07.10b, 07.10c, 07.10d | mitigate | Cada commit toca regions DISJUNTAS de App.jsx (L8468 + L2986 em 07.10b; L8488 em 07.10c; L8467 + L568 + L682 + L696 em 07.10d). Gate Shield por commit valida regional (awk + 10 linhas) impedindo edição cross-commit acidental. Gate Final cumulativo (Task 5) re-valida 3 Shields todos juntos. |
| T-07.10bcd-08 | Information Disclosure (drift histórico — UI legacy pagamentos_parciais escreve, mas NENHUM consumer pós-7.10bcd mostra) | App.jsx L2537+ UI legacy "Registrar pagamento parcial" | accept | **D-28 herdado** trade-off aceito por design. Cortar writes é phase própria (sub-backlog 7.10e hipotético). Pós-7.10bcd, pagamentos via UI legacy ficam órfãos no banco. Documentado visivelmente em SUMMARY. UAT comunica ao usuário (SC1 garante pagamentos via stored proc Phase 7). |
| T-07.10bcd-09 | Elevation of Privilege (push não autorizado em qualquer task do Plan 01) | Plan 01 Tasks 1-6 | mitigate | ZERO `git push` em qualquer task. Acceptance criteria de cada task explicitamente verifica "ZERO push executado". Push é ação humana em Plan 02 Task 3. |
| T-07.10bcd-10 | Repudiation (sub-PAUSA aprovada sem evidência grep) | Sub-PAUSAs #1a/b/c em Tasks 2-4 | mitigate | Cada `<how-to-verify>` tem comandos exatos pra usuário rodar (regressão + Shield greps + git log). Resume signal exige `APROVADO` ou `FAIL: <sintoma>` literal. Reporte registrado no output do executor pra inclusão no SUMMARY. |
| T-07.10bcd-11 | Tampering (D-30 commit count diluído — extra commit acidental, ou commit message wrong) | Cadeia de 3 commits | mitigate | Gate D-30 cumulativo em Task 5: `git log 2757f74..HEAD --oneline | wc -l` returns `3` exatamente + headers verbatim verify (`feat(07.10b/c/d)`). Se ANY desvio → FAIL Plan 01. |
| T-07.10bcd-12 | DoS (sub-PAUSA #1c contornada se usuário aprova sem rodar Plan 01 gates finais) | Task 4 sub-PAUSA #1c → Task 5 | mitigate | Task 4 sub-PAUSA exige usuário rodar 8 verificações específicas (build + regressão + C1 cumulativo + C2 cumulativo + D-30 + Shields + files cumulativos + reporte). Task 5 re-roda automatizado as mesmas verificações + Gate Final reporta matriz consolidada. Defesa em camadas. |
| T-07.10bcd-13 | Information Disclosure (Q5 baseline confusion — d354daa vs 2757f74) | Gates cumulativos C1/C2/D-30 | mitigate | Q5 decisão planner documenta uso de `2757f74` (post-7.10a HEAD pré-7.10bcd) como baseline cumulativo correto. Semântica de "EXATAMENTE 3 commits 07.10b/c/d" só é satisfeita com 2757f74. Documentado em CONTEXT.md interfaces + Plan 01 Q5 + Plan 02 PAUSA #2 + SUMMARY. |
| T-07.10bcd-14 | Spoofing (test fixture sintético engana shield helper) | Test trivial agruparPagamentosPorDevedor.test.js | mitigate | Fixture explícito com 3 pagamentos em 2 dívidas + boundary 0 dívidas. `expect(map.get("dev-1")).toHaveLength(3)` + `expect(map).toBeInstanceOf(Map)` + `expect(map.has("dev-2")).toBe(false)`. Equivalence semântica garantida (não só propriedade indireta). |
</threat_model>

<verification>
- [ ] Task 1 — helper agruparPagamentosPorDevedor.js + test 2 it() criados (NEW); package.json estendido; regressão 25/25 PASS; D-01 motor intocado; ZERO commit (4 files staged-ready)
- [ ] Task 2 — Commit 07.10b atômico criado: App.jsx L8468 callsite swap + L2984-2992 Map usa helper; 4 files (App.jsx + helper NEW + helper test NEW + package.json); message exato; sub-PAUSA #1a APROVADA; gates A/B/C1/C2/Shield24a/F verdes
- [ ] Task 3 — Commit 07.10c atômico criado: App.jsx L8488 callsite swap + TabelaDividas L41-44 Set lookup + DetalheDivida L74 Set lookup; 3 files; ModuloDividas zero diff; message exato; sub-PAUSA #1b APROVADA; gates A/B/C1/C2/Shield24b/F verdes
- [ ] Task 4 — Commit 07.10d atômico criado: App.jsx L8467 callsite swap + Dashboard L568 helper + L682 lookup intermediário + L696 Set lookup inline; 1 file; message exato; sub-PAUSA #1c APROVADA; gates A/B/C1/C2/Shield24c/D-30/F verdes
- [ ] Task 5 — Gates finais cumulativos consolidados: matriz de 12 gates toda verde (Build + Regressão 25/25 + C1 cumul + C2 cumul + D-30 cumul + Shields 24a/b/c + helper ≥2 callsites + ModuloDividas pass-through + 6 files cumulativos + working tree clean)
- [ ] Task 6 — SUMMARY 01 criado no pai (left uncommitted — Plan 02 Task 4 commit); D-30..D-34 verificadas; D-28 drift histórico documentado; Assumptions Q1-Q5 documentadas
- [ ] Motor Art.354 INTOCADO em todos 3 arquivos (D-01 estrito cumulativo) — verificado via Gate C1 cumulativo (working tree pré cada commit) + Gate C2 cumulativo (chain de 3 commits)
- [ ] D-30 atomicidade: EXATAMENTE 3 commits sequenciais com headers verbatim `feat(07.10b)`, `feat(07.10c)`, `feat(07.10d)`
- [ ] D-31 helper compartilhado em ≥2 callsites (Devedores Map + Dashboard pgtosPorDevedorCarteira)
- [ ] D-27 Set lookup inline replicado em 4 ocorrências (TabelaDividas + DetalheDivida + Dashboard L696 + Dashboard L682)
- [ ] D-32 5 humanos checkpoints: 3 sub-PAUSAs em Plan 01 + 2 PAUSAs Plan 02
- [ ] M5 regressão por commit: 25/25 PASS após CADA um dos 3 commits + cumulativo final
- [ ] ZERO `git push` executado em qualquer task
- [ ] ZERO bump de submódulo no pai (diferido Plan 02 Task 3)
- [ ] ZERO criação de tag `v1.4-phase7.10bcd` (diferido pós-smoke prod)
</verification>

<success_criteria>
1. Helper `src/mr-3/mr-cobrancas/src/utils/agruparPagamentosPorDevedor.js` (NEW) criado com signature verbatim CONTEXT §2 Fix A; test trivial 2 it() (devedor com pagamentos múltiplos + boundary 0 dívidas)
2. `src/mr-3/mr-cobrancas/package.json` (mod) — `test:regressao` inclui `agruparPagamentosPorDevedor.test.js`
3. Commit 07.10b — App.jsx L8468 callsite swap (case "devedores") + L2984-2992 Devedores Map usa helper; message exato `feat(07.10b): Devedores lê pagamentos_divida (Map agrupador via helper compartilhado)`; 4 files; sub-PAUSA #1a APROVADA
4. Commit 07.10c — App.jsx L8488 callsite swap (case "dividas") + TabelaDividas L41-44 Set lookup + DetalheDivida L74 Set lookup; ModuloDividas ZERO edição (pass-through preservado); message exato `feat(07.10c): ModuloDividas + sub-componentes leem pagamentos_divida (Set lookup divida_id)`; 3 files; sub-PAUSA #1b APROVADA
5. Commit 07.10d — App.jsx L8467 callsite swap (case "dashboard") + Dashboard L568 helper compartilhado + L682 lookup intermediário + L696 Set lookup inline; L613/L667 inalterados (D-29 motor compat); message exato `feat(07.10d): Dashboard agregados leem pagamentos_divida (helper + Set lookup inline)`; 1 file; sub-PAUSA #1c APROVADA
6. Gates por commit (Tasks 2/3/4): A (build), B (regressão 25/25 — M5), C1 (D-01 working tree), C2 (D-01 commit contents), Shield 24x correspondente, F (git status) — todos verdes em cada commit ANTES de prosseguir
7. Gates finais cumulativos (Task 5): matriz de 12 gates toda verde
8. D-01 ESTRITO cumulativo: motor intocado em todos 3 motor files (devedorCalc.js + pagamentos.js + dividas.js) ao longo dos 3 commits — verificado via dual gate cumulativo C1 + C2 desde baseline 2757f74
9. D-30 atomicidade cumulativa: EXATAMENTE 3 commits sequenciais com headers exatos `feat(07.10b)`, `feat(07.10c)`, `feat(07.10d)` (verbatim CONTEXT.md §2)
10. D-31 helper compartilhado: agruparPagamentosPorDevedor em ≥2 callsites em App.jsx (Devedores Map + Dashboard)
11. D-27 Set lookup pattern: replicado em 4 ocorrências (TabelaDividas + DetalheDivida + Dashboard L696 + Dashboard L682) — ainda inline OK (≤5)
12. Files cumulativos: 6 paths (helper NEW + helper test NEW + App.jsx + TabelaDividas + DetalheDivida + package.json); ModuloDividas zero diff cumulativo
13. SUMMARY 01 criado em `.planning/phases/07.10bcd-migracao-pagamentos-divida-tres-consumidores/07.10bcd-01-SUMMARY.md` (left uncommitted — Plan 02 Task 4 commitará junto)
14. ZERO `git push` autorizado neste plan
15. ZERO bump de submódulo no pai (diferido Plan 02 Task 3) + ZERO criação de tag `v1.4-phase7.10bcd` (diferido pós-smoke prod)
</success_criteria>

<output>
Após completion: `.planning/phases/07.10bcd-migracao-pagamentos-divida-tres-consumidores/07.10bcd-01-SUMMARY.md` contendo:
- Cadeia de 3 commits SHAs (07.10b → 07.10c → 07.10d)
- Estratégia fast-track + M1-M6 mitigations aplicadas (tabela explícita)
- D-30..D-34 invariantes verificadas
- Helper agruparPagamentosPorDevedor + test 2/2
- 6 files cumulativos no chain (helper NEW + helper test NEW + App.jsx + TabelaDividas + DetalheDivida + package.json) + ModuloDividas zero diff (pass-through preservado)
- Shields 24a/b/c grep evidence (positivo + negativo regional + filter pattern + helper grep)
- Regressão 25/25 PASS cumulativo (M5 — passou após CADA um dos 3 commits + final)
- D-01 ESTRITO cumulativo (Gate C1 + C2 ambos vazios em motor files desde baseline 2757f74)
- D-28 drift histórico CONTINUA aceito por design — pós-7.10bcd, TODOS consumidores em prod usam pagamentos_divida; pagamentos via UI legacy ficam órfãos
- Assumptions planner Q1-Q5 documentadas
- Próximos passos: Plan 02 (UAT 7 SCs MANDATÓRIOS — D-33; PAUSA #1 + diff consolidado pré-bump PAUSA #2; D-34 atomicidade no ship)
- SUMMARY left uncommitted — Plan 02 Task 4 commit + bump pai + STATE SHIPPED juntos
</output>
