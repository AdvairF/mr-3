---
phase: 07.9-custas-judiciais-crud
plan: 02
type: execute
wave: 2
depends_on: ["07.9-01"]
autonomous: false
requirements_addressed: []
source: "ROADMAP.md line 333 + 07.9-CONTEXT.md §6 (SC1-SC9 UAT) + §8 (task 07.9-02) + D-14 (3 PAUSAs) + D-15 (commit convention)"

files_modified: []  # Plan 02 não edita código-fonte — só PAUSAs humanas, bump no pai (submódulo já committado no Plan 01), commit docs SUMMARY
files_touched:
  - src/mr-3                                                            # bump no pai (git add src/mr-3 no repo pai)
  - .planning/phases/07.9-custas-judiciais-crud/07.9-02-SUMMARY.md      # novo — SUMMARY após UAT + bump

must_haves:
  truths:
    - "UAT SC1-SC9 executado pelo usuário em localhost com PASS explícito em TODOS os SCs mandatórios (SC1, SC2, SC3, SC4, SC5, SC8) — SC6/SC7/SC9 podem SKIP com justificativa"
    - "Diff consolidado do submódulo aprovado pelo usuário (PAUSA #2) antes do bump"
    - "Bump de submódulo no pai criado com mensagem D-15 `chore(07.9): bump submodule mr-3 — custas judiciais CRUD`"
    - "SHA-bump no pai === SHA do commit submódulo aprovado em PAUSA #2 (cross-check em PAUSA #3)"
    - "Push é ação HUMANA — planner NÃO executa `git push`"
    - "Tag `v1.4-phase7.9` NÃO criada nesta phase (out of scope — ação separada pós-smoke prod)"
  artifacts:
    - path: ".planning/phases/07.9-custas-judiciais-crud/07.9-02-SUMMARY.md"
      provides: "Registro UAT SC1-SC9 + SHA-bump submódulo↔pai + resultados das 3 PAUSAs"
      contains: "SC1.*PASS"
  key_links:
    - from: "Plan 02 PAUSA #1 (UAT)"
      to: "Plan 02 Task 2 (diff consolidado)"
      via: "Sem OK UAT, não autoriza PAUSA #2"
      pattern: "PAUSA #1.*APROVADO"
    - from: "Plan 02 PAUSA #2 (diff consolidado)"
      to: "Plan 02 Task 3 (bump pai)"
      via: "Sem OK diff consolidado submódulo, não bumpa"
      pattern: "PAUSA #2.*APROVADO"
    - from: "Plan 02 PAUSA #3 (bump review)"
      to: "Push humano (fora do plan)"
      via: "Sem OK bump review, não autoriza push"
      pattern: "PAUSA #3.*APROVADO"
---

<objective>
Executar UAT dos 9 success criteria (SC1-SC9 — CONTEXT.md §6) em localhost, bumpar submódulo no pai, e deixar o código pronto pra push humano. O commit do submódulo já foi criado em Plan 01 Task 7 (`feat(07.9): custas judiciais CRUD avulsas com integração cache SWR`). Este plan executa 3 PAUSAs humanas sequenciais (D-14) antes de qualquer mudança irreversível no pai.

Purpose:
- **PAUSA #1 (UAT SC1-SC9 local)** impede bug comportamental indo pra prod.
- **PAUSA #2 (diff consolidado pós-commit submódulo)** impede push de submódulo errado.
- **PAUSA #3 (bump review no pai)** impede bump com SHA errado.

Output:
- UAT SC1-SC9 PASS registrado pelo usuário (com matriz de resultados)
- Bump de submódulo no pai com commit `chore(07.9): bump submodule mr-3 — custas judiciais CRUD`
- `07.9-02-SUMMARY.md` documentando UAT + SHAs + PAUSAs
- Código pronto pra push humano (planner NÃO executa push)

Fora de escopo:
- `git push` (ação humana pós-PAUSA #3)
- Tag `v1.4-phase7.9` (ação separada pós-smoke prod)
- Qualquer edit de código-fonte (Plan 01 já executou tudo)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07.9-custas-judiciais-crud/07.9-CONTEXT.md
@.planning/phases/07.9-custas-judiciais-crud/07.9-01-PLAN.md
@.planning/phases/07.9-custas-judiciais-crud/07.9-01-SUMMARY.md

<!-- Plan 01 criou o commit no submódulo; este plan bumpa no pai após UAT. -->
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1 — PAUSA #1: UAT SC1-SC9 em localhost</name>
  <read_first>
    - `.planning/phases/07.9-custas-judiciais-crud/07.9-CONTEXT.md` (§6 SC1-SC9 — **verbatim**)
    - `.planning/phases/07.9-custas-judiciais-crud/07.9-01-SUMMARY.md` (confirmar commit submódulo criado + gates todos verdes)
  </read_first>
  <what-built>
    Plan 01 criou 2 arquivos novos (NovaCustaModal.jsx + custasCache.test.js com 4 shields 17-20), modificou 4 arquivos existentes (useSaldoAtualizadoCache.js com D-06 amend 14 campos, contratos.js com 4 novas funções de custas avulsas, DetalheContrato.jsx com botão + lista + 4 handlers D-05, DecomposicaoSaldoModal.jsx com 2 linhas pagas + em aberto), atualizou `package.json` no submódulo, passou 20/20 testes regressão + build exit 0 + D-01 estrito motor diff vazio nos 3 arquivos (C1+C2) + D-02 isolation grep zero + D-05 shield tabela 10/10 YES + D-06 fingerprint 8 greps PASS, e commitou `feat(07.9): custas judiciais CRUD avulsas com integração cache SWR` no submódulo.

    O pai AINDA NÃO foi bumpado. O localhost deve rodar com o submódulo no SHA do commit do Plan 01.
  </what-built>
  <how-to-verify>
    **Preparação:**
    ```bash
    cd src/mr-3/mr-cobrancas
    npm install   # se necessário
    npm run dev   # inicia Vite
    ```
    Abrir `http://localhost:5173` (ou porta exibida), logar, criar 1-2 contratos de teste com pelo menos 1 documento (NF/Empréstimo) cada — seeds mínimos pros SCs abaixo.

    **Executar os 9 SCs (copiados VERBATIM da CONTEXT.md §6). Marcar PASS/FAIL em cada um:**

    ---

    **SC1 — Criar custa avulsa** [MANDATÓRIO]
    - Passos: Abrir DetalheContrato do Contrato A. Clicar "+ Nova Custa". Preencher: descrição="Custas iniciais", valor=500, data="2025-03-01". Clicar "Salvar".
    - Expectativa: Modal fecha. Linha nova aparece na seção "Custas Judiciais" (descrição="Custas iniciais", valor original=R$ 500, pago=Em aberto). No Resumo Financeiro → clicar "Saldo Atualizado" → modal DecomposicaoSaldoModal mostra nova linha "Custas judiciais em aberto (atualizadas)" com valor corrigido até hoje.
    - Resultado: PASS [ ] / FAIL [ ]

    ---

    **SC2 — Criar 2ª custa avulsa no mesmo contrato com data e valor distintos** [MANDATÓRIO]
    - Passos: No mesmo Contrato A, clicar "+ Nova Custa" novamente. Preencher: descrição="Honorários periciais", valor=150, data="2025-06-01". Salvar.
    - Expectativa: Tabela "Custas Judiciais" mostra agora 2 linhas (a anterior + a nova) com valores e datas distintos. Cada uma exibe seu próprio valor atualizado (correção desde data despesa). DecomposicaoSaldoModal soma ambas em "em aberto (atualizadas)".
    - Resultado: PASS [ ] / FAIL [ ]

    ---

    **SC3 — Editar custa existente** [MANDATÓRIO]
    - Passos: Na seção "Custas Judiciais" da Contrato A, clicar "Editar" numa custa existente. Mudar descrição ou valor. Salvar.
    - Expectativa: Linha reflete mudança imediata. DecomposicaoSaldoModal mostra saldo atualizado diferente.
    - Resultado: PASS [ ] / FAIL [ ]

    ---

    **SC4 — Excluir custa** [MANDATÓRIO]
    - Passos: Clicar "Excluir" numa custa. Confirmar window.confirm.
    - Expectativa: Linha some imediatamente. A dívida-fantasma (_so_custas:true) permanece vazia no banco (Q2 conservativo — não quebra). DecomposicaoSaldoModal recalcula. Cross-check: em Contratos (listagem), coluna "Saldo Atualizado" do Contrato A atualiza (via D-05 handler).
    - Resultado: PASS [ ] / FAIL [ ]

    ---

    **SC5 — Toggle pago** [MANDATÓRIO]
    - Passos: Clicar "Pagar" numa custa em aberto. Aguardar toast. Abrir DecomposicaoSaldoModal.
    - Expectativa: Custa movida de "Custas judiciais em aberto (atualizadas)" para "Custas judiciais pagas" na decomposição. Na tabela da seção "Custas Judiciais", badge muda pra "Pago em DD/MM/YYYY" (data_pagamento preenchida com data de hoje em fuso Goiânia).
    - Clicar "Despagar" na mesma custa → badge volta pra "Em aberto"; data_pagamento null; valor volta pra "em aberto (atualizadas)".
    - Resultado: PASS [ ] / FAIL [ ]

    ---

    **SC6 — Cross-entity: 2 custas no mesmo contrato** [NÃO-MANDATÓRIO — pode SKIP]
    - Passos: Criar 2 custas no Contrato A com valores e datas diferentes (ex: 100 em 2025-01-01 + 200 em 2025-12-01). (Pode reaproveitar o estado de SC2.)
    - Expectativa: Cada uma mantém seu próprio valor atualizado; tabela mostra 2 linhas; DecomposicaoSaldoModal soma corretamente.
    - Resultado: PASS [ ] / FAIL [ ] / SKIP [ ] (justificativa: __________)

    ---

    **SC7 — Indexador herdado** [NÃO-MANDATÓRIO — pode SKIP]
    - Passos: Em contrato com indexador IGP-M, criar custa avulsa. Editar contrato: mudar indexador pra INPC. Abrir DecomposicaoSaldoModal.
    - Expectativa: Valor atualizado da custa em aberto muda proporcionalmente (indexador herdado D-25).
    - Resultado: PASS [ ] / FAIL [ ] / SKIP [ ] (justificativa: coberto por Shield 19 custasCache.test.js)

    ---

    **SC8 — Invalidação cache SWR em listagem** [MANDATÓRIO]
    - Passos: Criar custa no Contrato A. Voltar pra listagem de Contratos (ModuloContratos). Observar linha do Contrato A na coluna "Saldo Atualizado".
    - Expectativa: Linha de A mostra saldo ATUALIZADO dentro de ~200ms (status pode ser "revalidating" com opacity 0.75 e voltar pra "fresh" em ~1s). Linha de outros contratos INALTERADA.
    - Observação crítica: Este SC valida o handler D-05 `handleCriarCusta` chamando `invalidateContrato(A.id)` ANTES do toast.
    - Resultado: PASS [ ] / FAIL [ ]

    ---

    **SC9 — Fingerprint revalidação (drift)** [NÃO-MANDATÓRIO — pode SKIP]
    - Passos: Editar valor de uma custa SEM passar pelo handler D-05 (simular via direct DB mutation ou outro browser).
    - Expectativa: Cache revalida via fingerprint C drift no próximo render (não só via D-05 explícito).
    - Resultado: PASS [ ] / FAIL [ ] / SKIP [ ] (justificativa: coberto por Shields 17-18 e Shield 20 D-06 amend coverage; cobrir manualmente requer direct DB access inapropriado pra UAT local)

    ---

    **Matriz consolidada — preencher TODOS antes de resumir:**
    ```
    SC1  — Criar custa avulsa:                              [ ] PASS   [ ] FAIL   Nota: ______
    SC2  — Criar 2ª custa avulsa (data/valor distintos):    [ ] PASS   [ ] FAIL   Nota: ______
    SC3  — Editar custa:                                     [ ] PASS   [ ] FAIL   Nota: ______
    SC4  — Excluir custa:                                    [ ] PASS   [ ] FAIL   Nota: ______
    SC5  — Toggle pago/despagar:                             [ ] PASS   [ ] FAIL   Nota: ______
    SC6  — Cross-entity custas:                              [ ] PASS   [ ] FAIL   [ ] SKIP   Just: ______
    SC7  — Indexador herdado:                                [ ] PASS   [ ] FAIL   [ ] SKIP   Just: ______
    SC8  — Invalidação cache listagem (D-05):                [ ] PASS   [ ] FAIL   Nota: ______
    SC9  — Fingerprint drift revalidação:                    [ ] PASS   [ ] FAIL   [ ] SKIP   Just: ______
    ```

    **Regra de aprovação:** PASS em **SC1, SC2, SC3, SC4, SC5, SC8** é MANDATÓRIO.
    SC6/SC7/SC9 podem SKIP com justificativa técnica documentada (justificativa aceitável: "coberto por Shield X no custasCache.test.js").

    **Se ALGUM SC mandatório = FAIL:**
    - Registrar sintoma observado.
    - **NÃO APROVAR este task** — responder `FAIL: <SCx>: <sintoma>`.
    - Voltar pro Plan 01 pra fix (`/gsd-execute-phase 07.9 --gaps-only` ou replanejar).
  </how-to-verify>
  <resume-signal>
    Digite `APROVADO` (ou `APROVADO com ressalva: <nota>`) SE todos os SCs mandatórios (SC1, SC2, SC3, SC4, SC5, SC8) passaram E SC6/SC7/SC9 estão PASS ou SKIP-com-justificativa. Caso contrário, responda `FAIL: <SCx>: <sintoma>` e o executor aborta o plan.
  </resume-signal>
  <acceptance_criteria>
    - Usuário respondeu explicitamente `APROVADO` (ou `APROVADO com ressalva`)
    - Matriz SC1-SC9 preenchida com PASS/FAIL/SKIP em cada item, a ser incluída no 07.9-02-SUMMARY.md
    - SC1, SC2, SC3, SC4, SC5, SC8 = PASS (mandatórios)
    - SC6/SC7/SC9 PASS ou SKIP com justificativa técnica
    - Se houver FAIL mandatório, task aborta e o executor NÃO prossegue pro Task 2
  </acceptance_criteria>
  <done>
    Matriz SC1-SC9 preenchida + resposta humana `APROVADO` registrada no output do task.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 — PAUSA #2: Diff consolidado pós-commit submódulo</name>
  <read_first>
    - `.planning/phases/07.9-custas-judiciais-crud/07.9-CONTEXT.md` (§4 D-14 PAUSA #2 + §4 D-15 commit convention)
  </read_first>
  <what-built>
    Task 1 (PAUSA #1) aprovada pelo usuário. Ainda não foi feito bump no pai. O commit do submódulo (Plan 01 Task 7) existe e pode ser revisado agora.
  </what-built>
  <how-to-verify>
    **Executar dentro de `src/mr-3`:**

    **1. Confirmar commit mais recente:**
    ```bash
    cd src/mr-3 && git log -1 --oneline
    ```
    Expectativa: último commit = `<SHA> feat(07.9): custas judiciais CRUD avulsas com integração cache SWR`. Copiar o SHA (7 primeiros chars) — será referenciado no SUMMARY.

    **2. Stat consolidado (count de arquivos):**
    ```bash
    cd src/mr-3 && git diff HEAD~1 HEAD --stat
    ```
    Expectativa: **7 arquivos** listados — 2 novos (NovaCustaModal.jsx, custasCache.test.js) + 4 modificados (useSaldoAtualizadoCache.js, contratos.js, DetalheContrato.jsx, DecomposicaoSaldoModal.jsx) + package.json. Ex:
    ```
    mr-cobrancas/package.json                                             | 2 +-
    mr-cobrancas/src/components/DecomposicaoSaldoModal.jsx                | <N> ++++++
    mr-cobrancas/src/components/DetalheContrato.jsx                       | <N> ++++++
    mr-cobrancas/src/components/NovaCustaModal.jsx                        | <N> +++++
    mr-cobrancas/src/hooks/useSaldoAtualizadoCache.js                     | <N> ++++++
    mr-cobrancas/src/services/__tests__/custasCache.test.js               | <N> +++++
    mr-cobrancas/src/services/contratos.js                                | <N> ++++++
    7 files changed, ...
    ```

    **3. D-01 estrito — Motor: TODOS 3 arquivos (`devedorCalc.js` + `pagamentos.js` + `dividas.js`) VAZIOS:**
    ```bash
    cd src/mr-3 && git diff HEAD~1 HEAD -- \
      mr-cobrancas/src/utils/devedorCalc.js \
      mr-cobrancas/src/services/pagamentos.js \
      mr-cobrancas/src/services/dividas.js
    ```
    Expectativa: ZERO linhas em todos os 3 motor files (D-01 estrito mantido). Qualquer linha = FAIL — abortar.

    **4. Revisar o diff completo** (recomendado):
    ```bash
    cd src/mr-3 && git show HEAD
    ```

    **Pontos de revisão humana (checklist):**
    1. [ ] 7 arquivos modificados (sem surpresas — nada fora de escopo)
    2. [ ] ZERO touch em `pagamentos.js`, `dividas.js`, `devedorCalc.js` (D-01 estrito — todos 3 motor files intocados)
    3. [ ] Mensagem de commit exata: `feat(07.9): custas judiciais CRUD avulsas com integração cache SWR`
    4. [ ] useSaldoAtualizadoCache.js — ver o bloco novo `const C = JSON.stringify(...)` + `const S = ...` + return com `|C:${C}|S:${S}` (D-06 amend)
    5. [ ] contratos.js — ver 4 novas funções exports (criarCusta, editarCusta, excluirCusta, togglePagoCusta) + helpers (gerarCustaId, hojeGoianiaDate); SEM import de buscarDivida
    6. [ ] DetalheContrato.jsx — 4 novos handlers (handleCriarCusta, handleEditarCusta, handleExcluirCusta, handleTogglePagoCusta) + seção "Custas Judiciais" + import NovaCustaModal; SEM coluna Vínculo
    7. [ ] DecomposicaoSaldoModal.jsx — prop `dividas` + split `custasPagasNominal` + 2 linhas ("Custas judiciais pagas" + "Custas judiciais em aberto (atualizadas)")
    8. [ ] NovaCustaModal.jsx (novo) — form simplificado com 3 campos + 2 botões (sem dropdown de vínculo); ZERO imports de useSaldoAtualizadoCache
    9. [ ] custasCache.test.js (novo) — 4 shields (17, 18, 19, 20 D-06 amend coverage)
    10. [ ] package.json — `test:regressao` agora inclui `custasCache.test.js`
    11. [ ] Sem arquivos de log/tmp acidentalmente commitados
    12. [ ] Sem `.env`, credenciais ou secrets
  </how-to-verify>
  <resume-signal>
    Digite `APROVADO` SE diff revisado está OK. Caso contrário, responda `FAIL: <motivo>` (ex: `FAIL: arquivo X fora de escopo`, `FAIL: devedorCalc.js modificado`). Em caso de FAIL, o executor aborta SEM bumpar o pai; usuário pode fazer `git reset --soft HEAD~1` no submódulo pra corrigir.
  </resume-signal>
  <acceptance_criteria>
    - Usuário respondeu `APROVADO` explicitamente
    - SHA do commit submódulo anotado pra uso no SUMMARY
    - Contagem de arquivos = 7 (confirmada pelo usuário no output)
    - `git diff HEAD~1 HEAD` em arquivos do motor (devedorCalc.js / pagamentos.js / dividas.js) = saída vazia
    - Checklist 12 pontos todos OK
    - Se houver FAIL, executor NÃO prossegue pro Task 3
  </acceptance_criteria>
  <done>
    Diff consolidado aprovado pelo usuário + SHA do submódulo registrado.
  </done>
</task>

<task type="auto">
  <name>Task 3 — Bump submódulo no pai (chore(07.9))</name>
  <read_first>
    - `.planning/phases/07.9-custas-judiciais-crud/07.9-CONTEXT.md` (D-15 commit convention pai)
  </read_first>
  <files>src/mr-3 (ponteiro de submódulo — staged no repo pai)</files>
  <action>
    Após APROVADO em Task 1 (UAT) + Task 2 (diff consolidado), criar bump do submódulo **no repo pai** (working dir é o root do repo — NÃO `cd` pra src/mr-3 aqui):

    **1. Verificar estado do pai:**
    ```bash
    git status
    ```
    Expectativa: apenas `modified: src/mr-3 (new commits)` listado. Nenhum outro arquivo do pai deve estar modificado (se houver docs pendentes, tratar em commit separado Task 4).

    **2. Adicionar o submódulo ao index:**
    ```bash
    git add src/mr-3
    ```
    **IMPORTANTE**: `git add src/mr-3` (SEM trailing slash). Isso registra o novo SHA do submódulo no pai — NÃO mexe no working tree do submódulo.

    **3. Commit no pai com mensagem D-15 EXATA:**
    ```bash
    git commit -m "chore(07.9): bump submodule mr-3 — custas judiciais CRUD"
    ```

    **4. Confirmar:**
    ```bash
    git log --oneline -1
    git diff HEAD~1 HEAD -- src/mr-3
    ```
    Esperado: último commit do pai = `<SHA-pai> chore(07.9): bump submodule mr-3 — custas judiciais CRUD`; diff do src/mr-3 mostra `-Subproject commit <velho>` e `+Subproject commit <novo>` onde `<novo>` = SHA do commit submódulo aprovado em Task 2.

    **NÃO executar `git push` aqui.** Push é ação humana pós-PAUSA #3 (Task 4).
    **NÃO criar tag `v1.4-phase7.9` aqui.** Tag é separada pós-smoke prod.
  </action>
  <verify>
    <automated>git log -1 --oneline | grep -qE "chore\(07\.9\): bump submodule mr-3"</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --oneline` output matches `chore(07.9): bump submodule mr-3 — custas judiciais CRUD`
    - `git diff HEAD~1 HEAD -- src/mr-3` shows `-Subproject commit <OLD>` and `+Subproject commit <NEW>` only
    - `git status` após commit está clean (exceto SUMMARY pendente — será tratado em Task 5)
    - ZERO `git push` executado neste task
    - ZERO tag criada neste task
  </acceptance_criteria>
  <done>
    Bump no pai criado com mensagem D-15; um único commit no pai; working tree clean (exceto SUMMARY pendente).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4 — PAUSA #3: Bump review no pai + autorização de push manual</name>
  <read_first>
    - `.planning/phases/07.9-custas-judiciais-crud/07.9-CONTEXT.md` (D-14 PAUSA #3 + D-15)
  </read_first>
  <what-built>
    Task 3 criou commit no pai `chore(07.9): bump submodule mr-3 — custas judiciais CRUD`. O pai agora aponta pro SHA do commit submódulo aprovado em PAUSA #2. Push ainda NÃO foi executado.
  </what-built>
  <how-to-verify>
    **Executar no root do repo pai:**

    **1. Último commit do pai:**
    ```bash
    git log --oneline -1
    ```
    Esperado: `<SHA-pai> chore(07.9): bump submodule mr-3 — custas judiciais CRUD`. Copiar SHA-pai.

    **2. Diff do src/mr-3 (dois Subproject commits):**
    ```bash
    git diff HEAD~1 HEAD -- src/mr-3
    ```
    Esperado: exatamente duas linhas mudadas — `-Subproject commit <SHA-velho>` e `+Subproject commit <SHA-novo>`. O `<SHA-novo>` DEVE bater com o SHA do commit aprovado em PAUSA #2 (Task 2).

    **3. Cross-check explícito com o submódulo:**
    ```bash
    git -C src/mr-3 log --oneline -1
    ```
    Esperado: `<SHA-novo-submódulo> feat(07.9): custas judiciais CRUD com integração cache SWR`.

    O `<SHA-novo-submódulo>` acima DEVE ser o MESMO `<SHA-novo>` do `+Subproject commit` no diff do pai.

    **Matriz de verificação (preencher):**
    ```
    SHA commit submódulo (PAUSA #2):   ________
    SHA em +Subproject commit no pai:  ________
    São IDÊNTICOS?                     [ ] SIM   [ ] NÃO
    SHA commit pai (Task 3):           ________
    Mensagem commit pai bate com D-15? [ ] SIM   [ ] NÃO
                                       (esperado: "chore(07.9): bump submodule mr-3 — custas judiciais CRUD")
    ```

    **Se SHAs NÃO idênticos ou mensagem errada:**
    - Abortar: `git reset --soft HEAD~1` no pai pra desfazer o bump.
    - Corrigir (raro — possível causa: `git add src/mr-3` antes do commit submódulo estabilizar).
    - Responder `FAIL: <motivo>`.

    **Se tudo OK:**
    Responder `APROVADO` para autorizar push humano.

    **IMPORTANTE — push é ação HUMANA:** Planner NÃO executa `git push`. Após APROVADO neste task, o USUÁRIO executa manualmente:
    ```bash
    # Dentro do submódulo primeiro (tem que ir primeiro):
    cd src/mr-3 && git push origin main
    # Voltar pro pai e pushar:
    cd ../.. && git push origin master
    ```

    Se push falhar com "rejected" por branch não-rastreada ou similar, user decide como resolver (pode precisar `git push -u origin <branch>`).
  </how-to-verify>
  <resume-signal>
    Digite `APROVADO` se SHA-bump idêntico ao commit aprovado em PAUSA #2 + mensagem D-15 correta. Digite `FAIL: <motivo>` se houver qualquer divergência.
  </resume-signal>
  <acceptance_criteria>
    - Usuário confirmou SHA-bump no pai = SHA commit submódulo aprovado em PAUSA #2
    - Usuário confirmou mensagem de commit pai = `chore(07.9): bump submodule mr-3 — custas judiciais CRUD`
    - Matriz de verificação preenchida (ambos "SIM") no output do task
    - Resposta `APROVADO` registrada
    - Push NÃO executado pelo planner — instrução explícita deixada para o usuário executar manualmente
    - Tag `v1.4-phase7.9` NÃO criada neste task (fora de escopo)
  </acceptance_criteria>
  <done>
    Bump review aprovado + instrução de push manual documentada + zero push/tag executado.
  </done>
</task>

<task type="auto">
  <name>Task 5 — Criar 07.9-02-SUMMARY.md + commit docs</name>
  <read_first>
    - `.planning/phases/07.9-custas-judiciais-crud/07.9-CONTEXT.md` (D-15 commit docs)
    - Outputs das Tasks 1-4 (SC matrix, SHAs, matriz bump)
  </read_first>
  <files>.planning/phases/07.9-custas-judiciais-crud/07.9-02-SUMMARY.md (NOVO)</files>
  <action>
    Criar `.planning/phases/07.9-custas-judiciais-crud/07.9-02-SUMMARY.md` com:

    ```markdown
    # Phase 7.9 — Plan 02 SUMMARY — Custas Judiciais CRUD avulsas (UAT + bump)

    **Status**: Bumped + awaiting human push
    **Data**: <YYYY-MM-DD>

    ## Tasks executados

    ### Task 1 — PAUSA #1 UAT SC1-SC9

    <matriz SC1-SC9 PASS/FAIL/SKIP preenchida pelo usuário — mandatórios: SC1, SC2, SC3, SC4, SC5, SC8>

    ```
    SC1  — Criar custa avulsa:                              [ ] PASS   [ ] FAIL   Nota: ______
    SC2  — Criar 2ª custa avulsa (data/valor distintos):    [ ] PASS   [ ] FAIL   Nota: ______
    SC3  — Editar custa:                                     [ ] PASS   [ ] FAIL   Nota: ______
    SC4  — Excluir custa:                                    [ ] PASS   [ ] FAIL   Nota: ______
    SC5  — Toggle pago/despagar:                             [ ] PASS   [ ] FAIL   Nota: ______
    SC6  — Cross-entity custas:                              [ ] PASS   [ ] FAIL   [ ] SKIP   Just: ______
    SC7  — Indexador herdado:                                [ ] PASS   [ ] FAIL   [ ] SKIP   Just: ______
    SC8  — Invalidação cache listagem (D-05):                [ ] PASS   [ ] FAIL   Nota: ______
    SC9  — Fingerprint drift revalidação:                    [ ] PASS   [ ] FAIL   [ ] SKIP   Just: ______
    ```

    Resultado: APROVADO | APROVADO com ressalva | FAIL aborted

    ### Task 2 — PAUSA #2 Diff consolidado

    - SHA commit submódulo: `<SHA-submódulo>`
    - Arquivos modificados: 7 (2 novos: NovaCustaModal.jsx + custasCache.test.js ; 4 modificados: useSaldoAtualizadoCache.js, contratos.js, DetalheContrato.jsx, DecomposicaoSaldoModal.jsx ; + package.json)
    - D-01 motor intocado: VAZIO ✓ (estrito mantido nos 3 arquivos — devedorCalc.js + pagamentos.js + dividas.js)

    Resultado: APROVADO

    ### Task 3 — Bump no pai

    - SHA commit pai: `<SHA-pai>`
    - Mensagem: `chore(07.9): bump submodule mr-3 — custas judiciais CRUD`

    ### Task 4 — PAUSA #3 Bump review

    <matriz preenchida: SHA submódulo ≡ +Subproject commit? SIM. Mensagem D-15? SIM>

    Resultado: APROVADO — push autorizado manualmente

    ## SHAs registrados

    | Escopo | SHA | Mensagem |
    |--------|-----|----------|
    | Submódulo mr-3 | `<sha7-sub>` | feat(07.9): custas judiciais CRUD avulsas com integração cache SWR |
    | Pai (root) | `<sha7-pai>` | chore(07.9): bump submodule mr-3 — custas judiciais CRUD |

    ## Push

    - Push do submódulo: **pendente** (usuário executa `cd src/mr-3 && git push origin main`)
    - Push do pai: **pendente** (usuário executa `git push origin master` após push do submódulo)

    ## Fora de escopo (diferido)

    - Tag `v1.4-phase7.9`: NÃO criada — ação separada pós-smoke prod
    - Sub-phase 7.9.1 (cleanup de dívida avulsa vazia): backlog se Q2 conservativo gerar reclamação UAT
    - Sub-phase 7.9.2 (optimistic versioning em toggle pago): backlog se T-07.9-06 virar problema real
    - Phase 7.10 (Tech debt pagamentos_parciais) — backlog v1.4
    - Phase 7.11 (React Router) — backlog v1.4
    - Phase 8 (PDF Demonstrativo) — fecha v1.4

    ## Próximos passos

    1. Usuário executa push do submódulo + push do pai
    2. Smoke em prod (mrcobrancas.com.br) — SC1/SC2/SC5/SC8 definitivo
    3. Se smoke OK: criar tag `v1.4-phase7.9` em ação separada
    4. Se regressão descoberta pós-smoke: abrir sub-phase 7.9.x apropriada
    ```

    **Commit dos docs (D-15):**
    Após salvar o SUMMARY.md:
    ```bash
    git add .planning/phases/07.9-custas-judiciais-crud/07.9-02-SUMMARY.md
    git commit -m "docs(07.9): PLAN e feedback"
    ```
    *Nota*: o commit de docs é separado do commit de bump (D-15) — mantém a history limpa.

    **NÃO incluir `git push` aqui também.** Push é ação humana pós-PAUSA #3.
  </action>
  <verify>
    <automated>ls .planning/phases/07.9-custas-judiciais-crud/07.9-02-SUMMARY.md &amp;&amp; git log -1 --oneline | grep -qE "docs\(07\.9\)"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/07.9-custas-judiciais-crud/07.9-02-SUMMARY.md` exists
    - SUMMARY contém matriz SC1-SC9 + SHAs submódulo/pai + registro das 3 PAUSAs + lista arquivos (7) + próximos passos
    - `git log -1 --oneline` shows `docs(07.9): PLAN e feedback`
    - ZERO `git push` executado neste task
    - ZERO criação de tag `v1.4-phase7.9`
  </acceptance_criteria>
  <done>
    SUMMARY criado + commit docs aplicado + phase encerrada em estado "bumped awaiting human push".
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (Plan 02 — workflow correctness)

| Boundary | Description |
|----------|-------------|
| Plan 01 → Plan 02 | Commit submódulo já existe; plan 02 herda estado. Se Plan 01 falhou gates, Plan 02 não deveria ter começado. |
| PAUSA #1 → Plan 02 Task 2 | UAT FAIL impede diff review; evita bump com bug comportamental. |
| PAUSA #2 → Plan 02 Task 3 | Diff review FAIL impede bump; evita apontar pai pra commit errado. |
| PAUSA #3 → Push humano | Bump review FAIL impede push; evita prod receber SHA errado. |
| Planner → `git push` | Proibido. Só humano empurra. |

## Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.9-20 | Elevation of Privilege (push não autorizado) | Plan 02 Tasks | mitigate | ZERO `git push` em qualquer task. Acceptance criteria de cada task explicitamente verificam "ZERO push executado". |
| T-07.9-21 | Tampering (bump com SHA errado) | Task 3 (bump no pai) | mitigate | PAUSA #3 (Task 4) cross-check SHA submódulo vs `+Subproject commit` no pai. FAIL → `git reset --soft HEAD~1` no pai + retry. |
| T-07.9-22 | Repudiation (UAT sem registro) | Task 1 matriz SC1-SC9 | mitigate | Task 5 exige matriz preenchida no SUMMARY.md. Sem matriz, task fails acceptance. |
| T-07.9-23 | Information Disclosure (tag prematura) | Criação de tag antes de smoke prod | mitigate | Explicitly forbidden em múltiplos acceptance criteria. Tag `v1.4-phase7.9` = ação separada pós-smoke. |
| T-07.9-24 | DoS (UAT skipped) | PAUSA #1 contornada | mitigate | Task 1 é `checkpoint:human-verify gate="blocking"`. Resume signal exige `APROVADO` literal. |
| T-07.9-25 | Tampering (docs commit mistura com bump) | Task 3 + Task 5 | mitigate | Task 3 commita só bump (`chore`); Task 5 commita só docs (`docs`). Commits separados mantêm history limpa (D-15). |
| T-07.9-27 | Tampering (bump sem UAT) | Sequência PAUSA #1 → #2 → #3 contornada | mitigate | 3 checkpoints blocking em ordem estrita; cada task depende do APROVADO anterior. |
</threat_model>

<verification>
- [ ] Task 1 — PAUSA #1 — Matriz SC1-SC9 preenchida + APROVADO do usuário (mandatórios: SC1, SC2, SC3, SC4, SC5, SC8 = PASS)
- [ ] Task 2 — PAUSA #2 — Diff consolidado submódulo aprovado + SHA submódulo registrado; 7 arquivos; motor diff vazio em todos 3 arquivos
- [ ] Task 3 — Bump no pai commitado com mensagem D-15 `chore(07.9): bump submodule mr-3 — custas judiciais CRUD`
- [ ] Task 4 — PAUSA #3 — Cross-check SHA submódulo ≡ +Subproject commit no pai + APROVADO
- [ ] Task 5 — SUMMARY.md criado + commit docs `docs(07.9): PLAN e feedback`
- [ ] ZERO `git push` executado em nenhum task
- [ ] ZERO tag `v1.4-phase7.9` criada
- [ ] Phase state final: "bumped awaiting human push"
</verification>

<success_criteria>
1. UAT SC1-SC9 executado em localhost com PASS mandatório em SC1, SC2, SC3, SC4, SC5, SC8 (SC6/SC7/SC9 aceita SKIP com justificativa técnica)
2. Diff consolidado do submódulo aprovado pelo usuário com 7 arquivos + saída vazia em arquivos do motor (D-01 estrito — todos 3)
3. Bump no pai criado com commit `chore(07.9): bump submodule mr-3 — custas judiciais CRUD`
4. Cross-check SHA submódulo ≡ `+Subproject commit` no pai executado e aprovado
5. `07.9-02-SUMMARY.md` criado com matriz SC1-SC9, SHAs submódulo/pai e registro das 3 PAUSAs
6. Commit docs `docs(07.9): PLAN e feedback` aplicado
7. ZERO `git push` executado por este plano (push é ação humana)
8. ZERO criação da tag `v1.4-phase7.9` (out of scope — pós-smoke prod)
9. Estado final: phase 7.9 "bumped + awaiting human push + smoke prod + tag separada"
</success_criteria>

<output>
Após completion: `.planning/phases/07.9-custas-judiciais-crud/07.9-02-SUMMARY.md` contendo:
- Matriz SC1-SC9 preenchida com PASS/FAIL/SKIP
- SHA commit submódulo (mr-3) + SHA commit pai
- Resultado de cada uma das 3 PAUSAs
- Lista explícita de ações pendentes (push humano, smoke prod, tag separada)

Fim da phase 7.9. Próxima ação: usuário executa pushes + smoke prod + (se OK) cria tag `v1.4-phase7.9`.
</output>
</content>
</invoke>