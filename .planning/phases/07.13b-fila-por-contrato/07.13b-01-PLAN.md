---
plan: 07.13b-01
phase: 7.13b
phase_name: "Fila por Contrato (refactor estrutural)"
type: execute
wave: 1
depends_on: []
autonomous: false
requirements: []
source: "ROADMAP.md L414-421 Phase 7.13b entry + 07.13b-CONTEXT.md (D-pre-1..D-pre-12). Phase has NO REQ-IDs (refactor estrutural — alinhar fonte de verdade com modelo 3 níveis Phase 5+6+7.x)."
estimated_tasks: 10  # 9 originais + Task 6.5 OPCIONAL (W1.4 smoke run)

files_modified:
  - src/mr-3/mr-cobrancas/src/services/filaDevedor.js                                          # MOD (rewrite total — 10 funções, fonte CONTRATOS, fix-forward bug `contratos`→`contratos_dividas` em 6+ callsites)
  - src/mr-3/mr-cobrancas/src/services/filaDevedor.test.js                                     # MOD (rewrite total — testes integração contra contratos_dividas + eventos_andamento.contrato_id; B1.2 sem numero_parcela; B1.3 schema real referencia/valor_total)
  - src/mr-3/mr-cobrancas/src/utils/devedorCalc.js                                             # MOD opcional (wrapper thin 1-linha calcularSaldoContratoAtualizado — D-pre-12 Claude's Discretion; D-01 motor INTOCADO — wrapper compõe motor existente)
  - src/mr-3/mr-cobrancas/src/utils/constants.js                                               # MOD (adicionar STATUS_CONTRATO + STATUS_CONTRATO_TERMINAIS + STATUS_CONTRATO_ATIVOS — I2.1 Plan 02 importa AMBOS)
  - src/mr-3/mr-cobrancas/src/services/migrations/004_fila_por_contrato.sql                    # NEW (Task 1 — STRATEGY A/B robusto; W1.2 sed pós-PAUSA #0 se STRATEGY=B aplicada)

must_haves:
  truths:
    - "Migration manual SQL executada no Supabase SQL Editor (D-pre-10) — 5 blocos com pre-flight + STRATEGY A/B robusto: Bloco 1 ALTER contratos_dividas ADD COLUMN status TEXT NOT NULL DEFAULT 'em_cobranca' CHECK (7 valores); Bloco 2 ALTER eventos_andamento ADD COLUMN contrato_id UUID REFERENCES contratos_dividas(id); Bloco 2.5 PRE-FLIGHT SELECT COUNT(*) FROM eventos_andamento; Bloco 3 TRUNCATE eventos_andamento (STRATEGY A) com fallback DELETE FROM eventos_andamento (STRATEGY B se TRUNCATE der erro RLS/privilégio); Bloco 4 ALTER eventos_andamento ALTER COLUMN contrato_id SET NOT NULL; Bloco 5 SELECT pg_notify('pgrst', 'reload schema'). STRATEGY aplicada documentada no SUMMARY"
    - "Pre-execute audit (lição feedback_helper_fanout_must_cover_motor_inputs.md + feedback_proc_judiciais_create_bug_preexisting.md) PASS antes de rewrite: (a) `psql` ou Supabase Table Editor confirma `contratos_dividas.status` coluna existe com CHECK 7 valores; (b) `eventos_andamento.contrato_id` UUID NOT NULL existe com FK pra contratos_dividas; (c) INSERT de teste em eventos_andamento com contrato_id válido + devedor_id válido FUNCIONA (não FK NOT NULL drift); (d) `eventos_andamento` está vazia pós-truncate (SELECT COUNT(*) = 0); (e) `pg_notify('pgrst','reload schema')` aplicado — PostgREST cache atualizado"
    - "filaDevedor.js rewrite total — 10 funções com fonte CONTRATOS (não devedores). Tabela primária = `contratos_dividas`; junction com eventos via `eventos_andamento.contrato_id` (NOT `devedor_id`); helper saldo = `calcularDetalheEncargosContrato(dividasDoContrato, allPagamentosDivida, hoje).saldoAtualizado` (D-pre-12 reusa Phase 7.8 adapter D-02; motor `calcularDetalheEncargos` D-01 INTOCADO)"
    - "listarContratosParaFila SHAPE ENRIQUECIDO (B2.1 cross-impact Plan 02 — DetalheContrato signature REAL exige props completos): cada contrato retornado inclui `_dividas` (rows da tabela `dividas` filtradas por `contrato_id`); `_devedores` (devedor principal + co-devedores via junction `devedores_dividas` → fetch nomes em `devedores`; array para múltiplos co-devedores); `_credores` (credor via `devedor.credor_id` ou `dividas.credor_id`; array para múltiplos); `_pagamentos` (preservado nome legado, mas semanticamente é pagamentos_divida — fetch global de `pagamentos_divida` filtrado por `divida_id IN (todas dividas do contrato)`); `_pagamentos_divida` (NOVO fetch consistente com Phase 7.10bcd — `pagamentos_divida` onde `divida_id IN (...)` para Plan 02 consumir). Task 4 (`listarContratosParaFila`) FAZ todos os fetches em paralelo (Promise.all) e enriquece o shape de cada contrato. Plan 02 consome esses campos para passar shape REAL ao DetalheContrato — D-pre-3 PROIBE tocar DetalheContrato.jsx, então alimentar shape existente é a ÚNICA opção. Acceptance: `grep -cE \"_dividas|_devedores|_credores|_pagamentos_divida\" src/services/filaDevedor.js` >= 4 (campos enriquecidos no return shape)"
    - "Bug fix incluso D-pre-8 (fix-forward natural pelo rewrite — escopo REVISADO): 6+ CALLSITES STALE confirmados em audit (NÃO 2 como briefing original): L217 calcularScorePrioridade `dbGet(\"contratos\", ...)`; L253 entrarNaFila `dbGet(\"contratos\", ...)`; L491 reciclarContratos `dbGet(\"contratos\", ...)`; L496 reciclarContratos fallback `dbGet(\"contratos\", ...)`; L584-585 listarFila `dbGet(\"contratos\", ...)`; L607-608 atualizarValoresAtrasados `dbGet(\"contratos\", ...)`; L623 atualizarValoresAtrasados `dbUpdate(\"contratos\", ...)`. Todos os 6+ callsites passam a apontar para `contratos_dividas` pós-rewrite. Grep CORRETO `(dbGet|sb|dbInsert|dbUpdate|dbDelete)\\([\'\"]contratos[\'\"]` (helper-call regex anchored — NÃO casa em contratos_dividas/contratos_historico/prose PT-BR) deve retornar 0 ocorrências em filaDevedor.js após rewrite. Funções dormentes em prod não-regressão; lição transferível 'ranges de linha em CONTEXT são estimativas; pre-execute audit deve enumerar TODAS ocorrências antes de prometer escopo no commit message' (7.10a + 7.13b)"
    - "Score por contrato (D-pre-8 Q4 — fórmula adaptada do atual): bonusStatus[em_cobranca]=100, [em_localizacao]=80, [notificado]=60, [em_negociacao]=40, terminais (quitado/arquivado/ajuizado)=0. score = bonusStatus + saldoAtualizado/100 + diasCriacao*0.5; prioridade ALTA(>=120) / MEDIA(>=80) / BAIXA. Pattern atual preservado, drift mínimo"
    - "Filtro `devedores.deleted_at=is.null` aplicado em `listarFila` (D-pre-9 + 7.14/D-pre-9 strategy b consistência cumulativa) — devedor soft-deleted some da Fila"
    - "filaDevedor.test.js rewrite total — testes integração contra schema novo: setup cria 1 devedor + 1 credor + 1 contrato (contratos_dividas com status='em_cobranca') + 1 dívida (dividas com contrato_id) + 1 pagamento_divida; cleanup ordenado eventos_andamento → pagamentos_divida → dividas → devedores_dividas → contratos_dividas → devedor → credor. NÃO referencia tabela `contratos` ou `parcelas` (não existem em prod desde Phase 5+)"
    - "STATUS_CONTRATO export em utils/constants.js — análogo a STATUS_DEV (formato `[{ v, l, cor, bg }]` — 7 valores PT-BR labels): em_cobranca='Em Cobrança', em_localizacao='Em Localização', em_negociacao='Em Negociação', notificado='Notificado', ajuizado='Ajuizado', quitado='Quitado', arquivado='Arquivado'. Cores reusam paleta STATUS_DEV existente (consistência visual Plan 02)"
    - "STATUS_CONTRATO_TERMINAIS export em utils/constants.js (I2.1 — Plan 02 importa AMBOS): `export const STATUS_CONTRATO_TERMINAIS = ['quitado', 'arquivado', 'ajuizado']` (3 valores). Plan 02 consome em FilaPainel cards (filtra naoTerminais), FilaPesquisa toggle, FilaAtendimento alterarStatus terminal-detect. Acceptance: `grep \"export const STATUS_CONTRATO_TERMINAIS\" src/utils/constants.js` exit 0"
    - "STATUS_CONTRATO_ATIVOS export em utils/constants.js — `['em_cobranca', 'em_localizacao', 'em_negociacao', 'notificado']` (4 valores ativos = não-terminais). filaDevedor.js consome em listarContratosParaFila + entrarNaFila + reciclarContratos + atualizarValoresAtrasados (filtro `status=in.(...)`)"
    - "D-01 motor INTOCADO: `devedorCalc.js` calcularDetalheEncargos (L365), calcularSaldoDevedorAtualizado (L59), calcularDetalheEncargosContrato (L849) — todos PRESERVADOS verbatim. Wrapper opcional `calcularSaldoContratoAtualizado = (d,p,h) => calcularDetalheEncargosContrato(d,p,h).saldoAtualizado` é Claude's Discretion (D-pre-12) — apenas COMPÕE motor existente, não modifica. Gate dual C1 diff `git -C src/mr-3 diff HEAD~1..HEAD -- src/utils/devedorCalc.js` deve mostrar APENAS adição do wrapper (se criado) ou VAZIO (se não criado); funções existentes inalteradas. Gate C2 `git log --format='' --name-only` confirma services/pagamentos.js + services/dividas.js AUSENTES no commit do rewrite filaDevedor.js"
    - "Gates Plan 01 verdes em commit final de rewrite: (a) build `npm run build` exit 0; (b) regressão `npm run test:regressao` (suite atual da 7.14) exit 0; (c) grep `'\\bcontratos\\b'` (substring isolada com word boundary) em filaDevedor.js retorna 0 ocorrências; (d) grep `'\\bparcelas\\b'` (substring isolada) em filaDevedor.js retorna 0 ocorrências (lição feedback_grep_global_before_pointwise_fix.md — varredura global de stale refs); (e) D-01 motor cumulative INTOCADO via dual gate"
    - "PAUSA humana Plan 01 #1 após Task 5 (rewrite filaDevedor.js completo) — usuário lê diff cumulativo do rewrite + confirma APROVADO antes de Task 6 reescrever testes. PAUSA humana Plan 01 #2 após Task 8 (testes rodando + gates verdes locais) — usuário aprova commit final do rewrite. autonomous:false LOCKED para todas as tasks"
  artifacts:
    - path: "src/mr-3/mr-cobrancas/src/services/filaDevedor.js"
      provides: "Rewrite total — 10 funções com fonte CONTRATOS (contratos_dividas). Funções renomeadas/adaptadas: listarContratosParaFila (rename de listarDevedoresParaFila — fonte contratos_dividas), entrarNaFila (fix-forward bug L253), proximoContrato (rename de proximoDevedor), registrarEvento (assinatura contratoId não devedorId, payload eventos_andamento com contrato_id), alterarStatusContrato (rename de alterarStatusDevedor; UPDATE contratos_dividas.status), reciclarContratos (fix-forward bug L491), removerDaFila, listarFila (filtro deleted_at=is.null herdado), calcularScorePrioridade (assinatura contratoId; usa contratos_dividas.status), atualizarValoresAtrasados. Helpers extractUsuario + validateUUID + validateBigInt PRESERVADOS"
      contains: "listarContratosParaFila"
    - path: "src/mr-3/mr-cobrancas/src/services/filaDevedor.test.js"
      provides: "Rewrite total — testes integração schema novo (contratos_dividas + eventos_andamento.contrato_id). Setup cria fixtures válidos pós-D-pre-10. Cleanup respeita FK ordering"
      contains: "contratos_dividas"
    - path: "src/mr-3/mr-cobrancas/src/utils/constants.js"
      provides: "STATUS_CONTRATO export — 7 valores PT-BR labels com cores. Plan 02 consome este export no select de status"
      contains: "STATUS_CONTRATO"
    - path: "src/mr-3/mr-cobrancas/src/utils/devedorCalc.js"
      provides: "Opcional (Claude's Discretion D-pre-12): wrapper thin 1-linha calcularSaldoContratoAtualizado ao lado de calcularDetalheEncargosContrato. Motor INTOCADO — apenas compõe"
      contains: "calcularDetalheEncargosContrato"
  key_links:
    - from: "Migration SQL Bloco 4 (eventos_andamento.contrato_id NOT NULL)"
      to: "filaDevedor.js registrarEvento (rewrite — payload INSERT eventos_andamento)"
      via: "INSERT em eventos_andamento DEVE incluir contrato_id NOT NULL pós-migração — caso contrário FK violation; assinatura registrarEvento(contratoId, usuarioId, dadosEvento) garante invariante"
      pattern: "contrato_id"
    - from: "filaDevedor.js listarContratosParaFila"
      to: "calcularDetalheEncargosContrato (devedorCalc.js:849)"
      via: "Adapter contrato-level Phase 7.8 D-02 (D-pre-12) — extrai .saldoAtualizado por contrato. Filtra pagamentos_divida por divida_id internamente. Motor D-01 INTOCADO"
      pattern: "calcularDetalheEncargosContrato\\("
    - from: "filaDevedor.js listarFila"
      to: "devedores deleted_at=is.null filter"
      via: "Consistência D-pre-9 com 7.14/D-pre-9 strategy b — devedor soft-deleted some da Fila"
      pattern: "deleted_at=is\\.null"
    - from: "filaDevedor.js entrarNaFila + reciclarContratos (rewrite)"
      to: "Tabela contratos_dividas (NOT contratos)"
      via: "Fix-forward D-pre-8 — bug `dbGet('contratos', ...)` substituído por `dbGet('contratos_dividas', ...)` em ambas funções (L253 + L491 do arquivo legado)"
      pattern: "contratos_dividas"
    - from: "STATUS_CONTRATO export (constants.js)"
      to: "Plan 02 select de status (FilaPainel/FilaAtendimento)"
      via: "Reuso de constante centralizada — mesmo pattern STATUS_DEV consumido por FilaDevedor.jsx hoje"
      pattern: "STATUS_CONTRATO"
---

<objective>
Refatorar a camada de dados da Fila — migration manual no Supabase SQL Editor (D-pre-10 robusta a 2 strategies) + rewrite total de `filaDevedor.js` (649 linhas → fonte CONTRATOS) + rewrite total de `filaDevedor.test.js` (248 linhas outdated) + adição de `STATUS_CONTRATO` em `utils/constants.js` + (opcional D-pre-12) wrapper thin `calcularSaldoContratoAtualizado` em `devedorCalc.js`. Bug fix incluso D-pre-8: stale refs `contratos`/`parcelas` (tabelas inexistentes) substituídas por `contratos_dividas`/`dividas`.

Purpose:
- **D-pre-1 Fila puxa CONTRATOS** — fonte de verdade alinhada com modelo 3 níveis (devedor → contrato → dívida) das Phases 5+6+7.x.
- **D-pre-4 status PER CONTRATO independente** — schema delta `contratos_dividas.status` TEXT NOT NULL com CHECK em 7 valores (em_cobranca, em_localizacao, em_negociacao, notificado, ajuizado, quitado, arquivado). Cada contrato do mesmo devedor tem ciclo próprio.
- **D-pre-7 γ DROP histórico legado** — `eventos_andamento` truncate aceito como decisão de produto (banco minúsculo, 5 devedores ativos). Atendimentos novos vão pro contrato via `eventos_andamento.contrato_id` UUID NOT NULL pós-migration.
- **D-pre-8 bug fix incluso** — `filaDevedor.js` hoje aponta `dbGet("contratos", ...)` em `entrarNaFila` L253 e `reciclarContratos` L491 (tabela inexistente — funções dormentes em prod). Fix-forward natural pelo rewrite total. Sem isolar como 7.13c (lição feedback_proc_judiciais_create_bug_preexisting.md — quando rewrite cobre call sites, fix-forward é mais barato que phase isolada).
- **D-pre-9 filter deleted_at** — `listarFila` consistência com 7.14/D-pre-9 strategy b. Devedor soft-deleted some da Fila.
- **D-pre-10 migration robusta a 2 strategies** — TRUNCATE preferida (fast, reseta sequences); DELETE fallback se TRUNCATE der erro RLS/privilégio. STRATEGY aplicada documentada no SUMMARY do Plan 03.
- **D-pre-12 helper saldo por contrato** — REUSA `calcularDetalheEncargosContrato` Phase 7.8 D-02 (envolve motor `calcularDetalheEncargos` D-01 INTOCADO). Wrapper opcional `calcularSaldoContratoAtualizado` 1-linha (Claude's Discretion) se callsite ficar ruidoso.
- **D-01 motor INTOCADO** — `devedorCalc.js` motor base preservado verbatim. Apenas wrapper opcional COMPÕE motor existente.
- **autonomous:false** — usuário valida diff de rewrite (Plan 01 PAUSA #1) e gates pré-commit (Plan 01 PAUSA #2). Phase grande proporcional ao risco (1175 linhas reescritas).

Output:
- 1 commit atômico no submódulo: `feat(07.13b-01): refactor camada de dados Fila por contrato + bug fix D-pre-8`
- 4 arquivos modificados (3 MOD + 1 MOD opcional wrapper)
- Migration SQL aplicada manualmente em Supabase SQL Editor (registrada em SUMMARY do Plan 03 com STRATEGY aplicada)
- Regressão atual da 7.14 PRESERVADA verde (motor INTOCADO)
- Gates locais: build + test:regressao + grep stale refs (0 ocorrências `contratos`/`parcelas` substring isolada) + dual gate D-01

Fora de escopo:
- Rewrite UI `FilaDevedor.jsx` 4 telas (Plan 02)
- UAT humano cross-entity (Plan 03)
- Bump pai / push / Vercel deploy (Plan 03)
- Tag `v1.4-phase7.13b` (Plan 03)
- LEARNINGS.md + memory feedbacks (Plan 03)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/PROJECT.md
@.planning/phases/07.13b-fila-por-contrato/07.13b-CONTEXT.md
@.planning/phases/07.14-blindagens-de-integridade-de-dados/07.14-CONTEXT.md

<!-- Service alvo do rewrite (D-pre-1, D-pre-8) -->
@src/mr-3/mr-cobrancas/src/services/filaDevedor.js
@src/mr-3/mr-cobrancas/src/services/filaDevedor.test.js

<!-- Helper saldo por contrato (D-pre-12 — REUSAR) -->
@src/mr-3/mr-cobrancas/src/utils/devedorCalc.js

<!-- Schema vivo (verificar shape em pre-execute audit) -->
@src/mr-3/mr-cobrancas/src/services/contratos.js
@src/mr-3/mr-cobrancas/src/services/migrations/001_devedores_dividas.sql
@src/mr-3/mr-cobrancas/src/services/migrations/002_dividas_tabela.sql
@src/mr-3/mr-cobrancas/src/services/migrations/003_devedores_soft_delete.sql

<!-- Constants (adicionar STATUS_CONTRATO análogo a STATUS_DEV) -->
@src/mr-3/mr-cobrancas/src/utils/constants.js

<!-- Supabase REST helpers (assinatura sb/dbGet/dbInsert/dbUpdate/dbDelete) -->
@src/mr-3/mr-cobrancas/src/config/supabase.js

<!-- Memory feedbacks aplicados pre-execute -->
<!-- feedback_db_integration_gate_missing.md (gates Plan 01 não pegam INSERT real, FK NOT NULL drift — UAT humano em Plan 03 essencial) -->
<!-- feedback_grep_global_before_pointwise_fix.md (grep global stale refs `contratos`/`parcelas` em filaDevedor.js após rewrite) -->
<!-- feedback_proc_judiciais_create_bug_preexisting.md (pre-execute audit que feature alvo seja utilizável — D-pre-8 bug filaDevedor.js dormente fix-forward) -->
<!-- feedback_helper_fanout_must_cover_motor_inputs.md (pre-execute audit dos campos lidos pelo motor antes de adapter contrato-level — calcularDetalheEncargosContrato espera dividasDoContrato + allPagamentosDivida) -->
<!-- feedback_devedores_dividas_corrupted_pre_existing.md (Fila por contrato NÃO passa pela junction — segura desse risco) -->
<!-- feedback_grep_shield_strict_imports.md (não há D-02 isolation crítica neste plan — Plan 02 trata) -->
<!-- feedback_gate_motor_grep_use_name_only.md (gate motor C2 usa --name-only, NÃO --stat) -->

<interfaces>
Key contracts the executor needs (extracted from codebase) — NO codebase exploration needed.

**Submódulo baseline pré-7.13b:** SHA HEAD atual (post-7.10.bug = `3508bb4`).
- Cumulative motor diff `git -C src/mr-3 diff 3508bb4..HEAD -- src/utils/devedorCalc.js src/services/pagamentos.js src/services/dividas.js` deve ser VAZIO ao final do plan (D-01 cumulativo). Wrapper opcional em `devedorCalc.js` é APENAS adição (não modifica funções existentes) — diff teria SOMENTE linhas `+` no fim do arquivo.
- Cumulative motor name-only `git -C src/mr-3 log 3508bb4..HEAD --format="" --name-only | grep -E "(pagamentos|dividas)\.js"` deve retornar VAZIO (services/pagamentos.js + services/dividas.js AUSENTES).
- Cumulative regressão final 7.14 PRESERVADA: 58 testes PASS (38 baseline + 20 parser tests).

**Schema delta D-pre-10 (verbatim CONTEXT.md L36-65):**
```sql
-- Bloco 1: status por contrato
ALTER TABLE public.contratos_dividas
  ADD COLUMN IF NOT EXISTS status TEXT NOT NULL DEFAULT 'em_cobranca'
  CHECK (status IN ('em_cobranca','em_localizacao','em_negociacao','notificado','ajuizado','quitado','arquivado'));

-- Bloco 2: contrato_id em eventos
ALTER TABLE public.eventos_andamento
  ADD COLUMN IF NOT EXISTS contrato_id UUID REFERENCES public.contratos_dividas(id);

-- Bloco 2.5: PRE-FLIGHT — confirma acesso à tabela antes do drop legado
SELECT COUNT(*) FROM public.eventos_andamento;

-- Bloco 3: γ — DROP histórico legado (decisão de produto, banco minúsculo)
-- STRATEGY A (preferida): TRUNCATE — fast, reseta sequences, requer privilégio TRUNCATE
TRUNCATE public.eventos_andamento;

-- STRATEGY B (fallback): se TRUNCATE der erro de permissão (RLS bypass / privilégio),
-- substituir Bloco 3 por:
--   DELETE FROM public.eventos_andamento;
-- DELETE respeita RLS e não requer privilégio TRUNCATE. Mesmo efeito final no contexto γ
-- (banco minúsculo, < 1000 rows). Documentar no SUMMARY qual strategy foi aplicada.

-- Bloco 4: NOT NULL após drop limpo
ALTER TABLE public.eventos_andamento
  ALTER COLUMN contrato_id SET NOT NULL;

-- Bloco 5: PostgREST schema reload
SELECT pg_notify('pgrst', 'reload schema');
```

**Score por contrato (D-pre-8 Q4 — fórmula adaptada):**
```javascript
const SCORE_STATUS_CONTRATO = {
  em_cobranca: 100,
  em_localizacao: 80,
  notificado: 60,
  em_negociacao: 40,
  // terminais (quitado/arquivado/ajuizado) recebem 0 implicitamente via SCORE_STATUS_CONTRATO[status] || 0
};

function calcularScoreContrato(contrato) {
  const bonus = SCORE_STATUS_CONTRATO[contrato.status] || 0;
  const saldo = parseFloat(contrato._saldo_atualizado || 0);
  const criado = contrato.created_at ? new Date(contrato.created_at) : new Date();
  const diasCadastro = Math.floor((Date.now() - criado) / 86400000);
  return bonus + (saldo / 100) + (diasCadastro * 0.5);
}

function calcularPrioridadeContrato(score) {
  return score >= 120 ? "ALTA" : score >= 80 ? "MEDIA" : "BAIXA";
}
```

**Helper saldo por contrato D-pre-12 (REUSAR — devedorCalc.js:849 verbatim):**
```javascript
export function calcularDetalheEncargosContrato(dividasDoContrato, allPagamentosDivida, hoje) {
  const dividaIds = new Set((dividasDoContrato || []).map(d => String(d.id)));
  const pagamentosFiltrados = (allPagamentosDivida || []).filter(p => dividaIds.has(String(p.divida_id)));
  const pseudoDevedor = { dividas: dividasDoContrato || [] };
  return calcularDetalheEncargos(pseudoDevedor, pagamentosFiltrados, hoje);
}
```

**Wrapper opcional D-pre-12 (Claude's Discretion — adicionar APÓS calcularDetalheEncargosContrato):**
```javascript
/**
 * Phase 7.13b D-pre-12: helper thin para Fila por Contrato.
 * Compõe motor existente — NÃO modifica calcularDetalheEncargos (D-01 INTOCADO).
 */
export const calcularSaldoContratoAtualizado = (dividasDoContrato, allPagamentosDivida, hoje) =>
  calcularDetalheEncargosContrato(dividasDoContrato, allPagamentosDivida, hoje).saldoAtualizado;
```

**STATUS_CONTRATO target shape (utils/constants.js):**
```javascript
// ─── STATUS CONTRATO (Phase 7.13b D-pre-4) ────────────────────
export const STATUS_CONTRATO = [
  { v: "em_cobranca",     l: "Em Cobrança",      cor: "#64748b", bg: "#f1f5f9" },
  { v: "em_localizacao",  l: "Em Localização",   cor: "#2563eb", bg: "#dbeafe" },
  { v: "em_negociacao",   l: "Em Negociação",    cor: "#d97706", bg: "#fef3c7" },
  { v: "notificado",      l: "Notificado",       cor: "#7c3aed", bg: "#ede9fe" },
  { v: "ajuizado",        l: "Ajuizado",         cor: "#c2410c", bg: "#ffedd5" },
  { v: "quitado",         l: "Quitado",          cor: "#065f46", bg: "#d1fae5" },
  { v: "arquivado",       l: "Arquivado",        cor: "#475569", bg: "#e2e8f0" },
];
```

**Existing helpers a PRESERVAR no rewrite (filaDevedor.js L5-42):**
- `validateUUID(id, label)` — guard CR-01 para UUIDs (contrato_id, fila_id)
- `validateBigInt(id, label)` — guard CR-01 para BIGINTs (devedor_id quando consultar devedor por FK)
- `extractUsuario(u)` — normaliza usuario_id/objeto para `{ uid, uNome, uEmail }`
- `parseDividasSvc(raw)` — converte string|array|null para array (PRESERVAR — usado em fallback de cálculo de valor face)

**Rewrite scope filaDevedor.js — 10 funções alvo (rename + adaptação):**
| Atual | Rewrite | Mudança principal |
|-------|---------|-------------------|
| `listarDevedoresParaFila` | `listarContratosParaFila` | Fonte = `contratos_dividas` (não `devedores`); filter `status IN (não-terminais)`; helper saldo via `calcularDetalheEncargosContrato`; ordena score desc |
| `calcularScorePrioridade(contratoId)` | `calcularScorePrioridade(contratoId)` (assinatura igual; corpo adaptado) | UPDATE `contratos_dividas.status`-aware; remove referência a `parcelas` (não existe); usa `dividas` |
| `entrarNaFila` | `entrarNaFila` | Fix-forward D-pre-8: `dbGet("contratos_dividas", ...)`; insere por contrato (1 linha por contrato_id); status filter via `contratos_dividas.status NOT IN (terminais)` |
| `proximoDevedor` | `proximoContrato` | Lock otimista por contrato_id; retorna `{ fila, contrato, devedor, dividas, eventos }` (eventos por `contrato_id`) |
| `registrarEvento(devedorId, ...)` | `registrarEvento(contratoId, usuarioId, dadosEvento)` | INSERT `eventos_andamento` com `contrato_id NOT NULL`; lookup `devedor_id` via `dividas.devedor_id` ou `contratos_dividas.devedor_id` (denormalização legada) — payload eventos_andamento mantém ambos `contrato_id` E `devedor_id` (back-compat com Detalhe-Devedor que ainda lê devedor_id) |
| `alterarStatusDevedor` | `alterarStatusContrato(contratoId, novoStatus, usuarioId)` | UPDATE `contratos_dividas.status`; NÃO toca `devedores.status`; remove da fila se status terminal (quitado/arquivado/ajuizado) |
| `reciclarContratos` | `reciclarContratos` | Fix-forward D-pre-8: `dbGet("contratos_dividas", ...)`; filtra por `contratos_dividas.status NOT IN (terminais)` ao invés de `estagio=ANDAMENTO` (campo legacy) |
| `removerDaFila(filaId, motivo, usuarioId)` | `removerDaFila(filaId, motivo, usuarioId)` | Igual; INSERT `eventos_andamento` agora inclui `contrato_id` (lookup via fila_cobranca.contrato_id) |
| `listarFila(filtros)` | `listarFila(filtros)` | Filter `devedores.deleted_at=is.null` (D-pre-9); enrichment de contrato via `contratos_dividas` |
| `atualizarValoresAtrasados` | `atualizarValoresAtrasados` | Fonte = `contratos_dividas`; usa `dividas.valor_total` somado para fator correção |

**Pre-existing eventos_andamento row shape (PRESERVAR back-compat):**
```javascript
// Eventos antes da Phase 7.13b (legacy):
{ devedor_id, tipo_evento, data_evento, descricao, telefone_usado, data_promessa, giro_carteira_dias, usuario_id, usuario_nome, usuario_email }
// Eventos pós-Phase 7.13b (novo — D-pre-10 Bloco 2+4):
{ devedor_id, contrato_id /* UUID NOT NULL */, tipo_evento, data_evento, descricao, telefone_usado, data_promessa, giro_carteira_dias, usuario_id, usuario_nome, usuario_email }
// Lookup devedor_id pra payload: SELECT devedor_id FROM contratos_dividas WHERE id=$contratoId
// (denormalização legacy — back-compat com Detalhe-Devedor que ainda lê eventos por devedor_id)
```

**Cleanup ordering filaDevedor.test.js (D-pre-10 schema novo):**
```
eventos_andamento (FK: contrato_id → contratos_dividas; devedor_id → devedores)
↓
pagamentos_divida (FK: divida_id → dividas)
↓
dividas (FK: contrato_id → contratos_dividas; devedor_id → devedores)
↓
devedores_dividas (FK: divida_id → dividas; devedor_id → devedores)
↓
contratos_dividas (FK: devedor_id → devedores; credor_id → credores)
↓
fila_cobranca (FK opcional contrato_id)
↓
devedores
↓
credores
```
</interfaces>

<open_questions>
**Q1 — Migration manual aplicada por quem (planner via SDK ou usuário)?**

→ **DECISÃO PLANNER (Task 1):** **executor (Claude) prepara o SQL e PAUSA UAT humano para usuário rodar no Supabase SQL Editor**. Pattern já estabelecido em Phase 7.14 Migration 003 (devedores.deleted_at). Razões:
  (a) Sem framework npm de migration (CONTEXT linha "Migrations manuais em Supabase SQL Editor — sem framework de migration");
  (b) Privilégio TRUNCATE pode requerer auth role específico — usuário sabe credenciais;
  (c) STRATEGY A/B robusto: usuário aplica TRUNCATE; se erro, troca para DELETE e re-roda Bloco 3 isolado;
  (d) Pre-flight (Bloco 2.5 SELECT COUNT) confirma acesso antes de drop.

**Q2 — Wrapper `calcularSaldoContratoAtualizado` em devedorCalc.js: criar ou pular?**

→ **DECISÃO PLANNER:** **CRIAR** — leitura de filaDevedor.js mostra que o callsite vai precisar do `.saldoAtualizado` em ~3-4 pontos diferentes (`listarContratosParaFila` enrichment + `calcularScoreContrato` + score recalc em `entrarNaFila` + `atualizarValoresAtrasados`). Wrapper 1-linha reduz ruído sem violar D-01 (apenas COMPÕE motor). Adicionado em Task 4 antes de rewrite filaDevedor.js (Task 5) consumir.

**Q3 — Como lidar com eventos_andamento.devedor_id pós-D-pre-10 (campo legacy)?**

→ **DECISÃO PLANNER:** **PRESERVAR `devedor_id` no payload** (denormalização). Razões:
  (a) Detalhe-Devedor (`registros_contato`/`lembretes`) e Dashboard cards de lembretes ainda leem por `devedor_id` (D-pre-11 side-effects fora do escopo);
  (b) Migration NÃO drop `devedor_id` — mantém coluna pra back-compat;
  (c) registrarEvento(contratoId, ...) faz lookup `SELECT devedor_id FROM contratos_dividas WHERE id=$contratoId` antes de INSERT eventos_andamento — payload mantém ambos `contrato_id` E `devedor_id`. Status `quitado` auto-derive deferred (CONTEXT.md L185 specifics).

**Q4 — Status terminais para filtro de Fila vs filtro de status do select UI?**

→ **DECISÃO PLANNER:**
  - **Fila ativa (listarContratosParaFila + entrarNaFila + reciclarContratos)** filtra `status NOT IN ('quitado','arquivado','ajuizado')` — 3 terminais excluem do score/atendimento.
  - **listarFila (visualização)** retorna TODOS os status; toggle UI Plan 02 default ON esconde quitados/arquivados client-side (D-pre-6).
  - **Fila operacional excluindo arquivado E quitado E ajuizado**: pattern análogo ao STATUS_TERMINAIS atual em filaDevedor.js L46.

**Q5 — Pre-execute audit obrigatório antes de Task 5 (rewrite filaDevedor.js)?**

→ **DECISÃO PLANNER (Task 3 — pre-execute audit):** **SIM, obrigatório**. Lições aplicadas:
  - feedback_helper_fanout_must_cover_motor_inputs.md — confirmar `calcularDetalheEncargosContrato` espera shapes existentes (dividas + pagamentos_divida).
  - feedback_db_integration_gate_missing.md — gates de PLAN não pegam INSERT real; validar INSERT eventos_andamento com contrato_id NOT NULL FUNCIONA (não FK violation).
  - feedback_proc_judiciais_create_bug_preexisting.md — auditar feature alvo (Fila) utilizável em prod pré-rewrite (smoke navegando 4 telas com estado atual quebrado é OK — bug filaDevedor.js dormente já confirmado).

**Q6 — Plan 01 produz 1 commit ou múltiplos sub-commits atômicos?**

→ **DECISÃO PLANNER:** **1 commit atômico** `feat(07.13b-01): refactor camada de dados Fila por contrato + bug fix D-pre-8`. Razões:
  (a) Migration manual NÃO tem footprint git (vive no Supabase) — registrada em SUMMARY do Plan 03;
  (b) Rewrite filaDevedor.js + filaDevedor.test.js + STATUS_CONTRATO + wrapper formam unidade semântica — tests novos vão junto com rewrite (não fazem sentido separados);
  (c) Pattern 7.10.bug `1 commit atomic` para sub-phases simples;
  (d) Plan 02 fará seu próprio commit `feat(07.13b-02)` para UI; Plan 03 não cria código (só docs + tag).
</open_questions>
</context>

<tasks>

<task type="auto" autonomous="false">
  <name>Task 1: Preparar SQL de migração + PAUSA humana — usuário aplica no Supabase SQL Editor (D-pre-10 STRATEGY A/B)</name>
  <read_first>
    - .planning/phases/07.13b-fila-por-contrato/07.13b-CONTEXT.md (D-pre-10 — 5 blocos SQL + STRATEGY A/B)
    - src/mr-3/mr-cobrancas/src/services/migrations/001_devedores_dividas.sql (pattern Migration 001)
    - src/mr-3/mr-cobrancas/src/services/migrations/002_dividas_tabela.sql (pattern Migration 002)
    - src/mr-3/mr-cobrancas/src/services/migrations/003_devedores_soft_delete.sql (pattern 7.14 Migration 003 — auditar STRATEGY aplicada)
    - .planning/phases/07.14-blindagens-de-integridade-de-dados/07.14-CONTEXT.md (precedente de migration manual prod)
  </read_first>
  <action>
    **Parte A — Criar arquivo de referência da migration** (NÃO é executado por framework — apenas registro pra rastreabilidade):

    Criar `src/mr-3/mr-cobrancas/src/services/migrations/004_fila_por_contrato.sql` com:

    ```sql
    -- Phase 7.13b — Fila por Contrato (D-pre-10)
    -- Aplicar manualmente no Supabase SQL Editor.
    -- STRATEGY A (preferida): TRUNCATE no Bloco 3.
    -- STRATEGY B (fallback): se TRUNCATE der erro RLS/privilégio, substituir Bloco 3 por DELETE.
    -- Documentar STRATEGY aplicada no SUMMARY do Plan 03.

    -- Bloco 1: status por contrato
    ALTER TABLE public.contratos_dividas
      ADD COLUMN IF NOT EXISTS status TEXT NOT NULL DEFAULT 'em_cobranca'
      CHECK (status IN ('em_cobranca','em_localizacao','em_negociacao','notificado','ajuizado','quitado','arquivado'));

    -- Bloco 2: contrato_id em eventos
    ALTER TABLE public.eventos_andamento
      ADD COLUMN IF NOT EXISTS contrato_id UUID REFERENCES public.contratos_dividas(id);

    -- Bloco 2.5: PRE-FLIGHT — confirma acesso à tabela antes do drop legado
    SELECT COUNT(*) FROM public.eventos_andamento;

    -- Bloco 3: γ — DROP histórico legado (decisão de produto, banco minúsculo)
    -- STRATEGY A (preferida): TRUNCATE — fast, reseta sequences, requer privilégio TRUNCATE
    TRUNCATE public.eventos_andamento;

    -- STRATEGY B (fallback): se TRUNCATE der erro de permissão, substituir Bloco 3 por:
    --   DELETE FROM public.eventos_andamento;

    -- Bloco 4: NOT NULL após drop limpo
    ALTER TABLE public.eventos_andamento
      ALTER COLUMN contrato_id SET NOT NULL;

    -- Bloco 5: PostgREST schema reload
    SELECT pg_notify('pgrst', 'reload schema');
    ```

    **Parte B — PAUSA HUMANA bloqueante:** parar execução com mensagem clara:

    ```
    ╔════════════════════════════════════════════════════════════════════╗
    ║ PAUSA #0 — Migration manual obrigatória ANTES de continuar         ║
    ╠════════════════════════════════════════════════════════════════════╣
    ║ 1. Abrir Supabase SQL Editor (projeto mr-cobrancas)                ║
    ║ 2. Copiar o conteúdo de:                                           ║
    ║    src/mr-3/mr-cobrancas/src/services/migrations/004_fila_por_co…  ║
    ║ 3. Executar Bloco 1 → 2 → 2.5 (verificar COUNT) → 3 STRATEGY A     ║
    ║    (TRUNCATE)                                                      ║
    ║ 4. Se TRUNCATE der erro (RLS/privilégio), trocar Bloco 3 por       ║
    ║    DELETE FROM public.eventos_andamento; e re-executar             ║
    ║ 5. Executar Bloco 4 (SET NOT NULL) → Bloco 5 (pg_notify reload)    ║
    ║ 6. Confirmar: SELECT COUNT(*) FROM public.eventos_andamento; = 0   ║
    ║ 7. Confirmar: \\d public.contratos_dividas mostra coluna status    ║
    ║ 8. Confirmar: \\d public.eventos_andamento mostra coluna           ║
    ║    contrato_id NOT NULL                                            ║
    ║                                                                    ║
    ║ Responder: "APROVADO STRATEGY=A" ou "APROVADO STRATEGY=B" ou       ║
    ║ "FALHOU [erro]"                                                    ║
    ║                                                                    ║
    ║ Caso FALHOU → debug com usuário antes de prosseguir.               ║
    ║ STRATEGY aplicada será documentada em SUMMARY do Plan 03.          ║
    ╚════════════════════════════════════════════════════════════════════╝
    ```

    Aguardar resposta APROVADO STRATEGY=X. Anotar STRATEGY aplicada em variável de contexto pro SUMMARY.

    **Parte C — Stage migration file:**
    ```bash
    git -C src/mr-3 add src/mr-cobrancas/src/services/migrations/004_fila_por_contrato.sql
    # NÃO commitar ainda — commit final em Task 9 incluirá esse arquivo
    ```
  </action>
  <acceptance_criteria>
    - Arquivo `src/mr-3/mr-cobrancas/src/services/migrations/004_fila_por_contrato.sql` existe e contém os 5 blocos verbatim do CONTEXT.md L36-65
    - `grep -q "ADD COLUMN IF NOT EXISTS status TEXT NOT NULL DEFAULT 'em_cobranca'" src/mr-3/mr-cobrancas/src/services/migrations/004_fila_por_contrato.sql` exit 0
    - `grep -q "REFERENCES public.contratos_dividas(id)" src/mr-3/mr-cobrancas/src/services/migrations/004_fila_por_contrato.sql` exit 0
    - `grep -q "TRUNCATE public.eventos_andamento" src/mr-3/mr-cobrancas/src/services/migrations/004_fila_por_contrato.sql` exit 0
    - `grep -q "STRATEGY B (fallback)" src/mr-3/mr-cobrancas/src/services/migrations/004_fila_por_contrato.sql` exit 0
    - `grep -q "SET NOT NULL" src/mr-3/mr-cobrancas/src/services/migrations/004_fila_por_contrato.sql` exit 0
    - `grep -q "pg_notify" src/mr-3/mr-cobrancas/src/services/migrations/004_fila_por_contrato.sql` exit 0
    - PAUSA #0 documentada com "APROVADO STRATEGY=A" ou "APROVADO STRATEGY=B" pelo usuário
    - Arquivo staged em `git -C src/mr-3 status --porcelain | grep "004_fila_por_contrato.sql"` retorna match
  </acceptance_criteria>
</task>

<task type="auto" autonomous="false">
  <name>Task 2: Pre-execute audit do schema vivo pós-migração (lições feedback_db_integration_gate_missing.md + feedback_helper_fanout_must_cover_motor_inputs.md)</name>
  <read_first>
    - src/mr-3/mr-cobrancas/src/utils/devedorCalc.js (L840-855 — calcularDetalheEncargosContrato signature + retorno)
    - src/mr-3/mr-cobrancas/src/services/contratos.js (L1-70 — comentário cabeçalho schema)
    - src/mr-3/mr-cobrancas/src/services/filaDevedor.js (FULL — auditar TODOS callsites/refs `contratos`/`parcelas`/`eventos_andamento.devedor_id`)
    - src/mr-3/mr-cobrancas/src/services/migrations/004_fila_por_contrato.sql (Task 1)
  </read_first>
  <action>
    **Parte A — Audit schema vivo via Supabase SQL Editor (PAUSA mini bloqueante):**

    Pedir usuário para rodar 4 queries no Supabase SQL Editor e colar resultado:

    ```sql
    -- Audit 1: contratos_dividas tem status com CHECK
    SELECT column_name, data_type, is_nullable, column_default
      FROM information_schema.columns
     WHERE table_name='contratos_dividas' AND column_name='status';
    -- Esperado: status | text | NO | 'em_cobranca'::text

    SELECT con.consrc
      FROM pg_constraint con
      JOIN pg_class rel ON rel.oid = con.conrelid
     WHERE rel.relname = 'contratos_dividas' AND con.conname LIKE '%status%';
    -- Esperado: CHECK (status IN ('em_cobranca','em_localizacao',...,'arquivado'))
    -- (em Postgres 12+ usar pg_get_constraintdef(con.oid) ao invés de consrc)

    -- Audit 2: eventos_andamento.contrato_id existe NOT NULL com FK
    SELECT column_name, data_type, is_nullable
      FROM information_schema.columns
     WHERE table_name='eventos_andamento' AND column_name='contrato_id';
    -- Esperado: contrato_id | uuid | NO

    -- Audit 3: eventos_andamento truncado (γ)
    SELECT COUNT(*) FROM public.eventos_andamento;
    -- Esperado: 0

    -- Audit 4: INSERT de teste eventos_andamento com contrato_id válido NÃO falha
    -- (Pegar 1 contrato existente, 1 devedor existente, INSERT simulado)
    SELECT id FROM public.contratos_dividas LIMIT 1;
    SELECT id FROM public.devedores WHERE deleted_at IS NULL LIMIT 1;
    -- Usuário usa esses IDs para INSERT real:
    -- INSERT INTO public.eventos_andamento (devedor_id, contrato_id, tipo_evento, data_evento)
    -- VALUES (<dev_id>, '<contrato_id>', 'CONTATO_COM_CLIENTE', NOW())
    -- RETURNING id;
    -- Depois: DELETE FROM public.eventos_andamento WHERE id = <id_inserido>;
    ```

    **PAUSA mini bloqueante:** aguardar usuário colar resultado dos 4 audits ou responder "APROVADO" se todos os checks passaram conforme esperado. Caso algum FALHE → PAUSA UAT e abre backlog antes de prosseguir (lição feedback_proc_judiciais_create_bug_preexisting.md).

    **Parte B — Audit estático no código (executor faz):**

    Confirmar grep counts atuais (baseline pré-rewrite):
    ```bash
    cd src/mr-3/mr-cobrancas

    # Stale refs `contratos` (substring isolada — exclui contratos_dividas/contratos_historico)
    grep -nE "\\bcontratos\\b[^_]" src/services/filaDevedor.js | grep -v "contratos_dividas\\|contratos_historico"
    # Esperado: 2+ matches em entrarNaFila L253 + reciclarContratos L491 (bug pré-existente D-pre-8 — confirmar antes de rewrite)

    # Stale refs `parcelas` (tabela inexistente — Phase 5+ migrou pra dividas)
    grep -nE "\\bparcelas\\b" src/services/filaDevedor.js
    # Esperado: 1+ match em calcularScorePrioridade L221-223 (referência a parcelas — schema antigo)

    grep -nE "\\bparcelas\\b" src/services/filaDevedor.test.js
    # Esperado: 4+ matches no setup() (tabela inexistente — D-pre-8 confirma test outdated)

    # Audit calcularDetalheEncargosContrato signature
    grep -nE "calcularDetalheEncargosContrato" src/utils/devedorCalc.js
    # Esperado: 1 match em L849 (definição) + 1+ matches em consumers (DetalheContrato.jsx etc.)

    # W1.1 — Audit supabase.js Prefer: return=representation header (lock otimista proximoContrato — PATCH precisa retornar array)
    grep -E "Prefer.*return=representation" src/config/supabase.js
    # Esperado: 1+ match em sb() function — confirma que PATCH com filter retorna array (lock otimista proximoContrato Função 4 funciona)
    ```

    Documentar contagens encontradas em comentário da PR/SUMMARY (Task 9): "Audit estático pré-rewrite: stale refs `contratos` = N, `parcelas` filaDevedor.js = M, `parcelas` filaDevedor.test.js = K. Pós-rewrite todos devem ser 0. Audit W1.1: supabase.js Prefer:return=representation = CONFIRMED (PATCH retorna array)."
  </action>
  <acceptance_criteria>
    - Audit SQL 1: usuário confirma `contratos_dividas.status` existe com CHECK 7 valores e default 'em_cobranca'
    - Audit SQL 2: usuário confirma `eventos_andamento.contrato_id` UUID NOT NULL existe com FK pra contratos_dividas
    - Audit SQL 3: usuário confirma `SELECT COUNT(*) FROM eventos_andamento` retorna 0 (γ DROP confirmado)
    - Audit SQL 4: usuário confirma INSERT real em eventos_andamento com contrato_id válido + devedor_id válido FUNCIONA (não FK violation) e cleanup do row de teste
    - Audit estático Parte B: contagens grep documentadas (esperado: pré-rewrite tem stale refs `contratos`/`parcelas` que serão zerados pós-rewrite)
    - **W1.1 supabase.js Prefer header confirmed:** `grep -E "Prefer.*return=representation" src/mr-3/mr-cobrancas/src/config/supabase.js | wc -l` >= 1 (lock otimista proximoContrato Função 4 retorna array em PATCH com filter)
    - PAUSA aprovada explicitamente pelo usuário antes de prosseguir
  </acceptance_criteria>
</task>

<task type="auto" autonomous="false">
  <name>Task 3: Adicionar STATUS_CONTRATO em utils/constants.js (D-pre-4 — 7 valores PT-BR)</name>
  <read_first>
    - src/mr-3/mr-cobrancas/src/utils/constants.js (FULL — pattern STATUS_DEV)
    - .planning/phases/07.13b-fila-por-contrato/07.13b-CONTEXT.md (D-pre-4 lista 7 valores)
  </read_first>
  <files>
    - src/mr-3/mr-cobrancas/src/utils/constants.js
  </files>
  <action>
    Adicionar bloco DEPOIS de `STATUS_DEV` export (L2-12) e ANTES de `UFS` export (L15):

    ```javascript
    // ─── STATUS CONTRATO (Phase 7.13b D-pre-4) ────────────────────
    export const STATUS_CONTRATO = [
      { v: "em_cobranca",     l: "Em Cobrança",      cor: "#64748b", bg: "#f1f5f9" },
      { v: "em_localizacao",  l: "Em Localização",   cor: "#2563eb", bg: "#dbeafe" },
      { v: "em_negociacao",   l: "Em Negociação",    cor: "#d97706", bg: "#fef3c7" },
      { v: "notificado",      l: "Notificado",       cor: "#7c3aed", bg: "#ede9fe" },
      { v: "ajuizado",        l: "Ajuizado",         cor: "#c2410c", bg: "#ffedd5" },
      { v: "quitado",         l: "Quitado",          cor: "#065f46", bg: "#d1fae5" },
      { v: "arquivado",       l: "Arquivado",        cor: "#475569", bg: "#e2e8f0" },
    ];

    // STATUS terminais — Fila operacional EXCLUI; toggle UI Plan 02 default ON esconde quitado/arquivado
    export const STATUS_CONTRATO_TERMINAIS = ["quitado", "arquivado", "ajuizado"];

    // STATUS ativos — usados em listarContratosParaFila / entrarNaFila / reciclarContratos
    export const STATUS_CONTRATO_ATIVOS = ["em_cobranca", "em_localizacao", "em_negociacao", "notificado"];
    ```

    Não modificar STATUS_DEV existente — Plan 02 vai usar STATUS_DEV em outras telas (e.g. cadastro de devedor) que continuam por devedor.

    Validar build:
    ```bash
    cd src/mr-3/mr-cobrancas && npm run build
    ```
  </action>
  <acceptance_criteria>
    - `grep -q "export const STATUS_CONTRATO" src/mr-3/mr-cobrancas/src/utils/constants.js` exit 0
    - `grep -q "STATUS_CONTRATO_TERMINAIS" src/mr-3/mr-cobrancas/src/utils/constants.js` exit 0
    - `grep -q "STATUS_CONTRATO_ATIVOS" src/mr-3/mr-cobrancas/src/utils/constants.js` exit 0
    - 7 valores presentes: `grep -cE '\\bv: "(em_cobranca|em_localizacao|em_negociacao|notificado|ajuizado|quitado|arquivado)"' src/mr-3/mr-cobrancas/src/utils/constants.js` >= 7
    - STATUS_DEV preservado: `grep -q "export const STATUS_DEV" src/mr-3/mr-cobrancas/src/utils/constants.js` exit 0
    - Build limpo: `cd src/mr-3/mr-cobrancas && npm run build` exit 0
  </acceptance_criteria>
</task>

<task type="auto" autonomous="false">
  <name>Task 4: Adicionar wrapper opcional calcularSaldoContratoAtualizado em devedorCalc.js (D-pre-12 Claude's Discretion — D-01 motor INTOCADO)</name>
  <read_first>
    - src/mr-3/mr-cobrancas/src/utils/devedorCalc.js (L840-855 — definição de calcularDetalheEncargosContrato + comentário cabeçalho)
    - .planning/phases/07.13b-fila-por-contrato/07.13b-CONTEXT.md (D-pre-12 — wrapper thin opcional)
  </read_first>
  <files>
    - src/mr-3/mr-cobrancas/src/utils/devedorCalc.js
  </files>
  <action>
    Adicionar APENAS após linha 854 (final da função `calcularDetalheEncargosContrato`), preservando export existente:

    ```javascript
    /**
     * Phase 7.13b D-pre-12: helper thin para Fila por Contrato.
     * Compõe motor existente — NÃO modifica calcularDetalheEncargos (D-01 INTOCADO).
     * Reduz ruído em callsites da Fila (listarContratosParaFila + calcularScoreContrato + atualizarValoresAtrasados).
     *
     * @param {Array}  dividasDoContrato    parcelas/dívidas do contrato (lista de rows da tabela `dividas`)
     * @param {Array}  allPagamentosDivida  lista global de pagamentos_divida (adapter filtra por divida_id)
     * @param {string} hoje                 "YYYY-MM-DD"
     * @returns {number}                    saldo atualizado em R$ (já com encargos aplicados pelo motor)
     */
    export const calcularSaldoContratoAtualizado = (dividasDoContrato, allPagamentosDivida, hoje) =>
      calcularDetalheEncargosContrato(dividasDoContrato, allPagamentosDivida, hoje).saldoAtualizado;
    ```

    **NÃO MODIFICAR** funções existentes:
    - `calcularDetalheEncargos` (L365) — D-01 motor base INTOCADO
    - `calcularSaldoDevedorAtualizado` (L59) — preservado (outros consumers)
    - `calcularDetalheEncargosContrato` (L849) — preservado (DetalheContrato.jsx + outros)
    - `calcularValorFace` (preservado)
    - `calcularResumoFinanceiro` (preservado)

    Validar diff:
    ```bash
    cd src/mr-3/mr-cobrancas
    git diff src/utils/devedorCalc.js
    # Esperado: APENAS adições no fim do arquivo (linhas após L854); ZERO modificações em funções existentes
    ```

    Validar build + regressão:
    ```bash
    npm run build           # exit 0
    npm run test:regressao  # exit 0 — suite atual da 7.14 PRESERVADA verde (motor INTOCADO)
    ```
  </action>
  <acceptance_criteria>
    - `grep -q "export const calcularSaldoContratoAtualizado" src/mr-3/mr-cobrancas/src/utils/devedorCalc.js` exit 0
    - `grep -q "calcularDetalheEncargosContrato.*saldoAtualizado" src/mr-3/mr-cobrancas/src/utils/devedorCalc.js` exit 0 (wrapper compõe — não duplica)
    - **D-01 motor INTOCADO (gate dual):** `git -C src/mr-3 diff -- src/mr-cobrancas/src/utils/devedorCalc.js | grep -E "^-(\\s+)" | wc -l` retorna 0 (zero linhas removidas — apenas adições)
    - `git -C src/mr-3 diff -- src/mr-cobrancas/src/utils/devedorCalc.js | grep -cE "^@@" ` retorna 1 (UM hunk apenas — adição contígua no fim do arquivo)
    - Funções D-01 preservadas verbatim (N >= 5 exports): `grep -cE "^export (async )?function (calcularDetalheEncargos|calcularSaldoDevedorAtualizado|calcularDetalheEncargosContrato|calcularValorFace|calcularResumoFinanceiro)" src/mr-3/mr-cobrancas/src/utils/devedorCalc.js` >= 5
    - Build: `cd src/mr-3/mr-cobrancas && npm run build` exit 0
    - Regressão atual da 7.14 verde: `cd src/mr-3/mr-cobrancas && npm run test:regressao` exit 0
  </acceptance_criteria>
</task>

<task type="auto" autonomous="false">
  <name>Task 5: Rewrite total filaDevedor.js (10 funções com fonte CONTRATOS — fix-forward D-pre-8 — B2.1 cross-impact: shape enriquecido para Plan 02 DetalheContrato)</name>
  <read_first>
    - src/mr-3/mr-cobrancas/src/services/filaDevedor.js (FULL — referência das 10 funções legadas)
    - src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx L117 (signature REAL: { contrato, dividas, devedores, credores, allPagamentos, allPagamentosDivida, hoje, onVoltar, onVerDetalhe, onCarregarTudo } — Plan 02 alimenta esse shape via enrichment do listarContratosParaFila)
    - src/mr-3/mr-cobrancas/src/services/devedoresDividas.js (junction shape se exists — para fetch co-devedores via devedores_dividas)
    - src/mr-3/mr-cobrancas/src/utils/devedorCalc.js (L840-865 pós-Task 4 — calcularDetalheEncargosContrato + calcularSaldoContratoAtualizado)
    - src/mr-3/mr-cobrancas/src/utils/constants.js (pós-Task 3 — STATUS_CONTRATO + STATUS_CONTRATO_ATIVOS + STATUS_CONTRATO_TERMINAIS)
    - src/mr-3/mr-cobrancas/src/services/contratos.js (L1-100 — schema cabeçalho + funções existentes pra back-compat)
    - src/mr-3/mr-cobrancas/src/config/supabase.js (sb/dbGet/dbInsert/dbUpdate/dbDelete signatures)
    - .planning/phases/07.13b-fila-por-contrato/07.13b-CONTEXT.md (D-pre-1, D-pre-4, D-pre-8, D-pre-9, D-pre-12 — escopo do rewrite)
  </read_first>
  <files>
    - src/mr-3/mr-cobrancas/src/services/filaDevedor.js
  </files>
  <action>
    **REWRITE TOTAL.** Substituir TODO o conteúdo de `src/mr-3/mr-cobrancas/src/services/filaDevedor.js` por:

    1. **Imports** — substituir L1-3 atual:
    ```javascript
    import { dbGet, dbInsert, dbUpdate, dbDelete, sb } from "../config/supabase.js";
    import { calcularFatorCorrecao } from "../utils/correcao.js";
    import { calcularSaldoContratoAtualizado, calcularDetalheEncargosContrato } from "../utils/devedorCalc.js";
    import { STATUS_CONTRATO_ATIVOS, STATUS_CONTRATO_TERMINAIS } from "../utils/constants.js";
    ```

    2. **Helpers preservados verbatim** (L5-42 do arquivo legado):
    - `validateUUID(id, label)`
    - `validateBigInt(id, label)`
    - `extractUsuario(u)`
    - `parseDividasSvc(raw)` (preservar — usado em fallback)

    3. **Constantes adaptadas** — substituir L44-54 por:
    ```javascript
    // Bonus de score por status contrato (quanto maior, mais urgente)
    const SCORE_STATUS_CONTRATO = {
      em_cobranca:    100,
      em_localizacao:  80,
      notificado:      60,
      em_negociacao:   40,
      // terminais (quitado/arquivado/ajuizado) recebem 0 implicitamente
    };

    function calcularScoreContrato(contrato) {
      const bonus = SCORE_STATUS_CONTRATO[contrato.status] || 0;
      const saldo = parseFloat(contrato._saldo_atualizado || 0);
      const criado = contrato.created_at ? new Date(contrato.created_at) : new Date();
      const diasCadastro = Math.floor((Date.now() - criado) / 86400000);
      return bonus + (saldo / 100) + (diasCadastro * 0.5);
    }

    function calcularPrioridadeContrato(score) {
      return score >= 120 ? "ALTA" : score >= 80 ? "MEDIA" : "BAIXA";
    }
    ```

    4. **Função 1: `listarContratosParaFila(filtros = {})`** (rename + rewrite):
    ```javascript
    async function listarContratosParaFila(filtros = {}) {
      try {
        // 1. Buscar contratos com status ativo (D-pre-4 7 valores; ativos = NOT IN terminais)
        const ativosList = STATUS_CONTRATO_ATIVOS.join(",");
        const contratos = await dbGet(
          "contratos_dividas",
          `select=*&status=in.(${ativosList})&order=created_at.asc`
        );
        if (!contratos.length) return { success: true, data: [], error: null };

        const contratoIds = contratos.map((c) => c.id);
        const devedorIds = [...new Set(contratos.map((c) => c.devedor_id).filter(Boolean))];
        const credorIds = [...new Set(contratos.map((c) => c.credor_id).filter(Boolean))];

        // 2. Buscar devedores (filter deleted_at — D-pre-9) + credores em paralelo
        const hoje = new Date().toISOString().slice(0, 10);
        const [devedores, credores, dividas, allPagamentosDivida, filaAtiva] = await Promise.all([
          devedorIds.length > 0
            ? dbGet("devedores", `id=in.(${devedorIds.join(",")})&deleted_at=is.null`)
            : Promise.resolve([]),
          credorIds.length > 0
            ? dbGet("credores", `id=in.(${credorIds.join(",")})`)
            : Promise.resolve([]),
          dbGet("dividas", `contrato_id=in.(${contratoIds.join(",")})&select=*`),
          dbGet("pagamentos_divida", `divida_id=neq.00000000-0000-0000-0000-000000000000&select=*`),
          dbGet(
            "fila_cobranca",
            `select=contrato_id,status_fila,bloqueado_ate,usuario_id&contrato_id=in.(${contratoIds.join(",")})&status_fila=in.(AGUARDANDO,EM_ATENDIMENTO,ACIONADO)`
          ),
        ]);

        const devedorMap = Object.fromEntries(devedores.map((d) => [String(d.id), d]));
        const credorMap = Object.fromEntries(credores.map((c) => [String(c.id), c]));
        const filaMap = {};
        for (const f of filaAtiva) {
          const key = String(f.contrato_id);
          if (!filaMap[key] || f.status_fila === "EM_ATENDIMENTO") filaMap[key] = f;
        }

        // 3. Group dividas por contrato_id
        const dividasPorContrato = {};
        for (const d of dividas) {
          const key = String(d.contrato_id);
          if (!dividasPorContrato[key]) dividasPorContrato[key] = [];
          dividasPorContrato[key].push(d);
        }

        // 4. Eventos por contrato (D-pre-10 — eventos_andamento.contrato_id NOT NULL pós-migração)
        const [eventosRecentes, countHoje] = await Promise.all([
          dbGet(
            "eventos_andamento",
            `select=contrato_id,tipo_evento,data_evento,usuario_nome&contrato_id=in.(${contratoIds.join(",")})&order=data_evento.desc&limit=2000`
          ),
          dbGet(
            "eventos_andamento",
            `select=id&contrato_id=in.(${contratoIds.join(",")})&data_evento=gte.${hoje}T00:00:00&limit=1000`
          ),
        ]);

        const ultimoEventoPorContrato = {};
        for (const ev of eventosRecentes) {
          const key = String(ev.contrato_id);
          if (!ultimoEventoPorContrato[key]) ultimoEventoPorContrato[key] = ev;
        }
        const totalEventosHoje = countHoje.length;

        // 5. Filter devedor soft-deleted (D-pre-9): drop contratos cujo devedor não está em devedorMap
        let resultado = contratos
          .filter((c) => c.devedor_id == null || devedorMap[String(c.devedor_id)])
          .map((c) => {
            const dividasDoContrato = dividasPorContrato[String(c.id)] || [];
            const saldoAtualizado = calcularSaldoContratoAtualizado(dividasDoContrato, allPagamentosDivida, hoje);
            const filaEntry = filaMap[String(c.id)] || null;
            const bloqueado = filaEntry?.bloqueado_ate && filaEntry.bloqueado_ate >= hoje;
            const emAtendimento = filaEntry?.status_fila === "EM_ATENDIMENTO";
            const ue = ultimoEventoPorContrato[String(c.id)] || null;
            const ueData = ue?.data_evento?.slice(0, 10) || null;
            const diasSemContato = ueData
              ? Math.floor((Date.now() - new Date(ueData + "T12:00:00")) / 86400000)
              : null;
            // B2.1 cross-impact: arrays para DetalheContrato signature REAL ({ devedores, credores, allPagamentosDivida })
            const dividaIdsDoContrato = new Set(dividasDoContrato.map(d => String(d.id)));
            const pagamentosDoContrato = (allPagamentosDivida || []).filter(p => dividaIdsDoContrato.has(String(p.divida_id)));
            // Co-devedores via junction devedores_dividas (todos devedores ligados às dividas do contrato)
            const devedorIdsDoContrato = new Set();
            if (c.devedor_id != null) devedorIdsDoContrato.add(String(c.devedor_id));
            for (const d of dividasDoContrato) {
              if (d.devedor_id != null) devedorIdsDoContrato.add(String(d.devedor_id));
            }
            const devedoresDoContrato = [...devedorIdsDoContrato]
              .map(id => devedorMap[id])
              .filter(Boolean);
            // Credores: principal do contrato + qualquer credor referenciado nas dividas
            const credorIdsDoContrato = new Set();
            if (c.credor_id != null) credorIdsDoContrato.add(String(c.credor_id));
            for (const d of dividasDoContrato) {
              if (d.credor_id != null) credorIdsDoContrato.add(String(d.credor_id));
            }
            const credoresDoContrato = [...credorIdsDoContrato]
              .map(id => credorMap[id])
              .filter(Boolean);
            const enriched = {
              ...c,
              _devedor: devedorMap[String(c.devedor_id)] || null,    // singular (back-compat — devedor principal)
              _credor: credorMap[String(c.credor_id)] || null,        // singular (back-compat)
              _devedores: devedoresDoContrato,                        // PLURAL (B2.1 — Plan 02 DetalheContrato.devedores)
              _credores: credoresDoContrato,                          // PLURAL (B2.1 — Plan 02 DetalheContrato.credores)
              _dividas: dividasDoContrato,
              _pagamentos_divida: pagamentosDoContrato,               // B2.1 — Plan 02 DetalheContrato.allPagamentosDivida (pre-filtrado por contrato)
              _saldo_atualizado: saldoAtualizado,
              _fila: filaEntry,
              _bloqueado: !!bloqueado,
              _em_atendimento: emAtendimento,
              _ultimo_evento: ue,
              _dias_sem_contato: diasSemContato,
            };
            const score = calcularScoreContrato(enriched);
            const prioridade = calcularPrioridadeContrato(score);
            return { ...enriched, _score: score, _prioridade: prioridade };
          });

        // 6. Aplicar filtros client-side
        if (filtros.status_list?.length) {
          resultado = resultado.filter((c) => filtros.status_list.includes(c.status));
        }
        if (filtros.credor_id) {
          resultado = resultado.filter((c) => String(c.credor_id) === String(filtros.credor_id));
        }
        if (filtros.prioridade) {
          resultado = resultado.filter((c) => c._prioridade === filtros.prioridade);
        }
        if (filtros.busca) {
          const q = filtros.busca.toLowerCase();
          resultado = resultado.filter(
            (c) => c._devedor?.nome?.toLowerCase().includes(q)
                || c._devedor?.cpf_cnpj?.toLowerCase().includes(q)
          );
        }
        if (filtros.valor_min != null) {
          resultado = resultado.filter((c) => c._saldo_atualizado >= filtros.valor_min);
        }
        if (filtros.valor_max != null) {
          resultado = resultado.filter((c) => c._saldo_atualizado <= filtros.valor_max);
        }

        // 7. Ordenar por score desc
        resultado.sort((a, b) => b._score - a._score);

        return { success: true, data: resultado, totalEventosHoje, error: null };
      } catch (err) {
        return { success: false, data: null, error: err.message };
      }
    }
    ```

    5. **Função 2: `calcularScorePrioridade(contratoId)`** (assinatura igual; rewrite corpo — usa contratos_dividas + dividas, NÃO parcelas):
    ```javascript
    async function calcularScorePrioridade(contratoId) {
      try {
        validateUUID(contratoId, "contratoId");
        const contratos = await dbGet("contratos_dividas", `select=*&id=eq.${contratoId}`);
        if (!contratos.length) throw new Error(`Contrato ${contratoId} não encontrado`);
        const contrato = contratos[0];
        const dividas = await dbGet("dividas", `contrato_id=eq.${contratoId}&select=*`);
        const allPagamentos = await dbGet("pagamentos_divida", `divida_id=neq.00000000-0000-0000-0000-000000000000&select=*`);
        const hoje = new Date().toISOString().slice(0, 10);
        const saldo = calcularSaldoContratoAtualizado(dividas, allPagamentos, hoje);
        const enriched = { ...contrato, _saldo_atualizado: saldo };
        const score = calcularScoreContrato(enriched);
        const prioridade = calcularPrioridadeContrato(score);

        await sb(
          "fila_cobranca",
          "PATCH",
          { score_prioridade: score, prioridade, updated_at: new Date().toISOString() },
          `?contrato_id=eq.${contratoId}`
        );
        return { success: true, data: { score, prioridade }, error: null };
      } catch (err) {
        return { success: false, data: null, error: err.message };
      }
    }
    ```

    6. **Função 3: `entrarNaFila()`** (FIX-FORWARD D-pre-8 — `contratos` → `contratos_dividas`):
    ```javascript
    async function entrarNaFila() {
      try {
        const ativosList = STATUS_CONTRATO_ATIVOS.join(",");
        const contratos = await dbGet(
          "contratos_dividas",
          `select=id,devedor_id,status&status=in.(${ativosList})`
        );
        const filaAtiva = await dbGet(
          "fila_cobranca",
          "select=contrato_id&status_fila=in.(AGUARDANDO,EM_ATENDIMENTO)"
        );
        const idsNaFila = new Set(filaAtiva.map((f) => f.contrato_id));
        const novos = contratos.filter((c) => !idsNaFila.has(c.id));
        const jaExistentes = contratos.filter((c) => idsNaFila.has(c.id));

        let inseridos = 0;
        for (const contrato of novos) {
          await dbInsert("fila_cobranca", {
            contrato_id: contrato.id,
            devedor_id: contrato.devedor_id,
            status_fila: "AGUARDANDO",
            score_prioridade: 0,
            prioridade: "MEDIA",
          });
          await calcularScorePrioridade(contrato.id);
          inseridos++;
        }

        let atualizados = 0;
        for (const contrato of jaExistentes) {
          await calcularScorePrioridade(contrato.id);
          atualizados++;
        }

        return { success: true, data: { inseridos, atualizados }, error: null };
      } catch (err) {
        return { success: false, data: null, error: err.message };
      }
    }
    ```

    7. **Função 4: `proximoContrato(usuarioId, _tentativa = 1)`** (rename de `proximoDevedor`):
    ```javascript
    async function proximoContrato(usuarioId, _tentativa = 1) {
      try {
        const { uid, uNome, uEmail } = extractUsuario(usuarioId);
        const r = await listarContratosParaFila({});
        if (!r.success) throw new Error(r.error);

        const candidatos = (r.data || []).filter((c) => !c._em_atendimento && !c._bloqueado);
        if (!candidatos.length) return { success: true, data: null, error: null };

        const contrato = candidatos[0]; // ordenado por score desc
        const filaExistente = contrato._fila;
        let filaEntry;

        if (filaExistente && filaExistente.status_fila === "AGUARDANDO") {
          const updated = await sb(
            "fila_cobranca",
            "PATCH",
            {
              status_fila: "EM_ATENDIMENTO",
              usuario_id: uid,
              usuario_nome: uNome,
              usuario_email: uEmail,
              data_acionamento: new Date().toISOString(),
              updated_at: new Date().toISOString(),
            },
            `?contrato_id=eq.${contrato.id}&status_fila=eq.AGUARDANDO`
          );
          if (!updated || updated.length === 0) {
            if (_tentativa >= 3) return { success: true, data: null, error: null };
            return proximoContrato(usuarioId, _tentativa + 1);
          }
          filaEntry = updated[0];
        } else {
          const inserted = await dbInsert("fila_cobranca", {
            contrato_id: contrato.id,
            devedor_id: contrato.devedor_id,
            status_fila: "EM_ATENDIMENTO",
            usuario_id: uid,
            usuario_nome: uNome,
            usuario_email: uEmail,
            score_prioridade: contrato._score,
            prioridade: contrato._prioridade,
            data_acionamento: new Date().toISOString(),
          });
          filaEntry = Array.isArray(inserted) ? inserted[0] : inserted;
        }

        const eventos = await dbGet(
          "eventos_andamento",
          `contrato_id=eq.${contrato.id}&order=data_evento.desc`
        );

        return {
          success: true,
          data: { fila: filaEntry, contrato, devedor: contrato._devedor, dividas: contrato._dividas, eventos },
          error: null,
        };
      } catch (err) {
        return { success: false, data: null, error: err.message };
      }
    }
    ```

    8. **Função 5: `registrarEvento(contratoId, usuarioId, dadosEvento)`** (assinatura mudou — devedorId → contratoId; payload inclui contrato_id NOT NULL + devedor_id legacy):
    ```javascript
    async function registrarEvento(contratoId, usuarioId, dadosEvento) {
      try {
        validateUUID(contratoId, "contratoId");
        const { uid, uNome, uEmail } = extractUsuario(usuarioId);
        const { tipo_evento, descricao, telefone_usado, data_promessa, giro_carteira_dias } = dadosEvento;

        // Lookup devedor_id (denormalização legacy — D-pre-11 side-effects preservados)
        const cArr = await dbGet("contratos_dividas", `id=eq.${contratoId}&select=devedor_id&limit=1`);
        const devedorId = Array.isArray(cArr) && cArr.length ? cArr[0].devedor_id : null;

        const eventoPayload = {
          contrato_id: contratoId,
          devedor_id: devedorId, // back-compat: Detalhe-Devedor lê eventos por devedor_id
          usuario_id: uid,
          usuario_nome: uNome,
          usuario_email: uEmail,
          tipo_evento,
          data_evento: new Date().toISOString(),
        };
        if (descricao !== undefined) eventoPayload.descricao = descricao;
        if (telefone_usado !== undefined) eventoPayload.telefone_usado = telefone_usado;
        if (data_promessa !== undefined) eventoPayload.data_promessa = data_promessa;
        if (giro_carteira_dias !== undefined) eventoPayload.giro_carteira_dias = giro_carteira_dias;

        const resultado = await dbInsert("eventos_andamento", eventoPayload);
        const evento = Array.isArray(resultado) ? resultado[0] : resultado;

        if (tipo_evento === "ACORDO") {
          await sb(
            "fila_cobranca",
            "PATCH",
            { status_fila: "REMOVIDO", updated_at: new Date().toISOString() },
            `?contrato_id=eq.${contratoId}`
          );
          return { success: true, data: evento, error: null };
        }

        let bloqueadoAte = null;
        if (tipo_evento === "PROMESSA_PAGAMENTO" && data_promessa) bloqueadoAte = data_promessa;
        if (giro_carteira_dias > 0) {
          const giroData = new Date(Date.now() + giro_carteira_dias * 86400000)
            .toISOString().slice(0, 10);
          if (!bloqueadoAte || giroData > bloqueadoAte) bloqueadoAte = giroData;
        }
        if (bloqueadoAte !== null) {
          await sb(
            "fila_cobranca",
            "PATCH",
            { bloqueado_ate: bloqueadoAte, status_fila: "ACIONADO", updated_at: new Date().toISOString() },
            `?contrato_id=eq.${contratoId}&status_fila=eq.EM_ATENDIMENTO`
          );
        }
        return { success: true, data: evento, error: null };
      } catch (err) {
        return { success: false, data: null, error: err.message };
      }
    }
    ```

    9. **Função 6: `alterarStatusContrato(contratoId, novoStatus, usuarioId)`** (rename + UPDATE contratos_dividas.status, NÃO devedores.status):
    ```javascript
    async function alterarStatusContrato(contratoId, novoStatus, usuarioId) {
      try {
        validateUUID(contratoId, "contratoId");
        const { uid, uNome, uEmail } = extractUsuario(usuarioId);

        await sb(
          "contratos_dividas",
          "PATCH",
          { status: novoStatus, updated_at: new Date().toISOString() },
          `?id=eq.${contratoId}`
        );

        if (STATUS_CONTRATO_TERMINAIS.includes(novoStatus)) {
          await sb(
            "fila_cobranca",
            "PATCH",
            { status_fila: "REMOVIDO", updated_at: new Date().toISOString() },
            `?contrato_id=eq.${contratoId}&status_fila=in.(AGUARDANDO,EM_ATENDIMENTO,ACIONADO)`
          );
        }

        // Lookup devedor_id pra registrar evento (back-compat)
        const cArr = await dbGet("contratos_dividas", `id=eq.${contratoId}&select=devedor_id&limit=1`);
        const devedorId = Array.isArray(cArr) && cArr.length ? cArr[0].devedor_id : null;

        await dbInsert("eventos_andamento", {
          contrato_id: contratoId,
          devedor_id: devedorId,
          usuario_id: uid,
          usuario_nome: uNome,
          usuario_email: uEmail,
          tipo_evento: "CONTATO_COM_CLIENTE",
          descricao: `Status alterado para: ${novoStatus}`,
          data_evento: new Date().toISOString(),
        });

        return { success: true, data: { novoStatus }, error: null };
      } catch (err) {
        return { success: false, data: null, error: err.message };
      }
    }
    ```

    10. **Função 7: `reciclarContratos(filtros = {}, equipeId = null)`** (FIX-FORWARD D-pre-8 — `contratos` → `contratos_dividas`):
    ```javascript
    async function reciclarContratos(filtros = {}, equipeId = null) {
      try {
        if (filtros.devedor_id !== undefined) validateBigInt(filtros.devedor_id, "devedor_id");
        const filaAtiva = await dbGet(
          "fila_cobranca",
          "select=contrato_id&status_fila=in.(AGUARDANDO,EM_ATENDIMENTO)"
        );
        const idsNaFila = filaAtiva.map((f) => f.contrato_id).filter(Boolean);

        const ativosList = STATUS_CONTRATO_ATIVOS.join(",");
        let contratos;
        if (idsNaFila.length > 0) {
          contratos = await dbGet(
            "contratos_dividas",
            `select=*&status=in.(${ativosList})&id=not.in.(${idsNaFila.join(",")})`
          );
        } else {
          contratos = await dbGet("contratos_dividas", `select=*&status=in.(${ativosList})`);
        }

        if (filtros.dias_sem_contato) {
          const limiteData = new Date(Date.now() - filtros.dias_sem_contato * 86400000).toISOString();
          const filtered = [];
          for (const contrato of contratos) {
            const eventos = await dbGet(
              "eventos_andamento",
              `contrato_id=eq.${contrato.id}&order=data_evento.desc&limit=1`
            );
            const ultimoEvento = eventos[0];
            if (!ultimoEvento || ultimoEvento.data_evento < limiteData) filtered.push(contrato);
          }
          contratos = filtered;
        }

        const reciclados = [];
        for (const contrato of contratos) {
          const inserted = await dbInsert("fila_cobranca", {
            contrato_id: contrato.id,
            devedor_id: contrato.devedor_id,
            equipe_id: equipeId || null,
            status_fila: "AGUARDANDO",
            score_prioridade: 0,
          });
          const item = Array.isArray(inserted) ? inserted[0] : inserted;
          reciclados.push(item);
          await calcularScorePrioridade(contrato.id);
        }
        return { success: true, data: { reciclados }, error: null };
      } catch (err) {
        return { success: false, data: null, error: err.message };
      }
    }
    ```

    11. **Função 8: `removerDaFila(filaId, motivo, usuarioId)`** (assinatura igual; payload eventos_andamento agora inclui contrato_id):
    ```javascript
    async function removerDaFila(filaId, motivo, usuarioId) {
      try {
        validateUUID(filaId, "filaId");
        const { uid, uNome, uEmail } = extractUsuario(usuarioId);

        const resultado = await dbUpdate("fila_cobranca", filaId, {
          status_fila: "REMOVIDO",
          updated_at: new Date().toISOString(),
        });

        const filaItems = await dbGet("fila_cobranca", `select=devedor_id,contrato_id&id=eq.${filaId}`);
        const fila = filaItems[0] || {};

        if (fila.contrato_id) {
          await dbInsert("eventos_andamento", {
            contrato_id: fila.contrato_id,
            devedor_id: fila.devedor_id ? Number(fila.devedor_id) : null,
            usuario_id: uid,
            usuario_nome: uNome,
            usuario_email: uEmail,
            tipo_evento: "SEM_CONTATO",
            descricao: "Removido da fila: " + motivo,
            data_evento: new Date().toISOString(),
          });
        }

        return { success: true, data: resultado, error: null };
      } catch (err) {
        return { success: false, data: null, error: err.message };
      }
    }
    ```

    12. **Função 9: `listarFila(filtros = {})`** (D-pre-9 filter deleted_at + enrichment contrato):
    ```javascript
    async function listarFila(filtros = {}) {
      try {
        if (filtros.equipe_id !== undefined) validateUUID(filtros.equipe_id, "equipe_id");

        let query = "select=*&order=score_prioridade.desc";
        if (filtros.equipe_id) query += `&equipe_id=eq.${filtros.equipe_id}`;
        if (filtros.prioridade) query += `&prioridade=eq.${filtros.prioridade}`;
        if (filtros.status_fila) query += `&status_fila=eq.${filtros.status_fila}`;

        const items = await dbGet("fila_cobranca", query);
        if (!items.length) return { success: true, data: [], error: null };

        const contratoIds = [...new Set(items.map((i) => i.contrato_id).filter(Boolean))];
        const devedorIds = [...new Set(items.map((i) => i.devedor_id).filter(Boolean))];

        const [contratos, devedores] = await Promise.all([
          contratoIds.length > 0
            ? dbGet("contratos_dividas", `id=in.(${contratoIds.join(",")})`)
            : Promise.resolve([]),
          devedorIds.length > 0
            ? dbGet("devedores", `id=in.(${devedorIds.join(",")})&deleted_at=is.null`)
            : Promise.resolve([]),
        ]);

        const contratoMap = Object.fromEntries(contratos.map((c) => [c.id, c]));
        const devedorMap = Object.fromEntries(devedores.map((d) => [String(d.id), d]));

        // D-pre-9: drop items cujo devedor está soft-deleted (não em devedorMap)
        const enriched = items
          .filter((item) => item.devedor_id == null || devedorMap[String(item.devedor_id)])
          .map((item) => ({
            ...item,
            contrato: item.contrato_id ? contratoMap[item.contrato_id] || null : null,
            devedor: devedorMap[String(item.devedor_id)] || null,
          }));

        return { success: true, data: enriched, error: null };
      } catch (err) {
        return { success: false, data: null, error: err.message };
      }
    }
    ```

    13. **Função 10: `atualizarValoresAtrasados()`** (fonte = contratos_dividas + dividas, NÃO contratos+parcelas):
    ```javascript
    async function atualizarValoresAtrasados() {
      try {
        const ativosList = STATUS_CONTRATO_ATIVOS.join(",");
        const contratos = await dbGet(
          "contratos_dividas",
          `select=id,data_origem,created_at&status=in.(${ativosList})`
        );

        let atualizados = 0;
        const dataFim = new Date().toISOString().slice(0, 10);

        for (const contrato of contratos) {
          const dividas = await dbGet("dividas", `contrato_id=eq.${contrato.id}&select=valor_total`);
          const valorOriginal = (dividas || []).reduce(
            (s, d) => s + (parseFloat(d.valor_total) || 0),
            0
          );
          if (valorOriginal === 0) continue;

          const dataInicio = (contrato.data_origem || contrato.created_at || dataFim).slice(0, 10);
          const fator = calcularFatorCorrecao("igpm", dataInicio, dataFim);
          const valorAtualizado = (valorOriginal * fator).toFixed(2);

          await dbUpdate("contratos_dividas", contrato.id, {
            valor_atualizado: valorAtualizado,
            updated_at: new Date().toISOString(),
          });
          atualizados++;
        }

        return { success: true, data: { atualizados }, error: null };
      } catch (err) {
        return { success: false, data: null, error: err.message };
      }
    }
    ```

    14. **EXPORT (final do arquivo):**
    ```javascript
    // ─── EXPORT ───────────────────────────────────────────────────
    export const filaDevedor = {
      // Renomeados (Phase 7.13b D-pre-1):
      listarContratosParaFila,
      proximoContrato,
      alterarStatusContrato,
      // Assinatura adaptada (contrato_id ao invés de devedor_id):
      registrarEvento,
      // Mantidos (assinatura igual):
      calcularScorePrioridade,
      entrarNaFila,
      reciclarContratos,
      removerDaFila,
      listarFila,
      atualizarValoresAtrasados,

      // ─── Aliases legacy (DEPRECATED — back-compat com FilaDevedor.jsx pré-Plan 02) ───
      // Plan 02 reescreve UI consumindo nomes novos. Estes aliases serão removidos em Plan 02 commit.
      listarDevedoresParaFila: listarContratosParaFila,
      proximoDevedor: proximoContrato,
      alterarStatusDevedor: alterarStatusContrato,
    };
    ```

    Aliases legacy garantem que o build atual da UI (Plan 02 ainda não rodou) não quebre — o componente FilaDevedor.jsx atual chama `filaDevedor.listarDevedoresParaFila` etc. Plan 02 substitui consumers e remove aliases.

    **Validar pós-rewrite:**
    ```bash
    cd src/mr-3/mr-cobrancas

    # Stale refs zerados (B1.1 helper-call regex — lição feedback_grep_global_before_pointwise_fix.md + feedback_grep_shield_strict_imports.md)
    grep -nE "(dbGet|sb|dbInsert|dbUpdate|dbDelete)\\([\'\"]contratos[\'\"]," src/services/filaDevedor.js | wc -l   # Esperado: 0 (pattern correto — não casa em contratos_dividas/contratos_historico/prose PT-BR)
    grep -nE "\\bparcelas\\b" src/services/filaDevedor.js | wc -l   # Esperado: 0 (parcelas é tabela inexistente — sem ambiguidade word-boundary)

    # Build limpo
    npm run build   # exit 0

    # Regressão atual da 7.14 PRESERVADA
    npm run test:regressao   # exit 0
    ```

    **PAUSA HUMANA Plan 01 #1:** mostrar diff cumulativo do rewrite (`git -C src/mr-3 diff src/mr-cobrancas/src/services/filaDevedor.js | head -300`). Pedir APROVADO antes de prosseguir para Task 6 (rewrite testes).
  </action>
  <acceptance_criteria>
    - `grep -q "import { calcularSaldoContratoAtualizado, calcularDetalheEncargosContrato } from \"../utils/devedorCalc.js\"" src/mr-3/mr-cobrancas/src/services/filaDevedor.js` exit 0
    - `grep -q "import { STATUS_CONTRATO_ATIVOS, STATUS_CONTRATO_TERMINAIS } from \"../utils/constants.js\"" src/mr-3/mr-cobrancas/src/services/filaDevedor.js` exit 0
    - 10 funções presentes: `grep -cE "^async function (listarContratosParaFila|calcularScorePrioridade|entrarNaFila|proximoContrato|registrarEvento|alterarStatusContrato|reciclarContratos|removerDaFila|listarFila|atualizarValoresAtrasados)" src/mr-3/mr-cobrancas/src/services/filaDevedor.js` retorna 10
    - Aliases legacy presentes (back-compat Plan 02): `grep -cE "(listarDevedoresParaFila|proximoDevedor|alterarStatusDevedor): " src/mr-3/mr-cobrancas/src/services/filaDevedor.js` retorna 3
    - **Stale refs zerados (D-pre-8 fix-forward — B1.1 helper-call regex):** `grep -nE "(dbGet|sb|dbInsert|dbUpdate|dbDelete)\\([\'\"]contratos[\'\"]," src/mr-3/mr-cobrancas/src/services/filaDevedor.js | wc -l` retorna 0
    - **Stale refs `parcelas` zerados:** `grep -cE "\\bparcelas\\b" src/mr-3/mr-cobrancas/src/services/filaDevedor.js` retorna 0
    - Helpers preservados: `grep -cE "^function (validateUUID|validateBigInt|extractUsuario|parseDividasSvc)" src/mr-3/mr-cobrancas/src/services/filaDevedor.js` retorna 4
    - eventos_andamento payload com contrato_id: `grep -c "contrato_id:" src/mr-3/mr-cobrancas/src/services/filaDevedor.js` >= 5 (>=5 callsites: registrarEvento + alterarStatusContrato + removerDaFila + entrarNaFila inserts + reciclarContratos)
    - **B2.1 shape enriquecido (cross-impact Plan 02):** `grep -cE "_devedores|_credores|_pagamentos_divida" src/mr-3/mr-cobrancas/src/services/filaDevedor.js` >= 4 (campos plurais + _pagamentos_divida no return shape de listarContratosParaFila para Plan 02 alimentar DetalheContrato signature REAL)
    - **B2.1 fields populados:** `grep -E "_devedores: devedoresDoContrato" src/mr-3/mr-cobrancas/src/services/filaDevedor.js` exit 0; `grep -E "_pagamentos_divida: pagamentosDoContrato" src/mr-3/mr-cobrancas/src/services/filaDevedor.js` exit 0
    - Build: `cd src/mr-3/mr-cobrancas && npm run build` exit 0
    - Regressão atual (7.14): `cd src/mr-3/mr-cobrancas && npm run test:regressao` exit 0
    - PAUSA #1 aprovada explicitamente pelo usuário (response "APROVADO" ou equivalente)
  </acceptance_criteria>
</task>

<task type="auto" autonomous="false">
  <name>Task 6: Rewrite total filaDevedor.test.js (testes integração contra schema novo D-pre-10)</name>
  <read_first>
    - src/mr-3/mr-cobrancas/src/services/filaDevedor.test.js (FULL — referência setup/cleanup atual outdated)
    - src/mr-3/mr-cobrancas/src/services/filaDevedor.js (pós-Task 5 — assinaturas novas)
    - src/mr-3/mr-cobrancas/src/services/contratos.js (referência shape de contratos_dividas)
    - src/mr-3/mr-cobrancas/src/services/migrations/004_fila_por_contrato.sql (pós-migration)
  </read_first>
  <files>
    - src/mr-3/mr-cobrancas/src/services/filaDevedor.test.js
  </files>
  <action>
    **REWRITE TOTAL.** Substituir TODO o conteúdo de `src/mr-3/mr-cobrancas/src/services/filaDevedor.test.js` por:

    ```javascript
    /**
     * Phase 7.13b — Test integração filaDevedor.js (schema novo D-pre-10)
     *
     * Cobre as 10 funções do rewrite:
     *   1. listarContratosParaFila — fonte contratos_dividas
     *   2. calcularScorePrioridade — usa dividas (não parcelas)
     *   3. entrarNaFila — fix-forward D-pre-8 (contratos_dividas, não contratos)
     *   4. proximoContrato — lock otimista por contrato_id
     *   5. registrarEvento — INSERT eventos_andamento com contrato_id NOT NULL
     *   6. alterarStatusContrato — UPDATE contratos_dividas.status
     *   7. reciclarContratos — fix-forward D-pre-8
     *   8. removerDaFila — INSERT eventos com contrato_id
     *   9. listarFila — filter devedores.deleted_at=is.null (D-pre-9)
     *  10. atualizarValoresAtrasados — fonte contratos_dividas + dividas
     *
     * Setup cria fixtures válidos pós-D-pre-10 (devedor + credor + contrato_dividas + dividas + pagamento_divida).
     * Cleanup respeita FK ordering: eventos → pagamentos_divida → dividas → devedores_dividas → contratos_dividas → fila → devedor → credor.
     *
     * Run: npx vitest run src/services/filaDevedor.test.js
     * (Test integração contra Supabase real — exige .env com SUPABASE_URL+ANON_KEY)
     */

    import { dbGet, dbInsert, dbDelete, sb } from "../config/supabase.js";
    import { filaDevedor } from "./filaDevedor.js";

    const testIds = {
      devedor: null,
      credor: null,
      contrato: null,
      dividas: [],
      pagamentos: [],
      eventos: [],
      filaItems: [],
    };

    async function setup() {
      // 1. Buscar devedor existente (não criar — tabela legada BIGINT, FKs cascade)
      const devedores = await dbGet("devedores", "select=id&limit=1&deleted_at=is.null");
      if (!devedores.length) throw new Error("Nenhum devedor encontrado para teste");
      testIds.devedor = devedores[0].id;
      console.log(`[SETUP] Devedor existente: id=${testIds.devedor}`);

      // 2. Buscar credor existente
      const credores = await dbGet("credores", "select=id&limit=1");
      if (!credores.length) throw new Error("Nenhum credor encontrado para teste");
      testIds.credor = credores[0].id;
      console.log(`[SETUP] Credor existente: id=${testIds.credor}`);

      // 3. Criar contrato_dividas de teste com status=em_cobranca (D-pre-4)
      // B1.3: schema real (contratos.js L20-33) — usa `referencia` + `valor_total` derivado, NÃO `numero_contrato`/`valor_original`
      const contrato = await dbInsert("contratos_dividas", {
        devedor_id: testIds.devedor,
        credor_id: testIds.credor,
        referencia: "TEST-FILA-7.13B-" + Date.now(),
        status: "em_cobranca",
      });
      testIds.contrato = Array.isArray(contrato) ? contrato[0]?.id : contrato.id;
      console.log(`[SETUP] Contrato criado: ${testIds.contrato}`);

      // 4. Criar 2 dívidas (não parcelas — schema novo Phase 5+)
      // B1.2: schema dividas (002_dividas_tabela.sql L16-44) NÃO TEM coluna `numero_parcela` — identificação via observacoes (TEXT free-form)
      for (let i = 1; i <= 2; i++) {
        const d = await dbInsert("dividas", {
          contrato_id: testIds.contrato,
          devedor_id: testIds.devedor,
          valor_total: 2500.00,
          data_vencimento: i === 1 ? "2025-01-15" : "2026-06-15",
          observacoes: "TEST-FILA-7.13B parcela " + i,
        });
        testIds.dividas.push(Array.isArray(d) ? d[0]?.id : d.id);
      }
      console.log(`[SETUP] 2 dívidas criadas`);
    }

    async function cleanup() {
      console.log("\n[CLEANUP] Removendo dados de teste...");
      // Ordem FK: eventos → pagamentos_divida → dividas → devedores_dividas → fila → contrato → (devedor/credor preservados)

      // 1. eventos_andamento criados durante teste (filter por contrato_id do test)
      if (testIds.contrato) {
        const eventos = await dbGet(
          "eventos_andamento",
          `select=id&contrato_id=eq.${testIds.contrato}`
        ).catch(() => []);
        for (const ev of eventos) {
          await dbDelete("eventos_andamento", ev.id).catch(() => {});
        }
      }

      // 2. pagamentos_divida (filter divida_id IN dividas do contrato)
      if (testIds.dividas.length) {
        const pagamentos = await dbGet(
          "pagamentos_divida",
          `select=id&divida_id=in.(${testIds.dividas.join(",")})`
        ).catch(() => []);
        for (const p of pagamentos) {
          await dbDelete("pagamentos_divida", p.id).catch(() => {});
        }
      }

      // 3. devedores_dividas (junction — FK CASCADE on dividas, mas explicitamos)
      if (testIds.dividas.length) {
        const dd = await dbGet(
          "devedores_dividas",
          `select=id&divida_id=in.(${testIds.dividas.join(",")})`
        ).catch(() => []);
        for (const x of dd) {
          await dbDelete("devedores_dividas", x.id).catch(() => {});
        }
      }

      // 4. fila_cobranca
      if (testIds.contrato) {
        const filaItems = await dbGet(
          "fila_cobranca",
          `select=id&contrato_id=eq.${testIds.contrato}`
        ).catch(() => []);
        for (const f of filaItems) {
          await dbDelete("fila_cobranca", f.id).catch(() => {});
        }
      }

      // 5. dividas
      for (const id of testIds.dividas) {
        if (id) await dbDelete("dividas", id).catch(() => {});
      }

      // 6. contrato
      if (testIds.contrato) {
        await dbDelete("contratos_dividas", testIds.contrato).catch(() => {});
      }

      console.log("[CLEANUP] Concluído");
    }

    let passed = 0, failed = 0;
    function assert(cond, msg) {
      if (cond) { passed++; console.log(`  ✓ ${msg}`); }
      else { failed++; console.error(`  ✗ ${msg}`); }
    }

    async function testListarContratosParaFila() {
      console.log("\n[TEST] listarContratosParaFila (D-pre-1, D-pre-9)");
      const r = await filaDevedor.listarContratosParaFila({});
      assert(r.success === true, "success=true");
      assert(Array.isArray(r.data), "data is array");
      assert(r.data.some(c => c.id === testIds.contrato), "test contrato presente em data");
      const c = r.data.find(c => c.id === testIds.contrato);
      assert(c?._devedor?.id == testIds.devedor, "_devedor enriched");
      assert(c?._credor?.id == testIds.credor, "_credor enriched");
      assert(Array.isArray(c?._dividas) && c._dividas.length === 2, "_dividas array com 2 entries");
      assert(typeof c?._saldo_atualizado === "number", "_saldo_atualizado é number");
      assert(["ALTA","MEDIA","BAIXA"].includes(c?._prioridade), "_prioridade ∈ {ALTA,MEDIA,BAIXA}");
    }

    async function testEntrarNaFila() {
      console.log("\n[TEST] entrarNaFila (fix-forward D-pre-8)");
      const r = await filaDevedor.entrarNaFila();
      assert(r.success === true, "success=true");
      assert(typeof r.data?.inseridos === "number", "data.inseridos é number");

      const fila = await dbGet("fila_cobranca", `select=*&contrato_id=eq.${testIds.contrato}`);
      assert(fila.length >= 1, "test contrato inserido em fila_cobranca");
    }

    async function testRegistrarEvento() {
      console.log("\n[TEST] registrarEvento (D-pre-10 contrato_id NOT NULL)");
      const r = await filaDevedor.registrarEvento(testIds.contrato, 1, {
        tipo_evento: "LIGACAO",
        descricao: "Test 7.13b — evento por contrato",
      });
      assert(r.success === true, "success=true");
      assert(r.data?.id != null, "evento criado retorna id");
      assert(r.data?.contrato_id === testIds.contrato, "evento.contrato_id === testIds.contrato");
      assert(r.data?.devedor_id != null, "evento.devedor_id back-compat preserved (lookup via contratos_dividas)");
    }

    async function testAlterarStatusContrato() {
      console.log("\n[TEST] alterarStatusContrato (D-pre-4 — UPDATE contratos_dividas.status)");
      const r = await filaDevedor.alterarStatusContrato(testIds.contrato, "em_localizacao", 1);
      assert(r.success === true, "success=true");
      assert(r.data?.novoStatus === "em_localizacao", "novoStatus retornado");

      const cArr = await dbGet("contratos_dividas", `select=status&id=eq.${testIds.contrato}`);
      assert(cArr[0]?.status === "em_localizacao", "contratos_dividas.status persistido");
    }

    async function testListarFilaDeletedAt() {
      console.log("\n[TEST] listarFila (D-pre-9 filter deleted_at=is.null)");
      const r = await filaDevedor.listarFila({});
      assert(r.success === true, "success=true");
      assert(Array.isArray(r.data), "data array");
      // Drift impossível de mensurar sem soft-delete real — apenas confirma shape (UAT humano em Plan 03)
      const item = r.data.find(i => i.contrato_id === testIds.contrato);
      if (item) {
        assert(item.devedor != null, "devedor enriched (não soft-deleted)");
      }
    }

    async function testAtualizarValoresAtrasados() {
      console.log("\n[TEST] atualizarValoresAtrasados (fonte contratos_dividas + dividas)");
      const r = await filaDevedor.atualizarValoresAtrasados();
      assert(r.success === true, "success=true");
      assert(typeof r.data?.atualizados === "number", "data.atualizados é number");
    }

    async function main() {
      try {
        await setup();
        await testListarContratosParaFila();
        await testEntrarNaFila();
        await testRegistrarEvento();
        await testAlterarStatusContrato();
        await testListarFilaDeletedAt();
        await testAtualizarValoresAtrasados();
      } catch (err) {
        console.error("FATAL:", err);
        failed++;
      } finally {
        await cleanup();
        console.log(`\n[RESULTADO] passed=${passed} failed=${failed}`);
        if (failed > 0) process.exit(1);
      }
    }

    main();
    ```

    **NÃO ADICIONAR** este arquivo ao `npm run test:regressao` (suite vitest). Ele é teste integração rodado manualmente em Plan 03 (UAT smoke). Razão: depende de credenciais Supabase + dados reais; não roda em CI.
  </action>
  <acceptance_criteria>
    - `grep -q "contratos_dividas" src/mr-3/mr-cobrancas/src/services/filaDevedor.test.js` exit 0
    - `grep -q "filaDevedor.listarContratosParaFila" src/mr-3/mr-cobrancas/src/services/filaDevedor.test.js` exit 0
    - `grep -q "filaDevedor.alterarStatusContrato" src/mr-3/mr-cobrancas/src/services/filaDevedor.test.js` exit 0
    - **Stale refs `parcelas` zerados:** `grep -cE "\\bparcelas\\b" src/mr-3/mr-cobrancas/src/services/filaDevedor.test.js` retorna 0
    - **Stale refs `contratos` zerados (B1.1 helper-call regex):** `grep -nE "(dbGet|sb|dbInsert|dbUpdate|dbDelete)\\([\'\"]contratos[\'\"]," src/mr-3/mr-cobrancas/src/services/filaDevedor.test.js | wc -l` retorna 0
    - Cleanup ordering correto: ordem `eventos_andamento` → `pagamentos_divida` → `devedores_dividas` → `fila_cobranca` → `dividas` → `contratos_dividas` documentada em comentário
    - Setup cria fixtures: `grep -cE "(dbInsert|await sb).*\"(contratos_dividas|dividas)\"" src/mr-3/mr-cobrancas/src/services/filaDevedor.test.js` >= 2
    - **B1.2 schema-correctness — sem coluna fantasma `numero_parcela` em dividas:** `grep -cE "numero_parcela" src/mr-3/mr-cobrancas/src/services/filaDevedor.test.js` retorna 0
    - **B1.3 schema-correctness — sem colunas fantasmas em contratos_dividas:** `grep -cE "numero_contrato|valor_original" src/mr-3/mr-cobrancas/src/services/filaDevedor.test.js` retorna 0 (schema real usa `referencia` + `valor_total` derivado)
    - **B1.3 referencia presente no setup:** `grep -E "referencia: \"TEST-FILA-7.13B-" src/mr-3/mr-cobrancas/src/services/filaDevedor.test.js` exit 0
    - Build não regride: `cd src/mr-3/mr-cobrancas && npm run build` exit 0
  </acceptance_criteria>
</task>

<task type="auto" autonomous="false">
  <name>Task 6.5 (OPCIONAL — W1.4): Smoke run local filaDevedor.test.js antes do gate Task 7</name>
  <read_first>
    - src/mr-3/mr-cobrancas/src/services/filaDevedor.test.js (pós-Task 6 — rewrite testes integração)
    - src/mr-3/mr-cobrancas/.env (credenciais Supabase para teste integração)
  </read_first>
  <action>
    **W1.4 — smoke run local pra descobrir bugs schema (numero_parcela ausente, valor_original ausente, lookup pattern errado, FK violations) em Plan 01, NÃO Plan 03 UAT humano.**

    Executor roda manualmente o teste integração contra Supabase real:

    ```bash
    cd src/mr-3/mr-cobrancas

    # Pre-flight: confirma .env tem credenciais
    grep -E "(SUPABASE_URL|SUPABASE_KEY|VITE_SUPABASE)" .env > /dev/null || \
      { echo "⚠ .env não tem credenciais Supabase — pular Task 6.5 OPCIONAL e seguir pra Task 7"; exit 0; }

    # Roda teste integração — output deve mostrar passed=N failed=0
    node src/services/filaDevedor.test.js
    ```

    **Esperado:** stdout contém "passed=" e "failed=0". Caso `failed > 0`:
    - Identifica bug pontual (FK violation, schema mismatch, lookup errado)
    - Volta para Task 5 ou Task 6 corrigir
    - Re-roda Task 6.5 até passed=N failed=0

    **Caso .env sem credenciais Supabase:** TASK SKIPPED (registrar em SUMMARY: "Task 6.5 SKIP — sem credenciais .env locais; UAT humano em Plan 03 cobre"). Não bloqueia Task 7.

    **Caso teste passa em smoke local:** registra em variável de contexto pro Task 9 SUMMARY: "Smoke run W1.4 PASS — N testes passed, 0 failed".
  </action>
  <acceptance_criteria>
    - **Caso .env presente:** `node src/mr-3/mr-cobrancas/src/services/filaDevedor.test.js` exit 0 + stdout contém "passed=N failed=0" (N >= 6 testes — listar/entrar/registrar/alterar/listarFila/atualizarValores)
    - **Caso .env ausente:** task SKIP documentado em SUMMARY (não bloqueia Task 7)
    - Bug schema pontual descoberto NÃO bloqueia Plan 01 — corrige em Task 5/6 e re-roda
  </acceptance_criteria>
</task>

<task type="auto" autonomous="false">
  <name>Task 7: Gates locais finais — build + regressão + grep stale refs (lição feedback_grep_global_before_pointwise_fix.md)</name>
  <read_first>
    - src/mr-3/mr-cobrancas/src/services/filaDevedor.js (pós-Task 5 — rewrite)
    - src/mr-3/mr-cobrancas/src/services/filaDevedor.test.js (pós-Task 6 — rewrite)
    - src/mr-3/mr-cobrancas/src/utils/devedorCalc.js (pós-Task 4 — wrapper)
    - src/mr-3/mr-cobrancas/src/utils/constants.js (pós-Task 3 — STATUS_CONTRATO)
    - src/mr-3/mr-cobrancas/package.json (script test:regressao atual)
  </read_first>
  <action>
    **Gate A — Build limpo:**
    ```bash
    cd src/mr-3/mr-cobrancas
    npm run build
    # Esperado exit 0; rollup/vite output limpo (FilaDevedor.jsx ainda usa filaDevedor — aliases legacy mantêm build verde até Plan 02)
    ```

    **Gate B — Regressão atual da 7.14 PRESERVADA:**
    ```bash
    cd src/mr-3/mr-cobrancas
    npm run test:regressao
    # Esperado exit 0; output mostra "58 passed" (38 baseline + 20 parser tests da 7.14)
    ```

    **Gate C — Stale refs globais zerados (lição feedback_grep_global_before_pointwise_fix.md):**

    Varredura GLOBAL (não só filaDevedor.js — busca em arquivos de service que possam referenciar tabelas inexistentes pós-rewrite):

    ```bash
    cd src/mr-3/mr-cobrancas

    # `dbGet("contratos"`/`dbGet('contratos'` — stale (tabela inexistente)
    grep -rnE "dbGet\\(['\"]contratos['\"]," src/services/ src/utils/
    # Esperado: 0 matches (sb também)

    grep -rnE "sb\\(['\"]contratos['\"]" src/services/ src/utils/
    # Esperado: 0 matches

    # `dbGet("parcelas"` — stale (tabela inexistente)
    grep -rnE "dbGet\\(['\"]parcelas['\"]" src/services/ src/utils/
    # Esperado: 0 matches

    # filaDevedor.js especificamente — B1.1 helper-call regex (consistente com Gate C1/C2 acima)
    grep -nE "(dbGet|sb|dbInsert|dbUpdate|dbDelete)\\([\'\"]contratos[\'\"]," src/services/filaDevedor.js | wc -l
    # Esperado: 0 (pattern correto — não casa em contratos_dividas/contratos_historico/prose)

    grep -nE "\\bparcelas\\b" src/services/filaDevedor.js | wc -l
    # Esperado: 0 (parcelas é tabela inexistente — word-boundary sem ambiguidade)

    grep -nE "\\bparcelas\\b" src/services/filaDevedor.test.js | wc -l
    # Esperado: 0

    # B1.1 helper-call regex no test também
    grep -nE "(dbGet|sb|dbInsert|dbUpdate|dbDelete)\\([\'\"]contratos[\'\"]," src/services/filaDevedor.test.js | wc -l
    # Esperado: 0
    ```

    **Gate D — D-01 motor INTOCADO (gate dual cumulativo desde 3508bb4 = post-7.10.bug HEAD):**

    ```bash
    cd src/mr-3

    # Gate dual C1 — diff cumulativo motor pagamentos.js + dividas.js (devedorCalc.js permite +adição wrapper)
    git diff 3508bb4..HEAD -- src/mr-cobrancas/src/services/pagamentos.js
    # Esperado: VAZIO

    git diff 3508bb4..HEAD -- src/mr-cobrancas/src/services/dividas.js
    # Esperado: VAZIO

    # devedorCalc.js: APENAS adições (wrapper Task 4); ZERO remoções/modificações
    git diff 3508bb4..HEAD -- src/mr-cobrancas/src/utils/devedorCalc.js | grep -E "^-(\\s+)" | wc -l
    # Esperado: 0 (zero linhas removidas — apenas adições)

    # Gate dual C2 — name-only confirma pagamentos.js + dividas.js AUSENTES no log
    git log 3508bb4..HEAD --format="" --name-only -- 'src/mr-cobrancas/src/services/pagamentos.js' 'src/mr-cobrancas/src/services/dividas.js' | wc -l
    # Esperado: 0
    ```

    **Gate E — Aliases legacy preservados (back-compat com FilaDevedor.jsx atual antes do Plan 02):**

    ```bash
    grep -E "(listarDevedoresParaFila|proximoDevedor|alterarStatusDevedor):" src/mr-cobrancas/src/services/filaDevedor.js | wc -l
    # Esperado: 3 (aliases retêm build verde até Plan 02 atualizar consumers)
    ```

    Se TODOS os gates passarem, anotar em variável de contexto pro commit message (Task 9): "Gates locais Plan 01: A=PASS, B=PASS (58 testes), C=PASS (0 stale refs), D=PASS (motor cumulative INTOCADO), E=PASS (3 aliases)".
  </action>
  <acceptance_criteria>
    - Gate A: `cd src/mr-3/mr-cobrancas && npm run build` exit 0
    - Gate B: `cd src/mr-3/mr-cobrancas && npm run test:regressao` exit 0 + output contém "passed" (suite atual da 7.14)
    - Gate C1: `grep -rnE "dbGet\\(['\\\"]contratos['\\\"]," src/mr-3/mr-cobrancas/src/services/ src/mr-3/mr-cobrancas/src/utils/ | wc -l` retorna 0
    - Gate C2: `grep -rnE "dbGet\\(['\\\"]parcelas['\\\"]" src/mr-3/mr-cobrancas/src/services/ src/mr-3/mr-cobrancas/src/utils/ | wc -l` retorna 0
    - Gate C3 (B1.1 helper-call regex — consistente com C1/C2): `grep -nE "(dbGet|sb|dbInsert|dbUpdate|dbDelete)\\([\'\"]contratos[\'\"]," src/mr-3/mr-cobrancas/src/services/filaDevedor.js | wc -l` retorna 0
    - Gate C4: `grep -cE "\\bparcelas\\b" src/mr-3/mr-cobrancas/src/services/filaDevedor.js` retorna 0
    - Gate C5: `grep -cE "\\bparcelas\\b" src/mr-3/mr-cobrancas/src/services/filaDevedor.test.js` retorna 0
    - Gate D1: `git -C src/mr-3 diff 3508bb4..HEAD -- src/mr-cobrancas/src/services/pagamentos.js | wc -l` retorna 0
    - Gate D2: `git -C src/mr-3 diff 3508bb4..HEAD -- src/mr-cobrancas/src/services/dividas.js | wc -l` retorna 0
    - Gate D3: `git -C src/mr-3 diff 3508bb4..HEAD -- src/mr-cobrancas/src/utils/devedorCalc.js | grep -E "^-(\\s+)" | wc -l` retorna 0 (zero linhas removidas)
    - Gate D4: `git -C src/mr-3 log 3508bb4..HEAD --format="" --name-only -- 'src/mr-cobrancas/src/services/pagamentos.js' 'src/mr-cobrancas/src/services/dividas.js' | wc -l` retorna 0
    - Gate E: `grep -cE "(listarDevedoresParaFila|proximoDevedor|alterarStatusDevedor):" src/mr-3/mr-cobrancas/src/services/filaDevedor.js` retorna 3
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 8: PAUSA #2 — diff consolidado pré-commit (autonomous:false LOCKED)</name>
  <what-built>
    Plan 01 completo localmente: 4 arquivos modificados (filaDevedor.js rewrite, filaDevedor.test.js rewrite, devedorCalc.js wrapper opcional, constants.js STATUS_CONTRATO + arrays auxiliares) + 1 arquivo novo (migrations/004_fila_por_contrato.sql). Migration aplicada manualmente em Supabase no Task 1 (STRATEGY A ou B aplicada). Gates A-E todos verdes em Task 7.
  </what-built>
  <how-to-verify>
    1. **Diff consolidado:**
       ```bash
       git -C src/mr-3 diff --stat
       ```
       Esperado: 4 modified + 1 new (migration), 0 deletes, total ~600-900 linhas alteradas (rewrite filaDevedor.js + test).

    2. **D-01 motor cumulative gate (lição feedback_gate_motor_grep_use_name_only.md):**
       ```bash
       git -C src/mr-3 log 3508bb4..HEAD --format="" --name-only -- 'src/mr-cobrancas/src/services/pagamentos.js' 'src/mr-cobrancas/src/services/dividas.js'
       # Esperado: VAZIO
       git -C src/mr-3 diff 3508bb4..HEAD -- src/mr-cobrancas/src/utils/devedorCalc.js | grep -E "^-(\\s+)" | wc -l
       # Esperado: 0 (zero deleções — apenas adição do wrapper)
       ```

    3. **Stale refs check final (B1.1 helper-call regex):**
       ```bash
       cd src/mr-3/mr-cobrancas && grep -nE "(dbGet|sb|dbInsert|dbUpdate|dbDelete)\\([\'\"]contratos[\'\"]," src/services/filaDevedor.js
       # Esperado: vazio (0 callsites helper-call para tabela `contratos` inexistente)
       ```

    4. **Build + regressão final:**
       ```bash
       cd src/mr-3/mr-cobrancas && npm run build && npm run test:regressao
       # Esperado: ambos exit 0
       ```

    5. **Confirmar STRATEGY aplicada na migration** (Task 1 PAUSA #0): "STRATEGY=A" (TRUNCATE) ou "STRATEGY=B" (DELETE).

    6. **Confirmar audit Schema vivo** (Task 2): contratos_dividas.status existe, eventos_andamento.contrato_id NOT NULL existe, INSERT teste FUNCIONA, eventos_andamento truncado.
  </how-to-verify>
  <resume-signal>
    Responder "APROVADO" para autorizar Task 9 (commit atomic). Caso "FAIL [motivo]" → debug pontual; caso "ROLLBACK" → executor desfaz mudanças locais (`git -C src/mr-3 checkout -- src/mr-cobrancas/`) e Plan 01 aborta.
  </resume-signal>
</task>

<task type="auto" autonomous="false">
  <name>Task 9: Commit atomic feat(07.13b-01) no submódulo</name>
  <read_first>
    - src/mr-3/mr-cobrancas/src/services/filaDevedor.js (rewrite)
    - src/mr-3/mr-cobrancas/src/services/filaDevedor.test.js (rewrite)
    - src/mr-3/mr-cobrancas/src/utils/devedorCalc.js (wrapper)
    - src/mr-3/mr-cobrancas/src/utils/constants.js (STATUS_CONTRATO)
    - src/mr-3/mr-cobrancas/src/services/migrations/004_fila_por_contrato.sql
  </read_first>
  <action>
    **W1.2 — Caso STRATEGY B aplicada (PAUSA #0 retornou STRATEGY=B), executor edita migration file ANTES do stage:**

    ```bash
    if [ "$STRATEGY_APLICADA" = "B" ]; then
      # Substitui Bloco 3 TRUNCATE por DELETE no arquivo committed (mantendo comentário audit-trail)
      sed -i 's|^TRUNCATE public\.eventos_andamento;|-- STRATEGY B applied — TRUNCATE blocked by RLS\nDELETE FROM public.eventos_andamento;|' \
        src/mr-3/mr-cobrancas/src/services/migrations/004_fila_por_contrato.sql
      echo "✓ Migration file atualizada para STRATEGY B (DELETE)"
    else
      echo "✓ STRATEGY A — migration file inalterada (TRUNCATE)"
    fi
    ```

    **Stage + commit** no submódulo (NÃO no pai — bump pai vai no Plan 03):

    ```bash
    cd src/mr-3
    git add src/mr-cobrancas/src/services/filaDevedor.js
    git add src/mr-cobrancas/src/services/filaDevedor.test.js
    git add src/mr-cobrancas/src/utils/devedorCalc.js
    git add src/mr-cobrancas/src/utils/constants.js
    git add src/mr-cobrancas/src/services/migrations/004_fila_por_contrato.sql

    git status --porcelain
    # Esperado: 5 arquivos staged (4 M + 1 A)

    # W1.3 — STRATEGY_APLICADA setada no Task 1 PAUSA #0 ('A' ou 'B' baseado em resposta usuário)
    # Caso variável não esteja exportada (ex: nova shell session), executor pergunta usuário antes do commit:
    : "${STRATEGY_APLICADA:?ERRO: variável STRATEGY_APLICADA não setada — confirmar com usuário antes do commit (export STRATEGY_APLICADA=A ou =B)}"

    git commit -m "feat(07.13b-01): refactor camada de dados Fila por contrato + bug fix D-pre-8

    Phase 7.13b — Fila por Contrato. Refactor estrutural alinhando fonte de
    verdade com modelo 3 níveis (devedor → contrato → dívida) das Phases 5+6+7.x.

    Schema delta (D-pre-10 — aplicado manualmente em Supabase SQL Editor,
    STRATEGY=${STRATEGY_APLICADA}):
    - contratos_dividas.status TEXT NOT NULL DEFAULT 'em_cobranca' CHECK em 7
      valores (em_cobranca, em_localizacao, em_negociacao, notificado,
      ajuizado, quitado, arquivado)
    - eventos_andamento.contrato_id UUID NOT NULL REFERENCES contratos_dividas(id)
    - TRUNCATE/DELETE eventos_andamento (γ — banco minúsculo, decisão de produto)
    - pg_notify('pgrst','reload schema') aplicado

    Rewrite filaDevedor.js (10 funções, fonte CONTRATOS):
    - listarContratosParaFila (rename de listarDevedoresParaFila)
    - proximoContrato (rename de proximoDevedor)
    - alterarStatusContrato (rename de alterarStatusDevedor)
    - registrarEvento (assinatura: contratoId não devedorId; payload mantém
      ambos contrato_id + devedor_id back-compat com Detalhe-Devedor)
    - calcularScorePrioridade (usa dividas.valor_total, não parcelas inexistente)
    - 6+ CALLSITES STALE consertados (B1.4 escopo revisado — não 2 como
      briefing): L217 calcularScorePrioridade, L253 entrarNaFila, L491+L496
      reciclarContratos, L584-585 listarFila, L607-608+L623
      atualizarValoresAtrasados — fix-forward D-pre-8 — tabela
      'contratos' (inexistente) substituída por 'contratos_dividas'
    - removerDaFila / listarFila / atualizarValoresAtrasados adaptadas
    - Aliases legacy (listarDevedoresParaFila/proximoDevedor/alterarStatusDevedor)
      mantidos para back-compat com FilaDevedor.jsx até Plan 02 reescrever UI

    Score por contrato (D-pre-8 Q4):
    - bonusStatus[em_cobranca]=100, [em_localizacao]=80, [notificado]=60,
      [em_negociacao]=40; terminais=0
    - score = bonus + saldoAtualizado/100 + diasCriacao*0.5
    - prioridade ALTA(>=120) / MEDIA(>=80) / BAIXA

    Helper saldo por contrato (D-pre-12):
    - REUSA calcularDetalheEncargosContrato (Phase 7.8 D-02 adapter)
    - Wrapper thin calcularSaldoContratoAtualizado adicionado em devedorCalc.js
      — APENAS COMPÕE motor existente (D-01 INTOCADO)

    Filter devedores.deleted_at=is.null em listarFila (D-pre-9 + 7.14/D-pre-9
    strategy b cumulativa).

    Constants:
    - STATUS_CONTRATO export (utils/constants.js) — 7 valores PT-BR labels
    - STATUS_CONTRATO_TERMINAIS = quitado/arquivado/ajuizado
    - STATUS_CONTRATO_ATIVOS = em_cobranca/em_localizacao/em_negociacao/notificado

    filaDevedor.test.js rewrite total — testes integração contra schema novo
    (contratos_dividas + eventos_andamento.contrato_id). Setup/cleanup respeita
    FK ordering. NÃO referencia tabela 'contratos' ou 'parcelas'.

    Bug fix incluso D-pre-8: 'contratos' inexistente em 6+ callsites (não 2
    como briefing original) — L217 + L253 + L491 + L496 + L584-585 + L607-608
    + L623 (funções dormentes em prod) → fix-forward natural pelo rewrite.
    Lição transferível: 'bugs pré-existentes em features dormentes só
    descobertos em audit' (7.10a + 7.13b) + 'ranges de linha em CONTEXT
    são estimativas; pre-execute audit deve enumerar TODAS ocorrências
    antes de prometer escopo no commit message' (B1.4).

    Gates locais Plan 01:
    - A build limpo PASS
    - B regressão atual 7.14 PASS (motor INTOCADO cumulative)
    - C grep stale refs PASS (0 ocorrências 'contratos'/'parcelas' substring
      isolada em filaDevedor.js + filaDevedor.test.js)
    - D D-01 motor dual gate PASS (services/pagamentos.js + services/dividas.js
      AUSENTES desde 3508bb4 = post-7.10.bug; devedorCalc.js APENAS adição
      wrapper, zero deleções)
    - E aliases legacy preservados (back-compat até Plan 02)

    Lições aplicadas pre-execute: feedback_db_integration_gate_missing,
    feedback_grep_global_before_pointwise_fix, feedback_proc_judiciais_create
    _bug_preexisting (D-pre-8 fix-forward), feedback_helper_fanout_must_cover
    _motor_inputs (audit calcularDetalheEncargosContrato shape), feedback_gate
    _motor_grep_use_name_only.

    Plan 02 (próximo): rewrite UI FilaDevedor.jsx 4 telas + remover aliases legacy."

    git log -1 --format=%H
    # Anotar SHA novo do submódulo (será SHA bumpado no pai em Plan 03)
    ```

    Substituir `<A_OU_B>` na mensagem do commit pela STRATEGY confirmada em Task 1 (A=TRUNCATE, B=DELETE).

    **NÃO bumpar pai neste plan.** Bump pai + push + tag vão no Plan 03 após UAT humano.

    **NÃO push.** Push humano em Plan 03.
  </action>
  <acceptance_criteria>
    - `git -C src/mr-3 log -1 --format=%s` contém "feat(07.13b-01): refactor camada de dados Fila por contrato"
    - **W1.3 STRATEGY shell variable expansion:** `git -C src/mr-3 log -1 --format=%B | grep -qE "STRATEGY=[AB]\b"` exit 0 (variável $STRATEGY_APLICADA expandida pra A ou B literal — não placeholder <A_OU_B>)
    - **W1.3 sem placeholder no commit:** `git -C src/mr-3 log -1 --format=%B | grep -q "<A_OU_B>"` retorna FALSE (placeholder não vazou pro commit)
    - **B1.4 6 callsites mencionados:** `git -C src/mr-3 log -1 --format=%B | grep -qE "(6\+ CALLSITES|6 callsites)"` exit 0
    - **B1.4 lição enumerar ocorrências:** `git -C src/mr-3 log -1 --format=%B | grep -q "enumerar TODAS ocorr"` exit 0
    - `git -C src/mr-3 log -1 --format=%B | grep -q "D-pre-8"` exit 0
    - `git -C src/mr-3 log -1 --format=%B | grep -q "D-01 motor"` exit 0
    - `git -C src/mr-3 log -1 --format=%B | grep -q "D-pre-12"` exit 0
    - `git -C src/mr-3 log -1 --name-only | grep -E "(filaDevedor\\.js|filaDevedor\\.test\\.js|devedorCalc\\.js|constants\\.js|004_fila_por_contrato\\.sql)" | wc -l` retorna 5
    - `git -C src/mr-3 status --porcelain | wc -l` retorna 0 (working tree clean pós-commit)
    - SHA novo do submódulo registrado em variável de contexto pra Plan 03 (bump pai)
    - Build/regressão pós-commit verdes (smoke): `cd src/mr-3/mr-cobrancas && npm run build && npm run test:regressao` ambos exit 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
Verificações finais Plan 01:

1. **Migration aplicada em prod (Task 1 PAUSA #0):** STRATEGY=A ou B confirmada pelo usuário.
2. **Schema audit (Task 2):** contratos_dividas.status existe, eventos_andamento.contrato_id NOT NULL, INSERT teste FUNCIONA, eventos truncado.
3. **Rewrite filaDevedor.js (Task 5):** 10 funções fonte CONTRATOS, stale refs zerados, aliases legacy preservados.
4. **Rewrite filaDevedor.test.js (Task 6):** schema novo, FK ordering correto, sem refs inexistentes.
5. **STATUS_CONTRATO + wrapper (Tasks 3-4):** export OK, D-01 motor INTOCADO.
6. **Gates locais (Task 7):** A build, B regressão, C stale refs, D motor dual, E aliases — todos verdes.
7. **PAUSA #2 (Task 8):** usuário aprovou diff consolidado.
8. **Commit atomic (Task 9):** feat(07.13b-01) criado, mensagem documenta D-pre-8 + STRATEGY aplicada + lições.
</verification>

<success_criteria>
- 1 commit atômico no submódulo: `feat(07.13b-01): refactor camada de dados Fila por contrato + bug fix D-pre-8`
- 5 arquivos no commit (4 MOD + 1 NEW migration .sql)
- Migration aplicada manualmente no Supabase (STRATEGY=A ou B documentada)
- Build + regressão atual da 7.14 (58 testes) verdes pós-commit
- D-01 motor cumulative INTOCADO desde 3508bb4 (gate dual C1 diff + C2 name-only)
- Stale refs `contratos`/`parcelas` zerados em filaDevedor.js + filaDevedor.test.js (D-pre-8 fix-forward)
- 2 PAUSAs humanas aprovadas (PAUSA #0 migration + PAUSA #2 diff consolidado)
- autonomous:false LOCKED em todas as 9 tasks
</success_criteria>

<output>
Após Plan 01 completo, criar `.planning/phases/07.13b-fila-por-contrato/07.13b-01-SUMMARY.md` registrando:
- STRATEGY aplicada (A TRUNCATE ou B DELETE)
- 4 audits SQL Task 2 (resultados colados pelo usuário)
- Diff stat consolidado
- Gate matrix A-E pass/fail
- SHA do commit feat(07.13b-01)
- 0 deviations (target) ou listadas se houver

SUMMARY commitado em Plan 03 junto com docs consolidados.
</output>
</content>
</invoke>