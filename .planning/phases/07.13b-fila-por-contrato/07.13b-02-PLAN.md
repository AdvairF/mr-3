---
plan: 07.13b-02
phase: 7.13b
phase_name: "Fila por Contrato (refactor estrutural)"
type: execute
wave: 2
depends_on: ["07.13b-01"]
autonomous: false
requirements: []
source: "ROADMAP.md L414-421 Phase 7.13b entry + 07.13b-CONTEXT.md (D-pre-2, D-pre-3, D-pre-4, D-pre-5, D-pre-6, Q5, Q6, Q7). Phase has NO REQ-IDs."
estimated_tasks: 8

files_modified:
  - src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx                                       # MOD (rewrite total — 4 telas adaptadas para shape contrato; 1278 linhas)
  - src/mr-3/mr-cobrancas/src/services/filaDevedor.js                                          # MOD (remover 3 aliases legacy listarDevedoresParaFila/proximoDevedor/alterarStatusDevedor)
  - src/mr-3/mr-cobrancas/src/App.jsx                                                          # MOD (verificar wire-up <FilaDevedor> case "fila" L8529 — props devedores+credores ainda fazem sentido para FilaPesquisa)

must_haves:
  truths:
    - "FilaDevedor.jsx rewrite total — 4 telas adaptadas para shape CONTRATO (D-pre-1): FilaPainel (supervisor — listagem com cards + filtros + tabela contratos), FilaOperador (lobby — botão 'Próximo'), FilaAtendimento (atendimento ativo de 1 contrato — registrar evento + alterar status), FilaPesquisa (busca contratos por devedor.nome/cpf retornando linhas múltiplas Q7)"
    - "1 contrato = 1 linha (D-pre-2): devedor com 2 contratos aparece em 2 linhas distintas. Repetição telefone/CPF aceita (D-pre-5 B1 — sem complexidade extra). Linha mostra: nome devedor + credor + saldo atualizado (D-pre-3) + status + prioridade + último evento + ações"
    - "Status select com novos valores (D-pre-4) — 7 valores em_cobranca/em_localizacao/em_negociacao/notificado/ajuizado/quitado/arquivado consumindo STATUS_CONTRATO de utils/constants.js (criado em Plan 01 Task 3). Substitui STATUS_DEV em FilaPainel + FilaAtendimento; STATUS_DEV preservado para outras telas fora do escopo (Detalhe-Devedor, cadastro)"
    - "Toggle 'Esconder quitados/arquivados' default ON (D-pre-6 Q6) — filtro client-side `c.status NOT IN ('quitado','arquivado')` aplicado em FilaPainel.tabela e FilaPesquisa.resultado. Toggle desativado mostra todos os contratos. ⚠ ajuizado NÃO incluído no toggle por design (operador pode querer ver contratos ajuizados separadamente)"
    - "Cards FilaPainel recalculados POR CONTRATO (Q5): Pendentes = contratos sem evento_andamento hoje; Atendidos Hoje = contratos com evento_andamento hoje; Eventos Hoje = COUNT global eventos_andamento WHERE data_evento >= hoje (sem mudança); Valor em Aberto = SUM(saldo_atualizado_por_contrato) — usa _saldo_atualizado pré-computado por listarContratosParaFila"
    - "Busca por devedor.nome/cpf retornando contratos múltiplos (Q7) — FilaPesquisa filtra `c._devedor?.nome.toLowerCase().includes(q) || c._devedor?.cpf_cnpj.toLowerCase().includes(q)`. 1 devedor com 2 contratos = 2 linhas; outro devedor com 1 contrato cuja busca não bate = 0 linhas. Cross-entity isolation valida em Plan 03 UAT-2"
    - "Filtros existentes preservados: Status (com novos valores STATUS_CONTRATO), Credor (por credor_id), Prioridade (ALTA/MEDIA/BAIXA), valor min/max (sobre _saldo_atualizado, NÃO valor_face). Ordenação intra-tab Pendentes preservada: ALTA→MEDIA→BAIXA, depois saldo desc, depois mais antigo (created_at asc)"
    - "Clique na linha abre DetalheContrato.jsx (componente existente — sem mudança) — passa objeto contrato completo via prop (shape REAL conforme action blocks L771-784 + L1205-1218). Substitui clique em DetalheDevedor que abria visão devedor-level"
    - "Polling 30s preservado em FilaPainel via setInterval (pattern legado L203). Toggle de pausa polling não muda"
    - "Componentes internos reaproveitados (CONTEXT.md L166): PriorityBadge, StatusBadge (consome STATUS_CONTRATO ao invés de STATUS_DEV), AtendimentoBadge, UltimoAtendimentoCell, DividaCell (vira ContratoCell mostrando saldo+vencimentos), CredorCell. Adaptações de prop registradas no SUMMARY"
    - "D-02 isolation strict imports: nenhum import quebrado em FilaDevedor.jsx pós-rewrite (lição feedback_grep_shield_strict_imports.md — usar `^import .* from \"...\"` regex anchored, NÃO substring genérico que pode casar prose). Todos os imports válidos: Modal, Btn, InputBR, dbGet, filaDevedor, STATUS_CONTRATO/STATUS_CONTRATO_TERMINAIS, calcularValorFace/calcularResumoFinanceiro/calcularDetalheEncargos (devedorCalc.js)"
    - "Aliases legacy do Plan 01 (listarDevedoresParaFila/proximoDevedor/alterarStatusDevedor) REMOVIDOS de filaDevedor.js neste plan (Task 7) — após FilaDevedor.jsx pós-rewrite consumir só os nomes novos (listarContratosParaFila/proximoContrato/alterarStatusContrato)"
    - "App.jsx wire-up case 'fila' L8529 PRESERVADO — `<FilaDevedor user={user} devedores={devedores} credores={credores} />`. Props ainda úteis: `devedores` consumido por FilaPesquisa.jsx para enrichment (cross-reference rápido sem fetch); `credores` consumido por filtro de credor no FilaPainel. NÃO remover props"
    - "D-01 motor cumulative INTOCADO desde 3508bb4: gate dual C1 (devedorCalc.js apenas adição wrapper Plan 01; pagamentos.js + dividas.js diff vazio) + C2 (--name-only confirma motor files AUSENTES nos 2 commits 7.13b-01 + 7.13b-02). Lição feedback_gate_motor_grep_use_name_only.md"
    - "PAUSA humana Plan 02 #1 após Task 5 (rewrite UI completo localmente) — usuário valida 4 telas em localhost (npm run dev) antes de remover aliases legacy. PAUSA humana Plan 02 #2 após Task 7 (aliases removidos + gates verdes) — usuário aprova commit final do rewrite UI. autonomous:false LOCKED em todas as tasks"
  artifacts:
    - path: "src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx"
      provides: "Rewrite total — 4 telas adaptadas para shape CONTRATO (D-pre-1..D-pre-6, Q5-Q7). Componentes internos PriorityBadge, StatusBadge, AtendimentoBadge, UltimoAtendimentoCell, ContratoCell (rename de DividaCell), CredorCell preservados/adaptados"
      contains: "listarContratosParaFila"
    - path: "src/mr-3/mr-cobrancas/src/services/filaDevedor.js"
      provides: "Aliases legacy removidos (listarDevedoresParaFila, proximoDevedor, alterarStatusDevedor) — back-compat já não necessária pós-rewrite UI"
      contains: "listarContratosParaFila"
    - path: "src/mr-3/mr-cobrancas/src/App.jsx"
      provides: "Wire-up case 'fila' verificado — props preservadas (devedores+credores ainda úteis para FilaPesquisa enrichment + filtro credor)"
      contains: "FilaDevedor"
  key_links:
    - from: "FilaDevedor.jsx FilaPainel.cards"
      to: "filaDevedor.listarContratosParaFila (Plan 01)"
      via: "Service retorna data com _saldo_atualizado pré-computado por contrato. Cards Pendentes/Atendidos/Valor recalculam client-side a partir desse shape"
      pattern: "_saldo_atualizado"
    - from: "FilaDevedor.jsx FilaPainel.tabela linha"
      to: "DetalheContrato.jsx (componente existente — sem mudança)"
      via: "Clique na linha abre DetalheContrato passando objeto contrato + dependências enriched (dividas, devedores, credores, allPagamentos, allPagamentosDivida) — shape conforme action blocks L771-784 + L1205-1218; substitui DetalheDevedor que era nível-devedor"
      pattern: "DetalheContrato"
    - from: "FilaDevedor.jsx Status select"
      to: "STATUS_CONTRATO (utils/constants.js Plan 01 Task 3)"
      via: "Reuso de constante centralizada — substitui STATUS_DEV nas 4 telas; STATUS_DEV preservado para outras telas (cadastro, Detalhe-Devedor)"
      pattern: "STATUS_CONTRATO"
    - from: "FilaDevedor.jsx Toggle 'Esconder quitados/arquivados'"
      to: "useState toggle local + filter client-side"
      via: "Default ON (D-pre-6 Q6); toggle OFF mostra TODOS os status incluindo terminais"
      pattern: "esconderQuitados"
    - from: "FilaDevedor.jsx FilaAtendimento.registrarEvento"
      to: "filaDevedor.registrarEvento(contratoId, ...) (Plan 01 — assinatura nova)"
      via: "UI passa contrato.id para serviço; service faz lookup devedor_id internamente para back-compat payload"
      pattern: "registrarEvento\\(contrato"
    - from: "FilaDevedor.jsx FilaAtendimento.alterarStatus"
      to: "filaDevedor.alterarStatusContrato(contratoId, novoStatus, usuarioId)"
      via: "UI passa contrato.id; service faz UPDATE em contratos_dividas.status (NÃO devedores.status)"
      pattern: "alterarStatusContrato"
    - from: "FilaDevedor.jsx FilaPesquisa.busca"
      to: "filaDevedor.listarContratosParaFila({busca: q})"
      via: "Service filtra client-side por _devedor?.nome OU _devedor?.cpf_cnpj retornando contratos múltiplos (Q7); 1 devedor 2 contratos = 2 linhas"
      pattern: "filtros\\.busca"
---

<objective>
<!-- REVISION patches aplicadas (orchestrator round 2):
     B2.1 — DetalheContrato shape REAL (Tasks 3+6): { contrato, dividas, devedores, credores, allPagamentos, allPagamentosDivida, hoje, onVoltar, onVerDetalhe, onCarregarTudo }
     B2.2 — Modal API REAL (Tasks 3+6): { title, onClose, children, width=560 } — sem `size` prop
     B2.3 — Btn API REAL (Tasks 3,4,5,6): { sm, color, outline, danger, lime } — sem `primary`/`small`
     B2.4 — Filtros UI completos (Task 3): status_list checkboxes multi + valor_min/valor_max InputBR
     W2.1 — Cards alinhados com tabela base (Task 3 — usar contratosFiltrados, não naoTerminais)
     W2.2 — Acceptance toggle default ON via useState(true) (Task 3)
     W2.3 — temPagamentos refactor (Task 2 ContratoCell — sem .some() falso)
     W2.4 — _totalPago client-side via allPagamentosDivida (Task 5)
-->
Rewrite total da camada de UI da Fila — `FilaDevedor.jsx` (1278 linhas) reescrito com 4 telas adaptadas para shape CONTRATO (D-pre-1..D-pre-6, Q5-Q7). Componentes internos preservados/adaptados (PriorityBadge, StatusBadge consumindo STATUS_CONTRATO, AtendimentoBadge, UltimoAtendimentoCell, ContratoCell, CredorCell). Wire-up `<FilaDevedor>` em App.jsx case 'fila' L8529 verificado/preservado. Aliases legacy do Plan 01 removidos após consumers atualizarem.

Purpose:
- **D-pre-1 + D-pre-2 1 contrato = 1 linha** — devedor com 2 contratos aparece 2x; cada linha tem ciclo de status/atendimento independente.
- **D-pre-3 nome devedor + credor + saldo atualizado por linha** — clique abre DetalheContrato (existente, sem mudança).
- **D-pre-4 status select 7 valores** — em_cobranca/em_localizacao/em_negociacao/notificado/ajuizado/quitado/arquivado via STATUS_CONTRATO export do Plan 01.
- **D-pre-5 telefone/CPF repetido aceito** — sem dedupe.
- **D-pre-6 toggle 'Esconder quitados/arquivados' default ON** — filtro client-side; ajuizado NÃO incluído no toggle (operador vê separado).
- **Q5 cards recalculados por contrato** — Pendentes/Atendidos Hoje/Eventos Hoje/Valor em Aberto.
- **Q7 busca retorna contratos múltiplos** — 1 devedor 2 contratos = 2 linhas; cross-entity isolation valida em Plan 03 UAT-2.
- **D-02 isolation** — strict imports (lição feedback_grep_shield_strict_imports.md), nenhum import quebrado.
- **D-01 motor INTOCADO cumulative** — desde 3508bb4 (post-7.10.bug HEAD).
- **autonomous:false** — usuário valida 4 telas em localhost (PAUSA #1) e diff consolidado pré-commit (PAUSA #2).

Output:
- 1 commit atômico no submódulo: `feat(07.13b-02): rewrite UI Fila — 4 telas por contrato`
- 3 arquivos modificados (FilaDevedor.jsx rewrite + filaDevedor.js aliases removidos + App.jsx wire-up verificado)
- Build + regressão atual da 7.14 verdes pós-commit
- D-01 motor cumulative INTOCADO (gate dual cumulativo desde 3508bb4)

Fora de escopo:
- Migration / service rewrite (Plan 01 já fez)
- UAT humano cross-entity (Plan 03)
- Bump pai / push / Vercel deploy (Plan 03)
- Tag `v1.4-phase7.13b` (Plan 03)
- LEARNINGS.md + memory feedbacks (Plan 03)
- DetalheContrato.jsx (componente existente — sem mudança por design)
- Lembretes/registros_contato/Pessoas listing (D-pre-11 — outros fluxos por devedor_id, preservados)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/PROJECT.md
@.planning/phases/07.13b-fila-por-contrato/07.13b-CONTEXT.md
@.planning/phases/07.13b-fila-por-contrato/07.13b-01-PLAN.md

<!-- UI alvo do rewrite (D-pre-1..D-pre-6) -->
@src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx

<!-- Service consumido (já reescrito em Plan 01) -->
@src/mr-3/mr-cobrancas/src/services/filaDevedor.js

<!-- Wire-up no App (case "fila") -->
@src/mr-3/mr-cobrancas/src/App.jsx

<!-- Componentes destino (PRESERVAR — clique abre DetalheContrato) -->
@src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx

<!-- Constants pós-Plan 01 -->
@src/mr-3/mr-cobrancas/src/utils/constants.js

<!-- UI primitives (REUSAR) -->
@src/mr-3/mr-cobrancas/src/components/ui/Modal.jsx
@src/mr-3/mr-cobrancas/src/components/ui/Btn.jsx
@src/mr-3/mr-cobrancas/src/components/ui/InputBR.jsx

<!-- Memory feedbacks aplicados pre-execute -->
<!-- feedback_grep_shield_strict_imports.md (D-02 isolation: ^import .* from "..." regex anchored) -->
<!-- feedback_strategy_a_shared_wrapper_delegation.md (componentes internos reaproveitáveis) -->
<!-- feedback_motor_parity_via_string_preservation.md (InputBR mantém STRING para parseFloat consumers — preservar pattern em FilaAtendimento se houver inputs numéricos) -->
<!-- feedback_gate_motor_grep_use_name_only.md (gate motor C2 cumulativo desde 3508bb4) -->
<!-- feedback_cross_entity_uat_isolation.md (Plan 03 UAT-2 cross-entity busca devedor) -->

<interfaces>
Key contracts the executor needs (extracted from codebase) — NO codebase exploration needed.

**Submódulo baseline pós-Plan 01:** SHA do commit `feat(07.13b-01)` (registrado em Plan 01 Task 9).
- Cumulative motor diff `git -C src/mr-3 diff 3508bb4..HEAD -- src/mr-cobrancas/src/services/pagamentos.js src/mr-cobrancas/src/services/dividas.js` deve ser VAZIO.
- Cumulative motor name-only `git -C src/mr-3 log 3508bb4..HEAD --format="" --name-only -- 'src/mr-cobrancas/src/services/pagamentos.js' 'src/mr-cobrancas/src/services/dividas.js'` retorna VAZIO.
- devedorCalc.js: APENAS adição wrapper Plan 01 (zero deleções).
- Total commits 7.13b: EXATAMENTE 2 ao final do Plan 02 (07.13b-01 + 07.13b-02).

**Service shape pós-Plan 01 (filaDevedor.js):**
```javascript
filaDevedor = {
  listarContratosParaFila,        // (filtros={}) → { success, data: [...contratos enriquecidos], totalEventosHoje, error }
  proximoContrato,                // (usuarioId) → { success, data: { fila, contrato, devedor, dividas, eventos } | null, error }
  alterarStatusContrato,          // (contratoId, novoStatus, usuarioId) → { success, data: { novoStatus }, error }
  registrarEvento,                // (contratoId, usuarioId, dadosEvento) → { success, data: evento, error }
  calcularScorePrioridade,        // (contratoId) → { success, data: { score, prioridade }, error }
  entrarNaFila,                   // () → { success, data: { inseridos, atualizados }, error }
  reciclarContratos,              // (filtros, equipeId) → { success, data: { reciclados }, error }
  removerDaFila,                  // (filaId, motivo, usuarioId) → { success, data, error }
  listarFila,                     // (filtros) → { success, data: [...itens enriquecidos], error }
  atualizarValoresAtrasados,      // () → { success, data: { atualizados }, error }
  // ALIASES REMOVIDOS NESTE PLANO (Task 7) após FilaDevedor.jsx consumir só nomes novos:
  // listarDevedoresParaFila, proximoDevedor, alterarStatusDevedor
};
```

**Contrato enriquecido shape (retorno listarContratosParaFila):**
```javascript
{
  // Campos de contratos_dividas:
  id, devedor_id, credor_id, numero_contrato, valor_original, valor_atualizado,
  data_origem, data_referencia, status, created_at, updated_at,
  multa_pct, juros_am, honorarios_pct, despesas, /* ... outros campos contrato */

  // Enrichment (Plan 01):
  _devedor: { id, nome, cpf_cnpj, email, telefone, ... } | null,    // filter deleted_at=is.null
  _credor: { id, nome, ... } | null,
  _dividas: [...rows da tabela dividas com contrato_id=this.id],
  _saldo_atualizado: number,                                          // R$ por contrato (motor D-01)
  _fila: { id, status_fila, bloqueado_ate, usuario_id, ... } | null,  // fila_cobranca entry
  _bloqueado: boolean,                                                // bloqueado_ate >= hoje
  _em_atendimento: boolean,                                           // status_fila === EM_ATENDIMENTO
  _ultimo_evento: { contrato_id, tipo_evento, data_evento, usuario_nome } | null,
  _dias_sem_contato: number | null,
  _score: number,                                                     // SCORE_STATUS_CONTRATO + saldo/100 + dias*0.5
  _prioridade: "ALTA" | "MEDIA" | "BAIXA",                            // >=120 / >=80 / <80
}
```

**4 Telas — escopo do rewrite (FilaDevedor.jsx atual L164-1278):**

| Tela | Linhas atuais | Mudança principal |
|------|---------------|-------------------|
| FilaPainel (Supervisor) | L164-487 (~324 linhas) | Cards recalculados POR CONTRATO; toggle 'Esconder quitados/arquivados' default ON; status select usa STATUS_CONTRATO; tabela mostra contratos não devedores; clique abre DetalheContrato |
| FilaOperador (Lobby) | L488-542 (~55 linhas) | Botão 'Próximo' invoca filaDevedor.proximoContrato (renomeado); display do contrato atual ao invés de devedor |
| FilaAtendimento | L543-1035 (~493 linhas) | Recebe `dadosIniciais` do tipo `{ fila, contrato, devedor, dividas, eventos }`; registrarEvento(contrato.id, ...); alterarStatusContrato(contrato.id, ...); display "Contrato X de Y do devedor Z" |
| FilaPesquisa | L1036-1262 (~227 linhas) | Busca em listarContratosParaFila({busca: q}); resultado = N linhas (1 contrato cada); 1 devedor 2 contratos = 2 linhas; clique abre DetalheContrato |

**Componentes internos preservados/adaptados (FilaDevedor.jsx):**
- `PriorityBadge` (L17-36) — preservar verbatim (props: prioridade)
- `StatusBadge` (L39-50) — adaptar: trocar `STATUS_DEV.find(...)` por `STATUS_CONTRATO.find(...)` quando renderizando status de contrato
- `AtendimentoBadge` (L74-92) — preservar lógica; props: `contrato` (no lugar de `devedor`); leitura de `_bloqueado` + `_ultimo_evento` igual
- `UltimoAtendimentoCell` (L95-159) — preservar verbatim (props: ultimoEvento)
- `DividaCell` → `ContratoCell` rename — props: contrato; mostra saldo + número parcelas + venc próximo
- `CredorCell` — preservar verbatim (props: credor)

**App.jsx wire-up (case 'fila' L8529 — PRESERVAR):**
```javascript
case "fila": return <FilaDevedor user={user} devedores={devedores} credores={credores} />;
```
Props `devedores` + `credores` ainda úteis:
- `credores` → filtro de credor no FilaPainel (dropdown)
- `devedores` → fallback enrichment em FilaPesquisa quando service não retornar todos os devedores (cross-reference rápido)

**Toggle 'Esconder quitados/arquivados' (D-pre-6 Q6) — pattern:**
```javascript
const [esconderQuitadosArquivados, setEsconderQuitadosArquivados] = useState(true);  // default ON

const contratosFiltrados = useMemo(() => {
  return contratos.filter(c => {
    if (esconderQuitadosArquivados && (c.status === 'quitado' || c.status === 'arquivado')) return false;
    // ⚠ ajuizado NÃO incluído — operador pode querer ver
    return true;
  });
}, [contratos, esconderQuitadosArquivados]);
```

**Cards FilaPainel recalculados (Q5) — pattern:**
```javascript
const cards = useMemo(() => {
  const hoje = new Date().toISOString().slice(0, 10);
  const contratosNaoTerminais = contratos.filter(c => !['quitado','arquivado','ajuizado'].includes(c.status));
  return {
    pendentes: contratosNaoTerminais.filter(c => !c._ultimo_evento || c._ultimo_evento.data_evento.slice(0,10) !== hoje).length,
    atendidosHoje: contratosNaoTerminais.filter(c => c._ultimo_evento?.data_evento.slice(0,10) === hoje).length,
    eventosHoje: totalEventosHoje,  // já vem do service (count global)
    valorEmAberto: contratosNaoTerminais.reduce((s, c) => s + (c._saldo_atualizado || 0), 0),
  };
}, [contratos, totalEventosHoje]);
```

**Polling 30s (preservar pattern legado L203):**
```javascript
useEffect(() => {
  carregar();
  pollRef.current = setInterval(() => carregar(true), 30000);
  return () => clearInterval(pollRef.current);
}, []);
```

**Imports atuais FilaDevedor.jsx (auditar pós-rewrite — D-02 isolation):**
```javascript
// Pre-rewrite (legacy):
import { useState, useEffect, useCallback, useRef } from "react";
import toast from "react-hot-toast";
import Modal from "./ui/Modal.jsx";
import Btn from "./ui/Btn.jsx";
import InputBR from "./ui/InputBR.jsx";
import { dbGet } from "../config/supabase.js";
import { filaDevedor } from "../services/filaDevedor.js";
import { STATUS_DEV } from "../utils/constants.js";   // ← MUDAR pra STATUS_CONTRATO no rewrite (mantendo STATUS_DEV se ainda usado em outras telas internas; auditar callsites)
import { calcularValorFace, calcularResumoFinanceiro, calcularDetalheEncargos } from "../utils/devedorCalc.js";  // ← preservar (motor D-01 INTOCADO consumido pelo FilaAtendimento)

// Pós-rewrite (alvo):
import { useState, useEffect, useCallback, useRef, useMemo } from "react";
import toast from "react-hot-toast";
import Modal from "./ui/Modal.jsx";
import Btn from "./ui/Btn.jsx";
import InputBR from "./ui/InputBR.jsx";
import DetalheContrato from "./DetalheContrato.jsx";   // ← NOVO: clique na linha abre DetalheContrato
import { dbGet } from "../config/supabase.js";
import { filaDevedor } from "../services/filaDevedor.js";
import { STATUS_CONTRATO, STATUS_CONTRATO_TERMINAIS } from "../utils/constants.js";   // ← MUDADO de STATUS_DEV
import { calcularDetalheEncargosContrato, calcularSaldoContratoAtualizado } from "../utils/devedorCalc.js";  // ← MUDADO: usar adapter contrato + wrapper Plan 01
// (calcularValorFace + calcularResumoFinanceiro removidos — eram do motor por-devedor; em FilaAtendimento usar dividas do contrato direto)
```
</interfaces>

<open_questions>
**Q1 — Onde abrir DetalheContrato? Modal interno vs navigation?**

→ **DECISÃO PLANNER:** **Modal interno overlay**. Razões:
  (a) Pattern atual de FilaAtendimento já é tela cheia interna (não navegação);
  (b) DetalheContrato.jsx é renderizável standalone — signature REAL `{ contrato, dividas, devedores, credores, allPagamentos, allPagamentosDivida, hoje, onVoltar, onVerDetalhe, onCarregarTudo }` (verificada em DetalheContrato.jsx:117);
  (c) Modal interno preserva contexto da Fila (volta para o painel após fechar);
  (d) Sem mudança em react-router (não há router atual).

**B2.1 RESOLVIDO em round 1 revision** — shape REAL alimentado pelo enrichment do Plan 01 (`contrato._dividas`, `contrato._devedores`, `contrato._credores`, `contrato._pagamentos_divida`). Ver action blocks finais L771-784 (FilaPainel) + L1205-1218 (FilaPesquisa) — props `contrato={contratos.find(c => c.id === contratoAberto)}` + listas auxiliares + `onVoltar={() => setContratoAberto(null)}`. D-pre-3 honrado (DetalheContrato.jsx INTOCADO).

**Q2 — FilaPesquisa busca em todos os contratos OU só ativos?**

→ **DECISÃO PLANNER:** **TODOS os contratos** (incluindo terminais). Razões:
  (a) Pesquisa serve para histórico — usuário pode buscar contrato quitado/arquivado;
  (b) listarContratosParaFila SOMENTE retorna não-terminais; FilaPesquisa precisa de query separada `dbGet("contratos_dividas", ...)` que retorne todos;
  (c) Toggle 'Esconder quitados/arquivados' aplica em FilaPesquisa também (default ON), permitindo opt-in para histórico.

**Q3 — Componente DividaCell vira ContratoCell ou continua DividaCell mostrando dividas[0]?**

→ **DECISÃO PLANNER:** **Rename para ContratoCell** — semanticamente correto. Mostra:
  - Número do contrato (truncado se longo)
  - Data origem
  - Saldo atualizado (preferido) ou valor original
  - Quantidade de dívidas/parcelas (e.g. "12 parcelas")
  - Indicador visual se tem pagamento parcial (verde) ou está em atraso (vermelho)

**Q4 — FilaAtendimento exibe TODAS as dívidas do contrato ou só uma?**

→ **DECISÃO PLANNER:** **TODAS as dívidas do contrato**. Razões:
  (a) Operador atende contrato como um todo, não dívidas individuais;
  (b) Display inspirado em DetalheContrato — tabela compact com vencimentos + valor + status amortização;
  (c) Botão "Registrar Pagamento" da tela ATUAL não está em escopo (paga via DetalheContrato — D-pre-3); FilaAtendimento foca em REGISTRAR EVENTO + ALTERAR STATUS apenas.

**Q5 — Remover aliases legacy do filaDevedor.js no Plan 02 ou deixar para phase futura?**

→ **DECISÃO PLANNER:** **Remover neste plan (Task 7)**, APÓS rewrite UI consumir só nomes novos. Razões:
  (a) Aliases criados em Plan 01 EXATAMENTE para back-compat até rewrite UI;
  (b) Manter aliases além do necessário polui surface da API;
  (c) Gate `grep -E "(listarDevedoresParaFila|proximoDevedor|alterarStatusDevedor)" src/services/filaDevedor.js | wc -l` retorna 0 confirma remoção;
  (d) Phase 7.13b deve fechar essa back-compat (não adiar).

**Q6 — Plan 02 produz 1 commit ou múltiplos sub-commits?**

→ **DECISÃO PLANNER:** **1 commit atomic** `feat(07.13b-02): rewrite UI Fila — 4 telas por contrato`. Razões:
  (a) Rewrite UI + remoção aliases + verificação App.jsx formam unidade semântica;
  (b) Pattern Plan 01: 1 commit atomic;
  (c) Total cumulative phase 7.13b = EXATAMENTE 2 commits (01 + 02);
  (d) Plan 03 não cria código (só docs + tag).
</open_questions>
</context>

<tasks>

<task type="auto" autonomous="false">
  <name>Task 1: Pre-execute audit — auditar callsites STATUS_DEV em FilaDevedor.jsx + verificar wire-up App.jsx</name>
  <read_first>
    - src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx (FULL — 1278 linhas; localizar todos os usos de STATUS_DEV, STATUS_ATIVOS, STATUS_TERMINAIS legados; identificar consumers de _devedor vs _contrato shape)
    - src/mr-3/mr-cobrancas/src/App.jsx L8529 + grep "FilaDevedor" (5 matches já confirmados em audit prévio)
    - src/mr-3/mr-cobrancas/src/utils/constants.js (pós-Plan 01 — STATUS_CONTRATO disponível)
    - src/mr-3/mr-cobrancas/src/services/filaDevedor.js (pós-Plan 01 — service shape)
    - src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx (props target — só ler signature do componente principal)
  </read_first>
  <action>
    **Parte A — Audit estático:**
    ```bash
    cd src/mr-3/mr-cobrancas

    # 1. Callsites STATUS_DEV em FilaDevedor.jsx (devem migrar para STATUS_CONTRATO)
    grep -nE "STATUS_DEV|STATUS_ATIVOS|STATUS_TERMINAIS" src/components/FilaDevedor.jsx
    # Anotar contagens e localizações para o rewrite

    # 2. Callsites de service legados (deveriam ser substituídos)
    grep -nE "filaDevedor\\.(listarDevedoresParaFila|proximoDevedor|alterarStatusDevedor)" src/components/FilaDevedor.jsx
    # Esperado: matches em FilaPainel, FilaOperador, FilaAtendimento — anotar para Task 4-6

    # 3. Imports atuais
    grep -nE "^import .* from \"\\.\\./" src/components/FilaDevedor.jsx
    # Confirmar shape exata pre-rewrite

    # 4. wire-up App.jsx — confirmar props passadas
    grep -nB 2 -A 1 "case \"fila\"" src/App.jsx
    # Esperado: <FilaDevedor user={user} devedores={devedores} credores={credores} />

    # 5. Confirmar Plan 01 já SHIPPED localmente (commit feat(07.13b-01) presente)
    cd .. && git log -2 --format="%h %s" | head -5
    # Esperado: HEAD = feat(07.13b-01)
    ```

    **Parte B — Verificar shape de DetalheContrato.jsx:**
    ```bash
    grep -nE "^(function|const|export) DetalheContrato" src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx | head -3
    grep -nE "^function DetalheContrato\\(" src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx
    # Confirmar que signature de DetalheContrato.jsx exports bate com shape do action block L771-784 (contrato + dividas + devedores + credores + allPagamentos + allPagamentosDivida + hoje + onVoltar + onVerDetalhe + onCarregarTudo) para Task 6 (modal overlay)
    ```

    **Parte C — Documentar contagens em variável de contexto pro SUMMARY (não commit):**
    - Total callsites STATUS_DEV em FilaDevedor.jsx: N (alvo pós-rewrite: 0 — substituído por STATUS_CONTRATO; exceto se houver tela interna fora do escopo Fila que ainda usa STATUS_DEV — documentar)
    - Total callsites filaDevedor.{listarDevedoresParaFila,proximoDevedor,alterarStatusDevedor}: M (alvo pós-rewrite: 0 — substituído por nomes novos; aliases removidos em Task 7)
    - Wire-up App.jsx case 'fila' confirmado: SIM
    - DetalheContrato.jsx props signature: <documentado>
  </action>
  <acceptance_criteria>
    - `grep -nE "STATUS_DEV|STATUS_ATIVOS|STATUS_TERMINAIS" src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx | wc -l` >= 1 (baseline pré-rewrite — anotado em context para Task 5)
    - `grep -nE "filaDevedor\\.(listarDevedoresParaFila|proximoDevedor|alterarStatusDevedor)" src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx | wc -l` >= 1 (baseline)
    - `grep -nE "case \"fila\"" src/mr-3/mr-cobrancas/src/App.jsx` retorna 1 match em L8529 (wire-up confirmado)
    - `git -C src/mr-3 log -1 --format=%s | grep -q "feat(07.13b-01)"` exit 0 (Plan 01 commitado — pré-condição)
    - DetalheContrato.jsx props signature documentada em variável de contexto (props nome + tipo)
  </acceptance_criteria>
</task>

<task type="auto" autonomous="false">
  <name>Task 2: Rewrite header + helpers + componentes internos compartilhados (top do arquivo — L1-163)</name>
  <read_first>
    - src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx L1-163 (imports, constantes, helpers compartilhados, componentes internos PriorityBadge/StatusBadge/AtendimentoBadge/UltimoAtendimentoCell/DividaCell/CredorCell)
    - src/mr-3/mr-cobrancas/src/utils/constants.js (STATUS_CONTRATO/STATUS_CONTRATO_TERMINAIS shape pós-Plan 01)
  </read_first>
  <files>
    - src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx
  </files>
  <action>
    **REWRITE TOP (L1-163).** Substituir cabeçalho do arquivo (manter o restante intacto até as 4 telas serem reescritas em Tasks 3-6):

    ```javascript
    /**
     * FilaDevedor.jsx — Phase 7.13b D-pre-1..D-pre-6 + Q5-Q7
     *
     * Refactor estrutural — Fila por CONTRATO (não por devedor).
     * 4 telas: FilaPainel (supervisor), FilaOperador (lobby),
     *          FilaAtendimento (atendimento), FilaPesquisa (busca histórica).
     *
     * 1 contrato = 1 linha (D-pre-2). Devedor com 2 contratos = 2 linhas.
     * Status PER CONTRATO independente (D-pre-4 — 7 valores STATUS_CONTRATO).
     * Toggle 'Esconder quitados/arquivados' default ON (D-pre-6 Q6).
     * Clique abre DetalheContrato modal overlay (D-pre-3).
     */

    import { useState, useEffect, useCallback, useRef, useMemo } from "react";
    import toast from "react-hot-toast";
    import Modal from "./ui/Modal.jsx";
    import Btn from "./ui/Btn.jsx";
    import InputBR from "./ui/InputBR.jsx";
    import DetalheContrato from "./DetalheContrato.jsx";
    import { dbGet } from "../config/supabase.js";
    import { filaDevedor } from "../services/filaDevedor.js";
    import { STATUS_CONTRATO, STATUS_CONTRATO_TERMINAIS } from "../utils/constants.js";
    import { calcularDetalheEncargosContrato } from "../utils/devedorCalc.js";

    // ─── PriorityBadge (preservada verbatim) ──────────────────────
    function PriorityBadge({ prioridade }) {
      const map = {
        ALTA:  { cor: "#DC2626", bg: "#FEE2E2", label: "Alta" },
        MEDIA: { cor: "#F59E0B", bg: "#FEF3C7", label: "Média" },
        BAIXA: { cor: "#10B981", bg: "#D1FAE5", label: "Baixa" },
      };
      const s = map[prioridade] || map.BAIXA;
      return (
        <span style={{
          display: "inline-flex", alignItems: "center", gap: 4,
          background: s.bg, color: s.cor,
          fontSize: 11, fontWeight: 700, padding: "3px 8px",
          borderRadius: 99, letterSpacing: ".2px",
          fontFamily: "'Plus Jakarta Sans',sans-serif",
        }}>
          <span style={{ width: 6, height: 6, borderRadius: "50%", background: s.cor, flexShrink: 0 }} />
          {s.label}
        </span>
      );
    }

    // ─── StatusBadge contrato (D-pre-4 — STATUS_CONTRATO) ─────────
    function StatusBadge({ status }) {
      const s = STATUS_CONTRATO.find(x => x.v === status) || { l: status, cor: "#64748b", bg: "#f1f5f9" };
      return (
        <span style={{
          display: "inline-block", background: s.bg, color: s.cor,
          fontSize: 11, fontWeight: 700, padding: "3px 8px",
          borderRadius: 99, whiteSpace: "nowrap",
        }}>
          {s.l}
        </span>
      );
    }

    // ─── Helpers compartilhados ───────────────────────────────────
    function fmtBRL(v) {
      if (v == null || v === "") return "—";
      return Number(v).toLocaleString("pt-BR", { style: "currency", currency: "BRL" });
    }
    function fmtData(iso) {
      if (!iso) return "—";
      const d = iso.slice(0, 10).split("-");
      return `${d[2]}/${d[1]}/${d[0]}`;
    }
    function diasDesde(iso) {
      if (!iso) return 0;
      return Math.floor((Date.now() - new Date(iso)) / 86400000);
    }

    const th = { padding: "8px 10px", textAlign: "left", fontWeight: 700, color: "#64748b", fontSize: 11, textTransform: "uppercase", letterSpacing: ".5px", whiteSpace: "nowrap" };
    const td = { padding: "9px 10px", color: "#374151", verticalAlign: "middle" };
    const inpS = { padding: "7px 10px", borderRadius: 9, border: "1px solid #d1d5db", fontSize: 12, fontFamily: "'Plus Jakarta Sans',sans-serif", background: "#fff", color: "#374151" };

    const TIPOS_EVENTO_LABEL = { LIGACAO: "Ligação", WHATSAPP: "WhatsApp", EMAIL: "E-mail", SMS: "SMS", PROMESSA_PAGAMENTO: "Promessa", SEM_CONTATO: "Sem contato", ACORDO: "Acordo", TELEFONE_NAO_EXISTE: "Tel. Inativo", CONTATO_COM_CLIENTE: "Contato", RECADO: "Recado" };

    // ─── AtendimentoBadge (props: contrato no lugar de devedor) ───
    function AtendimentoBadge({ contrato }) {
      const hoje = new Date().toISOString().slice(0, 10);
      const semana = new Date(Date.now() - 7 * 86400000).toISOString().slice(0, 10);
      if (contrato._bloqueado) {
        return <span style={{ fontSize: 10, fontWeight: 700, color: "#7c3aed", background: "#ede9fe", padding: "1px 6px", borderRadius: 99 }}>⏰ Promessa</span>;
      }
      const ue = contrato._ultimo_evento;
      if (!ue) {
        return <span style={{ fontSize: 10, fontWeight: 700, color: "#dc2626", background: "#fee2e2", padding: "1px 6px", borderRadius: 99 }}>🔴 Nunca</span>;
      }
      const data = ue.data_evento.slice(0, 10);
      if (data === hoje) {
        return <span style={{ fontSize: 10, fontWeight: 700, color: "#16a34a", background: "#dcfce7", padding: "1px 6px", borderRadius: 99 }}>🟢 Hoje</span>;
      }
      if (data >= semana) {
        return <span style={{ fontSize: 10, fontWeight: 700, color: "#d97706", background: "#fef3c7", padding: "1px 6px", borderRadius: 99 }}>🟡 Semana</span>;
      }
      return null;
    }

    // ─── UltimoAtendimentoCell (preservada verbatim) ──────────────
    function UltimoAtendimentoCell({ ultimoEvento }) {
      if (!ultimoEvento) {
        return <span style={{ color: "#94a3b8", fontSize: 11 }}>— Nunca atendido</span>;
      }
      const tipo = TIPOS_EVENTO_LABEL[ultimoEvento.tipo_evento] || ultimoEvento.tipo_evento;
      return (
        <div>
          <div style={{ fontSize: 12, color: "#374151", fontWeight: 600 }}>{tipo}</div>
          <div style={{ fontSize: 10, color: "#94a3b8" }}>
            {fmtData(ultimoEvento.data_evento)} • {ultimoEvento.usuario_nome || "—"}
          </div>
        </div>
      );
    }

    // ─── ContratoCell (rename de DividaCell — Q3) ─────────────────
    function ContratoCell({ contrato }) {
      const nDividas = contrato._dividas?.length || 0;
      const saldoOuValor = contrato._saldo_atualizado != null
        ? contrato._saldo_atualizado
        : (Number(contrato.valor_original) || 0);
      // W2.3 — predicado direto (sem .some() em closure constante que confundia)
      const valorOriginalNum = Number(contrato.valor_original || 0);
      const temPagamentos = nDividas > 0 && valorOriginalNum > 0 && saldoOuValor < valorOriginalNum;
      return (
        <div>
          <div style={{ fontSize: 12, color: "#374151", fontWeight: 700 }}>
            {fmtBRL(saldoOuValor)}
            {temPagamentos && <span style={{ marginLeft: 6, fontSize: 9, color: "#16a34a", background: "#dcfce7", padding: "1px 4px", borderRadius: 99 }}>parcial</span>}
          </div>
          <div style={{ fontSize: 10, color: "#94a3b8" }}>
            {nDividas} {nDividas === 1 ? "parcela" : "parcelas"}
            {contrato.numero_contrato && ` • ${contrato.numero_contrato}`}
          </div>
        </div>
      );
    }

    // ─── CredorCell (preservado verbatim) ─────────────────────────
    function CredorCell({ credor }) {
      if (!credor) return <span style={{ color: "#94a3b8", fontSize: 11 }}>—</span>;
      return (
        <div>
          <div style={{ fontSize: 12, color: "#374151", fontWeight: 600 }}>{credor.nome || "—"}</div>
        </div>
      );
    }
    ```

    Após esse rewrite L1-163, o restante (FilaPainel L164-487, FilaOperador L488-542, FilaAtendimento L543-1035, FilaPesquisa L1036-1262) ainda está com shape devedor antigo. Tasks 3-6 reescrevem cada tela.

    Validar build (esperado fail temporário se algum callsite estiver inconsistente — Tasks 3-6 corrigem):
    ```bash
    cd src/mr-3/mr-cobrancas && npm run build || echo "Build esperado fail intermediário — Tasks 3-6 vão corrigir"
    ```
  </action>
  <acceptance_criteria>
    - `grep -q "import { STATUS_CONTRATO, STATUS_CONTRATO_TERMINAIS } from \"../utils/constants.js\"" src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx` exit 0
    - `grep -q "import DetalheContrato from \"./DetalheContrato.jsx\"" src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx` exit 0
    - `grep -q "calcularDetalheEncargosContrato" src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx` exit 0
    - StatusBadge usa STATUS_CONTRATO: `grep -A 3 "function StatusBadge" src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx | grep -q "STATUS_CONTRATO.find"` exit 0
    - AtendimentoBadge prop renomeada para `contrato`: `grep -E "function AtendimentoBadge\\(\\{ contrato \\}\\)" src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx` exit 0
    - Componente ContratoCell criado (rename de DividaCell): `grep -q "function ContratoCell" src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx` exit 0
    - Helpers preservados: `grep -cE "^function (fmtBRL|fmtData|diasDesde|UltimoAtendimentoCell|CredorCell|PriorityBadge)" src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx` >= 6
  </acceptance_criteria>
</task>

<task type="auto" autonomous="false">
  <name>Task 3: Rewrite FilaPainel — cards por contrato + tabela contratos + toggle quitados/arquivados + filtros completos (D-pre-2, D-pre-3, D-pre-6, Q5, B2.4)</name>
  <read_first>
    - src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx L164-487 (FilaPainel atual — referência props/state/render)
    - src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx L117 (signature REAL: { contrato, dividas, devedores, credores, allPagamentos, allPagamentosDivida, hoje, onVoltar, onVerDetalhe, onCarregarTudo } — B2.1)
    - src/mr-3/mr-cobrancas/src/components/ui/Modal.jsx (API REAL: { title, onClose, children, width=560 } — B2.2; sem `size` prop)
    - src/mr-3/mr-cobrancas/src/components/ui/Btn.jsx (API REAL: { children, onClick, color, outline, danger, disabled, sm, lime } — B2.3; sem `primary` nem `small`)
    - src/mr-3/mr-cobrancas/src/services/filaDevedor.js (pós-Plan 01 — listarContratosParaFila signature + shape ENRIQUECIDO _devedores/_credores/_pagamentos_divida/_saldo_atualizado/_score/_prioridade)
    - .planning/phases/07.13b-fila-por-contrato/07.13b-CONTEXT.md (D-pre-6 toggle Q6, Q5 cards, L90 filtros prometidos)
  </read_first>
  <files>
    - src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx
  </files>
  <action>
    **Substituir bloco FilaPainel** (L164-487 — função `function FilaPainel(...)` completa). Novo skeleton:

    ```javascript
    // ─── TELA 1: FilaPainel (Supervisor — listagem por contrato) ──
    function FilaPainel({ usuarioId, credores, onAbrirAtendimento }) {
      const [contratos, setContratos] = useState([]);
      const [totalEventosHoje, setTotalEventosHoje] = useState(0);
      const [carregando, setCarregando] = useState(true);
      const [filtros, setFiltros] = useState({ status_list: [], credor_id: "", prioridade: "", busca: "", valor_min: null, valor_max: null });
      const [esconderQuitadosArquivados, setEsconderQuitadosArquivados] = useState(true);  // D-pre-6 default ON
      const [contratoAberto, setContratoAberto] = useState(null);
      const pollRef = useRef(null);

      const carregar = useCallback(async (silent = false) => {
        if (!silent) setCarregando(true);
        const r = await filaDevedor.listarContratosParaFila(filtros);
        if (r.success) {
          setContratos(r.data || []);
          setTotalEventosHoje(r.totalEventosHoje || 0);
        } else {
          toast.error("Erro ao carregar fila: " + (r.error || ""));
        }
        if (!silent) setCarregando(false);
      }, [filtros]);

      // Polling 30s preservado (pattern legacy L203)
      useEffect(() => {
        carregar();
        pollRef.current = setInterval(() => carregar(true), 30000);
        return () => clearInterval(pollRef.current);
      }, [carregar]);

      // D-pre-6 + filtros locais
      const contratosFiltrados = useMemo(() => {
        return contratos.filter(c => {
          if (esconderQuitadosArquivados && (c.status === 'quitado' || c.status === 'arquivado')) return false;
          // ⚠ ajuizado NÃO incluído no toggle (operador pode querer ver)
          return true;
        });
      }, [contratos, esconderQuitadosArquivados]);

      // Cards Q5 — recalculados POR CONTRATO
      // W2.1 — cards usam MESMA base que tabela (contratosFiltrados); ajuizado entra nos cards (consistência com tabela)
      // Decisão: cards refletem o que o usuário VÊ na tabela; não esconder mais que o toggle esconde.
      const cards = useMemo(() => {
        const hoje = new Date().toISOString().slice(0, 10);
        return {
          pendentes: contratosFiltrados.filter(c => !c._ultimo_evento || c._ultimo_evento.data_evento.slice(0,10) !== hoje).length,
          atendidosHoje: contratosFiltrados.filter(c => c._ultimo_evento?.data_evento.slice(0,10) === hoje).length,
          eventosHoje: totalEventosHoje,  // count global do service
          valorEmAberto: contratosFiltrados.reduce((s, c) => s + (Number(c._saldo_atualizado) || 0), 0),
        };
      }, [contratosFiltrados, totalEventosHoje]);

      return (
        <div style={{ padding: "16px 20px", fontFamily: "'Plus Jakarta Sans',sans-serif" }}>
          <h2 style={{ margin: "0 0 12px", fontSize: 18, color: "#1f2937" }}>Fila de Atendimento</h2>
          <p style={{ fontSize: 11, color: "#94a3b8", marginBottom: 8 }}>
            {contratosFiltrados.length} contrato(s) — atualização automática a cada 30s
          </p>

          {/* Cards Q5 */}
          <div style={{ display: "grid", gridTemplateColumns: "repeat(4, 1fr)", gap: 12, marginBottom: 16 }}>
            <Card title="Pendentes" value={cards.pendentes} cor="#dc2626" />
            <Card title="Atendidos Hoje" value={cards.atendidosHoje} cor="#16a34a" />
            <Card title="Eventos Hoje" value={cards.eventosHoje} cor="#2563eb" />
            <Card title="Valor em Aberto" value={fmtBRL(cards.valorEmAberto)} cor="#7c3aed" />
          </div>

          {/* B2.4 — Filtros completos: busca + credor + prioridade + status_list (multi) + valor_min/max + toggle */}
          {/* CONTEXT.md L90 + truth L26 prometem "Status + Credor + Prioridade + valor min/max preservados" */}
          <div style={{ display: "flex", flexWrap: "wrap", gap: 8, marginBottom: 8 }}>
            <input style={inpS} placeholder="Buscar por nome/CPF do devedor"
              value={filtros.busca} onChange={e => setFiltros({...filtros, busca: e.target.value})} />
            <select style={inpS} value={filtros.credor_id}
              onChange={e => setFiltros({...filtros, credor_id: e.target.value})}>
              <option value="">Todos credores</option>
              {(credores || []).map(c => <option key={c.id} value={c.id}>{c.nome}</option>)}
            </select>
            <select style={inpS} value={filtros.prioridade}
              onChange={e => setFiltros({...filtros, prioridade: e.target.value})}>
              <option value="">Todas prioridades</option>
              <option value="ALTA">Alta</option>
              <option value="MEDIA">Média</option>
              <option value="BAIXA">Baixa</option>
            </select>
            {/* B2.4 — InputBR type="value" mantém STRING (lição feedback_motor_parity_via_string_preservation.md) */}
            <InputBR type="value" placeholder="Valor mín"
              value={filtros.valor_min ?? ""}
              onChange={(v) => setFiltros({...filtros, valor_min: v === "" ? null : Number(v)})}
              style={{ ...inpS, width: 120 }} />
            <InputBR type="value" placeholder="Valor máx"
              value={filtros.valor_max ?? ""}
              onChange={(v) => setFiltros({...filtros, valor_max: v === "" ? null : Number(v)})}
              style={{ ...inpS, width: 120 }} />
            <label style={{ display: "flex", alignItems: "center", gap: 6, fontSize: 12, color: "#64748b" }}>
              <input type="checkbox"
                checked={esconderQuitadosArquivados}
                onChange={e => setEsconderQuitadosArquivados(e.target.checked)} />
              Esconder quitados/arquivados
            </label>
          </div>
          {/* B2.4 — Status multi-select checkboxes (pattern legacy FilaDevedor.jsx L342-352) */}
          <div style={{ display: "flex", flexWrap: "wrap", gap: 6, marginBottom: 12, padding: "6px 0" }}>
            <span style={{ fontSize: 11, color: "#64748b", fontWeight: 600, alignSelf: "center" }}>Status:</span>
            {STATUS_CONTRATO.map(s => (
              <label key={s.v} style={{ display: "flex", alignItems: "center", gap: 4, fontSize: 11, color: s.cor, background: s.bg, padding: "3px 8px", borderRadius: 99, cursor: "pointer" }}>
                <input type="checkbox"
                  checked={filtros.status_list.includes(s.v)}
                  onChange={e => {
                    const next = e.target.checked
                      ? [...filtros.status_list, s.v]
                      : filtros.status_list.filter(x => x !== s.v);
                    setFiltros({...filtros, status_list: next});
                  }} />
                {s.l}
              </label>
            ))}
          </div>

          {/* Tabela contratos */}
          {carregando ? <p style={{ color: "#94a3b8" }}>Carregando…</p> : (
            <table style={{ width: "100%", borderCollapse: "collapse", fontSize: 12 }}>
              <thead style={{ background: "#f8fafc", borderBottom: "1px solid #e2e8f0" }}>
                <tr>
                  <th style={th}>Devedor</th>
                  <th style={th}>Credor</th>
                  <th style={th}>Saldo / Parcelas</th>
                  <th style={th}>Status</th>
                  <th style={th}>Prioridade</th>
                  <th style={th}>Último Atendimento</th>
                  <th style={th}>Estado</th>
                  <th style={th}>Ações</th>
                </tr>
              </thead>
              <tbody>
                {contratosFiltrados.map(c => (
                  <tr key={c.id} style={{ borderBottom: "1px solid #f1f5f9", cursor: "pointer" }}
                      onClick={() => setContratoAberto(c.id)}>
                    <td style={td}>
                      <div style={{ fontSize: 12, fontWeight: 700, color: "#1f2937" }}>{c._devedor?.nome || "—"}</div>
                      <div style={{ fontSize: 10, color: "#94a3b8" }}>{c._devedor?.cpf_cnpj || "—"}</div>
                    </td>
                    <td style={td}><CredorCell credor={c._credor} /></td>
                    <td style={td}><ContratoCell contrato={c} /></td>
                    <td style={td}><StatusBadge status={c.status} /></td>
                    <td style={td}><PriorityBadge prioridade={c._prioridade} /></td>
                    <td style={td}><UltimoAtendimentoCell ultimoEvento={c._ultimo_evento} /></td>
                    <td style={td}><AtendimentoBadge contrato={c} /></td>
                    <td style={td} onClick={e => e.stopPropagation()}>
                      {/* B2.3 — Btn API: { sm, color, outline, danger, lime } — sem `primary` nem `small` */}
                      <Btn sm color="#3d9970" disabled={c._bloqueado || c._em_atendimento}
                        onClick={() => onAbrirAtendimento(c.id)}>Atender</Btn>
                    </td>
                  </tr>
                ))}
              </tbody>
            </table>
          )}

          {/* DetalheContrato modal overlay (D-pre-3 — Q1 decisão planner) */}
          {/* B2.1 — DetalheContrato signature REAL exige props completos: { contrato, dividas, devedores, credores, allPagamentos, allPagamentosDivida, hoje, onVoltar, onVerDetalhe, onCarregarTudo } */}
          {/* B2.2 — Modal API real é { title, onClose, children, width=560 } — NÃO aceita size; usa width={1200} pra modal full-width */}
          {contratoAberto && (() => {
            const cAberto = contratos.find(c => c.id === contratoAberto);
            if (!cAberto) return null;
            const hojeStr = new Date().toISOString().slice(0, 10);
            return (
              <Modal title="Detalhe do Contrato" onClose={() => setContratoAberto(null)} width={1200}>
                <DetalheContrato
                  contrato={cAberto}
                  dividas={cAberto._dividas || []}
                  devedores={cAberto._devedores || (cAberto._devedor ? [cAberto._devedor] : [])}
                  credores={cAberto._credores || (cAberto._credor ? [cAberto._credor] : [])}
                  allPagamentos={[]}
                  allPagamentosDivida={cAberto._pagamentos_divida || []}
                  hoje={hojeStr}
                  onVoltar={() => setContratoAberto(null)}
                  onVerDetalhe={() => {}}
                  onCarregarTudo={() => carregar(true)}
                />
              </Modal>
            );
          })()}
        </div>
      );
    }

    // Card auxiliar para FilaPainel
    function Card({ title, value, cor }) {
      return (
        <div style={{ padding: 12, border: "1px solid #e2e8f0", borderRadius: 10, background: "#fff" }}>
          <div style={{ fontSize: 11, color: "#94a3b8", textTransform: "uppercase", letterSpacing: ".5px" }}>{title}</div>
          <div style={{ fontSize: 22, fontWeight: 700, color: cor }}>{value}</div>
        </div>
      );
    }
    ```

    **NOTA sobre DetalheContrato props:** Shape confirmed via Task 1 Parte B grep DetalheContrato.jsx exports — signature L117 = `{ contrato, dividas, devedores, credores, allPagamentos, allPagamentosDivida, hoje, onVoltar, onVerDetalhe, onCarregarTudo }`. Props passed match exactly (B2.1 resolved round 1 revision). D-pre-3 honrado — DetalheContrato.jsx INTOCADO.
  </action>
  <acceptance_criteria>
    - `grep -nE "^function FilaPainel\\(\\{ usuarioId, credores, onAbrirAtendimento \\}\\)" src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx` retorna 1 match (signature preservada)
    - `grep -q "filaDevedor.listarContratosParaFila" src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx` exit 0 (consome service nome novo)
    - `grep -q "esconderQuitadosArquivados" src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx` exit 0 (toggle D-pre-6 implementado)
    - `grep -q "STATUS_CONTRATO_TERMINAIS.includes" src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx` exit 0 (cards Q5 usa terminais para naoTerminais filter)
    - `grep -q "30000" src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx` exit 0 (polling 30s preservado)
    - `grep -q "<DetalheContrato" src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx` exit 0 (modal overlay D-pre-3)
    - **W2.2 — toggle default ON (CÓDIGO):** `grep -cE "useState\(true\)" src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx` >= 2 (FilaPainel + FilaPesquisa cada um tem useState(true) para esconderQuitadosArquivados)
    - `grep -c "useMemo" src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx` >= 2 (cards + filtros memoizados)
    - **B2.4 status_list filtro UI renderizado:** `grep -cE "filtros\.status_list" src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx` >= 2 (declaration + UI checkbox)
    - **B2.4 valor_min/valor_max UI renderizado:** `grep -cE "valor_min|valor_max" src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx` >= 4 (state + setter + InputBR mín + InputBR máx)
    - **B2.4 status checkboxes usam STATUS_CONTRATO map:** `grep -E "STATUS_CONTRATO\.map\(s =>" src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx` exit 0
    - **B2.2 Modal API correta (sem prop `size`):** `grep -cE "<Modal[^>]*size=" src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx` retorna 0
    - **B2.2 Modal usa width prop:** `grep -E "<Modal[^>]*width=\{1200\}" src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx` exit 0
    - **B2.3 Btn primary removido:** `grep -cE "<Btn[[:space:]]+primary" src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx` retorna 0
    - **B2.3 Btn small removido:** `grep -cE "<Btn[[:space:]]+small" src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx` retorna 0
  </acceptance_criteria>
</task>

<task type="auto" autonomous="false">
  <name>Task 4: Rewrite FilaOperador — botão Próximo invoca proximoContrato</name>
  <read_first>
    - src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx L488-542 (FilaOperador atual — referência props onIniciar)
    - src/mr-3/mr-cobrancas/src/services/filaDevedor.js (proximoContrato signature: usuarioId → { fila, contrato, devedor, dividas, eventos })
  </read_first>
  <files>
    - src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx
  </files>
  <action>
    Substituir bloco FilaOperador (L488-542):

    ```javascript
    // ─── TELA 2: FilaOperador (Lobby — botão Próximo) ─────────────
    function FilaOperador({ usuarioId, onIniciar }) {
      const [carregando, setCarregando] = useState(false);

      const handleProximo = async () => {
        setCarregando(true);
        const r = await filaDevedor.proximoContrato(usuarioId);
        setCarregando(false);
        if (!r.success) {
          toast.error("Erro ao buscar próximo contrato: " + (r.error || ""));
          return;
        }
        if (!r.data) {
          toast.success("Nenhum contrato disponível na fila no momento.");
          return;
        }
        // r.data = { fila, contrato, devedor, dividas, eventos }
        onIniciar(r.data);
      };

      return (
        <div style={{
          padding: "60px 20px", textAlign: "center",
          fontFamily: "'Plus Jakarta Sans',sans-serif",
        }}>
          <h2 style={{ margin: "0 0 16px", fontSize: 24, color: "#1f2937" }}>Lobby do Operador</h2>
          <p style={{ fontSize: 14, color: "#64748b", marginBottom: 32 }}>
            Clique em "Próximo Contrato" para receber o próximo da fila.
          </p>
          {/* B2.3 — Btn color="#3d9970" (brand verde) ao invés de `primary` (não existe na API real Btn.jsx) */}
          <Btn color="#3d9970" onClick={handleProximo} disabled={carregando}>
            {carregando ? "Buscando…" : "🎯 Próximo Contrato"}
          </Btn>
        </div>
      );
    }
    ```
  </action>
  <acceptance_criteria>
    - `grep -nE "^function FilaOperador\\(\\{ usuarioId, onIniciar \\}\\)" src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx` retorna 1 match
    - `grep -q "filaDevedor.proximoContrato" src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx` exit 0 (consome nome novo)
    - `grep -q "Próximo Contrato" src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx` exit 0 (label UI atualizado)
  </acceptance_criteria>
</task>

<task type="auto" autonomous="false">
  <name>Task 5: Rewrite FilaAtendimento — registrar evento + alterar status (assinaturas novas)</name>
  <read_first>
    - src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx L543-1035 (FilaAtendimento atual — referência props/state/render)
    - src/mr-3/mr-cobrancas/src/services/filaDevedor.js (registrarEvento(contratoId, ...) + alterarStatusContrato(contratoId, ...))
    - src/mr-3/mr-cobrancas/src/utils/devedorCalc.js (calcularDetalheEncargosContrato — para tela mostrar saldo+composição)
  </read_first>
  <files>
    - src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx
  </files>
  <action>
    Substituir FilaAtendimento (L543-1035). Skeleton principal:

    ```javascript
    // ─── TELA 3: FilaAtendimento (atendimento ativo) ──────────────
    function FilaAtendimento({ usuarioId, dadosIniciais, onProximo, onSair }) {
      // dadosIniciais = { fila, contrato, devedor, dividas, eventos }
      const [fila, setFila] = useState(dadosIniciais.fila);
      const [contrato, setContrato] = useState(dadosIniciais.contrato);
      const [devedor, setDevedor] = useState(dadosIniciais.devedor);
      const [dividas, setDividas] = useState(dadosIniciais.dividas || []);
      const [eventos, setEventos] = useState(dadosIniciais.eventos || []);
      const [allPagamentosDivida, setAllPagamentosDivida] = useState([]);

      const [novoStatus, setNovoStatus] = useState(contrato.status);
      const [tipoEvento, setTipoEvento] = useState("");
      const [descricao, setDescricao] = useState("");
      const [telefoneUsado, setTelefoneUsado] = useState("");
      const [dataPromessa, setDataPromessa] = useState("");
      const [giroDias, setGiroDias] = useState("");
      const [salvando, setSalvando] = useState(false);

      // Carrega pagamentos_divida para mostrar saldo atualizado calculado em tempo real
      useEffect(() => {
        if (!dividas?.length) return;
        const ids = dividas.map(d => d.id);
        dbGet("pagamentos_divida", `divida_id=in.(${ids.join(",")})&select=*`)
          .then(rows => setAllPagamentosDivida(rows || []))
          .catch(() => setAllPagamentosDivida([]));
      }, [dividas]);

      const detalheEncargos = useMemo(() => {
        if (!dividas?.length) return null;
        const hoje = new Date().toISOString().slice(0, 10);
        return calcularDetalheEncargosContrato(dividas, allPagamentosDivida, hoje);
      }, [dividas, allPagamentosDivida]);

      // W2.4 — _totalPago per divida calculado client-side (Plan 01 não promete por divida; reduce dos pagamentos_divida)
      const dividasComPagamento = useMemo(() => {
        return (dividas || []).map(d => {
          const totalPago = (allPagamentosDivida || [])
            .filter(p => String(p.divida_id) === String(d.id))
            .reduce((s, p) => s + (parseFloat(p.valor_pago || p.valor) || 0), 0);
          return { ...d, _totalPago: totalPago };
        });
      }, [dividas, allPagamentosDivida]);

      const handleRegistrarEvento = async () => {
        if (!tipoEvento) { toast.error("Selecione o tipo de evento"); return; }
        setSalvando(true);
        const r = await filaDevedor.registrarEvento(contrato.id, usuarioId, {
          tipo_evento: tipoEvento,
          descricao: descricao || undefined,
          telefone_usado: telefoneUsado || undefined,
          data_promessa: dataPromessa || undefined,
          giro_carteira_dias: giroDias ? Number(giroDias) : undefined,
        });
        setSalvando(false);
        if (!r.success) {
          toast.error("Erro ao registrar evento: " + (r.error || ""));
          return;
        }
        toast.success("Evento registrado.");
        setEventos([r.data, ...eventos]);
        // Reset campos
        setTipoEvento(""); setDescricao(""); setTelefoneUsado("");
        setDataPromessa(""); setGiroDias("");
        // Se ACORDO → sair (contrato saiu da fila)
        if (tipoEvento === "ACORDO") { onSair(); }
      };

      const handleAlterarStatus = async () => {
        if (novoStatus === contrato.status) { toast("Status inalterado."); return; }
        setSalvando(true);
        const r = await filaDevedor.alterarStatusContrato(contrato.id, novoStatus, usuarioId);
        setSalvando(false);
        if (!r.success) {
          toast.error("Erro ao alterar status: " + (r.error || ""));
          return;
        }
        toast.success(`Status alterado para: ${novoStatus}`);
        setContrato({ ...contrato, status: novoStatus });
        if (STATUS_CONTRATO_TERMINAIS.includes(novoStatus)) {
          onSair();
        }
      };

      return (
        <div style={{ padding: "16px 20px", fontFamily: "'Plus Jakarta Sans',sans-serif" }}>
          {/* Header — devedor + contrato */}
          <div style={{ marginBottom: 12 }}>
            <h2 style={{ margin: 0, fontSize: 18, color: "#1f2937" }}>{devedor?.nome || "—"}</h2>
            <p style={{ margin: 0, fontSize: 12, color: "#64748b" }}>
              Contrato {contrato.numero_contrato || contrato.id.slice(0, 8)}
              {detalheEncargos && ` • Saldo Atualizado: ${fmtBRL(detalheEncargos.saldoAtualizado)}`}
            </p>
            <div style={{ marginTop: 6, display: "flex", gap: 6 }}>
              <StatusBadge status={contrato.status} />
              <span style={{ fontSize: 11, color: "#64748b" }}>{(dividas || []).length} parcela(s)</span>
            </div>
          </div>

          {/* Status select (D-pre-4) */}
          <div style={{ marginBottom: 12, display: "flex", gap: 8, alignItems: "center" }}>
            <label style={{ fontSize: 12, color: "#64748b" }}>Alterar status:</label>
            <select style={inpS} value={novoStatus} onChange={e => setNovoStatus(e.target.value)}>
              {STATUS_CONTRATO.map(s => <option key={s.v} value={s.v}>{s.l}</option>)}
            </select>
            {/* B2.3 — Btn sm (não `small`) */}
            <Btn sm onClick={handleAlterarStatus} disabled={salvando || novoStatus === contrato.status}>
              {salvando ? "..." : "Salvar Status"}
            </Btn>
          </div>

          {/* Tabela dívidas (read-only — operador vê todas) */}
          <h3 style={{ margin: "12px 0 6px", fontSize: 14, color: "#1f2937" }}>Parcelas / Dívidas do Contrato</h3>
          <table style={{ width: "100%", borderCollapse: "collapse", fontSize: 11, marginBottom: 16 }}>
            <thead style={{ background: "#f8fafc" }}>
              <tr>
                <th style={th}>#</th>
                <th style={th}>Vencimento</th>
                <th style={th}>Valor Original</th>
                <th style={th}>Pago</th>
                <th style={th}>Saldo</th>
              </tr>
            </thead>
            <tbody>
              {/* W2.4 — usa dividasComPagamento (com _totalPago calculado client-side via allPagamentosDivida) */}
              {dividasComPagamento.map((d, i) => (
                <tr key={d.id} style={{ borderBottom: "1px solid #f1f5f9" }}>
                  <td style={td}>{i + 1}</td>
                  <td style={td}>{fmtData(d.data_vencimento)}</td>
                  <td style={td}>{fmtBRL(d.valor_total)}</td>
                  <td style={td}>{fmtBRL(d._totalPago || 0)}</td>
                  <td style={td}>{fmtBRL((Number(d.valor_total) || 0) - (Number(d._totalPago) || 0))}</td>
                </tr>
              ))}
            </tbody>
          </table>

          {/* Registrar evento */}
          <h3 style={{ margin: "12px 0 6px", fontSize: 14, color: "#1f2937" }}>Registrar Evento</h3>
          <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 8, marginBottom: 8 }}>
            <select style={inpS} value={tipoEvento} onChange={e => setTipoEvento(e.target.value)}>
              <option value="">Selecione tipo…</option>
              {Object.entries(TIPOS_EVENTO_LABEL).map(([v, l]) => <option key={v} value={v}>{l}</option>)}
            </select>
            <input style={inpS} placeholder="Telefone usado"
              value={telefoneUsado} onChange={e => setTelefoneUsado(e.target.value)} />
          </div>
          <textarea style={{ ...inpS, width: "100%", minHeight: 60, marginBottom: 8 }}
            placeholder="Descrição"
            value={descricao} onChange={e => setDescricao(e.target.value)} />
          {tipoEvento === "PROMESSA_PAGAMENTO" && (
            <InputBR type="date" value={dataPromessa} onChange={setDataPromessa}
              placeholder="Data prometida" style={{ ...inpS, marginBottom: 8 }} />
          )}
          <input type="number" style={{ ...inpS, marginBottom: 8 }} min="0"
            placeholder="Giro carteira (dias) — opcional"
            value={giroDias} onChange={e => setGiroDias(e.target.value)} />

          <div style={{ display: "flex", gap: 8 }}>
            {/* B2.3 — Btn color (não `primary`) */}
            <Btn color="#3d9970" onClick={handleRegistrarEvento} disabled={salvando}>
              {salvando ? "Salvando…" : "💾 Registrar Evento"}
            </Btn>
            <Btn onClick={onProximo}>Próximo →</Btn>
            <Btn onClick={onSair}>Sair</Btn>
          </div>

          {/* Histórico de eventos */}
          <h3 style={{ margin: "16px 0 6px", fontSize: 14, color: "#1f2937" }}>Eventos Registrados</h3>
          {eventos.length === 0 ? <p style={{ color: "#94a3b8", fontSize: 11 }}>Nenhum evento.</p> : (
            <ul style={{ listStyle: "none", padding: 0, margin: 0 }}>
              {eventos.map(ev => (
                <li key={ev.id} style={{ padding: "6px 0", borderBottom: "1px solid #f1f5f9", fontSize: 12 }}>
                  <strong>{TIPOS_EVENTO_LABEL[ev.tipo_evento] || ev.tipo_evento}</strong> — {fmtData(ev.data_evento)}
                  {ev.descricao && <div style={{ color: "#64748b", fontSize: 11 }}>{ev.descricao}</div>}
                </li>
              ))}
            </ul>
          )}
        </div>
      );
    }
    ```

    Validar build pós-Task 5 (ainda fica fail se FilaPesquisa não foi reescrita — Task 6):
    ```bash
    cd src/mr-3/mr-cobrancas && npm run build || echo "Build esperado fail intermediário até Task 6"
    ```
  </action>
  <acceptance_criteria>
    - `grep -nE "^function FilaAtendimento\\(\\{ usuarioId, dadosIniciais, onProximo, onSair \\}\\)" src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx` retorna 1 match
    - `grep -q "filaDevedor.registrarEvento(contrato.id" src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx` exit 0 (assinatura nova contratoId)
    - `grep -q "filaDevedor.alterarStatusContrato(contrato.id" src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx` exit 0
    - `grep -q "STATUS_CONTRATO.map" src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx` exit 0 (select usa STATUS_CONTRATO)
    - `grep -q "STATUS_CONTRATO_TERMINAIS.includes(novoStatus)" src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx` exit 0 (terminal → onSair)
    - `grep -q "calcularDetalheEncargosContrato" src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx` exit 0 (saldo realtime)
  </acceptance_criteria>
</task>

<task type="auto" autonomous="false">
  <name>Task 6: Rewrite FilaPesquisa — busca devedor.nome/cpf retornando contratos múltiplos (Q7) + roteador interno + PAUSA #1 visual (B2.1+B2.2+B2.3 patches)</name>
  <read_first>
    - src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx L1036-1262 (FilaPesquisa atual + roteador interno L1263-1278)
    - src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx L117 (signature REAL — B2.1)
    - src/mr-3/mr-cobrancas/src/components/ui/Modal.jsx (API REAL — B2.2)
    - src/mr-3/mr-cobrancas/src/components/ui/Btn.jsx (API REAL — B2.3)
    - src/mr-3/mr-cobrancas/src/services/filaDevedor.js (listarContratosParaFila signature)
  </read_first>
  <files>
    - src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx
  </files>
  <action>
    **Parte A — Substituir FilaPesquisa + roteador interno (final do arquivo):**

    ```javascript
    // ─── TELA 4: FilaPesquisa (busca histórica por contrato) ──────
    function FilaPesquisa({ devedores }) {
      const [contratos, setContratos] = useState([]);
      const [busca, setBusca] = useState("");
      const [esconderQuitadosArquivados, setEsconderQuitadosArquivados] = useState(true);  // D-pre-6
      const [contratoAberto, setContratoAberto] = useState(null);
      const [carregando, setCarregando] = useState(false);

      const carregar = useCallback(async () => {
        setCarregando(true);
        // FilaPesquisa busca em TODOS os contratos (Q2 — incluindo terminais)
        // listarContratosParaFila SOMENTE retorna não-terminais; usa dbGet direto + enrichment manual
        const allContratos = await dbGet("contratos_dividas",
          "select=*&order=created_at.desc&limit=1000");
        setContratos(allContratos || []);
        setCarregando(false);
      }, []);

      useEffect(() => { carregar(); }, [carregar]);

      const devedoresMap = useMemo(() =>
        Object.fromEntries((devedores || []).map(d => [String(d.id), d])),
        [devedores]
      );

      // Q7 — busca por devedor.nome/cpf retornando contratos múltiplos
      const resultados = useMemo(() => {
        const q = busca.toLowerCase().trim();
        return contratos.filter(c => {
          if (esconderQuitadosArquivados && (c.status === 'quitado' || c.status === 'arquivado')) return false;
          if (!q) return true;
          const dev = devedoresMap[String(c.devedor_id)];
          return (dev?.nome || "").toLowerCase().includes(q)
              || (dev?.cpf_cnpj || "").toLowerCase().includes(q)
              || (c.numero_contrato || "").toLowerCase().includes(q);
        });
      }, [contratos, busca, esconderQuitadosArquivados, devedoresMap]);

      return (
        <div style={{ padding: "16px 20px", fontFamily: "'Plus Jakarta Sans',sans-serif" }}>
          <h2 style={{ margin: "0 0 12px", fontSize: 18, color: "#1f2937" }}>Pesquisar Contratos</h2>
          <p style={{ fontSize: 11, color: "#94a3b8", marginBottom: 12 }}>
            {resultados.length} contrato(s)
          </p>

          <div style={{ display: "flex", gap: 8, marginBottom: 12, alignItems: "center" }}>
            <input style={{ ...inpS, flex: 1 }}
              placeholder="Buscar por nome, CPF/CNPJ do devedor ou número do contrato"
              value={busca} onChange={e => setBusca(e.target.value)} />
            <label style={{ display: "flex", alignItems: "center", gap: 6, fontSize: 12, color: "#64748b" }}>
              <input type="checkbox"
                checked={esconderQuitadosArquivados}
                onChange={e => setEsconderQuitadosArquivados(e.target.checked)} />
              Esconder quitados/arquivados
            </label>
          </div>

          {carregando ? <p style={{ color: "#94a3b8" }}>Carregando…</p> : (
            <table style={{ width: "100%", borderCollapse: "collapse", fontSize: 12 }}>
              <thead style={{ background: "#f8fafc", borderBottom: "1px solid #e2e8f0" }}>
                <tr>
                  <th style={th}>Devedor</th>
                  <th style={th}>Contrato</th>
                  <th style={th}>Status</th>
                  <th style={th}>Valor Original</th>
                  <th style={th}>Criação</th>
                </tr>
              </thead>
              <tbody>
                {resultados.map(c => {
                  const dev = devedoresMap[String(c.devedor_id)];
                  return (
                    <tr key={c.id} style={{ borderBottom: "1px solid #f1f5f9", cursor: "pointer" }}
                        onClick={() => setContratoAberto(c.id)}>
                      <td style={td}>
                        <div style={{ fontSize: 12, fontWeight: 700, color: "#1f2937" }}>{dev?.nome || "—"}</div>
                        <div style={{ fontSize: 10, color: "#94a3b8" }}>{dev?.cpf_cnpj || "—"}</div>
                      </td>
                      <td style={td}>{c.numero_contrato || c.id.slice(0, 8)}</td>
                      <td style={td}><StatusBadge status={c.status} /></td>
                      <td style={td}>{fmtBRL(c.valor_original)}</td>
                      <td style={td}>{fmtData(c.created_at)}</td>
                    </tr>
                  );
                })}
              </tbody>
            </table>
          )}

          {/* B2.1 + B2.2 — DetalheContrato signature REAL + Modal width prop */}
          {contratoAberto && (() => {
            const cAberto = contratos.find(c => c.id === contratoAberto);
            if (!cAberto) return null;
            const dev = devedoresMap[String(cAberto.devedor_id)];
            const hojeStr = new Date().toISOString().slice(0, 10);
            return (
              <Modal title="Detalhe do Contrato" onClose={() => setContratoAberto(null)} width={1200}>
                <DetalheContrato
                  contrato={cAberto}
                  dividas={[]}
                  devedores={dev ? [dev] : []}
                  credores={[]}
                  allPagamentos={[]}
                  allPagamentosDivida={[]}
                  hoje={hojeStr}
                  onVoltar={() => setContratoAberto(null)}
                  onVerDetalhe={() => {}}
                  onCarregarTudo={() => carregar()}
                />
              </Modal>
            );
          })()}
        </div>
      );
    }

    // ─── ROTEADOR INTERNO + EXPORT default ────────────────────────
    export default function FilaDevedor({ user, devedores, credores }) {
      const [view, setView] = useState("painel");  // painel | operador | atendimento | pesquisa
      const [atendimentoData, setAtendimentoData] = useState(null);
      const usuario = user?.id || null;

      const handleAbrirAtendimento = async (contratoId) => {
        // Tenta lock otimista — se já estiver bloqueado/atendido, mostra erro
        const r = await filaDevedor.proximoContrato(usuario);
        if (!r.success || !r.data || r.data.contrato.id !== contratoId) {
          toast.error("Contrato indisponível. Use 'Próximo Contrato' do lobby para receber automaticamente.");
          return;
        }
        setAtendimentoData(r.data);
        setView("atendimento");
      };

      const handleIniciar = (data) => {
        setAtendimentoData(data);
        setView("atendimento");
      };

      const handleProximo = async () => {
        setAtendimentoData(null);
        setView("operador");
      };

      const handleSair = () => {
        setAtendimentoData(null);
        setView("painel");
      };

      return (
        <div>
          {/* Tab nav */}
          <div style={{ display: "flex", gap: 4, padding: "8px 20px", borderBottom: "1px solid #e2e8f0", background: "#fff" }}>
            {/* B2.3 — Btn API real: sm + color (não `small`/`primary`). Active tab usa color brand verde; inactive usa outline */}
            <Btn sm color={view === "painel" ? "#3d9970" : "#94a3b8"} outline={view !== "painel"} onClick={() => setView("painel")}>Painel</Btn>
            <Btn sm color={view === "operador" ? "#3d9970" : "#94a3b8"} outline={view !== "operador"} onClick={() => setView("operador")}>Operador</Btn>
            <Btn sm color={view === "pesquisa" ? "#3d9970" : "#94a3b8"} outline={view !== "pesquisa"} onClick={() => setView("pesquisa")}>Pesquisa</Btn>
          </div>

          {view === "painel" && <FilaPainel usuarioId={usuario} credores={credores} onAbrirAtendimento={handleAbrirAtendimento} />}
          {view === "operador" && <FilaOperador usuarioId={usuario} onIniciar={handleIniciar} />}
          {view === "atendimento" && atendimentoData && (
            <FilaAtendimento usuarioId={usuario} dadosIniciais={atendimentoData}
              onProximo={handleProximo} onSair={handleSair} />
          )}
          {view === "pesquisa" && <FilaPesquisa devedores={devedores} />}
        </div>
      );
    }
    ```

    **Parte B — Build pós-rewrite completo:**
    ```bash
    cd src/mr-3/mr-cobrancas && npm run build
    # Esperado exit 0 — todas as 4 telas reescritas, imports válidos
    ```

    **Parte C — PAUSA HUMANA Plan 02 #1 (validação visual em localhost):**

    Pedir usuário para rodar `npm run dev` e validar 4 telas:

    ```
    ╔════════════════════════════════════════════════════════════════════╗
    ║ PAUSA #1 Plan 02 — Validação visual local 4 telas                  ║
    ╠════════════════════════════════════════════════════════════════════╣
    ║ 1. cd src/mr-3/mr-cobrancas && npm run dev                         ║
    ║ 2. Abrir http://localhost:5173 (ou porta padrão Vite)              ║
    ║ 3. Navegar pra módulo Fila (sidebar)                               ║
    ║                                                                    ║
    ║ Tela 1 — Painel:                                                   ║
    ║   - Cards aparecem (Pendentes/Atendidos/Eventos/Valor)             ║
    ║   - Lista de contratos visível (1 contrato = 1 linha)              ║
    ║   - Toggle 'Esconder quitados/arquivados' default ON               ║
    ║   - Status badge mostra labels PT-BR (em_cobranca → "Em Cobrança") ║
    ║   - B2.4 — Filtros UI completos: busca + credor + prioridade +     ║
    ║     7 checkboxes status (multi-select) + valor mín/máx (InputBR)   ║
    ║   - B2.1 — Clique linha abre Modal width=1200 com DetalheContrato  ║
    ║     com TODAS as props: contrato/dividas/devedores/credores/       ║
    ║     allPagamentos/allPagamentosDivida/hoje/onVoltar/onVerDetalhe/  ║
    ║     onCarregarTudo (signature REAL respeitada — D-pre-3 PROIBE     ║
    ║     tocar DetalheContrato.jsx)                                     ║
    ║                                                                    ║
    ║ Tela 2 — Operador:                                                 ║
    ║   - Botão 'Próximo Contrato' visível                               ║
    ║   - Clique abre tela de Atendimento                                ║
    ║                                                                    ║
    ║ Tela 3 — Atendimento:                                              ║
    ║   - Header mostra devedor + contrato + saldo atualizado            ║
    ║   - Status select com 7 valores                                    ║
    ║   - Tabela de parcelas/dívidas visível                             ║
    ║   - Form 'Registrar Evento' funcional (pode submit sem confirmar)  ║
    ║                                                                    ║
    ║ Tela 4 — Pesquisa:                                                 ║
    ║   - Input busca + toggle quitados/arquivados                       ║
    ║   - Buscar por nome do devedor mostra contratos                    ║
    ║   - Devedor com 2 contratos = 2 linhas distintas (Q7 — D-pre-2)    ║
    ║                                                                    ║
    ║ Responder: "APROVADO" ou "FAIL [tela] [motivo]"                    ║
    ║                                                                    ║
    ║ Caso FAIL → debug pontual com usuário antes de Task 7.             ║
    ╚════════════════════════════════════════════════════════════════════╝
    ```

    Aguardar APROVADO antes de prosseguir para Task 7.
  </action>
  <acceptance_criteria>
    - `grep -nE "^function FilaPesquisa\\(\\{ devedores \\}\\)" src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx` retorna 1 match
    - `grep -nE "^export default function FilaDevedor\\(\\{ user, devedores, credores \\}\\)" src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx` retorna 1 match (export default preservado)
    - `grep -q "view === \"painel\"" src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx` exit 0 (roteador interno preservado)
    - `grep -q "view === \"atendimento\"" src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx` exit 0
    - `grep -q "esconderQuitadosArquivados" src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx` exit 0 (toggle nas 2 telas — Painel + Pesquisa)
    - Build limpo: `cd src/mr-3/mr-cobrancas && npm run build` exit 0
    - PAUSA #1 aprovada explicitamente pelo usuário ("APROVADO" ou equivalente)
  </acceptance_criteria>
</task>

<task type="auto" autonomous="false">
  <name>Task 7: Remover aliases legacy de filaDevedor.js + verificar wire-up App.jsx</name>
  <read_first>
    - src/mr-3/mr-cobrancas/src/services/filaDevedor.js (final do arquivo — bloco EXPORT com aliases legacy listarDevedoresParaFila/proximoDevedor/alterarStatusDevedor)
    - src/mr-3/mr-cobrancas/src/App.jsx L8529 (wire-up case 'fila')
    - src/mr-3/mr-cobrancas/src/components/FilaDevedor.jsx (pós-rewrite — confirmado que NÃO usa mais aliases)
  </read_first>
  <files>
    - src/mr-3/mr-cobrancas/src/services/filaDevedor.js
    - src/mr-3/mr-cobrancas/src/App.jsx
  </files>
  <action>
    **Parte A — Confirmar nenhum consumer usa aliases legacy:**

    ```bash
    cd src/mr-3/mr-cobrancas

    # Buscar callsites legacy em TODO o código (não só FilaDevedor)
    grep -rnE "filaDevedor\\.(listarDevedoresParaFila|proximoDevedor|alterarStatusDevedor)" src/
    # Esperado: 0 matches (após rewrite UI Plan 02)

    # Confirmar nomes novos consumidos
    grep -rnE "filaDevedor\\.(listarContratosParaFila|proximoContrato|alterarStatusContrato)" src/
    # Esperado: 5+ matches em FilaDevedor.jsx
    ```

    Se algum match legacy aparecer FORA de filaDevedor.js (definição), corrigir antes de prosseguir (não esperado — Plan 02 cobre todos consumers).

    **Parte B — Remover bloco de aliases:**

    Editar `src/mr-3/mr-cobrancas/src/services/filaDevedor.js`. Localizar o bloco final do export:
    ```javascript
    export const filaDevedor = {
      listarContratosParaFila,
      proximoContrato,
      alterarStatusContrato,
      registrarEvento,
      calcularScorePrioridade,
      entrarNaFila,
      reciclarContratos,
      removerDaFila,
      listarFila,
      atualizarValoresAtrasados,

      // ─── Aliases legacy (DEPRECATED) ──────────────────────────────
      // (... 3 aliases ...)
      listarDevedoresParaFila: listarContratosParaFila,
      proximoDevedor: proximoContrato,
      alterarStatusDevedor: alterarStatusContrato,
    };
    ```

    Remover as 3 linhas de aliases + o comentário do bloco. Resultado:
    ```javascript
    export const filaDevedor = {
      listarContratosParaFila,
      proximoContrato,
      alterarStatusContrato,
      registrarEvento,
      calcularScorePrioridade,
      entrarNaFila,
      reciclarContratos,
      removerDaFila,
      listarFila,
      atualizarValoresAtrasados,
    };
    ```

    **Parte C — Verificar wire-up App.jsx case 'fila' (sem mudança esperada):**

    ```bash
    grep -nE "case \"fila\"" src/App.jsx
    # Esperado: 1 match em L8529 — `case "fila": return <FilaDevedor user={user} devedores={devedores} credores={credores} />;`
    ```

    Se props `devedores`/`credores` não existirem mais no scope do switch (improvável — base post-7.14 ainda passa), AJUSTAR. Senão, NÃO MODIFICAR App.jsx — preservação total do wire-up.

    **Parte D — Validar build + regressão:**
    ```bash
    cd src/mr-3/mr-cobrancas && npm run build && npm run test:regressao
    # Esperado: ambos exit 0
    ```
  </action>
  <acceptance_criteria>
    - **Aliases REMOVIDOS:** `grep -cE "(listarDevedoresParaFila|proximoDevedor|alterarStatusDevedor): " src/mr-3/mr-cobrancas/src/services/filaDevedor.js` retorna 0
    - **Nomes novos preservados:** `grep -cE "^  (listarContratosParaFila|proximoContrato|alterarStatusContrato),$" src/mr-3/mr-cobrancas/src/services/filaDevedor.js` retorna 3
    - **Nenhum consumer legacy:** `grep -rnE "filaDevedor\\.(listarDevedoresParaFila|proximoDevedor|alterarStatusDevedor)" src/mr-3/mr-cobrancas/src/ | wc -l` retorna 0
    - **Wire-up App.jsx preservado:** `grep -nE "case \"fila\".*FilaDevedor" src/mr-3/mr-cobrancas/src/App.jsx` retorna 1 match
    - Build: `cd src/mr-3/mr-cobrancas && npm run build` exit 0
    - Regressão atual: `cd src/mr-3/mr-cobrancas && npm run test:regressao` exit 0
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 8: PAUSA #2 — diff consolidado pré-commit + commit feat(07.13b-02)</name>
  <what-built>
    Plan 02 completo localmente: FilaDevedor.jsx rewrite total (4 telas + componentes internos adaptados), filaDevedor.js aliases removidos, App.jsx wire-up verificado/preservado. Build + regressão verdes. PAUSA #1 (Task 6) já aprovada para validação visual local.
  </what-built>
  <how-to-verify>
    1. **Diff consolidado:**
       ```bash
       git -C src/mr-3 diff --stat
       ```
       Esperado: 2 modified (FilaDevedor.jsx + filaDevedor.js) + 0 modified em App.jsx (se wire-up preservado), 0 deletes em arquivos. Total ~800-1300 linhas alteradas.

    2. **D-01 motor cumulative gate** (lição feedback_gate_motor_grep_use_name_only.md):
       ```bash
       git -C src/mr-3 log 3508bb4..HEAD --format="" --name-only -- 'src/mr-cobrancas/src/services/pagamentos.js' 'src/mr-cobrancas/src/services/dividas.js'
       # Esperado: VAZIO

       git -C src/mr-3 diff 3508bb4..HEAD -- src/mr-cobrancas/src/utils/devedorCalc.js | grep -E "^-(\\s+)" | wc -l
       # Esperado: 0 (zero deleções; apenas adição wrapper Plan 01)
       ```

    3. **D-02 strict imports** (lição feedback_grep_shield_strict_imports.md):
       ```bash
       cd src/mr-3/mr-cobrancas
       grep -E "^import .* from \"\\.\\./" src/components/FilaDevedor.jsx
       # Esperado:
       # - import ... from "../config/supabase.js"
       # - import { filaDevedor } from "../services/filaDevedor.js"
       # - import { STATUS_CONTRATO, STATUS_CONTRATO_TERMINAIS } from "../utils/constants.js"
       # - import { calcularDetalheEncargosContrato } from "../utils/devedorCalc.js"
       # Todos válidos pós-Plan 01.
       ```

    4. **Aliases removidos:**
       ```bash
       grep -cE "(listarDevedoresParaFila|proximoDevedor|alterarStatusDevedor)" src/mr-3/mr-cobrancas/src/services/filaDevedor.js
       # Esperado: 0
       ```

    5. **Build + regressão:**
       ```bash
       cd src/mr-3/mr-cobrancas && npm run build && npm run test:regressao
       # Esperado: ambos exit 0
       ```

    6. **Confirmar 4 telas validadas em localhost** (PAUSA #1 Task 6 já aprovada).
  </how-to-verify>
  <resume-signal>
    Responder "APROVADO" para autorizar commit feat(07.13b-02). Caso "FAIL [motivo]" → debug pontual; caso "ROLLBACK" → executor desfaz mudanças locais (`git -C src/mr-3 checkout -- src/mr-cobrancas/`) e Plan 02 aborta.

    Após APROVADO, executor cria commit:

    ```bash
    cd src/mr-3
    git add src/mr-cobrancas/src/components/FilaDevedor.jsx
    git add src/mr-cobrancas/src/services/filaDevedor.js
    # App.jsx só staged se modificado (esperado: não modificado)
    git status --porcelain | grep "App.jsx" && git add src/mr-cobrancas/src/App.jsx || true

    git commit -m "feat(07.13b-02): rewrite UI Fila — 4 telas por contrato

    Phase 7.13b — Fila por Contrato. UI rewrite total alinhando shape com
    fonte CONTRATOS (refactor Plan 01 service layer).

    FilaDevedor.jsx (1278 linhas → ~900 linhas) — 4 telas reescritas:
    - FilaPainel: cards POR CONTRATO (Pendentes/Atendidos/Eventos/Valor — Q5);
      tabela contratos com 1 linha/contrato (D-pre-2); toggle 'Esconder
      quitados/arquivados' default ON (D-pre-6 Q6); clique abre DetalheContrato
      modal overlay (D-pre-3 — decisão Q1)
    - FilaOperador: botão 'Próximo Contrato' invoca filaDevedor.proximoContrato
    - FilaAtendimento: header devedor+contrato+saldo atualizado; status select
      com 7 valores STATUS_CONTRATO (D-pre-4); registrarEvento(contrato.id,...)
      + alterarStatusContrato(contrato.id,...) (assinaturas novas Plan 01);
      tabela parcelas read-only (Q4); histórico eventos
    - FilaPesquisa: busca por devedor.nome/cpf/numero_contrato em TODOS os
      contratos (Q2 inclui terminais); 1 devedor 2 contratos = 2 linhas (Q7
      — D-pre-2); toggle 'Esconder quitados/arquivados' default ON

    Componentes internos:
    - PriorityBadge, UltimoAtendimentoCell, CredorCell preservados verbatim
    - StatusBadge migrado de STATUS_DEV → STATUS_CONTRATO (D-pre-4)
    - AtendimentoBadge prop renomeada (contrato no lugar de devedor)
    - DividaCell → ContratoCell (rename Q3 — semantic)
    - Roteador interno (export default FilaDevedor) preservado: tabs
      Painel/Operador/Pesquisa + transição automática para Atendimento

    Imports D-02 strict (lição feedback_grep_shield_strict_imports.md):
    - STATUS_CONTRATO + STATUS_CONTRATO_TERMINAIS de utils/constants.js
    - calcularDetalheEncargosContrato de utils/devedorCalc.js (motor D-01
      INTOCADO consumido via adapter Phase 7.8)
    - DetalheContrato (modal overlay D-pre-3)

    Polling 30s preservado em FilaPainel (pattern legacy).

    Aliases legacy removidos de filaDevedor.js:
    - listarDevedoresParaFila, proximoDevedor, alterarStatusDevedor
    - Back-compat única razão de existirem (Plan 01 → Plan 02 transition)

    App.jsx case 'fila' L8529 preservado — props devedores+credores ainda
    úteis para FilaPesquisa enrichment + filtro credor FilaPainel.

    Side-effects fora do escopo preservados (D-pre-11):
    - lembretes/registros_contato continuam por devedor_id (Detalhe-Devedor)
    - Pessoas listing por devedor
    - Dashboard cards de lembretes
    - Carteira Total dedupe via papel=PRINCIPAL (7.10bcd)

    Cumulative phase 7.13b = EXATAMENTE 2 commits (07.13b-01 + 07.13b-02).

    D-01 motor cumulative INTOCADO desde 3508bb4 (gate dual C1 diff vazio
    pagamentos.js+dividas.js, devedorCalc.js apenas adição wrapper Plan 01;
    C2 --name-only confirma motor files AUSENTES nesses 2 commits 7.13b).

    Lições aplicadas pre-execute: feedback_grep_shield_strict_imports
    (D-02 isolation), feedback_strategy_a_shared_wrapper_delegation
    (componentes internos reaproveitáveis), feedback_motor_parity_via_string
    _preservation (InputBR pattern), feedback_gate_motor_grep_use_name_only.

    Plan 03 (próximo): UAT cross-entity humano + bump pai + push +
    Vercel deploy + tag v1.4-phase7.13b + LEARNINGS + 2 memory feedbacks
    novos."

    git log -1 --format=%H
    # Anotar SHA (será SHA bumpado no pai em Plan 03)
    ```

    NÃO bumpar pai. NÃO push. NÃO criar tag. Tudo em Plan 03.
  </resume-signal>
</task>

</tasks>

<verification>
Verificações finais Plan 02:

1. **Wire-up App.jsx confirmado (Task 1):** case 'fila' L8529, props passadas.
2. **Componentes compartilhados (Task 2):** PriorityBadge/UltimoAtendimentoCell/CredorCell preservados; StatusBadge migrado; ContratoCell criado.
3. **FilaPainel (Task 3):** cards Q5, tabela contratos, toggle D-pre-6, modal DetalheContrato D-pre-3.
4. **FilaOperador (Task 4):** botão Próximo invoca proximoContrato.
5. **FilaAtendimento (Task 5):** registrarEvento(contratoId, ...) + alterarStatusContrato(contratoId, ...).
6. **FilaPesquisa (Task 6):** busca devedor.nome/cpf retornando contratos múltiplos (Q7); PAUSA #1 visual aprovada.
7. **Aliases removidos (Task 7):** filaDevedor.js limpo de back-compat.
8. **PAUSA #2 (Task 8):** diff consolidado aprovado; commit feat(07.13b-02) criado.
</verification>

<success_criteria>
- 1 commit atômico no submódulo: `feat(07.13b-02): rewrite UI Fila — 4 telas por contrato`
- 2 arquivos modificados (FilaDevedor.jsx rewrite + filaDevedor.js aliases removidos); App.jsx eventualmente touched (zero esperado)
- Build + regressão atual da 7.14 verdes pós-commit
- D-01 motor cumulative INTOCADO desde 3508bb4 (gate dual)
- Aliases legacy removidos: 0 ocorrências em filaDevedor.js
- 2 PAUSAs humanas aprovadas (PAUSA #1 visual local + PAUSA #2 diff consolidado)
- autonomous:false LOCKED em todas as 8 tasks
- Cumulative phase 7.13b = EXATAMENTE 2 commits (07.13b-01 + 07.13b-02)
</success_criteria>

<output>
Após Plan 02 completo, criar `.planning/phases/07.13b-fila-por-contrato/07.13b-02-SUMMARY.md` registrando:
- Diff stat consolidado
- 4 telas validadas em localhost (PAUSA #1)
- Aliases removidos confirmados
- Wire-up App.jsx preservado (sem modificação)
- D-01 motor cumulative INTOCADO confirmado
- SHA do commit feat(07.13b-02)
- 0 deviations (target) ou listadas se houver

SUMMARY commitado em Plan 03 junto com docs consolidados + tag.
</output>
</content>
</invoke>