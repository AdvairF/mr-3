---
phase: 07.8.2a-saldo-atualizado-listagem-cache-swr
plan: 02
type: execute
wave: 2
depends_on: ["07.8.2a-01"]
autonomous: false
requirements_addressed: []
source: "ROADMAP.md line 324 + 07.8.2a-CONTEXT.md Section 6 (SC1-SC8) + Section 8 (task 07.8.2a-02) + D-14 + D-15"

files_modified: []  # Plan 02 não edita código-fonte — só PAUSAs humanas, commit do submódulo (já feito no Plan 01), bump no pai, commit docs
files_touched:
  - src/mr-3                                                                    # bump no pai (git add src/mr-3 no repo pai)
  - .planning/phases/07.8.2a-saldo-atualizado-listagem-cache-swr/07.8.2a-02-SUMMARY.md   # novo — SUMMARY após bump

must_haves:
  truths:
    - "UAT SC1-SC8 executado pelo usuário em localhost com PASS explícito em TODOS os 8 cenários"
    - "Bump de submódulo no pai criado com mensagem D-15 `chore(07.8.2a): bump submodule mr-3 — saldo atualizado cache SWR`"
    - "Usuário confirma diff consolidado do submódulo (PAUSA #2) antes do bump"
    - "Usuário confirma SHA-bump no pai (PAUSA #3) antes de autorizar push"
    - "Push é ação humana — planner NÃO executa `git push`"
    - "Tag `v1.4-phase7.8.2a` NÃO é criada nesta phase (out of scope — ação separada pós-smoke prod)"
  artifacts:
    - path: ".planning/phases/07.8.2a-saldo-atualizado-listagem-cache-swr/07.8.2a-02-SUMMARY.md"
      provides: "Registro UAT SC1-SC8 + SHA-bump + resultados das 3 PAUSAs"
      contains: "SC1.*PASS|FAIL"
  key_links:
    - from: "Plan 02 PAUSA #1"
      to: "Commit do submódulo (já criado no Plan 01 Task 8)"
      via: "Sem OK UAT, não autoriza PAUSA #2"
      pattern: "PAUSA #1.*APROVADO"
    - from: "Plan 02 PAUSA #2"
      to: "Bump no pai (Task 2)"
      via: "Sem OK diff consolidado submódulo, não bumpa"
      pattern: "PAUSA #2.*APROVADO"
    - from: "Plan 02 PAUSA #3"
      to: "Push humano (fora do plan)"
      via: "Sem OK bump review, não autoriza push"
      pattern: "PAUSA #3.*APROVADO"
---

<objective>
Executar UAT dos 8 success criteria (SC1-SC8 — CONTEXT.md Section 6) em localhost, bumpar submódulo no pai, e deixar o código pronto pra push humano. O commit do submódulo já foi criado em Plan 01 Task 8 (`feat(07.8.2a): saldo atualizado na listagem com cache SWR`). Este plan executa 3 PAUSAs humanas sequenciais (D-14) antes de qualquer mudança irreversível no pai.

Purpose:
- PAUSA #1 (UAT SC1-SC8 local) impede bug comportamental indo pra prod.
- PAUSA #2 (diff consolidado pós-commit submódulo) impede push submódulo errado.
- PAUSA #3 (bump review no pai) impede bump com SHA errado.

Output:
- UAT SC1-SC8 PASS registrado pelo usuário
- Bump de submódulo no pai com commit `chore(07.8.2a): bump submodule mr-3 — saldo atualizado cache SWR`
- `07.8.2a-02-SUMMARY.md` documentando UAT + SHAs + PAUSAs
- Código pronto pra push humano (planner NÃO executa push)

Fora de escopo:
- `git push` (ação humana)
- Tag `v1.4-phase7.8.2a` (ação separada pós-smoke prod — SC13)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07.8.2a-saldo-atualizado-listagem-cache-swr/07.8.2a-CONTEXT.md
@.planning/phases/07.8.2a-saldo-atualizado-listagem-cache-swr/07.8.2a-01-PLAN.md

<!-- Plan 01 criou o commit no submódulo; este plan bumpa no pai após UAT. -->
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1 — PAUSA #1: UAT SC1-SC8 em localhost</name>
  <read_first>
    - `.planning/phases/07.8.2a-saldo-atualizado-listagem-cache-swr/07.8.2a-CONTEXT.md` (Section 6 SC1-SC8 + D-16)
    - `.planning/phases/07.8.2a-saldo-atualizado-listagem-cache-swr/07.8.2a-01-SUMMARY.md` (confirmar commit submódulo criado + gates A-F verdes)
  </read_first>
  <what-built>
    Plan 01 criou 4 arquivos novos (hook, célula, batchCalculator, 3 shields de teste), modificou 3 arquivos existentes (ModuloContratos, DetalheContrato, AdicionarDocumento), atualizou `package.json` no submódulo, passou 14/14 testes regressão + build exit 0 + D-01 motor diff vazio + D-02 isolation grep zero + D-05 shield tabela com todos "YES", e commitou `feat(07.8.2a): saldo atualizado na listagem com cache SWR` no submódulo.

    O pai AINDA NÃO foi bumpado. O localhost deve rodar com o submódulo no SHA do commit do Plan 01.
  </what-built>
  <how-to-verify>
    **Preparação:**
    ```bash
    cd src/mr-3/mr-cobrancas
    npm install   # se necessário
    npm run dev   # inicia Vite
    ```
    Abrir `http://localhost:5173` (ou porta exibida), logar, ir pra tela **Contratos** (`ModuloContratos`).

    **Executar os 8 SCs (copiados VERBATIM da CONTEXT.md Section 6). Marcar PASS/FAIL em cada um:**

    ---

    **SC1 — Contrato A (sem pgtos) + B (com pgtos) valores independentes**
    - Passos: Escolher 2 contratos do banco: A sem nenhum pagamento registrado; B com pelo menos 1 pagamento.
    - Expectativa: Na listagem, coluna "Saldo Atualizado" mostra valores DIFERENTES pra A e B. A ~ `valor_total + correção/juros/etc`; B < A (pagamento reduziu saldo).
    - Resultado: PASS [ ] / FAIL [ ]

    ---

    **SC2 — Pagar em A → linha A atualiza (D-05), B intocada**
    - Passos: Clicar em A → ir pro DetalheContrato → registrar 1 pagamento (ex: R$ 100). Voltar pra listagem de Contratos.
    - Expectativa: Linha de A mostra saldo ATUALIZADO (100 a menos) dentro de ~200ms (status inicial pode ser "revalidating" com opacity 0.75 e voltar pra "fresh" em ~1s). Linha de B INALTERADA (mesmo valor).
    - Resultado: PASS [ ] / FAIL [ ]

    ---

    **SC3 — Contratos → Dashboard → voltar → cache hit < 100ms**
    - Passos: Em Contratos, esperar saldos carregarem. Navegar pra Dashboard (ou outra tela). Voltar pra Contratos.
    - Expectativa: Valores aparecem INSTANTANEAMENTE (< 100ms, sem skeleton). Timestamp header mostra hora anterior.
    - Resultado: PASS [ ] / FAIL [ ]

    ---

    **SC4 — Tab 5min + mudança em outro navegador + focus → revalida (gatilho 3)**
    - Passos: Abrir outra aba/navegador e logar; registrar pagamento num contrato X. Voltar pra tab original do Chrome (focus). (Simulação sem 5min real aceitável — só mudar de app e voltar.)
    - Expectativa: Ao receber focus, listagem revalida TODOS os contratos (status breve "revalidating" em todas as linhas); linha X atualiza com novo saldo.
    - Resultado: PASS [ ] / FAIL [ ]

    ---

    **SC5 — Virada meia-noite Goiânia + F5 → recalcula (gatilho 5)**
    - Passos: Opção (a) esperar virada real; Opção (b) mockar `Date.now()` via DevTools ou ajustar horário do sistema +1 dia; Opção (c) confirmar logicamente via código: fingerprint contém `${dataHoje}|...` e `hojeGoiania()` retorna "sv-SE" fuso Goiânia — se usuário mudar dia manualmente, F5 recalcula.
    - Expectativa: Após virada, F5 dispara recompute (fingerprint muda pelo prefixo `dataHoje`). Valores podem variar (juros/correção acumulam 1 dia).
    - Resultado: PASS [ ] / FAIL [ ] / N/A [ ] (com justificativa em comentário)

    ---

    **SC6 — N máximo existente (~30 contratos) → UI bloqueada < (200ms × N/200)**
    - Passos: Abrir listagem de Contratos com TODOS os contratos reais. Observar tempo de bloqueio da UI (via DevTools Performance ou percepção). Contar N real.
    - Expectativa (D-16): threshold = `200ms × N / 200` = 1ms × N. Pra N=30 → < 30ms de bloqueio perceptível. Batches via idleCallback devem diluir.
    - Resultado: PASS [ ] / FAIL [ ] (anotar N real e tempo observado)

    ---

    **SC7 — Excluir contrato → removeContrato, linha some**
    - Passos: Escolher contrato de teste (não produção). Clicar → DetalheContrato → "Excluir Contrato". Confirmar. Voltar pra listagem.
    - Expectativa: Linha some imediatamente. Cache não retém entrada do contrato excluído (verificável se aparece de volta por bug — não deve).
    - Resultado: PASS [ ] / FAIL [ ]

    ---

    **SC8 — Botão "🔄 Atualizar" → re-dispara batches, timestamp atualiza**
    - Passos: Em Contratos, anotar hora atual no timestamp. Clicar em "🔄 Atualizar". Observar linhas.
    - Expectativa: Todas as linhas brevemente em "revalidating" (opacity 0.75 + title "Atualizando…"); voltam pra "fresh" em < 2s. Timestamp header atualiza pra nova hora.
    - Resultado: PASS [ ] / FAIL [ ]

    ---

    **Matriz consolidada — preencher TODAS antes de resumir:**
    ```
    SC1: [ ] PASS   [ ] FAIL   Nota: ______
    SC2: [ ] PASS   [ ] FAIL   Nota: ______
    SC3: [ ] PASS   [ ] FAIL   Nota: ______
    SC4: [ ] PASS   [ ] FAIL   Nota: ______
    SC5: [ ] PASS   [ ] FAIL   [ ] N/A   Nota: ______
    SC6: [ ] PASS   [ ] FAIL   N=____  Tempo=____ms  Nota: ______
    SC7: [ ] PASS   [ ] FAIL   Nota: ______
    SC8: [ ] PASS   [ ] FAIL   Nota: ______
    ```

    **Regra de aprovação:** PASS em SC1, SC2, SC3, SC7, SC8 é MANDATÓRIO (comportamento core).
    SC4/SC5 podem ser N/A com justificativa técnica documentada.
    SC6 PASS mandatório com N atual; regressão de perf vira input pra 7.8.2b.

    **Se ALGUM SC mandatório = FAIL:**
    - Registrar sintoma observado.
    - **NÃO APROVAR este task** — responder "FAIL: <SCx>: <sintoma>".
    - Voltar pro Plan 01 pra fix (`/gsd-execute-phase 07.8.2a --gaps-only` ou replanejar).
  </how-to-verify>
  <resume-signal>
    Digite `APROVADO` (ou `APROVADO com ressalva: <nota>`) SE todos os SCs mandatórios passaram. Caso contrário, responda `FAIL: <SCx>: <sintoma>` e o executor aborta o plan.
  </resume-signal>
  <acceptance_criteria>
    - Usuário respondeu explicitamente `APROVADO` (ou `APROVADO com ressalva`)
    - Matriz SC1-SC8 preenchida com PASS/FAIL/N/A em cada item, a ser incluída no 07.8.2a-02-SUMMARY.md
    - SC1, SC2, SC3, SC7, SC8 = PASS (mandatórios)
    - SC6 com N real e tempo anotados
    - Se houver FAIL, task aborta e o executor NÃO prossegue pro Task 2
  </acceptance_criteria>
  <done>
    Matriz SC1-SC8 preenchida + resposta humana `APROVADO` registrada no output do task.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 — PAUSA #2: Diff consolidado pós-commit submódulo</name>
  <read_first>
    - `.planning/phases/07.8.2a-saldo-atualizado-listagem-cache-swr/07.8.2a-CONTEXT.md` (D-14 PAUSA #2 + D-15 commit convention)
  </read_first>
  <what-built>
    Task 1 (PAUSA #1) aprovada pelo usuário. Ainda não foi feito bump no pai. O commit do submódulo (Plan 01 Task 8) existe e pode ser revisado agora.
  </what-built>
  <how-to-verify>
    **Executar dentro de `src/mr-3`:**
    ```bash
    cd src/mr-3
    git log --oneline -1
    ```
    Expectativa: último commit = `<SHA> feat(07.8.2a): saldo atualizado na listagem com cache SWR`. Copiar o SHA (7 primeiros chars) — será referenciado no SUMMARY.

    ```bash
    cd src/mr-3
    git diff HEAD~1 HEAD --stat
    ```
    Expectativa: 8 arquivos listados (contagem exata) — os 4 novos + 3 modificados + `package.json`. Ex:
    ```
    mr-cobrancas/package.json                                             | 2 +-
    mr-cobrancas/src/components/AdicionarDocumento.jsx                    | 5 +++++
    mr-cobrancas/src/components/CelulaSaldoAtualizado.jsx                 | <N> ++++++
    mr-cobrancas/src/components/DetalheContrato.jsx                       | <N> +++++
    mr-cobrancas/src/components/ModuloContratos.jsx                       | <N> +++++
    mr-cobrancas/src/hooks/useSaldoAtualizadoCache.js                     | <N> +++++
    mr-cobrancas/src/services/__tests__/saldoAtualizadoCache.test.js      | <N> +++++
    mr-cobrancas/src/utils/batchCalculator.js                             | <N> +++++
    8 files changed, ...
    ```

    ```bash
    cd src/mr-3
    git diff HEAD~1 HEAD -- mr-cobrancas/src/utils/devedorCalc.js \
                             mr-cobrancas/src/services/pagamentos.js \
                             mr-cobrancas/src/services/dividas.js
    ```
    Expectativa: saída VAZIA (D-01 motor intocado).

    **Revisar o diff completo** (opcional mas recomendado):
    ```bash
    cd src/mr-3
    git show HEAD
    ```

    **Pontos de revisão humana:**
    1. 8 arquivos modificados (sem surpresas — nada fora de escopo)
    2. Nenhum touch em devedorCalc.js / pagamentos.js / dividas.js
    3. Mensagem de commit exata: `feat(07.8.2a): saldo atualizado na listagem com cache SWR`
    4. Sem arquivos de log/tmp acidentalmente commitados
    5. Sem `.env`, credenciais ou secrets (deve ser nada — este escopo não toca configs)
  </how-to-verify>
  <resume-signal>
    Digite `APROVADO` SE diff revisado está OK. Caso contrário, responda `FAIL: <motivo>` (ex: `FAIL: arquivo X fora de escopo`, `FAIL: devedorCalc.js modificado`). Em caso de FAIL, o executor aborta SEM bumpar o pai; usuário pode fazer `git reset --soft HEAD~1` no submódulo pra corrigir.
  </resume-signal>
  <acceptance_criteria>
    - Usuário respondeu `APROVADO` explicitamente
    - SHA do commit submódulo anotado pra uso no SUMMARY
    - Contagem de arquivos = 8 (confirmada pelo usuário no output)
    - `git diff HEAD~1 HEAD` em arquivos do motor (devedorCalc.js / pagamentos.js / dividas.js) = saída vazia (confirmado pelo output do comando)
    - Se houver FAIL, executor NÃO prossegue pro Task 3
  </acceptance_criteria>
  <done>
    Diff consolidado aprovado pelo usuário + SHA do submódulo registrado.
  </done>
</task>

<task type="auto">
  <name>Task 3 — Bump submódulo no pai (chore(07.8.2a))</name>
  <read_first>
    - `.planning/phases/07.8.2a-saldo-atualizado-listagem-cache-swr/07.8.2a-CONTEXT.md` (D-15 commit convention pai)
  </read_first>
  <files>src/mr-3 (ponteiro de submódulo — staged no repo pai)</files>
  <action>
    Após APROVADO em Task 1 (UAT) + Task 2 (diff consolidado), criar bump do submódulo **no repo pai** (working dir é o root do repo — NÃO `cd` pra src/mr-3 aqui):

    **1. Verificar estado do pai:**
    ```bash
    git status
    ```
    Expectativa: apenas `modified: src/mr-3 (new commits)` listado. Nenhum outro arquivo do pai deve estar modificado (se houver docs pendentes do planner, tratar em commit separado posterior).

    **2. Adicionar o submódulo ao index:**
    ```bash
    git add src/mr-3
    ```
    **IMPORTANTE**: `git add src/mr-3` (SEM slash trailing). Isso registra o novo SHA do submódulo no pai — NÃO mexe no working tree do submódulo.

    **3. Commit no pai com mensagem D-15 EXATA:**
    ```bash
    git commit -m "chore(07.8.2a): bump submodule mr-3 — saldo atualizado cache SWR"
    ```

    **4. Confirmar:**
    ```bash
    git log --oneline -1
    git diff HEAD~1 HEAD -- src/mr-3
    ```
    Esperado: último commit do pai = `<SHA-pai> chore(07.8.2a): bump submodule mr-3 — saldo atualizado cache SWR`; diff do src/mr-3 mostra `- -Subproject commit <velho>` e `+ +Subproject commit <novo>` onde `<novo>` = SHA do commit submódulo aprovado em Task 2.

    **NÃO executar `git push` aqui.** Push é ação humana pós-PAUSA #3 (Task 4).
    **NÃO criar tag `v1.4-phase7.8.2a` aqui.** Tag é separada pós-smoke prod (SC13).
  </action>
  <verify>
    <automated>git log -1 --oneline | grep -qE "chore\(07\.8\.2a\): bump submodule mr-3"</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --oneline` output matches `chore(07.8.2a): bump submodule mr-3 — saldo atualizado cache SWR`
    - `git diff HEAD~1 HEAD -- src/mr-3` shows `-Subproject commit <OLD>` and `+Subproject commit <NEW>` only
    - `git status` após commit está clean (exceto docs pendentes do planner, se houver)
    - ZERO `git push` executado neste task
    - ZERO tag criada neste task
  </acceptance_criteria>
  <done>
    Bump no pai criado com mensagem D-15; um único commit no pai; working tree clean.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4 — PAUSA #3: Bump review no pai + autorização de push manual</name>
  <read_first>
    - `.planning/phases/07.8.2a-saldo-atualizado-listagem-cache-swr/07.8.2a-CONTEXT.md` (D-14 PAUSA #3 + D-15)
  </read_first>
  <what-built>
    Task 3 criou commit no pai `chore(07.8.2a): bump submodule mr-3 — saldo atualizado cache SWR`. O pai agora aponta pro SHA do commit submódulo aprovado em PAUSA #2. Push ainda NÃO foi executado.
  </what-built>
  <how-to-verify>
    **Executar no root do repo pai:**
    ```bash
    git log --oneline -1
    ```
    Esperado: `<SHA-pai> chore(07.8.2a): bump submodule mr-3 — saldo atualizado cache SWR`. Copiar SHA-pai.

    ```bash
    git diff HEAD~1 HEAD -- src/mr-3
    ```
    Esperado: exatamente duas linhas mudadas — `-Subproject commit <SHA-velho>` e `+Subproject commit <SHA-novo>`. O `<SHA-novo>` DEVE bater com o SHA do commit aprovado em PAUSA #2 (Task 2).

    **Cross-check explícito:**
    ```bash
    git -C src/mr-3 log --oneline -1
    ```
    Esperado: `<SHA-novo-submódulo> feat(07.8.2a): saldo atualizado na listagem com cache SWR`.

    O `<SHA-novo-submódulo>` acima DEVE ser o MESMO `<SHA-novo>` do `+Subproject commit` no diff do pai.

    **Matriz de verificação (preencher):**
    ```
    SHA commit submódulo (PAUSA #2):   ________
    SHA em +Subproject commit no pai:  ________
    São IDÊNTICOS?                     [ ] SIM   [ ] NÃO
    SHA commit pai (Task 3):           ________
    Mensagem commit pai bate com D-15? [ ] SIM   [ ] NÃO
    ```

    **Se SHAs NÃO idênticos ou mensagem errada:**
    - Abortar: `git reset --soft HEAD~1` no pai pra desfazer o bump.
    - Corrigir (raro — possível causa: `git add src/mr-3` antes do commit submódulo estabilizar).
    - Responder `FAIL: <motivo>`.

    **Se tudo OK:**
    Responder `APROVADO` para autorizar push humano.

    **IMPORTANTE — push é ação HUMANA:** Planner NÃO executa `git push`. Após APROVADO neste task, o USUÁRIO executa manualmente:
    ```bash
    # Dentro do submódulo primeiro (tem que ir primeiro):
    cd src/mr-3 && git push origin main
    # Voltar pro pai e pushar:
    cd ../.. && git push origin master
    ```

    Se push falhar com "rejected" por branch não-rastreada ou similar, user decide como resolver (pode precisar `git push -u origin <branch>`).
  </how-to-verify>
  <resume-signal>
    Digite `APROVADO` se SHA-bump idêntico ao commit aprovado em PAUSA #2 + mensagem D-15 correta. Digite `FAIL: <motivo>` se houver qualquer divergência.
  </resume-signal>
  <acceptance_criteria>
    - Usuário confirmou SHA-bump no pai = SHA commit submódulo aprovado em PAUSA #2
    - Usuário confirmou mensagem de commit pai = `chore(07.8.2a): bump submodule mr-3 — saldo atualizado cache SWR`
    - Matriz de verificação preenchida (ambos "SIM") no output do task
    - Resposta `APROVADO` registrada
    - Push NÃO executado pelo planner — instrução explícita deixada para o usuário executar manualmente
    - Tag `v1.4-phase7.8.2a` NÃO criada neste task (fora de escopo — SC13)
  </acceptance_criteria>
  <done>
    Bump review aprovado + instrução de push manual documentada + zero push/tag executado.
  </done>
</task>

<task type="auto">
  <name>Task 5 — Criar 07.8.2a-02-SUMMARY.md + commit docs</name>
  <read_first>
    - `.planning/phases/07.8.2a-saldo-atualizado-listagem-cache-swr/07.8.2a-CONTEXT.md` (D-15 commit docs)
    - Outputs das Tasks 1-4 (SC matrix, SHAs, matriz bump)
  </read_first>
  <files>.planning/phases/07.8.2a-saldo-atualizado-listagem-cache-swr/07.8.2a-02-SUMMARY.md (NOVO)</files>
  <action>
    Criar `.planning/phases/07.8.2a-saldo-atualizado-listagem-cache-swr/07.8.2a-02-SUMMARY.md` com:

    ```markdown
    # Phase 7.8.2a — Plan 02 SUMMARY

    **Status**: Bumped + awaiting human push
    **Data**: <YYYY-MM-DD>

    ## Tasks executados

    ### Task 1 — PAUSA #1 UAT SC1-SC8
    <matriz SC1-SC8 PASS/FAIL/N/A preenchida pelo usuário>
    Resultado: APROVADO | APROVADO com ressalva | FAIL aborted

    ### Task 2 — PAUSA #2 Diff consolidado
    - SHA commit submódulo: `<SHA-submódulo>`
    - Arquivos modificados: 8 (4 novos + 3 modificados + package.json)
    - D-01 motor intocado: VAZIO ✓
    Resultado: APROVADO

    ### Task 3 — Bump no pai
    - SHA commit pai: `<SHA-pai>`
    - Mensagem: `chore(07.8.2a): bump submodule mr-3 — saldo atualizado cache SWR`

    ### Task 4 — PAUSA #3 Bump review
    <matriz preenchida: SHA submódulo ≡ +Subproject commit? SIM. Mensagem D-15? SIM>
    Resultado: APROVADO — push autorizado manualmente

    ## SHAs registrados

    | Escopo | SHA | Mensagem |
    |--------|-----|----------|
    | Submódulo mr-3 | `<sha7-sub>` | feat(07.8.2a): saldo atualizado na listagem com cache SWR |
    | Pai (root) | `<sha7-pai>` | chore(07.8.2a): bump submodule mr-3 — saldo atualizado cache SWR |

    ## Push

    - Push do submódulo: **pendente** (usuário executa `cd src/mr-3 && git push origin main`)
    - Push do pai: **pendente** (usuário executa `git push origin master` após push do submódulo)

    ## Fora de escopo (diferido)

    - Tag `v1.4-phase7.8.2a`: NÃO criada — ação separada pós-smoke prod (SC13)
    - Phase 7.8.2b (ordenação + virtualização): backlog
    - Phase 7.8.2c (modal na listagem): backlog

    ## Próximos passos

    1. Usuário executa push do submódulo + push do pai
    2. Smoke em prod (mrcobrancas.com.br) — SC6 definitivo
    3. Se smoke OK: criar tag `v1.4-phase7.8.2a` em ação separada
    4. Se regressão de perf SC6: abrir Phase 7.8.2b
    ```

    **Commit dos docs (D-15):**
    Após salvar o SUMMARY.md:
    ```bash
    git add .planning/phases/07.8.2a-saldo-atualizado-listagem-cache-swr/07.8.2a-02-SUMMARY.md
    git commit -m "docs(07.8.2a): PLAN e feedback"
    ```
    *Nota*: o commit de docs é separado do commit de bump (D-15) — mantém a history limpa.

    **NÃO incluir `git push` aqui também.** Push é ação humana pós-PAUSA #3.
  </action>
  <verify>
    <automated>ls -la .planning/phases/07.8.2a-saldo-atualizado-listagem-cache-swr/07.8.2a-02-SUMMARY.md &amp;&amp; git log -1 --oneline | grep -qE "docs\(07\.8\.2a\)"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/07.8.2a-saldo-atualizado-listagem-cache-swr/07.8.2a-02-SUMMARY.md` exists
    - SUMMARY contém matriz SC1-SC8 + SHAs submódulo/pai + registro das 3 PAUSAs
    - `git log -1 --oneline` shows `docs(07.8.2a): PLAN e feedback`
    - ZERO `git push` executado neste task
    - ZERO criação de tag `v1.4-phase7.8.2a`
  </acceptance_criteria>
  <done>
    SUMMARY criado + commit docs aplicado + phase encerrada em estado "bumped awaiting human push".
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (Plan 02 — workflow correctness)

| Boundary | Description |
|----------|-------------|
| Plan 01 → Plan 02 | Commit submódulo já existe; plan 02 herda estado. Se Plan 01 falhou gates, Plan 02 não deveria ter começado. |
| PAUSA #1 → Plan 02 Task 2 | UAT FAIL impede diff review; evita bump com bug comportamental. |
| PAUSA #2 → Plan 02 Task 3 | Diff review FAIL impede bump; evita apontar pai pra commit errado. |
| PAUSA #3 → Push humano | Bump review FAIL impede push; evita prod receber SHA errado. |
| Planner → `git push` | Proibido. Só humano empurra. |

## Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.8.2a-10 | Elevation of Privilege (push não autorizado) | Plan 02 Tasks | mitigate | ZERO `git push` em qualquer task. Acceptance criteria de cada task explicitamente verificam "ZERO push executado". |
| T-07.8.2a-11 | Tampering (bump com SHA errado) | Task 3 (bump no pai) | mitigate | PAUSA #3 (Task 4) cross-check SHA submódulo vs `+Subproject commit` no pai. FAIL → `git reset --soft HEAD~1` no pai + retry. |
| T-07.8.2a-12 | Repudiation (UAT sem registro) | Task 1 matriz SC1-SC8 | mitigate | Task 5 exige matriz preenchida no SUMMARY.md. Sem matriz, task fails acceptance. |
| T-07.8.2a-13 | Information Disclosure (tag prematura) | Criação de tag antes de smoke prod | mitigate | Explicitly forbidden em múltiplos acceptance criteria. Tag `v1.4-phase7.8.2a` = ação separada pós-smoke (SC13). |
| T-07.8.2a-14 | DoS (UAT skipped) | PAUSA #1 contornada | mitigate | Task 1 é `checkpoint:human-verify gate="blocking"`. Resume signal exige `APROVADO` literal. |
| T-07.8.2a-15 | Tampering (docs commit mistura com bump) | Task 3 + Task 5 | mitigate | Task 3 commita só bump (`chore`); Task 5 commita só docs (`docs`). Commits separados mantêm history limpa (D-15). |
</threat_model>

<verification>
- [ ] Task 1 — PAUSA #1 — Matriz SC1-SC8 preenchida + APROVADO do usuário
- [ ] Task 2 — PAUSA #2 — Diff consolidado submódulo aprovado + SHA submódulo registrado
- [ ] Task 3 — Bump no pai commitado com mensagem D-15 `chore(07.8.2a): bump submodule mr-3 — saldo atualizado cache SWR`
- [ ] Task 4 — PAUSA #3 — Cross-check SHA submódulo ≡ +Subproject commit no pai + APROVADO
- [ ] Task 5 — SUMMARY.md criado + commit docs `docs(07.8.2a): PLAN e feedback`
- [ ] ZERO `git push` executado em nenhum task
- [ ] ZERO tag `v1.4-phase7.8.2a` criada
- [ ] Phase state final: "bumped awaiting human push"
</verification>

<success_criteria>
1. UAT SC1-SC8 executado em localhost com PASS mandatório em SC1, SC2, SC3, SC7, SC8 (SC4/SC5 aceita N/A com justificativa; SC6 com N real anotado)
2. Diff consolidado do submódulo aprovado pelo usuário com saída vazia em arquivos do motor (D-01)
3. Bump no pai criado com commit `chore(07.8.2a): bump submodule mr-3 — saldo atualizado cache SWR`
4. Cross-check SHA submódulo ≡ `+Subproject commit` no pai executado e aprovado
5. `07.8.2a-02-SUMMARY.md` criado com matriz SC1-SC8, SHAs submódulo/pai e registro das 3 PAUSAs
6. Commit docs `docs(07.8.2a): PLAN e feedback` aplicado
7. ZERO `git push` executado por este plano (push é ação humana)
8. ZERO criação da tag `v1.4-phase7.8.2a` (out of scope — pós-smoke prod)
9. Estado final: phase 7.8.2a "bumped + awaiting human push + smoke prod + tag separada"
</success_criteria>

<output>
Após completion: `.planning/phases/07.8.2a-saldo-atualizado-listagem-cache-swr/07.8.2a-02-SUMMARY.md` contendo:
- Matriz SC1-SC8 preenchida com PASS/FAIL/N/A
- SHA commit submódulo (mr-3) + SHA commit pai
- Resultado de cada uma das 3 PAUSAs
- Lista explícita de ações pendentes (push humano, smoke prod, tag separada)

Fim da phase 7.8.2a. Próxima ação: usuário executa pushes + smoke prod + (se OK) cria tag `v1.4-phase7.8.2a`.
</output>
