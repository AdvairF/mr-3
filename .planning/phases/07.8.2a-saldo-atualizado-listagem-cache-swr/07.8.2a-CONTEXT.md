# Phase 7.8.2a — Saldo Atualizado na Listagem de Contratos (cache SWR)

**Status**: CONTEXT locked 2026-04-24 — awaiting /gsd-plan-phase 7.8.2a
**Inserção**: manual (bug CLI persistente). Numeração `7.8.2a` segue convenção split: letra sufixo = scope carve-out (a = cache SWR, b = sort + virtualização futura, c = modal-on-listagem eventual).
**Discussão original**: sessão 2026-04-24 pós-ship 7.8.1 (tag v1.4-phase7.8.1).
**Modo**: standard-reforçado (3 PAUSAs humanas: UAT, diff consolidado, bump review).

---

## 1. Problema

Listagem de contratos (`ModuloContratos.jsx`) mostra só valores NOMINAIS: `valor_total`, `num_documentos`, `num_parcelas`, `em_atraso`. Advogado não vê saldo ATUALIZADO (Art. 354 CC) na visão agregada — tem que clicar em cada contrato pra ver o valor real. Com 200 contratos, é inviável.

Phase 7.8 trouxe saldo atualizado no DetalheContrato (4ª coluna + modal). Phase 7.8.2a leva pra listagem via cache SWR (padrão Stripe/Shopify/Vercel) — instantâneo ao voltar pra tela, revalida em background.

## 2. Escopo aprovado

**Fix A — Hook `useSaldoAtualizadoCache` (novo em `src/hooks/`)**
- Singleton `Map<contratoId, { saldo, detalhe, fingerprint, status, calculadoEm }>`
- Pub/sub com `Set<listener>` — componente se inscreve por contrato.id
- Fingerprint **deep** (12 campos motor-relevantes — ver D-06)
- API: `useSaldoContrato(contrato, dividas, pagamentos, hoje)`, `invalidateContrato(id)`, `invalidateAll()`, `removeContrato(id)`, `kickoffBatch(contratos, dividas, pagamentos, hoje)`
- `window.focus` listener no module-load (guard `typeof window`)
- Retorna 3 estados: `"loading"` (miss), `"fresh"` (fingerprint match), `"revalidating"` (stale ou drift)

**Fix B — `CelulaSaldoAtualizado.jsx` (novo em `src/components/`)**
- Consome `useSaldoContrato`
- 3 estados com fade 180ms: skeleton dots (loading), valor pleno (fresh), valor opacity 0.75 + title tooltip (revalidating)
- **Display-only** (não clicável) — row inteira continua navegando pro DetalheContrato

**Fix C — `batchCalculator.js` (novo em `src/utils/`)**
- `runBatches(items, batchSize, processBatch)` — `requestIdleCallback` com fallback `setTimeout(0)`, timeout 500ms

**Fix D — `ModuloContratos.jsx` (modificado)**
- Nova coluna "Saldo Atualizado" (header + `<CelulaSaldoAtualizado />`)
- Header ganha botão "🔄 Atualizar" (invalidateAll + kickoffBatch) + timestamp "Atualizado às HH:MM" (lê max calculadoEm via subscribe)
- `kickoffBatch` no mount com deps `[contratos.length, dividas.length, allPagamentosDivida.length]`
- Guard StrictMode via `useRef` flag `kickedOff.current`

**Fix E — 6 callers mutation (D-05 enforcement)**
- `DetalheContrato.jsx::handleRegistrarPagamento` → `invalidateContrato(contrato.id)` pós `toast.success`
- `DetalheContrato.jsx::handleExcluirPagamento` → idem
- `DetalheContrato.jsx::handleSalvarParcelasCustom` → idem
- `DetalheContrato.jsx::handleExcluirDocumento` → idem
- `DetalheContrato.jsx::handleExcluirContrato` → `removeContrato(contrato.id)` (contrato deixou de existir)
- `AdicionarDocumento.jsx::handleSalvar` → `invalidateContrato(contrato.id)` pós sucesso

**Fix F — Testes shield (3 novos em `src/services/__tests__/saldoAtualizadoCache.test.js`)**
- Shield 1: cache cross-entity (mudança em B não afeta A)
- Shield 2: fingerprint muda com virada de data Goiânia
- Shield 3: `invalidateContrato(A)` não afeta B/C

## 3. Fora do escopo

- **Phase 7.8.2b**: ordenação por saldo atualizado + virtualização da tabela
- **Phase 7.8.2c**: modal de decomposição inline na listagem (display-only agora)
- Mudanças no motor Art.354 ou no adapter (D-01/D-03/D-04 invariantes)
- Persistência localStorage/sessionStorage (in-memory; F5 recomputa)
- Optimization shallow-compare antes de deep fingerprint (deferido pra 7.8.2b se virar gargalo)

## 4. Decisões locked (discuss 2026-04-24)

- **D-01** — Motor Art.354 (`utils/devedorCalc.js` — funções existentes) intocado. `git diff HEAD -- src/utils/devedorCalc.js src/services/pagamentos.js src/services/dividas.js` VAZIO. 11/11 regressão preservada.

- **D-02** — Hook e componentes novos NÃO importam `DetalheContrato.jsx` nem `AdicionarDocumento.jsx`. Isolamento unidirecional hook → componentes listagem. Grep negativo.

- **D-03** — Motor via `calcularDetalheEncargosContrato` apenas. Zero reimplementação Art.354.

- **D-04** — Adapter filtra `allPagamentosDivida` por `divida_id` INTERNAMENTE (shield 7.8.1-02). Hook passa prop global sem pré-filter.

- **D-05 (NOVA — lição 7.8)** — Callers completude: todo handler que mute `pagamentos_divida`, `documentos_contrato` OU `dividas` (insert/update/delete) DEVE chamar `invalidateContrato(contrato.id)` (ou `removeContrato` pro delete de contrato) ANTES do `toast.success`/`setState`. Complementa D-02 (hook→comp): D-05 cobre o inverso (callers→hook). Violação = silent-stale em prod. Shield grep obrigatório.

- **D-06 (Fingerprint deep — 12 campos)**:
  ```javascript
  function contratoFingerprint(dividasDoContrato, pagamentosFiltered, dataHoje) {
    const D = dividasDoContrato.map(d => [
      d.id, d.valor_total, d.data_vencimento, d.data_origem,
      d.data_inicio_atualizacao, d.indexador || "", d.multa_pct || 0,
      d.juros_tipo || "", d.juros_am || 0, d.honorarios_pct || 0,
      d.art523_opcao || "", d.saldo_quitado ? 1 : 0,
    ].join(":")).sort().join("|");
    const P = pagamentosFiltered.map(p =>
      [p.id, p.divida_id, p.valor, p.data_pagamento].join(":")
    ).sort().join("|");
    return `${dataHoje}|D:${D}|P:${P}`;
  }
  ```
  Captura fallback chain do motor (devedorCalc.js:387: `div.data_inicio_atualizacao || div.data_vencimento || div.data_origem`). Custo ~2-4ms pra 200 contratos — aceitável.

- **D-07 (SWR semantic — 3 conceitos distintos)**:
  - Cache key = `contrato.id`
  - Fingerprint = campos do motor (detecta props drift)
  - Revalidação = triggers 1-5 (força recompute regardless de fingerprint)
  - Hash NÃO decide revalidar — decide cache hit/miss na leitura.

- **D-08 (5 gatilhos)**:
  1. Mount cache vazio → `kickoffBatch` em batches de 10 via `requestIdleCallback`
  2. Mount cache preenchido → render instantâneo stale + revalida background
  3. `window.focus` → `invalidateAll()` (multi-usuário)
  4. Evento pagamento/documento → `invalidateContrato(id)` ou `removeContrato(id)` (D-05)
  5. Virada do dia Goiânia → `dataHoje` no fingerprint muda → drift automático

- **D-09 (Data Goiânia)** — `Intl.DateTimeFormat('sv-SE', {timeZone:'America/Sao_Paulo'}).format(new Date())` retorna "YYYY-MM-DD" fuso correto.

- **D-10 (Display-only listagem)** — Não-clicável. Row continua navegando pro DetalheContrato. Modal decomposição segue no DetalheContrato (Phase 7.8). Modal-na-listagem deferido 7.8.2c.

- **D-11 (Kickoff eager)** — `kickoffBatch` no mount pra TODOS contratos em batches de 10. Razões: (a) alinha gatilho 1; (b) pré-requisito future ordenação 7.8.2b; (c) lazy criaria lista "pulando" (UX ruim). Custo 200-400ms diluído pelo idle callback.

- **D-12 (Guard StrictMode)** — `useEffect` usa `useRef` flag `kickedOff.current`. Deps: `[contratos.length, dividas.length, allPagamentosDivida.length]`. `.length` permite re-kickoff quando CRUD muda contagem (ex: 201º novo); filter interno pula já-cached. Edits sem mudar contagem cobertos pelo D-05 (dupla defesa).

- **D-13 (Invalidação híbrida)**:
  - Automática — hook compara fingerprint a cada render; drift → revalida
  - Explícita — 6 callers (D-05)
  - Global — `window.focus` listener registered once no module-load

- **D-14 (3 PAUSAs humanas)**:
  - PAUSA #1 (UAT SC1-SC8 local) → impede commit com bug comportamental
  - PAUSA #2 (diff consolidado pós-commit submódulo) → impede push submódulo errado
  - PAUSA #3 (bump review no pai) → impede bump com SHA errado
  - Proporcional aos 7 arquivos (superfície de risco real).

- **D-15 (Convenção commit)**:
  - Submódulo: `feat(07.8.2a): saldo atualizado na listagem com cache SWR`
  - Pai: `chore(07.8.2a): bump submodule mr-3 — saldo atualizado cache SWR`
  - Docs: `docs(07.8.2a): PLAN e feedback`
  - Tag pós-smoke prod: `v1.4-phase7.8.2a` (ação separada autorizada)

- **D-16 (SC6 pragmático)** — Testar com N máximo existente (~30) e extrapolar linearmente. Motor Art.354 é linear em N. Seed de 200 contaminaria DB real. Smoke em prod (mrcobrancas.com.br) = teste definitivo. Critério pass local: N contratos UI bloqueada < (200ms × N / 200). Gargalo prod → input pra 7.8.2b.

## 5. Success Criteria

1. `src/hooks/useSaldoAtualizadoCache.js` com 5 exports (useSaldoContrato, invalidateContrato, invalidateAll, removeContrato, kickoffBatch)
2. `src/components/CelulaSaldoAtualizado.jsx` com 3 estados + fade 180ms + display-only
3. `src/utils/batchCalculator.js` com `runBatches` (idle callback + fallback)
4. `src/services/__tests__/saldoAtualizadoCache.test.js` com 3 shields
5. `ModuloContratos.jsx` — coluna + botão + timestamp + kickoffBatch guardado
6. D-05 enforcement — 6 handlers (5 em DetalheContrato + 1 em AdicionarDocumento) chamam invalidate/remove
7. `npm run test:regressao` 14/14 PASS (11 preexistentes + 3 shields)
8. `npm run build` exit 0
9. UAT SC1-SC8 PASS em localhost
10. Motor Art.354 intocado — diff vazio em devedorCalc.js/pagamentos.js/dividas.js (D-01)
11. D-02 — hook/componentes novos não importam DetalheContrato/AdicionarDocumento (grep negativo)
12. Zero `git push` sem autorização
13. Tag `v1.4-phase7.8.2a` NÃO criada nesta phase

## 6. UAT cenários (8 SCs)

- **SC1** — Contrato A (sem pgtos) + B (com pgtos) valores independentes
- **SC2** — Pagar em A → linha A atualiza (D-05), B intocada
- **SC3** — Contratos → Dashboard → voltar → cache hit < 100ms
- **SC4** — Tab 5min + mudança em outro navegador + focus → revalida (gatilho 3)
- **SC5** — Virada meia-noite Goiânia + F5 → recalcula (gatilho 5)
- **SC6** — N máximo existente (~30 contratos) → UI bloqueada proporcionalmente < (200ms × N/200)
- **SC7** — Excluir contrato → removeContrato, linha some
- **SC8** — Botão "🔄 Atualizar" → re-dispara batches, timestamp atualiza

## 7. Shield grep D-05 — mapa completude callers

```bash
grep -nE "registrarPagamentoContrato|excluirPagamentoContrato|atualizarParcelasCustom|excluirContrato|excluirDocumento|adicionarDocumento" \
  src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx \
  src/mr-3/mr-cobrancas/src/components/AdicionarDocumento.jsx

# Awk range por handler → verificar invalidateContrato/removeContrato antes do toast.success.
# Report format obrigatório (reproduzir no SUMMARY):
# Handler                             | Service call                  | Invalidate presente
# handleRegistrarPagamento            | registrarPagamentoContrato    | YES
# handleExcluirPagamento              | excluirPagamentoContrato      | YES
# handleSalvarParcelasCustom          | atualizarParcelasCustom       | YES
# handleExcluirContrato               | excluirContrato               | YES (via removeContrato)
# handleExcluirDocumento              | excluirDocumento              | YES
# handleSalvar (AdicionarDocumento)   | adicionarDocumento            | YES
```

## 8. Task structure preliminar (planner refina)

- **7.8.2a-01** — Impl completa: 4 novos + 3 modificados. `autonomous: false`. Gates: regressão 14/14, build 0, motor diff vazio, D-05 grep, D-02 grep. Commit: `feat(07.8.2a): saldo atualizado na listagem com cache SWR`

- **7.8.2a-02** — UAT SC1-SC8 + bump + push autorizado. `autonomous: false`. 3 PAUSAs: UAT, diff consolidado, bump review. Commit pai: `chore(07.8.2a): bump submodule mr-3 — saldo atualizado cache SWR`. Tag NÃO criada.

**Nota splitting**: escopo grande (7 arquivos) MAS unificado tematicamente (cache SWR + callers D-05). Split criaria janela de bug D-05 em prod entre plans. Lição 10 inversa — unificado aqui faz sentido.

## 9. Investigação pré-discuss — achados

### Adapter da 7.8 pós-fix 7.8.1-02
- `src/utils/devedorCalc.js:849` — `calcularDetalheEncargosContrato(dividasDoContrato, allPagamentosDivida, hoje)`
- Filtra internamente por `divida_id` (D-04)
- Retorna shape completo com `saldoAtualizado`

### Precedente filtragem
- `calcularTotaisContratoNominal` em `contratos.js:476-486` (Phase 7.3)

### Services reais (grep confirmado)
- `registrarPagamentoContrato`, `excluirPagamentoContrato`, `atualizarParcelasCustom`, `excluirContrato`, `excluirDocumento` (7.7), `adicionarDocumento` — todos em contratos.js

### Regression tests
- `src/services/__tests__/calculos.test.js` — 11 testes atuais (10 TJGO + 1 shield 7.8.1)
- Após 7.8.2a: 14 testes (+3 shields)

## 10. Notas de implementação

- **Fingerprint pode virar gargalo em baixa**: 200 contratos × 10 parcelas × 12 campos = ~24k ops por render. Se UAT SC6 detectar, otimização shallow-primeiro vira escopo 7.8.2b.
- **kickoffBatch guard StrictMode**: `useRef` flag + deps `.length` evita double-kickoff em dev. Prod: single-shot.
- **window.focus one-shot**: listener no module-load vive pela sessão. SSR/teste Node protegido por `typeof window`.

## 11. Backlog pós-7.8.2a

- **Phase 7.8.2b** — Ordenação por saldo + virtualização (depende de 7.8.2a)
- **Phase 7.8.2c** — Modal na listagem (se pedido)
- Phase 7.9 — Custas Judiciais (herda D-05)
- Phase 7.10 — Tech debt pagamentos_parciais
- Phase 7.11 — React Router
- Phase 7.13 — Múltiplos devedores (herda D-05)
- Phase 7.15 — Reconstruir Fila
- Phase 8 — PDF Demonstrativo (fecha v1.4)
