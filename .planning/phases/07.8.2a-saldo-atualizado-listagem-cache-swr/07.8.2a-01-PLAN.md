---
phase: 07.8.2a-saldo-atualizado-listagem-cache-swr
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: false
requirements_addressed: []
source: "ROADMAP.md line 324 + 07.8.2a-CONTEXT.md (goal-driven, UX-discovered post-7.8.1; phase has no REQ-IDs)"

files_modified:
  - src/mr-3/mr-cobrancas/src/hooks/useSaldoAtualizadoCache.js               # new (Fix A)
  - src/mr-3/mr-cobrancas/src/components/CelulaSaldoAtualizado.jsx           # new (Fix B)
  - src/mr-3/mr-cobrancas/src/utils/batchCalculator.js                       # new (Fix C)
  - src/mr-3/mr-cobrancas/src/components/ModuloContratos.jsx                 # modified (Fix D)
  - src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx                 # modified (Fix E — 5 handlers D-05)
  - src/mr-3/mr-cobrancas/src/components/AdicionarDocumento.jsx              # modified (Fix E — 1 handler D-05)
  - src/mr-3/mr-cobrancas/src/services/__tests__/saldoAtualizadoCache.test.js # new (Fix F — 3 shields)
  - src/mr-3/mr-cobrancas/package.json                                        # modified (test:regressao script update)

must_haves:
  truths:
    - "Listagem de contratos (ModuloContratos.jsx) exibe coluna 'Saldo Atualizado' com valor Art.354 CC por linha"
    - "Cache SWR serve render instantâneo ao retornar pra listagem (hit < 100ms — SC3)"
    - "Mutação em pagamento/documento invalida o cache do contrato afetado antes do toast.success (D-05)"
    - "Exclusão de contrato remove a entrada do cache (removeContrato — SC7)"
    - "Listagem do contrato A não se altera quando cache do B muda (shield cross-entity — herda 7.8.1)"
    - "Motor Art.354 (devedorCalc.js, pagamentos.js, dividas.js) permanece intocado — diff vazio"
    - "Hook e componentes novos não importam DetalheContrato.jsx nem AdicionarDocumento.jsx (D-02)"
    - "Fingerprint deep (12 campos D-06) captura virada do dia Goiânia via Intl.DateTimeFormat America/Sao_Paulo"
    - "Botão '🔄 Atualizar' re-dispara batches e atualiza timestamp (SC8)"
    - "14/14 testes regressão passam (11 preexistentes + 3 shields novos)"
    - "Hook expõe 7 exports (5 API principal + 2 helpers de observabilidade SC5 — subscribeToCache, getLastCalculadoEm)"
  artifacts:
    - path: "src/mr-3/mr-cobrancas/src/hooks/useSaldoAtualizadoCache.js"
      provides: "Cache singleton + hook SWR + 7 exports (5 API + 2 observability helpers)"
      exports: ["useSaldoContrato", "invalidateContrato", "invalidateAll", "removeContrato", "kickoffBatch", "subscribeToCache", "getLastCalculadoEm"]
    - path: "src/mr-3/mr-cobrancas/src/components/CelulaSaldoAtualizado.jsx"
      provides: "Célula display-only com 3 estados visuais"
      contains: "skeleton|fresh|revalidating"
    - path: "src/mr-3/mr-cobrancas/src/utils/batchCalculator.js"
      provides: "runBatches com requestIdleCallback + fallback setTimeout(0)"
      exports: ["runBatches"]
    - path: "src/mr-3/mr-cobrancas/src/services/__tests__/saldoAtualizadoCache.test.js"
      provides: "3 shields (cross-entity, virada dia, invalidate isolado)"
      contains: "describe.*saldoAtualizadoCache"
  key_links:
    - from: "src/mr-3/mr-cobrancas/src/components/ModuloContratos.jsx"
      to: "src/mr-3/mr-cobrancas/src/hooks/useSaldoAtualizadoCache.js"
      via: "kickoffBatch on mount + invalidateAll no botão + subscribe no timestamp"
      pattern: "kickoffBatch|invalidateAll"
    - from: "src/mr-3/mr-cobrancas/src/components/CelulaSaldoAtualizado.jsx"
      to: "src/mr-3/mr-cobrancas/src/hooks/useSaldoAtualizadoCache.js"
      via: "useSaldoContrato(contrato, dividas, pagamentos, hoje)"
      pattern: "useSaldoContrato"
    - from: "src/mr-3/mr-cobrancas/src/hooks/useSaldoAtualizadoCache.js"
      to: "src/mr-3/mr-cobrancas/src/utils/devedorCalc.js"
      via: "calcularDetalheEncargosContrato (apenas import — zero mudança no motor)"
      pattern: "calcularDetalheEncargosContrato"
    - from: "src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx"
      to: "src/mr-3/mr-cobrancas/src/hooks/useSaldoAtualizadoCache.js"
      via: "invalidateContrato(contrato.id) antes de toast.success em 4 handlers + removeContrato em 1 handler"
      pattern: "invalidateContrato|removeContrato"
    - from: "src/mr-3/mr-cobrancas/src/components/AdicionarDocumento.jsx"
      to: "src/mr-3/mr-cobrancas/src/hooks/useSaldoAtualizadoCache.js"
      via: "invalidateContrato(contrato.id) após sucesso de adicionarDocumento"
      pattern: "invalidateContrato"
---

<objective>
Implementar cache SWR caseiro (stale-while-revalidate) que traz a coluna "Saldo Atualizado" (Art.354 CC) pra listagem de contratos em `ModuloContratos.jsx`, mantendo motor Art.354 intocado (D-01). Entregar 4 arquivos novos (hook, célula, batchCalculator, shields de teste), 3 arquivos modificados (ModuloContratos, DetalheContrato, AdicionarDocumento) e atualizar o script `test:regressao` no `package.json` do submódulo para incluir o novo arquivo de shields.

Purpose: Advogado com 200 contratos precisa ver saldo atualizado na visão agregada sem clicar cada um. Padrão SWR (Stripe/Shopify/Vercel) dá render instantâneo ao voltar pra tela e revalida em background. D-05 enforcement (callers completude) blinda o silent-stale bug class — todo mutador (pagamento, documento, parcelas custom, exclusão) chama `invalidateContrato`/`removeContrato` ANTES do `toast.success`/`setState`.

Output:
- `src/mr-3/mr-cobrancas/src/hooks/useSaldoAtualizadoCache.js` (novo)
- `src/mr-3/mr-cobrancas/src/components/CelulaSaldoAtualizado.jsx` (novo)
- `src/mr-3/mr-cobrancas/src/utils/batchCalculator.js` (novo)
- `src/mr-3/mr-cobrancas/src/services/__tests__/saldoAtualizadoCache.test.js` (novo — 3 shields)
- `src/mr-3/mr-cobrancas/src/components/ModuloContratos.jsx` (modificado — coluna + botão + timestamp + kickoffBatch)
- `src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx` (modificado — 5 handlers D-05)
- `src/mr-3/mr-cobrancas/src/components/AdicionarDocumento.jsx` (modificado — 1 handler D-05)
- `src/mr-3/mr-cobrancas/package.json` (modificado — script test:regressao)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07.8.2a-saldo-atualizado-listagem-cache-swr/07.8.2a-CONTEXT.md

<!-- Adapter existente (intocado — apenas importado) -->
@src/mr-3/mr-cobrancas/src/utils/devedorCalc.js

<!-- Arquivos modificados por este plano -->
@src/mr-3/mr-cobrancas/src/components/ModuloContratos.jsx
@src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx
@src/mr-3/mr-cobrancas/src/components/AdicionarDocumento.jsx

<!-- Analogs pra padrões -->
@src/mr-3/mr-cobrancas/src/utils/correcao.js
@src/mr-3/mr-cobrancas/src/services/__tests__/calculos.test.js
@src/mr-3/mr-cobrancas/package.json

<interfaces>
<!-- Adapter EXISTENTE em devedorCalc.js (D-01 intocado — só é importado): -->

```javascript
// src/mr-3/mr-cobrancas/src/utils/devedorCalc.js:849
// SHAPE de retorno:
// {
//   saldoAtualizado: number,
//   valorOriginal: number,
//   totalPago: number,
//   correcao: number,
//   juros: number,
//   multa: number,
//   honorarios: number,
//   ...
// }
// IMPORTANTE: o adapter JÁ FILTRA allPagamentosDivida internamente por divida_id (shield 7.8.1-02, D-04).
// O hook passa allPagamentosDivida GLOBAL sem pré-filter.
export function calcularDetalheEncargosContrato(
  dividasDoContrato,    // Array<Divida> (apenas as do contrato alvo)
  allPagamentosDivida,  // Array<PagamentoDivida> (global — adapter filtra internamente)
  hoje                  // Date
): DetalheContratoShape;
```

<!-- Interface DO HOOK NOVO (7 exports — 5 API principal + 2 helpers observabilidade — Fix A): -->

```javascript
// src/mr-3/mr-cobrancas/src/hooks/useSaldoAtualizadoCache.js (NOVO — Fix A)

// Cache shape (singleton Map):
// Map<contratoId, {
//   saldo: number,
//   detalhe: DetalheContratoShape,
//   fingerprint: string,
//   status: "loading" | "fresh" | "revalidating",
//   calculadoEm: number  // Date.now()
// }>

// === API pública PRINCIPAL (5 exports) ===

// 1. Hook React — consumido por CelulaSaldoAtualizado + (via subscribe) por ModuloContratos pro timestamp
export function useSaldoContrato(
  contrato,           // { id, ... }
  dividasDoContrato,  // Array<Divida> (já filtrada pelo chamador — contratos.js já dá dividas_agrupadas)
  allPagamentosDivida, // Array<PagamentoDivida> (global — adapter filtra internamente — D-04)
  hoje                // string "YYYY-MM-DD" Goiânia (D-09)
): { saldo: number | null, detalhe: object | null, status: "loading" | "fresh" | "revalidating", calculadoEm: number | null };

// 2-4. API imperativa (chamada por callers D-05 + botão header D-08):
export function invalidateContrato(contratoId): void;   // marca entrada como stale ("revalidating")
export function invalidateAll(): void;                   // marca todas entradas como stale
export function removeContrato(contratoId): void;        // remove entrada do Map (pro delete de contrato)

// 5. Batch kickoff eager (D-11):
export function kickoffBatch(
  contratos,           // Array<Contrato>
  dividasAgrupadas,    // Map ou obj contratoId → Array<Divida>
  allPagamentosDivida, // global
  hoje                 // string Goiânia
): void;

// === Helpers de OBSERVABILIDADE (2 exports — SC5 timestamp reativo) ===

// 6. Subscribe pub/sub — permite ModuloContratos re-renderizar quando cache muda (SC5):
export function subscribeToCache(listener: () => void): () => void; // retorna unsubscribe

// 7. Leitura do último calculadoEm máximo — usado pelo timestamp "Atualizado às HH:MM":
export function getLastCalculadoEm(): number; // 0 se cache vazio

// --- Internals obrigatórios (verificar via grep) ---
// function contratoFingerprint(dividasDoContrato, pagamentosFiltered, dataHoje): string
//   (bloco D-06 VERBATIM — 12 campos; ver <action> da Tarefa 1)
//
// window.focus listener registrado no module-load, guarded por `typeof window !== 'undefined'`
//   chama invalidateAll() quando janela recebe foco
//
// pub/sub:
//   const listeners = new Set<() => void>();
//   função notify(): listeners.forEach(l => l());
//   useSaldoContrato: useSyncExternalStore ou useState + useEffect+subscribe retornando unsubscribe
```

<!-- Interface DE CelulaSaldoAtualizado.jsx (Fix B — display-only, D-10): -->

```jsx
// src/mr-3/mr-cobrancas/src/components/CelulaSaldoAtualizado.jsx (NOVO — Fix B)

// Props:
//   contrato            : objeto do contrato (com id)
//   dividasDoContrato   : Array<Divida> já filtrada
//   allPagamentosDivida : Array<PagamentoDivida> global
//   hoje                : string "YYYY-MM-DD" Goiânia

// 3 estados visuais (fade 180ms via CSS inline transition: "opacity 180ms"):
//   status === "loading"      → skeleton dots "…" ou três pontos animados, opacity 0.5
//   status === "fresh"        → valor formatado via fmtBRL (existente), opacity 1
//   status === "revalidating" → valor formatado via fmtBRL, opacity 0.75, title="Atualizando…"

// D-10: DISPLAY-ONLY. NÃO tem onClick, NÃO é <button>, NÃO é <a>. Row inteira (tr) continua
//       sendo o elemento navegável pro DetalheContrato (handler já existe em ModuloContratos).
//       Use <td> simples ou <span>.
```

<!-- Interface DE batchCalculator.js (Fix C): -->

```javascript
// src/mr-3/mr-cobrancas/src/utils/batchCalculator.js (NOVO — Fix C)

export function runBatches(items, batchSize, processBatch) {
  // Fatiar items em chunks de batchSize.
  // Agendar cada chunk via requestIdleCallback(cb, { timeout: 500 }) se disponível,
  //   senão setTimeout(cb, 0) como fallback.
  // processBatch(chunk) é chamado com cada chunk.
  // Retorna void (fire-and-forget).
}
```

<!-- Handlers D-05 que serão modificados (grep-verificado em CONTEXT.md Section 9): -->

```
DetalheContrato.jsx:
  handleRegistrarPagamento    → após await registrarPagamentoContrato(...) + toast.success → invalidateContrato(contrato.id)
  handleExcluirPagamento      → após await excluirPagamentoContrato(...) + toast.success → invalidateContrato(contrato.id)
  handleSalvarParcelasCustom  → após await atualizarParcelasCustom(...) + toast.success → invalidateContrato(contrato.id)
  handleExcluirDocumento      → após await excluirDocumento(...) + toast.success → invalidateContrato(contrato.id)
  handleExcluirContrato       → após await excluirContrato(...) + toast.success → removeContrato(contrato.id)

AdicionarDocumento.jsx:
  handleSalvar                → após await adicionarDocumento(...) + sucesso → invalidateContrato(contrato.id)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar hook useSaldoAtualizadoCache com 7 exports (5 API + 2 helpers observabilidade) + fingerprint D-06 verbatim + window.focus + 3 estados</name>
  <read_first>
    - `.planning/phases/07.8.2a-saldo-atualizado-listagem-cache-swr/07.8.2a-CONTEXT.md` (inteiro — D-01..D-16 são fonte da verdade; §5 SC1 lista os 7 exports)
    - `src/mr-3/mr-cobrancas/src/utils/devedorCalc.js` (confirmar assinatura de `calcularDetalheEncargosContrato` em linha ~849; NÃO modificar)
    - `src/mr-3/mr-cobrancas/src/utils/correcao.js` (analog pra estilo de utils puros no projeto)
  </read_first>
  <files>src/mr-3/mr-cobrancas/src/hooks/useSaldoAtualizadoCache.js (NOVO — diretório `src/hooks/` ainda não existe, criar)</files>
  <action>
    Criar `src/mr-3/mr-cobrancas/src/hooks/useSaldoAtualizadoCache.js` com a estrutura abaixo.

    **Comentário obrigatório no topo do arquivo (grep-verificável — SC1):**
    ```javascript
    // 5 exports da API principal + 2 helpers de observabilidade (SC5 timestamp reativo)
    ```

    **Imports:**
    ```javascript
    import { useEffect, useState } from "react";
    import { calcularDetalheEncargosContrato } from "../utils/devedorCalc.js";
    import { runBatches } from "../utils/batchCalculator.js";
    ```

    **Singletons module-level:**
    ```javascript
    // Map<contratoId, { saldo, detalhe, fingerprint, status, calculadoEm }>
    const cache = new Map();
    // Set<() => void>
    const listeners = new Set();
    function notify() { listeners.forEach(l => l()); }
    ```

    **Fingerprint VERBATIM (D-06 — NÃO ALTERAR uma vírgula, ordem, nem campos):**
    ```javascript
    function contratoFingerprint(dividasDoContrato, pagamentosFiltered, dataHoje) {
      const D = dividasDoContrato.map(d => [
        d.id, d.valor_total, d.data_vencimento, d.data_origem,
        d.data_inicio_atualizacao, d.indexador || "", d.multa_pct || 0,
        d.juros_tipo || "", d.juros_am || 0, d.honorarios_pct || 0,
        d.art523_opcao || "", d.saldo_quitado ? 1 : 0,
      ].join(":")).sort().join("|");
      const P = pagamentosFiltered.map(p =>
        [p.id, p.divida_id, p.valor, p.data_pagamento].join(":")
      ).sort().join("|");
      return `${dataHoje}|D:${D}|P:${P}`;
    }
    ```
    *Nota*: `pagamentosFiltered` aqui significa pagamentos já filtrados por contrato (i.e., cujos `divida_id` pertencem a `dividasDoContrato.map(d=>d.id)`). O hook faz esse filter ANTES de chamar `contratoFingerprint`. O adapter downstream refaz seu próprio filter (D-04), então a passagem pro adapter mantém `allPagamentosDivida` global.

    **IMPORTANTE — 12 campos D-06 VERBATIM (os 4 greps consolidados de `<acceptance_criteria>` dependem disso):** a linha da dívida no fingerprint deve conter as SUBSTRINGS CONTÍGUAS abaixo, exatamente como aparecem no bloco acima:
    - `d.id, d.valor_total, d.data_vencimento, d.data_origem,`
    - `d.data_inicio_atualizacao, d.indexador`  ← contíguo
    - `d.multa_pct || 0, d.juros_tipo`           ← contíguo
    - `d.juros_am || 0, d.honorarios_pct`        ← contíguo
    - `d.art523_opcao || "", d.saldo_quitado`    ← contíguo

    Se qualquer um dos 12 campos for deletado/renomeado/reordenado, ao menos uma das 4 greps consolidadas falha — e isso deve falhar.

    **Helper dataHoje Goiânia (D-09) — usado por `kickoffBatch` e como default quando caller não passa `hoje`:**
    ```javascript
    function hojeGoiania() {
      return new Intl.DateTimeFormat('sv-SE', { timeZone: 'America/Sao_Paulo' }).format(new Date());
    }
    ```

    **Função interna de recompute (pura — chamada por `useSaldoContrato` e `kickoffBatch`):**
    ```javascript
    function recomputeEntry(contratoId, dividasDoContrato, allPagamentosDivida, hoje) {
      const dividaIds = new Set(dividasDoContrato.map(d => d.id));
      const pagamentosFiltered = allPagamentosDivida.filter(p => dividaIds.has(p.divida_id));
      const fp = contratoFingerprint(dividasDoContrato, pagamentosFiltered, hoje);
      // Adapter recebe pagamentos GLOBAL (D-04 — filtra internamente) e Date real (não string).
      const detalhe = calcularDetalheEncargosContrato(dividasDoContrato, allPagamentosDivida, new Date());
      cache.set(contratoId, {
        saldo: detalhe?.saldoAtualizado ?? 0,
        detalhe,
        fingerprint: fp,
        status: "fresh",
        calculadoEm: Date.now(),
      });
      notify();
    }
    ```

    **API pública — 7 exports (5 API principal + 2 helpers observabilidade — NOMES EXATOS — grep-verificáveis):**

    1. `export function useSaldoContrato(contrato, dividasDoContrato, allPagamentosDivida, hoje)` — usa `useState` para forçar re-render + `useEffect` para subscribe em `listeners`. Comportamento:
       - Computa `fp` atual via `contratoFingerprint`.
       - Se `cache` NÃO tem entrada pra `contrato.id` → cria entrada `status: "loading"`, dispara `recomputeEntry` (pode ser síncrono ou agendado via `setTimeout(0)` pra não bloquear render), retorna `{ saldo: null, detalhe: null, status: "loading", calculadoEm: null }`.
       - Se entrada existe e `entry.fingerprint === fp` → retorna `{ saldo, detalhe, status: "fresh", calculadoEm }`.
       - Se entrada existe mas `entry.fingerprint !== fp` OU `entry.status === "revalidating"` → agenda `recomputeEntry` (setTimeout(0)), retorna imediatamente a entrada atual com `status: "revalidating"` (serve stale, revalida em background).
       - Subscribe: `useEffect(() => { const fn = () => force(); listeners.add(fn); return () => listeners.delete(fn); }, [])`.

    2. `export function invalidateContrato(contratoId)` — se `cache.has(contratoId)`: set `entry.status = "revalidating"` + `notify()`. Não recomputa sozinho — próximo render do hook detecta e agenda.

    3. `export function invalidateAll()` — percorre `cache.entries()` setando `status: "revalidating"` + `notify()` uma única vez no fim.

    4. `export function removeContrato(contratoId)` — `cache.delete(contratoId) + notify()`.

    5. `export function kickoffBatch(contratos, dividasAgrupadas, allPagamentosDivida, hoje)` — `hoje` default `hojeGoiania()`. Usa `runBatches(contratos, 10, (chunk) => chunk.forEach(c => { const dv = Array.isArray(dividasAgrupadas) ? dividasAgrupadas.filter(d => d.contrato_id === c.id) : (dividasAgrupadas.get?.(c.id) ?? dividasAgrupadas[c.id] ?? []); if (!cache.has(c.id) || cache.get(c.id).status === "revalidating") recomputeEntry(c.id, dv, allPagamentosDivida, hoje); }))`. *Shape de `dividasAgrupadas`*: em `ModuloContratos.jsx`, verifique se o componente já tem um array flat de `dividas` (filtra por `contrato_id`) ou um Map — adapte o acesso. Se necessário, exporte também um helper `dividasDoContratoFromFlat(allDividas, contratoId)`.

    6. `export function subscribeToCache(listener)` — `listeners.add(listener); return () => listeners.delete(listener);`. Helper de observabilidade (SC5) consumido por `ModuloContratos.jsx` pra re-renderizar o timestamp reativamente quando qualquer entry do cache muda. Não pertence à API SWR principal — é suporte UI (grep-verificável).

    7. `export function getLastCalculadoEm()` — `return Math.max(0, ...Array.from(cache.values()).map(e => e.calculadoEm || 0));`. Helper de leitura síncrona — retorna o maior `calculadoEm` presente no cache (ou 0 se vazio). Usado pelo header de `ModuloContratos.jsx` pra montar o label "Atualizado às HH:MM" (grep-verificável).

    **window.focus listener no module-load (D-08 gatilho 3):**
    ```javascript
    if (typeof window !== 'undefined') {
      window.addEventListener('focus', () => invalidateAll());
    }
    ```
    Registrar UMA vez no module-load — NÃO dentro de `useEffect`.

    **Estados retornados pelo hook (STRINGS EXATAS — grep-verificáveis):** `"loading"`, `"fresh"`, `"revalidating"`. Sem outros valores.

    **Guardrails D-01 / D-02 (por construção):**
    - ZERO edição em `src/utils/devedorCalc.js`, `src/services/pagamentos.js`, `src/services/dividas.js`.
    - ZERO `import ... from '../components/DetalheContrato'` ou `.../AdicionarDocumento'` neste arquivo.

    **Expose `cache` e `listeners` para testes (opcional mas recomendado):** adicionar export nomeado de teste `export const __test = { cache, listeners, recomputeEntry, contratoFingerprint, hojeGoiania }` (apenas pra `saldoAtualizadoCache.test.js`).
  </action>
  <verify>
    <automated>cd src/mr-3/mr-cobrancas &amp;&amp; node -e "const m = require('./src/hooks/useSaldoAtualizadoCache.js'); ['useSaldoContrato','invalidateContrato','invalidateAll','removeContrato','kickoffBatch','subscribeToCache','getLastCalculadoEm'].forEach(n=>{ if (typeof m[n] !== 'function') { console.error('MISSING:', n); process.exit(1); } }); console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function useSaldoContrato" src/mr-3/mr-cobrancas/src/hooks/useSaldoAtualizadoCache.js` returns `1`
    - `grep -c "export function invalidateContrato" src/mr-3/mr-cobrancas/src/hooks/useSaldoAtualizadoCache.js` returns `1`
    - `grep -c "export function invalidateAll" src/mr-3/mr-cobrancas/src/hooks/useSaldoAtualizadoCache.js` returns `1`
    - `grep -c "export function removeContrato" src/mr-3/mr-cobrancas/src/hooks/useSaldoAtualizadoCache.js` returns `1`
    - `grep -c "export function kickoffBatch" src/mr-3/mr-cobrancas/src/hooks/useSaldoAtualizadoCache.js` returns `1`
    - `grep -c "export function subscribeToCache" src/mr-3/mr-cobrancas/src/hooks/useSaldoAtualizadoCache.js` returns `1` (helper observabilidade SC5)
    - `grep -c "export function getLastCalculadoEm" src/mr-3/mr-cobrancas/src/hooks/useSaldoAtualizadoCache.js` returns `1` (helper observabilidade SC5)
    - **Fingerprint D-06 grep-completo (4 substring greps cobrindo 12 campos contíguos — deletar qualquer campo quebra ao menos um grep):**
      - `grep -F "d.data_inicio_atualizacao, d.indexador" src/mr-3/mr-cobrancas/src/hooks/useSaldoAtualizadoCache.js` exits 0
      - `grep -F "d.multa_pct || 0, d.juros_tipo" src/mr-3/mr-cobrancas/src/hooks/useSaldoAtualizadoCache.js` exits 0
      - `grep -F "d.juros_am || 0, d.honorarios_pct" src/mr-3/mr-cobrancas/src/hooks/useSaldoAtualizadoCache.js` exits 0
      - `grep -F "d.art523_opcao || \"\", d.saldo_quitado" src/mr-3/mr-cobrancas/src/hooks/useSaldoAtualizadoCache.js` exits 0
      - (8 fields verified via substrings: `data_inicio_atualizacao, indexador, multa_pct, juros_tipo, juros_am, honorarios_pct, art523_opcao, saldo_quitado`; plus `d.id`, `d.valor_total`, `d.data_vencimento`, `d.data_origem` which sit contiguous with `d.data_inicio_atualizacao` on the preceding line in the D-06 block — if any of those 4 is deleted, the following line's first substring grep also breaks.)
    - **Comentário 7-exports obrigatório no arquivo (SC1):** `grep -E "5 exports.*API.*2 helpers.*observabilidade" src/mr-3/mr-cobrancas/src/hooks/useSaldoAtualizadoCache.js` exits 0
    - `grep -c "America/Sao_Paulo" src/mr-3/mr-cobrancas/src/hooks/useSaldoAtualizadoCache.js` returns `>= 1` (D-09)
    - `grep -c "sv-SE" src/mr-3/mr-cobrancas/src/hooks/useSaldoAtualizadoCache.js` returns `>= 1` (D-09)
    - `grep -cE "window\.addEventListener\\(.focus." src/mr-3/mr-cobrancas/src/hooks/useSaldoAtualizadoCache.js` returns `>= 1`
    - `grep -c "typeof window" src/mr-3/mr-cobrancas/src/hooks/useSaldoAtualizadoCache.js` returns `>= 1` (SSR guard)
    - `grep -c '"loading"' src/mr-3/mr-cobrancas/src/hooks/useSaldoAtualizadoCache.js` returns `>= 1`
    - `grep -c '"fresh"' src/mr-3/mr-cobrancas/src/hooks/useSaldoAtualizadoCache.js` returns `>= 1`
    - `grep -c '"revalidating"' src/mr-3/mr-cobrancas/src/hooks/useSaldoAtualizadoCache.js` returns `>= 1`
    - D-02 isolation: `grep -nE "import.*DetalheContrato|import.*AdicionarDocumento" src/mr-3/mr-cobrancas/src/hooks/useSaldoAtualizadoCache.js` returns ZERO matches
  </acceptance_criteria>
  <done>
    Hook criado com 7 exports (5 API + 2 helpers observabilidade), comentário SC1 presente, fingerprint D-06 verbatim (4 greps consolidados passam), window.focus guardado, 3 estados literais, zero imports de componentes DetalheContrato/AdicionarDocumento, require/import carrega sem erro.
  </done>
</task>

<task type="auto">
  <name>Task 2: Criar batchCalculator.js com runBatches (requestIdleCallback + fallback setTimeout + timeout 500ms)</name>
  <read_first>
    - `.planning/phases/07.8.2a-saldo-atualizado-listagem-cache-swr/07.8.2a-CONTEXT.md` (Fix C + D-11)
    - `src/mr-3/mr-cobrancas/src/utils/correcao.js` (analog pra estilo de util puro)
  </read_first>
  <files>src/mr-3/mr-cobrancas/src/utils/batchCalculator.js (NOVO)</files>
  <action>
    Criar `src/mr-3/mr-cobrancas/src/utils/batchCalculator.js`:

    ```javascript
    // src/mr-3/mr-cobrancas/src/utils/batchCalculator.js
    // Agenda processamento de items em chunks de batchSize usando requestIdleCallback
    // quando disponível, com fallback setTimeout(cb, 0). Timeout idle = 500ms.
    // Fire-and-forget: não retorna Promise.

    export function runBatches(items, batchSize, processBatch) {
      if (!Array.isArray(items) || items.length === 0) return;
      const size = Math.max(1, batchSize | 0);
      const chunks = [];
      for (let i = 0; i < items.length; i += size) {
        chunks.push(items.slice(i, i + size));
      }
      const schedule = (typeof window !== 'undefined' && typeof window.requestIdleCallback === 'function')
        ? (cb) => window.requestIdleCallback(cb, { timeout: 500 })
        : (cb) => setTimeout(cb, 0);

      const step = () => {
        const chunk = chunks.shift();
        if (!chunk) return;
        try { processBatch(chunk); } catch (e) { console.error('[runBatches] processBatch threw:', e); }
        if (chunks.length > 0) schedule(step);
      };
      schedule(step);
    }
    ```

    Zero state module-level, zero imports de componentes. Função pura agendadora.
  </action>
  <verify>
    <automated>cd src/mr-3/mr-cobrancas &amp;&amp; node -e "const { runBatches } = require('./src/utils/batchCalculator.js'); if (typeof runBatches !== 'function') process.exit(1); let called = 0; runBatches([1,2,3,4,5], 2, (c) => { called += c.length; }); setTimeout(() => { if (called !== 5) { console.error('called=', called); process.exit(1); } console.log('OK'); }, 100);"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function runBatches" src/mr-3/mr-cobrancas/src/utils/batchCalculator.js` returns `1`
    - `grep -c "requestIdleCallback" src/mr-3/mr-cobrancas/src/utils/batchCalculator.js` returns `>= 1`
    - `grep -c "setTimeout" src/mr-3/mr-cobrancas/src/utils/batchCalculator.js` returns `>= 1` (fallback)
    - `grep -c "timeout: 500" src/mr-3/mr-cobrancas/src/utils/batchCalculator.js` returns `>= 1`
    - Zero imports de `DetalheContrato` ou `AdicionarDocumento`: `grep -nE "import.*DetalheContrato|import.*AdicionarDocumento" src/mr-3/mr-cobrancas/src/utils/batchCalculator.js` returns ZERO
  </acceptance_criteria>
  <done>
    runBatches agenda chunks de 10 via idle callback com fallback setTimeout; smoke via Node confirma que todos items são processados.
  </done>
</task>

<task type="auto">
  <name>Task 3: Criar CelulaSaldoAtualizado.jsx com 3 estados visuais + fade 180ms + display-only (D-10)</name>
  <read_first>
    - `.planning/phases/07.8.2a-saldo-atualizado-listagem-cache-swr/07.8.2a-CONTEXT.md` (Fix B + D-10)
    - `src/mr-3/mr-cobrancas/src/hooks/useSaldoAtualizadoCache.js` (criado na Task 1 — consumir `useSaldoContrato`)
    - Analog pra estilo JSX: `src/mr-3/mr-cobrancas/src/components/ModuloContratos.jsx` (verificar helper `fmtBRL` e padrão de células)
  </read_first>
  <files>src/mr-3/mr-cobrancas/src/components/CelulaSaldoAtualizado.jsx (NOVO)</files>
  <action>
    Criar `src/mr-3/mr-cobrancas/src/components/CelulaSaldoAtualizado.jsx` como componente **display-only** (D-10):

    ```jsx
    import React from "react";
    import { useSaldoContrato } from "../hooks/useSaldoAtualizadoCache.js";

    // Formatador local BRL (se fmtBRL existe em utils compartilhados, preferir import; fallback abaixo):
    function fmtBRL(v) {
      if (v == null || !isFinite(v)) return "—";
      return v.toLocaleString("pt-BR", { style: "currency", currency: "BRL" });
    }

    export default function CelulaSaldoAtualizado({ contrato, dividasDoContrato, allPagamentosDivida, hoje }) {
      const { saldo, status } = useSaldoContrato(contrato, dividasDoContrato, allPagamentosDivida, hoje);

      // D-10: NÃO clicável — <span> sem onClick; row inteira continua sendo a área de navegação.
      const baseStyle = { transition: "opacity 180ms", display: "inline-block" };

      if (status === "loading") {
        return (
          <span style={{ ...baseStyle, opacity: 0.5 }} aria-label="Carregando saldo atualizado">
            <span>…</span>
          </span>
        );
      }
      if (status === "revalidating") {
        return (
          <span style={{ ...baseStyle, opacity: 0.75 }} title="Atualizando…">
            {fmtBRL(saldo)}
          </span>
        );
      }
      // "fresh"
      return (
        <span style={{ ...baseStyle, opacity: 1 }}>
          {fmtBRL(saldo)}
        </span>
      );
    }
    ```

    **D-10 enforcement**: NÃO adicionar `onClick`, `onKeyDown`, `role="button"`, `<a>`, `<button>`, nem `cursor: pointer`. A row (`<tr>` em `ModuloContratos.jsx`) já tem o handler de navegação — esta célula é puramente display.

    **Se `fmtBRL` já existe compartilhado** (ex: em `src/utils/format.js`), importar em vez do fallback local.
  </action>
  <verify>
    <automated>cd src/mr-3/mr-cobrancas &amp;&amp; npm run build 2>&amp;1 | tee /tmp/07.8.2a-01-t3-build.log | tail -40 &amp;&amp; grep -qE "built in|✓" /tmp/07.8.2a-01-t3-build.log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export default function CelulaSaldoAtualizado" src/mr-3/mr-cobrancas/src/components/CelulaSaldoAtualizado.jsx` returns `1`
    - `grep -c "useSaldoContrato" src/mr-3/mr-cobrancas/src/components/CelulaSaldoAtualizado.jsx` returns `>= 1`
    - `grep -c "180ms" src/mr-3/mr-cobrancas/src/components/CelulaSaldoAtualizado.jsx` returns `>= 1`
    - `grep -c "0.75" src/mr-3/mr-cobrancas/src/components/CelulaSaldoAtualizado.jsx` returns `>= 1`
    - D-10 display-only: `grep -cE "onClick|onKeyDown|role=.button.|cursor:.pointer" src/mr-3/mr-cobrancas/src/components/CelulaSaldoAtualizado.jsx` returns `0`
    - D-02 isolation: `grep -nE "import.*DetalheContrato|import.*AdicionarDocumento" src/mr-3/mr-cobrancas/src/components/CelulaSaldoAtualizado.jsx` returns ZERO
    - `cd src/mr-3/mr-cobrancas && npm run build` exits 0
  </acceptance_criteria>
  <done>
    Componente display-only compila; 3 estados visuais implementados; build passes exit 0.
  </done>
</task>

<task type="auto">
  <name>Task 4: Modificar ModuloContratos.jsx — coluna Saldo Atualizado + botão 🔄 Atualizar + timestamp + kickoffBatch guardado</name>
  <read_first>
    - `.planning/phases/07.8.2a-saldo-atualizado-listagem-cache-swr/07.8.2a-CONTEXT.md` (Fix D + D-11 + D-12 + §5 SC1 locked em 7 exports)
    - `src/mr-3/mr-cobrancas/src/components/ModuloContratos.jsx` (inteiro — entender estrutura atual da tabela, imports, props, shape de `contratos`/`dividas`/`allPagamentosDivida`)
    - `src/mr-3/mr-cobrancas/src/hooks/useSaldoAtualizadoCache.js` (Task 1 — consumir `invalidateAll`, `kickoffBatch`, `subscribeToCache`, `getLastCalculadoEm`)
    - `src/mr-3/mr-cobrancas/src/components/CelulaSaldoAtualizado.jsx` (Task 3 — consumir)
  </read_first>
  <files>src/mr-3/mr-cobrancas/src/components/ModuloContratos.jsx (MODIFICADO)</files>
  <action>
    Modificar `src/mr-3/mr-cobrancas/src/components/ModuloContratos.jsx`:

    **1. Imports (adicionar):**
    ```jsx
    import { useEffect, useRef, useState } from "react"; // garantir useRef + useState presentes
    import CelulaSaldoAtualizado from "./CelulaSaldoAtualizado.jsx";
    import {
      invalidateAll,
      kickoffBatch,
      subscribeToCache,   // helper observabilidade SC5 — locked em CONTEXT.md §5
      getLastCalculadoEm, // helper observabilidade SC5 — locked em CONTEXT.md §5
    } from "../hooks/useSaldoAtualizadoCache.js";
    ```

    **Nota — 7 exports locked (SC1):** `subscribeToCache` e `getLastCalculadoEm` são helpers de observabilidade da API do hook, locked no CONTEXT.md §5 SC1 junto dos 5 exports principais. Usar diretamente (não são opcionais, não são "compromisso pragmático"; são parte do contrato).

    **2. State + effect de kickoff (D-11 + D-12):**
    ```jsx
    const kickedOff = useRef(false);

    useEffect(() => {
      // D-12: StrictMode guard via useRef — dev double-mount pula.
      if (kickedOff.current) return;
      kickedOff.current = true;
      kickoffBatch(contratos, dividas, allPagamentosDivida);
    }, [contratos.length, dividas.length, allPagamentosDivida.length]);
    // ↑ D-12: deps por .length permitem re-kickoff quando CRUD muda contagem.
    // Ao re-kickoff: resetar flag antes OU usar deps direto. Implementação recomendada:
    ```

    Recommended pattern (evita stuck flag após mudanças):
    ```jsx
    const kickedOff = useRef(false);
    useEffect(() => {
      if (kickedOff.current === false || /* contagem mudou */ true) {
        kickoffBatch(contratos, dividas, allPagamentosDivida);
        kickedOff.current = true;
      }
    }, [contratos.length, dividas.length, allPagamentosDivida.length]);
    ```
    A estratégia: flag protege StrictMode double-mount do primeiro render; mudanças nas deps por `.length` forçam re-run (React chama efeito de novo ao mudar deps — fingerprint filter interno pula os já-cached, então re-kickoff é barato).

    **3. Timestamp + botão header (SC5 via helpers observabilidade):**
    ```jsx
    const [tick, setTick] = useState(0);
    useEffect(() => {
      const unsub = subscribeToCache(() => setTick(t => t + 1));
      return unsub;
    }, []);

    function fmtHHMM(ts) {
      if (!ts) return "—";
      return new Date(ts).toLocaleTimeString("pt-BR", {
        hour: "2-digit", minute: "2-digit", timeZone: "America/Sao_Paulo"
      });
    }

    const lastCalc = getLastCalculadoEm();
    const timestampLabel = lastCalc ? `Atualizado às ${fmtHHMM(lastCalc)}` : "Atualizando…";

    // JSX no header da tabela:
    <div style={{ display: "flex", gap: 8, alignItems: "center" }}>
      <span style={{ fontSize: 12, color: "#666" }}>{timestampLabel}</span>
      <button
        type="button"
        onClick={() => { invalidateAll(); kickoffBatch(contratos, dividas, allPagamentosDivida); }}
        title="Forçar atualização do saldo"
      >🔄 Atualizar</button>
    </div>
    ```

    **4. Nova coluna "Saldo Atualizado":**

    Adicionar `<th>Saldo Atualizado</th>` no `<thead>` da tabela de contratos (posição: após a última coluna atual de valor; confirmar no arquivo).

    Em cada `<tr>` do map de contratos, adicionar `<td>` com:
    ```jsx
    <td>
      <CelulaSaldoAtualizado
        contrato={contrato}
        dividasDoContrato={dividas.filter(d => d.contrato_id === contrato.id)}
        allPagamentosDivida={allPagamentosDivida}
        hoje={undefined /* hook usa default Goiânia */}
      />
    </td>
    ```

    **CRÍTICO — row clicável preservada**: NÃO mover `onClick` da `<tr>` nem colocar `e.stopPropagation()` na célula. D-10: célula é display-only, row inteira navega. Se existe `onClick` no `<tr>`, preservar sem mudança.

    **5. Guardrails D-01 / D-02:**
    - ZERO mudança em `src/utils/devedorCalc.js`, `src/services/pagamentos.js`, `src/services/dividas.js`.
    - `ModuloContratos.jsx` pode importar o hook e `CelulaSaldoAtualizado`, mas NÃO importa `DetalheContrato`/`AdicionarDocumento` (isso já seria existente — não adicionar).
  </action>
  <verify>
    <automated>cd src/mr-3/mr-cobrancas &amp;&amp; npm run build 2>&amp;1 | tee /tmp/07.8.2a-01-t4-build.log | tail -40 &amp;&amp; grep -qE "built in|✓" /tmp/07.8.2a-01-t4-build.log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CelulaSaldoAtualizado" src/mr-3/mr-cobrancas/src/components/ModuloContratos.jsx` returns `>= 2` (import + uso em JSX)
    - `grep -c "kickoffBatch" src/mr-3/mr-cobrancas/src/components/ModuloContratos.jsx` returns `>= 2` (uso no useEffect + no botão)
    - `grep -c "invalidateAll" src/mr-3/mr-cobrancas/src/components/ModuloContratos.jsx` returns `>= 1` (botão)
    - `grep -c "subscribeToCache" src/mr-3/mr-cobrancas/src/components/ModuloContratos.jsx` returns `>= 1` (helper observabilidade SC5 — tick reativo)
    - `grep -c "getLastCalculadoEm" src/mr-3/mr-cobrancas/src/components/ModuloContratos.jsx` returns `>= 1` (helper observabilidade SC5 — timestamp label)
    - `grep -c "Saldo Atualizado" src/mr-3/mr-cobrancas/src/components/ModuloContratos.jsx` returns `>= 1` (header)
    - `grep -c "🔄 Atualizar" src/mr-3/mr-cobrancas/src/components/ModuloContratos.jsx` returns `>= 1`
    - `grep -c "kickedOff" src/mr-3/mr-cobrancas/src/components/ModuloContratos.jsx` returns `>= 1` (D-12 guard)
    - `grep -cE "\[contratos\.length, ?dividas\.length, ?allPagamentosDivida\.length\]" src/mr-3/mr-cobrancas/src/components/ModuloContratos.jsx` returns `>= 1` (D-12 deps)
    - `cd src/mr-3/mr-cobrancas && npm run build` exits 0
    - **D-01 motor intocado**: `git -C src/mr-3 diff -- src/mr-cobrancas/src/utils/devedorCalc.js src/mr-cobrancas/src/services/pagamentos.js src/mr-cobrancas/src/services/dividas.js` outputs nothing
  </acceptance_criteria>
  <done>
    Coluna, botão, timestamp (via `subscribeToCache` + `getLastCalculadoEm`) e kickoffBatch presentes; build passes; row navigation preservada; motor Art.354 diff vazio.
  </done>
</task>

<task type="auto">
  <name>Task 5: Modificar DetalheContrato.jsx — 5 handlers D-05 (invalidateContrato/removeContrato ANTES de toast.success)</name>
  <read_first>
    - `.planning/phases/07.8.2a-saldo-atualizado-listagem-cache-swr/07.8.2a-CONTEXT.md` (Fix E + D-05 + Section 7 shield grep)
    - `src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx` (inteiro — localizar os 5 handlers)
    - `src/mr-3/mr-cobrancas/src/hooks/useSaldoAtualizadoCache.js` (Task 1 — importar `invalidateContrato`, `removeContrato`)
  </read_first>
  <files>src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx (MODIFICADO — 5 handlers)</files>
  <action>
    Modificar `src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx`:

    **1. Import (adicionar):**
    ```jsx
    import { invalidateContrato, removeContrato } from "../hooks/useSaldoAtualizadoCache.js";
    ```

    **2. D-05 enforcement — 5 handlers. EM CADA UM, chamar `invalidateContrato(contrato.id)` (ou `removeContrato(contrato.id)`) ANTES de `toast.success(...)` / `setState(...)`/fechamento de modal:**

    **Handler 1 — `handleRegistrarPagamento`** (chama `registrarPagamentoContrato`):
    ```jsx
    // Antes: await registrarPagamentoContrato(...); toast.success(...);
    // Depois:
    await registrarPagamentoContrato(...);
    invalidateContrato(contrato.id);   // ← INSERIR AQUI (antes do toast)
    toast.success(...);
    // ... resto dos setters/refresh
    ```

    **Handler 2 — `handleExcluirPagamento`** (chama `excluirPagamentoContrato`):
    ```jsx
    await excluirPagamentoContrato(...);
    invalidateContrato(contrato.id);   // ← INSERIR AQUI
    toast.success(...);
    ```

    **Handler 3 — `handleSalvarParcelasCustom`** (chama `atualizarParcelasCustom`):
    ```jsx
    await atualizarParcelasCustom(...);
    invalidateContrato(contrato.id);   // ← INSERIR AQUI
    toast.success(...);
    ```

    **Handler 4 — `handleExcluirDocumento`** (chama `excluirDocumento`):
    ```jsx
    await excluirDocumento(...);
    invalidateContrato(contrato.id);   // ← INSERIR AQUI
    toast.success(...);
    ```

    **Handler 5 — `handleExcluirContrato`** (chama `excluirContrato`) — **USA `removeContrato`** (contrato deixou de existir):
    ```jsx
    await excluirContrato(...);
    removeContrato(contrato.id);       // ← INSERIR AQUI (não invalidate — contrato sumiu)
    toast.success(...);
    // navegação de volta pra listagem
    ```

    **REGRA DE POSICIONAMENTO (crítica — D-05)**: a chamada de invalidação SEMPRE entre o `await <service>()` e o `toast.success(...)` / `setState(...)`. Se o handler usa try/catch, colocar dentro do try, APÓS sucesso da service call.

    **NÃO tocar motor nem services.** O plan só edita JSX de `DetalheContrato.jsx`.

    **Shield grep report (reproduzir no SUMMARY deste plan):**
    Após edição, rodar dentro de `src/mr-3/mr-cobrancas/`:
    ```bash
    grep -nE "registrarPagamentoContrato|excluirPagamentoContrato|atualizarParcelasCustom|excluirContrato|excluirDocumento" src/components/DetalheContrato.jsx
    ```
    Depois, pra CADA handler, rodar range-grep verificando `invalidateContrato`/`removeContrato` entre a linha do service call e o próximo `toast.success`. Reproduzir a tabela:
    ```
    Handler                             | Service call                  | Invalidate presente
    handleRegistrarPagamento            | registrarPagamentoContrato    | YES
    handleExcluirPagamento              | excluirPagamentoContrato      | YES
    handleSalvarParcelasCustom          | atualizarParcelasCustom       | YES
    handleExcluirContrato               | excluirContrato               | YES (via removeContrato)
    handleExcluirDocumento              | excluirDocumento              | YES
    ```
  </action>
  <verify>
    <automated>cd src/mr-3/mr-cobrancas &amp;&amp; npm run build 2>&amp;1 | tee /tmp/07.8.2a-01-t5-build.log | tail -40 &amp;&amp; grep -qE "built in|✓" /tmp/07.8.2a-01-t5-build.log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from.*useSaldoAtualizadoCache" src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx` returns `>= 1`
    - `grep -c "invalidateContrato" src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx` returns `>= 4` (4 dos 5 handlers usam invalidate)
    - `grep -c "removeContrato" src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx` returns `>= 1` (handleExcluirContrato)
    - Handler-level shield (range grep): para cada handler abaixo, `awk '/function handle<Name>|const handle<Name>/,/^}/' src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx | grep -c "invalidateContrato\|removeContrato"` returns `>= 1`:
      - `handleRegistrarPagamento`
      - `handleExcluirPagamento`
      - `handleSalvarParcelasCustom`
      - `handleExcluirDocumento`
      - `handleExcluirContrato` (this one expects `removeContrato`)
    - `cd src/mr-3/mr-cobrancas && npm run build` exits 0
    - **D-01 motor intocado**: `git -C src/mr-3 diff -- src/mr-cobrancas/src/utils/devedorCalc.js src/mr-cobrancas/src/services/pagamentos.js src/mr-cobrancas/src/services/dividas.js` outputs nothing
    - Shield grep report reproduzido na mensagem de execução (deve aparecer no SUMMARY da phase)
  </acceptance_criteria>
  <done>
    5 handlers de mutação em DetalheContrato.jsx chamam invalidateContrato/removeContrato antes do toast.success; build passes; shield grep reporta todos YES.
  </done>
</task>

<task type="auto">
  <name>Task 6: Modificar AdicionarDocumento.jsx — handleSalvar chama invalidateContrato (D-05 handler #6)</name>
  <read_first>
    - `.planning/phases/07.8.2a-saldo-atualizado-listagem-cache-swr/07.8.2a-CONTEXT.md` (Fix E — handler #6)
    - `src/mr-3/mr-cobrancas/src/components/AdicionarDocumento.jsx` (inteiro — localizar `handleSalvar`)
    - `src/mr-3/mr-cobrancas/src/hooks/useSaldoAtualizadoCache.js` (Task 1)
  </read_first>
  <files>src/mr-3/mr-cobrancas/src/components/AdicionarDocumento.jsx (MODIFICADO — 1 handler)</files>
  <action>
    Modificar `src/mr-3/mr-cobrancas/src/components/AdicionarDocumento.jsx`:

    **1. Import (adicionar):**
    ```jsx
    import { invalidateContrato } from "../hooks/useSaldoAtualizadoCache.js";
    ```

    **2. Handler `handleSalvar`** (chama `adicionarDocumento`):
    ```jsx
    await adicionarDocumento(...);
    invalidateContrato(contrato.id);   // ← INSERIR AQUI (antes de toast/fechamento de modal)
    toast.success(...);
    // ... fechamento de modal/reset form
    ```

    **NÃO tocar motor nem services.** Só JSX de `AdicionarDocumento.jsx`.

    **Shield row (pra somar no report do Task 5):**
    ```
    handleSalvar (AdicionarDocumento)   | adicionarDocumento            | YES
    ```
  </action>
  <verify>
    <automated>cd src/mr-3/mr-cobrancas &amp;&amp; npm run build 2>&amp;1 | tee /tmp/07.8.2a-01-t6-build.log | tail -40 &amp;&amp; grep -qE "built in|✓" /tmp/07.8.2a-01-t6-build.log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from.*useSaldoAtualizadoCache" src/mr-3/mr-cobrancas/src/components/AdicionarDocumento.jsx` returns `>= 1`
    - `grep -c "invalidateContrato" src/mr-3/mr-cobrancas/src/components/AdicionarDocumento.jsx` returns `>= 1`
    - Handler-level: `awk '/function handleSalvar|const handleSalvar/,/^}/' src/mr-3/mr-cobrancas/src/components/AdicionarDocumento.jsx | grep -c "invalidateContrato"` returns `>= 1`
    - `cd src/mr-3/mr-cobrancas && npm run build` exits 0
    - **D-01 motor intocado**: `git -C src/mr-3 diff -- src/mr-cobrancas/src/utils/devedorCalc.js src/mr-cobrancas/src/services/pagamentos.js src/mr-cobrancas/src/services/dividas.js` outputs nothing
  </acceptance_criteria>
  <done>
    handleSalvar em AdicionarDocumento.jsx chama invalidateContrato antes do toast.success; build passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 7: Criar saldoAtualizadoCache.test.js com 3 shields + atualizar test:regressao no package.json</name>
  <read_first>
    - `.planning/phases/07.8.2a-saldo-atualizado-listagem-cache-swr/07.8.2a-CONTEXT.md` (Fix F + seção 9 adapter da 7.8 pós-fix 7.8.1-02)
    - `src/mr-3/mr-cobrancas/src/services/__tests__/calculos.test.js` (analog pra estilo vitest do projeto — padrão `describe/it/expect` + imports)
    - `src/mr-3/mr-cobrancas/src/hooks/useSaldoAtualizadoCache.js` (Task 1 — importar `__test` + funções públicas)
    - `src/mr-3/mr-cobrancas/package.json` (identificar script `test:regressao` atual)
  </read_first>
  <behavior>
    - Shield 1 (cross-entity): Dado 2 contratos A e B com dívidas e pagamentos independentes. Popular cache pra ambos via `kickoffBatch([A, B], ...)`. Mudar só pagamentos de B (adicionar novo `pagamentos_divida` pra B). Chamar `invalidateContrato(B.id)`. Re-renderizar via chamada de `useSaldoContrato(A, ...)` simulada (usar `__test.recomputeEntry` direto ou re-executar fingerprint via helper): o fingerprint de A NÃO muda; saldo de A permanece idêntico. O fingerprint de B muda; saldo de B recalcula.
    - Shield 2 (virada de data Goiânia): Dado 1 contrato com 1 dívida. Chamar `contratoFingerprint(dividas, [], "2026-04-24")` e depois com `"2026-04-25"`. Os 2 outputs devem ser DIFERENTES (prefixo `${dataHoje}|` garante virada).
    - Shield 3 (invalidate isolado): Dado 3 contratos A, B, C com cache populado. Chamar `invalidateContrato(A.id)`. Assert: `cache.get(A).status === "revalidating"`; `cache.get(B).status === "fresh"`; `cache.get(C).status === "fresh"`.
  </behavior>
  <files>
    - src/mr-3/mr-cobrancas/src/services/__tests__/saldoAtualizadoCache.test.js (NOVO)
    - src/mr-3/mr-cobrancas/package.json (MODIFICADO — script test:regressao)
  </files>
  <action>
    **Parte A — Criar `src/mr-3/mr-cobrancas/src/services/__tests__/saldoAtualizadoCache.test.js`:**

    Seguir estilo vitest do projeto (baseado em `calculos.test.js`):

    ```javascript
    import { describe, it, expect, beforeEach } from "vitest";
    import {
      invalidateContrato,
      removeContrato,
      kickoffBatch,
      __test,
    } from "../../hooks/useSaldoAtualizadoCache.js";

    const { cache, contratoFingerprint } = __test;

    // Fixtures mínimas — shape compatível com calcularDetalheEncargosContrato
    function mkDivida(id, contrato_id, override = {}) {
      return {
        id, contrato_id,
        valor_total: 1000,
        data_vencimento: "2025-01-01",
        data_origem: "2025-01-01",
        data_inicio_atualizacao: "2025-01-01",
        indexador: "IPCA",
        multa_pct: 2,
        juros_tipo: "simples",
        juros_am: 1,
        honorarios_pct: 10,
        art523_opcao: "nao",
        saldo_quitado: false,
        ...override,
      };
    }
    function mkPag(id, divida_id, valor, data = "2025-06-01") {
      return { id, divida_id, valor, data_pagamento: data };
    }

    describe("saldoAtualizadoCache — shields 7.8.2a", () => {
      beforeEach(() => { cache.clear(); });

      it("Shield 1 — cross-entity: mudança em B não afeta fingerprint de A", () => {
        const divA = [mkDivida("dA1", "A")];
        const divB = [mkDivida("dB1", "B")];
        const pagsBefore = [mkPag("p1", "dA1", 100)];
        const pagsAfter = [mkPag("p1", "dA1", 100), mkPag("p2", "dB1", 200)];

        const fpA_before = contratoFingerprint(divA, pagsBefore.filter(p => ["dA1"].includes(p.divida_id)), "2026-04-24");
        const fpA_after  = contratoFingerprint(divA, pagsAfter.filter(p => ["dA1"].includes(p.divida_id)),  "2026-04-24");
        expect(fpA_after).toBe(fpA_before); // A não mudou

        const fpB_before = contratoFingerprint(divB, pagsBefore.filter(p => ["dB1"].includes(p.divida_id)), "2026-04-24");
        const fpB_after  = contratoFingerprint(divB, pagsAfter.filter(p => ["dB1"].includes(p.divida_id)),  "2026-04-24");
        expect(fpB_after).not.toBe(fpB_before); // B mudou
      });

      it("Shield 2 — virada de data Goiânia: fingerprint muda entre dias", () => {
        const div = [mkDivida("d1", "A")];
        const fpDay1 = contratoFingerprint(div, [], "2026-04-24");
        const fpDay2 = contratoFingerprint(div, [], "2026-04-25");
        expect(fpDay1).not.toBe(fpDay2);
      });

      it("Shield 3 — invalidateContrato(A) não afeta B nem C", () => {
        cache.set("A", { saldo: 1000, detalhe: {}, fingerprint: "fpA", status: "fresh", calculadoEm: 1 });
        cache.set("B", { saldo: 2000, detalhe: {}, fingerprint: "fpB", status: "fresh", calculadoEm: 2 });
        cache.set("C", { saldo: 3000, detalhe: {}, fingerprint: "fpC", status: "fresh", calculadoEm: 3 });
        invalidateContrato("A");
        expect(cache.get("A").status).toBe("revalidating");
        expect(cache.get("B").status).toBe("fresh");
        expect(cache.get("C").status).toBe("fresh");
      });
    });
    ```

    *Nota*: se `__test` não for exportado do hook (Task 1 o documenta como opcional), adicionar o export no hook. Isso é necessário para os shields acessarem `cache` e `contratoFingerprint`.

    **Parte B — Atualizar `src/mr-3/mr-cobrancas/package.json`:**

    Encontrar o script `test:regressao` atual:
    ```json
    "test:regressao": "vitest run src/services/__tests__/calculos.test.js"
    ```

    Substituir por:
    ```json
    "test:regressao": "vitest run src/services/__tests__/calculos.test.js src/services/__tests__/saldoAtualizadoCache.test.js"
    ```

    **NÃO adicionar outros scripts, deps, nem mudar `name`/`version`/etc.** Só o comando do script `test:regressao`.
  </action>
  <verify>
    <automated>cd src/mr-3/mr-cobrancas &amp;&amp; npm run test:regressao 2>&amp;1 | tee /tmp/07.8.2a-01-t7-test.log | tail -30 &amp;&amp; grep -qE "14 passed|Test Files +[^0]* passed|Tests +14 passed" /tmp/07.8.2a-01-t7-test.log</automated>
  </verify>
  <acceptance_criteria>
    - `ls src/mr-3/mr-cobrancas/src/services/__tests__/saldoAtualizadoCache.test.js` exists (file created)
    - `grep -cE "Shield 1 — cross-entity|cross-entity" src/mr-3/mr-cobrancas/src/services/__tests__/saldoAtualizadoCache.test.js` returns `>= 1`
    - `grep -cE "Shield 2 — virada de data|virada" src/mr-3/mr-cobrancas/src/services/__tests__/saldoAtualizadoCache.test.js` returns `>= 1`
    - `grep -cE "Shield 3 — invalidateContrato|invalidateContrato\(A" src/mr-3/mr-cobrancas/src/services/__tests__/saldoAtualizadoCache.test.js` returns `>= 1`
    - `grep -c "saldoAtualizadoCache.test.js" src/mr-3/mr-cobrancas/package.json` returns `>= 1` (script atualizado)
    - `cd src/mr-3/mr-cobrancas && npm run test:regressao` exits 0 with output containing "14 passed" (11 preexistentes + 3 novos shields) ou equivalente "Test Files N passed / Tests 14 passed"
  </acceptance_criteria>
  <done>
    3 shields criados e passando; package.json atualizado; `test:regressao` executa 14/14 PASS (11 preexistentes + 3 novos).
  </done>
</task>

<task type="auto">
  <name>Task 8: Gate final + commit submódulo (feat 07.8.2a)</name>
  <read_first>
    - `.planning/phases/07.8.2a-saldo-atualizado-listagem-cache-swr/07.8.2a-CONTEXT.md` (Section 7 shield report format + D-15 commit convention)
    - Todos os arquivos modificados nas Tasks 1-7
  </read_first>
  <files>
    - src/mr-3 (commit dentro do submódulo)
  </files>
  <action>
    **Executar gates consolidados (D-14 pre-commit):**

    1. **Gate A — Regressão 14/14:**
       ```bash
       cd src/mr-3/mr-cobrancas && npm run test:regressao
       ```
       Esperado: exit 0, "14 passed".

    2. **Gate B — Build exit 0:**
       ```bash
       cd src/mr-3/mr-cobrancas && npm run build
       ```
       Esperado: exit 0.

    3. **Gate C1 (pré-commit) — D-01 motor intocado no WORKING TREE antes de `git add` (grep-verifiable):**
       ```bash
       cd src/mr-3 && git diff -- src/utils/devedorCalc.js src/services/pagamentos.js src/services/dividas.js
       ```
       Esperado: saída VAZIA. Grep-verificável:
       ```bash
       [ -z "$(cd src/mr-3 && git diff -- src/utils/devedorCalc.js src/services/pagamentos.js src/services/dividas.js)" ] && echo OK || echo FAIL
       ```
       Output esperado: `OK`.
       **Propósito:** captura edição acidental do motor no working tree ANTES que ela entre no index via `git add`.
       Se FAIL → ABORT commit, investigar violação D-01.

    4. **Gate D — D-02 isolation grep (zero matches):**
       ```bash
       cd src/mr-3/mr-cobrancas && grep -nE "import.*DetalheContrato|import.*AdicionarDocumento" \
         src/hooks/useSaldoAtualizadoCache.js \
         src/components/CelulaSaldoAtualizado.jsx \
         src/utils/batchCalculator.js
       ```
       Esperado: ZERO matches. Se algum match → ABORT.

    5. **Gate E — D-05 shield grep (tabela literal):**
       ```bash
       cd src/mr-3/mr-cobrancas && grep -nE "registrarPagamentoContrato|excluirPagamentoContrato|atualizarParcelasCustom|excluirContrato|excluirDocumento|adicionarDocumento" \
         src/components/DetalheContrato.jsx \
         src/components/AdicionarDocumento.jsx
       ```
       Depois, `awk`-range cada handler verificando `invalidateContrato`/`removeContrato` entre o service call e o próximo `toast.success`. **Reproduzir a tabela literal no output do Task (e no SUMMARY):**
       ```
       Handler                             | Service call                  | Invalidate presente
       handleRegistrarPagamento            | registrarPagamentoContrato    | YES
       handleExcluirPagamento              | excluirPagamentoContrato      | YES
       handleSalvarParcelasCustom          | atualizarParcelasCustom       | YES
       handleExcluirContrato               | excluirContrato               | YES (via removeContrato)
       handleExcluirDocumento              | excluirDocumento              | YES
       handleSalvar (AdicionarDocumento)   | adicionarDocumento            | YES
       ```
       Se ALGUM "NO" → ABORT + corrigir handler faltante.

    6. **Gate F — git status limpo exceto arquivos previstos:**
       ```bash
       cd src/mr-3 && git status
       ```
       Esperado: apenas arquivos listados em `files_modified` da phase modificados/adicionados. Nenhum arquivo fora do escopo.

    **Se TODOS os gates pré-commit (A, B, C1, D, E, F) passarem**, criar commit DENTRO do submódulo (D-15):
    ```bash
    cd src/mr-3 && git add \
      mr-cobrancas/src/hooks/useSaldoAtualizadoCache.js \
      mr-cobrancas/src/components/CelulaSaldoAtualizado.jsx \
      mr-cobrancas/src/utils/batchCalculator.js \
      mr-cobrancas/src/components/ModuloContratos.jsx \
      mr-cobrancas/src/components/DetalheContrato.jsx \
      mr-cobrancas/src/components/AdicionarDocumento.jsx \
      mr-cobrancas/src/services/__tests__/saldoAtualizadoCache.test.js \
      mr-cobrancas/package.json

    cd src/mr-3 && git commit -m "feat(07.8.2a): saldo atualizado na listagem com cache SWR"
    ```

    7. **Gate C2 (pós-commit) — bump commit NÃO contém motor files:**
       Imediatamente após o `git commit`, rodar:
       ```bash
       cd src/mr-3 && git log -1 --format="" --name-only | grep -E "devedorCalc|services/pagamentos|services/dividas"
       ```
       Esperado: output VAZIO (exit code 1 de grep). Grep-verificável:
       ```bash
       cd src/mr-3 && git log -1 --format="" --name-only | grep -qE "devedorCalc|services/pagamentos|services/dividas" && echo FAIL || echo OK
       ```
       Output esperado: `OK`.
       **Propósito:** captura o caso em que motor files entraram no commit apesar do working tree estar limpo (ex: arquivo já staged antes de C1, drift de index). Se FAIL → `git reset HEAD~1` + investigar + recommit.

    **NÃO executar `git push` em nenhum nível** (D-14 — push é ação humana pós-PAUSA #3 em Plan 02).
    **NÃO bumpar submódulo no pai** (isso é Plan 02 Task 2).
    **NÃO criar tag v1.4-phase7.8.2a** (explicitamente fora de escopo — SC13).

    Reportar SHA do commit criado no submódulo via `git -C src/mr-3 log -1 --oneline`.

    **Shield D-01 — reprodução no SUMMARY (OBRIGATÓRIA):**
    O SUMMARY deste plan DEVE reproduzir literalmente os outputs de ambos os gates:
    - `Gate C1 (pré-commit): OK` (ou `FAIL` se ocorreu)
    - `Gate C2 (pós-commit): OK` (ou `FAIL` se ocorreu)
    Ambos devem aparecer como "OK" para o plan ser considerado shippable.
  </action>
  <verify>
    <automated>cd src/mr-3 &amp;&amp; git log -1 --oneline | grep -qE "feat\(07\.8\.2a\)"</automated>
  </verify>
  <acceptance_criteria>
    - Gate A: `cd src/mr-3/mr-cobrancas && npm run test:regressao` exits 0 with "14 passed"
    - Gate B: `cd src/mr-3/mr-cobrancas && npm run build` exits 0
    - **Gate C1 (pré-commit)** — motor intocado no working tree: `[ -z "$(cd src/mr-3 && git diff -- src/utils/devedorCalc.js src/services/pagamentos.js src/services/dividas.js)" ] && echo OK || echo FAIL` outputs `OK` ANTES de `git add`.
    - **Gate C2 (pós-commit)** — motor não no commit: `cd src/mr-3 && git log -1 --format="" --name-only | grep -qE "devedorCalc|services/pagamentos|services/dividas" && echo FAIL || echo OK` outputs `OK` APÓS `git commit`.
    - Gate D: D-02 isolation grep across 3 new files outputs ZERO matches
    - Gate E: D-05 shield report reproduzido com TODOS os handlers "YES"
    - Gate F: `cd src/mr-3 && git log -1 --oneline` shows commit with message starting `feat(07.8.2a): saldo atualizado na listagem com cache SWR`
    - Both C1 and C2 outputs (`OK` / `OK`) reproduzidos na output do task e no SUMMARY (Gate C1 (pré-commit) e Gate C2 (pós-commit) como linhas literais).
    - ZERO `git push` executado no output deste task
    - ZERO bump de submódulo no pai executado neste task
    - ZERO criação de tag `v1.4-phase7.8.2a` neste task
  </acceptance_criteria>
  <done>
    Todos os gates (A, B, C1, C2, D, E, F) passam; commit único no submódulo com mensagem D-15; nenhum push, bump ou tag executado. C1 e C2 ambos reportam OK — motor intocado tanto no working tree (pré-commit) quanto no commit em si (pós-commit).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (SWR correctness focus — não security)

| Boundary | Description |
|----------|-------------|
| Caller (mutador) → Hook (cache) | Todo handler que mute `pagamentos_divida`, `documentos_contrato` ou `dividas` cruza pra notificar o cache (D-05). Silent failure = silent-stale em prod. |
| Module-load → Browser window | `window.focus` listener registrado no module-load; se SSR/teste Node, `typeof window` guard evita crash. |
| React StrictMode → useEffect | Dev double-mount duplica kickoff; `useRef` flag + deps por `.length` tornam o efeito idempotente (D-12). |
| Multi-tab / multi-user | Cache é in-memory por tab; foco em outra tab pode ter data stale → `window.focus` trigger (gatilho 3) força revalidação ao retornar. |

## Threat Register (correctness-critical — STRIDE adaptado pra state consistency)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.8.2a-01 | Tampering (stale state) | Handlers de mutação (6 callers) | mitigate | D-05 enforcement: `invalidateContrato`/`removeContrato` ANTES do `toast.success` em TODOS os 6 handlers (Tasks 5-6). Gate E (shield grep tabela literal) bloqueia commit se algum handler falhar. |
| T-07.8.2a-02 | Information Disclosure (cross-entity bleed) | Hook cache + adapter | mitigate | Shield Test 1 (Task 7) garante que mudança em contrato B não altera cache/fingerprint de A. Herda lição 7.8.1 (adapter filtra allPagamentosDivida internamente — D-04). |
| T-07.8.2a-03 | Denial of Service (UI blocking) | `kickoffBatch` com 200 contratos | mitigate | `runBatches` (Task 2) usa `requestIdleCallback` + fallback `setTimeout(0)` + timeout 500ms. Batches de 10. UAT SC6 (Plan 02) valida proporcionalmente. |
| T-07.8.2a-04 | Tampering (StrictMode double-kickoff) | `ModuloContratos::useEffect` kickoff | mitigate | D-12: `useRef` flag `kickedOff.current` + deps por `.length`. Gate B (build) + UAT SC3 (Plan 02). |
| T-07.8.2a-05 | Tampering (virada de dia Goiânia) | Fingerprint não sensível a data | mitigate | Shield Test 2 (Task 7) + D-09 (`Intl.DateTimeFormat America/Sao_Paulo`). UAT SC5 (Plan 02). |
| T-07.8.2a-06 | Spoofing (hook acoplado a componentes) | Hook novo importando DetalheContrato | mitigate | D-02 isolation grep (Gate D) bloqueia commit. Arquitetural invariant. |
| T-07.8.2a-07 | Elevation of Privilege (motor tocado) | Cache code alterando devedorCalc.js | mitigate | D-01 motor diff grep (Gates C1 pré-commit + C2 pós-commit) — ambos devem reportar `OK`. Split em dois gates elimina risco de false PASS dependente da ordem de execução. ABORT commit se C1 falhar; `git reset HEAD~1` se C2 falhar. |
| T-07.8.2a-08 | Repudiation (silent stale pós-logout) | Cache permanece após logout | accept | Cache é per-tab in-memory; F5 limpa (CONTEXT.md §3 — "F5 recomputa"). Se multi-usuário no mesmo browser vira issue real, cobertura separada pós-7.8.2a. |
| T-07.8.2a-09 | Information Disclosure (window.focus listener leak) | Listener sobrevive unmount | accept | Listener é module-load (one-shot por sessão — D-08/D-13). Vive pela sessão; React hot-reload em dev pode duplicar — tolerado (não afeta prod). |
</threat_model>

<verification>
Consolidated gates (D-14 pre-commit + pós-commit — Task 8):

- [ ] Gate A — `npm run test:regressao` → 14/14 PASS (11 preexistentes + 3 shields novos)
- [ ] Gate B — `npm run build` → exit 0
- [ ] **Gate C1 (pré-commit)** — `git diff --` motor files (devedorCalc.js / pagamentos.js / dividas.js) outputs VAZIO antes de `git add`. Grep: `[ -z "$(... git diff ...)" ] && echo OK` = OK.
- [ ] **Gate C2 (pós-commit)** — `git log -1 --format="" --name-only | grep -E "devedorCalc|services/pagamentos|services/dividas"` outputs VAZIO após `git commit`. Grep: `... | grep -qE ... && echo FAIL || echo OK` = OK.
- [ ] Gate D — D-02 isolation grep → 0 matches (hook/célula/batchCalculator NÃO importam DetalheContrato/AdicionarDocumento)
- [ ] Gate E — D-05 shield grep tabela literal reproduzida com TODOS handlers "YES"
- [ ] Gate F — `git status` limpo (apenas arquivos previstos em files_modified)
- [ ] Commit único no submódulo com mensagem `feat(07.8.2a): saldo atualizado na listagem com cache SWR`
- [ ] ZERO `git push` executado
- [ ] ZERO bump no pai executado (diferido pra Plan 02 Task 2)
- [ ] ZERO criação de tag `v1.4-phase7.8.2a` (diferido pra pós-smoke prod — SC13)
</verification>

<success_criteria>
1. `src/mr-3/mr-cobrancas/src/hooks/useSaldoAtualizadoCache.js` existe com 7 exports (5 API principal + 2 helpers observabilidade — subscribeToCache, getLastCalculadoEm) + comentário SC1 obrigatório + fingerprint D-06 verbatim (12 campos — 4 greps consolidados passam) + window.focus module-load listener + 3 estados `"loading"/"fresh"/"revalidating"` + Intl Goiânia
2. `src/mr-3/mr-cobrancas/src/components/CelulaSaldoAtualizado.jsx` existe com 3 estados visuais + fade 180ms + display-only (zero onClick)
3. `src/mr-3/mr-cobrancas/src/utils/batchCalculator.js` existe com `runBatches` + requestIdleCallback + fallback + timeout 500ms
4. `src/mr-3/mr-cobrancas/src/services/__tests__/saldoAtualizadoCache.test.js` existe com 3 shields (cross-entity, virada dia, invalidate isolado)
5. `src/mr-3/mr-cobrancas/src/components/ModuloContratos.jsx` modificado com coluna "Saldo Atualizado" + botão "🔄 Atualizar" + timestamp (via `subscribeToCache` + `getLastCalculadoEm`) + kickoffBatch guardado (useRef + deps .length)
6. `src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx` modificado — 5 handlers D-05 com invalidate/remove antes de toast
7. `src/mr-3/mr-cobrancas/src/components/AdicionarDocumento.jsx` modificado — handleSalvar com invalidateContrato
8. `src/mr-3/mr-cobrancas/package.json` com `test:regressao` incluindo os 2 arquivos de test
9. Gates A, B, C1, C2, D, E, F todos verdes
10. Commit único no submódulo com mensagem D-15 `feat(07.8.2a): saldo atualizado na listagem com cache SWR`
11. D-01 motor intocado verificado em 2 momentos — C1 (pré-commit, working tree) e C2 (pós-commit, commit contents) ambos `OK`
12. D-02 isolation grep zero
13. D-05 shield report tabela literal com todos "YES"
14. Tag `v1.4-phase7.8.2a` NÃO criada nesta phase
15. Zero `git push` autorizado neste plan
</success_criteria>

<output>
Após completion: criar `.planning/phases/07.8.2a-saldo-atualizado-listagem-cache-swr/07.8.2a-01-SUMMARY.md` contendo:
- SHA do commit no submódulo
- Shield D-05 tabela literal (6 linhas, todos YES)
- Output resumido dos gates A, B, C1, C2, D, E, F (PASS/FAIL) — C1 e C2 ambos devem aparecer como linhas LITERAIS "Gate C1 (pré-commit): OK" e "Gate C2 (pós-commit): OK"
- Lista de arquivos criados/modificados
- Nota "Plan 02 pendente — UAT SC1-SC8 + bump no pai + autorização push humana"
</output>
