---
phase: 07.8.2a-saldo-atualizado-listagem-cache-swr
plan: 01
type: execute
status: shipped (submodule commit only — Plan 02 pendente p/ UAT + bump no pai + push)
executor_agent: gsd-execute (sonnet — sequential inline)
executed_on: 2026-04-24
submodule_commit: 1907d59 (full: 1907d597ccb5ec6d326c4a032752b1869b434d2a)
gates:
  A_regressao: PASS (14/14 — 11 preexistentes + 3 shields novos)
  B_build: PASS (exit 0)
  C1_pre_commit: OK (motor diff vazio no working tree)
  C2_pos_commit: OK (motor não no commit)
  D_d02_isolation: PASS (0 matches em hook/celula/batchCalculator)
  E_d05_shield: PASS (6/6 handlers YES)
  F_status: PASS (apenas arquivos previstos staged + 1 deviação Rule 3 documentada)
files_created:
  - src/mr-3/mr-cobrancas/src/hooks/useSaldoAtualizadoCache.js
  - src/mr-3/mr-cobrancas/src/components/CelulaSaldoAtualizado.jsx
  - src/mr-3/mr-cobrancas/src/utils/batchCalculator.js
  - src/mr-3/mr-cobrancas/src/services/__tests__/saldoAtualizadoCache.test.js
files_modified:
  - src/mr-3/mr-cobrancas/src/components/ModuloContratos.jsx
  - src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx
  - src/mr-3/mr-cobrancas/src/components/AdicionarDocumento.jsx
  - src/mr-3/mr-cobrancas/src/components/TabelaContratos.jsx  # DEVIATION Rule 3 — ver §Deviations
  - src/mr-3/mr-cobrancas/package.json
---

# Phase 7.8.2a Plan 01: Saldo Atualizado na Listagem (cache SWR) — Summary

Cache SWR caseiro (singleton Map + pub/sub) entrega coluna "Saldo Atualizado" na listagem de contratos com render instantâneo (cache hit) e revalidação em background; D-05 enforcement (6 callers) blinda silent-stale; motor Art.354 intocado (D-01 dual gate C1+C2).

## Tasks executadas (8/8)

| # | Task                                                                              | Status | Notas                                                                                              |
| - | --------------------------------------------------------------------------------- | ------ | -------------------------------------------------------------------------------------------------- |
| 1 | Hook `useSaldoAtualizadoCache.js` — 7 exports + fingerprint D-06 + window.focus + 3 estados | OK   | Comentário SC1 OK. Fingerprint reformatado em 6 linhas para satisfazer 4 greps de substring contígua. D-02 grep limpo após reescrita do header doc (substring "import" + "DetalheContrato" coexistia em comentário PT — removido). |
| 2 | `batchCalculator.js` — runBatches + idle callback + fallback setTimeout + 500ms   | OK     | Função pura, zero state module-level.                                                              |
| 3 | `CelulaSaldoAtualizado.jsx` — display-only, 3 estados, fade 180ms                 | OK     | D-10 grep limpo após reescrita do header doc (literais `onClick`/`role="button"`/`cursor:pointer` removidos do comentário). |
| 4 | `ModuloContratos.jsx` — coluna + botão 🔄 + timestamp + kickoffBatch guardado    | OK     | Aliases `contratos = allContratos \|\| []` e `dividas = allDividas \|\| []` adicionados p/ deps grep literal `[contratos.length, dividas.length, allPagamentosDivida.length]`. |
| 5 | `DetalheContrato.jsx` — 5 handlers D-05 (4× invalidate + 1× remove)              | OK     | invalidateContrato em handleRegistrarPagamento/handleExcluirPagamento/handleSalvarParcelasCustom/handleExcluirDocumento; removeContrato em handleExcluirContrato. Posicionamento: ENTRE service call e toast.success. |
| 6 | `AdicionarDocumento.jsx` — handleSalvar D-05 (#6)                                 | OK     | invalidateContrato após `await adicionarDocumento` antes do `toast.success`.                       |
| 7 | `saldoAtualizadoCache.test.js` (3 shields) + `package.json` test:regressao        | OK     | Teste roda 14/14 (11 + 3) na primeira execução (TDD-vermelho desnecessário — shields independentes do hook bem definidos via `__test`). |
| 8 | Gate final (A/B/C1/D/E/F) + commit submódulo + Gate C2                            | OK     | Commit `1907d59` — `feat(07.8.2a): saldo atualizado na listagem com cache SWR`.                    |

## Gates (literal output)

```
Gate A (regressão 14/14):   PASS — Test Files 2 passed (2) | Tests 14 passed (14)
Gate B (build):             PASS — vite v8.0.8, 131 modules, ✓ built in 457ms (exit 0)
Gate C1 (pré-commit):       OK
Gate C2 (pós-commit):       OK
Gate D (D-02 isolation):    PASS — 0 matches across 3 new files
Gate E (D-05 shield 6/6):   PASS — todos YES
Gate F (status):            PASS — 9 arquivos staged conforme escopo (8 do files_modified + 1 deviation Rule 3 documentada)
```

## Shield D-05 — tabela literal (Gate E)

```
Handler                             | Service call                  | Invalidate presente
handleRegistrarPagamento            | registrarPagamentoContrato    | YES
handleExcluirPagamento              | excluirPagamentoContrato      | YES
handleSalvarParcelasCustom          | atualizarParcelasCustom       | YES
handleExcluirContrato               | excluirContrato               | YES (via removeContrato)
handleExcluirDocumento              | excluirDocumento              | YES
handleSalvar (AdicionarDocumento)   | adicionarDocumento            | YES
```

Verificação awk-range por handler (output literal):

```
handleRegistrarPagamento : YES
handleExcluirPagamento : YES
handleSalvarParcelasCustom : YES
handleExcluirContrato : YES
handleExcluirDocumento : YES
handleSalvar (AdicionarDocumento) : YES
```

## Commit submódulo

- **SHA:** `1907d59` (full `1907d597ccb5ec6d326c4a032752b1869b434d2a`)
- **Branch:** `main` (submódulo `src/mr-3`)
- **Mensagem (D-15 verbatim):** `feat(07.8.2a): saldo atualizado na listagem com cache SWR`
- **Stat:** 9 files changed, 434 insertions(+), 8 deletions(-)
- **Push:** **NÃO executado** (Plan 02 PAUSA #3 — autorização humana)
- **Tag:** **NÃO criada** (`v1.4-phase7.8.2a` fora de escopo)
- **Bump no pai:** **NÃO executado** (Plan 02 Task 2)

## Arquivos do commit (9)

Em scope (Plan files_modified — 8):

1. `mr-cobrancas/src/hooks/useSaldoAtualizadoCache.js` (NEW, +194 lines) — 7 exports
2. `mr-cobrancas/src/components/CelulaSaldoAtualizado.jsx` (NEW, +39) — display-only
3. `mr-cobrancas/src/utils/batchCalculator.js` (NEW, +26) — idle scheduler
4. `mr-cobrancas/src/services/__tests__/saldoAtualizadoCache.test.js` (NEW, +77) — 3 shields
5. `mr-cobrancas/src/components/ModuloContratos.jsx` (+69 / -8)
6. `mr-cobrancas/src/components/DetalheContrato.jsx` (+7) — 1 import + 5 handlers patched
7. `mr-cobrancas/src/components/AdicionarDocumento.jsx` (+3) — 1 import + handleSalvar patched
8. `mr-cobrancas/package.json` (+1 / -1) — `test:regressao` adiciona `saldoAtualizadoCache.test.js`

Deviation (Rule 3 — necessário p/ correctness):

9. `mr-cobrancas/src/components/TabelaContratos.jsx` (+25) — render condicional da coluna

## Deviations from Plan

### 1. [Rule 3 — Auto-fix blocking issue] Modificado `TabelaContratos.jsx` (não previsto em files_modified)

- **Found during:** Task 4
- **Issue:** O Plan instrui literalmente adicionar `<th>Saldo Atualizado</th>` no `<thead>` e `<td><CelulaSaldoAtualizado /></td>` em cada `<tr>` do map de contratos, com greps targeting `ModuloContratos.jsx`. Porém a tabela física vive em `TabelaContratos.jsx` (linhas 36-115 do arquivo): `ModuloContratos.jsx` apenas chama `<TabelaContratos contratos={...} parcelasPorContrato={...} ... />` e nunca renderiza `<table>` diretamente. Sem tocar TabelaContratos, a coluna não renderizaria — aceitação prática (UAT SC1) impossível.
- **Fix:** Mantive todos os identificadores grep-relevantes (import `CelulaSaldoAtualizado`, literal "Saldo Atualizado", `kickoffBatch`, `invalidateAll`, `subscribeToCache`, `getLastCalculadoEm`, `kickedOff`, `🔄 Atualizar`, deps literal `[contratos.length, dividas.length, allPagamentosDivida.length]`) DENTRO de `ModuloContratos.jsx`. Adicionei prop opcional `saldoAtualizadoColuna={ header, Cell, allPagamentosDivida }` em `TabelaContratos.jsx`. Quando a prop é fornecida, render condicional injeta a coluna entre "Saldo" e "Em Atraso". Quando ausente (caller hipotético futuro), comportamento legado preservado — zero risco de regressão.
- **Files modified:** `src/mr-3/mr-cobrancas/src/components/TabelaContratos.jsx` (+25 lines, -3)
- **Verification:** Build PASS (exit 0, 131 modules), regressão 14/14 PASS, D-02 isolation grep não cobre TabelaContratos (não é arquivo da Phase 7.8.2a — só passa prop, não importa o hook diretamente).
- **Commit:** Incluído no commit único `1907d59`.
- **Discovery (aprovado pelo usuário 2026-04-24):** Arquitetura real tem split `ModuloContratos` (state + orchestration: hook wiring, kickoffBatch, invalidateAll, botão 🔄, timestamp reativo) / `TabelaContratos` (table HTML puro: thead/tbody/tr/td, paginação, hover states). Planner não conhecia esse split quando escreveu o plan — `files_modified` listou só ModuloContratos achando que a tabela era inline. Fix via prop opcional preserva separação de responsabilidades e backward-compat. **Anotado como input pra backlog**: Phase 7.8.2b futura pode considerar (a) consolidar ModuloContratos + TabelaContratos num único componente OU (b) manter split com nomes mais claros (ex: `ModuloContratosContainer` + `ContratosTable`). Decisão deferida — não bloqueia 7.8.2a.

### 2. [Reescritas de comentários — sem alteração funcional] Header docs em hook + Celula

- **Found during:** Task 1 e Task 3 (acceptance grep checks).
- **Issue:** Greps de isolamento (D-02 e D-10) são literais e casavam acidentalmente substrings dentro dos comentários PT-BR de header dos novos arquivos:
  - `useSaldoAtualizadoCache.js` linha 4 dizia `"D-02 isolamento (NÃO importa DetalheContrato/AdicionarDocumento)"` → grep `import.*DetalheContrato` (regex) casava porque a palavra portuguesa "importa" começa com "import".
  - `CelulaSaldoAtualizado.jsx` linhas 3-4 dizia `"sem onClick, sem onKeyDown, sem role=\"button\", sem cursor: pointer"` → grep D-10 casava todas as 4 substrings literais no próprio comentário.
- **Fix:** Reescrevi os comentários sem citar literalmente as APIs proibidas. Sentido preservado:
  - Hook: "D-02 isolamento (sem dependência reversa de UI)".
  - Celula: descrição em prosa sem mencionar `onClick`/`role="button"`/`cursor: pointer`.
- **Files modified:** Os mesmos arquivos novos (apenas comentários no topo). Zero impacto runtime.
- **Verification:** Greps D-02 e D-10 retornaram 0 matches. Build PASS.
- **Commit:** Incluído no commit único `1907d59`.

### 3. [Observação grep emoji — não-bloqueante] `grep -c "🔄 Atualizar"` no Git Bash retorna 0 mas string está presente

- **Found during:** Task 4 acceptance check.
- **Issue:** O grep nativo do Git Bash neste Windows não casa multi-byte UTF-8 (`🔄` = `f0 9f 94 84`) com `-c`/`-F`. Verificação alternativa via `ripgrep` (`rg -c "🔄 Atualizar"` → 1) e leitura visual da linha 124 (`>🔄 Atualizar</button>`) confirmam presença literal. Hexdump da linha confirma `f0 9f 94 84 20 41 74 75 61 6c 69 7a 61 72` no arquivo. Critério "string presente" satisfeito.
- **Fix:** Nenhum — limitação local de toolchain, não bug. Aceito como deviation observacional.
- **Verification:** `rg -c "🔄 Atualizar" src/components/ModuloContratos.jsx` → `1`.
- **Commit:** Sem alteração de código adicional necessária.

## Auth gates / blockers

Nenhum. Execução 100% local, zero rede, zero credenciais.

## D-01 motor untouched (dual gate)

- Gate C1 (pré-commit, working tree): `git diff -- src/utils/devedorCalc.js src/services/pagamentos.js src/services/dividas.js` → vazio → **OK**
- Gate C2 (pós-commit, commit content) *[reformulado — ver seção abaixo]*: inicialmente `git log -1 --stat | grep -E "..."` → reformulado pra `git log -1 --format="" --name-only | grep -E "..."` após descoberta de falso positivo no commit 608c124 (fix H3). Para o commit 1907d59 (feat): zero motor files no diff → **OK**.

## Gate C2 — reformulado pós-fact

Durante execução do commit `608c124` (fix H3 — PAUSA #1 pós-UAT), o Gate C2 original disparou **FAIL falso positivo**. Diagnóstico: grep usou `git log -1 --stat` que inclui commit message + diffstat no stream de saída. A mensagem rica do commit mencionava arquivos do motor (`devedorCalc.js:846`, `services/pagamentos.js`, `services/dividas.js`) pra auditar a causa raiz do H3 (Date vs string mismatch) — grep casou em prose explicativa, não em arquivos realmente tocados.

Confirmação motor intocado via comando porcelana `git show --name-only 608c124`:
```
mr-cobrancas/src/hooks/useSaldoAtualizadoCache.js
mr-cobrancas/src/services/__tests__/saldoAtualizadoCache.test.js
```
Zero motor files no diff real. D-01 preservado.

**Fix aplicado ao 01-PLAN.md antes do bundle final (Plan 02 Task 5):**
- Antes: `git log -1 --stat | grep -qE "..." && FAIL || OK`
- Depois: `git log -1 --format="" --name-only | grep -qE "..." && FAIL || OK`

`--format=""` suprime header e mensagem; `--name-only` lista só paths. Combinado: grep casa EXCLUSIVAMENTE em arquivos tocados, zero risco de casar em commit message rica.

**Decisão preservar commit `608c124` como-está** (não amend): mensagem referenciando arquivos do motor é evidência forense valiosa pra quem auditar H3 no futuro. Mutilar commit pra passar shield defeituoso seria anti-pattern ("mudar mensagem só pra passar CI" → fadiga de alerta → shields viram rubber-stamp).

Lição indexada em `memory/feedback_gate_motor_grep_use_name_only.md`. Aplica a phases futuras que criarem shields de "arquivo intocado" — usar sempre `--format="" --name-only`, nunca `--stat`.

## Self-Check

Verificação final dos artifacts e commit:

- File `src/mr-3/mr-cobrancas/src/hooks/useSaldoAtualizadoCache.js` — FOUND
- File `src/mr-3/mr-cobrancas/src/components/CelulaSaldoAtualizado.jsx` — FOUND
- File `src/mr-3/mr-cobrancas/src/utils/batchCalculator.js` — FOUND
- File `src/mr-3/mr-cobrancas/src/services/__tests__/saldoAtualizadoCache.test.js` — FOUND
- File `src/mr-3/mr-cobrancas/package.json` — modified (test:regressao OK)
- Commit `1907d59` em `src/mr-3` HEAD — FOUND
- Working tree pós-commit: limpo (apenas `.planning/*` parent + `node_modules/` continuam untracked — fora de scope)

## Self-Check: PASSED

## Próximo passo

**Plan 02 (07.8.2a-02) pendente** — UAT SC1-SC8 local + bump no pai (`chore(07.8.2a): bump submodule mr-3 — saldo atualizado cache SWR`) + autorização push humana (PAUSA #3). Tag `v1.4-phase7.8.2a` deferida para pós-smoke prod.
