---
plan: 08-01
phase: 8
title: PDF Demonstrativo do Contrato
wave: 1
depends_on: []
autonomous: false
files_modified:
  - src/mr-3/mr-cobrancas/src/utils/pdfDemonstrativo.js (NEW)
  - src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx (modified)
files_preserved:
  - src/mr-3/mr-cobrancas/src/utils/devedorCalc.js (D-01 motor INTOCADO)
  - src/mr-3/mr-cobrancas/src/services/pagamentos.js (D-01 motor INTOCADO)
  - src/mr-3/mr-cobrancas/src/services/dividas.js (D-01 motor INTOCADO)
  - src/mr-3/mr-cobrancas/src/utils/masks.js (D-01 motor INTOCADO)
  - app principal CSS (D-13 ZERO @media print preservada)
  - schema banco (D-pre-12 zero Migration)
requirements: []
tags:
  - feature
  - pdf
  - milestone-wrap
  - v1.4
---

# Plan 08-01: PDF Demonstrativo do Contrato

<objective>
Implementar **PDF Demonstrativo do Contrato** — feature wrap do milestone v1.4. Botão "🖨️ Gerar PDF" no header do `DetalheContrato.jsx` (L709-714, adjacente a "Editar Contrato" / "Excluir Contrato") dispara handler `handleGerarPDF` que delega ao utility module `src/utils/pdfDemonstrativo.js` (NEW), o qual carrega jsPDF via CDN dinâmico, busca devedores via `listarDevedoresDoContrato`, calcula encargos via `calcularDetalheEncargosContrato` (D-02 adapter Phase 7.8 que envolve motor D-01 INTOCADO), e renderiza PDF A4 portrait com 7 seções (cabeçalho escritório / identificação partes / resumo financeiro / tabela parcelas / custas judiciais / pagamentos recebidos / rodapé Art.354). Filename pattern `demonstrativo_contrato_<referencia>_<YYYYMMDD>.pdf`. Identificação no PDF é pelo **escritório** (D-pre-13), não pelo advogado individual (sem nome do advogado, sem OAB, sem assinatura). **Constraints invioláveis**: D-01 motor INTOCADO (gate dual) + D-13 CSS print preservada + Schema INTOCADO + autonomous false (UAT humano em prod obrigatório) + ZERO push até 9 SCs PASS.
</objective>

<must_haves>
truths:
  - "Operador clica '🖨️ Gerar PDF' no DetalheContrato L709-714 → download PDF imediato (sem reload)"
  - "PDF cabeçalho mostra dados escritório (4 constants NOME_ESCRITORIO + ENDERECO_ESCRITORIO + TELEFONE_ESCRITORIO + EMAIL_ESCRITORIO) + data emissão + número/referência contrato"
  - "PDF identificação mostra credor (nome + CPF/CNPJ via lookup contrato.credor_id em credores prop) + TODOS devedores (nome + CPF/CNPJ + papel + responsabilidade via listarDevedoresDoContrato + lookup devedores prop)"
  - "PDF resumo financeiro mostra 9 campos motor (valor_original + multa + honorarios + correcao + juros + art523_multa + art523_honorarios + total_atualizado + total_pago + saldo_devedor_final)"
  - "PDF tabela parcelas mostra # | Vencimento | Valor Original | Valor Atualizado | Pago | Saldo para todas dívidas regulares"
  - "PDF custas judiciais discriminadas com correção INPC por custa (workaround α D-pre-3, ignora c.pago boolean)"
  - "PDF pagamentos recebidos mostra data + valor + parcelas amortizadas (source allPagamentosDivida filtrado)"
  - "PDF rodapé com nota legal Art.354 amortização sequencial (sem assinatura individual — D-pre-13)"
  - "Filename pattern demonstrativo_contrato_<referencia_sanitizada>_<YYYYMMDD>.pdf (fallback contratoId 8 chars se referência vazia)"
  - "Loading state durante geração: botão disabled + Spinner inline (pattern canônico DetalheContrato L750-751)"
  - "Toast success ('PDF gerado com sucesso') ou error ('Erro ao gerar PDF: <msg>') via react-hot-toast"
  - "D-01 motor INTOCADO cumulative confirmed dual gate (4a working tree + 4b post-commit git log --name-only)"
  - "D-13 invariante CSS print preservada (Pattern A jsPDF não usa @media print no app principal)"
  - "Schema INTOCADO (zero Migration nova; Phase 8 é READ-only no banco)"
  - "4 constants escritório (NOME_ESCRITORIO + ENDERECO_ESCRITORIO + TELEFONE_ESCRITORIO + EMAIL_ESCRITORIO) têm valores reais — não placeholders [PREENCHER ANTES DO EXECUTE PHASE 8]"
  - "9/9 SCs UAT PASS REAL em prod (cumulative)"

artifacts:
  - path: "src/mr-3/mr-cobrancas/src/utils/pdfDemonstrativo.js"
    provides: "Utility module NEW — função pura async gerarDemonstrativoPDF(contrato, dividas, devedores, credores, allPagamentosDivida, hoje). ~300-500 linhas. Exports: gerarDemonstrativoPDF (default ou named). Constants: NOME_ESCRITORIO/ENDERECO_ESCRITORIO/TELEFONE_ESCRITORIO/EMAIL_ESCRITORIO + PAPEL_META (clone verbatim D-pre-9 de DevedoresDoContrato.jsx L10-17) + RESP_LABELS"
  - path: "src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx"
    provides: "Modified — import gerarDemonstrativoPDF + state gerandoPDF + handler handleGerarPDF + botão 🖨️ Gerar PDF L709-714 (entre Editar/Excluir). ~10-15 linhas adicionadas/modificadas"
  - "1 commit feat submódulo (mr-3) com mensagem 'feat(08): PDF Demonstrativo do Contrato — utility + UI + UAT 9/9 PASS'"
  - "1 commit chore bump pai com mensagem 'chore(08): bump submódulo mr-3 — PDF Demonstrativo SHIPPED' (body com SHA submódulo + tag + 9 SCs PASS — feedback_state_snapshot_must_list_all_phase_commits)"
  - "1 tag v1.4-phase8 no commit chore bump pai (tags v1.4 ativas: 19 → 20)"
  - "5 PAUSAs UAT (#UAT-1 a #UAT-5) com prints + autorização operador entre cada"
</must_haves>

<context>

## D-pre lockeds (cópia ID-only do CONTEXT.md — referência canonica)

Source of truth: `.planning/phases/08-pdf-demonstrativo/08-CONTEXT.md` Section 3.

| ID | Resumo | Aplicação Plan 08-01 |
|---|---|---|
| **D-pre-1** | Pattern A jsPDF programático | Task 1 Step 1.3 (loader CDN espelha L2604-2618) |
| **D-pre-2** | Utility module isolado `src/utils/pdfDemonstrativo.js` | Task 1 Step 1.4 (criar arquivo NEW) |
| **D-pre-3** | Custas workaround α — INPC por custa, ignora c.pago | Task 1 Step 1.9 seção custas (renderiza todas com `calcularFatorCorrecao`) |
| **D-pre-4** | Source motor `calcularDetalheEncargosContrato` | Task 1 Step 1.7 (chamar adapter já importado em DetalheContrato.jsx:15) |
| **D-pre-5** | Botão DetalheContrato L709-714 cor teal `#0d9488` | Task 2 Step 2.4 (inserir entre Editar/Excluir) |
| **D-pre-6** | Async data: usar `allPagamentosDivida` prop direto | Task 1 Step 1.4 (assinatura recebe prop, sem fetch adicional) |
| **D-pre-7** | Devedores via `listarDevedoresDoContrato` (skip hook/component) | Task 1 Step 1.5 (import service, await direct call) |
| **D-pre-8** | Filename `demonstrativo_contrato_<referencia>_<YYYYMMDD>.pdf` + sanitização + fallback id 8 chars | Task 1 Step 1.11 (sanitize regex + Date format YYYYMMDD) |
| **D-pre-9** | PAPEL_META clone verbatim no utility | Task 1 Step 1.2 (constants block, copiar L10-17 de DevedoresDoContrato.jsx) |
| **D-pre-10** | D-01 motor INTOCADO (gate dual cumulative) | Task 3 Step 3.3 + post-commit Task 5 |
| **D-pre-11** | D-13 CSS print preservada | Task 3 Step 3.4 |
| **D-pre-12** | Schema INTOCADO (zero Migration) | Trivialmente — Phase 8 é READ-only |
| **D-pre-13** | Identificação pelo ESCRITÓRIO (não advogado), 4 constants placeholders pré-Execute | Task 1 Step 1.1 pre-flight gate ABORT se placeholder |

## Memory feedbacks aplicáveis (10 — pre-execute)

Source of truth: `.planning/phases/08-pdf-demonstrativo/08-CONTEXT.md` Section 5.

1. `feedback_blindagens_integridade_apos_funcionamento` — walkthrough "tentar quebrar" pre-execute (contrato sem dividas/custas/pagamentos/PRINCIPAL)
2. `feedback_db_integration_gate_missing` — UAT humano essencial; gates automatizados não pegam fetch real
3. `feedback_helper_fanout_must_cover_motor_inputs` — audit shape do retorno `calcularDetalheEncargosContrato` antes de Task 1
4. `feedback_uat_humano_pega_bugs_que_gates_automatizados_nao_pegam` — Task 4 UAT humano é gate primário pra layout/charset/paginação
5. `feedback_helper_signature_mirror_motor_return_shape` — utility consume motor retorno sem adapter intermediário
6. `feedback_ux_consistency_principle_drives_scope_expansion` — **VIGILÂNCIA mid-UAT**: NÃO expandir escopo (ex: "também ficha do devedor"); defer para phase futura
7. `feedback_abort_gate_during_execution_can_reduce_scope` — Task 1 Step 1.8 ABORT pre-render se contrato vazio total
8. `feedback_memory_must_factcheck_codebase_before_claiming_severity` — severidade MÉDIA factchecked
9. `feedback_grep_global_before_pointwise_fix` — **VIGILÂNCIA UAT**: drift UI↔PDF investiga shape antes de patch pontual
10. `feedback_state_snapshot_must_list_all_phase_commits` — Task 5 commit message inclui SHA submódulo + SHA pai + tag

## Risks (6 — mitigações concretas)

Source of truth: `.planning/phases/08-pdf-demonstrativo/08-CONTEXT.md` Section 6.

| ID | Risco | Mitigação Plan 08-01 |
|---|---|---|
| R-1 | CDN jsPDF requer internet | Task 1 Step 1.3 try/catch + Toast error claro; defer npm dep pra phase futura |
| R-2 | Paginação tabular grande | Task 1 Step 1.10 helper `checkPage(yPos, needed)` reusado de imprimirFicha L1935 (auto-paginação a 280mm) |
| R-3 | Race conditions async data | Task 1 Step 1.4 await sequencial; handler async function |
| R-4 | Filename collision | Aceitar comportamento navegador "(1)/(2)" auto |
| R-5 | Charset acentos Helvetica | Task 1 Step 1.9 default Helvetica; fallback Times se UAT detectar |
| R-6 | Placeholder esquecido | Task 1 Step 1.1 pre-flight gate ABORT explícito (string não contém "PREENCHER" + length > 0) |

## Invariantes invioláveis

- **D-01 motor INTOCADO** — `devedorCalc.js` + `services/pagamentos.js` + `services/dividas.js` + `utils/masks.js` ZERO modifications. Gate dual: 4a working tree + 4b post-commit `git log -1 --format="" --name-only`.
- **D-13 CSS print preservada** — ZERO `@media print` no app principal. Pattern A jsPDF não dispara CSS print.
- **Schema INTOCADO** — zero Migration nova. Phase 8 é READ-only.
- **autonomous: false** — UAT humano em prod obrigatório, 5 PAUSAs explícitas (#UAT-1..#UAT-5). NÃO push até 9/9 SCs PASS.
- **ZERO commit/tag/push** até Task 4 PASS REAL (operador autoriza Task 5).
- **4 constants escritório** preenchidos com valores reais antes de Task 1 inicial (gate Step 1.1).

</context>

## Tasks

### Task 1 — Criar `src/utils/pdfDemonstrativo.js` (utility module NEW)

**Pre-conditions**:
- 4 constants escritório (`NOME_ESCRITORIO` + `ENDERECO_ESCRITORIO` + `TELEFONE_ESCRITORIO` + `EMAIL_ESCRITORIO`) têm valores reais (operador forneceu pré-Execute, não são `[PREENCHER ANTES DO EXECUTE PHASE 8]`)
- jsPDF CDN URL `https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.5.1/jspdf.umd.min.js` acessível (verificável via curl ou ping pré-Task 1)
- Services disponíveis: `listarDevedoresDoContrato` (services/devedoresDividas.js:95) + `calcularDetalheEncargosContrato` (utils/devedorCalc.js:849) + `calcularFatorCorrecao` (utils/correcao.js)
- **Step 1.0 PASS** (shape de retorno do `calcularDetalheEncargosContrato` mapeado e bate com Step 1.7/1.9 — feedback_helper_fanout_must_cover_motor_inputs)

**Steps**:

- **Step 1.0 — Audit shape de retorno do `calcularDetalheEncargosContrato`** (memory `feedback_helper_fanout_must_cover_motor_inputs`):
  ```bash
  # No submódulo:
  grep -A 50 "function calcularDetalheEncargosContrato" src/utils/devedorCalc.js | head -60
  ```
  Mapear shape EXATO do return antes de consumir no Step 1.7. Possíveis shapes:
  - (a) Direto: `{ valor_original, multa, honorarios, correcao, juros, art523_multa, art523_honorarios, total_atualizado, total_pago, saldo_devedor_final }`
  - (b) Aninhado em `resumo`: `{ resumo: { ... }, secoes: [...], _meta: { ... } }` (espelha `calcularPlanilhaCompleta`)
  - (c) Outra estrutura
  
  Se shape NÃO bate com Step 1.7 comment "// detalhe expõe: valor_original, ...", **ajustar Steps 1.7 + 1.9 ANTES** de implementar render. **ABORT Task 1 se shape ambíguo**, pedir audit detalhado pro operador.

- **Step 1.1 — Pre-flight gate ABORT (D-pre-13 + R-6)**:
  - Validar que cada constant tem valor real:
    ```js
    const ESCRITORIO_CONSTANTS = { NOME_ESCRITORIO, ENDERECO_ESCRITORIO, TELEFONE_ESCRITORIO, EMAIL_ESCRITORIO };
    for (const [k, v] of Object.entries(ESCRITORIO_CONSTANTS)) {
      if (!v || v.length === 0 || v.includes("PREENCHER")) {
        throw new Error(`Plan 08-01 Task 1 ABORT: constant ${k} ainda é placeholder. Preencher valor real antes de prosseguir.`);
      }
    }
    ```
  - Se ABORT disparar: parar Task 1, pedir valores pro operador, RE-iniciar Task 1 com valores reais.

- **Step 1.2 — Constants block** (topo do arquivo):
  ```js
  const NOME_ESCRITORIO = "MR Cobranças";
  const ENDERECO_ESCRITORIO = "<valor real fornecido pelo operador>";
  const TELEFONE_ESCRITORIO = "<valor real fornecido pelo operador>";
  const EMAIL_ESCRITORIO = "<valor real fornecido pelo operador>";

  // PAPEL_META clone verbatim de DevedoresDoContrato.jsx L10-17 (D-pre-9)
  const PAPEL_META = {
    PRINCIPAL:  { label: "Principal",  bg: "#fef3c7", cor: "#92400e" },
    COOBRIGADO: { label: "Coobrigado", bg: "#ede9fe", cor: "#4c1d95" },
    AVALISTA:   { label: "Avalista",   bg: "#dbeafe", cor: "#1e3a8a" },
    FIADOR:     { label: "Fiador",     bg: "#dcfce7", cor: "#14532d" },
    CONJUGE:    { label: "Cônjuge",    bg: "#fce7f3", cor: "#831843" },
    OUTRO:      { label: "Outro",      bg: "#f1f5f9", cor: "#334155" },
  };

  const RESP_LABELS = {
    SOLIDARIA:   "Solidária",
    SUBSIDIARIA: "Subsidiária",
    DIVISIVEL:   "Divisível",
  };
  ```

- **Step 1.3 — Loader CDN jsPDF** (espelha App.jsx L2604-2618):
  ```js
  async function carregarJsPDF() {
    if (window.jspdf?.jsPDF) return window.jspdf.jsPDF;
    await new Promise((resolve, reject) => {
      if (document.querySelector('script[data-jspdf]')) { setTimeout(resolve, 500); return; }
      const s = document.createElement('script');
      s.src = 'https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.5.1/jspdf.umd.min.js';
      s.setAttribute('data-jspdf', '1');
      s.onload = resolve;
      s.onerror = () => reject(new Error("Não foi possível carregar o jsPDF. Verifique sua conexão."));
      document.head.appendChild(s);
    });
    if (!window.jspdf?.jsPDF) throw new Error("jsPDF carregado mas global window.jspdf indisponível.");
    return window.jspdf.jsPDF;
  }
  ```

- **Step 1.4 — Função principal** assinatura:
  ```js
  export async function gerarDemonstrativoPDF(contrato, dividas, devedores, credores, allPagamentosDivida, hoje) {
    // ... orquestração steps 1.5-1.11
  }
  ```

- **Step 1.5 — Resolver devedores** via service (D-pre-7):
  ```js
  import { listarDevedoresDoContrato } from "../services/devedoresDividas.js";
  // ...
  const vinculos = await listarDevedoresDoContrato(contrato.id);
  const devedoresEnriquecidos = vinculos.map(v => ({
    ...v,  // { devedor_id, papel, responsabilidade }
    devedor: (devedores || []).find(d => String(d.id) === String(v.devedor_id)) || null,
  }));
  ```

- **Step 1.6 — Resolver credor** via lookup (D-pre-7 nota credor):
  ```js
  const credor = (credores || []).find(c => String(c.id) === String(contrato.credor_id)) || null;
  ```

- **Step 1.7 — Resolver cálculo motor** (D-pre-4):
  ```js
  import { calcularDetalheEncargosContrato } from "../utils/devedorCalc.js";
  // ...
  const detalhe = calcularDetalheEncargosContrato(contrato, dividas, allPagamentosDivida, hoje);
  // detalhe expõe: valor_original, multa, honorarios, correcao, juros, art523_*, total_atualizado, total_pago, saldo_devedor_final, etc.
  ```

- **Step 1.8 — ABORT GATE pre-render** (feedback_abort_gate_during_execution_can_reduce_scope):
  ```js
  const dividasRegulares = (dividas || []).filter(d => !d._so_custas && !d._nominal);
  const custasJsonb = Array.isArray(contrato?.custas) ? contrato.custas : [];
  const pagamentosContrato = (allPagamentosDivida || []).filter(/* dívidas do contrato */);

  if (dividasRegulares.length === 0 && custasJsonb.length === 0 && pagamentosContrato.length === 0) {
    throw new Error("Contrato sem dividas, custas ou pagamentos — nada para demonstrar. Cancele a operação e adicione dados.");
  }
  ```

- **Step 1.9 — Renderizar 7 seções** (helpers + paleta cores; espelha imprimirFicha L1908-1944 helpers `cabecalhoSecao`/`linha`/`hrLine`):
  - 9.a Cabeçalho: 4 constants escritório + data emissão (`new Date().toLocaleDateString("pt-BR")`) + número/referência do contrato
  - 9.b Identificação: credor (nome + CPF/CNPJ) + tabela devedores (nome + CPF/CNPJ + papel via PAPEL_META + responsabilidade via RESP_LABELS)
  - 9.c Resumo financeiro: 9 campos do `detalhe` em formato vertical (label + valor BRL); só renderizar campos > 0.005 (espelha gerarPlanilhaPDF L2687-2696)
  - 9.d Tabela parcelas: # | Vencimento | Valor Original | Valor Atualizado | Pago | Saldo (header verde institucional)
  - 9.e Custas judiciais (D-pre-3): para cada custa em `contrato.custas[]`, calcular `valorAtualizado = c.valor * calcularFatorCorrecao(c.data_despesa, hoje, contrato.indice_correcao)` — IGNORA `c.pago` boolean
  - 9.f Pagamentos recebidos: `allPagamentosDivida` filtrado por dívidas do contrato, ordenado por data crescente; mostrar data + valor + parcelas amortizadas
  - 9.g Rodapé: "Pode haver discrepância de R$ 0,01 por arredondamento. Amortização sequencial conforme Art. 354 do Código Civil." (espelha DecomposicaoSaldoModal L213) + escritório identificado no cabeçalho (D-pre-13)

- **Step 1.10 — Helper `checkPage(yPos, needed = 20)`** (R-2 mitigação, espelha imprimirFicha L1935-1938):
  ```js
  function checkPage(yPos, needed = 20) {
    if (yPos + needed > 280) { doc.addPage(); return 15; }
    return yPos;
  }
  ```

- **Step 1.11 — Sanitize filename + `doc.save()`** (D-pre-8):
  ```js
  function sanitizeFilenamePart(s) {
    return String(s || "").replace(/[^a-zA-Z0-9_-]/g, "_").substring(0, 50);
  }
  function fmtDataYYYYMMDD(d) {
    const dt = d ? new Date(d) : new Date();
    return dt.toISOString().slice(0, 10).replace(/-/g, "");
  }
  // ...
  const ref = sanitizeFilenamePart(contrato.referencia);
  const idShort = String(contrato.id || "").replace(/-/g, "").substring(0, 8);
  const datePart = fmtDataYYYYMMDD(hoje);
  const filename = ref
    ? `demonstrativo_contrato_${ref}_${datePart}.pdf`
    : `demonstrativo_contrato_${idShort}_${datePart}.pdf`;
  doc.save(filename);
  ```

**Post-conditions**:
- Arquivo `src/mr-3/mr-cobrancas/src/utils/pdfDemonstrativo.js` existe
- Exporta `gerarDemonstrativoPDF` (função pura async)
- Pre-flight gate Step 1.1 testado manualmente com placeholder → ABORT triggers (smoke test isolado)
- Função pura testável (recebe data, retorna `Promise<void>` que dispara `doc.save()`)
- ZERO modifications em motor D-01 files (devedorCalc.js + services/pagamentos.js + services/dividas.js + utils/masks.js)

---

### Task 2 — Editar `src/components/DetalheContrato.jsx`

**Pre-conditions**:
- Task 1 PASS (utility criado e testável)
- DetalheContrato.jsx L709-714 inalterado vs HEAD (audit pre-edit `git diff HEAD src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx` deve ser ZERO antes de iniciar Task 2)

**Steps**:

- **Step 2.1 — Adicionar import** no topo do arquivo (após L20 `import { invalidateContrato, removeContrato }`):
  ```js
  import { gerarDemonstrativoPDF } from "../utils/pdfDemonstrativo.js";
  ```

- **Step 2.2 — Adicionar state** após L162 (last useState `custaEmEdicao`):
  ```js
  // Phase 8 — PDF Demonstrativo do Contrato
  const [gerandoPDF, setGerandoPDF] = useState(false);
  ```

- **Step 2.3 — Adicionar handler** em escopo apropriado (após handleExcluirContrato ou similar; ~próximo a outras handlers):
  ```js
  async function handleGerarPDF() {
    setGerandoPDF(true);
    try {
      await gerarDemonstrativoPDF(contrato, dividas, devedores, credores, allPagamentosDivida, hoje);
      toast.success("PDF gerado com sucesso.");
    } catch (e) {
      toast.error("Erro ao gerar PDF: " + (e.message || "erro desconhecido"));
    } finally {
      setGerandoPDF(false);
    }
  }
  ```

- **Step 2.4 — Adicionar botão** entre "Editar Contrato" L710 e "Excluir Contrato" L711 (D-pre-5, dentro do `<div style={{ display: "flex", gap: 8 }}>` L709-714):
  ```jsx
  <Btn color="#0d9488" sm onClick={handleGerarPDF} disabled={gerandoPDF}>
    {gerandoPDF ? <Spinner /> : "🖨️ Gerar PDF"}
  </Btn>
  ```
  Posição: entre `<Btn ... onClick={() => setEditando(true)}>Editar Contrato</Btn>` e `<Btn ... onClick={handleExcluirContrato}>...</Btn>`.

**Post-conditions**:
- Botão "🖨️ Gerar PDF" visível no header DetalheContrato em prod
- Click dispara `handleGerarPDF` → utility → download PDF
- Loading state: botão `disabled={gerandoPDF}` + Spinner inline durante geração
- Toast feedback (success/error) pós-geração
- ZERO modifications em motor D-01 files (gate Task 3 confirma)

---

### Task 3 — Pre-flight gates

**Pre-conditions**:
- Task 1 + Task 2 completas

**Steps**:

- **Step 3.1 — Build** no submódulo:
  ```bash
  cd src/mr-3/mr-cobrancas
  npm run build
  ```
  Esperado: `vite build` PASS sem erros. `prebuild` hook auto-roda `test:regressao` (Step 3.2).

- **Step 3.2 — Testes regressão motor**:
  ```bash
  npm run test:regressao
  ```
  Esperado: 34/34 PASS (8 test files: calculos.test.js + saldoAtualizadoCache.test.js + custasCache.test.js + processosJudiciaisFilter.test.js + agruparPagamentosPorDevedor.test.js + devedoresDividasContrato.test.js + agruparPagamentosPorDevedorIncluindoSolidarios.test.js + dividasPorDevedorIncluindoSolidarios.test.js).

- **Step 3.3 — Gate D-01 working tree** (D-pre-10, 4a):
  ```bash
  git diff HEAD --name-only src/utils/devedorCalc.js src/services/pagamentos.js src/services/dividas.js src/utils/masks.js
  ```
  Esperado: **ZERO output** (motor INTOCADO em working tree).

- **Step 3.4 — Gate D-13** (D-pre-11):
  Confirmar que **ZERO `@media print` foi adicionado ao app principal** (apenas em strings template HTML pop-up):
  ```bash
  # Grep amplo
  grep -rn "@media print" src/mr-3/mr-cobrancas/src/ --include="*.css" --include="*.jsx" --include="*.js"
  ```
  Esperado: matches **apenas** em DecomposicaoSaldoModal.jsx (L198) + GerarPeticao.jsx (L1011) — strings template HTML pop-up. ZERO matches em CSS principal ou utility novo.

- **Step 3.5 — Lint check** (se script configurado):
  ```bash
  npm run lint 2>&1 || echo "lint script not configured (skip — gate efetivo é build + test:regressao)"
  ```
  Esperado: PASS ou skip se script não existir.

**Post-conditions**:
- Build PASS
- test:regressao 34/34 PASS
- Gate D-01 PASS (ZERO modifications em motor)
- Gate D-13 PASS (CSS print apenas em template strings pop-up)
- Lint PASS ou skip

---

### Task 4 — UAT humano em prod (9 SCs, 5 PAUSAs)

**Pre-conditions**:
- Task 3 PASS (gates automatizados verdes)
- Vercel deploy "Ready" do submódulo HEAD com Task 1 + Task 2 (verificar dashboard Vercel)
- Operador tem contrato real disponível (pre-check: pelo menos 1 contrato em prod com >= 1 dívida regular)
- **Pre-check M L FRIOS executado ANTES de PAUSA #UAT-2** (descobrir se cobaia será necessária — ver passos 1-4 da PAUSA #UAT-2 abaixo)
- **OPCIONAL** (SC-3 contrato cobaia, apenas se pre-check M L FRIOS retornar NÃO): autorização explícita do operador antes de criar cobaia advair (1 PRINCIPAL + 1 COOBRIGADO advair, devedor de teste id=8) — anotar contrato_id pra deletar via UI Excluir Contrato pós-SC-3 PASS

**Steps**:

- **PAUSA #UAT-1 — SC-1 + SC-2** (botão visível + filename pattern):
  - Operador abre prod, navega para DetalheContrato de 1 contrato real
  - **SC-1**: confirma botão "🖨️ Gerar PDF" visível entre "Editar Contrato" e "Excluir Contrato" (cor teal #0d9488)
  - **SC-2**: clica botão, baixa PDF, valida nome do arquivo:
    - Cenário (a): contrato com referência → `demonstrativo_contrato_<ref>_20260501.pdf` (ou data atual)
    - Cenário (b) [opcional]: contrato sem referência → `demonstrativo_contrato_<id8>_20260501.pdf`
  - Operador reporta: SC-1 PASS/FAIL + SC-2 PASS/FAIL + print do download
  - **Se PASS**: autoriza PAUSA #UAT-2

- **PAUSA #UAT-2 — SC-3** (identificação credor + devedores com papéis):
  - Operador abre o PDF baixado em PAUSA #UAT-1
  - **SC-3**: valida cabeçalho/identificação:
    - Credor: nome + CPF/CNPJ visíveis
    - **TODOS** devedores listados com papel (PRINCIPAL / COOBRIGADO / etc.) + responsabilidade (SOLIDARIA / SUBSIDIARIA / DIVISIVEL) + CPF/CNPJ
  - **Pre-check OBRIGATÓRIO antes de SC-3**:
    1. Operador busca em prod o contrato M L FRIOS UUID `335a2ad2-...` (ou similar contrato multi-devedor real conhecido)
    2. Confirma que contrato ainda existe **E** tem múltiplos devedores (2+, com pelo menos 1 PRINCIPAL e 1 não-PRINCIPAL)
    3. Se **SIM** → usar contrato real direto (sem criar cobaia, ZERO risco de lixo em prod)
    4. Se **NÃO** (contrato sumiu ou foi convertido pra single-devedor):
       - Operador autoriza explicitamente criação de cobaia (PAUSA decisão antes de criar)
       - Cobaia: 1 PRINCIPAL + 1 COOBRIGADO advair (devedor de teste id=8)
       - Anotar contrato_id da cobaia
       - **APÓS SC-3 PASS: deletar contrato cobaia VIA UI Excluir Contrato** (não SQL direto, exercitando flow real)
       - Confirmar deleção em prod (Módulo Contratos não mostra mais)
  - **ABORT Task 4 se cobaia for criada mas deleção falhar**. Operador autoriza próximos SCs apenas após cobaia limpa.
  - Operador reporta: SC-3 PASS/FAIL + print da seção identificação
  - **Se PASS**: autoriza PAUSA #UAT-3

- **PAUSA #UAT-3 — SC-4 + SC-5 + SC-6** (3 tabelas: parcelas + custas + pagamentos):
  - Operador continua no PDF aberto
  - **SC-4**: valida tabela parcelas com 6 colunas (# | Vencimento | Valor Original | Valor Atualizado | Pago | Saldo). "Valor Atualizado" reflete encargos. "Saldo" = atualizado - pago. Cross-check com UI DetalheContrato (drift UI↔PDF detectado → vigilância feedback_grep_global_before_pointwise_fix)
  - **SC-5**: valida custas judiciais discriminadas (descrição + data despesa + valor original + valor atualizado INPC). `c.pago` boolean ignorado (D-pre-3 workaround α)
  - **SC-6**: valida pagamentos recebidos (data + valor + parcelas amortizadas). Ordenado data crescente
  - Operador reporta: SC-4 + SC-5 + SC-6 PASS/FAIL + prints das 3 tabelas
  - **Se PASS**: autoriza PAUSA #UAT-4

- **PAUSA #UAT-4 — SC-7** (rodapé Art.354 + cabeçalho 4 constants):
  - Operador valida:
    - **SC-7**: rodapé com nota legal "amortização sequencial conforme Art. 354 do Código Civil"
    - Cabeçalho com 4 constants escritório preenchidos (NÃO `[PREENCHER ANTES DO EXECUTE PHASE 8]` visível) — D-pre-13
    - Sem nome do advogado, sem OAB, sem assinatura individual
  - Operador reporta: SC-7 PASS/FAIL + print do cabeçalho + rodapé
  - **Se PASS**: autoriza PAUSA #UAT-5

- **PAUSA #UAT-5 — SC-8 + SC-9** (loading state + toast):
  - Operador re-clica botão "🖨️ Gerar PDF" para nova geração
  - **SC-8**: confirma durante geração: botão fica disabled + Spinner inline (espelha L750-751 Salvar)
  - **SC-9**: confirma toast "PDF gerado com sucesso" pós-geração via react-hot-toast
  - **Bonus** (opcional): testar erro forçando offline (Wi-Fi off) — toast error claro "Erro ao gerar PDF: ..."
  - Operador reporta: SC-8 + SC-9 PASS/FAIL
  - **Se 9/9 PASS**: autoriza Task 5 commit/tag/push

**Post-conditions**:
- 9/9 SCs PASS REAL em prod (ou 8/9 com 1 PASS parcial documentado e operador autoriza)
- Contrato cobaia advair (se criado em #UAT-2) deletado pós-PASS
- Operador autoriza Task 5

---

### Task 5 — Commit + Tag + Push

**Pre-conditions**:
- Task 4 PASS (operador autoriza)
- Working tree limpo no submódulo exceto Task 1 + Task 2 changes

**Steps**:

- **Step 5.1 — Stage submódulo**:
  ```bash
  cd src/mr-3/mr-cobrancas
  git add src/utils/pdfDemonstrativo.js src/components/DetalheContrato.jsx
  git status --short
  ```
  Esperado: `A  src/utils/pdfDemonstrativo.js` + `M  src/components/DetalheContrato.jsx` (apenas).

- **Step 5.2 — Commit submódulo**:
  ```bash
  git commit -m "$(cat <<'EOF'
  feat(08): PDF Demonstrativo do Contrato — utility + UI integration + UAT 9/9 PASS

  Novo utility src/utils/pdfDemonstrativo.js (~300-500 linhas) renderiza
  PDF A4 portrait com 7 seções (cabeçalho escritório + identificação
  partes + resumo financeiro + tabela parcelas + custas INPC + pagamentos
  + rodapé Art.354). DetalheContrato.jsx ganha botão "🖨️ Gerar PDF"
  no header (L709-714, cor teal #0d9488) com state gerandoPDF +
  handler async + Spinner loading.

  D-pre-13: identificação pelo ESCRITÓRIO (NOME/ENDERECO/TELEFONE/EMAIL),
  sem nome do advogado, sem OAB, sem assinatura individual.

  Pre-flight gate Task 1.1: 4 constants escritório preenchidos com valores
  reais (não placeholders).

  D-pre-3 workaround α: custas exibidas com correção INPC por custa,
  ignora c.pago boolean (motor já cobra INPC corretamente).

  D-01 motor INTOCADO cumulative dual gate confirmed (devedorCalc +
  services/pagamentos + services/dividas + masks zero modifications).
  D-13 CSS print preservada (Pattern A jsPDF não dispara @media print).
  Schema INTOCADO (zero Migration).

  UAT prod 9/9 SCs PASS REAL (5 PAUSAs operador autorizadas).

  Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
  EOF
  )"
  ```

- **Step 5.3 — Push submódulo**:
  ```bash
  git push origin main
  ```
  Capturar SHA do commit (ex: `<sha-submod>`).

- **Step 5.4 — Stage pai (chore bump)**:
  ```bash
  cd <root>
  git add src/mr-3
  git status --short
  ```
  Esperado: `M  src/mr-3` (apenas pointer bump).

- **Step 5.5 — Commit chore bump pai** (feedback_state_snapshot_must_list_all_phase_commits):
  ```bash
  git commit -m "$(cat <<'EOF'
  chore(08): bump submódulo mr-3 — PDF Demonstrativo SHIPPED

  Aponta pro submódulo <sha-submod> (feat PDF Demonstrativo + UAT 9/9 PASS).

  Tag v1.4-phase8 criada neste commit (TAG TARGET).

  Phase 8 SHIPPED — milestone v1.4 wrap. 19 → 20 tags v1.4 ativas.

  Backlog v1.5 herda: Phase 7.10.bug2 sub-bug 2 (BACKLOG MÉDIA-BAIXA) +
  Phase 7.10.bug4 (BACKLOG BAIXA) + Phase 7.10.bug5/bug6 (BACKLOG BAIXA)
  + Phase 7.13d.bug2 (BACKLOG BAIXA-MÉDIA).

  D-01 motor INTOCADO cumulative confirmed dual gate (4a working tree
  + 4b post-commit git log --name-only). D-13 CSS print preservada.
  Schema INTOCADO.

  Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
  EOF
  )"
  ```

- **Step 5.6 — Tag v1.4-phase8**:
  ```bash
  git tag -a v1.4-phase8 -m "Phase 8 PDF Demonstrativo SHIPPED — milestone v1.4 wrap (Tag pai apenas — feedback_tag_push_apenas_no_pai_chore_bump)"
  ```

- **Step 5.7 — Push pai + tag**:
  ```bash
  git push origin master
  git push origin v1.4-phase8
  ```

- **Step 5.8 — Verificação post-push**:
  ```bash
  git rev-parse origin/master  # bate com HEAD local
  git status -sb               # zero ahead/behind
  git tag -l "v1.4*" | wc -l   # 20 tags
  ```

**Post-conditions**:
- Commit submódulo `<sha-submod>` push REMOTE main
- Commit chore bump pai `<sha-pai>` push REMOTE master
- Tag `v1.4-phase8` push REMOTE
- 20 tags v1.4 ativas (era 19)
- Drift legado intocado fora do stage
- Phase 8 SHIPPED — milestone v1.4 wrap completo

---

<verification>

## Gates automatizados (Task 3)

- ✅ `npm run build` PASS (Step 3.1)
- ✅ `npm run test:regressao` 34/34 PASS (Step 3.2)
- ✅ Gate D-01 working tree — ZERO modifications em motor (Step 3.3)
- ✅ Gate D-13 — `@media print` apenas em template strings pop-up (Step 3.4)
- ✅ Lint PASS ou skip (Step 3.5)

## Gates UAT (Task 4)

- ✅ SC-1 PASS — botão "🖨️ Gerar PDF" visível header DetalheContrato L709-714 (PAUSA #UAT-1)
- ✅ SC-2 PASS — filename pattern `demonstrativo_contrato_<ref>_<YYYYMMDD>.pdf` (PAUSA #UAT-1)
- ✅ SC-3 PASS — credor + TODOS devedores com papéis + CPF/CNPJ (PAUSA #UAT-2)
- ✅ SC-4 PASS — tabela parcelas 6 colunas (PAUSA #UAT-3)
- ✅ SC-5 PASS — custas com INPC (PAUSA #UAT-3)
- ✅ SC-6 PASS — pagamentos recebidos (PAUSA #UAT-3)
- ✅ SC-7 PASS — rodapé Art.354 + 4 constants escritório (PAUSA #UAT-4)
- ✅ SC-8 PASS — loading state Spinner (PAUSA #UAT-5)
- ✅ SC-9 PASS — toast success/error (PAUSA #UAT-5)

## Gates post-commit (Task 5)

- ✅ Gate D-01 4b post-commit — `git log -1 --format="" --name-only <sha-submod>` ZERO matches motor files
- ✅ Tag `v1.4-phase8` push REMOTE
- ✅ Origin master bate com HEAD local
- ✅ 20 tags v1.4 ativas

</verification>

<success_criteria>

- ✅ Tasks 1-5 PASS sequencial (autonomous false, PAUSAs operador entre tasks-críticas)
- ✅ 9/9 SCs UAT PASS REAL em prod (ou 8/9 com 1 PASS parcial documentado + autorização operador)
- ✅ D-01 motor INTOCADO cumulative dual gate confirmed (4a working tree + 4b post-commit)
- ✅ D-13 CSS print preservada (ZERO `@media print` no app principal; apenas em template strings DecomposicaoSaldoModal + GerarPeticao)
- ✅ Schema INTOCADO (zero Migration nova)
- ✅ 4 constants escritório (NOME + ENDERECO + TELEFONE + EMAIL) preenchidos com valores reais (não placeholders)
- ✅ Tag `v1.4-phase8` pushed REMOTE — 20 tags v1.4 ativas (era 19)
- ✅ Commit submódulo (`<sha-submod>`) + commit chore bump pai (`<sha-pai>`) + tag pai (TAG TARGET)
- ✅ Milestone v1.4 SHIPPED total — Phase 8 = wrap; v1.5 backlog herda

</success_criteria>

<output>

- ✅ 1 plan dossiê executado (Plan 08-01)
- ✅ 2 files modificados/criados:
  - `src/mr-3/mr-cobrancas/src/utils/pdfDemonstrativo.js` (NEW, ~300-500 linhas)
  - `src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx` (modified, ~10-15 linhas)
- ✅ 1 commit feat submódulo (mr-3 main)
- ✅ 1 commit chore bump pai (master)
- ✅ 1 tag `v1.4-phase8` pushed (pai apenas — feedback_tag_push_apenas_no_pai_chore_bump)
- ✅ PDF Demonstrativo funcional em prod
- ✅ Memory feedbacks consolidados pós-execute (10 aplicados pre-execute + candidatos NOVOS pós-execute identificados em LEARNINGS futuro)
- ✅ Milestone v1.4 SHIPPED — wrap completo (Phase 8 último item)

</output>
