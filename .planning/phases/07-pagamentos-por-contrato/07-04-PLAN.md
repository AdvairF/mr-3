---
phase: 07-pagamentos-por-contrato
plan: 04
type: execute
wave: 4
depends_on:
  - "07-03"
files_modified:
  - src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx
autonomous: false
requirements:
  - PAGCON-04
  - PAGCON-06

must_haves:
  truths:
    - "Seção 'Pagamentos Recebidos' colapsável existe no DetalheContrato, começa recolhida"
    - "Ao expandir pela primeira vez, lazy-load via useEffect busca pagamentos do contrato"
    - "Lista mostra: DATA | VALOR | PARCELAS AMORTIZADAS | OBSERVAÇÃO | [X] para cada pagamento"
    - "Coluna PARCELAS exibe texto 'Parc. 1/12, 2/12' mapeando parcelas_ids para números de sequência"
    - "Botão [X] exibe window.confirm antes de excluir; após confirmar, chama excluirPagamentoContrato"
    - "Exclusão mostra spinner na linha afetada (excluindoPagamentoId) sem bloquear outras linhas"
    - "Toast 'Pagamento excluído. Amortização revertida.' após exclusão bem-sucedida"
    - "PAGCON-06 é implementado como delete-only conforme D-02 — edição com estorno+re-aplicação adiada para v1.5"
  artifacts:
    - path: "src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx"
      provides: "Seção Pagamentos Recebidos colapsável com tabela e exclusão"
      contains: "Pagamentos Recebidos"
  key_links:
    - from: "useEffect lazy-load pagamentos"
      to: "listarPagamentosContrato(contrato.id)"
      via: "if (!pagamentosAberto || pagamentosCarregado) return;"
      pattern: "pagamentosAberto.*pagamentosCarregado\|pagamentosCarregado.*pagamentosAberto"
    - from: "handleExcluirPagamento"
      to: "excluirPagamentoContrato (contratos.js)"
      via: "await excluirPagamentoContrato(pagamento.id)"
      pattern: "excluirPagamentoContrato\\(pagamento"
    - from: "parcelas display"
      to: "dividas prop"
      via: "map parcelas_ids to sequence number via sorted dividas array"
      pattern: "parcelas_ids.*map\|map.*parcelas_ids"
---

<objective>
Completar DetalheContrato.jsx com a seção "Pagamentos Recebidos" colapsável (PAGCON-04) e o handler de exclusão com reversão (PAGCON-06). Esta é a última entrega da Phase 7.

Purpose: Fechar o ciclo completo — advogado registra, consulta e exclui pagamentos com reversão atômica e rastreamento no histórico.

Output: DetalheContrato.jsx completo com seção colapsável Pagamentos Recebidos, lazy-load, tabela com 5 colunas, botão [X] com confirm e spinner por linha.

PAGCON-06 scope note: PAGCON-06 (gerenciar pagamentos registrados) is implemented as delete-only per CONTEXT.md D-02. Edit with estorno+re-aplicação is explicitly deferred to v1.5. An executor reading this plan in isolation should not interpret the absence of an edit path as an oversight — it is a deliberate locked decision.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-pagamentos-por-contrato/07-CONTEXT.md
@.planning/phases/07-pagamentos-por-contrato/07-PATTERNS.md
@.planning/phases/07-pagamentos-por-contrato/07-UI-SPEC.md

<interfaces>
<!-- useEffect lazy-load análogo para histórico (DetalheContrato.jsx linhas 157-169 — após plano 07-03 será o useEffect de histórico): -->
```jsx
useEffect(() => {
  if (!historicoAberto || historicoCarregado) return;
  setHistoricoLoading(true);
  listarHistorico(contrato.id)
    .then(rows => {
      setHistorico(Array.isArray(rows) ? rows : []);
      setHistoricoCarregado(true);
    })
    .catch(e => toast.error("Erro ao carregar histórico: " + e.message))
    .finally(() => setHistoricoLoading(false));
}, [historicoAberto, contrato.id]);
```

<!-- Collapsible section header (análogo para Pagamentos Recebidos): -->
```jsx
<div
  onClick={() => setPagamentosAberto(p => !p)}
  style={{ display: "flex", alignItems: "center", justifyContent: "space-between",
           cursor: "pointer", padding: "8px 0", marginTop: 0 }}
>
  <p style={{ fontFamily: "'Space Grotesk',sans-serif", fontWeight: 700, fontSize: 13,
               color: "#0f172a", margin: 0 }}>
    Pagamentos Recebidos {pagamentosAberto ? "▲" : "▼"}
  </p>
</div>
```

<!-- State variables disponíveis após 07-03 (já declarados): -->
```jsx
const [pagamentosContrato, setPagamentosContrato] = useState([]);
const [pagamentosAberto,   setPagamentosAberto]   = useState(false);
const [pagamentosLoading,  setPagamentosLoading]  = useState(false);
const [pagamentosCarregado, setPagamentosCarregado] = useState(false);
const [excluindoPagamentoId, setExcluindoPagamentoId] = useState(null);
```

<!-- th e td já definidos como constantes no arquivo (linhas 83-84 antes do 07-03): -->
```js
const th = { padding: "8px 10px", textAlign: "left", fontWeight: 700, color: "#64748b", fontSize: 11, textTransform: "uppercase", letterSpacing: ".5px", whiteSpace: "nowrap" };
const td = { padding: "9px 10px", color: "#374151", verticalAlign: "middle" };
```

<!-- Ponto de inserção na JSX: seção Pagamentos Recebidos fica APÓS a seção 3b (botão/form de registro, já adicionada no 07-03) e ANTES do comentário {/* 4. Documentos section */} -->

<!-- D-03: formato de exibição de parcelas amortizadas -->
<!-- pagamentos_contrato.parcelas_ids: UUID[] de dividas.id amortizadas -->
<!-- dividas prop: array de todas as dividas do contrato, ordenar por data_vencimento para obter sequência -->
<!-- Fórmula: dado uuid em parcelas_ids, encontrar index+1 na lista de dividas ordenadas por data_vencimento -->
<!-- Total de parcelas: dividas.length -->
<!-- Resultado: "Parc. 1/12, 2/12, 3/12" -->
```js
// Helper inline para construir o texto de parcelas:
function buildParcelasText(parcelasIds, dividas) {
  if (!parcelasIds || parcelasIds.length === 0) return "—";
  const sorted = [...(dividas || [])].sort((a, b) =>
    (a.data_vencimento || "").localeCompare(b.data_vencimento || "")
  );
  const total = sorted.length;
  const nums = parcelasIds.map(uid => {
    const idx = sorted.findIndex(d => String(d.id) === String(uid));
    return idx >= 0 ? idx + 1 : null;
  }).filter(n => n !== null).sort((a, b) => a - b);
  if (nums.length === 0) return "—";
  return nums.map(n => `Parc. ${n}/${total}`).join(", ");
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar useEffect lazy-load e handleExcluirPagamento (PAGCON-06: delete-only per D-02)</name>
  <read_first>
    - src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx (linhas 157-175 — useEffect de histórico como analog; linhas 270-280 — região após os useEffects onde inserir o novo useEffect de pagamentos; região dos handlers para inserir handleExcluirPagamento antes de handleDocumentoAdicionado)
    - .planning/phases/07-pagamentos-por-contrato/07-PATTERNS.md (seção "useEffect lazy-load on section open" e "window.confirm + destructive action pattern" e "handleExcluirPagamento complete code")
    - .planning/phases/07-pagamentos-por-contrato/07-UI-SPEC.md (seção "Excluir Pagamento Full Flow" — especialmente step 2b: excluindoPagamentoId loading state)
  </read_first>
  <action>
**Alteração 1 — Após o useEffect de histórico (atualmente linhas 157-169, agora expandido para incluir linhas após o 07-03): inserir novo useEffect para lazy-load de pagamentos.**

Inserir APÓS o useEffect de histórico e ANTES da função handleEncargos:

```jsx
useEffect(() => {
  if (!pagamentosAberto || pagamentosCarregado) return;
  setPagamentosLoading(true);
  listarPagamentosContrato(contrato.id)
    .then(rows => {
      setPagamentosContrato(Array.isArray(rows) ? rows : []);
      setPagamentosCarregado(true);
    })
    .catch(e => toast.error("Erro ao carregar pagamentos: " + e.message))
    .finally(() => setPagamentosLoading(false));
}, [pagamentosAberto, contrato.id]);
```

**Alteração 2 — Inserir `handleExcluirPagamento` ANTES de `handleDocumentoAdicionado` (que inicia com `async function handleDocumentoAdicionado`).**

PAGCON-06 is implemented as delete-only per CONTEXT.md D-02. No edit handler is needed. Edit with estorno+re-aplicação is deferred to v1.5.

```jsx
async function handleExcluirPagamento(pagamento) {
  if (!window.confirm("Excluir este pagamento vai reverter a amortização das parcelas. Confirmar?")) return;
  setExcluindoPagamentoId(pagamento.id);
  try {
    await excluirPagamentoContrato(pagamento.id);
    toast.success("Pagamento excluído. Amortização revertida.");
    const rows = await listarPagamentosContrato(contrato.id);
    setPagamentosContrato(Array.isArray(rows) ? rows : []);
    await onCarregarTudo();
  } catch (e) {
    toast.error(e.message || "Erro ao excluir pagamento.");
  } finally {
    setExcluindoPagamentoId(null);
  }
}
```
  </action>
  <verify>
    <automated>grep -n "handleExcluirPagamento\|pagamentosAberto.*pagamentosCarregado\|setPagamentosLoading\|Excluir este pagamento\|Amortização revertida" src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `useEffect(() => { if (!pagamentosAberto || pagamentosCarregado) return;` presente no arquivo
    - `setPagamentosLoading(true)` e `setPagamentosLoading(false)` dentro do useEffect de pagamentos
    - `listarPagamentosContrato(contrato.id)` chamado no body do useEffect de pagamentos
    - `setPagamentosCarregado(true)` chamado após success do fetch
    - useEffect tem dependências `[pagamentosAberto, contrato.id]`
    - `async function handleExcluirPagamento(pagamento)` presente no arquivo
    - `window.confirm("Excluir este pagamento vai reverter a amortização das parcelas. Confirmar?")` presente exatamente (texto conforme D-02 e UI-SPEC)
    - `setExcluindoPagamentoId(pagamento.id)` chamado antes da SP
    - `excluirPagamentoContrato(pagamento.id)` chamado na função
    - `toast.success("Pagamento excluído. Amortização revertida.")` presente exatamente (texto conforme UI-SPEC)
    - `setExcluindoPagamentoId(null)` em bloco `finally`
    - `onCarregarTudo()` chamado após exclusão para sincronizar saldos
    - Nenhum handler de edição de pagamento presente — PAGCON-06 é delete-only per D-02; ausência intencional
  </acceptance_criteria>
  <done>useEffect lazy-load e handleExcluirPagamento implementados seguindo padrões estabelecidos de histórico e window.confirm. PAGCON-06 entregue como delete-only conforme D-02.</done>
</task>

<task type="auto">
  <name>Task 2: Adicionar seção JSX "Pagamentos Recebidos" colapsável com tabela e botão [X]</name>
  <read_first>
    - src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx (linhas 350-400 após 07-03 — região entre o form de registro 3b e o comentário {/* 4. Documentos section */}, para confirmar ponto exato de inserção)
    - .planning/phases/07-pagamentos-por-contrato/07-UI-SPEC.md (seção "3c Pagamentos Recebidos Collapsible Section" — especificação completa da tabela, empty state, loading state, delete button)
    - .planning/phases/07-pagamentos-por-contrato/07-PATTERNS.md (seção "Collapsible section toggle header" e "Collapsible section body with empty state")
  </read_first>
  <action>
Inserir o bloco JSX da seção "Pagamentos Recebidos" APÓS o bloco `{registrandoPagamento && ...}` (adicionado no 07-03) e ANTES do comentário `{/* 4. Documentos section */}`.

O bloco começa com o toggle header e o conteúdo colapsável. Incluir a função helper `buildParcelasText` como função inline antes do return do componente (ou como função aninhada na JSX):

**Passo 1 — Inserir helper `buildParcelasText` antes do `return (` do componente:**
```jsx
function buildParcelasText(parcelasIds, dividas) {
  if (!parcelasIds || parcelasIds.length === 0) return "—";
  const sorted = [...(dividas || [])].sort((a, b) =>
    (a.data_vencimento || "").localeCompare(b.data_vencimento || "")
  );
  const total = sorted.length;
  const nums = parcelasIds
    .map(uid => {
      const idx = sorted.findIndex(d => String(d.id) === String(uid));
      return idx >= 0 ? idx + 1 : null;
    })
    .filter(n => n !== null)
    .sort((a, b) => a - b);
  if (nums.length === 0) return "—";
  return nums.map(n => `Parc. ${n}/${total}`).join(", ");
}
```

**Passo 2 — Inserir bloco JSX da seção 3c após o form de registro (bloco `{registrandoPagamento && ...}`):**

```jsx
      {/* 3c. Pagamentos Recebidos — seção colapsável */}
      <div
        onClick={() => setPagamentosAberto(p => !p)}
        style={{
          display: "flex",
          alignItems: "center",
          justifyContent: "space-between",
          cursor: "pointer",
          padding: "8px 0",
          marginTop: 0,
        }}
      >
        <p style={{
          fontFamily: "'Space Grotesk',sans-serif",
          fontWeight: 700,
          fontSize: 13,
          color: "#0f172a",
          margin: 0,
        }}>
          Pagamentos Recebidos {pagamentosAberto ? "▲" : "▼"}
        </p>
      </div>

      {pagamentosAberto && (
        <div style={{
          background: "#fff",
          borderRadius: 12,
          padding: "16px 20px",
          border: "1px solid #e2e8f0",
          marginTop: 8,
          marginBottom: 16,
        }}>
          {pagamentosLoading && (
            <p style={{ textAlign: "center", fontSize: 13, color: "#94a3b8", padding: "16px 0" }}>
              Carregando pagamentos...
            </p>
          )}

          {!pagamentosLoading && pagamentosContrato.length === 0 && (
            <div style={{ textAlign: "center", padding: "24px 0", color: "#94a3b8" }}>
              <p style={{ fontWeight: 700, fontSize: 13, marginBottom: 4 }}>Nenhum pagamento registrado.</p>
            </div>
          )}

          {!pagamentosLoading && pagamentosContrato.length > 0 && (
            <div style={{ overflowX: "auto" }}>
              <table style={{ width: "100%", borderCollapse: "collapse", fontSize: 12 }}>
                <thead>
                  <tr style={{ background: "#f8fafc" }}>
                    <th style={th}>DATA</th>
                    <th style={th}>VALOR</th>
                    <th style={th}>PARCELAS AMORTIZADAS</th>
                    <th style={th}>OBSERVAÇÃO</th>
                    <th style={{ ...th, width: 40 }}></th>
                  </tr>
                </thead>
                <tbody>
                  {pagamentosContrato.map(pagamento => (
                    <tr key={pagamento.id} style={{ borderBottom: "1px solid #f1f5f9" }}>
                      <td style={td}>{fmtData(pagamento.data_pagamento)}</td>
                      <td style={td}>{fmtBRL(pagamento.valor)}</td>
                      <td style={{ ...td, fontSize: 12, color: "#374151" }}>
                        {buildParcelasText(pagamento.parcelas_ids, dividas)}
                      </td>
                      <td style={td}>{truncate(pagamento.observacao || "—", 60)}</td>
                      <td style={{ ...td, textAlign: "center" }}>
                        {excluindoPagamentoId === pagamento.id ? (
                          <Spinner />
                        ) : (
                          <button
                            onClick={() => handleExcluirPagamento(pagamento)}
                            style={{
                              background: "none",
                              border: "none",
                              cursor: "pointer",
                              fontSize: 13,
                              color: "#dc2626",
                              fontWeight: 700,
                              padding: "0 4px",
                            }}
                            title="Excluir pagamento"
                          >
                            [X]
                          </button>
                        )}
                      </td>
                    </tr>
                  ))}
                </tbody>
              </table>
            </div>
          )}
        </div>
      )}
```

O bloco `{/* 3c. Pagamentos Recebidos */}` deve terminar antes do comentário `{/* 4. Documentos section */}`.
  </action>
  <verify>
    <automated>grep -n "Pagamentos Recebidos\|buildParcelasText\|pagamentosAberto.*▲\|PARCELAS AMORTIZADAS\|Nenhum pagamento registrado\|Carregando pagamentos\|excluindoPagamentoId.*Spinner\|handleExcluirPagamento.*pagamento\b" src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx</automated>
  </verify>
  <acceptance_criteria>
    - Função `buildParcelasText(parcelasIds, dividas)` definida antes do `return (` do componente
    - buildParcelasText: ordena dividas por data_vencimento, mapeia UUID → index+1, formata como "Parc. N/total"
    - Toggle header com `onClick={() => setPagamentosAberto(p => !p)}` e texto "Pagamentos Recebidos {pagamentosAberto ? '▲' : '▼'}" presente
    - Conteúdo condicional `{pagamentosAberto && (` com card border="#e2e8f0" presente
    - Estado loading: `pagamentosLoading` → texto "Carregando pagamentos..." com color "#94a3b8" presente
    - Estado empty: `pagamentosContrato.length === 0` → `<p>Nenhum pagamento registrado.</p>` presente
    - Tabela com 5 colunas: DATA, VALOR, PARCELAS AMORTIZADAS, OBSERVAÇÃO, e coluna de ação sem header
    - Célula DATA: `fmtData(pagamento.data_pagamento)` presente
    - Célula VALOR: `fmtBRL(pagamento.valor)` presente
    - Célula PARCELAS: `buildParcelasText(pagamento.parcelas_ids, dividas)` presente
    - Célula OBSERVAÇÃO: `truncate(pagamento.observacao || "—", 60)` presente (reutiliza helper existente)
    - Botão [X]: `excluindoPagamentoId === pagamento.id ? <Spinner /> : <button ...>[X]</button>` presente
    - Botão [X]: color="#dc2626" e `onClick={() => handleExcluirPagamento(pagamento)}` presente
    - Seção 3c posicionada ANTES do comentário `{/* 4. Documentos section */}` (verificar via grep de números de linha)
    - Nenhum botão ou handler de "Editar pagamento" presente — PAGCON-06 é delete-only per D-02
  </acceptance_criteria>
  <done>Seção "Pagamentos Recebidos" completamente implementada: colapsável, lazy-load, 5 colunas, parcelas como texto, [X] com spinner por linha, empty e loading states conforme UI-SPEC. PAGCON-06 entregue como delete-only conforme D-02.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Phase 7 completa: form de registro de pagamento, seção Pagamentos Recebidos, exclusão com reversão, lazy-load, TIPO_EVENTO_LABELS, todas as migrations SQL prontas.</what-built>
  <how-to-verify>
    1. Execute as 4 migrations do arquivo `.planning/phases/07-pagamentos-por-contrato/SQL-MIGRATIONS.md` no Supabase SQL Editor na ordem: Migration 3 → Migration 5 → Migration 4a → Migration 4b.
    2. Inicie o servidor de desenvolvimento: `cd src/mr-3/mr-cobrancas && npm run dev`
    3. Abra um contrato com pelo menos 2 parcelas em aberto no DetalheContrato
    4. Verifique: botão "Registrar Pagamento" aparece entre o financial summary e a seção Documentos
    5. Clique "Registrar Pagamento" — form inline aparece com campos Data, Valor (R$), Observação
    6. Tente salvar com valor 0 — toast "Informe um valor maior que zero." deve aparecer
    7. Tente salvar com data futura — toast "Data não pode ser futura." deve aparecer
    8. Tente salvar com valor maior que saldo — toast "Valor superior ao saldo devedor (R$ X,XX)." deve aparecer
    9. Preencha data válida, valor válido (ex: valor de uma parcela), clique Salvar — toast "Pagamento registrado. N parcela(s) amortizada(s)." deve aparecer; form fecha
    10. Expanda "Pagamentos Recebidos" — deve mostrar o pagamento registrado com data, valor, parcelas (ex: "Parc. 1/6") e observação
    11. Expanda "Histórico" — deve mostrar evento "Pagamento recebido" com data e valor
    12. Clique [X] no pagamento registrado — confirm aparece; confirme — toast "Pagamento excluído. Amortização revertida."
    13. Verifique que a parcela que estava quitada voltou para "em aberto" (expandir o documento e verificar o saldo)
    14. Expanda "Histórico" novamente — deve mostrar evento "Pagamento revertido"
    15. Feche e abra outro contrato — verifique que a seção Pagamentos Recebidos começa recolhida (reset)
  </how-to-verify>
  <resume-signal>Type "approved" if all 15 steps pass, or describe which steps failed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User click [X] → handleExcluirPagamento | pagamento.id vem de dados já carregados do banco (não de input do usuário) |
| buildParcelasText input | parcelas_ids é UUID[] do banco; dividas é prop validada do contrato atual |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | Tampering | handleExcluirPagamento pagamento.id | mitigate | pagamento.id é UUID do banco, não input de formulário; window.confirm obrigatório antes da ação destrutiva; SP verifica existência no banco |
| T-07-04-02 | Denial of Service | buildParcelasText sort em cada render | accept | Array dividas limitado às parcelas do contrato (tipicamente < 100); sort é O(n log n) — imperceptível |
| T-07-04-03 | Elevation of Privilege | excluirPagamentoContrato chamado com id arbitrário | accept | SP valida existência do pagamento_id e pertencimento ao contrato; PostgREST valida JWT antes da chamada |
| T-07-04-04 | Repudiation | Reversão de pagamento sem log | mitigate | SP registra evento 'pagamento_revertido' em contratos_historico com snapshot; visível na seção Histórico |
</threat_model>

<verification>
```bash
# Verificar toda a Phase 7 em contratos.js:
grep -n "registrarPagamentoContrato\|excluirPagamentoContrato\|listarPagamentosContrato\|PAG_TABLE\|sb.*rpc" src/mr-3/mr-cobrancas/src/services/contratos.js

# Verificar DetalheContrato completo:
grep -c "pagamento" src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx
# Deve ser consideravelmente maior que antes da Phase 7

# Verificar que nenhum supabase.rpc() foi introduzido:
grep -rn "supabase\.rpc\b" src/mr-3/mr-cobrancas/src/
# Deve retornar 0 matches

# Verificar que TIPO_EVENTO_LABELS tem 8 entradas:
grep -A 10 "TIPO_EVENTO_LABELS" src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx | grep -c '":"'
# Deve retornar 8
```
</verification>

<success_criteria>
- Seção "Pagamentos Recebidos" colapsável implementada conforme UI-SPEC §3c
- Lazy-load via useEffect com guard pagamentosAberto + pagamentosCarregado
- buildParcelasText mapeia parcelas_ids UUIDs para "Parc. N/total" via sort por data_vencimento
- handleExcluirPagamento: window.confirm + excluirPagamentoContrato + toast + reload + onCarregarTudo
- Spinner por linha (excluindoPagamentoId) sem bloquear outras linhas
- PAGCON-06 implementado como delete-only per D-02 — sem handler de edição (intencional)
- Checkpoint human-verify: todas as 15 etapas passam
- Phase 7 complete: PAGCON-01..06 + HIS-05 todos implementados
</success_criteria>

<output>
After completion, create `.planning/phases/07-pagamentos-por-contrato/07-04-SUMMARY.md`
</output>
