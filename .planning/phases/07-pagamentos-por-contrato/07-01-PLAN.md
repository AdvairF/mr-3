---
phase: 07-pagamentos-por-contrato
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase_sql_editor_migration_3_alter_check.sql
  - supabase_sql_editor_migration_5_pagamentos_contrato.sql
  - supabase_sql_editor_migration_4a_sp_registrar.sql
  - supabase_sql_editor_migration_4b_sp_reverter.sql
autonomous: false
requirements:
  - PAGCON-02
  - PAGCON-03
  - PAGCON-05
  - PAGCON-06
  - HIS-05

must_haves:
  truths:
    - "Migration 3: contratos_historico aceita tipo_evento = 'pagamento_recebido' e 'pagamento_revertido' sem violar CHECK constraint"
    - "Tabela pagamentos_contrato existe com colunas id, contrato_id, data_pagamento, valor, observacao, parcelas_ids UUID[], created_at"
    - "SP registrar_pagamento_contrato existe, amortiza parcelas do mais antigo para mais novo, insere em pagamentos_contrato, registra em contratos_historico, retorna { parcelas_amortizadas: N, parcelas_ids: [...] }"
    - "SP reverter_pagamento_contrato existe, reverte saldo_quitado e pagamentos_divida das parcelas afetadas, remove row de pagamentos_contrato, registra 'pagamento_revertido' em contratos_historico"
    - "RLS em pagamentos_contrato usa USING(true) WITH CHECK(true) — não auth.role()='authenticated'"
    - "Desenvolvedor confirmou que todas as 4 migrations foram executadas no Supabase SQL Editor e que SELECT proname FROM pg_proc WHERE proname IN ('registrar_pagamento_contrato', 'reverter_pagamento_contrato') retorna 2 rows"
  artifacts:
    - path: ".planning/phases/07-pagamentos-por-contrato/SQL-MIGRATIONS.md"
      provides: "SQL completo das 4 migrations para execução no Supabase SQL Editor"
  key_links:
    - from: "SP registrar_pagamento_contrato"
      to: "pagamentos_contrato + pagamentos_divida + contratos_historico"
      via: "INSERT atômico dentro da stored procedure"
      pattern: "INSERT INTO public.pagamentos_contrato"
    - from: "SP reverter_pagamento_contrato"
      to: "pagamentos_divida + pagamentos_contrato + contratos_historico"
      via: "DELETE + UPDATE dentro da stored procedure"
      pattern: "DELETE FROM public.pagamentos_divida WHERE divida_id = ANY"
---

<objective>
Executar todas as migrations de banco de dados necessárias para a Phase 7 no Supabase SQL Editor. Esta é a prerequisite blocking de todos os outros planos — nenhum código de service ou UI pode funcionar sem estas migrations.

Purpose: Fornecer o SQL completo e correto para Migration 3 (ALTER CHECK), Migration 5 (CREATE TABLE pagamentos_contrato), Migration 4a (SP registrar_pagamento_contrato) e Migration 4b (SP reverter_pagamento_contrato). O executor cria o arquivo SQL-MIGRATIONS.md com o SQL para o desenvolvedor executar no Supabase.

Output: Arquivo SQL-MIGRATIONS.md em .planning/phases/07-pagamentos-por-contrato/ com as 4 migrations prontas para copiar/colar no Supabase SQL Editor.

IMPORTANT: 07-02 CANNOT proceed until the developer confirms all 4 migrations were applied (Task 2 checkpoint below).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-pagamentos-por-contrato/07-CONTEXT.md
@.planning/phases/07-pagamentos-por-contrato/07-PATTERNS.md

<interfaces>
<!-- Schema atual de contratos_historico (contratos.js linhas 7-16): -->
```sql
CREATE TABLE IF NOT EXISTS public.contratos_historico (
  id              UUID         PRIMARY KEY DEFAULT gen_random_uuid(),
  contrato_id     UUID         NOT NULL REFERENCES public.contratos_dividas(id) ON DELETE CASCADE,
  tipo_evento     TEXT         NOT NULL CHECK (tipo_evento IN (
                                 'criacao', 'alteracao_encargos', 'cessao_credito',
                                 'assuncao_divida', 'alteracao_referencia', 'outros')),
  snapshot_campos JSONB        NOT NULL DEFAULT '{}',
  usuario_id      UUID         DEFAULT auth.uid(),
  created_at      TIMESTAMPTZ  NOT NULL DEFAULT NOW()
);
```

<!-- Schema de pagamentos_divida (analog para pagamentos_contrato): -->
```sql
CREATE TABLE public.pagamentos_divida (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  divida_id UUID NOT NULL REFERENCES public.dividas(id) ON DELETE CASCADE,
  data_pagamento DATE NOT NULL,
  valor NUMERIC(15,2) NOT NULL,
  observacao TEXT,
  created_at TIMESTAMPTZ DEFAULT NOW()
);
```

<!-- RLS pattern obrigatório (CRÍTICO — NUNCA usar auth.role()='authenticated'): -->
```sql
CREATE POLICY "Acesso autenticado" ON public.<table>
  FOR ALL USING (true) WITH CHECK (true);
```

<!-- sb() para PostgREST RPC — o que a SP deve retornar para o toast D-05: -->
-- SP retorna: { "parcelas_amortizadas": N, "parcelas_ids": ["uuid1", "uuid2", ...] }
-- Component usa: toast.success(`Pagamento registrado. ${result.parcelas_amortizadas} parcela(s) amortizada(s).`)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar SQL-MIGRATIONS.md com os 4 scripts completos para o Supabase SQL Editor</name>
  <read_first>
    - .planning/ROADMAP.md (linhas 181-199 — Migration 3 já definida, Migration 4 referenciada)
    - .planning/phases/07-pagamentos-por-contrato/07-PATTERNS.md (pagamentos_contrato DDL completo, SP return contract)
    - src/mr-3/mr-cobrancas/src/services/contratos.js (linhas 1-16 — schema contratos_historico atual, registrarEvento signature)
  </read_first>
  <action>
Criar arquivo `.planning/phases/07-pagamentos-por-contrato/SQL-MIGRATIONS.md` com os 4 scripts SQL completos abaixo.

**Migration 3 — ALTER CHECK em contratos_historico:**
```sql
-- Migration 3 — Adicionar 'pagamento_recebido' e 'pagamento_revertido' ao CHECK constraint
-- Executar ANTES de qualquer código de service da Phase 7.
-- Run in Supabase SQL Editor

ALTER TABLE public.contratos_historico
  DROP CONSTRAINT IF EXISTS contratos_historico_tipo_evento_check;
ALTER TABLE public.contratos_historico
  ADD CONSTRAINT contratos_historico_tipo_evento_check
  CHECK (tipo_evento IN (
    'criacao', 'alteracao_encargos', 'cessao_credito',
    'assuncao_divida', 'alteracao_referencia', 'outros',
    'pagamento_recebido', 'pagamento_revertido'
  ));
```

**Migration 5 — CREATE TABLE pagamentos_contrato:**
```sql
-- Migration 5 — Nova tabela pagamentos_contrato
-- Run in Supabase SQL Editor

CREATE TABLE IF NOT EXISTS public.pagamentos_contrato (
  id             UUID          PRIMARY KEY DEFAULT gen_random_uuid(),
  contrato_id    UUID          NOT NULL REFERENCES public.contratos_dividas(id) ON DELETE CASCADE,
  data_pagamento DATE          NOT NULL,
  valor          NUMERIC(15,2) NOT NULL,
  observacao     TEXT,
  parcelas_ids   UUID[]        NOT NULL DEFAULT '{}',
  created_at     TIMESTAMPTZ   DEFAULT NOW()
);
ALTER TABLE public.pagamentos_contrato ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Acesso autenticado" ON public.pagamentos_contrato
  FOR ALL USING (true) WITH CHECK (true);
CREATE INDEX IF NOT EXISTS idx_pagamentos_contrato_contrato_id
  ON public.pagamentos_contrato (contrato_id, data_pagamento ASC);
```

**Migration 4a — SP registrar_pagamento_contrato:**

A SP deve:
1. Buscar todas as parcelas `dividas` onde `contrato_id = p_contrato_id` E `saldo_quitado = false`, ordenadas por `data_vencimento ASC`
2. Para cada parcela aberta em ordem: buscar pagamentos existentes em `pagamentos_divida` para calcular o saldo atual. Amortizar até esgotar `p_valor`. A amortização de cada parcela insere em `pagamentos_divida` com `valor = min(saldo_parcela, valor_restante)` e atualiza `dividas.saldo_quitado = true` se saldo zerar.
3. Inserir em `pagamentos_contrato` com `parcelas_ids = array de UUIDs das parcelas amortizadas`
4. Inserir em `contratos_historico` com `tipo_evento = 'pagamento_recebido'` e `snapshot_campos = { "valor": p_valor, "data_pagamento": p_data_pagamento, "parcelas_amortizadas": count }`
5. Retornar `JSONB` com `{ "parcelas_amortizadas": N, "parcelas_ids": [...] }`

```sql
-- Migration 4a — SP registrar_pagamento_contrato
-- Run in Supabase SQL Editor (executar APÓS Migration 3 e Migration 5)

CREATE OR REPLACE FUNCTION public.registrar_pagamento_contrato(
  p_contrato_id    UUID,
  p_data_pagamento DATE,
  p_valor          NUMERIC,
  p_observacao     TEXT DEFAULT NULL
) RETURNS JSONB
LANGUAGE plpgsql
SECURITY DEFINER
AS $$
DECLARE
  v_parcela        RECORD;
  v_saldo_parcela  NUMERIC;
  v_total_pago_parcela NUMERIC;
  v_pago           NUMERIC;
  v_restante       NUMERIC;
  v_amortizadas    UUID[] := '{}';
  v_count          INT    := 0;
  v_pagamento_id   UUID;
BEGIN
  v_restante := p_valor;

  -- Iterar parcelas abertas do contrato, da mais antiga para a mais nova
  FOR v_parcela IN
    SELECT id, valor_total, data_vencimento
    FROM public.dividas
    WHERE contrato_id = p_contrato_id
      AND saldo_quitado = false
    ORDER BY data_vencimento ASC, id ASC
  LOOP
    EXIT WHEN v_restante <= 0;

    -- Calcular saldo atual da parcela (valor_total - soma de pagamentos já registrados)
    SELECT COALESCE(SUM(valor), 0)
      INTO v_total_pago_parcela
      FROM public.pagamentos_divida
     WHERE divida_id = v_parcela.id;

    v_saldo_parcela := v_parcela.valor_total - v_total_pago_parcela;

    IF v_saldo_parcela <= 0 THEN
      CONTINUE;
    END IF;

    -- Amortizar: pagar o mínimo entre saldo da parcela e valor restante
    v_pago := LEAST(v_saldo_parcela, v_restante);

    INSERT INTO public.pagamentos_divida (divida_id, data_pagamento, valor, observacao)
    VALUES (v_parcela.id, p_data_pagamento, v_pago, p_observacao);

    v_restante := v_restante - v_pago;

    -- Quitar parcela se saldo zerou
    IF (v_saldo_parcela - v_pago) <= 0 THEN
      UPDATE public.dividas SET saldo_quitado = true WHERE id = v_parcela.id;
    END IF;

    v_amortizadas := array_append(v_amortizadas, v_parcela.id);
    v_count := v_count + 1;
  END LOOP;

  -- Inserir registro em pagamentos_contrato
  INSERT INTO public.pagamentos_contrato (contrato_id, data_pagamento, valor, observacao, parcelas_ids)
  VALUES (p_contrato_id, p_data_pagamento, p_valor, p_observacao, v_amortizadas)
  RETURNING id INTO v_pagamento_id;

  -- Registrar evento em contratos_historico (HIS-05)
  INSERT INTO public.contratos_historico (contrato_id, tipo_evento, snapshot_campos)
  VALUES (
    p_contrato_id,
    'pagamento_recebido',
    jsonb_build_object(
      'pagamento_id',        v_pagamento_id,
      'valor',               p_valor,
      'data_pagamento',      p_data_pagamento,
      'parcelas_amortizadas', v_count,
      'parcelas_ids',        v_amortizadas
    )
  );

  RETURN jsonb_build_object(
    'parcelas_amortizadas', v_count,
    'parcelas_ids',         v_amortizadas
  );
END;
$$;
```

**Migration 4b — SP reverter_pagamento_contrato:**

A SP deve:
1. Buscar o row em `pagamentos_contrato` pelo `p_pagamento_id`
2. Para cada UUID em `parcelas_ids`: deletar os registros em `pagamentos_divida` com `divida_id = uuid AND data_pagamento = pagamento.data_pagamento AND observacao IS NOT DISTINCT FROM pagamento.observacao` e setar `saldo_quitado = false` na parcela correspondente
3. Deletar o row de `pagamentos_contrato`
4. Inserir em `contratos_historico` com `tipo_evento = 'pagamento_revertido'` e snapshot do pagamento revertido

```sql
-- Migration 4b — SP reverter_pagamento_contrato
-- Run in Supabase SQL Editor (executar APÓS Migration 4a)

CREATE OR REPLACE FUNCTION public.reverter_pagamento_contrato(
  p_pagamento_id UUID
) RETURNS VOID
LANGUAGE plpgsql
SECURITY DEFINER
AS $$
DECLARE
  v_pagamento RECORD;
  v_parcela_id UUID;
BEGIN
  -- Buscar o pagamento a reverter
  SELECT id, contrato_id, data_pagamento, valor, observacao, parcelas_ids
    INTO v_pagamento
    FROM public.pagamentos_contrato
   WHERE id = p_pagamento_id;

  IF NOT FOUND THEN
    RAISE EXCEPTION 'Pagamento % não encontrado', p_pagamento_id;
  END IF;

  -- Reverter amortização para cada parcela afetada
  FOREACH v_parcela_id IN ARRAY v_pagamento.parcelas_ids
  LOOP
    -- Remover os pagamentos_divida inseridos por este pagamento de contrato
    -- Identificados por: mesma divida_id, mesma data_pagamento, mesma observacao
    DELETE FROM public.pagamentos_divida
     WHERE divida_id = v_parcela_id
       AND data_pagamento = v_pagamento.data_pagamento
       AND observacao IS NOT DISTINCT FROM v_pagamento.observacao;

    -- Reabrir parcela (pode ter sido quitada por este pagamento)
    UPDATE public.dividas
       SET saldo_quitado = false
     WHERE id = v_parcela_id;
  END LOOP;

  -- Remover o registro do pagamento de contrato
  DELETE FROM public.pagamentos_contrato WHERE id = p_pagamento_id;

  -- Registrar evento de reversão em contratos_historico (HIS-05)
  INSERT INTO public.contratos_historico (contrato_id, tipo_evento, snapshot_campos)
  VALUES (
    v_pagamento.contrato_id,
    'pagamento_revertido',
    jsonb_build_object(
      'pagamento_id',        p_pagamento_id,
      'valor',               v_pagamento.valor,
      'data_pagamento',      v_pagamento.data_pagamento,
      'parcelas_ids',        v_pagamento.parcelas_ids
    )
  );
END;
$$;
```

Criar o arquivo como `SQL-MIGRATIONS.md` com todos os 4 blocos, cada um prefixado com `-- Run in Supabase SQL Editor` e ordem de execução: Migration 3 → Migration 5 → Migration 4a → Migration 4b.
  </action>
  <verify>
    <automated>test -f ".planning/phases/07-pagamentos-por-contrato/SQL-MIGRATIONS.md" && grep -c "Run in Supabase SQL Editor" ".planning/phases/07-pagamentos-por-contrato/SQL-MIGRATIONS.md"</automated>
  </verify>
  <acceptance_criteria>
    - SQL-MIGRATIONS.md existe em .planning/phases/07-pagamentos-por-contrato/
    - Contém exatamente 4 seções marcadas com "Run in Supabase SQL Editor"
    - Migration 3: grep "pagamento_recebido.*pagamento_revertido\|pagamento_revertido.*pagamento_recebido" encontrado na cláusula CHECK
    - Migration 5: grep "CREATE TABLE IF NOT EXISTS public.pagamentos_contrato" e "parcelas_ids.*UUID\[\]" presentes
    - Migration 4a: grep "CREATE OR REPLACE FUNCTION public.registrar_pagamento_contrato" presente; corpo contém "INSERT INTO public.pagamentos_divida", "INSERT INTO public.pagamentos_contrato", "INSERT INTO public.contratos_historico", "pagamento_recebido", "RETURNS JSONB", "parcelas_amortizadas"
    - Migration 4b: grep "CREATE OR REPLACE FUNCTION public.reverter_pagamento_contrato" presente; corpo contém "DELETE FROM public.pagamentos_divida", "DELETE FROM public.pagamentos_contrato", "pagamento_revertido", "FOREACH.*IN ARRAY"
    - Migration 5 RLS: grep "USING (true) WITH CHECK (true)" presente — NÃO "auth.role()='authenticated'"
    - Ordem no arquivo: Migration 3 antes de Migration 5, Migration 5 antes de Migration 4a, Migration 4a antes de Migration 4b
  </acceptance_criteria>
  <done>SQL-MIGRATIONS.md criado com 4 migrations completas, RLS correto, SPs com PL/pgSQL completo e retorno correto.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>SQL-MIGRATIONS.md com as 4 migrations (Migration 3, 5, 4a, 4b) prontas para execução no Supabase SQL Editor.</what-built>
  <how-to-verify>
    1. Abra o Supabase SQL Editor para este projeto.
    2. Execute cada um dos 4 blocos SQL do arquivo `.planning/phases/07-pagamentos-por-contrato/SQL-MIGRATIONS.md` na ordem indicada:
       - Migration 3 (ALTER CHECK em contratos_historico)
       - Migration 5 (CREATE TABLE pagamentos_contrato + RLS + index)
       - Migration 4a (SP registrar_pagamento_contrato)
       - Migration 4b (SP reverter_pagamento_contrato)
    3. Após executar todas as 4, rode a seguinte query de verificação no SQL Editor:
       ```sql
       SELECT proname FROM pg_proc
       WHERE proname IN ('registrar_pagamento_contrato', 'reverter_pagamento_contrato');
       ```
       Resultado esperado: 2 rows (ambas as SPs presentes).
    4. Verifique também que pagamentos_contrato existe:
       ```sql
       SELECT column_name FROM information_schema.columns
       WHERE table_name = 'pagamentos_contrato';
       ```
       Resultado esperado: 7 colunas incluindo `parcelas_ids`.

    IMPORTANT: 07-02 CANNOT proceed until you confirm all 4 migrations applied successfully and the verification queries return the expected results.
  </how-to-verify>
  <resume-signal>Type "approved" after confirming both verification queries return expected results, or describe any migration errors encountered</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JS service → PostgREST RPC | Parâmetros passados via JSON POST ao endpoint `/rest/v1/rpc/registrar_pagamento_contrato` |
| SP PL/pgSQL → tabelas internas | Todas as operações ocorrem dentro do banco com SECURITY DEFINER |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Tampering | `registrar_pagamento_contrato` SP | mitigate | Parâmetros são tipados (UUID, DATE, NUMERIC) — PostgREST rejeita tipos inválidos antes de chegar na SP; SQL injection impossível via parâmetros PostgREST parametrizados |
| T-07-01-02 | Tampering | `reverter_pagamento_contrato` SP | mitigate | `p_pagamento_id UUID` tipado; NOT FOUND check lança RAISE EXCEPTION antes de qualquer DELETE |
| T-07-01-03 | Elevation of Privilege | pagamentos_contrato RLS | mitigate | `USING(true) WITH CHECK(true)` — acesso controlado pelo JWT Supabase; usuários não-autenticados são bloqueados pelo apikey/JWT no nível do PostgREST |
| T-07-01-04 | Denial of Service | SP amortização loop infinito | accept | Loop é limitado às parcelas abertas do contrato (finito); sem recursão; pior caso = N parcelas do contrato |
| T-07-01-05 | Information Disclosure | snapshot_campos em contratos_historico | accept | Dados são do próprio contrato do escritório; sem PII de terceiros além de UUIDs de parcelas |
</threat_model>

<verification>
Após executar as migrations no Supabase SQL Editor na ordem correta:

1. Migration 3 verificada: `SELECT constraint_name FROM information_schema.table_constraints WHERE table_name = 'contratos_historico' AND constraint_type = 'CHECK'` — deve retornar o constraint atualizado
2. Migration 5 verificada: `SELECT column_name, data_type FROM information_schema.columns WHERE table_name = 'pagamentos_contrato'` — deve listar 7 colunas incluindo `parcelas_ids` com tipo `ARRAY`
3. Migration 4a verificada: `SELECT proname FROM pg_proc WHERE proname = 'registrar_pagamento_contrato'` — deve retornar 1 row
4. Migration 4b verificada: `SELECT proname FROM pg_proc WHERE proname = 'reverter_pagamento_contrato'` — deve retornar 1 row
5. Teste rápido: INSERT manual em `pagamentos_contrato` com `tipo_evento = 'pagamento_recebido'` em `contratos_historico` — sem erro de CHECK violation
</verification>

<success_criteria>
- SQL-MIGRATIONS.md com 4 migrations completas, corretas e em ordem de execução
- RLS em pagamentos_contrato usa USING(true) WITH CHECK(true)
- SP registrar_pagamento_contrato retorna JSONB { parcelas_amortizadas, parcelas_ids }
- SP reverter_pagamento_contrato usa FOREACH para reverter cada parcela_id
- Ambas as SPs inserem em contratos_historico com tipo_evento correto (HIS-05)
- Desenvolvedor confirmou via checkpoint que ambas as SPs existem no banco (2 rows retornados pela query de verificação)
</success_criteria>

<output>
After completion, create `.planning/phases/07-pagamentos-por-contrato/07-01-SUMMARY.md`
</output>
