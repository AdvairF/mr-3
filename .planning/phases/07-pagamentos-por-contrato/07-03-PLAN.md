---
phase: 07-pagamentos-por-contrato
plan: 03
type: execute
wave: 3
depends_on:
  - "07-02"
files_modified:
  - src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx
autonomous: true
requirements:
  - PAGCON-01
  - PAGCON-03
  - PAGCON-05
  - HIS-05

must_haves:
  truths:
    - "Import de contratos.js estendido com registrarPagamentoContrato, excluirPagamentoContrato, listarPagamentosContrato"
    - "8 novas variáveis de estado declaradas: registrandoPagamento, salvandoPagamento, pagamentosContrato, pagamentosAberto, pagamentosLoading, pagamentosCarregado, excluindoPagamentoId, formPagamento"
    - "TIPO_EVENTO_LABELS contém pagamento_recebido: 'Pagamento recebido' e pagamento_revertido: 'Pagamento revertido'"
    - "Botão 'Registrar Pagamento' e form inline implementados conforme UI-SPEC §3b"
    - "handleRegistrarPagamento valida valor > 0, valor ≤ saldo calculado, data ≤ hoje com toasts específicos"
    - "useEffect reset para [contrato.id] inclui reset de registrandoPagamento, pagamentosAberto, pagamentosContrato, pagamentosCarregado"
  artifacts:
    - path: "src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx"
      provides: "Form de registro de pagamento + TIPO_EVENTO_LABELS atualizado"
      contains: "registrandoPagamento"
  key_links:
    - from: "handleRegistrarPagamento"
      to: "registrarPagamentoContrato (contratos.js)"
      via: "await registrarPagamentoContrato(contrato.id, { data_pagamento, valor, observacao })"
      pattern: "registrarPagamentoContrato\\(contrato\\.id"
    - from: "form inline card"
      to: "registrandoPagamento state"
      via: "conditional render {registrandoPagamento && ...}"
      pattern: "registrandoPagamento &&"
---

<objective>
Adicionar ao DetalheContrato.jsx: (a) import das 3 novas funções de contratos.js, (b) 8 novos state variables, (c) entradas de TIPO_EVENTO_LABELS para pagamento_recebido/pagamento_revertido, (d) handleRegistrarPagamento com validação completa e chamada à SP, (e) seção de registro de pagamento na JSX (botão + form inline) entre o financial summary e a seção Documentos, (f) resets no useEffect de contrato.id.

Purpose: Entregar o form de registro de pagamento funcional. O plano 07-04 depende do state e handlers criados aqui.

Output: DetalheContrato.jsx com form de pagamento operacional — advogado pode registrar pagamentos com validação e toast.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-pagamentos-por-contrato/07-CONTEXT.md
@.planning/phases/07-pagamentos-por-contrato/07-PATTERNS.md
@.planning/phases/07-pagamentos-por-contrato/07-UI-SPEC.md

<interfaces>
<!-- Import atual em DetalheContrato.jsx (linha 8): -->
```js
import { listarDocumentosPorContrato, editarContrato, cascatearCredorDevedor, registrarEvento, listarHistorico } from "../services/contratos.js";
```
<!-- Substituir por: -->
```js
import { listarDocumentosPorContrato, editarContrato, cascatearCredorDevedor, registrarEvento, listarHistorico,
         registrarPagamentoContrato, excluirPagamentoContrato, listarPagamentosContrato } from "../services/contratos.js";
```

<!-- Estado atual (linhas 107-119): -->
```jsx
const [documentos, setDocumentos] = useState([]);
const [loadingDocumentos, setLoadingDocumentos] = useState(false);
const [expandedDoc, setExpandedDoc] = useState(null);
const [saldosMap, setSaldosMap] = useState({});
const [saldosLoading, setSaldosLoading] = useState(false);
const [adicionandoDocumento, setAdicionandoDocumento] = useState(false);
const [editando,  setEditando]  = useState(false);
const [salvando,  setSalvando]  = useState(false);
const [editForm,  setEditForm]  = useState(() => initEditForm(contrato));
const [historicoAberto,    setHistoricoAberto]    = useState(false);
const [historico,          setHistorico]           = useState([]);
const [historicoLoading,   setHistoricoLoading]    = useState(false);
const [historicoCarregado, setHistoricoCarregado]  = useState(false);
```

<!-- useEffect contrato.id reset (linhas 149-155): -->
```jsx
useEffect(() => {
  setEditForm(initEditForm(contrato));
  setEditando(false);
  setHistoricoAberto(false);
  setHistorico([]);
  setHistoricoCarregado(false);
}, [contrato.id]);
```

<!-- TIPO_EVENTO_LABELS atual (linhas 45-52): -->
```js
const TIPO_EVENTO_LABELS = {
  criacao:               "Contrato criado",
  cessao_credito:        "Cessão de crédito",
  assuncao_divida:       "Assunção de dívida",
  alteracao_encargos:    "Alteração de encargos",
  alteracao_referencia:  "Alteração de referência",
  outros:                "Edição salva",
};
```

<!-- Ponto de inserção na JSX — após Financial summary card (linha ~369, após o </div> do financial summary): -->
<!-- INSERIR AQUI: botão "Registrar Pagamento" + form inline -->
<!-- ANTES do comentário {/* 4. Documentos section */} -->

<!-- listarPagamentos e calcularSaldoPorDividaIndividual já importados (linha 9): -->
```js
import { listarPagamentos, calcularSaldoPorDividaIndividual } from "../services/pagamentos.js";
```

<!-- Btn e Inp já importados (linhas 3, 7): -->
```js
import Btn from "./ui/Btn.jsx";
import { Inp } from "./ui/Inp.jsx";
```

<!-- Spinner já definido localmente (linhas 14-29) — reutilizar diretamente, sem import -->

<!-- hoje está disponível como prop do componente (linha 104): -->
```jsx
export default function DetalheContrato({ contrato, dividas, ..., hoje, ... })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atualizar import, state variables, TIPO_EVENTO_LABELS e useEffect reset</name>
  <read_first>
    - src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx (arquivo completo — linhas 1-170 para imports, state, effects; linhas 45-52 para TIPO_EVENTO_LABELS)
    - .planning/phases/07-pagamentos-por-contrato/07-UI-SPEC.md (seção "State Variables" — lista exata de 8 variáveis com tipos e valores iniciais)
    - .planning/phases/07-pagamentos-por-contrato/07-PATTERNS.md (seção "State declaration pattern" — valores iniciais exatos)
  </read_first>
  <action>
**Alteração 1 — Linha 8: estender import de contratos.js:**
```js
import { listarDocumentosPorContrato, editarContrato, cascatearCredorDevedor, registrarEvento, listarHistorico,
         registrarPagamentoContrato, excluirPagamentoContrato, listarPagamentosContrato } from "../services/contratos.js";
```

**Alteração 2 — TIPO_EVENTO_LABELS (linhas 45-52): adicionar 2 entradas novas:**
```js
const TIPO_EVENTO_LABELS = {
  criacao:               "Contrato criado",
  cessao_credito:        "Cessão de crédito",
  assuncao_divida:       "Assunção de dívida",
  alteracao_encargos:    "Alteração de encargos",
  alteracao_referencia:  "Alteração de referência",
  outros:                "Edição salva",
  pagamento_recebido:    "Pagamento recebido",
  pagamento_revertido:   "Pagamento revertido",
};
```

**Alteração 3 — Após a linha com `const [historicoCarregado, ...]` (linha 119): adicionar 8 novas variáveis de estado:**
```jsx
const [registrandoPagamento,  setRegistrandoPagamento]  = useState(false);
const [salvandoPagamento,     setSalvandoPagamento]     = useState(false);
const [pagamentosContrato,    setPagamentosContrato]    = useState([]);
const [pagamentosAberto,      setPagamentosAberto]      = useState(false);
const [pagamentosLoading,     setPagamentosLoading]     = useState(false);
const [pagamentosCarregado,   setPagamentosCarregado]   = useState(false);
const [excluindoPagamentoId,  setExcluindoPagamentoId]  = useState(null);
const [formPagamento,         setFormPagamento]         = useState({ data: "", valor: "", observacao: "" });
```

**Alteração 4 — useEffect [contrato.id] (linhas 149-155): adicionar resets para novos estados:**
```jsx
useEffect(() => {
  setEditForm(initEditForm(contrato));
  setEditando(false);
  setHistoricoAberto(false);
  setHistorico([]);
  setHistoricoCarregado(false);
  setRegistrandoPagamento(false);
  setPagamentosAberto(false);
  setPagamentosContrato([]);
  setPagamentosCarregado(false);
}, [contrato.id]);
```
  </action>
  <verify>
    <automated>grep -n "pagamento_recebido\|pagamento_revertido\|registrandoPagamento\|salvandoPagamento\|pagamentosContrato\|pagamentosAberto\|excluindoPagamentoId\|formPagamento\|registrarPagamentoContrato.*contratos\|setPagamentosCarregado" src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx</automated>
  </verify>
  <acceptance_criteria>
    - Linha de import de contratos.js contém `registrarPagamentoContrato, excluirPagamentoContrato, listarPagamentosContrato`
    - `TIPO_EVENTO_LABELS` contém as entradas `pagamento_recebido: "Pagamento recebido"` e `pagamento_revertido: "Pagamento revertido"`
    - `useState(false)` para `registrandoPagamento` presente (8 novas declarações de state)
    - `useState({ data: "", valor: "", observacao: "" })` para `formPagamento` presente — nota: campo é `data` (não `data_pagamento`)
    - `useState(null)` para `excluindoPagamentoId` presente
    - useEffect com `[contrato.id]` contém `setRegistrandoPagamento(false)`, `setPagamentosAberto(false)`, `setPagamentosContrato([])`, `setPagamentosCarregado(false)`
  </acceptance_criteria>
  <done>Imports, state, TIPO_EVENTO_LABELS e reset effect atualizados. Arquivo sem erros de sintaxe.</done>
</task>

<task type="auto">
  <name>Task 2: Adicionar handleRegistrarPagamento e seção JSX de pagamento (botão + form inline)</name>
  <read_first>
    - src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx (linhas 149-370 — após os useEffects e antes da JSX do financial summary, para encontrar o ponto de inserção exato dos handlers e da JSX)
    - .planning/phases/07-pagamentos-por-contrato/07-UI-SPEC.md (seção "§3b Registrar Pagamento Button + Inline Form" e "Interaction Contract — Registrar Pagamento Full Flow")
    - .planning/phases/07-pagamentos-por-contrato/07-PATTERNS.md (seção "Inline form toggle pattern" e "calcularSaldoPorDividaIndividual usage for D-04")
  </read_first>
  <action>
**Alteração 1 — Handlers: inserir `handleRegistrarPagamento` antes de `handleDocumentoAdicionado` (atualmente linha 273):**

```jsx
async function handleRegistrarPagamento() {
  const valor = parseFloat(formPagamento.valor);
  const data  = formPagamento.data;
  const hoje_str = typeof hoje === "string" ? hoje : hoje.toISOString().slice(0, 10);

  // PAGCON-05 — validações
  if (!valor || valor <= 0) {
    toast.error("Informe um valor maior que zero.");
    return;
  }
  if (data > hoje_str) {
    toast.error("Data não pode ser futura.");
    return;
  }

  // D-04 — calcular saldo devedor total das parcelas abertas
  const parcelasAbertas = (dividas || []).filter(d => !d.saldo_quitado);
  let saldoTotal = 0;
  for (const p of parcelasAbertas) {
    const pgtos = await listarPagamentos(p.id);
    saldoTotal += calcularSaldoPorDividaIndividual(p, pgtos, hoje_str);
  }
  if (valor > saldoTotal) {
    toast.error(`Valor superior ao saldo devedor (${fmtBRL(saldoTotal)}).`);
    return;
  }

  setSalvandoPagamento(true);
  try {
    const result = await registrarPagamentoContrato(contrato.id, {
      data_pagamento: data,
      valor,
      observacao: formPagamento.observacao || null,
    });
    const n = result?.parcelas_amortizadas ?? 0;
    toast.success(`Pagamento registrado. ${n} parcela(s) amortizada(s).`);
    setFormPagamento({ data: "", valor: "", observacao: "" });
    setRegistrandoPagamento(false);
    // Forçar reload da seção Pagamentos Recebidos
    const rows = await listarPagamentosContrato(contrato.id);
    setPagamentosContrato(Array.isArray(rows) ? rows : []);
    setPagamentosCarregado(true);
    await onCarregarTudo();
  } catch (e) {
    toast.error(e.message || "Erro ao registrar pagamento.");
  } finally {
    setSalvandoPagamento(false);
  }
}
```

**Alteração 2 — JSX: inserir bloco de pagamento APÓS o financial summary card e ANTES do comentário `{/* 4. Documentos section */}` (atualmente linha ~370):**

```jsx
      {/* 3b. Registrar Pagamento — botão e form inline */}
      {!registrandoPagamento && (
        <div style={{ marginTop: 0, marginBottom: 16 }}>
          <Btn color="#0d9488" sm onClick={() => setRegistrandoPagamento(true)}>Registrar Pagamento</Btn>
        </div>
      )}

      {registrandoPagamento && (
        <div style={{ background: "#fff", borderRadius: 16, padding: "16px 24px", marginBottom: 16, border: "1px solid #c7d2fe" }}>
          <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 12, marginBottom: 12 }}>
            <Inp
              label="Data"
              type="date"
              value={formPagamento.data}
              onChange={v => setFormPagamento(f => ({ ...f, data: v }))}
              max={typeof hoje === "string" ? hoje : hoje.toISOString().slice(0, 10)}
            />
            <Inp
              label="Valor (R$)"
              type="number"
              step="0.01"
              min="0"
              value={formPagamento.valor}
              onChange={v => setFormPagamento(f => ({ ...f, valor: v }))}
            />
          </div>
          <div style={{ marginBottom: 12 }}>
            <Inp
              label="Observação"
              value={formPagamento.observacao}
              onChange={v => setFormPagamento(f => ({ ...f, observacao: v }))}
              span
            />
          </div>
          <div style={{ display: "flex", gap: 8, justifyContent: "flex-end", marginTop: 16 }}>
            <Btn outline color="#64748b" sm
              onClick={() => { setRegistrandoPagamento(false); setFormPagamento({ data: "", valor: "", observacao: "" }); }}
              disabled={salvandoPagamento}>
              Cancelar
            </Btn>
            <Btn color="#0d9488" sm disabled={salvandoPagamento} onClick={handleRegistrarPagamento}>
              {salvandoPagamento ? <Spinner /> : "Salvar"}
            </Btn>
          </div>
        </div>
      )}
```

O bloco JSX deve ficar imediatamente antes do bloco `{/* 4. Documentos section */}`. Não deve haver espaço entre o financial summary e este novo bloco além de `marginBottom: 16` no próprio financial summary card.
  </action>
  <verify>
    <automated>grep -n "handleRegistrarPagamento\|Registrar Pagamento\|salvandoPagamento.*Spinner\|Valor superior ao saldo\|parcela.*amortizada" src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `async function handleRegistrarPagamento()` presente no arquivo
    - Validação `valor <= 0` com toast.error("Informe um valor maior que zero.") presente
    - Validação `data > hoje_str` com toast.error("Data não pode ser futura.") presente
    - Validação `valor > saldoTotal` com toast.error contendo "Valor superior ao saldo devedor" e `fmtBRL(saldoTotal)` presente
    - Chamada `registrarPagamentoContrato(contrato.id, { data_pagamento: data, valor, observacao: ... })` presente
    - Toast success com template literal contendo `parcela(s) amortizada(s)` e variável `n` presente
    - `setFormPagamento({ data: "", valor: "", observacao: "" })` para reset após sucesso presente
    - `listarPagamentosContrato(contrato.id)` chamado após sucesso para recarregar lista
    - `setPagamentosCarregado(true)` chamado após reload
    - JSX: `{!registrandoPagamento && (` com botão color="#0d9488" e texto "Registrar Pagamento" presente
    - JSX: `{registrandoPagamento && (` com card border="#c7d2fe" presente
    - Form: campo Data type="date" e campo Valor type="number" em grid 1fr 1fr presentes
    - Form: campo Observação com prop `span` para largura total presente
    - Botão Salvar com `{salvandoPagamento ? <Spinner /> : "Salvar"}` presente
    - Botão Cancelar reseta form fields E seta registrandoPagamento=false
  </acceptance_criteria>
  <done>Form "Registrar Pagamento" completamente funcional. Validações PAGCON-05 implementadas. Toast PAGCON-03 correto. Handlers seguros com try/finally.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser form → handleRegistrarPagamento | Dados de entrada: data (string), valor (number via parseFloat), observacao (text) |
| handleRegistrarPagamento → contratos.js service | Dados validados antes de chegar ao service |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Tampering | formPagamento.valor input | mitigate | `parseFloat(formPagamento.valor)` + verificação `valor <= 0` bloqueia envio de zero/negativo/NaN antes da chamada ao service |
| T-07-03-02 | Tampering | formPagamento.data input | mitigate | Comparação `data > hoje_str` bloqueia datas futuras; HTML `max={hoje}` adiciona validação client-side como primeira linha |
| T-07-03-03 | Tampering | valor > saldo devedor | mitigate | Cálculo via `calcularSaldoPorDividaIndividual` em todas as parcelas abertas; toast de erro específico com valor máximo |
| T-07-03-04 | Denial of Service | listarPagamentos loop em handleRegistrarPagamento | accept | Loop limitado às parcelas abertas do contrato (finito); dados já carregados em memória via `dividas` prop — não recarrega do banco |
</threat_model>

<verification>
```bash
# Verificar presença de todas as mensagens de toast corretas:
grep -n "Informe um valor maior que zero\|Data não pode ser futura\|Valor superior ao saldo devedor\|Pagamento registrado.*parcela.*amortizada" src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx

# Verificar form JSX inserido na posição correta (deve aparecer antes de "Documentos section"):
grep -n "Registrar Pagamento\|Documentos section\|3b\." src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx

# Verificar chamada correta ao service:
grep -n "registrarPagamentoContrato(contrato.id" src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx

# Verificar TIPO_EVENTO_LABELS:
grep -n "pagamento_recebido\|pagamento_revertido" src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx
```
</verification>

<success_criteria>
- TIPO_EVENTO_LABELS tem pagamento_recebido: "Pagamento recebido" e pagamento_revertido: "Pagamento revertido"
- 8 estados declarados: registrandoPagamento, salvandoPagamento, pagamentosContrato, pagamentosAberto, pagamentosLoading, pagamentosCarregado, excluindoPagamentoId, formPagamento
- handleRegistrarPagamento valida os 3 casos de PAGCON-05 com toasts exatos do UI-SPEC
- Form JSX posicionado entre financial summary e seção Documentos (conforme D-01 e UI-SPEC §3b)
- Reset do form e da seção após sucesso/cancelamento
- useEffect [contrato.id] reseta os 4 novos estados de pagamento
</success_criteria>

<output>
After completion, create `.planning/phases/07-pagamentos-por-contrato/07-03-SUMMARY.md`
</output>
