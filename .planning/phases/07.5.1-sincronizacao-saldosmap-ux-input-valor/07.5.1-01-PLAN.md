---
phase: 07.5.1-sincronizacao-saldosmap-ux-input-valor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx
  - src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx
autonomous: false   # Task 3 is a checkpoint:human-verify (duplo gate commit + submodule bump, SEM PUSH — user reviews before any push)
requirements: []    # Gap closure — no formal REQ-IDs. Success Criteria 1–7 in ROADMAP.md § Phase 7.5.1 são o contrato de verificação.

must_haves:
  truths:
    - "SC-1: useEffect de saldosMap em DetalheContrato.jsx tem deps [expandedDoc, hoje, dividas] (linha confirmada 173) — verificável via grep"
    - "SC-2: TabelaParcelasEditaveis.jsx usa state local de string para o input valor (permite campo vazio durante edição, sem '0' residual) — verificável via grep + leitura"
    - "SC-3: git diff HEAD~N -- src/mr-3/mr-cobrancas/src/utils/devedorCalc.js retorna vazio E idem src/services/pagamentos.js (INV — motor Art.354 literalmente intocado)"
    - "SC-4: npm run test:regressao continua 9/9 verde (motor intocado, sem teste novo)"
    - "SC-5: npm run build exit 0 (sem regressão de compilação)"
    - "SC-6: UAT localhost confirma 3 cenários (Bug C apagar zero + Bug B Saldo recalcula após editar valor + Bug A sem fantasma em parcela futura) sem F5"
    - "SC-7: Zero push executado — git log origin/main..main no submodule ≥1 commit local; git log origin/master..master no pai ≥1 commit local"
  artifacts:
    - path: "src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx"
      provides: "Input de valor da parcela aceita ficar vazio durante edição (state local string, não Number puro). Não concatena keystrokes ao '0' quando usuário apaga tudo com backspace."
      contains: "valoresStr"
    - path: "src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx"
      provides: "useEffect de saldosMap dispara quando prop dividas muda (deps [expandedDoc, hoje, dividas]) — saldo invalida automaticamente após edição de parcelas via onCarregarTudo"
      contains: "[expandedDoc, hoje, dividas]"
  key_links:
    - from: "src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx (useEffect linha 173)"
      to: "prop dividas (vinda de App.jsx via onCarregarTudo após atualizarParcelasCustom da Phase 7.5)"
      via: "dependency array [expandedDoc, hoje, dividas] — quando dividas muda, useEffect re-roda e setSaldosMap merge atualiza IDs editados"
      pattern: "\\}, \\[expandedDoc, hoje, dividas\\]\\);"
    - from: "src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx (input valor)"
      to: "state local de string (valoresStr) em vez de Number puro"
      via: "input value={valoresStr[i]} onChange=atualiza valoresStr (string) e propaga Number(valoresStr[i] || 0) para parcelas[i].valor_total"
      pattern: "valoresStr"
    - from: "Task 3 (checkpoint)"
      to: "Commit submodule main + bump pai master + SHAs reportados"
      via: "PAUSA #1 (diff review pré-commit submodule) + PAUSA #2 (status review pré-commit pai) — SEM PUSH em nenhum momento"
      pattern: "PAUSA #1|PAUSA #2"
---

<objective>
Corrigir 3 bugs cirúrgicos descobertos no UAT localhost da Phase 7.5, todos sem tocar no motor Art.354 (INVARIANTE ESTRUTURAL D-01):

1. **Bug C (Task 1)** — Input valor da parcela em `TabelaParcelasEditaveis.jsx` não apaga o "0" preexistente: state local string em vez de Number puro.
2. **Bugs A + B (Task 2)** — `saldosMap` em `DetalheContrato.jsx:173` não invalida após edição de parcelas: adicionar `dividas` às deps do useEffect. Mesma raiz para os dois bugs; fix é de **1 linha**.
3. **Checkpoint (Task 3)** — Regressão 9/9 + build + prova de invariante motor (`git diff` vazio) + PAUSA #1 (diff review) + commits submodule main + PAUSA #2 (bump review) + commit bump pai master + reportar SHAs. **SEM PUSH.**

Motor Art.354 (`utils/devedorCalc.js` + `services/pagamentos.js`) **literalmente intocado** — verificação determinística via `git diff` vazio ao fim.

Purpose: Advogado edita parcelas via "Editar parcelas" (Phase 7.5) e vê a coluna Saldo recalcular imediatamente (sem F5), sem penalidade fantasma em parcelas futuras, e consegue apagar o conteúdo do input valor sem ficar com "0" residual que concatena aos keystrokes seguintes.

Output:
- `src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx` — state `valoresStr: string[]` paralelo a `parcelas` (input usa `valoresStr[i]`; onChange atualiza string e propaga `Number(valoresStr[i] || 0)` para `parcelas[i].valor_total`); soma/validações continuam usando `parcelas[i].valor_total` numérico.
- `src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx` linha 173 — dependency array do useEffect que calcula `saldosMap` passa de `[expandedDoc, hoje]` para `[expandedDoc, hoje, dividas]`.
- Commit local no submodule `src/mr-3/mr-cobrancas` branch `main` (1 commit por task — Task 1 e Task 2) + bump SHA no repo pai branch `master` (commit único da Task 3). Zero push.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07.5.1-sincronizacao-saldosmap-ux-input-valor/07.5.1-CONTEXT.md

<!-- Phase 7.5 context (componente TabelaParcelasEditaveis e integração DetalheContrato): -->
@.planning/phases/07.5-parcelas-datas-valores-customizados/07.5-CONTEXT.md

<!-- Padrão de duplo-checkpoint (Task 3 espelha estrutura): -->
<!-- .planning/phases/07.5-parcelas-datas-valores-customizados/07.5-01-PLAN.md Task 4 = template -->

<interfaces>
<!-- Contratos-chave extraídos direto do código atual. Executor não precisa re-explorar. -->

From src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx (linhas 155–186 — useEffect de saldosMap):
```javascript
// Linha 155–173 (approx — o fim do useEffect está na linha 173 da base atual):
//
//   setSaldosLoading(true);
//   Promise.all(
//     parcelasDoDoc.map(async p => {
//       const pgtos = await listarPagamentos(p.id);
//       const saldo = calcularSaldoPorDividaIndividual(p, pgtos, hoje);
//       return [String(p.id), saldo];
//     })
//   ).then(entries => {
//     setSaldosMap(prev => ({ ...prev, ...Object.fromEntries(entries) }));   // merge — IDs existentes nunca sobrescritos sem re-run
//     setSaldosLoading(false);
//   }).catch(e => {
//     toast.error("Erro ao calcular saldos: " + e.message);
//     setSaldosLoading(false);
//   });
// }, [expandedDoc, hoje]);                                                  // ← LINHA 173: deps INCOMPLETAS. Faltam `dividas`.

// DEPOIS:
// }, [expandedDoc, hoje, dividas]);
```

From src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx (estado + input de valor):
```javascript
// Linha 48–55 — state parcelas (mantido):
const [parcelas, setParcelas] = useState(() =>
  (parcelasIniciais || []).map((p, i) => ({
    id: p.id,
    numero: p.numero != null ? p.numero : i + 1,
    valor_total: p.valor_total != null ? p.valor_total : 0,
    data_vencimento: p.data_vencimento || "",
  }))
);

// Linha 65–71 — atualizarLinha atual (Number conversion CAUSA o bug C quando campo vazio):
function atualizarLinha(i, campo, valor) {
  setParcelas(prev => {
    const next = prev.map((p, idx) => idx === i ? { ...p, [campo]: campo === "valor_total" ? Number(valor) : valor } : p);
    if (typeof onChange === "function") onChange(next);
    return next;
  });
}

// Linha 227–236 — input valor atual (value={p.valor_total} força mostrar "0"):
<input
  type="number"
  min="0"
  step="0.01"
  value={p.valor_total}                    // ← valor 0 renderiza como "0"; browser concatena keystrokes
  onChange={e => atualizarLinha(i, "valor_total", e.target.value)}
  disabled={ro}
  title={tooltip}
  style={ro ? cellInputDisabledStyle : cellInputStyle}
/>
```

**Decisão planner (entre as 2 opções do CONTEXT D-06):**
Escolhida **Opção A — state auxiliar paralelo `valoresStr: string[]`**.
- Diff é previsível (não depende de `focoAtual` / eventos onFocus/onBlur).
- Mais legível: a separação "string no input / número no state" é explícita.
- `soma`/`diff`/validações continuam usando `parcelas[i].valor_total` numérico — zero impacto em D-07 (validações Phase 7.5).

Opção B (value condicional com `editandoVazio` / `focoAtual`) descartada: adiciona state de foco por linha, complica acessibilidade e tem mais pontos de falha.

From src/mr-3/mr-cobrancas/src/utils/devedorCalc.js (linha 238 — REFERÊNCIA, NÃO MODIFICAR):
```javascript
// Linha 238 (confirmado por leitura):
if (periodoInicio && periodoInicio < hoje) {
  // Bloco de penalidades — só executa se período_inicio < hoje.
  // Parcela futura → periodoInicio >= hoje → motor pula este bloco e retorna saldo puro.
  // Logo, Bug A NÃO é bug de motor: é staleness do cache saldosMap no componente.
}
```

</interfaces>
</context>

<tasks>

<!--
  3 tasks em 1 wave SEQUENCIAL (D-10 do CONTEXT.md):
  - Task 1 → Task 2 → Task 3 (sequência rígida)
  - Task 1 e Task 2 são commits separados no submodule (subject distinto por bug)
  - Task 3 é o checkpoint humano (duplo gate + bump do pai + reporte de SHAs)
  - Não paralelizar: Task 3 precisa ver os commits de Task 1 + Task 2 no `git log` antes de bumpar o pai.
-->

<task type="auto">
  <name>Task 1: Fix Bug C — state local string no input valor de TabelaParcelasEditaveis</name>
  <files>
    src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx
  </files>

  <read_first>
    - `src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx` — componente inteiro (272 linhas). Atenção aos blocos:
      - Linhas 48–55 (`useState` de `parcelas`)
      - Linhas 65–71 (`atualizarLinha` — onde acontece `Number(valor)` para `valor_total`)
      - Linhas 73–77 (`soma` via `reduce` usando `Number(p.valor_total || 0)`)
      - Linhas 81–99 (`sugerirDatas` — setParcelas, não altera valor, só datas)
      - Linhas 112–148 (`handleSalvar` — validações usam `soma` e `parcelas`)
      - Linhas 227–236 (JSX do input valor)
    - `.planning/phases/07.5.1-sincronizacao-saldosmap-ux-input-valor/07.5.1-CONTEXT.md` — §decisions D-04, D-05, D-06 (root cause + fix correto via state local string; discretion entre opções A e B) e §specifics (as 2 opções literais)
    - `.planning/phases/07.5-parcelas-datas-valores-customizados/07.5-CONTEXT.md` — §decisions D-02 (isolamento do componente — zero novos imports proibidos), D-06 (default de valor), D-07 (validações soma/ordem/datas — devem continuar funcionando), D-08 (soma invariante)
  </read_first>

  <action>
    **Abordagem escolhida (planner — Opção A):** State auxiliar paralelo `valoresStr: string[]`. Separação explícita "string no input / número no state das parcelas". Mais legível que value condicional.

    **Passo 1 — Adicionar state `valoresStr` logo após o `useState` de `parcelas` (após linha 55):**
    ```jsx
    // Espelha o array de parcelas — 1 string por linha. Única fonte de verdade do texto renderizado no input.
    // Inicializado a partir de parcelasIniciais (ou "" se valor é 0 e queremos permitir campo vazio desde o mount — NÃO: no mount, valor_total=0 vira "0" pra manter compatibilidade visual com padrão; usuário apaga se quiser vazio).
    const [valoresStr, setValoresStr] = useState(() =>
      (parcelasIniciais || []).map(p => (p.valor_total != null ? String(p.valor_total) : "0"))
    );
    ```

    **Passo 2 — Criar um setter dedicado `atualizarValorStr(i, str)` que propaga a string para `valoresStr[i]` e, em paralelo, escreve `Number(str || 0)` em `parcelas[i].valor_total`.** Substitui o uso de `atualizarLinha(i, "valor_total", ...)` no input de valor. Exemplo literal:
    ```jsx
    function atualizarValorStr(i, str) {
      // 1) atualiza a string renderizada
      setValoresStr(prev => {
        const next = [...prev];
        next[i] = str;
        return next;
      });
      // 2) atualiza o número no state principal; "" → 0, "050" → 50 (Number normaliza), "abc" → NaN (evitar)
      const n = str === "" ? 0 : Number(str);
      const valorNumerico = Number.isFinite(n) ? n : 0;
      setParcelas(prev => {
        const next = prev.map((p, idx) => idx === i ? { ...p, valor_total: valorNumerico } : p);
        if (typeof onChange === "function") onChange(next);
        return next;
      });
    }
    ```

    **Passo 3 — Atualizar o JSX do input de valor (linhas 227–236) para usar `valoresStr[i]` e `atualizarValorStr`:**
    ```jsx
    <input
      type="number"
      min="0"
      step="0.01"
      value={valoresStr[i] ?? ""}                          // ← string local, permite "" (vazio)
      onChange={e => atualizarValorStr(i, e.target.value)} // ← novo setter
      disabled={ro}
      title={tooltip}
      style={ro ? cellInputDisabledStyle : cellInputStyle}
    />
    ```
    **NÃO alterar** o input de `data_vencimento` (linhas 239–246) — continua usando `atualizarLinha(i, "data_vencimento", ...)`.

    **Passo 4 — Manter `atualizarLinha` existente intocado para uso pelo input de `data_vencimento`.** A função atual não é removida — só o input de valor deixa de usá-la.

    **Passo 5 — Sincronizar `valoresStr` quando `sugerirDatas` é chamado (linhas 81–99):** `sugerirDatas` só toca `data_vencimento`, não `valor_total`. Portanto nenhuma alteração em `sugerirDatas` é necessária. Verificar por grep que `sugerirDatas` não modifica `valor_total` — se modificar, atualizar `valoresStr` em paralelo.

    **Passo 6 — NÃO modificar as validações em `handleSalvar` (linhas 112–148).** Elas usam `parcelas` (número) e `soma` (número) — continuam corretas pois `atualizarValorStr` já escreveu o número em `parcelas[i].valor_total`.

    **Passo 7 — Rodar `npm run build` dentro de `src/mr-3/mr-cobrancas` pra validar JSX/imports. Exit 0 obrigatório.**

    **Passo 8 — Commit no submodule (branch `main`) APENAS deste arquivo.** Exatamente 1 arquivo no commit.
    ```bash
    cd src/mr-3/mr-cobrancas
    git checkout main                   # garantir branch (não detached HEAD)
    git add src/components/TabelaParcelasEditaveis.jsx
    git commit -m "$(cat <<'EOF'
    fix(07.5.1-01): input valor aceita limpar em TabelaParcelasEditaveis

    - Adicionar state auxiliar `valoresStr: string[]` paralelo a `parcelas` —
      1 string por linha, única fonte de verdade do texto renderizado no input valor.
    - Novo setter `atualizarValorStr(i, str)` atualiza `valoresStr[i]` (string)
      e propaga `Number(str || 0)` para `parcelas[i].valor_total` (número).
    - Input de valor (linha 227) passa a usar `value={valoresStr[i] ?? ""}` e `onChange={atualizarValorStr}` —
      permite campo vazio durante edição (backspace apaga tudo sem forçar "0" residual).
    - `atualizarLinha` existente MANTIDO para o input de `data_vencimento` — não afeta datas.
    - Validações `handleSalvar` (soma/ordem/datas) INALTERADAS — usam `parcelas`/`soma` numéricos.
    - Fix UAT Phase 7.5 — Bug C (input concatenava keystrokes ao "0" preexistente após apagar com backspace).
    - Motor Art.354 LITERALMENTE intocado (INV D-01/D-07 do 07.5.1-CONTEXT.md).
    - Abordagem Opção A (state auxiliar paralelo) escolhida pelo planner — diff previsível
      e mais legível que Opção B (value condicional com focoAtual).

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
    EOF
    )"
    git log -1 --format=%H              # capturar SHA pra reportar na Task 3
    ```

    **NÃO FAZER PUSH.**
  </action>

  <verify>
    <automated>
      cd src/mr-3/mr-cobrancas && \
      grep -n "valoresStr" src/components/TabelaParcelasEditaveis.jsx && \
      grep -n "value={valoresStr\[i\]" src/components/TabelaParcelasEditaveis.jsx && \
      grep -n "atualizarValorStr" src/components/TabelaParcelasEditaveis.jsx && \
      npm run build
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "const \\[valoresStr, setValoresStr\\] = useState" src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx` retorna ≥1 match
    - `grep -n "function atualizarValorStr" src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx` retorna ≥1 match
    - `grep -n "value={valoresStr" src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx` retorna ≥1 match (input valor usa string local)
    - `grep -n "value={p.valor_total}" src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx` retorna **0 matches** (o binding antigo `value={p.valor_total}` no input numérico foi substituído)
    - `grep -n "atualizarLinha" src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx` retorna ≥1 match (função continua existindo para o input de data)
    - `grep -n 'atualizarLinha(i, "data_vencimento"' src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx` retorna ≥1 match (input data continua usando `atualizarLinha`)
    - `cd src/mr-3/mr-cobrancas && npm run build` exit 0
    - `cd src/mr-3/mr-cobrancas && git diff HEAD -- src/utils/devedorCalc.js src/services/pagamentos.js` retorna **vazio** (invariante motor — este fix NÃO toca motor)
    - `cd src/mr-3/mr-cobrancas && git log -1 --format=%s` mostra subject EXATO `fix(07.5.1-01): input valor aceita limpar em TabelaParcelasEditaveis`
    - `cd src/mr-3/mr-cobrancas && git log -1 --name-only --format=` lista EXATAMENTE 1 arquivo: `src/components/TabelaParcelasEditaveis.jsx`
    - `cd src/mr-3/mr-cobrancas && git rev-parse --abbrev-ref HEAD` retorna `main`
    - `cd src/mr-3/mr-cobrancas && git status --porcelain` retorna **vazio** para `src/components/TabelaParcelasEditaveis.jsx` (**ignorar** possíveis linhas `AtrasoCell.jsx` / `FiltroDividas.jsx` com CRLF pré-existentes — fora do escopo desta phase, constraint #9 do planning_context)
    - Nenhum push executado: `cd src/mr-3/mr-cobrancas && git log origin/main..main` retorna ≥1 commit local
  </acceptance_criteria>

  <done>
    `TabelaParcelasEditaveis.jsx` modificado com state `valoresStr` + setter `atualizarValorStr` + input valor bindado em `valoresStr[i]`. Build limpo. Validações intactas. 1 commit local no submodule `main` (subject `fix(07.5.1-01): ...`, exatamente 1 arquivo modificado). Zero push. Motor Art.354 git-diff vazio.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix Bugs A + B — adicionar `dividas` às deps do useEffect de saldosMap em DetalheContrato</name>
  <files>
    src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx
  </files>

  <read_first>
    - `src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx` linhas 155–186 — useEffect de `saldosMap` que termina na **linha 173 confirmada** (`}, [expandedDoc, hoje]);`) e o useEffect seguinte em 175–186 (reset por `contrato.id` — NÃO tocar). Ler mais 15 linhas antes (linhas 140–155) pra confirmar onde o `useEffect(() => { ... }` começa e capturar a lista de deps atual.
    - `.planning/phases/07.5.1-sincronizacao-saldosmap-ux-input-valor/07.5.1-CONTEXT.md` — §decisions D-01, D-02, D-03 (root cause unificado + deps simples sem hash signature), §specifics (snippet literal ANTES/DEPOIS)
    - `.planning/phases/07.5-parcelas-datas-valores-customizados/07.5-CONTEXT.md` — §decisions D-10 (atualizarParcelasCustom chama onCarregarTudo após salvar; fluxo que torna o Bug B observável)
    - Referência intocada: `src/mr-3/mr-cobrancas/src/utils/devedorCalc.js:238` (só ler pra confirmar que bloco de penalidades é condicionado a `periodoInicio < hoje` — prova de que motor está correto e Bug A é de cache no componente, não de cálculo)
  </read_first>

  <action>
    **Fix de 1 linha (D-01 do CONTEXT.md):** adicionar `dividas` ao dependency array do useEffect que calcula `saldosMap`.

    **Passo 1 — Ler `DetalheContrato.jsx` e CONFIRMAR que a linha 173 contém exatamente:**
    ```jsx
    }, [expandedDoc, hoje]);
    ```
    Se o número de linha mudou desde a base (muito improvável entre leitura do planner e execução), **procurar pela única ocorrência do padrão literal** `[expandedDoc, hoje]` no arquivo via `grep -n "\\[expandedDoc, hoje\\]" src/components/DetalheContrato.jsx` — **espera-se exatamente 1 match**.

    **Passo 2 — Substituir a linha:**
    ```jsx
    // ANTES:
    }, [expandedDoc, hoje]);

    // DEPOIS:
    }, [expandedDoc, hoje, dividas]);
    ```

    **Padrão literal para o Edit tool** (edit pontual, sem tocar em nada mais):
    - `old_string`: `}, [expandedDoc, hoje]);`
    - `new_string`: `}, [expandedDoc, hoje, dividas]);`

    **Passo 3 — Verificar que o padrão NÃO se repete.** `grep -n "\\[expandedDoc, hoje\\]" src/components/DetalheContrato.jsx` deve retornar **0 matches após o edit** (a única ocorrência foi substituída pela nova).

    **Passo 4 — Confirmar que NÃO há outras edições no arquivo:**
    ```bash
    cd src/mr-3/mr-cobrancas
    git diff --stat src/components/DetalheContrato.jsx
    # Esperado: 1 file changed, 1 insertion(+), 1 deletion(-)  (ou 1+/-1- se count parcial)
    git diff src/components/DetalheContrato.jsx
    # Esperado: APENAS a linha com [expandedDoc, hoje, dividas] mudada. Se houver outras linhas diff, PARAR e restaurar via `git checkout HEAD -- src/components/DetalheContrato.jsx` antes de tentar de novo.
    ```

    **Passo 5 — Build:**
    ```bash
    cd src/mr-3/mr-cobrancas && npm run build
    ```
    Exit 0 obrigatório. Nenhum warning novo ESLint sobre `react-hooks/exhaustive-deps` (aliás, este fix é exatamente a resposta correta para esse warning se estivesse ativado).

    **Passo 6 — Commit no submodule (branch `main`) APENAS deste arquivo.** Exatamente 1 arquivo no commit:
    ```bash
    cd src/mr-3/mr-cobrancas
    git checkout main
    git add src/components/DetalheContrato.jsx
    git commit -m "$(cat <<'EOF'
    fix(07.5.1-02): saldosMap não invalida após edição de parcelas (bugs A + B)

    - DetalheContrato.jsx: dependency array do useEffect que calcula saldosMap
      passa de [expandedDoc, hoje] para [expandedDoc, hoje, dividas].
    - Root cause unificado (CONTEXT D-01/D-02): `setSaldosMap(prev => ({...prev, ...new}))`
      faz merge — IDs existentes nunca são sobrescritos sem re-run. Sem `dividas` nas deps,
      o useEffect não disparava quando prop `dividas` mudava (via onCarregarTudo após
      atualizarParcelasCustom da Phase 7.5). Resultado: saldos cached.
    - Bug A (R$ 39,90 fantasma em parcela reagendada pra futuro) e Bug B (Saldo não
      recalcula após editar valor R$ 300 → R$ 500) eram a mesma falha — saldosMap stale.
    - Motor Art.354 LITERALMENTE intocado (devedorCalc.js:238 só aplica penalidades
      quando periodoInicio < hoje — verificado linha-por-linha, motor correto).
    - Trade-off aceito (CONTEXT D-03/D-04): re-cálculo completo do expandedDoc a cada
      mudança de `dividas`. Se UAT futuro detectar lentidão em contratos 50+ parcelas,
      Phase 7.5.2 introduz hash signature otimizado.
    - INV D-07 preservada: git diff HEAD -- src/utils/devedorCalc.js src/services/pagamentos.js = vazio.
    - Regressão 9/9 verde (motor não tocado — invariante estrutural).

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
    EOF
    )"
    git log -1 --format=%H              # capturar SHA pra reportar na Task 3
    ```

    **NÃO FAZER PUSH.**
  </action>

  <verify>
    <automated>
      cd src/mr-3/mr-cobrancas && \
      grep -n "\[expandedDoc, hoje, dividas\]" src/components/DetalheContrato.jsx && \
      npm run build
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "\\[expandedDoc, hoje, dividas\\]" src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx` retorna **exatamente 1 match**
    - `grep -n "\\[expandedDoc, hoje\\]" src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx` retorna **0 matches** (padrão antigo foi substituído completamente)
    - `cd src/mr-3/mr-cobrancas && git diff --numstat HEAD~1 -- src/components/DetalheContrato.jsx` mostra **exatamente 1 linha adicionada e 1 linha removida** (`1  1  src/components/DetalheContrato.jsx`), E não mais que isso
    - `cd src/mr-3/mr-cobrancas && npm run build` exit 0
    - `cd src/mr-3/mr-cobrancas && git diff HEAD~2 -- src/utils/devedorCalc.js` retorna **vazio** (comparando com antes dos 2 commits 07.5.1 — motor literalmente intocado)
    - `cd src/mr-3/mr-cobrancas && git diff HEAD~2 -- src/services/pagamentos.js` retorna **vazio**
    - `cd src/mr-3/mr-cobrancas && git log -1 --format=%s` mostra subject EXATO `fix(07.5.1-02): saldosMap não invalida após edição de parcelas (bugs A + B)`
    - `cd src/mr-3/mr-cobrancas && git log -1 --name-only --format=` lista EXATAMENTE 1 arquivo: `src/components/DetalheContrato.jsx`
    - `cd src/mr-3/mr-cobrancas && git rev-parse --abbrev-ref HEAD` retorna `main`
    - `cd src/mr-3/mr-cobrancas && git status --porcelain` retorna vazio para `src/components/DetalheContrato.jsx` (ignorar CRLF pré-existentes em `AtrasoCell.jsx` / `FiltroDividas.jsx` — constraint #9)
    - Nenhum push: `cd src/mr-3/mr-cobrancas && git log origin/main..main` retorna ≥2 commits locais (os 2 fixes da 07.5.1)
  </acceptance_criteria>

  <done>
    `DetalheContrato.jsx` linha do useEffect de saldosMap passa a ter deps `[expandedDoc, hoje, dividas]`. Build limpo. Exatamente 1 arquivo modificado em 1 linha. 2 commits locais no submodule `main` (o da Task 1 + este). Zero push. Motor Art.354 git-diff vazio. Pronto para checkpoint.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Regressão 9/9 + build + prova invariante motor + PAUSA #1 (diff review) + PAUSA #2 (bump review) + commit bump pai (SEM PUSH)</name>
  <files>
    src/mr-3/mr-cobrancas/ (submodule — verificação dos 2 commits prévios em `main`)
    .gitmodules / parent repo (bump do submodule SHA em `master`)
  </files>

  <what-built>
    Tasks 1 e 2 já commitaram localmente no submodule `src/mr-3/mr-cobrancas` branch `main`:
    - **Commit A (Task 1):** `fix(07.5.1-01): input valor aceita limpar em TabelaParcelasEditaveis` — 1 arquivo (`TabelaParcelasEditaveis.jsx`).
    - **Commit B (Task 2):** `fix(07.5.1-02): saldosMap não invalida após edição de parcelas (bugs A + B)` — 1 arquivo (`DetalheContrato.jsx`).

    **Task 3 executa a sequência de verificação + 2 checkpoints humanos explícitos + bump do pai, espelhando o padrão estabelecido nas Phases 7.2 / 7.3 / 7.4 / 7.5:**

    1. Rodar `npm run test:regressao` no submodule — 9/9 obrigatório (motor Art.354 literalmente intocado, sem teste novo).
    2. Rodar `npm run build` no submodule — exit 0 (sem erro de compilação nas mudanças das Tasks 1 e 2).
    3. Rodar `git diff` dos 2 arquivos do motor Art.354 comparando com antes dos 2 commits desta phase — ambos devem retornar vazio (prova direta de D-01/D-07/INV).
    4. **PAUSA #1 (checkpoint pré-bump do pai — diff review dos commits do submodule):** mostrar `git log origin/main..main --stat` + subject/name-only dos 2 commits + `git diff HEAD~2` (diff consolidado das 2 tasks juntos, pra conferência humana). Aguardar autorização explícita do usuário (`approved` ou `commit it`) antes de prosseguir.
    5. Após PAUSA #1 autorizada: cd pra raiz do repo pai e rodar `git status` — deve mostrar apenas `src/mr-3` como modified (bump automático do ponteiro).
    6. **PAUSA #2 (checkpoint pré-commit do pai — bump review):** reportar SHAs dos 2 commits do submodule + status do pai (somente `src/mr-3` modified) + confirmar branch `master`. Aguardar autorização explícita e **SEPARADA** (`bump it` ou `approved`).
    7. Após PAUSA #2 autorizada: commit único no repo pai (branch `master`) com mensagem `chore(07.5.1): bump submodule mr-3 — saldosMap sync + input UX (07.5.1)`.
    8. Reportar SHAs finais + instruções de UAT em localhost. **NÃO FAZER PUSH em nenhum momento.**

    Os commits das Tasks 1 e 2 NÃO são undone nem rebase-ados. A Task 3 só **verifica + aprova + bumpa o pai**.
  </what-built>

  <read_first>
    - `.planning/phases/07.5.1-sincronizacao-saldosmap-ux-input-valor/07.5.1-CONTEXT.md` — §decisions D-01/D-07 (INV motor intocado), D-08 (fluxo commit+bump+SEM PUSH), D-12 (mensagens de commit literais), §uat (3 cenários localhost)
    - `.planning/phases/07.5-parcelas-datas-valores-customizados/07.5-01-PLAN.md` — Task 4 completa (linhas 1329–1578) = **TEMPLATE do padrão duplo checkpoint + SEM PUSH** (Passos 1–10 da <action>, inclusive formato das PAUSAs e texto do commit de bump)
    - `.planning/phases/07.2-excluir-contrato/07.2-01-PLAN.md` + `.planning/phases/07.3-fix-pagamentos-parciais-resumo-e-listagem/07.3-01-PLAN.md` + `.planning/phases/07.4-cache-headers-vercel/07.4-01-PLAN.md` (caso existam) — TEMPLATES adicionais do mesmo padrão commit+bump+SEM PUSH
    - `.planning/STATE.md` — `last_activity` confirma padrão "commits locais, sem push" adotado desde 7.1
  </read_first>

  <action>
    **Passo 1 — Regressão (gate bloqueante):**
    ```bash
    cd src/mr-3/mr-cobrancas
    npm run test:regressao
    ```
    Esperado: `9/9 PASS`. Se falhar: PARAR, reportar teste quebrado. Este plano NÃO tocou o motor (`devedorCalc.js` e `pagamentos.js`), então regressão não pode ter regredido por causa desta phase — se falhou, algo foi editado indevidamente nas Tasks 1 ou 2. Diagnóstico:
    ```bash
    git diff HEAD~2 -- src/utils/devedorCalc.js src/services/pagamentos.js
    ```
    Se aparecer qualquer byte de diff, foi a raiz — reverter via `git revert` ou `git checkout HEAD~N` do(s) commit(s) que mexeram no motor.

    **Passo 2 — Build (gate bloqueante):**
    ```bash
    cd src/mr-3/mr-cobrancas
    npm run build
    ```
    Esperado: exit 0. Causas prováveis de falha (improvável dado o tamanho dos fixes):
    - Typo em `valoresStr` (Task 1) — checar brackets / paren balanceados.
    - Typo em `dividas` dentro do array (Task 2) — checar vírgula.
    - JSX mal fechado (se executor acidentalmente removeu linha errada).
    Se falhar, reportar linha exata e NÃO prosseguir.

    **Passo 3 — Prova da invariante motor (gate bloqueante, D-01/D-07):**
    ```bash
    cd src/mr-3/mr-cobrancas
    git diff HEAD~2 -- src/utils/devedorCalc.js
    git diff HEAD~2 -- src/services/pagamentos.js
    ```
    **Esperado: ambos retornam vazio (0 bytes).** Se qualquer um tiver conteúdo: PARAR, reverter o arquivo afetado via `git checkout HEAD~2 -- <arquivo>` + criar commit de correção (ou amendar o commit ofensor se ainda não foi bumpado no pai).

    **Passo 4 — Sanidade do histórico de commits do submodule (últimos 2):**
    ```bash
    cd src/mr-3/mr-cobrancas
    git log -2 --format="%H %s" origin/main..main
    git log -2 --name-only --format="=== %h %s ==="
    ```
    Esperado:
    - **2 commits locais** (não 0, não 1, não 3+).
    - Subjects exatos:
      - Mais recente: `fix(07.5.1-02): saldosMap não invalida após edição de parcelas (bugs A + B)` — lista `src/components/DetalheContrato.jsx`.
      - Anterior: `fix(07.5.1-01): input valor aceita limpar em TabelaParcelasEditaveis` — lista `src/components/TabelaParcelasEditaveis.jsx`.
    - **Nenhum** dos 2 commits tocou `src/utils/devedorCalc.js` ou `src/services/pagamentos.js`.

    **Passo 5 — Grep de verificação final dos 2 fixes:**
    ```bash
    cd src/mr-3/mr-cobrancas
    grep -n "\[expandedDoc, hoje, dividas\]" src/components/DetalheContrato.jsx
    grep -n "valoresStr" src/components/TabelaParcelasEditaveis.jsx
    ```
    Esperado:
    - `[expandedDoc, hoje, dividas]` presente (1 match).
    - `valoresStr` presente (≥2 matches — declaração + uso).

    **Passo 6 — PAUSA #1 (checkpoint pré-bump do pai, diff review):**
    Rodar e mostrar ao usuário:
    ```bash
    cd src/mr-3/mr-cobrancas
    git status --short
    git log origin/main..main --stat
    git diff HEAD~2 -- src/components/TabelaParcelasEditaveis.jsx
    git diff HEAD~2 -- src/components/DetalheContrato.jsx
    git diff HEAD~2 -- src/utils/devedorCalc.js         # DEVE SER VAZIO
    git diff HEAD~2 -- src/services/pagamentos.js       # DEVE SER VAZIO
    ```
    Reportar:
    > "Regressão 9/9 OK. Build OK. Invariante motor Art.354 OK (diff HEAD~2 vazio em devedorCalc.js e pagamentos.js). Submodule main tem 2 commits locais: `fix(07.5.1-01)` (TabelaParcelasEditaveis — valoresStr) e `fix(07.5.1-02)` (DetalheContrato — dividas nas deps). Diff completo dos 2 arquivos afetados mostrado acima. SHAs: <hashA_7chars> e <hashB_7chars>. Autoriza bumpar o pai em master? Responda 'approved' ou 'bump it' para prosseguir; qualquer outra coisa cancela (commits do submodule ficam intactos, só o bump fica pendente)."

    **AGUARDAR resposta do usuário. NÃO prosseguir sem autorização explícita.** Se o usuário apontar correção necessária num dos 2 commits:
    - Se for no commit mais recente (Task 2): usar `git commit --amend` ou criar um 3º commit pequeno de correção, depois repetir PAUSA #1.
    - Se for no commit anterior (Task 1): considerar `git rebase -i` (com aprovação do usuário — é destrutivo) ou 3º commit de correção (preferível).
    - Em qualquer caso: re-rodar Passos 1-3 antes de apresentar a PAUSA #1 de novo.

    **Passo 7 — Verificação de estado do pai antes do bump:**
    ```bash
    cd ../../..                                        # voltar pra raiz do pai
    pwd                                                # deve imprimir o path do repo pai (mr-cobrancas-vercel_1/mr-cobrancas)
    git rev-parse --abbrev-ref HEAD                    # deve imprimir `master`
    git status --short
    ```
    Esperado: `git status` mostra **apenas** `src/mr-3` como modified (bump pendente). **Atenção:** pode haver entries pré-existentes no `git status` do pai (por exemplo `.planning/STATE.md` modified, `.planning/ROADMAP.md` modified, plan files deletados — vide `gitStatus` no planning_context). Isso é de sessões anteriores e **não faz parte desta phase**. O commit do Passo 9 adiciona APENAS `src/mr-3`, nada mais.

    **Passo 8 — PAUSA #2 (checkpoint pré-commit do pai, bump review):**
    Reportar:
    > "Commits do submodule autorizados. Pai mostra `src/mr-3` como modified (bump do ponteiro pendente). SHAs a serem apontados: <hashA_7chars> + <hashB_7chars> (tip: <hashB_full>). Autoriza commit de bump no pai branch `master` com mensagem `chore(07.5.1): bump submodule mr-3 — saldosMap sync + input UX (07.5.1)`? Responda 'bump it' ou 'approved' para prosseguir. Esta autorização é SEPARADA da PAUSA #1 — qualquer outra coisa cancela o bump (submodule continua commitado)."

    **AGUARDAR resposta do usuário. NÃO prosseguir sem autorização explícita e SEPARADA desta.** A autorização do Passo 6 NÃO cobre o Passo 8 — são dois gates distintos.

    **Passo 9 — Bump do submodule no repo pai (somente após autorização SEPARADA da PAUSA #2):**
    ```bash
    # Continuamos na raiz do pai.
    git add src/mr-3                                   # adiciona APENAS o bump do submodule, nada mais
    git commit -m "$(cat <<'EOF'
    chore(07.5.1): bump submodule mr-3 — saldosMap sync + input UX (07.5.1)

    Submodule src/mr-3 @ branch main: 2 fixes cirúrgicos da Phase 7.5.1 (gap closure do UAT da 7.5).
    - fix(07.5.1-01): input valor aceita limpar em TabelaParcelasEditaveis — Bug C
    - fix(07.5.1-02): saldosMap não invalida após edição de parcelas (bugs A + B) — Bugs A+B consolidados (mesma raiz, 1 linha)

    Motor Art.354 (devedorCalc.js + pagamentos.js) LITERALMENTE intocado — invariante estrutural D-01/D-07.
    Regressão 9/9 verde (motor não tocado). Build OK.

    Sem push — aguardar UAT manual em localhost (3 cenários do CONTEXT.md §uat) antes de autorizar push.

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
    EOF
    )"
    git log -1 --format=%H              # capturar SHA do bump pra reportar
    ```

    **Passo 10 — Reportar finalizado. NÃO FAZER `git push` em nenhum dos dois repos.** Mostrar ao usuário:
    - **SHAs:**
      - Submodule `main` (Task 1 — Bug C): `<hashA>`
      - Submodule `main` (Task 2 — Bugs A+B): `<hashB>`
      - Pai `master` (bump): `<hashBump>`
    - **Confirmações:**
      - Regressão: 9/9 PASS (submodule)
      - Build: exit 0 (submodule)
      - Invariante D-01/D-07: `git diff HEAD~2 -- src/utils/devedorCalc.js src/services/pagamentos.js` **vazio** (motor literalmente intocado)
      - Exatamente 2 arquivos modificados na phase: `src/components/TabelaParcelasEditaveis.jsx` + `src/components/DetalheContrato.jsx`
      - Zero push: `git log origin/main..main` no submodule ≥2 commits locais; `git log origin/master..master` no pai ≥1 commit local
    - **Instruções de UAT em localhost** (bloco `<how-to-verify>` abaixo — 3 cenários do CONTEXT.md §uat).
  </action>

  <how-to-verify>
    Após os 3 commits locais (2 no submodule + 1 no pai), o usuário roda UAT em **localhost** pra validar os 3 bugs do CONTEXT.md §uat.

    **Setup:**
    ```bash
    cd src/mr-3/mr-cobrancas
    npm run dev
    ```
    Abrir `http://localhost:<porta>` no navegador. Logar.

    **UAT-1 — Bug C: Input valor apaga zero (TabelaParcelasEditaveis)**

    Passos:
    1. Abrir o fluxo `AdicionarDocumento` — selecionar ou criar um contrato de teste e clicar `+ Adicionar Documento`.
    2. Preencher cabeçalho: Tipo = NF/Duplicata, Número = NF 7.5.1-TEST, Valor = R$ 900, Data de Emissão = hoje, Nº de Parcelas = 3.
    3. A tabela `TabelaParcelasEditaveis` renderiza com 3 linhas (valores defaults R$ 300 / R$ 300 / R$ 300).
    4. Clicar no input valor da **Parcela 1**. Apagar todo o conteúdo com backspace.
    5. ✅ **Esperado:** O campo aceita ficar **vazio** (não força exibir "0"). A indicação de Soma atualiza mostrando R$ 600 (soma das outras 2 linhas).
    6. Digitar `50` no campo vazio.
    7. ✅ **Esperado:** O campo mostra exatamente `50` (sem zero à esquerda, sem `050`).
    8. Clicar fora do input. O valor final persiste como `50` e a Soma mostra R$ 650.

    **Falha observável do bug pré-fix** (para referência): o campo mostrava `0` imediatamente após backspace, e `50` digitado ficava `050`.

    **UAT-2 — Bug B: Saldo recalcula após editar valor de parcela (sem F5)**

    Passos:
    1. Abrir `DetalheContrato` de um contrato que tenha documento com parcelas SEM pagamento registrado (valor da parcela ainda livre pra editar).
    2. Expandir o documento.
    3. Anotar o valor da **coluna Saldo** de uma parcela escolhida (ex: R$ 300 se parcela é R$ 300 sem atraso).
    4. Clicar **"Editar parcelas"** (botão da Phase 7.5).
    5. Alterar valor da parcela de R$ 300 → R$ 500. Ajustar outra parcela livre para compensar e manter soma invariante (D-08 da Phase 7.5 — soma das parcelas === valor do documento).
    6. Clicar **Salvar**.
    7. ✅ **Esperado (sem F5):** A tabela volta pro modo readonly. A coluna **Saldo** dessa parcela agora mostra o valor novo (R$ 500 puro se futura; R$ 500 + encargos se vencida) — **recalcula automaticamente**.

    **Falha observável do bug pré-fix**: coluna Valor atualizava mas coluna Saldo continuava R$ 300 até F5 manual.

    **UAT-3 — Bug A: Sem saldo fantasma em parcela reagendada pra futuro**

    Passos:
    1. Mesmo contrato do UAT-2 (ou similar — documento com parcela atualmente vencida e SEM pagamento).
    2. Anotar a coluna Saldo da parcela vencida (ex: R$ 39,90 de penalidade cumulativa, ou qualquer valor > valor puro da parcela).
    3. Clicar **"Editar parcelas"**.
    4. Mudar a data de vencimento da parcela vencida para uma data **no futuro** (ex: hoje + 60 dias). Manter valor invariante.
    5. Clicar **Salvar**.
    6. ✅ **Esperado (sem F5):** coluna Saldo da parcela agora mostra o **valor puro** (mesmo número da coluna Valor), **sem penalidade fantasma**.

    **Falha observável do bug pré-fix**: coluna Saldo continuava mostrando o valor cached com penalidade (`R$ 39,90` ou similar) mesmo com a parcela no futuro.

    **UAT-4 — Regressão de fluxos não tocados pela phase**
    1. Abrir outros `DetalheContrato` (contratos que NÃO tiveram edição de parcelas na sessão) — coluna Saldo deve seguir renderizando igual ao comportamento pré-fix (sem side effects).
    2. `handleRegistrarPagamento` + `handleExcluirPagamento` continuam chamando `onCarregarTudo` — verificar manualmente que registrar/excluir um pagamento continua atualizando Saldo sem F5 (comportamento pré-existente — prop `dividas` nas deps agora também dispara re-cálculo, o que é desejável).

    **UAT-5 — Sanidade de suite automática:**
    ```bash
    cd src/mr-3/mr-cobrancas && npm run test:regressao
    ```
    Esperado: **9/9 PASS**. Motor Art.354 intocado → regressão preservada por invariante estrutural (D-01/D-07/D-08 do CONTEXT.md).

    **UAT-6 — Guard-rail da invariante motor:**
    ```bash
    cd src/mr-3/mr-cobrancas && git diff HEAD~2 -- src/utils/devedorCalc.js src/services/pagamentos.js
    ```
    Esperado: **vazio** — prova determinística de que motor Art.354 não foi tocado nos 2 commits da phase.

    **Guard-rail anti-push:**
    ```bash
    cd src/mr-3/mr-cobrancas && git log origin/main..main --oneline     # ≥2 commits locais
    cd ../../.. && git log origin/master..master --oneline              # ≥1 commit local
    ```

    Se UAT-1, UAT-2 ou UAT-3 falhar: reportar qual cenário caiu. Abrir Phase 7.5.2 (gap closure) ou amendar commits desta phase antes de autorizar push. **Não fazer push até os 3 cenários confirmados ✅.**
  </how-to-verify>

  <acceptance_criteria>
    <!-- Verificação automática pré-UAT (gate antes de apresentar ao usuário nas PAUSAs) -->
    - `cd src/mr-3/mr-cobrancas && npm run test:regressao` exits 0 (9/9 PASS)
    - `cd src/mr-3/mr-cobrancas && npm run build` exits 0
    - `cd src/mr-3/mr-cobrancas && git diff HEAD~2 -- src/utils/devedorCalc.js` retorna **vazio** (0 bytes)
    - `cd src/mr-3/mr-cobrancas && git diff HEAD~2 -- src/services/pagamentos.js` retorna **vazio** (0 bytes)
    - `cd src/mr-3/mr-cobrancas && git log -2 --format=%s origin/main..main` lista **exatamente 2 linhas**: `fix(07.5.1-02): saldosMap não invalida após edição de parcelas (bugs A + B)` e `fix(07.5.1-01): input valor aceita limpar em TabelaParcelasEditaveis` (nesta ordem — mais recente primeiro)
    - `cd src/mr-3/mr-cobrancas && git log -2 --name-only --format= origin/main..main | grep -v "^$" | sort -u` retorna **exatamente 2 arquivos**: `src/components/DetalheContrato.jsx` e `src/components/TabelaParcelasEditaveis.jsx`
    - `cd src/mr-3/mr-cobrancas && grep -n "\\[expandedDoc, hoje, dividas\\]" src/components/DetalheContrato.jsx` retorna **1 match**
    - `cd src/mr-3/mr-cobrancas && grep -n "valoresStr" src/components/TabelaParcelasEditaveis.jsx` retorna **≥2 matches**
    - `cd src/mr-3/mr-cobrancas && git rev-parse --abbrev-ref HEAD` retorna `main`
    - No repo pai: `git rev-parse --abbrev-ref HEAD` retorna `master`
    - No repo pai (após Passo 9): `git log -1 --format=%s` mostra subject EXATO `chore(07.5.1): bump submodule mr-3 — saldosMap sync + input UX (07.5.1)`
    - No repo pai: `git log -1 --name-only --format= | grep -v "^$"` lista **exatamente 1 path**: `src/mr-3`
    - **Nenhum `git push` foi executado:**
      - `cd src/mr-3/mr-cobrancas && git log origin/main..main --oneline | wc -l` ≥ 2
      - No repo pai: `git log origin/master..master --oneline | wc -l` ≥ 1
    - **PAUSA #1 e PAUSA #2 ambas receberam autorização explícita e SEPARADA do usuário antes de cada comando destrutivo (bump do pai).** Rastreável no transcript da sessão de execução.
  </acceptance_criteria>

  <resume-signal>
    Usuário responde com uma das opções em cada PAUSA:

    **Na PAUSA #1 (antes do bump do pai — diff review dos 2 commits do submodule):**
    - **`approved`** ou **`commit it`** → executor prossegue para Passo 7 (verificação do pai) e depois PAUSA #2.
    - **`fix <descrição>`** → executor aplica a correção (amend no commit mais recente ou novo commit), re-roda Passos 1–5, reapresenta PAUSA #1.
    - **`cancel`** / **`hold`** → executor PARA sem bumpar o pai. Commits do submodule ficam intactos. Usuário retoma depois.

    **Na PAUSA #2 (antes do commit do bump no pai):**
    - **`bump it`** ou **`approved`** → executor faz o commit do bump no pai `master`, depois Passo 10 (relatório final + instruções de UAT).
    - **`cancel`** / **`hold`** → executor PARA. Bump não é feito. Commits do submodule ficam intactos no `main`. Usuário retoma depois.

    **Após UAT localhost do usuário (pós-checkpoint, fora do gate automático):**
    - **`UAT ok`** / **`3/3`** / **`approved`** → Phase 7.5.1 fechada localmente. Usuário pode autorizar push em prompt separado (fora desta phase).
    - **`fail UAT-N: descrição`** → abrir Phase 7.5.2 (gap closure) ou amendar commits.
    - **`rollback`** → executor faz `git reset --hard HEAD~1` no pai + `git reset --hard HEAD~2` no submodule `main` (**APENAS** se usuário pedir explicitamente — destrutivo, descarta ambos fixes).
  </resume-signal>

  <done>
    Regressão 9/9 verde. Build exit 0. Invariante motor Art.354 preservada (`git diff HEAD~2` vazio em `devedorCalc.js` e `pagamentos.js`). 2 commits locais no submodule `main` (`fix(07.5.1-01):` e `fix(07.5.1-02):`, exatamente 1 arquivo por commit). 1 commit local no pai `master` (`chore(07.5.1):`, bump de `src/mr-3`). **Zero push executado.** PAUSA #1 e PAUSA #2 ambas autorizadas pelo usuário no transcript. Usuário tem instruções de UAT localhost para os 3 cenários (Bug C, Bug B, Bug A).
  </done>
</task>

</tasks>

<verification>
Mapeamento dos 7 Success Criteria de ROADMAP.md § Phase 7.5.1 → verificação:

| SC | Critério (ROADMAP) | Verificação |
|----|---------------------|-------------|
| SC-1 | `DetalheContrato.jsx` linha do useEffect de `saldosMap` tem deps `[expandedDoc, hoje, dividas]` | Task 2 AC (grep literal `[expandedDoc, hoje, dividas]` = 1 match + grep `[expandedDoc, hoje]` = 0 matches) + Task 3 AC final |
| SC-2 | `TabelaParcelasEditaveis.jsx` usa state local de string para o input valor (não Number puro) | Task 1 AC (grep `valoresStr` presente + grep `value={p.valor_total}` no input = 0 matches + `atualizarValorStr` presente) + Task 3 AC final |
| SC-3 | `git diff HEAD~N -- devedorCalc.js pagamentos.js` vazio (invariante motor Art.354) | Task 1 AC, Task 2 AC, Task 3 AC Passo 3 + checkpoint PAUSA #1 (`git diff HEAD~2 -- ...` vazio é pré-requisito pra autorização) |
| SC-4 | `npm run test:regressao` continua 9/9 verde | Task 3 Passo 1 (gate bloqueante antes da PAUSA #1) + Task 3 AC (`npm run test:regressao` exits 0) |
| SC-5 | `npm run build` exit 0 | Task 1 verify automated + Task 2 verify automated + Task 3 Passo 2 + Task 3 AC |
| SC-6 | UAT localhost confirma 3 cenários (Bug C apagar zero + Bug B Saldo recalcula + Bug A sem fantasma em futura) sem F5 | Task 3 `<how-to-verify>` UAT-1 / UAT-2 / UAT-3 — usuário executa manualmente após Passo 10 e responde `UAT ok` para fechar a phase |
| SC-7 | Zero push executado sem autorização explícita do usuário | Task 3 AC anti-push (`git log origin/main..main` ≥ 2; `git log origin/master..master` ≥ 1) + PAUSAs #1 e #2 criam 2 gates humanos antes de qualquer escrita destrutiva |

**Guard-rails da invariante motor (D-01/D-07/SC-3):**
- `cd src/mr-3/mr-cobrancas && git diff HEAD~2 -- src/utils/devedorCalc.js` retorna **vazio**
- `cd src/mr-3/mr-cobrancas && git diff HEAD~2 -- src/services/pagamentos.js` retorna **vazio**
- `cd src/mr-3/mr-cobrancas && git log --name-only origin/main..main | grep -v "^$" | grep -v "^===" | sort -u` lista **apenas** `src/components/DetalheContrato.jsx` e `src/components/TabelaParcelasEditaveis.jsx` (nunca devedorCalc.js nem pagamentos.js)

**Guard-rails de escopo cirúrgico:**
- Exatamente 2 arquivos modificados em toda a phase (`DetalheContrato.jsx` + `TabelaParcelasEditaveis.jsx`) — verificado via `git log --name-only origin/main..main` no submodule
- Task 2: exatamente 1 linha adicionada e 1 linha removida em `DetalheContrato.jsx` — verificado via `git diff --numstat HEAD~1 -- src/components/DetalheContrato.jsx` = `1  1`
- Task 1: 1 arquivo modificado (`TabelaParcelasEditaveis.jsx`), introduzindo `valoresStr` state + `atualizarValorStr` setter — verificado via grep literal

**Guard-rail anti-push (política "sem push"):**
- `cd src/mr-3/mr-cobrancas && git log origin/main..main` retorna ≥2 commits locais (os 2 fixes) — commits NÃO foram pra remoto
- No repo pai: `git log origin/master..master` retorna ≥1 commit local (o bump) — idem
- Política reforçada em ambas as PAUSAs: usuário precisa responder explicitamente `approved`/`commit it`/`bump it` para prosseguir; default é NÃO prosseguir.

**Guard-rail anti-CRLF (constraint #9 do planning_context):**
- Working tree do submodule tem modificações pré-existentes em `src/mr-3/mr-cobrancas/src/components/AtrasoCell.jsx` e `src/mr-3/mr-cobrancas/src/components/FiltroDividas.jsx` (CRLF). Tasks 1 e 2 **NÃO fazem `git add .` nem `git add src/components/`** — usam `git add <arquivo específico>`. Esses dois arquivos continuam fora dos commits da phase.
</verification>

<success_criteria>
Plano considerado completo quando:

1. `src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx` modificado:
   - State `valoresStr` adicionado (string[] paralelo a `parcelas`).
   - Setter `atualizarValorStr(i, str)` existe e propaga string pro `valoresStr` e número pro `parcelas[i].valor_total`.
   - Input valor (linha ~231 pré-fix) usa `value={valoresStr[i] ?? ""}` e `onChange={atualizarValorStr}`.
   - Input data continua usando `atualizarLinha(i, "data_vencimento", ...)` (sem regressão).
   - Validações `handleSalvar` usam `parcelas`/`soma` numéricos (sem regressão da Phase 7.5 D-07/D-08).
   - Task 1 ACs verdes.

2. `src/mr-3/mr-cobrancas/src/components/DetalheContrato.jsx` modificado:
   - Linha do useEffect de `saldosMap` (173 pré-fix) passa de `}, [expandedDoc, hoje]);` para `}, [expandedDoc, hoje, dividas]);`.
   - Mudança de exatamente 1 linha (+1/-1 no diff).
   - Task 2 ACs verdes.

3. Motor Art.354 literalmente intocado — invariante D-01/D-07:
   - `cd src/mr-3/mr-cobrancas && git diff HEAD~2 -- src/utils/devedorCalc.js` retorna vazio.
   - `cd src/mr-3/mr-cobrancas && git diff HEAD~2 -- src/services/pagamentos.js` retorna vazio.

4. `npm run test:regressao` exits 0 (9/9 PASS) no submodule — sem teste novo adicionado (D-01/D-08 do CONTEXT.md).

5. `npm run build` exits 0 no submodule após cada task.

6. Commits locais feitos:
   - Submodule `main`: 2 commits, subjects **exatos** `fix(07.5.1-01): input valor aceita limpar em TabelaParcelasEditaveis` e `fix(07.5.1-02): saldosMap não invalida após edição de parcelas (bugs A + B)`. Exatamente 1 arquivo por commit.
   - Pai `master`: 1 commit, subject **exato** `chore(07.5.1): bump submodule mr-3 — saldosMap sync + input UX (07.5.1)`. Exatamente 1 path (`src/mr-3`) no commit.
   - **Zero push executado** (`git log origin/main..main` ≥ 2; `git log origin/master..master` ≥ 1).

7. PAUSA #1 e PAUSA #2 ambas autorizadas explicitamente pelo usuário com `approved`/`commit it`/`bump it` (ou equivalente). Rastreável no transcript da sessão de execução.

8. UAT manual em localhost pelo usuário confirma os 3 cenários do CONTEXT.md §uat:
   - UAT-1 (Bug C): input valor aceita campo vazio, digitar `50` mostra `50` (sem `050`).
   - UAT-2 (Bug B): coluna Saldo recalcula após editar valor de parcela, sem F5.
   - UAT-3 (Bug A): coluna Saldo mostra valor puro após reagendar parcela pra futuro, sem F5.

9. Ao fim, SHAs reportados ao usuário (submodule × 2 + pai × 1) + regressão 9/9 + build exit 0 + diff motor vazio + instruções de UAT claras.
</success_criteria>

<output>
Após completion, criar `.planning/phases/07.5.1-sincronizacao-saldosmap-ux-input-valor/07.5.1-01-SUMMARY.md` com:
- SHAs dos 3 commits (submodule `main` × 2 com `fix(07.5.1-01):` e `fix(07.5.1-02):`, pai `master` × 1 com `chore(07.5.1):`).
- Output literal de `git log -2 --format="%H %s" origin/main..main` (submodule) e `git log -1 --format="%H %s" origin/master..master` (pai).
- Resultado final: `npm run test:regressao` → 9/9 PASS.
- Resultado final: `npm run build` → exit 0.
- Prova textual da invariante D-01/D-07: output literal de `git diff HEAD~2 -- src/utils/devedorCalc.js src/services/pagamentos.js` — **vazio**.
- Prova textual dos 2 fixes:
  - Output de `grep -n "\[expandedDoc, hoje, dividas\]" src/components/DetalheContrato.jsx` (1 match).
  - Output de `grep -n "valoresStr" src/components/TabelaParcelasEditaveis.jsx` (≥2 matches).
- Lista dos 7 Success Criteria (ROADMAP § Phase 7.5.1) e se cada um foi validado em UAT ou via verificação automática.
- Flag explícita **"SEM PUSH EXECUTADO — aguardar autorização do usuário"**.
- Transcript (ou resumo) das respostas do usuário nas PAUSA #1 e PAUSA #2 (prova de autorização explícita para cada gate).
- Resultado do UAT localhost 3/3 (ou ponteiro para Phase 7.5.2 se algum falhou).
- Confirmação de que `AtrasoCell.jsx` e `FiltroDividas.jsx` (CRLF pré-existentes do working tree) NÃO foram incluídos em nenhum commit da phase.
- Qualquer comentário sobre Phase 7.5.2 (hash signature otimizado — só se UAT detectar lentidão; D-03/D-09 do CONTEXT.md) ou outras descobertas durante execução.
</output>
