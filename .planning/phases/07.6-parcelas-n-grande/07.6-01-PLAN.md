---
phase: 07.6-parcelas-n-grande
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mr-3/mr-cobrancas/src/components/AdicionarDocumento.jsx
autonomous: true   # single auto task: bump limit + validation + toast + commit no submodule. Checkpoint humano é no plano 07.6-03 (UAT + bump pai).
requirements: []   # Gap closure — no formal REQ-IDs. Contrato de verificação = Success Criteria 1 e 2 em 07.6-CONTEXT.md § 5.

must_haves:
  truths:
    - "SC-1: Input 'Nº de Parcelas' em AdicionarDocumento.jsx tem max=\"999\" (D-02) — verificável via grep"
    - "SC-2: `podesSalvar` rejeita N fora do range [1, 999] — expressão contém `parseInt(numParcelas) <= 999` — verificável via grep"
    - "SC-3: `handleSalvar` tem bloco inicial que rejeita N>999 com toast vermelho (toast.error) ANTES de qualquer outra validação da tabela — verificável via grep + leitura"
    - "SC-4: Motor Art.354 literalmente intocado — `git diff HEAD -- src/utils/devedorCalc.js src/services/pagamentos.js` vazio"
    - "SC-5: npm run test:regressao 9/9 verde (motor intocado, invariante D-01)"
    - "SC-6: npm run build exit 0 (sem regressão de compilação)"
    - "SC-7: 1 commit local no submodule main, 1 arquivo modificado; zero push (autorização explícita pendente na fase 07.6-03)"
  artifacts:
    - path: "src/mr-3/mr-cobrancas/src/components/AdicionarDocumento.jsx"
      provides: "Input de Nº de Parcelas aceita até 999 (atributo HTML max=\"999\"); podesSalvar enforça range [1,999]; handleSalvar bloqueia submit com toast vermelho quando N>999."
      contains: "max=\"999\""
  key_links:
    - from: "src/mr-3/mr-cobrancas/src/components/AdicionarDocumento.jsx (input linha ~172)"
      to: "atributo HTML max=\"999\" (era max=\"360\")"
      via: "edição pontual do atributo no JSX do input de número de parcelas"
      pattern: "max=\"999\""
    - from: "src/mr-3/mr-cobrancas/src/components/AdicionarDocumento.jsx (expressão `podesSalvar` linha ~36-41)"
      to: "cláusula `parseInt(numParcelas) <= 999`"
      via: "AND extra na expressão boolean — botão Salvar fica desabilitado se N>999 mesmo que a tabela esteja preenchida"
      pattern: "parseInt\\(numParcelas\\) <= 999"
    - from: "src/mr-3/mr-cobrancas/src/components/AdicionarDocumento.jsx (handleSalvar linha ~62-82)"
      to: "toast.error bloqueando submit quando N<1 ou N>999"
      via: "bloco adicional no topo de handleSalvar, depois da validação `parseInt(numParcelas) < 1` existente"
      pattern: "parseInt\\(numParcelas\\) > 999"
---

<objective>
Bump do limite cosmético de parcelas no formulário "Novo Documento" para cobrir o domínio forense real (imobiliário 35 anos = 420x, consignado = 96x). Fix atômico de 3 edições pontuais em `AdicionarDocumento.jsx`, todas triviais e revertíveis por `git revert` puro (D-13).

1. **Atributo HTML:** `max="360"` → `max="999"` na linha ~172 do input de número de parcelas (D-02).
2. **Expressão `podesSalvar`:** adicionar `parseInt(numParcelas) <= 999` como AND extra (D-02, 2ª camada de enforcement).
3. **Validação `handleSalvar`:** adicionar bloco que emite `toast.error` vermelho bloqueante quando N>999, ANTES das validações da tabela (D-02, toast vermelho bloqueante).

Motor Art.354 (`utils/devedorCalc.js` + `services/pagamentos.js`) **literalmente intocado** — verificação determinística via `git diff` vazio ao fim (D-01).

Schema do banco **inalterado** — `contratos.js:43` já aceita `num_parcelas INT CHECK >= 1` (qualquer N positivo; ver CONTEXT § 3).

Purpose: Advogado consegue tentar criar documento com N=420 (imobiliário 35 anos) e N=999 (teto). Tentativa de N=1000 é bloqueada por toast vermelho antes de qualquer chamada ao service (D-02). Componente `TabelaParcelasEditaveis` ainda NÃO é virtualizado nesta phase — plano 07.6-02 assume essa responsabilidade.

Output:
- `src/mr-3/mr-cobrancas/src/components/AdicionarDocumento.jsx` — 3 alterações pontuais (atributo max, `podesSalvar`, bloco inicial de `handleSalvar`).
- 1 commit local no submodule `src/mr-3/mr-cobrancas` branch `main`. Zero push (autorização explícita pendente na fase 07.6-03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07.6-parcelas-n-grande/07.6-CONTEXT.md

<!-- Phase 7.5 (origem do componente editável) + 7.5.1 (fix UX do input valor — precedente de edição cirúrgica em AdicionarDocumento.jsx): -->
@.planning/phases/07.5-parcelas-datas-valores-customizados/07.5-CONTEXT.md

<!-- Padrão de commit single-file no submodule sem push (mesmo padrão das 7.2/7.3/7.4/7.5/7.5.1): -->
<!-- .planning/phases/07.5.1-sincronizacao-saldosmap-ux-input-valor/07.5.1-01-PLAN.md Task 1 = template -->

<interfaces>
<!-- Contratos-chave extraídos direto do código atual. Executor não precisa re-explorar. -->

From src/mr-3/mr-cobrancas/src/components/AdicionarDocumento.jsx (linhas 36-41 — expressão `podesSalvar` ATUAL):
```javascript
const podesSalvar =
  !!tipo &&
  !!valor && parseFloat(valor) > 0 &&
  !!dataEmissao &&
  !!numParcelas && parseInt(numParcelas) >= 1 &&
  Array.isArray(parcelasCustom) && parcelasCustom.length === parseInt(numParcelas);
```

**DEPOIS (D-02, teto 999 adicionado):**
```javascript
const podesSalvar =
  !!tipo &&
  !!valor && parseFloat(valor) > 0 &&
  !!dataEmissao &&
  !!numParcelas && parseInt(numParcelas) >= 1 && parseInt(numParcelas) <= 999 &&
  Array.isArray(parcelasCustom) && parcelasCustom.length === parseInt(numParcelas);
```

From src/mr-3/mr-cobrancas/src/components/AdicionarDocumento.jsx (linhas 62-65 — início de `handleSalvar` ATUAL):
```javascript
async function handleSalvar() {
  if (!valor || parseFloat(valor) <= 0) { toast("Informe o valor do documento.", { icon: "⚠️" }); return; }
  if (!dataEmissao) { toast("Informe a data de emissão.", { icon: "⚠️" }); return; }
  if (!numParcelas || parseInt(numParcelas) < 1) { toast("Informe o número de parcelas.", { icon: "⚠️" }); return; }
  // ... (validações da tabela parcelasCustom seguem após)
```

**DEPOIS (D-02, toast vermelho bloqueante quando N>999 — inserido logo após a validação `< 1` existente):**
```javascript
async function handleSalvar() {
  if (!valor || parseFloat(valor) <= 0) { toast("Informe o valor do documento.", { icon: "⚠️" }); return; }
  if (!dataEmissao) { toast("Informe a data de emissão.", { icon: "⚠️" }); return; }
  if (!numParcelas || parseInt(numParcelas) < 1) { toast("Informe o número de parcelas.", { icon: "⚠️" }); return; }
  if (parseInt(numParcelas) > 999) { toast.error("Número de parcelas máximo é 999 (limite prático para empréstimos imobiliários de até 35 anos)."); return; }
  // ... (validações da tabela parcelasCustom seguem após)
```

From src/mr-3/mr-cobrancas/src/components/AdicionarDocumento.jsx (linhas 168-178 — input "Nº de Parcelas" ATUAL):
```jsx
<div>
  <label style={labelStyle}>Nº de Parcelas *</label>
  <input
    type="number"
    min="1"
    max="360"
    placeholder="Ex.: 3"
    value={numParcelas}
    onChange={e => { setNumParcelas(e.target.value); setParcelasCustom(null); }}
    style={inputStyle}
  />
</div>
```

**DEPOIS (D-02, atributo HTML max=\"999\"):**
```jsx
<div>
  <label style={labelStyle}>Nº de Parcelas *</label>
  <input
    type="number"
    min="1"
    max="999"
    placeholder="Ex.: 3"
    value={numParcelas}
    onChange={e => { setNumParcelas(e.target.value); setParcelasCustom(null); }}
    style={inputStyle}
  />
</div>
```

**Importante:** `toast.error(...)` já está importado via `import toast from "react-hot-toast"` (linha 2) — **não adicionar import novo**. O resto das chamadas no arquivo usa `toast(...)` ou `toast.error(...)` — mantém consistência.

Motor Art.354 (referência, NÃO modificar):
- `src/mr-3/mr-cobrancas/src/utils/devedorCalc.js` — intocado (D-01)
- `src/mr-3/mr-cobrancas/src/services/pagamentos.js` — intocado (D-01)

</interfaces>
</context>

<tasks>

<!--
  1 task única em 1 wave. Commit atômico de 3 edições pontuais em 1 arquivo.
  Checkpoint humano fica para o plano 07.6-03 (UAT + bump pai + tag futura).
-->

<task type="auto">
  <name>Task 1: Bump max="360"→"999" + podesSalvar ≤999 + handleSalvar toast vermelho quando N>999</name>
  <files>
    src/mr-3/mr-cobrancas/src/components/AdicionarDocumento.jsx
  </files>

  <read_first>
    - `src/mr-3/mr-cobrancas/src/components/AdicionarDocumento.jsx` — arquivo inteiro (220 linhas). Atenção especial aos blocos:
      - Linhas 36-41 (`podesSalvar` expression)
      - Linhas 62-82 (`handleSalvar` — validações iniciais)
      - Linhas 166-178 (campo "Nº de Parcelas" — input com `max="360"` na linha 172)
    - `.planning/phases/07.6-parcelas-n-grande/07.6-CONTEXT.md` — §4 D-01 (motor intocado), D-02 (teto 999, 2 camadas, toast vermelho bloqueante), D-13 (3 plans atômicos, split por reversibilidade), §5 Success Criteria 1 e 2
    - `.planning/phases/07.5-parcelas-datas-valores-customizados/07.5-CONTEXT.md` — §decisions D-03 (fluxo de criação com `parcelasCustom`), D-07 (validação soma/datas — continua funcionando em `handleSalvar` após esta task)
    - `.planning/phases/07.5.1-sincronizacao-saldosmap-ux-input-valor/07.5.1-01-PLAN.md` — Task 1 = **template** de commit single-file no submodule `main` sem push (mesma sintaxe de HEREDOC + grep de verificação)
  </read_first>

  <action>
    **Abordagem:** 3 edições cirúrgicas em `AdicionarDocumento.jsx`. Zero import novo. Usar tool Edit com `old_string`/`new_string` literais para cada edição pontual (menor chance de colateral que Write).

    ---

    **Passo 1 — Edição A (atributo HTML `max`, linha 172).** Substituir literalmente:

    ```jsx
    // ANTES:
            max="360"

    // DEPOIS:
            max="999"
    ```

    Padrão para Edit tool:
    - `old_string`: `            max="360"`
    - `new_string`: `            max="999"`

    Verificar via `grep -n 'max="360"' src/mr-3/mr-cobrancas/src/components/AdicionarDocumento.jsx` → deve retornar **0 matches** após o edit. `grep -n 'max="999"' ...` → deve retornar ≥1 match.

    ---

    **Passo 2 — Edição B (`podesSalvar`, linha ~40).** Substituir a linha inteira da cláusula N:

    ```jsx
    // ANTES (linha 40):
        !!numParcelas && parseInt(numParcelas) >= 1 &&

    // DEPOIS:
        !!numParcelas && parseInt(numParcelas) >= 1 && parseInt(numParcelas) <= 999 &&
    ```

    Padrão para Edit tool:
    - `old_string`: `    !!numParcelas && parseInt(numParcelas) >= 1 &&`
    - `new_string`: `    !!numParcelas && parseInt(numParcelas) >= 1 && parseInt(numParcelas) <= 999 &&`

    **Atenção a indentação:** a linha atual começa com 4 spaces (parte da expressão multi-linha iniciada em `const podesSalvar =`). Não quebrar em múltiplas linhas — manter tudo em uma linha só para preservar legibilidade do bloco.

    Verificar via `grep -n "parseInt(numParcelas) <= 999" src/mr-3/mr-cobrancas/src/components/AdicionarDocumento.jsx` → deve retornar **≥1 match** (deve aparecer 2x: uma aqui em `podesSalvar`, outra no bloco do Passo 3 após a edição — mas o teste de aceitação exige só ≥1).

    ---

    **Passo 3 — Edição C (`handleSalvar`, após linha 65).** Inserir bloco novo de toast vermelho bloqueante logo após a validação `< 1` existente.

    ```jsx
    // ANTES (linha 65):
        if (!numParcelas || parseInt(numParcelas) < 1) { toast("Informe o número de parcelas.", { icon: "⚠️" }); return; }

    // DEPOIS (linha 65 preservada + linha 66 nova):
        if (!numParcelas || parseInt(numParcelas) < 1) { toast("Informe o número de parcelas.", { icon: "⚠️" }); return; }
        if (parseInt(numParcelas) > 999) { toast.error("Número de parcelas máximo é 999 (limite prático para empréstimos imobiliários de até 35 anos)."); return; }
    ```

    Padrão para Edit tool:
    - `old_string`: `    if (!numParcelas || parseInt(numParcelas) < 1) { toast("Informe o número de parcelas.", { icon: "⚠️" }); return; }`
    - `new_string`: `    if (!numParcelas || parseInt(numParcelas) < 1) { toast("Informe o número de parcelas.", { icon: "⚠️" }); return; }\n    if (parseInt(numParcelas) > 999) { toast.error("Número de parcelas máximo é 999 (limite prático para empréstimos imobiliários de até 35 anos)."); return; }`

    **Atenção:** indentação de 4 spaces (padrão do arquivo — dentro de `async function handleSalvar() { ... }`). **NÃO** alterar outras validações subsequentes (`parcelasCustom` soma, datas, etc.) — elas continuam intactas.

    Verificar via `grep -n "parseInt(numParcelas) > 999" src/mr-3/mr-cobrancas/src/components/AdicionarDocumento.jsx` → deve retornar **1 match exato**.

    ---

    **Passo 4 — Confirmar que nenhum outro arquivo foi editado:**
    ```bash
    cd src/mr-3/mr-cobrancas
    git diff --stat
    # Esperado: APENAS src/components/AdicionarDocumento.jsx modificado.
    # Counts esperados: ~4-5 insertions, 2 deletions (1 linha substituída no podesSalvar + 1 atributo max + 1 nova linha de validação em handleSalvar).
    git diff src/components/AdicionarDocumento.jsx
    # Esperado: SOMENTE as 3 edições dos Passos 1-3. Se houver outras linhas no diff, PARAR e restaurar via `git checkout HEAD -- src/components/AdicionarDocumento.jsx` antes de tentar de novo.
    ```

    **Passo 5 — Invariante motor (D-01, gate bloqueante):**
    ```bash
    cd src/mr-3/mr-cobrancas
    git diff HEAD -- src/utils/devedorCalc.js src/services/pagamentos.js
    ```
    Esperado: **vazio (0 bytes)**. Se houver qualquer diff, restaurar esses arquivos via `git checkout HEAD -- <arquivo>` antes de prosseguir.

    **Passo 6 — Build (gate bloqueante):**
    ```bash
    cd src/mr-3/mr-cobrancas && npm run build
    ```
    Exit 0 obrigatório. Erros prováveis (todos improváveis dado o tamanho dos fixes):
    - Typo no atributo max (quotes faltando).
    - Parêntese ou ponto-e-vírgula faltando na nova linha de `handleSalvar`.
    - Escape incorreto de string (o texto do toast contém `.` e `(35 anos)` — caracteres seguros; não há interpolação de template literal).

    Se falhar: reportar linha exata, NÃO prosseguir para commit.

    **Passo 7 — Regressão (gate bloqueante, invariante estrutural D-01):**
    ```bash
    cd src/mr-3/mr-cobrancas && npm run test:regressao
    ```
    Esperado: `9/9 PASS`. Motor Art.354 não foi tocado (verificado no Passo 5), então teste não pode ter regredido por esta task. Se falhar: diagnóstico via `git diff HEAD -- src/utils/ src/services/` pra ver se houve edição acidental.

    **Passo 8 — Commit no submodule (branch `main`) APENAS deste arquivo.** Exatamente 1 arquivo no commit:
    ```bash
    cd src/mr-3/mr-cobrancas
    git checkout main                   # garantir branch (não detached HEAD)
    git add src/components/AdicionarDocumento.jsx
    git commit -m "$(cat <<'EOF'
    feat(07.6-01): bump limite parcelas 360→999 em AdicionarDocumento

    - Atributo HTML do input "Nº de Parcelas": max="360" → max="999" (D-02, 1ª camada de enforcement).
    - Expressão `podesSalvar` ganha cláusula `parseInt(numParcelas) <= 999` —
      botão Salvar fica desabilitado se N>999 mesmo que a tabela esteja preenchida.
    - `handleSalvar` ganha validação de teto com `toast.error` vermelho bloqueante
      ANTES de qualquer processamento de parcelasCustom (D-02, 2ª camada).
      Texto: "Número de parcelas máximo é 999 (limite prático para empréstimos imobiliários de até 35 anos)."
    - Cobre domínio forense real: consignado 96x, imobiliário 30 anos 360x,
      imobiliário 35 anos 420x, e teto prático 999x (CONTEXT § 1).
    - Motor Art.354 (devedorCalc.js + pagamentos.js) LITERALMENTE intocado (INV D-01).
    - Schema do banco inalterado — contratos.js:43 CHECK num_parcelas >= 1 já aceita qualquer N positivo.
    - Virtualização de render (Fix B) fica para o plano 07.6-02 — este plano é só o bump de limite.
    - Regressão 9/9 verde (motor não tocado — invariante estrutural).

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
    EOF
    )"
    git log -1 --format=%H              # capturar SHA pra reportar no plano 07.6-03
    ```

    **NÃO FAZER PUSH.** O bump do pai e o push só acontecem após autorização explícita do usuário no plano 07.6-03.
  </action>

  <verify>
    <automated>
      cd src/mr-3/mr-cobrancas && \
      grep -n 'max="999"' src/components/AdicionarDocumento.jsx && \
      grep -n "parseInt(numParcelas) <= 999" src/components/AdicionarDocumento.jsx && \
      grep -n "parseInt(numParcelas) > 999" src/components/AdicionarDocumento.jsx && \
      npm run build && \
      npm run test:regressao
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -n 'max="999"' src/mr-3/mr-cobrancas/src/components/AdicionarDocumento.jsx` retorna **≥1 match** (atributo HTML bumpado)
    - `grep -n 'max="360"' src/mr-3/mr-cobrancas/src/components/AdicionarDocumento.jsx` retorna **0 matches** (atributo antigo substituído completamente)
    - `grep -n "parseInt(numParcelas) <= 999" src/mr-3/mr-cobrancas/src/components/AdicionarDocumento.jsx` retorna **≥1 match** (podesSalvar enforça teto)
    - `grep -n "parseInt(numParcelas) > 999" src/mr-3/mr-cobrancas/src/components/AdicionarDocumento.jsx` retorna **exatamente 1 match** (handleSalvar toast bloqueante)
    - `grep -n "toast.error(\"Número de parcelas máximo é 999" src/mr-3/mr-cobrancas/src/components/AdicionarDocumento.jsx` retorna **exatamente 1 match** (mensagem exata do toast, D-02)
    - `grep -c "import toast" src/mr-3/mr-cobrancas/src/components/AdicionarDocumento.jsx` retorna **1** (sem duplicar import — `toast.error` usa o default import já existente)
    - `cd src/mr-3/mr-cobrancas && git diff HEAD -- src/utils/devedorCalc.js` retorna **vazio** (INV motor Art.354)
    - `cd src/mr-3/mr-cobrancas && git diff HEAD -- src/services/pagamentos.js` retorna **vazio** (INV motor Art.354)
    - `cd src/mr-3/mr-cobrancas && npm run build` exit 0
    - `cd src/mr-3/mr-cobrancas && npm run test:regressao` exit 0 (9/9 PASS)
    - `cd src/mr-3/mr-cobrancas && git log -1 --format=%s` mostra subject EXATO `feat(07.6-01): bump limite parcelas 360→999 em AdicionarDocumento`
    - `cd src/mr-3/mr-cobrancas && git log -1 --name-only --format=` lista EXATAMENTE 1 arquivo: `src/components/AdicionarDocumento.jsx`
    - `cd src/mr-3/mr-cobrancas && git rev-parse --abbrev-ref HEAD` retorna `main`
    - `cd src/mr-3/mr-cobrancas && git status --porcelain src/components/AdicionarDocumento.jsx` retorna **vazio** para este arquivo (ignorar CRLF pré-existentes em outros arquivos — constraint #9 / fora do escopo)
    - Nenhum push executado: `cd src/mr-3/mr-cobrancas && git log origin/main..main` retorna ≥1 commit local (este)
    - **Zero** comandos `git push` executados em qualquer ponto desta task
  </acceptance_criteria>

  <done>
    `AdicionarDocumento.jsx` modificado com 3 edições pontuais: `max="999"` no atributo HTML, `<= 999` no `podesSalvar`, toast vermelho `> 999` no topo de `handleSalvar`. Build limpo. Regressão 9/9. Invariante motor Art.354 preservada (git diff vazio). 1 commit local no submodule `main` (subject EXATO `feat(07.6-01): bump limite parcelas 360→999 em AdicionarDocumento`, exatamente 1 arquivo modificado). Zero push. Bump do pai e push ficam para o plano 07.6-03.
  </done>
</task>

</tasks>

<verification>
Verificação final (após completar a task):

1. **Grep determinístico dos 3 fixes** (executar na raiz do repo pai):
   - `grep -n 'max="999"' src/mr-3/mr-cobrancas/src/components/AdicionarDocumento.jsx` → ≥1 match
   - `grep -n 'max="360"' src/mr-3/mr-cobrancas/src/components/AdicionarDocumento.jsx` → 0 matches
   - `grep -n "parseInt(numParcelas) <= 999" src/mr-3/mr-cobrancas/src/components/AdicionarDocumento.jsx` → ≥1 match
   - `grep -n "parseInt(numParcelas) > 999" src/mr-3/mr-cobrancas/src/components/AdicionarDocumento.jsx` → 1 match

2. **Invariante motor Art.354** (D-01):
   - `cd src/mr-3/mr-cobrancas && git diff HEAD -- src/utils/devedorCalc.js src/services/pagamentos.js` → vazio

3. **Regressão + build**:
   - `cd src/mr-3/mr-cobrancas && npm run test:regressao` → 9/9 PASS
   - `cd src/mr-3/mr-cobrancas && npm run build` → exit 0

4. **Histórico de commits**:
   - `cd src/mr-3/mr-cobrancas && git log -1 --format=%s` → `feat(07.6-01): bump limite parcelas 360→999 em AdicionarDocumento`
   - `cd src/mr-3/mr-cobrancas && git log -1 --name-only --format=` → lista apenas `src/components/AdicionarDocumento.jsx`
   - `cd src/mr-3/mr-cobrancas && git log origin/main..main` → ≥1 commit local (pendente de push, autorizado em 07.6-03)

5. **Pai (repo master)**: estado INALTERADO nesta fase. O ponteiro `src/mr-3` fica desincronizado temporariamente — normal. Bump ocorre no plano 07.6-03 após autorização.
</verification>

<success_criteria>
Plan 07.6-01 bem-sucedido quando:
- ✅ Input "Nº de Parcelas" em `AdicionarDocumento.jsx` tem `max="999"`
- ✅ `podesSalvar` rejeita N<1 ou N>999
- ✅ `handleSalvar` emite `toast.error` vermelho bloqueante com a mensagem exata quando N>999, antes das validações da tabela
- ✅ Motor Art.354 (`devedorCalc.js` + `pagamentos.js`) literalmente intocado (`git diff HEAD` vazio)
- ✅ `npm run test:regressao` 9/9 verde
- ✅ `npm run build` exit 0
- ✅ 1 commit local no submodule `main` (subject exato, 1 arquivo modificado)
- ✅ Zero push executado — bump do pai pendente para 07.6-03 (mesma sequência das 7.2/7.3/7.4/7.5/7.5.1)
</success_criteria>

<output>
Após conclusão desta task, criar `.planning/phases/07.6-parcelas-n-grande/07.6-01-SUMMARY.md` resumindo:
- Arquivo modificado (path + linhas alteradas aproximadas)
- SHA do commit no submodule main (7 chars)
- Output das verificações (greps + regressão 9/9 + build)
- Evidência de invariante motor (git diff HEAD vazio em devedorCalc.js e pagamentos.js)
- Estado pendente: bump do pai autorizado em 07.6-03, zero push
</output>
</content>
</invoke>