---
phase: 07.6-parcelas-n-grande
plan: 03
type: execute
wave: 3
depends_on:
  - 07.6-01
  - 07.6-02
files_modified:
  - src/mr-3 (bump do submodule pointer no repo pai branch master)
autonomous: false   # Task 1 e Task 2 são checkpoint:human-verify (UAT manual + duplo gate commit). SEM PUSH em qualquer ponto.
requirements: []   # Gap closure — sem REQ-IDs formais. Contrato de verificação = Success Criteria 6, 7, 8, 9 em 07.6-CONTEXT.md § 5 + 6 cenários SC-1..SC-6 em § 4 D-11.

must_haves:
  truths:
    - "SC-1: UAT localhost 6 cenários executados e reportados (SC-1 N=10, SC-2 N=100, SC-3 Soma/Total/Δ ao vivo, SC-4 Sugerir datas N=100, SC-5 N=1000 bloqueado por toast, SC-6 foco retention após scroll/edição) — D-11"
    - "SC-2: Regressão 9/9 verde (motor Art.354 não tocado, invariante D-01)"
    - "SC-3: Build do submodule exit 0"
    - "SC-4: git diff HEAD~2 -- src/utils/devedorCalc.js + src/services/pagamentos.js retorna vazio no submodule (invariante motor preservada pelos 2 commits da phase)"
    - "SC-5: 2 commits locais no submodule main existem e têm subjects EXATOS — feat(07.6-01) e feat(07.6-02) na ordem correta"
    - "SC-6: PAUSA #1 (diff review dos 2 commits do submodule) executada e autorizada pelo usuário antes de qualquer bump do pai"
    - "SC-7: PAUSA #2 (bump review + SHAs + branch master) executada e autorizada pelo usuário antes do commit do bump no pai"
    - "SC-8: 1 commit único no pai branch master — subject EXATO `chore(07.6): bump submodule mr-3 — virtualização parcelas N grande (07.6-01+02)` — com apenas src/mr-3 stageado"
    - "SC-9: Zero push em TODA a phase — `git log origin/main..main` no submodule ≥2 commits locais; `git log origin/master..master` no pai ≥1 commit local"
    - "SC-10: Tag `v1.4-phase7.6` NÃO é criada nesta phase — fica para ação separada do usuário após UAT de produção (padrão da 7.5.1)"
  artifacts:
    - path: "src/mr-3 (submodule pointer no pai)"
      provides: "Bump do ponteiro para o tip do submodule main (SHA do commit 07.6-02). Encapsula os 2 commits da phase 7.6 (07.6-01 + 07.6-02) em 1 commit atômico no pai."
      contains: "bump submodule mr-3"
    - path: ".planning/phases/07.6-parcelas-n-grande/07.6-03-SUMMARY.md"
      provides: "Evidência da execução do UAT + SHAs + autorização das 2 PAUSAs + reporte de invariante motor + próximos passos (push + tag em ação separada)"
      contains: "SC-1|SC-2|SC-3|SC-4|SC-5|SC-6"
  key_links:
    - from: "Task 1 (checkpoint UAT)"
      to: "localhost `npm run dev` no submodule + 6 cenários manuais (D-11) reportados ao usuário"
      via: "PAUSA para usuário validar cenários antes de autorizar commits do submodule já existentes + bump do pai"
      pattern: "SC-1|SC-2|SC-3|SC-4|SC-5|SC-6"
    - from: "Task 2 (checkpoint duplo gate)"
      to: "Commit bump do submodule no pai branch master + SHAs reportados"
      via: "PAUSA #1 (diff review dos 2 commits existentes do submodule) + PAUSA #2 (bump review + confirmação branch master + SHAs) — SEM PUSH em nenhum momento"
      pattern: "PAUSA #1|PAUSA #2"
    - from: "Tag v1.4-phase7.6"
      to: "Ação separada do usuário após UAT de produção — NÃO criar nesta phase"
      via: "Padrão estabelecido nas 7.2/7.3/7.4/7.5/7.5.1 — tag só após smoke test em prod"
      pattern: "NÃO criar tag"
---

<objective>
Fechamento da Phase 7.6 com UAT manual dos 6 cenários D-11 em localhost + duplo checkpoint humano + bump do submodule no repo pai branch master. **SEM PUSH em qualquer momento**. **Tag `v1.4-phase7.6` NÃO é criada aqui** — fica para ação separada do usuário após UAT de produção, padrão das 7.5/7.5.1.

Os 2 commits já estão localmente no submodule `src/mr-3/mr-cobrancas` branch `main` (vindo dos planos 07.6-01 e 07.6-02):
- **Commit A (07.6-01):** `feat(07.6-01): bump limite parcelas 360→999 em AdicionarDocumento` — 1 arquivo (AdicionarDocumento.jsx).
- **Commit B (07.6-02):** `feat(07.6-02): virtualização @tanstack/react-virtual em TabelaParcelasEditaveis` — 3 arquivos (package.json, package-lock.json, TabelaParcelasEditaveis.jsx).

Esta phase executa:

1. **Task 1 (checkpoint:human-verify):** UAT manual em localhost dos 6 cenários D-11 — usuário executa no browser e reporta resultado; agente aguarda confirmação explícita antes de prosseguir para Task 2. Inclui regressão 9/9 + build exit 0 + prova invariante motor via `git diff HEAD~2` vazio em devedorCalc.js e pagamentos.js.

2. **Task 2 (checkpoint:human-verify com duplo gate):**
   - **PAUSA #1 (diff review):** agente mostra os 2 commits do submodule (`git log origin/main..main --stat` + diffs) e aguarda autorização explícita (`approved` / `bump it`) do usuário antes de tocar o pai.
   - **PAUSA #2 (bump review):** após PAUSA #1, agente cd pra raiz do pai, confirma `src/mr-3` stageado, mostra SHAs, e aguarda autorização SEPARADA antes do commit final.
   - Após PAUSA #2: commit único no pai branch `master` com subject exato da CONTEXT D-14. SEM PUSH.

**Padrão idêntico ao seguido nas Phases 7.2 / 7.3 / 7.4 / 7.5 / 7.5.1** (D-14 do 07.6-CONTEXT.md + precedente 07.5.1-01-PLAN.md Task 3 linhas 415-556).

Os commits das Tasks 07.6-01 e 07.6-02 NÃO são undone, amendados ou rebase-ados. Esta phase só **verifica + aprova + bumpa o pai**.

Purpose: Advogado faz smoke test em localhost de N=100 e N=999 e confirma que virtualização não quebra validações / UX. Commit do bump fica local no pai até o usuário decidir push manualmente (provavelmente em sessão separada, como fez na 7.5.1 com o push de 2026-04-23 pós-UAT).

Output:
- Log de execução dos 6 cenários UAT (reportado inline no chat, resumido no SUMMARY).
- 1 commit local no pai branch `master` com mensagem EXATA `chore(07.6): bump submodule mr-3 — virtualização parcelas N grande (07.6-01+02)` e apenas `src/mr-3` stageado.
- SUMMARY da phase registrando SHAs do submodule + SHA do pai + resultado do UAT + confirmação de autorização das 2 PAUSAs + confirmação SEM PUSH.
- **NENHUMA TAG criada.** Tag `v1.4-phase7.6` fica para ação separada pós-UAT de produção.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07.6-parcelas-n-grande/07.6-CONTEXT.md

<!-- 2 planos predecessores (Tasks dependem de ambos completos): -->
@.planning/phases/07.6-parcelas-n-grande/07.6-01-PLAN.md
@.planning/phases/07.6-parcelas-n-grande/07.6-02-PLAN.md

<!-- TEMPLATE literal do padrão duplo-checkpoint + SEM PUSH (seguir a estrutura passo-a-passo): -->
@.planning/phases/07.5.1-sincronizacao-saldosmap-ux-input-valor/07.5.1-01-PLAN.md

<!-- Precedentes adicionais do mesmo padrão commit+bump+SEM PUSH: -->
<!-- .planning/phases/07.2-excluir-contrato/07.2-01-PLAN.md -->
<!-- .planning/phases/07.3-fix-pagamentos-parciais-resumo-e-listagem/07.3-01-PLAN.md -->
<!-- .planning/phases/07.4-cache-headers-vercel/07.4-01-PLAN.md -->
<!-- .planning/phases/07.5-parcelas-datas-valores-customizados/07.5-01-PLAN.md (Task 4 = 10-passo template) -->

<interfaces>
<!-- Pré-condições verificáveis desta phase. Executor checa ANTES de abrir Task 1. -->

Pré-condições (se FALHAREM, abortar phase e reportar ao usuário):

```bash
cd src/mr-3/mr-cobrancas
git rev-parse --abbrev-ref HEAD                                       # esperado: main
git log -2 --format="%H %s" origin/main..main
# esperado EXATAMENTE 2 linhas, mais recente primeiro:
# <SHA2>  feat(07.6-02): virtualização @tanstack/react-virtual em TabelaParcelasEditaveis
# <SHA1>  feat(07.6-01): bump limite parcelas 360→999 em AdicionarDocumento

git diff HEAD~2 -- src/utils/devedorCalc.js src/services/pagamentos.js
# esperado: vazio (invariante D-01)
```

Estado do pai (repo master) ANTES desta phase:
```bash
cd <raiz do pai>
git rev-parse --abbrev-ref HEAD                                       # esperado: master
git status --short
# esperado: `src/mr-3` apontando pra SHA antigo (= SHA anterior aos 2 commits desta phase).
# git status pode listar `src/mr-3 (new commits)` como modified — normal.
# ATENÇÃO: podem existir entries pré-existentes no git status (.planning/STATE.md,
# .planning/ROADMAP.md deletados/modificados, etc — vide gitStatus no planning_context).
# Essas são de sessões anteriores e NÃO fazem parte desta phase. O commit do Passo 9
# adiciona APENAS `src/mr-3`, nada mais.
```

Textos exatos das PAUSAs (replicar literal no chat quando o gate for atingido):

**PAUSA #1** (Task 2, Passo 1):
> "Regressão 9/9 OK. Build OK. Invariante motor Art.354 OK (diff HEAD~2 vazio em devedorCalc.js e pagamentos.js). UAT localhost dos 6 cenários D-11 executado (reportado na Task 1). Submodule main tem 2 commits locais:
> - `feat(07.6-01): bump limite parcelas 360→999 em AdicionarDocumento` (SHA `<SHA1_7chars>`) — 1 arquivo
> - `feat(07.6-02): virtualização @tanstack/react-virtual em TabelaParcelasEditaveis` (SHA `<SHA2_7chars>`) — 3 arquivos (package.json + package-lock.json + TabelaParcelasEditaveis.jsx)
>
> Diff consolidado dos 2 commits mostrado acima. Autoriza bumpar o submodule no pai branch `master`?
> Responda **'approved'** ou **'bump it'** para prosseguir.
> Qualquer outra coisa cancela a Task 2 — os 2 commits do submodule ficam intactos, só o bump fica pendente."

**PAUSA #2** (Task 2, Passo 3):
> "Commits do submodule autorizados. Pai (raiz do repo) em branch `master`, status mostra `src/mr-3` como modified (bump do ponteiro pendente). SHAs a serem apontados: `<SHA1_7chars>` + `<SHA2_7chars>` (tip: `<SHA2_full>`).
>
> Autoriza commit no pai com mensagem EXATA:
> `chore(07.6): bump submodule mr-3 — virtualização parcelas N grande (07.6-01+02)` ?
>
> Responda **'bump it'** ou **'approved'** para prosseguir.
> Esta autorização é SEPARADA da PAUSA #1 — qualquer outra coisa cancela o bump (submodule continua commitado localmente, não haverá commit no pai)."

Mensagem EXATA do commit do pai (CONTEXT D-14 + planning_context "Commit message prefixes"):
```
chore(07.6): bump submodule mr-3 — virtualização parcelas N grande (07.6-01+02)

Submodule src/mr-3 @ branch main: 2 commits cirúrgicos da Phase 7.6 (gap closure descoberto em uso real pelo advogado).
- feat(07.6-01): bump limite parcelas 360→999 em AdicionarDocumento — cobre imobiliário 35 anos (420x), consignado (96x), e teto prático 999x.
- feat(07.6-02): virtualização @tanstack/react-virtual em TabelaParcelasEditaveis — render via useVirtualizer (overscan=10, maxHeight=400px, CSS grid com ARIA roles) para evitar degradação de DOM em N grande.

Motor Art.354 (devedorCalc.js + pagamentos.js) LITERALMENTE intocado — invariante estrutural D-01.
Schema do banco inalterado (contratos.js CHECK num_parcelas >= 1 já aceita qualquer N positivo).
Contrato de props de TabelaParcelasEditaveis INTOCADO (D-02 da Phase 7.5 preservada).
State valoresStr + setter atualizarValorStr (fix 07.5.1) preservados.
Validações 100% preservadas: soma===valorTotal (tolerância R$0,01), datas preenchidas, ordem não-decrescente, readonly em parcelas pagas, indicador Soma/Total/Δ em tempo real.
Regressão 9/9 verde. Build OK. UAT localhost (6 cenários D-11) reportado pelo usuário.

Tag v1.4-phase7.6 pendente — só após UAT de produção em ação separada (padrão 7.5.1).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

</interfaces>
</context>

<tasks>

<!--
  2 tasks em 1 wave SEQUENCIAL:
  - Task 1: UAT manual dos 6 cenários D-11 + gates regressão/build/invariante motor. Bloqueia até usuário confirmar os 6 cenários.
  - Task 2: Duplo checkpoint (PAUSA #1 diff review do submodule + PAUSA #2 bump review do pai) e commit final do bump no pai.
  Task 2 SÓ executa depois que Task 1 for autorizada — e Task 2 tem 2 gates humanos próprios.
  NENHUMA das duas tasks faz push. Tag é ação separada do usuário, fora desta phase.
-->

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: UAT localhost dos 6 cenários D-11 + regressão 9/9 + build exit 0 + prova invariante motor Art.354</name>
  <files>
    (nenhum arquivo modificado nesta task — é UAT manual + gates determinísticos)
  </files>

  <what-built>
    Tasks dos planos 07.6-01 e 07.6-02 já commitaram localmente no submodule `src/mr-3/mr-cobrancas` branch `main`:
    - **Commit A (07.6-01):** `feat(07.6-01): bump limite parcelas 360→999 em AdicionarDocumento` — 1 arquivo.
    - **Commit B (07.6-02):** `feat(07.6-02): virtualização @tanstack/react-virtual em TabelaParcelasEditaveis` — 3 arquivos.

    Esta Task 1 executa os 3 gates determinísticos + UAT manual em localhost conforme D-11:
    1. **Gate regressão:** `npm run test:regressao` 9/9 no submodule (motor Art.354 não tocado → pass obrigatório).
    2. **Gate build:** `npm run build` exit 0 no submodule.
    3. **Gate invariante motor:** `git diff HEAD~2 -- src/utils/devedorCalc.js src/services/pagamentos.js` → vazio.
    4. **UAT manual (6 cenários D-11 do CONTEXT):**
       - SC-1: criar documento N=10 (path virtualizado com poucas linhas — sanity).
       - SC-2: criar documento N=100 (path virtualizado realista) — observar lag de keystroke (D-10 monitora).
       - SC-3: Soma/Total/Δ atualiza em tempo real scrollando + editando parcelas fora da viewport.
       - SC-4: "Sugerir datas Mensal" com N=100 — verificar que datas fora da viewport foram preenchidas.
       - SC-5: tentar N=1000 no input — toast vermelho bloqueia, sem submit.
       - SC-6: foco retention — digitar em parcela 50, scrollar até topo, voltar: foco no input esperado (overscan=10 mitiga).

    Agente reporta resultado de cada gate + cenário e **aguarda confirmação explícita do usuário** antes de abrir a Task 2.
  </what-built>

  <read_first>
    - `.planning/phases/07.6-parcelas-n-grande/07.6-CONTEXT.md` — §4 D-01 (INV motor), D-04 (sempre virtualiza), D-06 (maxHeight 400px), D-07 (overscan 10), D-08 (validações preservadas), D-10 (React.memo monitorado, não aplicado), D-11 (6 cenários UAT EXATOS), §5 Success Criteria
    - `.planning/phases/07.6-parcelas-n-grande/07.6-01-PLAN.md` e `07.6-02-PLAN.md` — confirmar SHAs esperados (lendo git log após Task 1 de cada)
    - `.planning/phases/07.5.1-sincronizacao-saldosmap-ux-input-valor/07.5.1-01-PLAN.md` linhas 415-556 (Task 3) — **template literal** do padrão de verificação (gates determinísticos + 2 commits submodule existentes + aguardar autorização antes de bumpar)
  </read_first>

  <how-to-verify>

### PARTE A — Gates determinísticos (agente executa antes de abrir UAT manual)

**Pré-requisito:** cwd = raiz do repo pai.

**Passo 1 — Confirmar branch e 2 commits locais no submodule main:**
```bash
cd src/mr-3/mr-cobrancas
git rev-parse --abbrev-ref HEAD
# esperado: main
git log -2 --format="%H %s" origin/main..main
# esperado: EXATAMENTE 2 linhas, mais recente primeiro:
# <SHA2> feat(07.6-02): virtualização @tanstack/react-virtual em TabelaParcelasEditaveis
# <SHA1> feat(07.6-01): bump limite parcelas 360→999 em AdicionarDocumento
```

Se o output não bater exatamente o esperado: **PARAR** e reportar `git log -5 --format="%H %s" origin/main..main` completo pro usuário diagnosticar.

**Passo 2 — Gate regressão (bloqueante, D-01 invariante estrutural):**
```bash
cd src/mr-3/mr-cobrancas
npm run test:regressao
```
Esperado: `9/9 PASS`. Se falhar, **PARAR** — diagnóstico:
```bash
git diff HEAD~2 -- src/utils/devedorCalc.js src/services/pagamentos.js
```
Se qualquer byte de diff → motor foi tocado indevidamente numa das 2 tasks anteriores; reverter via `git revert <SHA_ofensor>` ou solicitar rollback manual do usuário antes de prosseguir.

**Passo 3 — Gate build (bloqueante):**
```bash
cd src/mr-3/mr-cobrancas
npm run build
```
Exit 0 obrigatório. Se falhar, reportar saída completa (linha exata do erro) — causas prováveis em ordem de probabilidade:
- Import do `useVirtualizer` quebrado ou `@tanstack/react-virtual` não instalado (`cd src/mr-3/mr-cobrancas && ls node_modules/@tanstack/react-virtual/package.json`).
- JSX mal fechado na região refatorada do 07.6-02.
- Typo em alguma edição do 07.6-01 (quotes faltando em `max="999"` ou mensagem do toast).

**Passo 4 — Gate invariante motor (bloqueante, D-01):**
```bash
cd src/mr-3/mr-cobrancas
git diff HEAD~2 -- src/utils/devedorCalc.js
git diff HEAD~2 -- src/services/pagamentos.js
```
**Esperado:** ambos vazios (0 bytes). Se qualquer um tiver conteúdo → reportar EXATAMENTE o diff pro usuário e pedir orientação (revert do commit ofensor, ou ignorar se for cosmetic whitespace acidental — mas default é bloquear até confirmação humana).

**Passo 5 — Sanidade dos 2 arquivos-chave da phase (grep de verificação pós-commit):**
```bash
cd src/mr-3/mr-cobrancas
grep -c 'max="999"' src/components/AdicionarDocumento.jsx                                    # esperado ≥1
grep -c "parseInt(numParcelas) > 999" src/components/AdicionarDocumento.jsx                  # esperado 1
grep -c "useVirtualizer" src/components/TabelaParcelasEditaveis.jsx                          # esperado ≥2 (import + chamada)
grep -c "overscan: 10" src/components/TabelaParcelasEditaveis.jsx                            # esperado 1
grep -c "maxHeight: 400" src/components/TabelaParcelasEditaveis.jsx                          # esperado 1
grep -c 'role="table"\|role="row"\|role="cell"' src/components/TabelaParcelasEditaveis.jsx   # esperado ≥6
grep -c "<table\|<thead\|<tbody\|<tr>\|<th>\|<td>" src/components/TabelaParcelasEditaveis.jsx  # esperado 0
grep -c "@tanstack/react-virtual" package.json                                               # esperado 1
```

Se QUALQUER gate acima falhar → reportar ao usuário e **NÃO** prosseguir para Parte B.

---

### PARTE B — UAT manual dos 6 cenários D-11 (usuário executa no browser localhost)

**Passo 6 — Iniciar dev server:**
```bash
cd src/mr-3/mr-cobrancas
npm run dev
```
(Rodar em background — Bash tool com `run_in_background: true`. Capturar URL do output — tipicamente `http://localhost:5173/` ou similar.)

**Passo 7 — Reportar os 6 cenários ao usuário com formato EXATO:**

> ```
> ## UAT Phase 7.6 — 6 cenários D-11 em localhost
> Dev server: <URL capturada do npm run dev>
>
> Por favor execute os 6 cenários abaixo no browser e reporte resultado de cada um
> (PASS / FAIL / nota). Todos devem passar antes de eu prosseguir para o bump do pai.
>
> ### SC-1 — Criar documento N=10 (sanity path virtualizado)
> 1. Abrir um Contrato existente ou criar novo
> 2. Clicar "Adicionar Documento"
> 3. Preencher: Tipo=NF, Valor=R$ 1000, Data=hoje, Nº Parcelas=10
> 4. Preencher Data da parcela 1 (qualquer)
> 5. Clicar "Sugerir datas" > Mensal → 9 datas adicionais preenchidas
> 6. Verificar: 10 linhas visíveis (N pequeno, scroll não esperado); Soma R$ 1000 / Total R$ 1000 verde
> 7. Clicar "Adicionar Documento" → toast "Documento adicionado com 10 parcelas"
> Pass/Fail:
>
> ### SC-2 — Criar documento N=100 (path virtualizado realista, D-10 monitora lag)
> 1. Novo documento; Valor=R$ 10000; Nº Parcelas=100
> 2. Preencher Data parcela 1; Sugerir datas > Mensal
> 3. Verificar: container com scroll vertical, apenas ~10-15 linhas + overscan no DOM (devtools > Elements > contar <div role="row">)
> 4. Editar valor da parcela 1 (digitar devagar 3-5 caracteres); perceber lag de keystroke
> 5. Clicar "Adicionar Documento" → toast "Documento adicionado com 100 parcelas"
> Pass/Fail + nota sobre lag percebido (aceitável? severo?):
>
> ### SC-3 — Soma/Total/Δ ao vivo durante scroll + edição fora da viewport
> 1. Novo documento N=100 (como SC-2)
> 2. Scrollar até parcela 50, editar valor de R$ 100 → R$ 200 (keystroke-by-keystroke)
> 3. Olhar o indicador no topo (Soma: ... / Total: ... (Δ +...))
> 4. Verificar que indicador atualiza em tempo real mesmo quando a parcela editada está fora da viewport atual (scrollar pra baixo após editar e ver que indicador continua atualizado)
> Pass/Fail:
>
> ### SC-4 — Sugerir datas com N=100 (verificar linhas fora da viewport)
> 1. Novo documento N=100, valor R$ 10000
> 2. Preencher Data parcela 1 = 2026-01-15
> 3. Clicar "Sugerir datas" > Quinzenal
> 4. Scrollar até parcela 50 → verificar data preenchida (2026-12-31 aprox, 50*15 dias após base)
> 5. Scrollar até parcela 99 → verificar data preenchida (2030-04-xx aprox)
> Pass/Fail:
>
> ### SC-5 — Bloqueio N=1000 (toast vermelho, sem submit)
> 1. Novo documento, valor qualquer, Data qualquer
> 2. Nº Parcelas = 1000
> 3. Notar que o atributo HTML max="999" PODE impedir digitar 4 dígitos dependendo do browser (Chrome/Firefox geralmente permitem digitar mas destacam inválido)
> 4. Se conseguiu digitar 1000: botão "Adicionar Documento" fica desabilitado (podesSalvar <= 999 bloqueia)
> 5. Forçar clique via devtools (se quiser): handleSalvar emite toast vermelho "Número de parcelas máximo é 999 (limite prático para empréstimos imobiliários de até 35 anos)."
> 6. Verificar que NENHUMA linha aparece na tabela editável (parcelasIniciais é null quando N>999 pela condição do useState da linha ~50 de AdicionarDocumento.jsx)
> Pass/Fail:
>
> ### SC-6 — Foco retention após scroll + edição (overscan=10 mitiga)
> 1. Novo documento N=100
> 2. Clicar no input valor da parcela 50; digitar (foco no input)
> 3. Scrollar pra cima até parcela 10 (parcela 50 sai da viewport + overscan)
> 4. Scrollar de volta pra parcela 50
> 5. Foco esperado: input de valor da parcela 50 (aceitável se perdeu foco — D-07 só mitiga, não elimina 100%). Reportar qual foi o comportamento observado.
> Pass/Fail (notar que perda parcial de foco é RISCO CONHECIDO — D-07 registra):
>
> ---
> Quando responder, também confirme: nenhum erro de console (abrir devtools > Console),
> nenhum warning React novo (ignorar warnings pré-existentes da base).
> ```

**Passo 8 — AGUARDAR resposta do usuário.** Não prosseguir para Task 2 sem confirmação explícita de que os 6 cenários passaram (ou que falhas são conhecidas/aceitáveis, tipo SC-6 com foco perdido parcial = D-07 já prevê).

Se o usuário reportar FAIL genuíno:
- Bugs em SC-1/SC-2/SC-4: provavelmente bug no refactor do 07.6-02 (useVirtualizer). Diagnóstico via `grep -n "virtualItem" src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx`.
- Bugs em SC-3: indicador Soma/Total/Δ quebrado? Verificar `grep -n "Soma: {fmtBRL(soma)}" ...`.
- Bugs em SC-5: validação não disparou? Verificar `grep -n "parseInt(numParcelas) > 999" src/mr-3/mr-cobrancas/src/components/AdicionarDocumento.jsx`.
- Bugs em SC-6: foco perdido COMPLETAMENTE? `D-07` prevê mitigação parcial, não total — reportar severidade pro usuário decidir se aceita risco.

Em qualquer FAIL bloqueante: reverter o commit ofensor (07.6-01 ou 07.6-02) no submodule via `git revert` e NÃO prosseguir com Task 2; abrir novo ciclo.

  </how-to-verify>

  <acceptance_criteria>
    - `cd src/mr-3/mr-cobrancas && git log -2 --format=%s origin/main..main` retorna EXATAMENTE 2 linhas com subjects `feat(07.6-02): ...` e `feat(07.6-01): ...` (nessa ordem, mais recente primeiro)
    - `cd src/mr-3/mr-cobrancas && npm run test:regressao` exit 0 (9/9 PASS)
    - `cd src/mr-3/mr-cobrancas && npm run build` exit 0
    - `cd src/mr-3/mr-cobrancas && git diff HEAD~2 -- src/utils/devedorCalc.js` retorna **vazio** (INV motor D-01)
    - `cd src/mr-3/mr-cobrancas && git diff HEAD~2 -- src/services/pagamentos.js` retorna **vazio** (INV motor D-01)
    - Grep dos 8 patterns da Parte A Passo 5 retornam os contadores esperados (verificação determinística do trabalho das 2 tasks anteriores)
    - Usuário reportou resultado dos 6 cenários SC-1..SC-6 no chat (texto contendo "SC-1", "SC-2", "SC-3", "SC-4", "SC-5", "SC-6" ou equivalente) e indicou aprovação para prosseguir para Task 2
    - **NENHUM** comando `git push` executado em Parte A nem durante Parte B
  </acceptance_criteria>

  <resume-signal>
    Para prosseguir, responda:
    - **`approved`** ou **`next`** ou **`ship it`** → UAT passou, agente abre Task 2 (duplo checkpoint do bump do pai)
    - **Qualquer descrição de FAIL** → reportar cenário e comportamento; agente investiga e NÃO avança para Task 2.
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Duplo checkpoint (PAUSA #1 diff review + PAUSA #2 bump review) + commit bump do submodule no pai branch master (SEM PUSH)</name>
  <files>
    src/mr-3 (submodule pointer no repo pai — bump)
  </files>

  <what-built>
    Após UAT da Task 1 autorizado pelo usuário, executa o protocolo duplo-checkpoint do padrão 7.2/7.3/7.4/7.5/7.5.1 (D-14):

    - **PAUSA #1:** mostrar os 2 commits do submodule (subjects, name-only, diff consolidado `git diff HEAD~2`, e confirmar `git diff HEAD~2 -- src/utils/devedorCalc.js src/services/pagamentos.js` vazio). Aguardar autorização explícita.
    - **Passo intermediário:** cd pra raiz do pai, confirmar `master` + `src/mr-3` modified.
    - **PAUSA #2:** reportar SHAs + mensagem EXATA do commit proposto no pai. Aguardar autorização SEPARADA.
    - **Commit único no pai:** `git add src/mr-3 && git commit -m "chore(07.6): bump submodule mr-3 — virtualização parcelas N grande (07.6-01+02)"` com corpo conforme <interfaces> acima.
    - **Reportar SHA final do pai + confirmação SEM PUSH + próximos passos** (push e tag ficam para ação separada do usuário pós-UAT de produção).

    NENHUM dos commits do submodule é undone, amendado ou rebase-ado nesta task.
  </what-built>

  <read_first>
    - `.planning/phases/07.6-parcelas-n-grande/07.6-CONTEXT.md` — §4 D-14 (fluxo commit+bump+SEM PUSH + ambos PAUSEs), §5 Success Criteria 9
    - `.planning/phases/07.5.1-sincronizacao-saldosmap-ux-input-valor/07.5.1-01-PLAN.md` — **template literal** Task 3 Passos 6-9 (PAUSA #1 + Passo de cd, PAUSA #2, commit de bump)
    - `.planning/phases/07.5-parcelas-datas-valores-customizados/07.5-01-PLAN.md` — Task 4 = precedente adicional do padrão 10-passo
    - `.planning/STATE.md` — `last_activity` confirma padrão "commits locais, sem push" usado desde 7.1
  </read_first>

  <how-to-verify>

**Passo 1 — PAUSA #1 (checkpoint pré-bump do pai, diff review dos commits do submodule):**

Rodar e mostrar ao usuário (no chat):
```bash
cd src/mr-3/mr-cobrancas
git status --short
git log origin/main..main --stat
git log -2 --format="=== %h %s ===" origin/main..main
git diff HEAD~2 -- src/components/AdicionarDocumento.jsx
git diff HEAD~2 -- src/components/TabelaParcelasEditaveis.jsx
git diff HEAD~2 -- package.json
git diff HEAD~2 -- src/utils/devedorCalc.js        # DEVE SER VAZIO
git diff HEAD~2 -- src/services/pagamentos.js      # DEVE SER VAZIO
```

(Para `package-lock.json`, diff pode ser enorme — agente reporta apenas:
```bash
git diff --stat HEAD~2 -- package-lock.json
```
com explicação "diff grande do npm install, omitido por tamanho — pode revisar depois se quiser com `git diff HEAD~2 -- package-lock.json`".)

Capturar SHAs:
```bash
SHA1=$(git log origin/main..main --reverse --format=%H | head -1)     # feat(07.6-01)
SHA2=$(git log origin/main..main --format=%H | head -1)               # feat(07.6-02) — tip
SHA1_short=$(echo $SHA1 | cut -c1-7)
SHA2_short=$(echo $SHA2 | cut -c1-7)
```

Reportar (replicar texto EXATO do <interfaces> PAUSA #1 acima, substituindo `<SHA1_7chars>` e `<SHA2_7chars>`).

**AGUARDAR resposta do usuário.** NÃO prosseguir sem resposta `approved` ou `bump it` (ou equivalente explícito).

Se o usuário apontar correção necessária num dos 2 commits:
- Correção no commit mais recente (07.6-02): `git commit --amend` OU commit 3º de correção (preferível — amend pode quebrar SHA já reportado). Re-rodar PAUSA #1.
- Correção no commit anterior (07.6-01): 3º commit de correção é preferível (rebase interativo é destrutivo e requer autorização explícita separada).
- Em qualquer caso: re-rodar Passos 1-4 da Task 1 (gates determinísticos) antes de apresentar PAUSA #1 de novo.

---

**Passo 2 — Após PAUSA #1 autorizada: cd pra raiz do repo pai e verificar estado:**

```bash
cd ../../..                                                # volta pra raiz do pai (mr-cobrancas-vercel_1/mr-cobrancas)
pwd                                                        # confirmar path correto
git rev-parse --abbrev-ref HEAD                           # esperado: master
git status --short
```

Esperado:
- `pwd` imprime path do repo pai.
- Branch = `master`.
- `git status` mostra `src/mr-3` como modified (bump pendente). **Pode** aparecer entries pré-existentes (`.planning/STATE.md`, `.planning/ROADMAP.md` modificados ou deletados por sessões anteriores — vide gitStatus no planning_context). **Essas entries NÃO fazem parte desta phase.**

**IMPORTANTE — hygiene do commit do pai:** No próximo passo, `git add src/mr-3` adiciona APENAS o bump do submodule. **NÃO** usar `git add -A` / `git add .` / `git add -u`. Qualquer outro arquivo modificado no pai fica como `unstaged` e o commit NÃO os inclui. Isso é intencional — phases anteriores deixaram arquivos modificados que não devem ser resgatados por esta phase.

---

**Passo 3 — PAUSA #2 (checkpoint pré-commit do pai, bump review — separada da PAUSA #1):**

Reportar (replicar texto EXATO do <interfaces> PAUSA #2 acima, substituindo `<SHA1_7chars>`, `<SHA2_7chars>`, `<SHA2_full>`).

**AGUARDAR resposta do usuário.** NÃO prosseguir sem resposta explícita `bump it` ou `approved` (ou equivalente).

A autorização da PAUSA #1 **NÃO** cobre a PAUSA #2 — são 2 gates distintos (D-14).

---

**Passo 4 — Após PAUSA #2 autorizada: commit do bump no pai:**

```bash
# Continuamos na raiz do pai.
git add src/mr-3                                           # APENAS o bump do submodule, nada mais
git status --short                                         # confirmar SOMENTE `M src/mr-3` stageado (outros entries ficam unstaged, não são commitados)

git commit -m "$(cat <<'EOF'
chore(07.6): bump submodule mr-3 — virtualização parcelas N grande (07.6-01+02)

Submodule src/mr-3 @ branch main: 2 commits cirúrgicos da Phase 7.6 (gap closure descoberto em uso real pelo advogado).
- feat(07.6-01): bump limite parcelas 360→999 em AdicionarDocumento — cobre imobiliário 35 anos (420x), consignado (96x), e teto prático 999x.
- feat(07.6-02): virtualização @tanstack/react-virtual em TabelaParcelasEditaveis — render via useVirtualizer (overscan=10, maxHeight=400px, CSS grid com ARIA roles) para evitar degradação de DOM em N grande.

Motor Art.354 (devedorCalc.js + pagamentos.js) LITERALMENTE intocado — invariante estrutural D-01.
Schema do banco inalterado (contratos.js CHECK num_parcelas >= 1 já aceita qualquer N positivo).
Contrato de props de TabelaParcelasEditaveis INTOCADO (D-02 da Phase 7.5 preservada).
State valoresStr + setter atualizarValorStr (fix 07.5.1) preservados.
Validações 100% preservadas: soma===valorTotal (tolerância R$0,01), datas preenchidas, ordem não-decrescente, readonly em parcelas pagas, indicador Soma/Total/Δ em tempo real.
Regressão 9/9 verde. Build OK. UAT localhost (6 cenários D-11) reportado pelo usuário.

Tag v1.4-phase7.6 pendente — só após UAT de produção em ação separada (padrão 7.5.1).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"

git log -1 --format=%H                                    # capturar SHA do pai para reportar
```

---

**Passo 5 — Reportar SHAs finais + estado "zero push" + próximos passos:**

Formato EXATO no chat:
```
## Phase 7.6 shipped LOCALLY (SEM PUSH)

Submodule src/mr-3/mr-cobrancas @ main:
- <SHA1_7chars> feat(07.6-01): bump limite parcelas 360→999 em AdicionarDocumento
- <SHA2_7chars> feat(07.6-02): virtualização @tanstack/react-virtual em TabelaParcelasEditaveis

Parent repo master:
- <SHA_pai_7chars> chore(07.6): bump submodule mr-3 — virtualização parcelas N grande (07.6-01+02)

Invariante motor Art.354: git diff HEAD~2 -- src/utils/devedorCalc.js src/services/pagamentos.js → VAZIO. 9/9 regressão preservada. Build exit 0.

UAT localhost 6 cenários D-11: reportados pelo usuário na Task 1.

Próximos passos (ação separada do usuário, NÃO executar agora):
1. **Push quando autorizar:** `cd src/mr-3/mr-cobrancas && git push origin main` + `cd <raiz_pai> && git push origin master` (NESSA ORDEM — submodule primeiro, pai depois, pra evitar push com ponteiro quebrado).
2. **Deploy Vercel:** deploy auto após push do pai (cache headers da 7.4 garantem F5 serve versão nova).
3. **UAT de produção:** testar N=100 real em mrcobrancas.com.br.
4. **Tag `v1.4-phase7.6`:** criar SOMENTE após UAT de prod autorizado (`git tag v1.4-phase7.6 <SHA_pai>` + push tag).

Status: phase 7.6 pronta para push — zero push executado nesta sessão.
```

---

**Passo 6 — Atualizar SUMMARY + (opcionalmente) STATE/ROADMAP:**

Criar `.planning/phases/07.6-parcelas-n-grande/07.6-03-SUMMARY.md` com:
- Arquivos modificados (lista com SHAs)
- Output das 2 PAUSAs (texto do usuário que autorizou)
- Output do UAT (PASS/FAIL de cada cenário SC-1..SC-6, reportado pelo usuário)
- Evidência do invariante motor Art.354
- Próximos passos EXATOS (push autorizado separado, tag pós-UAT prod)

**NÃO** atualizar `.planning/STATE.md` ou `.planning/ROADMAP.md` nesta task — essa é ação do `/gsd-close-phase` ou `/gsd-complete-phase` (padrão do workflow GSD).

---

**Passo 7 — Confirmar SEM PUSH:**

```bash
cd src/mr-3/mr-cobrancas
git log origin/main..main --oneline                       # esperado: 2 linhas (07.6-01 + 07.6-02)
cd ../../..
git log origin/master..master --oneline                   # esperado: ≥1 linha (o commit de bump desta phase)
```

Se alguma linha for 0 → push foi executado indevidamente, investigar. (Não deveria acontecer — nenhum comando `git push` foi chamado nesta phase.)

  </how-to-verify>

  <acceptance_criteria>
    **PAUSA #1 autorizada:**
    - Agente executou `git log origin/main..main --stat` e mostrou ao usuário os 2 commits (subjects + name-only + diffs dos 3 arquivos mexidos)
    - Agente reportou `git diff HEAD~2 -- src/utils/devedorCalc.js src/services/pagamentos.js` vazio (evidência INV motor)
    - Usuário respondeu `approved`, `bump it`, ou equivalente explícito ANTES do commit no pai
    - Texto do chat contém marcador `PAUSA #1` ou equivalente

    **PAUSA #2 autorizada:**
    - Agente cd pra raiz do pai e executou `git rev-parse --abbrev-ref HEAD` retornando `master`
    - Agente reportou SHAs EXATOS (`<SHA1_7chars>` + `<SHA2_7chars>`) ANTES do commit
    - Agente reportou mensagem EXATA do commit proposto: `chore(07.6): bump submodule mr-3 — virtualização parcelas N grande (07.6-01+02)`
    - Usuário respondeu `bump it`, `approved`, ou equivalente explícito ANTES do commit (autorização SEPARADA da PAUSA #1)
    - Texto do chat contém marcador `PAUSA #2` ou equivalente

    **Commit do pai correto:**
    - `git rev-parse --abbrev-ref HEAD` na raiz do pai retorna `master`
    - `git log -1 --format=%s` retorna EXATAMENTE `chore(07.6): bump submodule mr-3 — virtualização parcelas N grande (07.6-01+02)`
    - `git log -1 --name-only --format=` lista EXATAMENTE 1 arquivo: `src/mr-3`
    - `git show --stat HEAD` mostra 1 submodule pointer atualizado (tipicamente formato `Subproject commit <SHA_antigo> → <SHA_novo>`)
    - `git log -1 --format=%b` contém os subjects dos 2 commits do submodule + referência à invariante D-01 + "Tag v1.4-phase7.6 pendente"

    **Zero push:**
    - `cd src/mr-3/mr-cobrancas && git log origin/main..main --oneline` retorna **exatamente 2 linhas** (07.6-01 + 07.6-02 locais)
    - `cd <raiz do pai> && git log origin/master..master --oneline` retorna **≥1 linha** (o commit de bump + possivelmente commits de phases anteriores como 07.5.1 que também não foram pushed)
    - **NENHUM** comando `git push` foi executado em toda a Task 2 (histórico do shell deve confirmar — se executado, falha crítica)
    - **NENHUMA** tag Git foi criada (`git tag -l "v1.4-phase7.6"` retorna vazio)

    **SUMMARY criado:**
    - `.planning/phases/07.6-parcelas-n-grande/07.6-03-SUMMARY.md` existe e contém: SHA submodule x2, SHA pai x1, resultado UAT 6 cenários, evidência invariante motor, próximos passos.

    **Nenhum arquivo fora de escopo foi commitado:**
    - `git show HEAD --stat` no pai mostra EXATAMENTE 1 entrada: `src/mr-3` (ou equivalente "Submodule src/mr-3")
    - Arquivos `.planning/STATE.md`, `.planning/ROADMAP.md` etc. pré-existentes como modified NÃO aparecem no commit de bump (ficam unstaged)
  </acceptance_criteria>

  <resume-signal>
    Esta task tem 2 gates humanos internos (PAUSA #1 e PAUSA #2). Cada gate espera autorização SEPARADA (`approved` / `bump it`). Agente NÃO prossegue sem ambas.

    Após Passo 5 reportado: task completa. Usuário pode encerrar a sessão ou pedir `/gsd-close-phase 07.6` para registrar ship no STATE/ROADMAP (fora do escopo desta task).
  </resume-signal>
</task>

</tasks>

<verification>
Verificação final da phase (após Task 2 completa):

1. **Submodule main (estado final):**
   - `cd src/mr-3/mr-cobrancas && git log origin/main..main --oneline` → 2 linhas (07.6-01 + 07.6-02)
   - `cd src/mr-3/mr-cobrancas && git diff HEAD~2 -- src/utils/devedorCalc.js src/services/pagamentos.js` → vazio
   - `cd src/mr-3/mr-cobrancas && npm run test:regressao` → 9/9 PASS
   - `cd src/mr-3/mr-cobrancas && npm run build` → exit 0

2. **Pai master (estado final):**
   - `cd <raiz> && git log -1 --format=%s` → `chore(07.6): bump submodule mr-3 — virtualização parcelas N grande (07.6-01+02)`
   - `cd <raiz> && git log -1 --name-only --format=` → `src/mr-3`
   - `cd <raiz> && git log origin/master..master --oneline` → inclui o commit novo desta phase

3. **Zero push:**
   - Verificado inline no Passo 7 da Task 2 — ambos os `git log origin/*..` retornam ≥1 commit local; nenhum `git push` executado.

4. **Sem tag:**
   - `cd <raiz> && git tag -l "v1.4-phase7.6"` → vazio (padrão 7.5.1 — tag só após UAT prod)

5. **UAT:**
   - 6 cenários SC-1..SC-6 reportados pelo usuário no chat (Task 1), agente referenciou no SUMMARY.

6. **SUMMARY criado** com os 6 data points (SHAs + UAT + invariante motor + zero push + próximos passos).
</verification>

<success_criteria>
Plan 07.6-03 bem-sucedido quando:
- ✅ Task 1: gates determinísticos PASS (regressão 9/9, build exit 0, git diff motor vazio, 2 commits no submodule presentes, greps de verificação OK)
- ✅ Task 1: UAT 6 cenários SC-1..SC-6 reportados pelo usuário e aceitos
- ✅ Task 2 PAUSA #1: diff review autorizado explicitamente (`approved` / `bump it`)
- ✅ Task 2 PAUSA #2: bump review autorizado explicitamente (`approved` / `bump it`, SEPARADA da PAUSA #1)
- ✅ Task 2: commit no pai branch master com subject EXATO e **APENAS** `src/mr-3` stageado (zero arquivos fora de escopo)
- ✅ Zero push em toda a phase (ambos os `git log origin/*..` retornam ≥1 commit local)
- ✅ Zero tag criada (tag fica para ação separada do usuário pós-UAT prod)
- ✅ SUMMARY criado em `.planning/phases/07.6-parcelas-n-grande/07.6-03-SUMMARY.md`
- ✅ Invariante motor Art.354 preservada de ponta a ponta (verificada 3x: 07.6-01, 07.6-02, 07.6-03)
</success_criteria>

<output>
Após conclusão desta task, criar `.planning/phases/07.6-parcelas-n-grande/07.6-03-SUMMARY.md` contendo:
- SHAs do submodule (2 commits — 07.6-01 e 07.6-02) e do pai (1 commit — bump)
- Resultado UAT dos 6 cenários SC-1..SC-6 (copiar literalmente o report do usuário)
- Saída dos gates determinísticos (regressão + build + git diff motor vazio)
- Texto literal das 2 autorizações do usuário (PAUSA #1 e PAUSA #2)
- Confirmação explícita: **zero push executado, zero tag criada**
- Próximos passos exatos: push quando autorizar + deploy Vercel + UAT produção + tag v1.4-phase7.6 pós-UAT prod

O SUMMARY é o artefato de fechamento da phase — `/gsd-close-phase` usa para atualizar STATE.md e ROADMAP.md.
</output>
</content>
</invoke>