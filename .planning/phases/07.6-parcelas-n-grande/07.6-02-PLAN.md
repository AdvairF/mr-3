---
phase: 07.6-parcelas-n-grande
plan: 02
type: execute
wave: 2
depends_on:
  - 07.6-01
files_modified:
  - src/mr-3/mr-cobrancas/package.json
  - src/mr-3/mr-cobrancas/package-lock.json
  - src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx
autonomous: true   # auto: install dep + refactor + commit no submodule. Checkpoint humano é no plano 07.6-03 (UAT + bump pai + tag).
requirements: []   # Gap closure — no formal REQ-IDs. Contrato de verificação = Success Criteria 3, 4, 5 em 07.6-CONTEXT.md § 5.

must_haves:
  truths:
    - "SC-1: @tanstack/react-virtual v3 listado em dependencies de src/mr-3/mr-cobrancas/package.json (D-03)"
    - "SC-2: `useVirtualizer` importado e chamado em TabelaParcelasEditaveis.jsx; overscan=10 (D-07); estimateSize retorna altura fixa razoável"
    - "SC-3: Estrutura DOM = CSS grid com role=\"table\" / role=\"row\" / role=\"cell\" (D-05); NENHUM <table>/<thead>/<tbody> no arquivo pós-refactor"
    - "SC-4: Container de scroll tem maxHeight=400px E overflowY=\"auto\" (D-06)"
    - "SC-5: Contrato de props INALTERADO — assinatura exporta default TabelaParcelasEditaveis({ valorTotal, parcelasIniciais, modoEdicao, dividasComPagamentoIds, onChange, onSubmit, onCancel, hideFooter }) intacta (D-02 da Phase 7.5)"
    - "SC-6: Validações D-08 100% preservadas — soma===valorTotal (tolerância R$0,01), todas datas preenchidas, ordem não-decrescente, readonly em parcelas pagas, indicador Soma/Total/Δ em tempo real (cor verde quando |diff|<=0,01, vermelha caso contrário)"
    - "SC-7: `sugerirDatas` permanece síncrono (D-09) — sem async/await em `sugerirDatas`, sem Promise/setTimeout"
    - "SC-8: `atualizarValorStr`, `atualizarLinha`, state `valoresStr` (fix 07.5.1) PRESERVADOS — input valor continua usando valoresStr[i]"
    - "SC-9: Motor Art.354 literalmente intocado — `git diff HEAD -- src/utils/devedorCalc.js src/services/pagamentos.js` vazio"
    - "SC-10: npm run test:regressao 9/9 verde; npm run build exit 0; zero push"
  artifacts:
    - path: "src/mr-3/mr-cobrancas/package.json"
      provides: "Dependência @tanstack/react-virtual v3 disponível em runtime"
      contains: "@tanstack/react-virtual"
    - path: "src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx"
      provides: "Renderiza N parcelas (1 a 999) com virtualização via useVirtualizer — só linhas visíveis + overscan=10 ficam no DOM; estrutura CSS grid com ARIA roles; maxHeight 400px; todas validações e UX preexistentes intactas."
      contains: "useVirtualizer"
  key_links:
    - from: "src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx (ref container scroll)"
      to: "hook useVirtualizer({ count: parcelas.length, getScrollElement: () => scrollRef.current, estimateSize: () => 44, overscan: 10 })"
      via: "import { useVirtualizer } from '@tanstack/react-virtual' + useRef pro container + chamada do hook no corpo do componente"
      pattern: "useVirtualizer\\(\\{"
    - from: "virtualItems (rowVirtualizer.getVirtualItems())"
      to: "render de linhas via .map(virtualItem => ...) com transform: translateY(virtualItem.start) e position: absolute dentro de um wrapper com height: rowVirtualizer.getTotalSize()"
      via: "padrão canônico @tanstack/react-virtual — scroll container > inner height=totalSize > absolute rows com translateY"
      pattern: "getVirtualItems\\(\\)"
    - from: "input valor dentro de cada linha virtualizada"
      to: "valoresStr[virtualItem.index] + atualizarValorStr(virtualItem.index, e.target.value)"
      via: "index correto no virtualizer — NÃO usar `i` do .map, mas `virtualItem.index` (pode pular números)"
      pattern: "virtualItem\\.index"
    - from: "indicador Soma/Total/Δ (header, fora do scroll container)"
      to: "useMemo soma/diff (iteração em parcelas[] completa — não só virtualItems)"
      via: "MANTIDO FORA do container virtualizado — renderiza uma vez mesmo com N=999; iteração O(N) por keystroke é aceitável (D-10 monitora em UAT)"
      pattern: "Soma: \\{fmtBRL\\(soma\\)\\}"
---

<objective>
Refactor interno de `TabelaParcelasEditaveis.jsx` para virtualizar o render da tabela de parcelas via `@tanstack/react-virtual` v3. Permite N grande (até 999) sem degradação de DOM/paint; mantém 100% das validações, UX preexistente e contrato de props (D-02 da Phase 7.5 é INVARIANTE).

Plano atômico, single-commit, revertível por `git revert` se UAT detectar problema. O plano 07.6-01 (bump de limite) já foi commitado; este plano é pré-requisito (wave 2) antes do checkpoint humano do 07.6-03.

Escopo:
1. **Instalar `@tanstack/react-virtual` v3** no submodule `src/mr-3/mr-cobrancas` (D-03). Atualiza `package.json` + `package-lock.json`.
2. **Refatorar o render da tabela** (linhas ~222-275 atuais) de `<table>`/`<thead>`/`<tbody>` para **CSS grid com ARIA roles** (D-05), envelopado em container de scroll `maxHeight: 400px, overflowY: "auto"` (D-06).
3. **Usar `useVirtualizer`** com `overscan: 10` (D-07). Renderizar apenas `getVirtualItems()` (linhas visíveis + overscan).
4. **Preservar INTOCADO:** `valoresStr` state, `atualizarValorStr`, `atualizarLinha`, `sugerirDatas` (síncrono, D-09), `sugerirDatasPersonalizado`, `handleSalvar` com as 3 validações (linhas 134-170), indicador Soma/Total/Δ (linhas 174-182), dropdown "Sugerir datas" (linhas 185-219), botões Salvar/Cancelar (linhas 277-290).
5. **Sempre virtualizar** (D-04) — zero branch `if N < 50 then render normal else virtualize`. Mesmo código pra N=3 e N=999.
6. **NÃO tocar o motor Art.354** (`utils/devedorCalc.js` e `services/pagamentos.js`) — invariante D-01. Verificação determinística via `git diff HEAD` vazio ao fim.

Purpose: Advogado consegue cadastrar contrato com N=420 (imobiliário 35 anos) ou N=999 e a UI não trava. Scroll suave na tabela, indicador Soma/Total atualiza em tempo real conforme edição, "Sugerir datas" preenche todas as linhas (mesmo fora da viewport), parcelas pagas ficam readonly, soma ≠ total continua bloqueando salvamento com toast vermelho.

Output:
- `src/mr-3/mr-cobrancas/package.json` — entry `"@tanstack/react-virtual": "^3.x"` em `dependencies`.
- `src/mr-3/mr-cobrancas/package-lock.json` — árvore de dependências atualizada consistentemente com `npm install`.
- `src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx` — refactor de ~60-80 linhas na região da tabela, com `useVirtualizer` + CSS grid + ARIA roles; resto do arquivo **preservado**.
- 1 commit local no submodule `src/mr-3/mr-cobrancas` branch `main`. Zero push (autorização no 07.6-03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07.6-parcelas-n-grande/07.6-CONTEXT.md

<!-- Phase 7.5 (origem do componente — contrato de props invariante D-02 da 7.5): -->
@.planning/phases/07.5-parcelas-datas-valores-customizados/07.5-CONTEXT.md

<!-- Phase 7.5.1 (fix UX do input valor — state valoresStr que DEVE ser preservado neste refactor): -->
@.planning/phases/07.5.1-sincronizacao-saldosmap-ux-input-valor/07.5.1-CONTEXT.md

<!-- Plano 07.6-01 (bump de limite — pré-requisito desta wave, contrato feat/fix no submodule mesma branch): -->
@.planning/phases/07.6-parcelas-n-grande/07.6-01-PLAN.md

<interfaces>
<!-- Contratos-chave extraídos direto do código atual (linhas confirmadas por leitura do planner). Executor não precisa re-explorar. -->

From src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx (contrato completo — 293 linhas):

**Imports atuais (linhas 1-3):**
```javascript
import { useState, useMemo } from "react";
import toast from "react-hot-toast";
import Btn from "./ui/Btn.jsx";
```

**Adicionar ao import React (linha 1):** `useRef`
**Novo import (após linha 1):** `import { useVirtualizer } from "@tanstack/react-virtual";`

**State atual (linhas 48-62 — INTOCADO):**
```javascript
const [parcelas, setParcelas] = useState(() =>
  (parcelasIniciais || []).map((p, i) => ({
    id: p.id,
    numero: p.numero != null ? p.numero : i + 1,
    valor_total: p.valor_total != null ? p.valor_total : 0,
    data_vencimento: p.data_vencimento || "",
  }))
);
const [valoresStr, setValoresStr] = useState(() =>          // Phase 7.5.1 fix — PRESERVAR
  (parcelasIniciais || []).map(p => (p.valor_total != null ? String(p.valor_total) : "0"))
);
const [salvando, setSalvando] = useState(false);
const [sugerirAberto, setSugerirAberto] = useState(false);

const readonlySet = dividasComPagamentoIds || new Set();
```

**Adicionar após linha 62 (novo `useRef` + chamada do hook `useVirtualizer`):**
```javascript
const parentRef = useRef(null);

const rowVirtualizer = useVirtualizer({
  count: parcelas.length,
  getScrollElement: () => parentRef.current,
  estimateSize: () => 44,     // altura média de linha em pixels (input 32 + padding ~12 = 44) — D-07 margin; overscan absorve variação
  overscan: 10,               // D-07 — mitiga perda de foco ao scrollar digitando
});
```

**Funções auxiliares atuais (PRESERVAR INTOCADAS — linhas 66-132):**
- `isReadonly(p)` — linha 66
- `atualizarLinha(i, campo, valor)` — linhas 70-76 (usado pelo input de data)
- `atualizarValorStr(i, str)` — linhas 80-93 (Phase 7.5.1 fix — input valor)
- `soma` via useMemo — linhas 95-98
- `diff` via useMemo — linha 99
- `parcela1DataPreenchida` — linha 101
- `sugerirDatas(intervaloDias)` — linhas 103-121 (SÍNCRONO, D-09 — não tocar)
- `sugerirDatasPersonalizado()` — linhas 123-132

**handleSalvar (PRESERVAR INTOCADO — linhas 134-170):** as 3 validações (soma, datas preenchidas, ordem não-decrescente) ficam como estão. **ZERO mudanças** nessa função.

**JSX — HEADER (PRESERVAR INTOCADO — linhas 172-182):**
```jsx
<div style={{ background: "#fff", border: "1px solid #e8f0f7", borderRadius: 12, padding: "12px 16px", marginTop: 8 }}>
  <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: 8 }}>
    <p style={{ fontFamily: "'Space Grotesk',sans-serif", fontWeight: 700, fontSize: 13, color: "#0f172a", margin: 0 }}>
      {modoEdicao === "edit" ? "Editar parcelas" : "Parcelas do documento"}
    </p>
    <span style={{ fontSize: 11, color: Math.abs(diff) <= 0.01 ? "#16a34a" : "#dc2626", fontWeight: 700 }}>
      Soma: {fmtBRL(soma)} / Total: {fmtBRL(valorTotal)}
      {Math.abs(diff) > 0.01 && ` (Δ ${diff > 0 ? "+" : ""}${fmtBRL(Math.abs(diff))})`}
    </span>
  </div>
```

**JSX — DROPDOWN "Sugerir datas" (PRESERVAR INTOCADO — linhas 185-219).**

**JSX — TABELA ATUAL (SUBSTITUIR — linhas 221-275):**
Este é o bloco de refactor. Estrutura atual `<div style={{ overflowX: "auto" }}> <table> <thead><tr>... <tbody>{parcelas.map(...)}`.

**DEPOIS (refactor completo desta região):**
```jsx
      {/* Tabela virtualizada (Phase 7.6 D-03/D-04/D-05/D-06/D-07) */}
      <div
        ref={parentRef}
        role="table"
        aria-rowcount={parcelas.length + 1}
        style={{
          maxHeight: 400,               // D-06
          overflowY: "auto",            // D-06
          overflowX: "auto",
          border: "1px solid #f1f5f9",
          borderRadius: 8,
          fontSize: 13,
        }}
      >
        {/* Header row (sticky) — CSS grid com 3 colunas: 60px / 1fr / 180px */}
        <div
          role="row"
          style={{
            display: "grid",
            gridTemplateColumns: "60px 1fr 180px",
            gap: 8,
            padding: "8px 10px",
            background: "#f8fafc",
            position: "sticky",
            top: 0,
            zIndex: 1,
            borderBottom: "1px solid #e2e8f0",
          }}
        >
          <div role="columnheader" style={{ fontWeight: 700, color: "#64748b", fontSize: 11, textTransform: "uppercase", letterSpacing: ".5px" }}>Nº</div>
          <div role="columnheader" style={{ fontWeight: 700, color: "#64748b", fontSize: 11, textTransform: "uppercase", letterSpacing: ".5px" }}>Valor</div>
          <div role="columnheader" style={{ fontWeight: 700, color: "#64748b", fontSize: 11, textTransform: "uppercase", letterSpacing: ".5px" }}>Data de vencimento</div>
        </div>

        {/* Inner wrapper: height = totalSize do virtualizer; linhas absolute positioned com translateY */}
        <div
          style={{
            height: rowVirtualizer.getTotalSize(),
            position: "relative",
            width: "100%",
          }}
        >
          {rowVirtualizer.getVirtualItems().map(virtualItem => {
            const i = virtualItem.index;
            const p = parcelas[i];
            const ro = isReadonly(p);
            const tooltip = ro ? "Parcela com pagamento registrado — estorne primeiro para editar (botão 'Excluir pagamento' na seção 'Pagamentos Recebidos')." : undefined;
            return (
              <div
                key={p.id || `novo-${i}`}
                role="row"
                aria-rowindex={i + 2}       // +2: header é rowindex 1, dados começam em 2
                title={tooltip}
                style={{
                  position: "absolute",
                  top: 0,
                  left: 0,
                  width: "100%",
                  height: virtualItem.size,
                  transform: `translateY(${virtualItem.start}px)`,
                  display: "grid",
                  gridTemplateColumns: "60px 1fr 180px",
                  gap: 8,
                  padding: "6px 10px",
                  borderBottom: "1px solid #f1f5f9",
                  background: ro ? "#f1f5f9" : "transparent",
                  boxSizing: "border-box",
                  alignItems: "center",
                }}
              >
                <div role="cell" style={{ color: "#64748b", fontWeight: 700 }}>
                  {ro && <span style={{ marginRight: 6 }} aria-label="parcela bloqueada">🔒</span>}
                  {p.numero}
                </div>
                <div role="cell">
                  <input
                    type="number"
                    min="0"
                    step="0.01"
                    value={valoresStr[i] ?? ""}
                    onChange={e => atualizarValorStr(i, e.target.value)}
                    disabled={ro}
                    title={tooltip}
                    style={ro ? cellInputDisabledStyle : cellInputStyle}
                  />
                </div>
                <div role="cell">
                  <input
                    type="date"
                    value={p.data_vencimento}
                    onChange={e => atualizarLinha(i, "data_vencimento", e.target.value)}
                    disabled={ro}
                    title={tooltip}
                    style={ro ? cellInputDisabledStyle : cellInputStyle}
                  />
                </div>
              </div>
            );
          })}
        </div>
      </div>
```

**JSX — FOOTER (PRESERVAR INTOCADO — linhas 277-290):** botões Salvar/Cancelar com `hideFooter` check.

**Styles existentes (usar como estão):**
- `cellInputStyle` (linha 11) — reusado no input de valor e data não-readonly.
- `cellInputDisabledStyle` (linha 12) — reusado em readonly.
- `th`, `td` (linhas 13-14) — **DEIXAR no código** (podem ficar como legado não-usado; remover é opcional — NÃO É OBRIGATÓRIO remover pra evitar risco de colateral).

Motor Art.354 (referência, NÃO modificar):
- `src/mr-3/mr-cobrancas/src/utils/devedorCalc.js` — intocado (D-01)
- `src/mr-3/mr-cobrancas/src/services/pagamentos.js` — intocado (D-01)

</interfaces>
</context>

<tasks>

<!--
  1 task única em 1 wave. Commit atômico combinando:
  - npm install da dep + mudanças em package.json/package-lock.json
  - refactor do JSX da tabela em TabelaParcelasEditaveis.jsx
  - verificação de invariante motor Art.354
  - 1 commit local no submodule main (3 arquivos: package.json, package-lock.json, TabelaParcelasEditaveis.jsx)
  Checkpoint humano fica para o plano 07.6-03 (UAT manual + bump pai + tag).
-->

<task type="auto">
  <name>Task 1: Install @tanstack/react-virtual v3 + refactor virtualizado em TabelaParcelasEditaveis.jsx</name>
  <files>
    src/mr-3/mr-cobrancas/package.json
    src/mr-3/mr-cobrancas/package-lock.json
    src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx
  </files>

  <read_first>
    - `src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx` — arquivo inteiro (293 linhas). Atenção aos blocos:
      - Linhas 1-3 (imports) e 11-14 (styles `cellInputStyle`, `cellInputDisabledStyle`, `th`, `td`)
      - Linhas 48-62 (state `parcelas`, `valoresStr`, `salvando`, `sugerirAberto`; `readonlySet`) — PRESERVAR
      - Linhas 66-77 (`isReadonly`, `atualizarLinha`) — PRESERVAR
      - Linhas 80-93 (`atualizarValorStr` — fix da 07.5.1) — PRESERVAR
      - Linhas 95-101 (`soma`, `diff`, `parcela1DataPreenchida`) — PRESERVAR
      - Linhas 103-132 (`sugerirDatas`, `sugerirDatasPersonalizado`) — PRESERVAR (SÍNCRONO, D-09)
      - Linhas 134-170 (`handleSalvar` — 3 validações) — PRESERVAR INTOCADAS
      - Linhas 172-182 (JSX header + indicador Soma/Total/Δ) — PRESERVAR
      - Linhas 185-219 (JSX dropdown "Sugerir datas") — PRESERVAR
      - **Linhas 221-275 (JSX `<table>` — ÁREA DO REFACTOR)** — SUBSTITUIR por virtualização CSS grid
      - Linhas 277-290 (JSX footer — Salvar/Cancelar com `hideFooter`) — PRESERVAR
    - `src/mr-3/mr-cobrancas/package.json` — verificar ausência atual de `@tanstack/react-virtual` nas `dependencies`; confirmar que `react` e `react-dom` estão em versão compatível (@tanstack/react-virtual v3 requer React 16.8+; qualquer projeto Vite atual passa)
    - `.planning/phases/07.6-parcelas-n-grande/07.6-CONTEXT.md` — §4 D-01 (motor intocado), D-03 (lib v3), D-04 (sempre virtualizar), D-05 (CSS grid + ARIA), D-06 (maxHeight 400px), D-07 (overscan 10), D-08 (validações preservadas), D-09 (sugerirDatas síncrono), D-10 (React.memo NÃO aplicar preventivamente), §5 Success Criteria 3, 4, 5, §6 pre-push hook (E2 — confirmado: instalação passa no pre-push)
    - `.planning/phases/07.5-parcelas-datas-valores-customizados/07.5-CONTEXT.md` — §decisions D-02 (contrato de props INVARIANTE — não pode mudar), D-07 (validações), D-08 (soma invariante)
    - `.planning/phases/07.5.1-sincronizacao-saldosmap-ux-input-valor/07.5.1-CONTEXT.md` — §decisions D-04/D-06 (state `valoresStr`, setter `atualizarValorStr` — preservar)
    - Documentação @tanstack/react-virtual v3 (padrão canônico):
      - Hook signature: `useVirtualizer({ count, getScrollElement, estimateSize, overscan })`
      - API de render: `rowVirtualizer.getVirtualItems()` retorna `[{ index, key, start, end, size }, ...]`
      - Parent div **DEVE** ter altura fixa (`maxHeight` + `overflowY: auto`) pra virtualizer conseguir medir viewport
      - Inner wrapper **DEVE** ter `height: rowVirtualizer.getTotalSize()` pra scroll ter alcance correto
      - Cada row **DEVE** ser `position: absolute` com `transform: translateY(virtualItem.start)` e `height: virtualItem.size`
  </read_first>

  <action>
    Task dividida em 5 blocos atômicos. Execute sequencialmente; NÃO faça commit intermediário — commit final é ÚNICO, combinando package.json + package-lock.json + TabelaParcelasEditaveis.jsx.

    ---

    **BLOCO 1 — Instalação da dependência @tanstack/react-virtual v3 (D-03).**

    ```bash
    cd src/mr-3/mr-cobrancas
    git checkout main                                       # garantir branch (não detached HEAD)
    git status --short                                      # esperado vazio (ou apenas CRLF pré-existentes em AtrasoCell.jsx/FiltroDividas.jsx — constraint fora do escopo)
    npm install @tanstack/react-virtual@^3
    ```

    Após instalar, verificar:
    ```bash
    grep "@tanstack/react-virtual" package.json
    # esperado: "@tanstack/react-virtual": "^3.x.y" em "dependencies"
    grep -c "@tanstack/react-virtual" package-lock.json
    # esperado: ≥1 (tipicamente 2-5: root deps + resolved + dependencies internas se houver)
    ```

    Se `npm install` tentar gravar entry em `devDependencies`, forçar `dependencies`:
    ```bash
    npm install --save @tanstack/react-virtual@^3   # --save força dependencies (padrão em npm 7+ mas explícito é mais seguro)
    ```

    **ATENÇÃO pre-push hook (CONTEXT § 6):** `.git/hooks/pre-push` do pai só valida bump do `src/mr-3` — diff de arquivos não é inspecionado. Instalação aqui não afeta hook.

    ---

    **BLOCO 2 — Atualizar imports (linhas 1-3 do TabelaParcelasEditaveis.jsx).**

    Edit pontual para trocar:
    ```javascript
    // ANTES (linhas 1-3):
    import { useState, useMemo } from "react";
    import toast from "react-hot-toast";
    import Btn from "./ui/Btn.jsx";

    // DEPOIS (acrescentar useRef + linha de import do virtualizer):
    import { useState, useMemo, useRef } from "react";
    import { useVirtualizer } from "@tanstack/react-virtual";
    import toast from "react-hot-toast";
    import Btn from "./ui/Btn.jsx";
    ```

    Padrão para Edit tool (2 edits separados):

    Edit A — expandir import de React:
    - `old_string`: `import { useState, useMemo } from "react";`
    - `new_string`: `import { useState, useMemo, useRef } from "react";`

    Edit B — adicionar linha de import do virtualizer (inserir APÓS o import de React):
    - `old_string`: `import { useState, useMemo, useRef } from "react";\nimport toast from "react-hot-toast";`
    - `new_string`: `import { useState, useMemo, useRef } from "react";\nimport { useVirtualizer } from "@tanstack/react-virtual";\nimport toast from "react-hot-toast";`

    Verificar:
    ```bash
    grep -n "^import" src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx
    # esperado 4 lines:
    # 1: import { useState, useMemo, useRef } from "react";
    # 2: import { useVirtualizer } from "@tanstack/react-virtual";
    # 3: import toast from "react-hot-toast";
    # 4: import Btn from "./ui/Btn.jsx";
    ```

    ---

    **BLOCO 3 — Adicionar `useRef` e chamada de `useVirtualizer` após o state existente.**

    Localizar a linha final do state (depois de `const readonlySet = dividasComPagamentoIds || new Set();` — linha 64 aproximada). Inserir o novo bloco ANTES da declaração `function isReadonly(p)`.

    Edit pontual:
    - `old_string`:
      ```
      const readonlySet = dividasComPagamentoIds || new Set();

        function isReadonly(p) {
      ```
    - `new_string`:
      ```
      const readonlySet = dividasComPagamentoIds || new Set();

        const parentRef = useRef(null);

        const rowVirtualizer = useVirtualizer({
          count: parcelas.length,
          getScrollElement: () => parentRef.current,
          estimateSize: () => 44,     // altura média de linha em pixels — D-07 overscan absorve variação
          overscan: 10,               // D-07 — mitiga perda de foco ao scrollar digitando
        });

        function isReadonly(p) {
      ```

    **Atenção a indentação:** 2 spaces antes de `const readonlySet`, 2 spaces antes das novas declarações (mesmo escopo dentro de `export default function TabelaParcelasEditaveis({...})`).

    Verificar:
    ```bash
    grep -n "useVirtualizer" src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx
    # esperado ≥2 matches (1 import + 1 chamada do hook)
    grep -n "overscan: 10" src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx
    # esperado 1 match
    grep -n "useRef(null)" src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx
    # esperado 1 match (parentRef)
    ```

    ---

    **BLOCO 4 — Substituir JSX da tabela (linhas ~221-275 da base) por CSS grid virtualizado.**

    **Antes, marcar exatamente o bloco a substituir.** Identificar por busca literal do começo e fim:
    - Início: `{/* Tabela */}\n      <div style={{ overflowX: "auto" }}>\n        <table style={{ width: "100%", borderCollapse: "collapse", fontSize: 13 }}>`
    - Fim: `        </table>\n      </div>\n`

    **Executar o refactor inteiro como 1 único Edit tool call** (bloco grande de substituição — `old_string` e `new_string` ambos cobrindo a região completa de `{/* Tabela */}` até o `</div>` que fecha o container `<div style={{ overflowX: "auto" }}>`).

    Padrão para Edit tool:

    - `old_string`:
      ```
            {/* Tabela */}
            <div style={{ overflowX: "auto" }}>
              <table style={{ width: "100%", borderCollapse: "collapse", fontSize: 13 }}>
                <thead>
                  <tr style={{ background: "#f8fafc" }}>
                    <th style={{ ...th, width: 40 }}>Nº</th>
                    <th style={th}>Valor</th>
                    <th style={th}>Data de vencimento</th>
                  </tr>
                </thead>
                <tbody>
                  {parcelas.map((p, i) => {
                    const ro = isReadonly(p);
                    const tooltip = ro ? "Parcela com pagamento registrado — estorne primeiro para editar (botão 'Excluir pagamento' na seção 'Pagamentos Recebidos')." : undefined;
                    return (
                      <tr
                        key={p.id || `novo-${i}`}
                        style={{
                          borderBottom: "1px solid #f1f5f9",
                          background: ro ? "#f1f5f9" : "transparent",
                        }}
                        title={tooltip}
                      >
                        <td style={{ ...td, color: "#64748b", fontWeight: 700 }}>
                          {ro && <span style={{ marginRight: 6 }} aria-label="parcela bloqueada">🔒</span>}
                          {p.numero}
                        </td>
                        <td style={td}>
                          <input
                            type="number"
                            min="0"
                            step="0.01"
                            value={valoresStr[i] ?? ""}
                            onChange={e => atualizarValorStr(i, e.target.value)}
                            disabled={ro}
                            title={tooltip}
                            style={ro ? cellInputDisabledStyle : cellInputStyle}
                          />
                        </td>
                        <td style={td}>
                          <input
                            type="date"
                            value={p.data_vencimento}
                            onChange={e => atualizarLinha(i, "data_vencimento", e.target.value)}
                            disabled={ro}
                            title={tooltip}
                            style={ro ? cellInputDisabledStyle : cellInputStyle}
                          />
                        </td>
                      </tr>
                    );
                  })}
                </tbody>
              </table>
            </div>
      ```

    - `new_string`:
      ```
            {/* Tabela virtualizada (Phase 7.6 — @tanstack/react-virtual v3, D-03/D-04/D-05/D-06/D-07) */}
            <div
              ref={parentRef}
              role="table"
              aria-rowcount={parcelas.length + 1}
              style={{
                maxHeight: 400,                             /* D-06 */
                overflowY: "auto",                          /* D-06 */
                overflowX: "auto",
                border: "1px solid #f1f5f9",
                borderRadius: 8,
                fontSize: 13,
              }}
            >
              {/* Header row (sticky) — CSS grid D-05 */}
              <div
                role="row"
                style={{
                  display: "grid",
                  gridTemplateColumns: "60px 1fr 180px",
                  gap: 8,
                  padding: "8px 10px",
                  background: "#f8fafc",
                  position: "sticky",
                  top: 0,
                  zIndex: 1,
                  borderBottom: "1px solid #e2e8f0",
                }}
              >
                <div role="columnheader" style={{ fontWeight: 700, color: "#64748b", fontSize: 11, textTransform: "uppercase", letterSpacing: ".5px" }}>Nº</div>
                <div role="columnheader" style={{ fontWeight: 700, color: "#64748b", fontSize: 11, textTransform: "uppercase", letterSpacing: ".5px" }}>Valor</div>
                <div role="columnheader" style={{ fontWeight: 700, color: "#64748b", fontSize: 11, textTransform: "uppercase", letterSpacing: ".5px" }}>Data de vencimento</div>
              </div>

              {/* Inner wrapper: height=totalSize; linhas absolute com translateY */}
              <div
                style={{
                  height: rowVirtualizer.getTotalSize(),
                  position: "relative",
                  width: "100%",
                }}
              >
                {rowVirtualizer.getVirtualItems().map(virtualItem => {
                  const i = virtualItem.index;
                  const p = parcelas[i];
                  const ro = isReadonly(p);
                  const tooltip = ro ? "Parcela com pagamento registrado — estorne primeiro para editar (botão 'Excluir pagamento' na seção 'Pagamentos Recebidos')." : undefined;
                  return (
                    <div
                      key={p.id || `novo-${i}`}
                      role="row"
                      aria-rowindex={i + 2}
                      title={tooltip}
                      style={{
                        position: "absolute",
                        top: 0,
                        left: 0,
                        width: "100%",
                        height: virtualItem.size,
                        transform: `translateY(${virtualItem.start}px)`,
                        display: "grid",
                        gridTemplateColumns: "60px 1fr 180px",
                        gap: 8,
                        padding: "6px 10px",
                        borderBottom: "1px solid #f1f5f9",
                        background: ro ? "#f1f5f9" : "transparent",
                        boxSizing: "border-box",
                        alignItems: "center",
                      }}
                    >
                      <div role="cell" style={{ color: "#64748b", fontWeight: 700 }}>
                        {ro && <span style={{ marginRight: 6 }} aria-label="parcela bloqueada">🔒</span>}
                        {p.numero}
                      </div>
                      <div role="cell">
                        <input
                          type="number"
                          min="0"
                          step="0.01"
                          value={valoresStr[i] ?? ""}
                          onChange={e => atualizarValorStr(i, e.target.value)}
                          disabled={ro}
                          title={tooltip}
                          style={ro ? cellInputDisabledStyle : cellInputStyle}
                        />
                      </div>
                      <div role="cell">
                        <input
                          type="date"
                          value={p.data_vencimento}
                          onChange={e => atualizarLinha(i, "data_vencimento", e.target.value)}
                          disabled={ro}
                          title={tooltip}
                          style={ro ? cellInputDisabledStyle : cellInputStyle}
                        />
                      </div>
                    </div>
                  );
                })}
              </div>
            </div>
      ```

    **Atenção a indentação:** a região começa com 6 spaces (aninhada dentro de `return (` > `<div style={{ background: "#fff" ... }}>` > após o dropdown "Sugerir datas"). **NÃO** alterar a indentação do bloco externo — manter os 6 spaces iniciais de cada linha.

    ---

    **BLOCO 5 — Confirmar que o refactor NÃO mexeu em nada fora da região esperada.**

    ```bash
    cd src/mr-3/mr-cobrancas
    git diff --stat
    # Esperado: EXATAMENTE 3 arquivos modificados:
    #   package.json | X +-
    #   package-lock.json | Y +-
    #   src/components/TabelaParcelasEditaveis.jsx | Z +-
    # Contagens típicas: package-lock.json ~100-500 linhas (normal pro npm install);
    #                    package.json ~1 linha (entry de @tanstack/react-virtual);
    #                    TabelaParcelasEditaveis.jsx ~60 insertions / ~55 deletions (refactor da região tabela).
    ```

    **Verificar ausência de `<table>`/`<thead>`/`<tbody>`/`<th>`/`<tr>`/`<td>` no JSX do componente:**
    ```bash
    grep -n "<table" src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx
    # esperado 0 matches
    grep -n "<thead\|<tbody\|<tr \|<tr>\|<th \|<th>\|<td \|<td>" src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx
    # esperado 0 matches
    ```

    **Verificar presença de ARIA roles CSS grid (D-05):**
    ```bash
    grep -n 'role="table"' src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx   # ≥1 match (container)
    grep -n 'role="row"' src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx     # ≥2 matches (header + linha virtualizada)
    grep -n 'role="cell"' src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx    # ≥3 matches (Nº, valor, data)
    grep -n 'role="columnheader"' src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx  # ≥3 matches (3 colunas)
    ```

    **Verificar D-06 (container scroll):**
    ```bash
    grep -n "maxHeight: 400" src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx   # ≥1 match
    grep -n 'overflowY: "auto"' src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx  # ≥1 match
    ```

    **Verificar D-09 (sugerirDatas síncrono — não virou async):**
    ```bash
    grep -n "async function sugerirDatas\|async () => sugerirDatas" src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx
    # esperado 0 matches (sugerirDatas continua síncrono)
    grep -n "function sugerirDatas(intervaloDias)" src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx
    # esperado 1 match (assinatura inalterada)
    ```

    **Verificar contrato de props INALTERADO (D-02 da 7.5):**
    ```bash
    grep -n "export default function TabelaParcelasEditaveis" src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx
    # esperado 1 match
    grep -n "valorTotal,\|parcelasIniciais,\|modoEdicao,\|dividasComPagamentoIds,\|onChange,\|onSubmit,\|onCancel,\|hideFooter" src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx
    # esperado 8 matches (1 por cada prop destructurada no signature)
    ```

    **Verificar preservação do state valoresStr (7.5.1):**
    ```bash
    grep -n "const \[valoresStr, setValoresStr\]" src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx  # 1 match
    grep -n "function atualizarValorStr" src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx           # 1 match
    grep -n "value={valoresStr\[i\] ?? \"\"}\|value={valoresStr\[i\] \?\? \"\"}" src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx  # ≥1 match
    ```

    **Verificar handleSalvar intocado (validações D-08):**
    ```bash
    grep -n "Todas as parcelas devem ter data" src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx        # 1 match (validação datas)
    grep -n "Soma das parcelas" src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx                       # 1 match (validação soma)
    grep -n "Datas devem estar em ordem crescente" src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx    # 1 match (validação ordem)
    grep -n "Math.abs(soma - Number(valorTotal" src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx       # 1 match (tolerância 0,01)
    ```

    **Verificar indicador Soma/Total/Δ intocado:**
    ```bash
    grep -n "Soma: {fmtBRL(soma)}" src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx         # 1 match
    grep -n 'Math.abs(diff) <= 0.01 ? "#16a34a" : "#dc2626"' src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx   # 1 match (cor verde/vermelha)
    ```

    **Gate invariante motor Art.354 (D-01):**
    ```bash
    cd src/mr-3/mr-cobrancas
    git diff HEAD -- src/utils/devedorCalc.js src/services/pagamentos.js
    # esperado vazio (0 bytes) — se houver qualquer diff, restaurar via `git checkout HEAD -- <arquivo>`
    ```

    **Build (gate bloqueante):**
    ```bash
    cd src/mr-3/mr-cobrancas && npm run build
    ```
    Exit 0 obrigatório. Causas prováveis de falha (todas diagnosticáveis por grep):
    - Import do `useVirtualizer` errado → verificar `grep -n "@tanstack/react-virtual" package.json` retorna match.
    - JSX mal fechado na região refatorada → abrir devtools/console do build pra ver linha.
    - Typo em `parentRef` / `rowVirtualizer` → grep por ambos os identificadores.

    **Regressão (gate bloqueante, D-01 invariante estrutural):**
    ```bash
    cd src/mr-3/mr-cobrancas && npm run test:regressao
    ```
    Esperado: 9/9 PASS. Motor não tocado (verificado acima), então teste não pode regredir por esta task.

    ---

    **BLOCO 6 — Commit atômico no submodule main (3 arquivos).**

    ```bash
    cd src/mr-3/mr-cobrancas
    git rev-parse --abbrev-ref HEAD                         # esperado: main
    git status --short
    # esperado (ordem não importa):
    #  M package.json
    #  M package-lock.json
    #  M src/components/TabelaParcelasEditaveis.jsx
    # (pode aparecer linhas CRLF pré-existentes em AtrasoCell.jsx / FiltroDividas.jsx — ignorar, constraint fora do escopo)

    git add package.json package-lock.json src/components/TabelaParcelasEditaveis.jsx
    git commit -m "$(cat <<'EOF'
    feat(07.6-02): virtualização @tanstack/react-virtual em TabelaParcelasEditaveis

    - Instalar @tanstack/react-virtual@^3 nas dependencies do submodule (D-03).
    - Refactor do render da tabela: <table>/<thead>/<tbody> → CSS grid com ARIA roles
      role="table" / role="row" / role="cell" / role="columnheader" (D-05).
    - Container scroll maxHeight=400px, overflowY="auto", border sutil (D-06).
    - useVirtualizer({ count, getScrollElement, estimateSize: () => 44, overscan: 10 }) — D-07.
    - Sempre virtualiza, sem threshold — mesmo código pra N=3 e N=999 (D-04).
    - Cobre domínio forense real: consignado 96x, imobiliário 30 anos 360x,
      imobiliário 35 anos 420x, e teto prático 999x (CONTEXT § 1).

    Preservações (INVARIANTES desta phase):
    - Contrato de props INALTERADO — D-02 da 7.5 (valorTotal, parcelasIniciais,
      modoEdicao, dividasComPagamentoIds, onChange, onSubmit, onCancel, hideFooter).
    - State valoresStr e setter atualizarValorStr (fix 07.5.1) mantidos — input valor
      continua usando value={valoresStr[i] ?? ""} + atualizarValorStr.
    - handleSalvar 100% intocado — 3 validações (soma R$0,01 tolerância, datas preenchidas,
      ordem não-decrescente) seguem idênticas. D-08.
    - Indicador Soma/Total/Δ em tempo real FORA do container virtualizado, renderiza
      uma vez com todos os dados (cor verde quando |diff|<=0,01, vermelha caso contrário).
    - sugerirDatas permanece síncrono (D-09) — .map sobre parcelas[] completo.
    - Dropdown "Sugerir datas" com opções Mensal/Quinzenal/Semanal/Personalizado intocado.
    - Readonly em parcelas pagas (dividasComPagamentoIds) preservado — 🔒 + background
      cinza + disabled=true nos inputs + tooltip "Parcela com pagamento registrado..."

    Riscos conhecidos (monitorados em UAT da 07.6-03):
    - E1 re-render cascade por keystroke (iteração O(N) em parcelas[] ainda ocorre) —
      D-10: React.memo NÃO aplicado preventivamente; UAT com N=100 valida severidade.
    - E2 foco perdido ao scrollar parcialmente mitigado por overscan=10 (D-07).

    Motor Art.354 (devedorCalc.js + pagamentos.js) LITERALMENTE intocado —
    invariante estrutural D-01. Regressão 9/9 verde. Build OK.

    Schema do banco inalterado. 07.6-01 (bump de limite) já está em main.
    Bump do pai e push só após autorização no plano 07.6-03.

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
    EOF
    )"
    git log -1 --format=%H              # capturar SHA pra reportar no plano 07.6-03
    ```

    **NÃO FAZER PUSH.** Bump do pai e push só acontecem após autorização explícita no plano 07.6-03.
  </action>

  <verify>
    <automated>
      cd src/mr-3/mr-cobrancas && \
      grep -q "@tanstack/react-virtual" package.json && \
      grep -q "useVirtualizer" src/components/TabelaParcelasEditaveis.jsx && \
      grep -q "overscan: 10" src/components/TabelaParcelasEditaveis.jsx && \
      grep -q "maxHeight: 400" src/components/TabelaParcelasEditaveis.jsx && \
      grep -q 'role="table"' src/components/TabelaParcelasEditaveis.jsx && \
      grep -q 'role="row"' src/components/TabelaParcelasEditaveis.jsx && \
      grep -q 'role="cell"' src/components/TabelaParcelasEditaveis.jsx && \
      ! grep -q "<table" src/components/TabelaParcelasEditaveis.jsx && \
      ! grep -q "<thead\|<tbody" src/components/TabelaParcelasEditaveis.jsx && \
      npm run build && \
      npm run test:regressao
    </automated>
  </verify>

  <acceptance_criteria>
    **Dependência instalada:**
    - `grep -c "@tanstack/react-virtual" src/mr-3/mr-cobrancas/package.json` retorna **≥1** (entrada em dependencies)
    - `grep -c "@tanstack/react-virtual" src/mr-3/mr-cobrancas/package-lock.json` retorna **≥1** (tree lock atualizada)
    - Versão bate `^3` em package.json: `grep -E '"@tanstack/react-virtual": "\^3\.' src/mr-3/mr-cobrancas/package.json` retorna ≥1 match

    **Imports corretos:**
    - `grep -n "import { useVirtualizer } from \"@tanstack/react-virtual\"" src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx` retorna **exatamente 1 match**
    - `grep -n "import { useState, useMemo, useRef } from \"react\"" src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx` retorna **exatamente 1 match**

    **Hook useVirtualizer configurado corretamente (D-07):**
    - `grep -n "useVirtualizer({" src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx` retorna **exatamente 1 match**
    - `grep -n "overscan: 10" src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx` retorna **exatamente 1 match**
    - `grep -n "count: parcelas.length" src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx` retorna **exatamente 1 match**
    - `grep -n "getScrollElement: () => parentRef.current" src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx` retorna **exatamente 1 match**
    - `grep -n "useRef(null)" src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx` retorna **exatamente 1 match**

    **Estrutura DOM = CSS grid com ARIA (D-05) — NUNCA `<table>`:**
    - `grep -c "<table\|<thead\|<tbody\|<th \|<th>\|<tr \|<tr>\|<td \|<td>" src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx` retorna **0** (zero ocorrências de qualquer tag tabular HTML)
    - `grep -c 'role="table"' src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx` retorna **≥1** (container)
    - `grep -c 'role="row"' src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx` retorna **≥2** (header row + data row)
    - `grep -c 'role="cell"' src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx` retorna **≥3** (Nº, valor, data)
    - `grep -c 'role="columnheader"' src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx` retorna **≥3** (3 headers)

    **Container scroll vertical (D-06):**
    - `grep -n "maxHeight: 400" src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx` retorna **exatamente 1 match**
    - `grep -n 'overflowY: "auto"' src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx` retorna **exatamente 1 match**

    **Render virtualizado correto (padrão canônico):**
    - `grep -n "rowVirtualizer.getTotalSize()" src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx` retorna **exatamente 1 match**
    - `grep -n "rowVirtualizer.getVirtualItems()" src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx` retorna **exatamente 1 match**
    - `grep -n "translateY(\${virtualItem.start}px)" src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx` retorna **exatamente 1 match**
    - `grep -n "virtualItem.index" src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx` retorna **≥1 match** (index correto em linha virtualizada)
    - `grep -n "virtualItem.size" src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx` retorna **≥1 match**

    **Contrato de props invariante D-02 da 7.5 — INTOCADO:**
    - `grep -n "export default function TabelaParcelasEditaveis" src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx` retorna **exatamente 1 match**
    - `grep -n "valorTotal," src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx` retorna **≥1 match** (destructure do signature)
    - `grep -n "parcelasIniciais," src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx` retorna **≥1 match**
    - `grep -n "modoEdicao," src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx` retorna **≥1 match**
    - `grep -n "dividasComPagamentoIds," src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx` retorna **≥1 match**
    - `grep -n "hideFooter," src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx` retorna **≥1 match**

    **State valoresStr (fix 07.5.1) INTOCADO:**
    - `grep -n "const \\[valoresStr, setValoresStr\\]" src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx` retorna **exatamente 1 match**
    - `grep -n "function atualizarValorStr" src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx` retorna **exatamente 1 match**
    - `grep -n "value={valoresStr\\[i\\]" src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx` retorna **≥1 match** (input valor virtualizado usa string local)

    **handleSalvar intocado (D-08 — 3 validações):**
    - `grep -n "Todas as parcelas devem ter data" src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx` retorna **exatamente 1 match** (validação datas)
    - `grep -n "Soma das parcelas" src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx` retorna **exatamente 1 match** (validação soma)
    - `grep -n "Datas devem estar em ordem crescente" src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx` retorna **exatamente 1 match** (validação ordem)
    - `grep -n "Math.abs(soma - Number(valorTotal" src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx` retorna **exatamente 1 match** (tolerância R$0,01)

    **Indicador Soma/Total/Δ intocado:**
    - `grep -n "Soma: {fmtBRL(soma)}" src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx` retorna **exatamente 1 match**
    - `grep -n "#16a34a.*#dc2626\|Math.abs(diff) <= 0.01" src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx` retorna **≥1 match** (cor verde/vermelha do indicador)

    **sugerirDatas síncrono preservado (D-09):**
    - `grep -c "async function sugerirDatas" src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx` retorna **0** (nunca virou async)
    - `grep -n "function sugerirDatas(intervaloDias)" src/mr-3/mr-cobrancas/src/components/TabelaParcelasEditaveis.jsx` retorna **exatamente 1 match** (assinatura preservada)

    **Invariante motor Art.354 (D-01 — gate bloqueante):**
    - `cd src/mr-3/mr-cobrancas && git diff HEAD -- src/utils/devedorCalc.js` retorna **vazio** (0 bytes)
    - `cd src/mr-3/mr-cobrancas && git diff HEAD -- src/services/pagamentos.js` retorna **vazio** (0 bytes)

    **Build + regressão (gate bloqueante):**
    - `cd src/mr-3/mr-cobrancas && npm run build` exit 0
    - `cd src/mr-3/mr-cobrancas && npm run test:regressao` exit 0 (9/9 PASS)

    **Commit correto no submodule main:**
    - `cd src/mr-3/mr-cobrancas && git log -1 --format=%s` retorna EXATAMENTE `feat(07.6-02): virtualização @tanstack/react-virtual em TabelaParcelasEditaveis`
    - `cd src/mr-3/mr-cobrancas && git log -1 --name-only --format=` lista EXATAMENTE 3 arquivos: `package.json`, `package-lock.json`, `src/components/TabelaParcelasEditaveis.jsx` (em qualquer ordem)
    - `cd src/mr-3/mr-cobrancas && git rev-parse --abbrev-ref HEAD` retorna `main`
    - `cd src/mr-3/mr-cobrancas && git log origin/main..main` retorna ≥2 commits locais (o da 07.6-01 + este)

    **Zero push:**
    - **Nenhum** comando `git push` executado em qualquer ponto desta task
    - `cd src/mr-3/mr-cobrancas && git log origin/main..main --oneline` mostra 2 commits locais (07.6-01 + 07.6-02)
  </acceptance_criteria>

  <done>
    `@tanstack/react-virtual@^3` instalado no submodule (package.json + package-lock.json atualizados). `TabelaParcelasEditaveis.jsx` refatorado: CSS grid com ARIA roles, container maxHeight=400px, useVirtualizer com overscan=10, render de apenas virtual items. Contrato de props INTOCADO (D-02 da 7.5). State valoresStr + atualizarValorStr PRESERVADOS (fix 07.5.1). handleSalvar com 3 validações INTOCADO (D-08). Indicador Soma/Total/Δ em tempo real PRESERVADO (cor verde/vermelha). sugerirDatas SÍNCRONO (D-09). Build limpo. Regressão 9/9 verde. Invariante motor Art.354 preservada (git diff vazio). 1 commit local no submodule `main` (subject EXATO `feat(07.6-02): virtualização @tanstack/react-virtual em TabelaParcelasEditaveis`, 3 arquivos modificados). Zero push. Bump pai e tag ficam para 07.6-03.
  </done>
</task>

</tasks>

<verification>
Verificação final (após completar a task):

1. **Dependência instalada:**
   - `grep "@tanstack/react-virtual" src/mr-3/mr-cobrancas/package.json` → match em `dependencies`
   - `test -s src/mr-3/mr-cobrancas/node_modules/@tanstack/react-virtual/package.json` → OK (opcional, confirma install real)

2. **Grep determinístico da estrutura nova** (executar na raiz do pai — todos os contracts do acceptance_criteria acima).

3. **Invariante motor Art.354** (D-01):
   - `cd src/mr-3/mr-cobrancas && git diff HEAD -- src/utils/devedorCalc.js src/services/pagamentos.js` → vazio

4. **Regressão + build:**
   - `cd src/mr-3/mr-cobrancas && npm run test:regressao` → 9/9 PASS
   - `cd src/mr-3/mr-cobrancas && npm run build` → exit 0

5. **Histórico de commits:**
   - `cd src/mr-3/mr-cobrancas && git log -2 --format=%s origin/main..main` → 2 linhas:
     - Mais recente: `feat(07.6-02): virtualização @tanstack/react-virtual em TabelaParcelasEditaveis`
     - Anterior: `feat(07.6-01): bump limite parcelas 360→999 em AdicionarDocumento`
   - `cd src/mr-3/mr-cobrancas && git log -1 --name-only --format=` → lista 3 arquivos: package.json, package-lock.json, src/components/TabelaParcelasEditaveis.jsx

6. **Pai (repo master): estado INALTERADO nesta fase.** O ponteiro `src/mr-3` fica desincronizado mas o bump acontece só no 07.6-03 após autorização.
</verification>

<success_criteria>
Plan 07.6-02 bem-sucedido quando:
- ✅ `@tanstack/react-virtual` listado em `dependencies` de `src/mr-3/mr-cobrancas/package.json` (D-03)
- ✅ `TabelaParcelasEditaveis.jsx` usa `useVirtualizer` + `useRef(null)` + CSS grid com `role="table"`/`"row"`/`"cell"`/`"columnheader"` (D-05)
- ✅ Zero tags HTML tabulares (`<table>`, `<thead>`, `<tbody>`, `<tr>`, `<th>`, `<td>`) no componente (D-05)
- ✅ Container de scroll `maxHeight: 400`, `overflowY: "auto"` (D-06)
- ✅ `overscan: 10` configurado (D-07)
- ✅ Sempre virtualiza — sem branching por threshold (D-04)
- ✅ Contrato de props INVARIANTE preservado (D-02 da 7.5)
- ✅ State `valoresStr` e `atualizarValorStr` preservados (fix 07.5.1)
- ✅ `handleSalvar` intocado — 3 validações D-08 preservadas
- ✅ Indicador Soma/Total/Δ em tempo real (cor verde/vermelha) preservado
- ✅ `sugerirDatas` continua síncrono (D-09)
- ✅ Motor Art.354 (`devedorCalc.js` + `pagamentos.js`) literalmente intocado (`git diff HEAD` vazio)
- ✅ `npm run test:regressao` 9/9 verde
- ✅ `npm run build` exit 0
- ✅ 1 commit local no submodule `main` (subject exato, 3 arquivos modificados)
- ✅ Zero push executado — bump do pai pendente para 07.6-03
</success_criteria>

<output>
Após conclusão desta task, criar `.planning/phases/07.6-parcelas-n-grande/07.6-02-SUMMARY.md` resumindo:
- Versão exata do `@tanstack/react-virtual` instalada (ex: `^3.10.1`)
- SHA do commit no submodule main (7 chars)
- Diff summary (3 arquivos modificados, contagem de +/- linhas)
- Output das verificações (≥16 grep checks + regressão 9/9 + build)
- Evidência de invariante motor (git diff HEAD vazio em devedorCalc.js e pagamentos.js)
- Estado pendente: bump do pai + UAT manual autorizado em 07.6-03, zero push
</output>
</content>
</invoke>