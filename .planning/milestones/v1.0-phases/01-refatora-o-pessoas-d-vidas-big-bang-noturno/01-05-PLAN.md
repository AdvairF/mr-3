---
phase: 01-refatora-o-pessoas-d-vidas-big-bang-noturno
plan: 05
type: execute
wave: 3
depends_on: [03]
files_modified:
  - src/mr-3/mr-cobrancas/src/App.jsx
autonomous: true
requirements:
  - REQ-06

must_haves:
  truths:
    - "NAV menu label shows 'Pessoas' instead of 'Devedores'"
    - "Dashboard card label shows 'Pessoas' instead of 'Devedores'"
  artifacts:
    - path: "src/mr-3/mr-cobrancas/src/App.jsx"
      provides: "UI label rename from Devedores to Pessoas"
      contains: "label: \"Pessoas\""
  key_links: []
---

<objective>
Rename the NAV menu label "Devedores" to "Pessoas" and the Dashboard card label "Devedores" to "Pessoas" in App.jsx.

Purpose: UI-only change per Decisao 3 (LOCKED). Table names and internal code remain unchanged.
Output: Two string changes in App.jsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-refatora-o-pessoas-d-vidas-big-bang-noturno/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename NAV label and Dashboard card from "Devedores" to "Pessoas"</name>
  <files>src/mr-3/mr-cobrancas/src/App.jsx</files>
  <read_first>
    - src/mr-3/mr-cobrancas/src/App.jsx (lines 8538-8542 for NAV array)
    - src/mr-3/mr-cobrancas/src/App.jsx (lines 8785-8792 for Dashboard cards)
  </read_first>
  <action>
**Change 1 — NAV array (line 8540):**

BEFORE:
```javascript
    { id: "devedores", label: "Devedores", icon: I.dev, color: "#ec4899", bg: "rgba(236,72,153,.18)" },
```

AFTER:
```javascript
    { id: "devedores", label: "Pessoas", icon: I.dev, color: "#ec4899", bg: "rgba(236,72,153,.18)" },
```

Only change the `label` value. Keep `id: "devedores"` unchanged (internal routing).

**Change 2 — Dashboard KPI card (line 8787):**

BEFORE:
```javascript
              { label: "Devedores", value: devedores.length, tone: "#eaf9ef", ink: "#166534" },
```

AFTER:
```javascript
              { label: "Pessoas", value: devedores.length, tone: "#eaf9ef", ink: "#166534" },
```

Only these 2 string changes. No other occurrences of "Devedores" should be changed (internal variable names, table names, etc. stay as-is per Decisao 3).
  </action>
  <verify>
    <automated>grep -n "label: \"Pessoas\"" src/mr-3/mr-cobrancas/src/App.jsx | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - grep `label: "Pessoas"` in App.jsx returns exactly 2 matches
    - grep `label: "Devedores"` in App.jsx returns 0 matches (both instances changed)
    - grep `id: "devedores"` in App.jsx still returns at least 1 match (internal id unchanged)
    - Build passes: `cd src/mr-3/mr-cobrancas && npx vite build --mode development`
  </acceptance_criteria>
  <done>NAV sidebar shows "Pessoas" label. Dashboard card shows "Pessoas" label. Internal id "devedores" and all table/variable names unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected by string label change.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05-01 | — | — | accept | No security impact from label rename |
</threat_model>

<verification>
- `grep 'label: "Pessoas"' App.jsx` returns 2 matches
- `grep 'label: "Devedores"' App.jsx` returns 0 matches
- Build passes
</verification>

<success_criteria>
- Menu sidebar displays "Pessoas" instead of "Devedores"
- Dashboard KPI card displays "Pessoas" instead of "Devedores"
</success_criteria>

<output>
After completion, create `.planning/phases/01-refatora-o-pessoas-d-vidas-big-bang-noturno/01-05-SUMMARY.md`
</output>
