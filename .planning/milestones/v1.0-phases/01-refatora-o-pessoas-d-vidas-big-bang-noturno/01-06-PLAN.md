---
phase: 01-refatora-o-pessoas-d-vidas-big-bang-noturno
plan: 06
type: execute
wave: 4
depends_on: [03, 04, 05]
files_modified:
  - src/mr-3/mr-cobrancas/build/
autonomous: false
requirements:
  - REQ-07
  - REQ-08
  - REQ-09

must_haves:
  truths:
    - "npm run build passes (includes test:regressao prebuild)"
    - "Vercel deploy succeeds and app loads at mrcobrancas.com.br"
    - "4 devedores and their dividas are visible and correct in production"
    - "Calculos financeiros are correct after migration"
  artifacts:
    - path: "src/mr-3/mr-cobrancas/build/"
      provides: "Production build output"
  key_links:
    - from: "npm run build"
      to: "test:regressao"
      via: "prebuild hook"
      pattern: "prebuild.*test:regressao"
---

<objective>
Run `npm run build` (which triggers test:regressao as prebuild), verify tests pass, commit build artifacts, push to Vercel, and verify the 4 devedores and their dividas are visible and correct in production.

Purpose: Final verification that the migration is successful end-to-end.
Output: Green build, successful Vercel deploy, verified production data.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-refatora-o-pessoas-d-vidas-big-bang-noturno/01-CONTEXT.md
@.planning/phases/01-refatora-o-pessoas-d-vidas-big-bang-noturno/01-03-SUMMARY.md
@.planning/phases/01-refatora-o-pessoas-d-vidas-big-bang-noturno/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run npm run build and verify test:regressao passes</name>
  <files>src/mr-3/mr-cobrancas/build/</files>
  <read_first>
    - src/mr-3/mr-cobrancas/package.json (verify "prebuild" script exists)
  </read_first>
  <action>
1. From `src/mr-3/mr-cobrancas/`, run `npm run build`
2. This will automatically run `npm run test:regressao` as the prebuild hook
3. Verify all tests pass (9/9 tests in calculos.test.js)
4. Verify build completes without errors
5. The build output goes to `src/mr-3/mr-cobrancas/build/`

If tests fail, investigate the failure. The tests are pure unit tests in `calculos.test.js` that test `devedorCalc.js` functions. They should NOT be affected by our changes because:
- We did not modify `devedorCalc.js`
- We did not modify `correcao.js`
- The tests create mock devedor objects with inline dividas arrays
- The tests do not touch any DB

If the build fails with import errors, check that:
- `dbGet("dividas")` is correctly placed in the Promise.all array
- No unclosed braces or parentheses in the refactored write surfaces
- `setAllDividas` is properly declared with useState
  </action>
  <verify>
    <automated>cd src/mr-3/mr-cobrancas && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exit code is 0
    - Output contains "9 tests" or "Tests" followed by "passed"
    - Output contains "built in" (Vite build success message)
    - `src/mr-3/mr-cobrancas/build/index.html` exists
  </acceptance_criteria>
  <done>npm run build passes with all tests green and build artifacts generated in build/</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify production deploy and data integrity</name>
  <what-built>
    The full migration is complete:
    - SQL migration created and run (dividas table with UUID PK, devedores_dividas with real FK)
    - dividas.js service created
    - carregarTudo() loads dividas in parallel, builds dividasMap, populates devedor.dividas
    - All 7 write surfaces use dbInsert/dbUpdate/dbDelete("dividas")
    - NAV label changed to "Pessoas"
    - Build passes with all tests green
  </what-built>
  <how-to-verify>
    1. Commit all changes and push to trigger Vercel deploy
    2. Wait for Vercel deploy to complete
    3. Open https://mrcobrancas.com.br (or your Vercel URL)
    4. Verify the sidebar shows "Pessoas" (not "Devedores")
    5. Click on "Pessoas" — verify all 4 entries load:
       - advair — 1 divida de R$ 4.000,00
       - TRADIO PAGAMENTOS — divida R$ 5.323,47
       - TRADIO SOLUCOES — divida R$ 115,44
       - LOURENCO CONSTRUTORA — divida R$ 0,00
    6. Open advair's detail — verify:
       - Dividas section shows the correct divida
       - Valor is correct (R$ 4.000,00 base)
       - Pagamentos parciais are visible
       - Saldo atualizado (with encargos) matches expectation
    7. Test creating a NEW divida:
       - Click "+ Divida" on any devedor
       - Fill in valor, data de vencimento
       - Save — verify it appears in the list
       - Verify the devedores_dividas junction row was created (check DevedoresDaDivida panel)
    8. Test editing a divida:
       - Edit the test divida just created
       - Change the valor
       - Save — verify the new valor persists after page refresh
    9. Delete the test divida — verify it disappears
    10. Dashboard card shows "Pessoas" with correct count
  </how-to-verify>
  <resume-signal>Type "verified" if all checks pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build -> Production | Vercel deploy from pushed build artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06-01 | Availability | Vercel deploy | mitigate | JSONB column kept intact — revert to previous commit if deploy fails |
| T-01-06-02 | Integrity | Data migration | mitigate | Manual verification of all 4 devedores' dividas in production |
</threat_model>

<verification>
- `npm run build` passes (exit code 0)
- All 9 regression tests pass
- Vercel deploy succeeds
- 4 devedores visible with correct dividas in production
- CRUD operations work (create/edit/delete divida)
</verification>

<success_criteria>
- Build green, tests green, deploy successful
- All 4 devedores and dividas visible and correct in production
- CRUD operations functional with new dividas table
- Menu shows "Pessoas"
- Calculos financeiros correct (saldo atualizado matches)
</success_criteria>

<output>
After completion, create `.planning/phases/01-refatora-o-pessoas-d-vidas-big-bang-noturno/01-06-SUMMARY.md`
</output>
