---
phase: 03-nova-divida-com-co-devedores
plan: 04
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - src/mr-3/mr-cobrancas/src/utils/devedorCalc.js
  - src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx
autonomous: true
requirements:
  - CR-03

must_haves:
  truths:
    - "calcularTotalPagoPorDivida is exported from devedorCalc.js"
    - "DetalheDivida.jsx imports calcularTotalPagoPorDivida alongside calcularSaldosPorDivida"
    - "totalPago in DetalheDivida uses pagoPorDividaMap[String(divida.id)] — NOT a raw reduce over allPagamentos"
    - "calcularSaldoDevedorAtualizado and calcularSaldosPorDivida are NOT modified"
    - "npm run test:regressao exits 0 (9/9 TJGO cases)"
    - "npm run build exits 0"
  artifacts:
    - path: "src/mr-3/mr-cobrancas/src/utils/devedorCalc.js"
      provides: "calcularTotalPagoPorDivida — Art. 354 CC loop tracking absorbed per debt"
      exports: ["calcularTotalPagoPorDivida"]
      contains: "calcularTotalPagoPorDivida"
    - path: "src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx"
      provides: "Per-debt Total Pago display using calcularTotalPagoPorDivida"
      contains: "calcularTotalPagoPorDivida"
  key_links:
    - from: "src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx"
      to: "src/mr-3/mr-cobrancas/src/utils/devedorCalc.js"
      via: "import { calcularSaldosPorDivida, calcularTotalPagoPorDivida }"
      pattern: "calcularTotalPagoPorDivida"
---

<objective>
Fix CR-03: "Total Pago" no card Resumo Financeiro de DetalheDivida.jsx exibe o mesmo valor
(soma de todos os pagamentos do devedor) para dívidas distintas do mesmo devedor.

Root cause: linha 77 de DetalheDivida.jsx soma `pagamentosDoDevedor` inteiro sem filtrar por dívida.
Evidência: devedor Advair com duas dívidas — ambas mostram R$ 2.100,00 como Total Pago.

Fix: adicionar `calcularTotalPagoPorDivida(devedor, pagamentos, hoje)` em devedorCalc.js usando o
mesmo loop sequencial Art. 354 CC de `calcularSaldosPorDivida`, mas rastreando quanto cada pagamento
foi absorvido por cada dívida em vez de retornar saldo final. DetalheDivida.jsx passa a usar
`pagoPorDividaMap[String(divida.id)]` no lugar do reduce ingênuo.

**Restrição crítica:** `calcularSaldoDevedorAtualizado` e `calcularSaldosPorDivida` NÃO são tocados.
Os 9 casos TJGO do test:regressao passam por `calcularSaldoDevedorAtualizado` — qualquer alteração
nessa função quebraria a suite.

Este plan DEVE ser executado antes do Plan 03-03 (E2E sign-off), pois o Flow 1 do checklist de
verificação humana testa o campo "Total Pago" e falhará com o bug presente.

Output:
- `src/mr-3/mr-cobrancas/src/utils/devedorCalc.js` (novo export adicionado ao final)
- `src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx` (import + linha 77 atualizados)
</objective>

<execution_context>
@C:/Users/advai/Downloads/mr-cobrancas-vercel_1/mr-cobrancas/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/advai/Downloads/mr-cobrancas-vercel_1/mr-cobrancas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/advai/Downloads/mr-cobrancas-vercel_1/mr-cobrancas/.planning/ROADMAP.md
@C:/Users/advai/Downloads/mr-cobrancas-vercel_1/mr-cobrancas/.planning/phases/03-nova-divida-com-co-devedores/03-CONTEXT.md

<interfaces>
<!-- Contratos relevantes extraídos da base de código. -->

De src/mr-3/mr-cobrancas/src/utils/devedorCalc.js (linhas 172–263):
```js
// calcularSaldosPorDivida — NÃO TOCAR. Leia para entender o loop; a nova função
// usa a mesma estrutura mas rastreia `absorbed` em vez de `saldo` final.
export function calcularSaldosPorDivida(devedor, pagamentos, hoje) {
  const dividasCalc = parseDividas(devedor?.dividas).filter(
    (d) => !d._nominal && !d._so_custas
  );
  if (!dividasCalc.length) return {};

  // pgtoRestantes: pool compartilhado entre dívidas (Art. 354 CC sequencial)
  const pgtoRestantes = [...(pagamentos || [])]
    .sort((a, b) => (a.data_pagamento || "").localeCompare(b.data_pagamento || ""))
    .map((p) => ({ ...p, remaining: parseFloat(p.valor) || 0 }));

  const result = {};
  for (const div of dividasCalc) {
    // ... loop Art. 354 CC que muta pgtoRestantes[*].remaining
    // ... result[String(div.id)] = saldoDiv + art523Div.total_art523;
  }
  return result; // { [dividaId: string]: saldo_final }
}
```

De src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx (linhas 1–77):
```js
import { calcularSaldosPorDivida } from "../utils/devedorCalc.js";
// ...
const saldosMap = devedor ? calcularSaldosPorDivida(devedor, pagamentosDoDevedor, hoje) : null;
const saldoDivida = saldosMap != null ? (saldosMap[String(divida.id)] ?? null) : null;
const totalPago = pagamentosDoDevedor.reduce((s, p) => s + (parseFloat(p.valor) || 0), 0); // ← BUG: soma tudo
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar calcularTotalPagoPorDivida em devedorCalc.js</name>
  <files>src/mr-3/mr-cobrancas/src/utils/devedorCalc.js</files>

  <read_first>
    - src/mr-3/mr-cobrancas/src/utils/devedorCalc.js (linhas 159–263 — calcularSaldosPorDivida completa, para copiar o loop exato)
    - src/mr-3/mr-cobrancas/src/utils/devedorCalc.js (linhas 1–30 — imports e helpers: parseDividas, calcularFatorCorrecao, calcularJurosAcumulados, calcularArt523)
  </read_first>

  <action>
Adicione a função `calcularTotalPagoPorDivida` ao final de devedorCalc.js, APÓS a função
`calcularSaldosPorDivida` (linha ~263). NÃO modifique nenhuma função existente.

A função usa o mesmo loop Art. 354 CC de `calcularSaldosPorDivida`, mas em vez de calcular
o saldo final, rastreia quanto de pagamento foi absorvido por cada dívida (`absorbed`).

**Não** aplica a apreciação pós-hoje (bloco `if (periodoInicio && periodoInicio < hoje)`) porque
esse bloco só afeta o saldo futuro — nenhum pagamento novo ocorre após `hoje`.

**Não** aplica Art.523 ao valor absorvido — `totalPago` deve refletir dinheiro real pago, não encargo.

```js
/**
 * Mesmo loop Art. 354 CC de calcularSaldosPorDivida, mas retorna mapa
 * { [dividaId]: totalAbsorvido } — valor dos pagamentos efetivamente
 * consumidos por cada dívida (não o saldo restante).
 *
 * calcularSaldoDevedorAtualizado e calcularSaldosPorDivida NÃO são tocados.
 *
 * @param {object} devedor
 * @param {Array}  pagamentos
 * @param {string} hoje "YYYY-MM-DD"
 * @returns {{ [dividaId: string]: number }}
 */
export function calcularTotalPagoPorDivida(devedor, pagamentos, hoje) {
  const dividasCalc = parseDividas(devedor?.dividas).filter(
    (d) => !d._nominal && !d._so_custas
  );
  if (!dividasCalc.length) return {};

  const pgtoRestantes = [...(pagamentos || [])]
    .sort((a, b) =>
      (a.data_pagamento || "").localeCompare(b.data_pagamento || "")
    )
    .map((p) => ({ ...p, remaining: parseFloat(p.valor) || 0 }));

  const result = {};

  for (const div of dividasCalc) {
    const pv = parseFloat(div.valor_total) || 0;
    const indexadorDiv = div.indexador || "nenhum";
    const jurosTipoDiv = div.juros_tipo || "sem_juros";
    const jurosAMDiv = parseFloat(div.juros_am) || 0;
    const multaPctDiv = parseFloat(div.multa_pct) || 0;
    const honorariosPctDiv = parseFloat(div.honorarios_pct) || 0;
    const dataInicioDiv =
      div.data_inicio_atualizacao || div.data_vencimento || div.data_origem;

    let saldo = pv;
    let periodoInicio = dataInicioDiv;
    let primeiroperiodo = true;
    let absorbed = 0;

    const pgtosDiv = pgtoRestantes.filter((p) => p.remaining > 0);

    for (const pgto of pgtosDiv) {
      if (saldo <= 0) break;
      const periodoFim = pgto.data_pagamento;
      if (!periodoFim || periodoFim <= periodoInicio) {
        const abate = Math.min(pgto.remaining, saldo);
        saldo -= abate;
        pgto.remaining -= abate;
        absorbed += abate;
        continue;
      }

      const fator = calcularFatorCorrecao(indexadorDiv, periodoInicio, periodoFim);
      const corrAbs = saldo * (fator - 1);
      const pcSaldo = saldo + corrAbs;

      const { juros } = calcularJurosAcumulados({
        principal: pcSaldo,
        dataInicio: periodoInicio,
        dataFim: periodoFim,
        jurosTipo: jurosTipoDiv,
        jurosAM: jurosAMDiv,
        regime: "simples",
      });

      const multaVal = primeiroperiodo ? pcSaldo * (multaPctDiv / 100) : 0;
      const honorariosVal = primeiroperiodo
        ? (pcSaldo + juros + multaVal) * (honorariosPctDiv / 100)
        : 0;

      const debitoTotal = pcSaldo + juros + multaVal + honorariosVal;
      const abate = Math.min(pgto.remaining, debitoTotal);
      saldo = debitoTotal - abate;
      pgto.remaining -= abate;
      absorbed += abate;
      periodoInicio = periodoFim;
      primeiroperiodo = false;
    }

    // Nota: bloco pós-hoje omitido intencionalmente — nenhum pagamento ocorre
    // após hoje, então absorbed não muda com a apreciação futura do saldo.
    result[String(div.id)] = absorbed;
  }

  return result;
}
```

**Posição exata para inserção:** Logo após o fechamento `}` da função `calcularSaldosPorDivida`
(linha ~263), antes da função `calcularDetalheEncargos` (linha ~265). Insira uma linha em branco
antes do bloco JSDoc e uma linha em branco após a função.
  </action>

  <verify>
    <automated>cd src/mr-3/mr-cobrancas && npm run test:regressao</automated>
  </verify>

  <acceptance_criteria>
    - `src/mr-3/mr-cobrancas/src/utils/devedorCalc.js` contém `export function calcularTotalPagoPorDivida(`
    - O arquivo contém `absorbed += abate` (rastreamento de absorção)
    - O arquivo contém `result[String(div.id)] = absorbed`
    - O arquivo NÃO tem nenhuma modificação nas linhas da função `calcularSaldosPorDivida` (verificar: `result[String(div.id)] = saldoDiv + art523Div.total_art523` ainda existe intacto)
    - O arquivo NÃO tem nenhuma modificação nas linhas da função `calcularSaldoDevedorAtualizado`
    - `npm run test:regressao` exits 0 (9/9 casos TJGO verdes)
  </acceptance_criteria>

  <done>calcularTotalPagoPorDivida exportada; test:regressao passa.</done>
</task>

<task type="auto">
  <name>Task 2: Atualizar DetalheDivida.jsx para usar calcularTotalPagoPorDivida</name>
  <files>src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx</files>

  <read_first>
    - src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx (linhas 1–80 — import, assinatura do componente, e bloco de derivações até totalPago)
    - src/mr-3/mr-cobrancas/src/utils/devedorCalc.js (verificar que calcularTotalPagoPorDivida está exportada — Task 1 deve ter concluído)
  </read_first>

  <action>
Duas mudanças cirúrgicas em DetalheDivida.jsx:

**Mudança 1 — Linha 7: atualizar import**

Atual:
```js
import { calcularSaldosPorDivida } from "../utils/devedorCalc.js";
```

Novo:
```js
import { calcularSaldosPorDivida, calcularTotalPagoPorDivida } from "../utils/devedorCalc.js";
```

**Mudança 2 — Linha 77: substituir totalPago**

Atual (linha 77):
```js
const totalPago = pagamentosDoDevedor.reduce((s, p) => s + (parseFloat(p.valor) || 0), 0);
```

Novo (2 linhas substituindo 1):
```js
const pagoPorDividaMap = devedor ? calcularTotalPagoPorDivida(devedor, pagamentosDoDevedor, hoje) : {};
const totalPago = pagoPorDividaMap[String(divida.id)] ?? 0;
```

Nenhuma outra linha do arquivo deve ser alterada.
  </action>

  <verify>
    <automated>cd src/mr-3/mr-cobrancas && npm run build</automated>
  </verify>

  <acceptance_criteria>
    - `src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx` contém `calcularTotalPagoPorDivida` no import da linha 7
    - O arquivo contém `pagoPorDividaMap[String(divida.id)]`
    - O arquivo NÃO contém `pagamentosDoDevedor.reduce` (a linha do bug foi removida)
    - `npm run build` exits 0 (inclui prebuild test:regressao — 9/9 casos TJGO)
  </acceptance_criteria>

  <done>DetalheDivida.jsx usa calcularTotalPagoPorDivida; build passa.</done>
</task>

<task type="auto">
  <name>Task 3: Commit e verificação visual rápida</name>
  <files></files>

  <read_first>
    - src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx (linhas 71–78 — confirmar que totalPago usa o mapa correto)
    - src/mr-3/mr-cobrancas/src/utils/devedorCalc.js (últimas 80 linhas — confirmar que a função foi adicionada e as demais estão intactas)
  </read_first>

  <action>
1. Confirme que o build está verde:
```bash
cd src/mr-3/mr-cobrancas && npm run build
```

2. Commit atômico dos dois arquivos modificados no submodule:
```bash
cd src/mr-3/mr-cobrancas
git add src/utils/devedorCalc.js src/components/DetalheDivida.jsx
git commit -m "fix(CR-03): totalPago por dívida — calcularTotalPagoPorDivida Art.354 CC"
```

3. Bump do submodule no repo pai:
```bash
cd ../../..
git add src/mr-3
git commit -m "fix(03-04): bump submodule — CR-03 totalPago por dívida"
```

**NÃO execute `git push`** — commit local apenas, conforme instrução do usuário.

4. Verificação visual rápida (não-bloqueante — apenas confirme no terminal):
```bash
# Verificar que a função existe e está exportada
grep -n "export function calcularTotalPagoPorDivida" src/mr-3/mr-cobrancas/src/utils/devedorCalc.js

# Verificar que DetalheDivida usa o mapa
grep -n "pagoPorDividaMap" src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx

# Verificar que o bug foi removido
grep -n "pagamentosDoDevedor.reduce" src/mr-3/mr-cobrancas/src/components/DetalheDivida.jsx
# Esperado: nenhuma saída (linha removida)
```
  </action>

  <verify>
    <automated>cd src/mr-3/mr-cobrancas && npm run build && grep -c "export function calcularTotalPagoPorDivida" src/utils/devedorCalc.js && grep -c "pagoPorDividaMap" src/components/DetalheDivida.jsx</automated>
  </verify>

  <acceptance_criteria>
    - `npm run build` exits 0
    - `grep -c "export function calcularTotalPagoPorDivida" src/utils/devedorCalc.js` retorna `1`
    - `grep -c "pagoPorDividaMap" src/components/DetalheDivida.jsx` retorna pelo menos `2` (definição + uso)
    - `grep -c "pagamentosDoDevedor.reduce" src/components/DetalheDivida.jsx` retorna `0`
    - Commit no submodule existe com mensagem contendo "CR-03"
    - Commit no repo pai existe com mensagem contendo "03-04"
  </acceptance_criteria>

  <done>Commits realizados; build verde; bug CR-03 eliminado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| calcularTotalPagoPorDivida → pagamentos prop | Dados já carregados via carregarTudo() e filtrados por devedor_id antes de entrar; sem nova superfície de rede |
| DetalheDivida display | Valor computado exibido como texto via fmtBRL() — sem HTML injection path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Tampering | calcularTotalPagoPorDivida — loop mutates pgtoRestantes | accept | `pgtoRestantes` é um array local criado com spread dentro da função; não há efeitos colaterais sobre o array original de pagamentos passado como argumento |
| T-03-04-02 | Information Disclosure | fmtBRL(totalPago) exibido no card | accept | Dado já visível para o usuário autenticado no contexto de DetalheDivida; sem nova exposição |
</threat_model>

<verification>
Após as tasks concluírem:
1. `npm run build` exits 0 (inclui prebuild test:regressao — 9/9 TJGO)
2. `export function calcularTotalPagoPorDivida` presente em devedorCalc.js
3. `pagoPorDividaMap[String(divida.id)]` presente em DetalheDivida.jsx
4. `pagamentosDoDevedor.reduce` AUSENTE de DetalheDivida.jsx
5. Commit "fix(CR-03)" no submodule; commit "fix(03-04)" no repo pai
6. Verificação manual (dev server): abrir DetalheDivida de duas dívidas distintas do mesmo devedor
   (ex: devedor Advair) — "Total Pago" deve mostrar valores diferentes e corretos por dívida,
   não o mesmo R$ 2.100,00 em ambas
</verification>

<success_criteria>
- calcularTotalPagoPorDivida exportada de devedorCalc.js sem modificar funções existentes
- DetalheDivida.jsx linha 77 substituída por 2 linhas usando o mapa por dívida
- Soma dos totalPago por dívida ≤ soma total de pagamentos do devedor (invariante Art.354 CC)
- Para devedor com apenas 1 dívida: totalPago = soma de todos os seus pagamentos (sem mudança)
- test:regressao: 9/9 casos TJGO verdes (calcularSaldoDevedorAtualizado intocado)
- npm run build: 0 erros
</success_criteria>

<output>
After completion, create `.planning/phases/03-nova-divida-com-co-devedores/03-04-SUMMARY.md`
</output>
