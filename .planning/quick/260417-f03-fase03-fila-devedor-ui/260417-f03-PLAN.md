---
phase: quick-260417-f03
version: 01
status: approved
goal: UI completa Fila de Devedor — 5 views em FilaDevedor.jsx + integração App.jsx
---

# Plan: 260417-f03 — Fase 3 Fila de Devedor UI

## TASK-1: Adicionar listarFila() em filaDevedor.js

Adicionar após removerDaFila, antes de atualizarValoresAtrasados:

```js
async function listarFila(filtros = {}) {
  // 1. Build query string
  // 2. Batch fetch fila_cobranca items
  // 3. Batch fetch contratos + devedores by ID sets
  // 4. Enrich and return
}
```

Also export in the filaDevedor object.

## TASK-2: Criar src/components/FilaDevedor.jsx

Estrutura:
1. PriorityBadge({ prioridade }) — ALTA/MEDIA/BAIXA com cores spec
2. FilaPainel({ operadorId, credores }) — Tela 1
3. FilaOperador({ operadorId, onIniciar }) — Tela 2
4. FilaAtendimento({ operadorId, dadosIniciais, onProximo, onSair }) — Tela 3
5. FilaPesquisa({ devedores }) — Tela 4
6. FilaHistorico() — Tela 5
7. FilaDevedor({ user, devedores, credores }) — default export, container

## TASK-3: Editar App.jsx

3a. Adicionar icon fila ao objeto I (linha ~205):
```js
fila: <svg ...list/queue icon...>
```

3b. Inserir na NAV array após regua (linha 8161):
```js
{ id: "fila", label: "Fila de Cobrança", icon: I.fila, color: "#f97316", bg: "rgba(249,115,22,.18)" },
```

3c. Adicionar import após import GerarPeticao (linha 45):
```js
import FilaDevedor from "./components/FilaDevedor.jsx";
```

3d. Adicionar caso no renderPage (após case "regua"):
```js
case "fila": return <FilaDevedor user={user} devedores={devedores} credores={credores} />;
```

## TASK-4: Build check + smoke test

```bash
cd src/mr-3/mr-cobrancas && npm run build
```

## TASK-5: Commit + push + vercel deploy

```bash
git add . && git commit -m "feat: fase 3 - UI completa Fila de Devedor"
git push
vercel --prod
```
