---
phase: quick/260416-gp2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mr-3/mr-cobrancas/src/App.jsx
autonomous: true
requirements:
  - PERSIST-G1  # 13 catches vazios
  - PERSIST-G2  # 3 fallbacks silenciosos
  - PERSIST-G3  # 1 sem try/catch em excluirDevedor

must_haves:
  truths:
    - "Nenhum catch em operações Supabase silencia um erro sem exibir toast.error ao usuário"
    - "Fallbacks locais com Date.now() não são adicionados ao estado quando o Supabase falha"
    - "excluirDevedor não altera estado local nem chama fecharModal se dbDelete falhar"
    - "toast.error segue o padrão: \"Erro ao X: \" + (e?.message || e)"
  artifacts:
    - path: "src/mr-3/mr-cobrancas/src/App.jsx"
      provides: "Arquivo modificado com os 17 pontos corrigidos"
      contains: "toast.error(\"Erro ao"
  key_links:
    - from: "catch (e) { }"
      to: "toast.error(\"Erro ao X: \" + (e?.message || e))"
      via: "substituição direta em cada ponto identificado"
    - from: "excluirDevedor"
      to: "try/catch com return no catch"
      via: "logAudit e setDevedores movidos para depois do await bem-sucedido"
---

<objective>
Corrigir 17 pontos críticos de persistência em App.jsx sem alterar lógica de negócio:

- Grupo 1 (13 catches vazios): adicionar toast.error em cada catch — nenhum dado é
  movido do estado local para dentro do catch (estado já é atualizado no fluxo normal).
- Grupo 2 (3 fallbacks silenciosos): remover o setXxx com Date.now() do catch e
  substituir por toast.error — dado NÃO entra no estado sem confirmação do banco.
- Grupo 3 (1 sem try/catch): envolver dbDelete em excluirDevedor com try/catch;
  mover logAudit, setDevedores e fecharModal para depois do await — retornar no catch.

Purpose: Eliminar divergência silenciosa entre estado local e Supabase; garantir que o
usuário sempre veja um aviso quando uma operação de persistência falha.

Output: App.jsx com 17 correções aplicadas via Edit; nenhuma lógica de negócio alterada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260416-gp2-corrigir-persistencia-supabase-catches-v/260416-gp2-RESEARCH.md
@.planning/quick/260416-gdo-diagnostico-persistencia-supabase-todos-/260416-gdo-REPORT.md

<interfaces>
<!-- Padrão de toast já importado — linha 2 de App.jsx -->
import toast, { Toaster } from 'react-hot-toast';

<!-- Padrão de toast.error a seguir em TODOS os catches deste plano -->
toast.error("Erro ao <operação>: " + (e?.message || e))

<!-- Exemplos de catches funcionais existentes no arquivo (referência de padrão) -->
} catch (e) { toast.error("Erro ao excluir: " + (e?.message || e)); }   // linha 3163
} catch (e) { toast.error("Não foi possível excluir o usuário no Supabase:" + e.message); return; }  // linha 6171
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grupo 1 — Corrigir os 13 catches vazios (e o console.error sem toast)</name>
  <files>src/mr-3/mr-cobrancas/src/App.jsx</files>
  <action>
Aplicar 14 edições no arquivo (13 catches vazios + G1-1 que tem console.error sem toast).
Usar a ferramenta Edit para cada substituição. Para pontos com old_string ambíguo
(G1-4/G1-8 e G1-5/G1-11), incluir 1–2 linhas de contexto no old_string para garantir
unicidade — conforme indicado no RESEARCH.md.

NÃO alterar a lógica de atualização do estado local após os catches. Apenas adicionar
toast.error dentro do catch existente. O estado continua sendo atualizado no fluxo normal
fora do catch.

Substituições (na ordem das linhas para evitar conflito de offsets):

**G1-1 (linha 831) — confirmarPagamento — já tem console.error, adicionar toast:**
old:
    } catch (e) { console.error(e); }
new:
    } catch (e) { console.error(e); toast.error("Erro ao confirmar pagamento: " + (e?.message || e)); }

**G1-2 (linha 838) — excluirAcordo:**
old:
    try { await dbUpdate("devedores", devedor.id, { acordos: JSON.stringify(novos) }); } catch (e) { }
new:
    try { await dbUpdate("devedores", devedor.id, { acordos: JSON.stringify(novos) }); } catch (e) { toast.error("Erro ao excluir acordo: " + (e?.message || e)); }

**G1-3 (linha 1058) — excluirRegistro:**
old:
    try { await dbDelete("registros_contato", id); } catch (e) { }
    setRegistros(r => r.filter(x => x.id !== id));
new:
    try { await dbDelete("registros_contato", id); } catch (e) { toast.error("Erro ao excluir registro: " + (e?.message || e)); }
    setRegistros(r => r.filter(x => x.id !== id));

**G1-4 (linha 1094) — concluirLem (ficha devedor) — usar contexto da função concluirLem:**
old:
  async function concluirLem(id) {
    try { await dbUpdate("lembretes", id, { status: "concluido", concluido_em: new Date().toISOString() }); } catch (e) { }
new:
  async function concluirLem(id) {
    try { await dbUpdate("lembretes", id, { status: "concluido", concluido_em: new Date().toISOString() }); } catch (e) { toast.error("Erro ao concluir lembrete: " + (e?.message || e)); }

**G1-5 (linha 1099) — excluirLem (ficha devedor) — usar contexto da função excluirLem:**
old:
  async function excluirLem(id) {
    if (!await confirm("Excluir lembrete?")) return;
    try { await dbDelete("lembretes", id); } catch (e) { }
new:
  async function excluirLem(id) {
    if (!await confirm("Excluir lembrete?")) return;
    try { await dbDelete("lembretes", id); } catch (e) { toast.error("Erro ao excluir lembrete: " + (e?.message || e)); }

**G1-6 (linha 3414) — atualizar proximo_prazo:**
old:
      try { await dbUpdate("processos", sel.id, { proximo_prazo: andForm.prazo }); } catch (e) { }
new:
      try { await dbUpdate("processos", sel.id, { proximo_prazo: andForm.prazo }); } catch (e) { toast.error("Erro ao atualizar prazo: " + (e?.message || e)); }

**G1-7 (linha 3431) — excluirProcesso:**
old:
    try { await dbDelete("processos", id); } catch (e) { }
new:
    try { await dbDelete("processos", id); } catch (e) { toast.error("Erro ao excluir processo: " + (e?.message || e)); }

**G1-8 (linha 4818) — concluir (módulo lembretes global) — usar contexto da função concluir:**
old:
  async function concluir(id) {
    try { await dbUpdate("lembretes", id, { status: "concluido", concluido_em: new Date().toISOString() }); } catch (e) { }
new:
  async function concluir(id) {
    try { await dbUpdate("lembretes", id, { status: "concluido", concluido_em: new Date().toISOString() }); } catch (e) { toast.error("Erro ao concluir lembrete: " + (e?.message || e)); }

**G1-9 (linha 4822) — cancelar (módulo lembretes global):**
old:
    try { await dbUpdate("lembretes", id, { status: "cancelado" }); } catch (e) { }
new:
    try { await dbUpdate("lembretes", id, { status: "cancelado" }); } catch (e) { toast.error("Erro ao cancelar lembrete: " + (e?.message || e)); }

**G1-10 (linha 4826) — reativar (módulo lembretes global):**
old:
    try { await dbUpdate("lembretes", id, { status: "pendente", concluido_em: null }); } catch (e) { }
new:
    try { await dbUpdate("lembretes", id, { status: "pendente", concluido_em: null }); } catch (e) { toast.error("Erro ao reativar lembrete: " + (e?.message || e)); }

**G1-11 (linha 4831) — excluir (módulo lembretes global) — usar contexto da função excluir:**
old:
  async function excluir(id) {
    if (!await confirm("Excluir este lembrete?")) return;
    try { await dbDelete("lembretes", id); } catch (e) { }
new:
  async function excluir(id) {
    if (!await confirm("Excluir este lembrete?")) return;
    try { await dbDelete("lembretes", id); } catch (e) { toast.error("Erro ao excluir lembrete: " + (e?.message || e)); }

**G1-12 (linha 5487) — catch externo de se(novas) — usar o fechamento da função como âncora:**
old:
    } catch (e) { }
  }

  async function incluirDev(id) {
new:
    } catch (e) { toast.error("Erro ao salvar etapas da régua: " + (e?.message || e)); }
  }

  async function incluirDev(id) {

**G1-13 (linha 5499) — catch de salvarRegua — usar a função seguinte como âncora:**
old:
    } catch (e) { }
  }

  async function incluirDev(id) {

NOTE: G1-12 e G1-13 têm o mesmo old_string com mesma âncora — aplicar G1-12 primeiro
(linha 5487 é antes de 5499). Após aplicar G1-12, a âncora ainda existirá para G1-13
porque são funções diferentes. Se o editor reportar ambiguidade, usar contexto adicional:
para G1-12 usar a linha `for (const id of idsExist)` antes; para G1-13 usar
`async function salvarRegua` como início do old_string.

**G1-14 (linha 5802) — catch inline no onClick da régua — usar comentário seguinte:**
old:
                    } catch (e) { }
                    // ✅ Atualiza estado local IMEDIATAMENTE — sem precisar recarregar
new:
                    } catch (e) { toast.error("Erro ao incluir devedor na régua: " + (e?.message || e)); }
                    // ✅ Atualiza estado local IMEDIATAMENTE — sem precisar recarregar
  </action>
  <verify>
    <automated>grep -c "toast.error" src/mr-3/mr-cobrancas/src/App.jsx</automated>
  </verify>
  <done>
O arquivo App.jsx contém ao menos 14 ocorrências novas de toast.error nas funções
correspondentes. Nenhuma alteração de lógica de negócio: setXxx fora dos catches
permanece intacto. Arquivo compila sem erros de sintaxe JSX.
  </done>
</task>

<task type="auto">
  <name>Task 2: Grupo 2 — Remover 3 fallbacks silenciosos e adicionar toast.error</name>
  <files>src/mr-3/mr-cobrancas/src/App.jsx</files>
  <action>
Aplicar 3 edições para remover o setXxx com Date.now() do catch e substituir por
toast.error. NÃO adicionar dado ao estado sem confirmação do banco.

Para G2-2: o toast.success que existe FORA do try (linha 1090) continua a executar mesmo
em caso de erro porque está fora do bloco try/catch. Mover o toast.success para dentro do
try, logo após o setLemsDevedor do try, para que só execute em caso de sucesso.

**G2-1 (linha 1051) — salvarRegistro — catch com fallback local:**
old:
    } catch (e) {
      setRegistros(r => [{ ...payload, id: Date.now() }, ...r]);
    }
new:
    } catch (e) {
      toast.error("Erro ao salvar registro de contato: " + (e?.message || e));
    }

**G2-2 (linha 1087) — salvarLem (ficha devedor) — catch com fallback local + toast.success fora do try:**

Ler o bloco completo (linhas 1083–1091) antes de editar para confirmar a posição do
toast.success externo. O bloco no RESEARCH.md é:
```
    try {
      const res = await dbInsert("lembretes", payload);
      const novo = Array.isArray(res) ? res[0] : res;
      setLemsDevedor(l => [...(novo?.id ? [novo] : [{ ...payload, id: Date.now() }]), ...l]);
    } catch (e) { setLemsDevedor(l => [{ ...payload, id: Date.now() }, ...l]); }
```
E o toast.success está logo após (linha 1090).

Substituir o bloco inteiro:
old:
    try {
      const res = await dbInsert("lembretes", payload);
      const novo = Array.isArray(res) ? res[0] : res;
      setLemsDevedor(l => [...(novo?.id ? [novo] : [{ ...payload, id: Date.now() }]), ...l]);
    } catch (e) { setLemsDevedor(l => [{ ...payload, id: Date.now() }, ...l]); }
new:
    try {
      const res = await dbInsert("lembretes", payload);
      const novo = Array.isArray(res) ? res[0] : res;
      setLemsDevedor(l => [...(novo?.id ? [novo] : [{ ...payload, id: Date.now() }]), ...l]);
      toast.success("Lembrete criado!");
    } catch (e) { toast.error("Erro ao criar lembrete: " + (e?.message || e)); }

Em seguida, remover o toast.success que ficou na linha seguinte (agora duplicado).
Ler as linhas ao redor após a primeira edição para confirmar a posição exata do
toast.success externo antes de removê-lo.

**G2-3 (linha 4812) — salvarLembrete (módulo global) — catch com fallback local:**
old:
    } catch (e) { setLembretes(l => [{ ...payload, id: Date.now(), criado_em: new Date().toISOString() }, ...l]); }
new:
    } catch (e) { toast.error("Erro ao criar lembrete: " + (e?.message || e)); }
  </action>
  <verify>
    <automated>grep -n "Date.now()" src/mr-3/mr-cobrancas/src/App.jsx</automated>
  </verify>
  <done>
As 3 ocorrências de `id: Date.now()` dentro de blocos catch foram removidas. Nenhum
setXxx com Date.now() permanece em nenhum bloco catch. O toast.success de G2-2 aparece
apenas dentro do bloco try. O arquivo compila sem erros de sintaxe JSX.
  </done>
</task>

<task type="auto">
  <name>Task 3: Grupo 3 — Adicionar try/catch em excluirDevedor com estado pós-sucesso</name>
  <files>src/mr-3/mr-cobrancas/src/App.jsx</files>
  <action>
Envolver dbDelete em excluirDevedor com try/catch. logAudit, setDevedores e fecharModal
só executam após sucesso confirmado — NÃO no catch.

old:
  async function excluirDevedor(d) {
    if (!await confirm(`Excluir "${d.nome}"?`)) return;
    await dbDelete("devedores", d.id);
    logAudit("Excluiu devedor", "devedores", { id: d.id, nome: d.nome, cpf_cnpj: d.cpf_cnpj });
    setDevedores(prev => prev.filter(x => x.id !== d.id));
    fecharModal();
  }
new:
  async function excluirDevedor(d) {
    if (!await confirm(`Excluir "${d.nome}"?`)) return;
    try {
      await dbDelete("devedores", d.id);
    } catch (e) {
      toast.error("Erro ao excluir devedor: " + (e?.message || e));
      return;
    }
    logAudit("Excluiu devedor", "devedores", { id: d.id, nome: d.nome, cpf_cnpj: d.cpf_cnpj });
    setDevedores(prev => prev.filter(x => x.id !== d.id));
    fecharModal();
  }
  </action>
  <verify>
    <automated>grep -A 10 "async function excluirDevedor" src/mr-3/mr-cobrancas/src/App.jsx</automated>
  </verify>
  <done>
A função excluirDevedor contém try/catch ao redor de dbDelete. O catch exibe toast.error
e retorna imediatamente. logAudit, setDevedores e fecharModal aparecem apenas após o
bloco try/catch (fora dele, executam somente em sucesso). O arquivo compila sem erros.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → Supabase REST | Operações de escrita/exclusão passam pelo wrapper sb() que lança em HTTP error |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-gp2-01 | Tampering | catch blocks | mitigate | toast.error expõe detalhes do erro apenas ao usuário autenticado na sessão local — e.message não vaza PII crítico |
| T-gp2-02 | Denial of Service | excluirDevedor sem try/catch | mitigate | Com try/catch + return no catch, falha de rede não remove devedor do estado local (falso positivo eliminado) |
| T-gp2-03 | Information Disclosure | e.message no toast | accept | e.message do wrapper sb() contém mensagem HTTP — aceitável para uso interno; app não tem usuários anônimos |
</threat_model>

<verification>
Após as 3 tasks:

1. `grep -c "} catch (e) { }"` deve retornar 0 (nenhum catch vazio restante nos pontos corrigidos).
2. `grep -n "Date.now()" App.jsx` — confirmar ausência de Date.now() em catches.
3. `grep -A 10 "async function excluirDevedor"` — confirmar estrutura try/catch com return.
4. Abrir o app em dev e simular falha de rede (DevTools > Network > Offline); tentar
   excluir um lembrete — deve aparecer toast de erro.
</verification>

<success_criteria>
- 13 catches vazios agora exibem toast.error com a operação descrita
- G1-1 (console.error) mantém o console.error e adiciona toast.error
- 3 fallbacks silenciosos removidos; catch exibe toast.error sem adicionar ao estado
- toast.success de salvarLem só executa dentro do try (em sucesso)
- excluirDevedor tem try/catch; estado e modal só atualizam após sucesso do dbDelete
- Nenhuma lógica de negócio foi alterada — apenas o bloco catch de cada ponto
- Arquivo App.jsx não apresenta erros de sintaxe JSX
</success_criteria>

<output>
Após conclusão, criar `.planning/quick/260416-gp2-corrigir-persistencia-supabase-catches-v/260416-gp2-SUMMARY.md`
com o resultado de cada task e os pontos corrigidos.
</output>
