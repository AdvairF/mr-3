---
quick_id: 260416-id0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - Criar tabela pagamentos_parciais com RLS (auth.role() = 'authenticated')
  - Criar tabela audit_log com migration existente
  - Criar tabela modelos_peticao com esquema inferido do código

must_haves:
  truths:
    - "Tabela pagamentos_parciais existe com RLS policy usando auth.role() = 'authenticated'"
    - "Tabela audit_log existe com índices e políticas de INSERT/SELECT separadas"
    - "Tabela modelos_peticao existe com RLS policy usando auth.role() = 'authenticated'"
    - "As 3 tabelas retornam HTTP 200 (não 404) na API REST do Supabase"
  artifacts:
    - path: "Supabase: public.pagamentos_parciais"
      provides: "Tabela de pagamentos parciais com FK para devedores"
      contains: "colunas: id, devedor_id, data_pagamento, valor, observacao, created_at"
    - path: "Supabase: public.audit_log"
      provides: "Trilha de auditoria do sistema"
      contains: "colunas: id, usuario_id, usuario_nome, acao, modulo, dados, criado_em"
    - path: "Supabase: public.modelos_peticao"
      provides: "Modelos de petição (.docx em base64)"
      contains: "colunas: id, nome, arquivo, tamanho, criado_em"
  key_links:
    - from: "src/utils/auditLog.js"
      to: "public.audit_log"
      via: "dbInsert('audit_log', payload)"
      pattern: "audit_log"
    - from: "src/components/GerarPeticao.jsx"
      to: "public.modelos_peticao"
      via: "dbGet/dbInsert/dbUpdate/dbDelete('modelos_peticao', ...)"
      pattern: "modelos_peticao"
---

<objective>
Criar as 2 tabelas faltando no Supabase (audit_log e modelos_peticao) e garantir que pagamentos_parciais está com a política RLS correta.

A pesquisa confirmou via sondagem HTTP ao vivo: 10 tabelas OK, 2 faltando (audit_log, modelos_peticao). A tabela pagamentos_parciais já existe mas sua política RLS pode usar `true` (mais permissiva) em vez do padrão `auth.role() = 'authenticated'` — o SQL do usuário deve ser executado para corrigir.

Purpose: O app usa essas 3 tabelas ativamente. Sem elas o sistema falha silenciosamente ao tentar auditar ações, carregar modelos de petição, e registrar pagamentos parciais.
Output: 3 tabelas funcionais no Supabase com RLS habilitado.
</objective>

<execution_context>
DATABASE MIGRATION TASK — sem modificações em arquivos do repositório.
O executor usa MCP Supabase tools para executar SQL diretamente no banco.
Todas as verificações são via MCP (SQL queries ou HTTP probing).
</execution_context>

<context>
@c:/Users/advai/Downloads/mr-cobrancas-vercel_1/mr-cobrancas/.planning/PROJECT.md
@c:/Users/advai/Downloads/mr-cobrancas-vercel_1/mr-cobrancas/.planning/quick/260416-id0-criar-tabela-pagamentos-parciais-no-supa/260416-id0-RESEARCH.md

# Arquivo de migration disponível (usar SQL exato):
# /c/Users/advai/Downloads/mr-cobrancas-vercel_1/mr-cobrancas/src/mr-3/mr-cobrancas/migration_audit_log.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verificar tipo de devedores.id e executar SQL de pagamentos_parciais</name>
  <files>Supabase: public.pagamentos_parciais (DDL + RLS)</files>
  <action>
Passo 1 — Verificar o tipo da coluna devedores.id via MCP SQL:
```sql
SELECT column_name, data_type, udt_name
FROM information_schema.columns
WHERE table_schema = 'public'
  AND table_name = 'devedores'
  AND column_name = 'id';
```

Passo 2 — Com base no resultado:

CASO A: devedores.id for BIGINT ou BIGSERIAL → executar versão adaptada:
```sql
CREATE TABLE IF NOT EXISTS public.pagamentos_parciais (
  id             BIGSERIAL PRIMARY KEY,
  devedor_id     BIGINT NOT NULL REFERENCES public.devedores(id) ON DELETE CASCADE,
  data_pagamento DATE NOT NULL,
  valor          NUMERIC(15,2) NOT NULL,
  observacao     TEXT,
  created_at     TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

ALTER TABLE public.pagamentos_parciais ENABLE ROW LEVEL SECURITY;

-- Remover política antiga se existir (pode ter nome diferente)
DROP POLICY IF EXISTS "allow_all" ON public.pagamentos_parciais;
DROP POLICY IF EXISTS "Acesso autenticado" ON public.pagamentos_parciais;

CREATE POLICY "Acesso autenticado" ON public.pagamentos_parciais
  FOR ALL USING (auth.role() = 'authenticated');

SELECT pg_notify('pgrst', 'reload schema');
```

CASO B: devedores.id for UUID → executar o SQL exato fornecido pelo usuário (per D-01):
```sql
CREATE TABLE IF NOT EXISTS public.pagamentos_parciais (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  devedor_id UUID NOT NULL REFERENCES public.devedores(id) ON DELETE CASCADE,
  data_pagamento DATE NOT NULL,
  valor NUMERIC(15,2) NOT NULL,
  observacao TEXT,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

ALTER TABLE public.pagamentos_parciais ENABLE ROW LEVEL SECURITY;

DROP POLICY IF EXISTS "allow_all" ON public.pagamentos_parciais;
DROP POLICY IF EXISTS "Acesso autenticado" ON public.pagamentos_parciais;

CREATE POLICY "Acesso autenticado" ON public.pagamentos_parciais
  FOR ALL USING (auth.role() = 'authenticated');

SELECT pg_notify('pgrst', 'reload schema');
```

Passo 3 — Verificar resultado:
```sql
SELECT column_name, data_type
FROM information_schema.columns
WHERE table_schema = 'public' AND table_name = 'pagamentos_parciais'
ORDER BY ordinal_position;

SELECT policyname, cmd, qual
FROM pg_policies
WHERE tablename = 'pagamentos_parciais';
```
  </action>
  <verify>
Query de colunas retorna: id, devedor_id, data_pagamento, valor, observacao, created_at.
Query de policies retorna policy "Acesso autenticado" com qual = `(auth.role() = 'authenticated')`.
  </verify>
  <done>Tabela pagamentos_parciais existe com as 6 colunas corretas e política RLS usando auth.role() = 'authenticated'. Tipo de devedor_id compatível com devedores.id.</done>
</task>

<task type="auto">
  <name>Task 2: Criar tabela audit_log (migration existente)</name>
  <files>Supabase: public.audit_log (DDL + índices + RLS)</files>
  <action>
Executar o conteúdo exato da migration `migration_audit_log.sql` via MCP SQL:

```sql
-- ─── AUDIT LOG — Trilha de Auditoria ─────────────────────────

-- 1. Criar tabela
CREATE TABLE IF NOT EXISTS audit_log (
  id          BIGSERIAL PRIMARY KEY,
  usuario_id  TEXT        NOT NULL,
  usuario_nome TEXT       NOT NULL,
  acao        TEXT        NOT NULL,
  modulo      TEXT        NOT NULL,
  dados       JSONB,
  criado_em   TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- 2. Índices para consultas frequentes
CREATE INDEX IF NOT EXISTS idx_audit_log_usuario  ON audit_log (usuario_id);
CREATE INDEX IF NOT EXISTS idx_audit_log_modulo   ON audit_log (modulo);
CREATE INDEX IF NOT EXISTS idx_audit_log_criado   ON audit_log (criado_em DESC);

-- 3. Habilitar Row Level Security
ALTER TABLE audit_log ENABLE ROW LEVEL SECURITY;

-- Política de INSERT: usuários autenticados podem inserir registros
CREATE POLICY "audit_insert" ON audit_log
  FOR INSERT
  TO authenticated
  WITH CHECK (true);

-- Política de SELECT: somente admins
CREATE POLICY "audit_select_admin" ON audit_log
  FOR SELECT
  TO authenticated
  USING (
    EXISTS (
      SELECT 1 FROM usuarios_sistema
      WHERE email = auth.jwt() ->> 'email'
        AND role = 'admin'
    )
  );

-- Permite leitura via service_role / anon key
CREATE POLICY "audit_select_anon" ON audit_log
  FOR SELECT
  TO anon
  USING (true);

-- 4. Comentários
COMMENT ON TABLE  audit_log IS 'Trilha de auditoria: registra todas as ações relevantes de usuários logados.';
COMMENT ON COLUMN audit_log.usuario_id   IS 'ID do usuário que realizou a ação';
COMMENT ON COLUMN audit_log.usuario_nome IS 'Nome do usuário no momento da ação';
COMMENT ON COLUMN audit_log.acao         IS 'Descrição da ação realizada (ex: "Criou devedor")';
COMMENT ON COLUMN audit_log.modulo       IS 'Módulo do sistema afetado (ex: devedores, processos, calculadora)';
COMMENT ON COLUMN audit_log.dados        IS 'Dados relevantes da ação em JSON';
COMMENT ON COLUMN audit_log.criado_em    IS 'Timestamp UTC da ação';

SELECT pg_notify('pgrst', 'reload schema');
```

Verificar após execução:
```sql
SELECT column_name, data_type
FROM information_schema.columns
WHERE table_schema = 'public' AND table_name = 'audit_log'
ORDER BY ordinal_position;

SELECT policyname, cmd
FROM pg_policies
WHERE tablename = 'audit_log';

SELECT indexname FROM pg_indexes
WHERE tablename = 'audit_log';
```
  </action>
  <verify>
Query de colunas retorna: id, usuario_id, usuario_nome, acao, modulo, dados, criado_em.
Query de policies retorna 3 políticas: audit_insert, audit_select_admin, audit_select_anon.
Query de índices retorna: idx_audit_log_usuario, idx_audit_log_modulo, idx_audit_log_criado.
  </verify>
  <done>Tabela audit_log criada com 7 colunas, 3 índices e 3 políticas RLS separadas (INSERT para authenticated, SELECT para admin, SELECT para anon). Registros de auditoria do app passarão a persistir no Supabase.</done>
</task>

<task type="auto">
  <name>Task 3: Criar tabela modelos_peticao (esquema inferido do código)</name>
  <files>Supabase: public.modelos_peticao (DDL + RLS)</files>
  <action>
Executar SQL de criação da tabela modelos_peticao (esquema inferido de GerarPeticao.jsx — payload de insert usa: nome, arquivo, tamanho; IDB local adiciona id e criado_em):

```sql
CREATE TABLE IF NOT EXISTS public.modelos_peticao (
  id        BIGSERIAL PRIMARY KEY,
  nome      TEXT NOT NULL,
  arquivo   TEXT NOT NULL,
  tamanho   INTEGER,
  criado_em TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

ALTER TABLE public.modelos_peticao ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Acesso autenticado" ON public.modelos_peticao
  FOR ALL USING (auth.role() = 'authenticated');

SELECT pg_notify('pgrst', 'reload schema');
```

Verificar após execução:
```sql
SELECT column_name, data_type
FROM information_schema.columns
WHERE table_schema = 'public' AND table_name = 'modelos_peticao'
ORDER BY ordinal_position;

SELECT policyname, cmd, qual
FROM pg_policies
WHERE tablename = 'modelos_peticao';
```
  </action>
  <verify>
Query de colunas retorna: id, nome, arquivo, tamanho, criado_em.
Query de policies retorna policy "Acesso autenticado" com cmd = ALL.
  </verify>
  <done>Tabela modelos_peticao criada com 5 colunas e RLS habilitado com política para authenticated. GerarPeticao.jsx poderá salvar e carregar modelos do Supabase (em vez de apenas do IndexedDB local).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated user → Supabase tables | Usuários autenticados podem ler/escrever nas tabelas com RLS ALL |
| audit_log INSERT | Qualquer usuário autenticado pode inserir — sem validação de usuario_id correto |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-id0-01 | Tampering | audit_log INSERT policy | accept | Usuário pode inserir registros com usuario_id falso — aceitável pois o app controla o payload e audit log não é dado sensível |
| T-id0-02 | Information Disclosure | modelos_peticao (arquivos base64) | mitigate | RLS com auth.role() = 'authenticated' garante que apenas usuários logados acessem modelos |
| T-id0-03 | Information Disclosure | audit_log SELECT | mitigate | Política audit_select_admin restringe SELECT a admins; anon policy usada apenas para service_role interno |
| T-id0-04 | Elevation of Privilege | pagamentos_parciais RLS | mitigate | Policy corrigida de `true` para `auth.role() = 'authenticated'` — elimina acesso anon inadvertido |
</threat_model>

<verification>
Após execução de todas as tasks, confirmar via MCP SQL:

```sql
SELECT table_name
FROM information_schema.tables
WHERE table_schema = 'public'
  AND table_name IN ('pagamentos_parciais', 'audit_log', 'modelos_peticao')
ORDER BY table_name;
```

Deve retornar 3 linhas. Se alguma estiver faltando, re-executar a task correspondente.

Verificar políticas de todas as tabelas de uma vez:
```sql
SELECT tablename, policyname, cmd
FROM pg_policies
WHERE tablename IN ('pagamentos_parciais', 'audit_log', 'modelos_peticao')
ORDER BY tablename, policyname;
```
</verification>

<success_criteria>
- `pagamentos_parciais` existe com tipo de devedor_id compatível com devedores.id e policy `auth.role() = 'authenticated'`
- `audit_log` existe com 3 políticas RLS separadas (insert/select-admin/select-anon) e 3 índices
- `modelos_peticao` existe com 5 colunas e policy `auth.role() = 'authenticated'`
- Nenhuma das 3 tabelas retorna HTTP 404 na API REST do Supabase
</success_criteria>

<output>
Após conclusão, criar `.planning/quick/260416-id0-criar-tabela-pagamentos-parciais-no-supa/260416-id0-SUMMARY.md` com:
- Tabelas criadas/verificadas
- Tipos confirmados (especialmente devedores.id)
- Políticas RLS aplicadas
- Qualquer desvio do plano encontrado durante execução
</output>
