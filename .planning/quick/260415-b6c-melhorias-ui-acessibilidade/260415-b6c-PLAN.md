---
phase: 260415-b6c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mr-3/mr-cobrancas/package.json
  - src/mr-3/mr-cobrancas/package-lock.json
  - src/mr-3/mr-cobrancas/src/App.jsx
autonomous: true
requirements: [UI-TOAST, UI-CONFIRM, A11Y-ARIA]

must_haves:
  truths:
    - "Nenhuma chamada alert() ou window.confirm() existe no App.jsx"
    - "Feedback visual aparece como toast notification (canto superior direito)"
    - "Ações destrutivas pedem confirmação via modal customizado antes de executar"
    - "Todos os botões icon-only possuem aria-label descritivo"
    - "O app compila e roda sem erros (npm run dev)"
  artifacts:
    - path: "src/mr-3/mr-cobrancas/src/App.jsx"
      provides: "App com toast, useConfirm, aria-labels"
      contains: "import toast"
    - path: "src/mr-3/mr-cobrancas/package.json"
      provides: "react-hot-toast dependency"
      contains: "react-hot-toast"
  key_links:
    - from: "App.jsx"
      to: "react-hot-toast"
      via: "import toast, { Toaster }"
      pattern: "import toast.*from.*react-hot-toast"
    - from: "App.jsx useConfirm hook"
      to: "ConfirmModal JSX"
      via: "useState + useRef + Promise resolver"
      pattern: "function useConfirm"
---

<objective>
Substituir todos os 86 alert() e 14 window.confirm() nativos por react-hot-toast + useConfirm hook customizado, e adicionar aria-labels em todos os botoes icon-only no App.jsx.

Purpose: Eliminar dialogs nativos bloqueantes do navegador, melhorar UX com feedback visual moderno e tornar o app acessivel para screen readers.
Output: App.jsx atualizado sem nenhum alert()/confirm() nativo, com toasts e modal de confirmacao customizado, e botoes icon-only com aria-labels.
</objective>

<execution_context>
@.planning/quick/260415-b6c-melhorias-ui-acessibilidade/260415-b6c-CONTEXT.md
@.planning/quick/260415-b6c-melhorias-ui-acessibilidade/260415-b6c-RESEARCH.md
</execution_context>

<context>
@.planning/PROJECT.md
@src/mr-3/mr-cobrancas/src/App.jsx

<interfaces>
<!-- Modal.jsx usa zIndex: 1000 (linha 7). ConfirmModal com zIndex: 9999 e seguro. -->

From src/mr-3/mr-cobrancas/src/App.jsx (imports, linha 1):
```jsx
import React, { useState, useEffect, useCallback } from "react";
```

From src/mr-3/mr-cobrancas/src/components/ui/Modal.jsx (linha 7):
```jsx
zIndex: 1000
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar react-hot-toast, implementar useConfirm hook, adicionar Toaster</name>
  <files>src/mr-3/mr-cobrancas/package.json, src/mr-3/mr-cobrancas/src/App.jsx</files>
  <action>
1. Instalar react-hot-toast:
   ```bash
   cd src/mr-3/mr-cobrancas && npm install react-hot-toast
   ```

2. No App.jsx, adicionar imports (linha 1 — junto aos imports React existentes):
   - Adicionar `useRef` ao import de React: `import React, { useState, useEffect, useCallback, useRef } from "react";`
   - Adicionar import do toast: `import toast, { Toaster } from 'react-hot-toast';`

3. Implementar o hook `useConfirm` ANTES do primeiro componente funcional no App.jsx (depois dos imports e constantes, antes de qualquer `function` component). Copiar exatamente o padrao do RESEARCH.md secao 2:

```jsx
function useConfirm() {
  const [state, setState] = useState({ open: false, message: '' });
  const resolverRef = useRef(null);
  const confirm = useCallback((message) => {
    return new Promise((resolve) => {
      resolverRef.current = resolve;
      setState({ open: true, message });
    });
  }, []);
  function handleConfirm() {
    resolverRef.current?.(true);
    setState({ open: false, message: '' });
  }
  function handleCancel() {
    resolverRef.current?.(false);
    setState({ open: false, message: '' });
  }
  const ConfirmModal = state.open ? (
    <div style={{
      position: 'fixed', inset: 0, zIndex: 9999,
      background: 'rgba(0,0,0,0.45)',
      display: 'flex', alignItems: 'center', justifyContent: 'center'
    }}>
      <div style={{
        background: '#fff', borderRadius: 14, padding: '28px 32px',
        maxWidth: 420, width: '90%', boxShadow: '0 20px 60px rgba(0,0,0,0.2)'
      }}>
        <p style={{ margin: '0 0 24px', fontSize: 15, color: '#1e293b', lineHeight: 1.5 }}>
          {state.message}
        </p>
        <div style={{ display: 'flex', gap: 10, justifyContent: 'flex-end' }}>
          <button aria-label="Cancelar" onClick={handleCancel}
            style={{ padding: '8px 18px', borderRadius: 8, border: '1.5px solid #e2e8f0',
                     background: '#f8fafc', color: '#64748b', cursor: 'pointer', fontWeight: 600 }}>
            Cancelar
          </button>
          <button aria-label="Confirmar" onClick={handleConfirm}
            style={{ padding: '8px 18px', borderRadius: 8, border: 'none',
                     background: '#dc2626', color: '#fff', cursor: 'pointer', fontWeight: 600 }}>
            Confirmar
          </button>
        </div>
      </div>
    </div>
  ) : null;
  return { confirm, ConfirmModal };
}
```

4. Adicionar `<Toaster />` no JSX do componente raiz App — logo no inicio do return(), FORA de qualquer condicional:
```jsx
<Toaster
  position="top-right"
  toastOptions={{
    success: { duration: 2000 },
    error: { duration: 4000 },
  }}
/>
```

5. Em CADA componente funcional interno que usa `window.confirm()`, adicionar `const { confirm, ConfirmModal } = useConfirm();` e renderizar `{ConfirmModal}` no JSX retornado desse componente. Identificar quais componentes internos usam confirm para saber onde instanciar o hook.

IMPORTANTE: Nao criar arquivo separado para useConfirm — implementar direto no App.jsx conforme decisao do usuario.
  </action>
  <verify>
    <automated>cd src/mr-3/mr-cobrancas && node -e "const pkg = require('./package.json'); if (!pkg.dependencies['react-hot-toast']) { process.exit(1); } console.log('react-hot-toast installed:', pkg.dependencies['react-hot-toast']);" && grep -c "import toast" src/App.jsx && grep -c "function useConfirm" src/App.jsx && grep -c "Toaster" src/App.jsx</automated>
  </verify>
  <done>react-hot-toast instalado, hook useConfirm definido no App.jsx, Toaster renderizado no componente raiz</done>
</task>

<task type="auto">
  <name>Task 2: Substituir todos os alert() por toast e todos os window.confirm() por useConfirm</name>
  <files>src/mr-3/mr-cobrancas/src/App.jsx</files>
  <action>
## Substituicao de alert() — ~86 ocorrencias

Aplicar as seguintes regras de mapeamento em TODAS as ocorrencias:

### Padrao 1: Success alerts (contem checkmark ou "cadastrado/salvo/atualizado/criado/excluido")
Linhas: 727, 1025, 1977, 2050, 2056, 2086, 2155, 2193, 2291, 3294, 3327, 6079, 6089
```jsx
// ANTES: alert("✅ Devedor cadastrado!")
// DEPOIS: toast.success("Devedor cadastrado!")
```
Remover o emoji checkmark da mensagem — o toast.success ja tem icone verde.

### Padrao 2: Error alerts (contem "Erro"/"Nao foi possivel")
Linhas: 730, 1979, 1982, 2059, 2088, 2104, 2157, 2163, 2195, 2200, 2294, 3098, 3105, 3296, 3299, 4233, 6059, 6082, 6098
```jsx
// ANTES: alert("Erro ao salvar: " + e.message)
// DEPOIS: toast.error("Erro ao salvar: " + e.message)
```

### Padrao 3: Validation guards com return (contem "Informe"/"Preencha"/"invalido")
Linhas: 566, 567, 577, 973, 1010, 1011, 1364, 1800, 1897, 1899, 1904, 1906, 1911, 1915, 1931, 1937, 2066, 2109, 2132, 2133, 2169, 2171, 2260, 2262, 3110, 3114, 3127, 3272, 3336, 3879, 4728, 4729, 4730, 5082, 5459, 6066, 6067, 6069
```jsx
// ANTES: return alert("Informe o nome.");
// DEPOIS: return toast("Informe o nome.", { icon: "⚠️" });

// ANTES: if (!form.nome.trim()) return alert("Informe o nome.");
// DEPOIS: if (!form.nome.trim()) return toast("Informe o nome.", { icon: "⚠️" });
```
O `return toast(...)` funciona como guard — toast() retorna string ID (truthy), irrelevante pois o return ja interrompe a funcao.

### Padrao 4: Info/warning alerts (nao se encaixam nos anteriores)
Linhas: 2289 (aviso de sincronizacao)
```jsx
// ANTES: alert("⚠️ Divida salva localmente...")
// DEPOIS: toast("Divida salva localmente...", { icon: "⚠️", duration: 5000 })
```

### Padrao 5: Multi-line alerts (com template literals e \n)
Linhas: 2050, 6079, 6089
```jsx
// ANTES: alert(`✅ Usuario "${form.nome}" cadastrado!\nEle ja pode fazer login...`)
// DEPOIS: toast.success(`Usuario "${form.nome}" cadastrado! Ele ja pode fazer login...`, { duration: 4000 })
```
Remover \n (toasts nao suportam bem quebra de linha). Se mensagem for muito longa, encurtar.

### ATENCAO ESPECIAL — Linha 1899 (busca CEP async):
```jsx
// ANTES: try { ... if (d.erro) return alert("CEP não encontrado."); ... } catch (e) { alert("Erro ao buscar CEP."); }
// DEPOIS: try { ... if (d.erro) return toast("CEP não encontrado.", { icon: "⚠️" }); ... } catch (e) { toast.error("Erro ao buscar CEP."); }
```

## Substituicao de window.confirm() — 14 ocorrencias

### Grupo A: confirm em funcoes nomeadas (ja async ou facilmente convertidas)
Estas funcoes ja estao definidas fora do JSX. Basta:
1. Tornar a funcao `async` (se nao for)
2. Adicionar `const { confirm, ConfirmModal } = useConfirm();` no componente que contem a funcao
3. Substituir `if (!window.confirm(...)) return;` por `if (!await confirm(...)) return;`
4. Renderizar `{ConfirmModal}` no JSX do componente

Linhas e funcoes:
- **772**: excluir acordo — `if (!window.confirm("Excluir este acordo e todas as parcelas?")) return;`
- **992**: excluir registro contato — `if (!window.confirm("Excluir este registro?")) return;`
- **1033**: excluir lembrete (inline devedor) — `if (!window.confirm("Excluir lembrete?")) return;`
- **2222**: excluir divida — `if (!sel || !window.confirm("Excluir esta dívida?")) return;`
- **2317**: excluir devedor — `if (!window.confirm(\`Excluir "${d.nome}"?\`)) return;`
- **3093**: excluir credor — `if (!window.confirm(\`Excluir o credor "${c.nome}"?...\`)) return;`
- **3362**: excluir processo — `if (!window.confirm("Excluir este processo?")) return;`
- **4760**: excluir lembrete (secao lembretes) — `if (!window.confirm("Excluir este lembrete?")) return;`
- **5437**: remover devedor da regua — `if (!window.confirm("Remover este devedor da régua?")) return;`
- **6096**: excluir usuario — `if (!window.confirm("Excluir este usuário?...")) return;`

### Grupo B: confirm INLINE em onClick handlers (precisam extrair para funcao async)
Estas estao dentro de `onClick={() => { if (!window.confirm(...)) return; ... }}` e precisam ser extraidas.

- **5722**: Mover devedor para etapa da regua
  ```jsx
  // ANTES (inline):
  onClick={() => { if (!window.confirm(`Mover "${dev.nome}" para...`)) return; /* logica */ }}
  // DEPOIS: Extrair para funcao async:
  async function handleMoverEtapa(dev, et) {
    if (!await confirm(`Mover "${dev.nome}" para a etapa "${et.titulo}" (dia ${et.dias})?`)) return;
    // ... resto da logica do onClick original
  }
  // No JSX: onClick={() => handleMoverEtapa(dev, et)}
  ```

- **5848**: Excluir etapa da regua
  ```jsx
  // ANTES (inline):
  onClick={() => { if (!window.confirm("Excluir?")) return; se(etapas.filter(x => x.id !== e.id)); }}
  // DEPOIS:
  async function handleExcluirEtapa(etapaId) {
    if (!await confirm("Excluir esta etapa?")) return;
    se(etapas.filter(x => x.id !== etapaId));
  }
  // No JSX: onClick={() => handleExcluirEtapa(e.id)}
  ```

- **6511 e 6876**: Logout (duas ocorrencias identicas — sidebar desktop e mobile)
  ```jsx
  // ANTES (inline):
  onClick={() => { if (!window.confirm("Deseja sair do sistema?")) return; logAudit(...); ... }}
  // DEPOIS: Extrair para funcao async definida uma vez:
  async function handleLogout() {
    if (!await confirm("Deseja sair do sistema?")) return;
    logAudit("Logout do sistema", "auth", {});
    setAuditUser(null);
    signOut();
    setUser(null);
    try { sessionStorage.removeItem("mr_user"); } catch {}
  }
  // Nos dois botoes: onClick={handleLogout}
  ```

### VALIDACAO FINAL: Apos todas as substituicoes, executar:
```bash
grep -n "window\.confirm\|[^.]alert(" src/App.jsx
```
O resultado deve ser ZERO linhas (exceto a linha 95 que e um SVG icon chamado "alert" — nao e uma chamada de funcao).
  </action>
  <verify>
    <automated>cd src/mr-3/mr-cobrancas && grep -c "window\.confirm" src/App.jsx | xargs -I{} test {} -eq 0 && echo "OK: 0 window.confirm" && grep -cP "(?<!\w)alert\(" src/App.jsx | xargs -I{} bash -c 'if [ {} -le 1 ]; then echo "OK: alert count <= 1 (SVG icon only)"; else echo "FAIL: {} alert() calls remain"; exit 1; fi'</automated>
  </verify>
  <done>Zero chamadas alert() (exceto SVG icon name) e zero chamadas window.confirm() no App.jsx. Todos os feedbacks usam toast e todas as confirmacoes usam useConfirm modal.</done>
</task>

<task type="auto">
  <name>Task 3: Adicionar aria-labels em todos os botoes icon-only</name>
  <files>src/mr-3/mr-cobrancas/src/App.jsx</files>
  <action>
Adicionar `aria-label` descritivo em todos os botoes que contem APENAS emoji ou simbolo como conteudo visivel (sem texto acompanhante). Botoes que ja tem texto visivel (ex: "🗑 Excluir", "📱 WhatsApp", "Sair") NAO precisam de aria-label.

### Lista de botoes icon-only a corrigir:

| Linha | Conteudo atual | aria-label a adicionar | Contexto |
|-------|---------------|----------------------|----------|
| 596 | `✕` | `"Fechar"` | Fechar modal de acordo |
| 1148 | `🗑` | `"Excluir registro de contato"` | Lista de registros de contato |
| 1194 | `✕` | `"Fechar formulario"` | Fechar form de registro |
| 1278 | `✅` | `"Concluir lembrete"` | Concluir lembrete inline |
| 1279 | `🗑` | `"Excluir lembrete"` | Excluir lembrete inline |
| 1826 | `✕` | `"Remover custa"` | Remover item de custas |
| 2472 | `🔍` (dentro de buscandoCEPEdit ternary) | `"Buscar CEP"` | Botao buscar CEP em edicao |
| 2585 | `✏️ Editar` | NAO precisa (tem texto) | — |
| 2586 | `🗑️ Excluir` | NAO precisa (tem texto) | — |
| 2643 | `✓` | `"Marcar como pago"` | Toggle parcela pago |
| 2643 | `↩` | `"Marcar como pendente"` | Toggle parcela pendente |
| 2727 | `✕` | `"Remover parcela"` | Remover parcela da edicao |
| 2913 | `✕ Limpar filtros` | NAO precisa (tem texto) | — |
| 3163 | `🗑️` | `"Excluir credor"` | Card do credor |
| 3704 | `✕ Limpar` | NAO precisa (tem texto) | — |
| 5848 | botao excluir etapa | `"Excluir etapa"` | Regua de cobranca |
| 5953 | `✕ Remover` | NAO precisa (tem texto) | — |

### Como aplicar:
Adicionar o atributo `aria-label="..."` no elemento `<button` ANTES do `onClick`. Exemplo:
```jsx
// ANTES:
<button onClick={() => excluirRegistro(r.id)} style={...}>🗑</button>
// DEPOIS:
<button aria-label="Excluir registro de contato" onClick={() => excluirRegistro(r.id)} style={...}>🗑</button>
```

### Botoes de busca CEP/CNPJ com ternary (linhas 2472, 2941, 2962, 3184):
Esses botoes tem conteudo condicional (`buscandoCEP ? "⏳" : "🔍"` ou `"🔍 Buscar"`). Se o conteudo for apenas emoji sem texto, adicionar aria-label:
```jsx
// Linha 2472 (somente emoji):
<button aria-label="Buscar CEP" onClick={buscarCEPEdit} ...>{buscandoCEPEdit ? "⏳" : "🔍"}</button>
```
Linhas 2941, 2962, 3184 tem texto "🔍 Buscar" — NAO precisam de aria-label.

### Botoes de logout (linhas 6510-6517, 6875-6879):
Ja tem texto "Sair" visivel — NAO precisam de aria-label.
  </action>
  <verify>
    <automated>cd src/mr-3/mr-cobrancas && grep -c 'aria-label' src/App.jsx | xargs -I{} bash -c 'if [ {} -ge 12 ]; then echo "OK: {} aria-labels found (expected 12+)"; else echo "FAIL: only {} aria-labels"; exit 1; fi' && npx vite build --mode development 2>&1 | tail -5</automated>
  </verify>
  <done>Todos os botoes icon-only tem aria-label descritivo. Minimo 12 aria-labels adicionados. App compila sem erros.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Tarefa puramente de UI/UX — sem novas superficies de ataque |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-b6c-01 | T (Tampering) | toast messages | accept | Toasts sao read-only, exibem mensagens ja existentes no codigo. Sem input do usuario nas mensagens de toast. |
</threat_model>

<verification>
1. `cd src/mr-3/mr-cobrancas && grep -cP "(?<!\w)alert\(" src/App.jsx` retorna 0 ou 1 (SVG icon name)
2. `cd src/mr-3/mr-cobrancas && grep -c "window\.confirm" src/App.jsx` retorna 0
3. `cd src/mr-3/mr-cobrancas && grep -c "aria-label" src/App.jsx` retorna >= 12
4. `cd src/mr-3/mr-cobrancas && grep -c "react-hot-toast" package.json` retorna >= 1
5. `cd src/mr-3/mr-cobrancas && grep -c "useConfirm" src/App.jsx` retorna >= 2 (definicao + uso)
6. `cd src/mr-3/mr-cobrancas && npx vite build --mode development` completa sem erros
</verification>

<success_criteria>
- Zero alert() nativos no App.jsx (exceto SVG icon name na linha 95)
- Zero window.confirm() no App.jsx
- react-hot-toast instalado e Toaster renderizado
- useConfirm hook implementado e usado em todos os pontos de confirmacao
- Minimo 12 aria-labels adicionados em botoes icon-only
- App compila e roda sem erros
</success_criteria>

<output>
After completion, create `.planning/quick/260415-b6c-melhorias-ui-acessibilidade/260415-b6c-SUMMARY.md`
</output>
